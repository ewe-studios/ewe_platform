//! Auto-generated API module for gcp/datastream datastream.
//!
//! Generated by `cargo run --bin ewe_platform gen_api`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp_datastream_datastream `

#![cfg(feature = "gcp_datastream_datastream")]
#![allow(clippy::too_many_arguments, clippy::type_complexity)]
#![allow(
    clippy::missing_errors_doc,
    clippy::doc_markdown,
    clippy::useless_format
)]
#![allow(unused_imports)]

use foundation_core::valtron::{TaskIterator, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{ClientRequestBuilder, SimpleHttpClient};
use foundation_macros::JsonHash;
use serde::{Deserialize, Serialize};

// Import shared types used by this module
use super::shared::Empty;
use super::shared::Operation;

use super::shared::ApiResponse;

// =============================================================================
// TYPE DECLARATIONS
// =============================================================================

/// `MysqlSourceConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct MysqlSourceConfig {
    /// binaryLogPosition property.
    pub binary_log_position: Option<BinaryLogPosition>,
    /// excludeObjects property.
    pub exclude_objects: Option<MysqlRdbms>,
    /// gtid property.
    pub gtid: Option<Gtid>,
    /// includeObjects property.
    pub include_objects: Option<MysqlRdbms>,
    /// maxConcurrentBackfillTasks property.
    pub max_concurrent_backfill_tasks: Option<i64>,
    /// maxConcurrentCdcTasks property.
    pub max_concurrent_cdc_tasks: Option<i64>,
}

/// `BigQueryDestinationConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct BigQueryDestinationConfig {
    /// appendOnly property.
    pub append_only: Option<AppendOnly>,
    /// blmtConfig property.
    pub blmt_config: Option<BlmtConfig>,
    /// dataFreshness property.
    pub data_freshness: Option<String>,
    /// merge property.
    pub merge: Option<Merge>,
    /// singleTargetDataset property.
    pub single_target_dataset: Option<SingleTargetDataset>,
    /// sourceHierarchyDatasets property.
    pub source_hierarchy_datasets: Option<SourceHierarchyDatasets>,
}

/// `SpecificStartPosition` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SpecificStartPosition {
    /// mongodbChangeStreamPosition property.
    pub mongodb_change_stream_position: Option<MongodbChangeStreamPosition>,
    /// mysqlGtidPosition property.
    pub mysql_gtid_position: Option<MysqlGtidPosition>,
    /// mysqlLogPosition property.
    pub mysql_log_position: Option<MysqlLogPosition>,
    /// oracleScnPosition property.
    pub oracle_scn_position: Option<OracleScnPosition>,
    /// sqlServerLsnPosition property.
    pub sql_server_lsn_position: Option<SqlServerLsnPosition>,
}

/// `ConnectionProfile` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ConnectionProfile {
    /// bigqueryProfile property.
    pub bigquery_profile: Option<BigQueryProfile>,
    /// createTime property.
    pub create_time: Option<String>,
    /// displayName property.
    pub display_name: Option<String>,
    /// forwardSshConnectivity property.
    pub forward_ssh_connectivity: Option<ForwardSshTunnelConnectivity>,
    /// gcsProfile property.
    pub gcs_profile: Option<GcsProfile>,
    /// labels property.
    pub labels: Option<serde_json::Value>,
    /// mongodbProfile property.
    pub mongodb_profile: Option<MongodbProfile>,
    /// mysqlProfile property.
    pub mysql_profile: Option<MysqlProfile>,
    /// name property.
    pub name: Option<String>,
    /// oracleProfile property.
    pub oracle_profile: Option<OracleProfile>,
    /// postgresqlProfile property.
    pub postgresql_profile: Option<PostgresqlProfile>,
    /// privateConnectivity property.
    pub private_connectivity: Option<PrivateConnectivity>,
    /// salesforceProfile property.
    pub salesforce_profile: Option<SalesforceProfile>,
    /// satisfiesPzi property.
    pub satisfies_pzi: Option<bool>,
    /// satisfiesPzs property.
    pub satisfies_pzs: Option<bool>,
    /// spannerProfile property.
    pub spanner_profile: Option<SpannerProfile>,
    /// sqlServerProfile property.
    pub sql_server_profile: Option<SqlServerProfile>,
    /// staticServiceIpConnectivity property.
    pub static_service_ip_connectivity: Option<StaticServiceIpConnectivity>,
    /// updateTime property.
    pub update_time: Option<String>,
}

/// `CustomizationRule` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct CustomizationRule {
    /// bigqueryClustering property.
    pub bigquery_clustering: Option<BigQueryClustering>,
    /// bigqueryPartitioning property.
    pub bigquery_partitioning: Option<BigQueryPartitioning>,
}

/// `PostgresqlObjectIdentifier` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct PostgresqlObjectIdentifier {
    /// schema property.
    pub schema: Option<String>,
    /// table property.
    pub table: Option<String>,
}

/// `BackfillAllStrategy` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct BackfillAllStrategy {
    /// mongodbExcludedObjects property.
    pub mongodb_excluded_objects: Option<MongodbCluster>,
    /// mysqlExcludedObjects property.
    pub mysql_excluded_objects: Option<MysqlRdbms>,
    /// oracleExcludedObjects property.
    pub oracle_excluded_objects: Option<OracleRdbms>,
    /// postgresqlExcludedObjects property.
    pub postgresql_excluded_objects: Option<PostgresqlRdbms>,
    /// salesforceExcludedObjects property.
    pub salesforce_excluded_objects: Option<SalesforceOrg>,
    /// spannerExcludedObjects property.
    pub spanner_excluded_objects: Option<SpannerDatabase>,
    /// sqlServerExcludedObjects property.
    pub sql_server_excluded_objects: Option<SqlServerRdbms>,
}

/// `MostRecentStartPosition` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct MostRecentStartPosition {}

/// `FetchStaticIpsResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct FetchStaticIpsResponse {
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// staticIps property.
    pub static_ips: Option<Vec<String>>,
}

/// `AppendOnly` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AppendOnly {}

/// `SalesforceSourceConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SalesforceSourceConfig {
    /// excludeObjects property.
    pub exclude_objects: Option<SalesforceOrg>,
    /// includeObjects property.
    pub include_objects: Option<SalesforceOrg>,
    /// pollingInterval property.
    pub polling_interval: Option<String>,
}

/// `SqlServerLsnPosition` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SqlServerLsnPosition {
    /// lsn property.
    pub lsn: Option<String>,
}

/// `IntegerRangePartition` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct IntegerRangePartition {
    /// column property.
    pub column: Option<String>,
    /// end property.
    pub end: Option<String>,
    /// interval property.
    pub interval: Option<String>,
    /// start property.
    pub start: Option<String>,
}

/// `DestinationConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct DestinationConfig {
    /// bigqueryDestinationConfig property.
    pub bigquery_destination_config: Option<BigQueryDestinationConfig>,
    /// destinationConnectionProfile property.
    pub destination_connection_profile: Option<String>,
    /// gcsDestinationConfig property.
    pub gcs_destination_config: Option<GcsDestinationConfig>,
}

/// `GcsDestinationConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GcsDestinationConfig {
    /// avroFileFormat property.
    pub avro_file_format: Option<AvroFileFormat>,
    /// fileRotationInterval property.
    pub file_rotation_interval: Option<String>,
    /// fileRotationMb property.
    pub file_rotation_mb: Option<i64>,
    /// jsonFileFormat property.
    pub json_file_format: Option<JsonFileFormat>,
    /// path property.
    pub path: Option<String>,
}

/// `DatasetTemplate` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct DatasetTemplate {
    /// datasetIdPrefix property.
    pub dataset_id_prefix: Option<String>,
    /// kmsKeyName property.
    pub kms_key_name: Option<String>,
    /// location property.
    pub location: Option<String>,
}

/// `DiscoverConnectionProfileRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct DiscoverConnectionProfileRequest {
    /// connectionProfile property.
    pub connection_profile: Option<ConnectionProfile>,
    /// connectionProfileName property.
    pub connection_profile_name: Option<String>,
    /// fullHierarchy property.
    pub full_hierarchy: Option<bool>,
    /// hierarchyDepth property.
    pub hierarchy_depth: Option<i64>,
    /// mongodbCluster property.
    pub mongodb_cluster: Option<MongodbCluster>,
    /// mysqlRdbms property.
    pub mysql_rdbms: Option<MysqlRdbms>,
    /// oracleRdbms property.
    pub oracle_rdbms: Option<OracleRdbms>,
    /// postgresqlRdbms property.
    pub postgresql_rdbms: Option<PostgresqlRdbms>,
    /// salesforceOrg property.
    pub salesforce_org: Option<SalesforceOrg>,
    /// spannerDatabase property.
    pub spanner_database: Option<SpannerDatabase>,
    /// sqlServerRdbms property.
    pub sql_server_rdbms: Option<SqlServerRdbms>,
}

/// `SpannerTable` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SpannerTable {
    /// columns property.
    pub columns: Option<Vec<SpannerColumn>>,
    /// table property.
    pub table: Option<String>,
}

/// `MongodbObjectIdentifier` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct MongodbObjectIdentifier {
    /// collection property.
    pub collection: Option<String>,
    /// database property.
    pub database: Option<String>,
}

/// `ListPrivateConnectionsResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ListPrivateConnectionsResponse {
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// privateConnections property.
    pub private_connections: Option<Vec<PrivateConnection>>,
    /// unreachable property.
    pub unreachable: Option<Vec<String>>,
}

/// `StopBackfillJobResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct StopBackfillJobResponse {
    /// object property.
    pub object: Option<StreamObject>,
}

/// `CdcStrategy` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct CdcStrategy {
    /// mostRecentStartPosition property.
    pub most_recent_start_position: Option<MostRecentStartPosition>,
    /// nextAvailableStartPosition property.
    pub next_available_start_position: Option<NextAvailableStartPosition>,
    /// specificStartPosition property.
    pub specific_start_position: Option<SpecificStartPosition>,
}

/// `StartBackfillJobRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct StartBackfillJobRequest {
    /// eventFilter property.
    pub event_filter: Option<EventFilter>,
}

/// `Route` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Route {
    /// createTime property.
    pub create_time: Option<String>,
    /// destinationAddress property.
    pub destination_address: Option<String>,
    /// destinationPort property.
    pub destination_port: Option<i64>,
    /// displayName property.
    pub display_name: Option<String>,
    /// labels property.
    pub labels: Option<serde_json::Value>,
    /// name property.
    pub name: Option<String>,
    /// updateTime property.
    pub update_time: Option<String>,
}

/// `Gtid` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Gtid {}

/// `ObjectFilter` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ObjectFilter {
    /// sourceObjectIdentifier property.
    pub source_object_identifier: Option<SourceObjectIdentifier>,
}

/// `UserCredentials` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct UserCredentials {
    /// password property.
    pub password: Option<String>,
    /// secretManagerStoredPassword property.
    pub secret_manager_stored_password: Option<String>,
    /// secretManagerStoredSecurityToken property.
    pub secret_manager_stored_security_token: Option<String>,
    /// securityToken property.
    pub security_token: Option<String>,
    /// username property.
    pub username: Option<String>,
}

/// `PostgresqlSchema` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct PostgresqlSchema {
    /// postgresqlTables property.
    pub postgresql_tables: Option<Vec<PostgresqlTable>>,
    /// schema property.
    pub schema: Option<String>,
}

/// `SqlServerColumn` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SqlServerColumn {
    /// column property.
    pub column: Option<String>,
    /// dataType property.
    pub data_type: Option<String>,
    /// length property.
    pub length: Option<i64>,
    /// nullable property.
    pub nullable: Option<bool>,
    /// ordinalPosition property.
    pub ordinal_position: Option<i64>,
    /// precision property.
    pub precision: Option<i64>,
    /// primaryKey property.
    pub primary_key: Option<bool>,
    /// scale property.
    pub scale: Option<i64>,
}

/// `VpcPeeringConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct VpcPeeringConfig {
    /// subnet property.
    pub subnet: Option<String>,
    /// vpc property.
    pub vpc: Option<String>,
}

/// `MysqlTable` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct MysqlTable {
    /// mysqlColumns property.
    pub mysql_columns: Option<Vec<MysqlColumn>>,
    /// table property.
    pub table: Option<String>,
}

/// `SalesforceProfile` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SalesforceProfile {
    /// domain property.
    pub domain: Option<String>,
    /// oauth2ClientCredentials property.
    pub oauth2_client_credentials: Option<Oauth2ClientCredentials>,
    /// userCredentials property.
    pub user_credentials: Option<UserCredentials>,
}

/// `SalesforceField` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SalesforceField {
    /// dataType property.
    pub data_type: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// nillable property.
    pub nillable: Option<bool>,
}

/// `RunStreamRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct RunStreamRequest {
    /// cdcStrategy property.
    pub cdc_strategy: Option<CdcStrategy>,
    /// force property.
    pub force: Option<bool>,
}

/// `ListConnectionProfilesResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ListConnectionProfilesResponse {
    /// connectionProfiles property.
    pub connection_profiles: Option<Vec<ConnectionProfile>>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// unreachable property.
    pub unreachable: Option<Vec<String>>,
}

/// `SqlServerTable` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SqlServerTable {
    /// columns property.
    pub columns: Option<Vec<SqlServerColumn>>,
    /// table property.
    pub table: Option<String>,
}

/// `GcsProfile` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GcsProfile {
    /// bucket property.
    pub bucket: Option<String>,
    /// rootPath property.
    pub root_path: Option<String>,
}

/// `MongodbField` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct MongodbField {
    /// field property.
    pub field: Option<String>,
}

/// `StreamLargeObjects` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct StreamLargeObjects {}

/// `OracleAsmConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct OracleAsmConfig {
    /// asmService property.
    pub asm_service: Option<String>,
    /// connectionAttributes property.
    pub connection_attributes: Option<serde_json::Value>,
    /// hostname property.
    pub hostname: Option<String>,
    /// oracleSslConfig property.
    pub oracle_ssl_config: Option<OracleSslConfig>,
    /// password property.
    pub password: Option<String>,
    /// port property.
    pub port: Option<i64>,
    /// secretManagerStoredPassword property.
    pub secret_manager_stored_password: Option<String>,
    /// username property.
    pub username: Option<String>,
}

/// `SrvConnectionFormat` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SrvConnectionFormat {}

/// `ListOperationsResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ListOperationsResponse {
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// operations property.
    pub operations: Option<Vec<Operation>>,
    /// unreachable property.
    pub unreachable: Option<Vec<String>>,
}

/// `PrivateConnectivity` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct PrivateConnectivity {
    /// privateConnection property.
    pub private_connection: Option<String>,
}

/// `OracleSslConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct OracleSslConfig {
    /// caCertificate property.
    pub ca_certificate: Option<String>,
    /// caCertificateSet property.
    pub ca_certificate_set: Option<bool>,
    /// serverCertificateDistinguishedName property.
    pub server_certificate_distinguished_name: Option<String>,
}

/// `SalesforceOrg` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SalesforceOrg {
    /// objects property.
    pub objects: Option<Vec<SalesforceObject>>,
}

/// `BinaryLogPosition` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct BinaryLogPosition {}

/// `BlmtConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct BlmtConfig {
    /// bucket property.
    pub bucket: Option<String>,
    /// connectionName property.
    pub connection_name: Option<String>,
    /// fileFormat property.
    pub file_format: Option<String>,
    /// rootPath property.
    pub root_path: Option<String>,
    /// tableFormat property.
    pub table_format: Option<String>,
}

/// `SqlServerSslConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SqlServerSslConfig {
    /// basicEncryption property.
    pub basic_encryption: Option<BasicEncryption>,
    /// encryptionAndServerValidation property.
    pub encryption_and_server_validation: Option<EncryptionAndServerValidation>,
    /// encryptionNotEnforced property.
    pub encryption_not_enforced: Option<EncryptionNotEnforced>,
}

/// `SpannerObjectIdentifier` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SpannerObjectIdentifier {
    /// schema property.
    pub schema: Option<String>,
    /// table property.
    pub table: Option<String>,
}

/// `OracleSchema` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct OracleSchema {
    /// oracleTables property.
    pub oracle_tables: Option<Vec<OracleTable>>,
    /// schema property.
    pub schema: Option<String>,
}

/// `MongodbCollection` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct MongodbCollection {
    /// collection property.
    pub collection: Option<String>,
    /// fields property.
    pub fields: Option<Vec<MongodbField>>,
}

/// `SqlServerSourceConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SqlServerSourceConfig {
    /// changeTables property.
    pub change_tables: Option<SqlServerChangeTables>,
    /// excludeObjects property.
    pub exclude_objects: Option<SqlServerRdbms>,
    /// includeObjects property.
    pub include_objects: Option<SqlServerRdbms>,
    /// maxConcurrentBackfillTasks property.
    pub max_concurrent_backfill_tasks: Option<i64>,
    /// maxConcurrentCdcTasks property.
    pub max_concurrent_cdc_tasks: Option<i64>,
    /// transactionLogs property.
    pub transaction_logs: Option<SqlServerTransactionLogs>,
}

/// `NextAvailableStartPosition` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct NextAvailableStartPosition {}

/// `PostgresqlProfile` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct PostgresqlProfile {
    /// database property.
    pub database: Option<String>,
    /// hostname property.
    pub hostname: Option<String>,
    /// password property.
    pub password: Option<String>,
    /// port property.
    pub port: Option<i64>,
    /// secretManagerStoredPassword property.
    pub secret_manager_stored_password: Option<String>,
    /// sslConfig property.
    pub ssl_config: Option<PostgresqlSslConfig>,
    /// username property.
    pub username: Option<String>,
}

/// `ListRoutesResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ListRoutesResponse {
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// routes property.
    pub routes: Option<Vec<Route>>,
    /// unreachable property.
    pub unreachable: Option<Vec<String>>,
}

/// `MongodbSourceConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct MongodbSourceConfig {
    /// excludeObjects property.
    pub exclude_objects: Option<MongodbCluster>,
    /// includeObjects property.
    pub include_objects: Option<MongodbCluster>,
    /// jsonMode property.
    pub json_mode: Option<String>,
    /// maxConcurrentBackfillTasks property.
    pub max_concurrent_backfill_tasks: Option<i64>,
}

/// `Oauth2ClientCredentials` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Oauth2ClientCredentials {
    /// clientId property.
    pub client_id: Option<String>,
    /// clientSecret property.
    pub client_secret: Option<String>,
    /// secretManagerStoredClientSecret property.
    pub secret_manager_stored_client_secret: Option<String>,
}

/// `ServerAndClientVerification` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ServerAndClientVerification {
    /// caCertificate property.
    pub ca_certificate: Option<String>,
    /// clientCertificate property.
    pub client_certificate: Option<String>,
    /// clientKey property.
    pub client_key: Option<String>,
    /// serverCertificateHostname property.
    pub server_certificate_hostname: Option<String>,
}

/// `RuleSet` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct RuleSet {
    /// customizationRules property.
    pub customization_rules: Option<Vec<CustomizationRule>>,
    /// objectFilter property.
    pub object_filter: Option<ObjectFilter>,
}

/// `OracleRdbms` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct OracleRdbms {
    /// oracleSchemas property.
    pub oracle_schemas: Option<Vec<OracleSchema>>,
}

/// `EventFilter` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct EventFilter {
    /// sqlWhereClause property.
    pub sql_where_clause: Option<String>,
}

/// `MongodbProfile` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct MongodbProfile {
    /// additionalOptions property.
    pub additional_options: Option<serde_json::Value>,
    /// hostAddresses property.
    pub host_addresses: Option<Vec<HostAddress>>,
    /// password property.
    pub password: Option<String>,
    /// replicaSet property.
    pub replica_set: Option<String>,
    /// secretManagerStoredPassword property.
    pub secret_manager_stored_password: Option<String>,
    /// srvConnectionFormat property.
    pub srv_connection_format: Option<SrvConnectionFormat>,
    /// sslConfig property.
    pub ssl_config: Option<MongodbSslConfig>,
    /// standardConnectionFormat property.
    pub standard_connection_format: Option<StandardConnectionFormat>,
    /// username property.
    pub username: Option<String>,
}

/// `ServerVerification` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ServerVerification {
    /// caCertificate property.
    pub ca_certificate: Option<String>,
    /// serverCertificateHostname property.
    pub server_certificate_hostname: Option<String>,
}

/// `SourceObjectIdentifier` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SourceObjectIdentifier {
    /// mongodbIdentifier property.
    pub mongodb_identifier: Option<MongodbObjectIdentifier>,
    /// mysqlIdentifier property.
    pub mysql_identifier: Option<MysqlObjectIdentifier>,
    /// oracleIdentifier property.
    pub oracle_identifier: Option<OracleObjectIdentifier>,
    /// postgresqlIdentifier property.
    pub postgresql_identifier: Option<PostgresqlObjectIdentifier>,
    /// salesforceIdentifier property.
    pub salesforce_identifier: Option<SalesforceObjectIdentifier>,
    /// spannerIdentifier property.
    pub spanner_identifier: Option<SpannerObjectIdentifier>,
    /// sqlServerIdentifier property.
    pub sql_server_identifier: Option<SqlServerObjectIdentifier>,
}

/// `PostgresqlRdbms` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct PostgresqlRdbms {
    /// postgresqlSchemas property.
    pub postgresql_schemas: Option<Vec<PostgresqlSchema>>,
}

/// `SqlServerRdbms` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SqlServerRdbms {
    /// schemas property.
    pub schemas: Option<Vec<SqlServerSchema>>,
}

/// `BinaryLogParser` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct BinaryLogParser {
    /// logFileDirectories property.
    pub log_file_directories: Option<LogFileDirectories>,
    /// oracleAsmLogFileAccess property.
    pub oracle_asm_log_file_access: Option<OracleAsmLogFileAccess>,
}

/// `HostAddress` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct HostAddress {
    /// hostname property.
    pub hostname: Option<String>,
    /// port property.
    pub port: Option<i64>,
}

/// `Location` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Location {
    /// displayName property.
    pub display_name: Option<String>,
    /// labels property.
    pub labels: Option<serde_json::Value>,
    /// locationId property.
    pub location_id: Option<String>,
    /// metadata property.
    pub metadata: Option<serde_json::Value>,
    /// name property.
    pub name: Option<String>,
}

/// `LogMiner` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct LogMiner {}

/// `PostgresqlColumn` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct PostgresqlColumn {
    /// column property.
    pub column: Option<String>,
    /// dataType property.
    pub data_type: Option<String>,
    /// length property.
    pub length: Option<i64>,
    /// nullable property.
    pub nullable: Option<bool>,
    /// ordinalPosition property.
    pub ordinal_position: Option<i64>,
    /// precision property.
    pub precision: Option<i64>,
    /// primaryKey property.
    pub primary_key: Option<bool>,
    /// scale property.
    pub scale: Option<i64>,
}

/// `PostgresqlSourceConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct PostgresqlSourceConfig {
    /// excludeObjects property.
    pub exclude_objects: Option<PostgresqlRdbms>,
    /// includeObjects property.
    pub include_objects: Option<PostgresqlRdbms>,
    /// maxConcurrentBackfillTasks property.
    pub max_concurrent_backfill_tasks: Option<i64>,
    /// publication property.
    pub publication: Option<String>,
    /// replicationSlot property.
    pub replication_slot: Option<String>,
}

/// `SpannerSourceConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SpannerSourceConfig {
    /// backfillDataBoostEnabled property.
    pub backfill_data_boost_enabled: Option<bool>,
    /// changeStreamName property.
    pub change_stream_name: Option<String>,
    /// excludeObjects property.
    pub exclude_objects: Option<SpannerDatabase>,
    /// fgacRole property.
    pub fgac_role: Option<String>,
    /// includeObjects property.
    pub include_objects: Option<SpannerDatabase>,
    /// maxConcurrentBackfillTasks property.
    pub max_concurrent_backfill_tasks: Option<i64>,
    /// maxConcurrentCdcTasks property.
    pub max_concurrent_cdc_tasks: Option<i64>,
    /// spannerRpcPriority property.
    pub spanner_rpc_priority: Option<String>,
}

/// `SqlServerObjectIdentifier` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SqlServerObjectIdentifier {
    /// schema property.
    pub schema: Option<String>,
    /// table property.
    pub table: Option<String>,
}

/// `EncryptionAndServerValidation` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct EncryptionAndServerValidation {
    /// caCertificate property.
    pub ca_certificate: Option<String>,
    /// serverCertificateHostname property.
    pub server_certificate_hostname: Option<String>,
}

/// `StartBackfillJobResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct StartBackfillJobResponse {
    /// object property.
    pub object: Option<StreamObject>,
}

/// `MysqlLogPosition` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct MysqlLogPosition {
    /// logFile property.
    pub log_file: Option<String>,
    /// logPosition property.
    pub log_position: Option<i64>,
}

/// `IngestionTimePartition` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct IngestionTimePartition {
    /// partitioningTimeGranularity property.
    pub partitioning_time_granularity: Option<String>,
}

/// `StopBackfillJobRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct StopBackfillJobRequest {}

/// `StandardConnectionFormat` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct StandardConnectionFormat {
    /// directConnection property.
    pub direct_connection: Option<bool>,
}

/// `JsonFileFormat` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct JsonFileFormat {
    /// compression property.
    pub compression: Option<String>,
    /// schemaFileFormat property.
    pub schema_file_format: Option<String>,
}

/// `MysqlObjectIdentifier` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct MysqlObjectIdentifier {
    /// database property.
    pub database: Option<String>,
    /// table property.
    pub table: Option<String>,
}

/// `CancelOperationRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct CancelOperationRequest {}

/// `OracleScnPosition` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct OracleScnPosition {
    /// scn property.
    pub scn: Option<String>,
}

/// `MysqlDatabase` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct MysqlDatabase {
    /// database property.
    pub database: Option<String>,
    /// mysqlTables property.
    pub mysql_tables: Option<Vec<MysqlTable>>,
}

/// `SourceConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SourceConfig {
    /// mongodbSourceConfig property.
    pub mongodb_source_config: Option<MongodbSourceConfig>,
    /// mysqlSourceConfig property.
    pub mysql_source_config: Option<MysqlSourceConfig>,
    /// oracleSourceConfig property.
    pub oracle_source_config: Option<OracleSourceConfig>,
    /// postgresqlSourceConfig property.
    pub postgresql_source_config: Option<PostgresqlSourceConfig>,
    /// salesforceSourceConfig property.
    pub salesforce_source_config: Option<SalesforceSourceConfig>,
    /// sourceConnectionProfile property.
    pub source_connection_profile: Option<String>,
    /// spannerSourceConfig property.
    pub spanner_source_config: Option<SpannerSourceConfig>,
    /// sqlServerSourceConfig property.
    pub sql_server_source_config: Option<SqlServerSourceConfig>,
}

/// `AvroFileFormat` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AvroFileFormat {}

/// `SqlServerProfile` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SqlServerProfile {
    /// database property.
    pub database: Option<String>,
    /// hostname property.
    pub hostname: Option<String>,
    /// password property.
    pub password: Option<String>,
    /// port property.
    pub port: Option<i64>,
    /// secretManagerStoredPassword property.
    pub secret_manager_stored_password: Option<String>,
    /// sslConfig property.
    pub ssl_config: Option<SqlServerSslConfig>,
    /// username property.
    pub username: Option<String>,
}

/// `OracleObjectIdentifier` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct OracleObjectIdentifier {
    /// schema property.
    pub schema: Option<String>,
    /// table property.
    pub table: Option<String>,
}

/// `MysqlRdbms` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct MysqlRdbms {
    /// mysqlDatabases property.
    pub mysql_databases: Option<Vec<MysqlDatabase>>,
}

/// `ListLocationsResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ListLocationsResponse {
    /// locations property.
    pub locations: Option<Vec<Location>>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
}

/// `BigQueryPartitioning` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct BigQueryPartitioning {
    /// ingestionTimePartition property.
    pub ingestion_time_partition: Option<IngestionTimePartition>,
    /// integerRangePartition property.
    pub integer_range_partition: Option<IntegerRangePartition>,
    /// requirePartitionFilter property.
    pub require_partition_filter: Option<bool>,
    /// timeUnitPartition property.
    pub time_unit_partition: Option<TimeUnitPartition>,
}

/// `MysqlSslConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct MysqlSslConfig {
    /// caCertificate property.
    pub ca_certificate: Option<String>,
    /// caCertificateSet property.
    pub ca_certificate_set: Option<bool>,
    /// clientCertificate property.
    pub client_certificate: Option<String>,
    /// clientCertificateSet property.
    pub client_certificate_set: Option<bool>,
    /// clientKey property.
    pub client_key: Option<String>,
    /// clientKeySet property.
    pub client_key_set: Option<bool>,
}

/// `ListStreamObjectsResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ListStreamObjectsResponse {
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// streamObjects property.
    pub stream_objects: Option<Vec<StreamObject>>,
}

/// `SqlServerSchema` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SqlServerSchema {
    /// schema property.
    pub schema: Option<String>,
    /// tables property.
    pub tables: Option<Vec<SqlServerTable>>,
}

/// `BackfillNoneStrategy` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct BackfillNoneStrategy {}

/// `OracleSourceConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct OracleSourceConfig {
    /// binaryLogParser property.
    pub binary_log_parser: Option<BinaryLogParser>,
    /// dropLargeObjects property.
    pub drop_large_objects: Option<DropLargeObjects>,
    /// excludeObjects property.
    pub exclude_objects: Option<OracleRdbms>,
    /// includeObjects property.
    pub include_objects: Option<OracleRdbms>,
    /// logMiner property.
    pub log_miner: Option<LogMiner>,
    /// maxConcurrentBackfillTasks property.
    pub max_concurrent_backfill_tasks: Option<i64>,
    /// maxConcurrentCdcTasks property.
    pub max_concurrent_cdc_tasks: Option<i64>,
    /// streamLargeObjects property.
    pub stream_large_objects: Option<StreamLargeObjects>,
}

/// `SpannerProfile` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SpannerProfile {
    /// database property.
    pub database: Option<String>,
    /// host property.
    pub host: Option<String>,
}

/// `MongodbSslConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct MongodbSslConfig {
    /// caCertificate property.
    pub ca_certificate: Option<String>,
    /// caCertificateSet property.
    pub ca_certificate_set: Option<bool>,
    /// clientCertificate property.
    pub client_certificate: Option<String>,
    /// clientCertificateSet property.
    pub client_certificate_set: Option<bool>,
    /// clientKey property.
    pub client_key: Option<String>,
    /// clientKeySet property.
    pub client_key_set: Option<bool>,
    /// secretManagerStoredClientKey property.
    pub secret_manager_stored_client_key: Option<String>,
}

/// `SalesforceObjectIdentifier` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SalesforceObjectIdentifier {
    /// objectName property.
    pub object_name: Option<String>,
}

/// `PostgresqlSslConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct PostgresqlSslConfig {
    /// serverAndClientVerification property.
    pub server_and_client_verification: Option<ServerAndClientVerification>,
    /// serverVerification property.
    pub server_verification: Option<ServerVerification>,
}

/// `OracleTable` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct OracleTable {
    /// oracleColumns property.
    pub oracle_columns: Option<Vec<OracleColumn>>,
    /// table property.
    pub table: Option<String>,
}

/// `LookupStreamObjectRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct LookupStreamObjectRequest {
    /// sourceObjectIdentifier property.
    pub source_object_identifier: Option<SourceObjectIdentifier>,
}

/// `MysqlColumn` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct MysqlColumn {
    /// collation property.
    pub collation: Option<String>,
    /// column property.
    pub column: Option<String>,
    /// dataType property.
    pub data_type: Option<String>,
    /// length property.
    pub length: Option<i64>,
    /// nullable property.
    pub nullable: Option<bool>,
    /// ordinalPosition property.
    pub ordinal_position: Option<i64>,
    /// precision property.
    pub precision: Option<i64>,
    /// primaryKey property.
    pub primary_key: Option<bool>,
    /// scale property.
    pub scale: Option<i64>,
}

/// `Stream` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Stream {
    /// backfillAll property.
    pub backfill_all: Option<BackfillAllStrategy>,
    /// backfillNone property.
    pub backfill_none: Option<BackfillNoneStrategy>,
    /// createTime property.
    pub create_time: Option<String>,
    /// customerManagedEncryptionKey property.
    pub customer_managed_encryption_key: Option<String>,
    /// destinationConfig property.
    pub destination_config: Option<DestinationConfig>,
    /// displayName property.
    pub display_name: Option<String>,
    /// errors property.
    pub errors: Option<Vec<Error>>,
    /// labels property.
    pub labels: Option<serde_json::Value>,
    /// lastRecoveryTime property.
    pub last_recovery_time: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// ruleSets property.
    pub rule_sets: Option<Vec<RuleSet>>,
    /// satisfiesPzi property.
    pub satisfies_pzi: Option<bool>,
    /// satisfiesPzs property.
    pub satisfies_pzs: Option<bool>,
    /// sourceConfig property.
    pub source_config: Option<SourceConfig>,
    /// state property.
    pub state: Option<String>,
    /// updateTime property.
    pub update_time: Option<String>,
}

/// `DiscoverConnectionProfileResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct DiscoverConnectionProfileResponse {
    /// mongodbCluster property.
    pub mongodb_cluster: Option<MongodbCluster>,
    /// mysqlRdbms property.
    pub mysql_rdbms: Option<MysqlRdbms>,
    /// oracleRdbms property.
    pub oracle_rdbms: Option<OracleRdbms>,
    /// postgresqlRdbms property.
    pub postgresql_rdbms: Option<PostgresqlRdbms>,
    /// salesforceOrg property.
    pub salesforce_org: Option<SalesforceOrg>,
    /// spannerDatabase property.
    pub spanner_database: Option<SpannerDatabase>,
    /// sqlServerRdbms property.
    pub sql_server_rdbms: Option<SqlServerRdbms>,
}

/// `Merge` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Merge {}

/// `MysqlProfile` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct MysqlProfile {
    /// hostname property.
    pub hostname: Option<String>,
    /// password property.
    pub password: Option<String>,
    /// port property.
    pub port: Option<i64>,
    /// secretManagerStoredPassword property.
    pub secret_manager_stored_password: Option<String>,
    /// sslConfig property.
    pub ssl_config: Option<MysqlSslConfig>,
    /// username property.
    pub username: Option<String>,
}

/// `OracleAsmLogFileAccess` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct OracleAsmLogFileAccess {}

/// `Status` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Status {
    /// code property.
    pub code: Option<i64>,
    /// details property.
    pub details: Option<Vec<serde_json::Value>>,
    /// message property.
    pub message: Option<String>,
}

/// `MongodbChangeStreamPosition` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct MongodbChangeStreamPosition {
    /// startTime property.
    pub start_time: Option<String>,
}

/// `StreamObject` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct StreamObject {
    /// backfillJob property.
    pub backfill_job: Option<BackfillJob>,
    /// createTime property.
    pub create_time: Option<String>,
    /// customizationRules property.
    pub customization_rules: Option<Vec<CustomizationRule>>,
    /// displayName property.
    pub display_name: Option<String>,
    /// errors property.
    pub errors: Option<Vec<Error>>,
    /// name property.
    pub name: Option<String>,
    /// sourceObject property.
    pub source_object: Option<SourceObjectIdentifier>,
    /// updateTime property.
    pub update_time: Option<String>,
}

/// `Error` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Error {
    /// details property.
    pub details: Option<serde_json::Value>,
    /// errorTime property.
    pub error_time: Option<String>,
    /// errorUuid property.
    pub error_uuid: Option<String>,
    /// message property.
    pub message: Option<String>,
    /// reason property.
    pub reason: Option<String>,
}

/// `SpannerSchema` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SpannerSchema {
    /// schema property.
    pub schema: Option<String>,
    /// tables property.
    pub tables: Option<Vec<SpannerTable>>,
}

/// `BigQueryProfile` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct BigQueryProfile {}

/// `PscInterfaceConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct PscInterfaceConfig {
    /// networkAttachment property.
    pub network_attachment: Option<String>,
}

/// `SpannerColumn` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SpannerColumn {
    /// column property.
    pub column: Option<String>,
    /// dataType property.
    pub data_type: Option<String>,
    /// isPrimaryKey property.
    pub is_primary_key: Option<bool>,
    /// ordinalPosition property.
    pub ordinal_position: Option<String>,
}

/// `SqlServerChangeTables` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SqlServerChangeTables {}

/// `SingleTargetDataset` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SingleTargetDataset {
    /// datasetId property.
    pub dataset_id: Option<String>,
}

/// `PostgresqlTable` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct PostgresqlTable {
    /// postgresqlColumns property.
    pub postgresql_columns: Option<Vec<PostgresqlColumn>>,
    /// table property.
    pub table: Option<String>,
}

/// `SqlServerTransactionLogs` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SqlServerTransactionLogs {}

/// `ListStreamsResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ListStreamsResponse {
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// streams property.
    pub streams: Option<Vec<Stream>>,
    /// unreachable property.
    pub unreachable: Option<Vec<String>>,
}

/// `BigQueryClustering` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct BigQueryClustering {
    /// columns property.
    pub columns: Option<Vec<String>>,
}

/// `SourceHierarchyDatasets` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SourceHierarchyDatasets {
    /// datasetTemplate property.
    pub dataset_template: Option<DatasetTemplate>,
    /// projectId property.
    pub project_id: Option<String>,
}

/// `MongodbCluster` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct MongodbCluster {
    /// databases property.
    pub databases: Option<Vec<MongodbDatabase>>,
}

/// `BasicEncryption` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct BasicEncryption {}

/// `OracleColumn` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct OracleColumn {
    /// column property.
    pub column: Option<String>,
    /// dataType property.
    pub data_type: Option<String>,
    /// encoding property.
    pub encoding: Option<String>,
    /// length property.
    pub length: Option<i64>,
    /// nullable property.
    pub nullable: Option<bool>,
    /// ordinalPosition property.
    pub ordinal_position: Option<i64>,
    /// precision property.
    pub precision: Option<i64>,
    /// primaryKey property.
    pub primary_key: Option<bool>,
    /// scale property.
    pub scale: Option<i64>,
}

/// `MysqlGtidPosition` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct MysqlGtidPosition {
    /// gtidSet property.
    pub gtid_set: Option<String>,
}

/// `SpannerDatabase` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SpannerDatabase {
    /// schemas property.
    pub schemas: Option<Vec<SpannerSchema>>,
}

/// `MongodbDatabase` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct MongodbDatabase {
    /// collections property.
    pub collections: Option<Vec<MongodbCollection>>,
    /// database property.
    pub database: Option<String>,
}

/// `LogFileDirectories` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct LogFileDirectories {
    /// archivedLogDirectory property.
    pub archived_log_directory: Option<String>,
    /// onlineLogDirectory property.
    pub online_log_directory: Option<String>,
}

/// `ForwardSshTunnelConnectivity` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ForwardSshTunnelConnectivity {
    /// hostname property.
    pub hostname: Option<String>,
    /// password property.
    pub password: Option<String>,
    /// port property.
    pub port: Option<i64>,
    /// privateKey property.
    pub private_key: Option<String>,
    /// username property.
    pub username: Option<String>,
}

/// `BackfillJob` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct BackfillJob {
    /// errors property.
    pub errors: Option<Vec<Error>>,
    /// lastEndTime property.
    pub last_end_time: Option<String>,
    /// lastStartTime property.
    pub last_start_time: Option<String>,
    /// state property.
    pub state: Option<String>,
    /// trigger property.
    pub trigger: Option<String>,
}

/// `TimeUnitPartition` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct TimeUnitPartition {
    /// column property.
    pub column: Option<String>,
    /// partitioningTimeGranularity property.
    pub partitioning_time_granularity: Option<String>,
}

/// `OracleProfile` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct OracleProfile {
    /// connectionAttributes property.
    pub connection_attributes: Option<serde_json::Value>,
    /// databaseService property.
    pub database_service: Option<String>,
    /// hostname property.
    pub hostname: Option<String>,
    /// oracleAsmConfig property.
    pub oracle_asm_config: Option<OracleAsmConfig>,
    /// oracleSslConfig property.
    pub oracle_ssl_config: Option<OracleSslConfig>,
    /// password property.
    pub password: Option<String>,
    /// port property.
    pub port: Option<i64>,
    /// secretManagerStoredPassword property.
    pub secret_manager_stored_password: Option<String>,
    /// username property.
    pub username: Option<String>,
}

/// `StaticServiceIpConnectivity` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct StaticServiceIpConnectivity {}

/// `EncryptionNotEnforced` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct EncryptionNotEnforced {}

/// `SalesforceObject` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SalesforceObject {
    /// fields property.
    pub fields: Option<Vec<SalesforceField>>,
    /// objectName property.
    pub object_name: Option<String>,
}

/// `DropLargeObjects` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct DropLargeObjects {}

/// `PrivateConnection` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct PrivateConnection {
    /// createTime property.
    pub create_time: Option<String>,
    /// displayName property.
    pub display_name: Option<String>,
    /// error property.
    pub error: Option<Error>,
    /// labels property.
    pub labels: Option<serde_json::Value>,
    /// name property.
    pub name: Option<String>,
    /// pscInterfaceConfig property.
    pub psc_interface_config: Option<PscInterfaceConfig>,
    /// satisfiesPzi property.
    pub satisfies_pzi: Option<bool>,
    /// satisfiesPzs property.
    pub satisfies_pzs: Option<bool>,
    /// state property.
    pub state: Option<String>,
    /// updateTime property.
    pub update_time: Option<String>,
    /// vpcPeeringConfig property.
    pub vpc_peering_config: Option<VpcPeeringConfig>,
}

// =============================================================================
// ARGS TYPES (per-endpoint)
// =============================================================================

/// Arguments for [`datastream.projects.locations.fetchStaticIps_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DatastreamProjectsLocationsFetchStaticIpsArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`datastream.projects.locations.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DatastreamProjectsLocationsGetArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`datastream.projects.locations.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DatastreamProjectsLocationsListArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `extraLocationTypes`.
    pub extra_location_types: Option<String>,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`datastream.projects.locations.connectionProfiles.create_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DatastreamProjectsLocationsConnectionProfilesCreateArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `connectionProfileId`.
    pub connection_profile_id: Option<String>,
    /// Query parameter: `force`.
    pub force: Option<String>,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Query parameter: `validateOnly`.
    pub validate_only: Option<String>,
    /// Request body.
    pub body: ConnectionProfile,
}

/// Arguments for [`datastream.projects.locations.connectionProfiles.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DatastreamProjectsLocationsConnectionProfilesDeleteArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
}

/// Arguments for [`datastream.projects.locations.connectionProfiles.discover_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DatastreamProjectsLocationsConnectionProfilesDiscoverArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Request body.
    pub body: DiscoverConnectionProfileRequest,
}

/// Arguments for [`datastream.projects.locations.connectionProfiles.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DatastreamProjectsLocationsConnectionProfilesGetArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`datastream.projects.locations.connectionProfiles.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DatastreamProjectsLocationsConnectionProfilesListArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`datastream.projects.locations.connectionProfiles.patch_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DatastreamProjectsLocationsConnectionProfilesPatchArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `force`.
    pub force: Option<String>,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Query parameter: `updateMask`.
    pub update_mask: Option<String>,
    /// Query parameter: `validateOnly`.
    pub validate_only: Option<String>,
    /// Request body.
    pub body: ConnectionProfile,
}

/// Arguments for [`datastream.projects.locations.operations.cancel_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DatastreamProjectsLocationsOperationsCancelArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Request body.
    pub body: CancelOperationRequest,
}

/// Arguments for [`datastream.projects.locations.operations.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DatastreamProjectsLocationsOperationsDeleteArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`datastream.projects.locations.operations.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DatastreamProjectsLocationsOperationsGetArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`datastream.projects.locations.operations.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DatastreamProjectsLocationsOperationsListArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
}

/// Arguments for [`datastream.projects.locations.privateConnections.create_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DatastreamProjectsLocationsPrivateConnectionsCreateArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `force`.
    pub force: Option<String>,
    /// Query parameter: `privateConnectionId`.
    pub private_connection_id: Option<String>,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Query parameter: `validateOnly`.
    pub validate_only: Option<String>,
    /// Request body.
    pub body: PrivateConnection,
}

/// Arguments for [`datastream.projects.locations.privateConnections.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DatastreamProjectsLocationsPrivateConnectionsDeleteArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `force`.
    pub force: Option<String>,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
}

/// Arguments for [`datastream.projects.locations.privateConnections.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DatastreamProjectsLocationsPrivateConnectionsGetArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`datastream.projects.locations.privateConnections.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DatastreamProjectsLocationsPrivateConnectionsListArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`datastream.projects.locations.privateConnections.routes.create_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DatastreamProjectsLocationsPrivateConnectionsRoutesCreateArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Query parameter: `routeId`.
    pub route_id: Option<String>,
    /// Request body.
    pub body: Route,
}

/// Arguments for [`datastream.projects.locations.privateConnections.routes.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DatastreamProjectsLocationsPrivateConnectionsRoutesDeleteArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
}

/// Arguments for [`datastream.projects.locations.privateConnections.routes.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DatastreamProjectsLocationsPrivateConnectionsRoutesGetArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`datastream.projects.locations.privateConnections.routes.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DatastreamProjectsLocationsPrivateConnectionsRoutesListArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`datastream.projects.locations.streams.create_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DatastreamProjectsLocationsStreamsCreateArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `force`.
    pub force: Option<String>,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Query parameter: `streamId`.
    pub stream_id: Option<String>,
    /// Query parameter: `validateOnly`.
    pub validate_only: Option<String>,
    /// Request body.
    pub body: Stream,
}

/// Arguments for [`datastream.projects.locations.streams.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DatastreamProjectsLocationsStreamsDeleteArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
}

/// Arguments for [`datastream.projects.locations.streams.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DatastreamProjectsLocationsStreamsGetArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`datastream.projects.locations.streams.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DatastreamProjectsLocationsStreamsListArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`datastream.projects.locations.streams.patch_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DatastreamProjectsLocationsStreamsPatchArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `force`.
    pub force: Option<String>,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Query parameter: `updateMask`.
    pub update_mask: Option<String>,
    /// Query parameter: `validateOnly`.
    pub validate_only: Option<String>,
    /// Request body.
    pub body: Stream,
}

/// Arguments for [`datastream.projects.locations.streams.run_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DatastreamProjectsLocationsStreamsRunArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Request body.
    pub body: RunStreamRequest,
}

/// Arguments for [`datastream.projects.locations.streams.objects.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DatastreamProjectsLocationsStreamsObjectsGetArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`datastream.projects.locations.streams.objects.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DatastreamProjectsLocationsStreamsObjectsListArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`datastream.projects.locations.streams.objects.lookup_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DatastreamProjectsLocationsStreamsObjectsLookupArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Request body.
    pub body: LookupStreamObjectRequest,
}

/// Arguments for [`datastream.projects.locations.streams.objects.startBackfillJob_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DatastreamProjectsLocationsStreamsObjectsStartBackfillJobArgs {
    /// Path parameter: `object`.
    pub object: String,
    /// Request body.
    pub body: StartBackfillJobRequest,
}

/// Arguments for [`datastream.projects.locations.streams.objects.stopBackfillJob_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DatastreamProjectsLocationsStreamsObjectsStopBackfillJobArgs {
    /// Path parameter: `object`.
    pub object: String,
    /// Request body.
    pub body: StopBackfillJobRequest,
}

// =============================================================================
// CLIENT FUNCTIONS (per-endpoint)
// =============================================================================

// -----------------------------------------------------------------------------
// GET v1/{+name}:fetchStaticIps
// -----------------------------------------------------------------------------

/// GET v1/{+name}:fetchStaticIps.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = datastream_projects_locations_fetch_static_ips_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn datastream_projects_locations_fetch_static_ips_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DatastreamProjectsLocationsFetchStaticIpsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<FetchStaticIpsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://datastream.googleapis.com/v1/{}:fetchStaticIps",
        args.name,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: FetchStaticIpsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+name}
// -----------------------------------------------------------------------------

/// GET v1/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = datastream_projects_locations_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn datastream_projects_locations_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DatastreamProjectsLocationsGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Location>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://datastream.googleapis.com/v1/{}", args.name,);

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Location =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+name}/locations
// -----------------------------------------------------------------------------

/// GET v1/{+name}/locations.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = datastream_projects_locations_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn datastream_projects_locations_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DatastreamProjectsLocationsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListLocationsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://datastream.googleapis.com/v1/{}/locations",
        args.name,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.extra_location_types {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("extraLocationTypes=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListLocationsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/{+parent}/connectionProfiles
// -----------------------------------------------------------------------------

/// POST v1/{+parent}/connectionProfiles.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = datastream_projects_locations_connection_profiles_create_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn datastream_projects_locations_connection_profiles_create_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DatastreamProjectsLocationsConnectionProfilesCreateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://datastream.googleapis.com/v1/{}/connectionProfiles",
        args.parent,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.connection_profile_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("connectionProfileId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.force {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("force=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.validate_only {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("validateOnly=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE v1/{+name}
// -----------------------------------------------------------------------------

/// DELETE v1/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = datastream_projects_locations_connection_profiles_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn datastream_projects_locations_connection_profiles_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DatastreamProjectsLocationsConnectionProfilesDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://datastream.googleapis.com/v1/{}", args.name,);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/{+parent}/connectionProfiles:discover
// -----------------------------------------------------------------------------

/// POST v1/{+parent}/connectionProfiles:discover.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = datastream_projects_locations_connection_profiles_discover_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn datastream_projects_locations_connection_profiles_discover_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DatastreamProjectsLocationsConnectionProfilesDiscoverArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<DiscoverConnectionProfileResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://datastream.googleapis.com/v1/{}/connectionProfiles:discover",
        args.parent,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: DiscoverConnectionProfileResponse = serde_json::from_str(&body)
                    .map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+name}
// -----------------------------------------------------------------------------

/// GET v1/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = datastream_projects_locations_connection_profiles_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn datastream_projects_locations_connection_profiles_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DatastreamProjectsLocationsConnectionProfilesGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ConnectionProfile>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://datastream.googleapis.com/v1/{}", args.name,);

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ConnectionProfile =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+parent}/connectionProfiles
// -----------------------------------------------------------------------------

/// GET v1/{+parent}/connectionProfiles.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = datastream_projects_locations_connection_profiles_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn datastream_projects_locations_connection_profiles_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DatastreamProjectsLocationsConnectionProfilesListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListConnectionProfilesResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://datastream.googleapis.com/v1/{}/connectionProfiles",
        args.parent,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListConnectionProfilesResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH v1/{+name}
// -----------------------------------------------------------------------------

/// PATCH v1/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = datastream_projects_locations_connection_profiles_patch_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn datastream_projects_locations_connection_profiles_patch_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DatastreamProjectsLocationsConnectionProfilesPatchArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://datastream.googleapis.com/v1/{}", args.name,);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.force {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("force=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.update_mask {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("updateMask=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.validate_only {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("validateOnly=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/{+name}:cancel
// -----------------------------------------------------------------------------

/// POST v1/{+name}:cancel.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = datastream_projects_locations_operations_cancel_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn datastream_projects_locations_operations_cancel_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DatastreamProjectsLocationsOperationsCancelArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://datastream.googleapis.com/v1/{}:cancel", args.name,);

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Empty =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE v1/{+name}
// -----------------------------------------------------------------------------

/// DELETE v1/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = datastream_projects_locations_operations_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn datastream_projects_locations_operations_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DatastreamProjectsLocationsOperationsDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://datastream.googleapis.com/v1/{}", args.name,);

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Empty =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+name}
// -----------------------------------------------------------------------------

/// GET v1/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = datastream_projects_locations_operations_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn datastream_projects_locations_operations_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DatastreamProjectsLocationsOperationsGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://datastream.googleapis.com/v1/{}", args.name,);

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+name}/operations
// -----------------------------------------------------------------------------

/// GET v1/{+name}/operations.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = datastream_projects_locations_operations_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn datastream_projects_locations_operations_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DatastreamProjectsLocationsOperationsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListOperationsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://datastream.googleapis.com/v1/{}/operations",
        args.name,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.return_partial_success {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("returnPartialSuccess=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListOperationsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/{+parent}/privateConnections
// -----------------------------------------------------------------------------

/// POST v1/{+parent}/privateConnections.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = datastream_projects_locations_private_connections_create_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn datastream_projects_locations_private_connections_create_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DatastreamProjectsLocationsPrivateConnectionsCreateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://datastream.googleapis.com/v1/{}/privateConnections",
        args.parent,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.force {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("force=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.private_connection_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("privateConnectionId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.validate_only {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("validateOnly=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE v1/{+name}
// -----------------------------------------------------------------------------

/// DELETE v1/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = datastream_projects_locations_private_connections_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn datastream_projects_locations_private_connections_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DatastreamProjectsLocationsPrivateConnectionsDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://datastream.googleapis.com/v1/{}", args.name,);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.force {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("force=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+name}
// -----------------------------------------------------------------------------

/// GET v1/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = datastream_projects_locations_private_connections_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn datastream_projects_locations_private_connections_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DatastreamProjectsLocationsPrivateConnectionsGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<PrivateConnection>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://datastream.googleapis.com/v1/{}", args.name,);

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: PrivateConnection =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+parent}/privateConnections
// -----------------------------------------------------------------------------

/// GET v1/{+parent}/privateConnections.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = datastream_projects_locations_private_connections_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn datastream_projects_locations_private_connections_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DatastreamProjectsLocationsPrivateConnectionsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListPrivateConnectionsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://datastream.googleapis.com/v1/{}/privateConnections",
        args.parent,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListPrivateConnectionsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/{+parent}/routes
// -----------------------------------------------------------------------------

/// POST v1/{+parent}/routes.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = datastream_projects_locations_private_connections_routes_create_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn datastream_projects_locations_private_connections_routes_create_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DatastreamProjectsLocationsPrivateConnectionsRoutesCreateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://datastream.googleapis.com/v1/{}/routes",
        args.parent,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.route_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("routeId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE v1/{+name}
// -----------------------------------------------------------------------------

/// DELETE v1/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = datastream_projects_locations_private_connections_routes_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn datastream_projects_locations_private_connections_routes_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DatastreamProjectsLocationsPrivateConnectionsRoutesDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://datastream.googleapis.com/v1/{}", args.name,);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+name}
// -----------------------------------------------------------------------------

/// GET v1/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = datastream_projects_locations_private_connections_routes_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn datastream_projects_locations_private_connections_routes_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DatastreamProjectsLocationsPrivateConnectionsRoutesGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Route>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://datastream.googleapis.com/v1/{}", args.name,);

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Route =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+parent}/routes
// -----------------------------------------------------------------------------

/// GET v1/{+parent}/routes.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = datastream_projects_locations_private_connections_routes_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn datastream_projects_locations_private_connections_routes_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DatastreamProjectsLocationsPrivateConnectionsRoutesListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListRoutesResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://datastream.googleapis.com/v1/{}/routes",
        args.parent,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListRoutesResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/{+parent}/streams
// -----------------------------------------------------------------------------

/// POST v1/{+parent}/streams.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = datastream_projects_locations_streams_create_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn datastream_projects_locations_streams_create_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DatastreamProjectsLocationsStreamsCreateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://datastream.googleapis.com/v1/{}/streams",
        args.parent,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.force {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("force=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.stream_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("streamId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.validate_only {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("validateOnly=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE v1/{+name}
// -----------------------------------------------------------------------------

/// DELETE v1/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = datastream_projects_locations_streams_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn datastream_projects_locations_streams_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DatastreamProjectsLocationsStreamsDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://datastream.googleapis.com/v1/{}", args.name,);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+name}
// -----------------------------------------------------------------------------

/// GET v1/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = datastream_projects_locations_streams_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn datastream_projects_locations_streams_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DatastreamProjectsLocationsStreamsGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Stream>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://datastream.googleapis.com/v1/{}", args.name,);

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Stream =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+parent}/streams
// -----------------------------------------------------------------------------

/// GET v1/{+parent}/streams.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = datastream_projects_locations_streams_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn datastream_projects_locations_streams_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DatastreamProjectsLocationsStreamsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListStreamsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://datastream.googleapis.com/v1/{}/streams",
        args.parent,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListStreamsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH v1/{+name}
// -----------------------------------------------------------------------------

/// PATCH v1/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = datastream_projects_locations_streams_patch_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn datastream_projects_locations_streams_patch_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DatastreamProjectsLocationsStreamsPatchArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://datastream.googleapis.com/v1/{}", args.name,);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.force {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("force=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.request_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requestId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.update_mask {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("updateMask=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.validate_only {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("validateOnly=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/{+name}:run
// -----------------------------------------------------------------------------

/// POST v1/{+name}:run.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = datastream_projects_locations_streams_run_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn datastream_projects_locations_streams_run_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DatastreamProjectsLocationsStreamsRunArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://datastream.googleapis.com/v1/{}:run", args.name,);

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+name}
// -----------------------------------------------------------------------------

/// GET v1/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = datastream_projects_locations_streams_objects_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn datastream_projects_locations_streams_objects_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DatastreamProjectsLocationsStreamsObjectsGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<StreamObject>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://datastream.googleapis.com/v1/{}", args.name,);

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: StreamObject =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+parent}/objects
// -----------------------------------------------------------------------------

/// GET v1/{+parent}/objects.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = datastream_projects_locations_streams_objects_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn datastream_projects_locations_streams_objects_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DatastreamProjectsLocationsStreamsObjectsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListStreamObjectsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://datastream.googleapis.com/v1/{}/objects",
        args.parent,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListStreamObjectsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/{+parent}/objects:lookup
// -----------------------------------------------------------------------------

/// POST v1/{+parent}/objects:lookup.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = datastream_projects_locations_streams_objects_lookup_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn datastream_projects_locations_streams_objects_lookup_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DatastreamProjectsLocationsStreamsObjectsLookupArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<StreamObject>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://datastream.googleapis.com/v1/{}/objects:lookup",
        args.parent,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: StreamObject =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/{+object}:startBackfillJob
// -----------------------------------------------------------------------------

/// POST v1/{+object}:startBackfillJob.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = datastream_projects_locations_streams_objects_start_backfill_job_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn datastream_projects_locations_streams_objects_start_backfill_job_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DatastreamProjectsLocationsStreamsObjectsStartBackfillJobArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<StartBackfillJobResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://datastream.googleapis.com/v1/{}:startBackfillJob",
        args.object,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: StartBackfillJobResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/{+object}:stopBackfillJob
// -----------------------------------------------------------------------------

/// POST v1/{+object}:stopBackfillJob.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = datastream_projects_locations_streams_objects_stop_backfill_job_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn datastream_projects_locations_streams_objects_stop_backfill_job_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DatastreamProjectsLocationsStreamsObjectsStopBackfillJobArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<StopBackfillJobResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://datastream.googleapis.com/v1/{}:stopBackfillJob",
        args.object,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: StopBackfillJobResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}
