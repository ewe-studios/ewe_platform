//! Auto-generated API module for gcp/datastream objects.
//!
//! Generated by `cargo run --bin ewe_platform gen_api`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp_datastream_objects `

#![cfg(feature = "gcp_datastream_objects")]
#![allow(clippy::too_many_arguments, clippy::type_complexity)]
#![allow(
    clippy::missing_errors_doc,
    clippy::doc_markdown,
    clippy::useless_format
)]
#![allow(unused_imports)]

use foundation_core::valtron::{TaskIterator, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{ClientRequestBuilder, SimpleHttpClient};
use foundation_macros::JsonHash;
use serde::{Deserialize, Serialize};

// Import shared types used by this module
use super::shared::StreamObject;

use super::shared::ApiResponse;

// =============================================================================
// TYPE DECLARATIONS
// =============================================================================

/// `StartBackfillJobResponse` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct StartBackfillJobResponse {
    /// object property.
    pub object: Option<StreamObject>,
}

/// `BigQueryPartitioning` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct BigQueryPartitioning {
    /// ingestionTimePartition property.
    pub ingestion_time_partition: Option<IngestionTimePartition>,
    /// integerRangePartition property.
    pub integer_range_partition: Option<IntegerRangePartition>,
    /// requirePartitionFilter property.
    pub require_partition_filter: Option<bool>,
    /// timeUnitPartition property.
    pub time_unit_partition: Option<TimeUnitPartition>,
}

/// `SpannerObjectIdentifier` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct SpannerObjectIdentifier {
    /// schema property.
    pub schema: Option<String>,
    /// table property.
    pub table: Option<String>,
}

/// `StopBackfillJobResponse` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct StopBackfillJobResponse {
    /// object property.
    pub object: Option<StreamObject>,
}

/// `ListStreamObjectsResponse` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ListStreamObjectsResponse {
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// streamObjects property.
    pub stream_objects: Option<Vec<StreamObject>>,
}

/// `CustomizationRule` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct CustomizationRule {
    /// bigqueryClustering property.
    pub bigquery_clustering: Option<BigQueryClustering>,
    /// bigqueryPartitioning property.
    pub bigquery_partitioning: Option<BigQueryPartitioning>,
}

/// `Error` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Error {
    /// details property.
    pub details: Option<serde_json::Value>,
    /// errorTime property.
    pub error_time: Option<String>,
    /// errorUuid property.
    pub error_uuid: Option<String>,
    /// message property.
    pub message: Option<String>,
    /// reason property.
    pub reason: Option<String>,
}

/// `PostgresqlObjectIdentifier` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct PostgresqlObjectIdentifier {
    /// schema property.
    pub schema: Option<String>,
    /// table property.
    pub table: Option<String>,
}

/// `SqlServerObjectIdentifier` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct SqlServerObjectIdentifier {
    /// schema property.
    pub schema: Option<String>,
    /// table property.
    pub table: Option<String>,
}

/// `OracleObjectIdentifier` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct OracleObjectIdentifier {
    /// schema property.
    pub schema: Option<String>,
    /// table property.
    pub table: Option<String>,
}

/// `TimeUnitPartition` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct TimeUnitPartition {
    /// column property.
    pub column: Option<String>,
    /// partitioningTimeGranularity property.
    pub partitioning_time_granularity: Option<String>,
}

/// `SalesforceObjectIdentifier` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct SalesforceObjectIdentifier {
    /// objectName property.
    pub object_name: Option<String>,
}

/// `MongodbObjectIdentifier` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct MongodbObjectIdentifier {
    /// collection property.
    pub collection: Option<String>,
    /// database property.
    pub database: Option<String>,
}

/// `MysqlObjectIdentifier` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct MysqlObjectIdentifier {
    /// database property.
    pub database: Option<String>,
    /// table property.
    pub table: Option<String>,
}

/// `IntegerRangePartition` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct IntegerRangePartition {
    /// column property.
    pub column: Option<String>,
    /// end property.
    pub end: Option<String>,
    /// interval property.
    pub interval: Option<String>,
    /// start property.
    pub start: Option<String>,
}

/// `SourceObjectIdentifier` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct SourceObjectIdentifier {
    /// mongodbIdentifier property.
    pub mongodb_identifier: Option<MongodbObjectIdentifier>,
    /// mysqlIdentifier property.
    pub mysql_identifier: Option<MysqlObjectIdentifier>,
    /// oracleIdentifier property.
    pub oracle_identifier: Option<OracleObjectIdentifier>,
    /// postgresqlIdentifier property.
    pub postgresql_identifier: Option<PostgresqlObjectIdentifier>,
    /// salesforceIdentifier property.
    pub salesforce_identifier: Option<SalesforceObjectIdentifier>,
    /// spannerIdentifier property.
    pub spanner_identifier: Option<SpannerObjectIdentifier>,
    /// sqlServerIdentifier property.
    pub sql_server_identifier: Option<SqlServerObjectIdentifier>,
}

/// `BigQueryClustering` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct BigQueryClustering {
    /// columns property.
    pub columns: Option<Vec<String>>,
}

/// `BackfillJob` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct BackfillJob {
    /// errors property.
    pub errors: Option<Vec<Error>>,
    /// lastEndTime property.
    pub last_end_time: Option<String>,
    /// lastStartTime property.
    pub last_start_time: Option<String>,
    /// state property.
    pub state: Option<String>,
    /// trigger property.
    pub trigger: Option<String>,
}

/// `IngestionTimePartition` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct IngestionTimePartition {
    /// partitioningTimeGranularity property.
    pub partitioning_time_granularity: Option<String>,
}

// =============================================================================
// ARGS TYPES (per-endpoint)
// =============================================================================

/// Arguments for [`datastream.projects.locations.streams.objects.get_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DatastreamProjectsLocationsStreamsObjectsGetArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`datastream.projects.locations.streams.objects.list_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DatastreamProjectsLocationsStreamsObjectsListArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`datastream.projects.locations.streams.objects.startBackfillJob_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DatastreamProjectsLocationsStreamsObjectsStartBackfillJobArgs {
    /// Path parameter: `object`.
    pub object: String,
}

/// Arguments for [`datastream.projects.locations.streams.objects.stopBackfillJob_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DatastreamProjectsLocationsStreamsObjectsStopBackfillJobArgs {
    /// Path parameter: `object`.
    pub object: String,
}

// =============================================================================
// CLIENT FUNCTIONS (per-endpoint)
// =============================================================================

// -----------------------------------------------------------------------------
// GET v1/projects/{projectsId}/locations/{locationsId}/streams/{streamsId}/objects/{objectsId}
// -----------------------------------------------------------------------------

/// GET v1/projects/{projectsId}/locations/{locationsId}/streams/{streamsId}/objects/{objectsId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = datastream_projects_locations_streams_objects_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn datastream_projects_locations_streams_objects_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<StreamObject>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://datastream.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/streams/{{streamsId}}/objects/{{objectsId}}",
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: StreamObject =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/projects/{projectsId}/locations/{locationsId}/streams/{streamsId}/objects
// -----------------------------------------------------------------------------

/// GET v1/projects/{projectsId}/locations/{locationsId}/streams/{streamsId}/objects.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = datastream_projects_locations_streams_objects_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn datastream_projects_locations_streams_objects_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListStreamObjectsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://datastream.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/streams/{{streamsId}}/objects",
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListStreamObjectsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/projects/{projectsId}/locations/{locationsId}/streams/{streamsId}/objects/{objectsId}:startBackfillJob
// -----------------------------------------------------------------------------

/// POST v1/projects/{projectsId}/locations/{locationsId}/streams/{streamsId}/objects/{objectsId}:startBackfillJob.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = datastream_projects_locations_streams_objects_start_backfill_job_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn datastream_projects_locations_streams_objects_start_backfill_job_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<StartBackfillJobResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://datastream.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/streams/{{streamsId}}/objects/{{objectsId}}:startBackfillJob",
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: StartBackfillJobResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/projects/{projectsId}/locations/{locationsId}/streams/{streamsId}/objects/{objectsId}:stopBackfillJob
// -----------------------------------------------------------------------------

/// POST v1/projects/{projectsId}/locations/{locationsId}/streams/{streamsId}/objects/{objectsId}:stopBackfillJob.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = datastream_projects_locations_streams_objects_stop_backfill_job_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn datastream_projects_locations_streams_objects_stop_backfill_job_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<StopBackfillJobResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://datastream.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/streams/{{streamsId}}/objects/{{objectsId}}:stopBackfillJob",
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: StopBackfillJobResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}
