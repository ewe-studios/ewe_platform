//! Auto-generated API module for gcp/localservices localservices.
//!
//! Generated by `cargo run --bin ewe_platform gen_api`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp_localservices_localservices `

#![cfg(feature = "gcp_localservices_localservices")]
#![allow(clippy::too_many_arguments, clippy::type_complexity)]
#![allow(
    clippy::missing_errors_doc,
    clippy::doc_markdown,
    clippy::useless_format
)]
#![allow(unused_imports)]

use foundation_core::valtron::{TaskIterator, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{ClientRequestBuilder, SimpleHttpClient};
use foundation_macros::JsonHash;
use serde::{Deserialize, Serialize};

use super::shared::ApiResponse;

// =============================================================================
// TYPE DECLARATIONS
// =============================================================================

/// `GoogleTypeTimeZone` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleTypeTimeZone {
    /// id property.
    pub id: Option<String>,
    /// version property.
    pub version: Option<String>,
}

/// `GoogleAdsHomeservicesLocalservicesV1BookingLead` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleAdsHomeservicesLocalservicesV1BookingLead {
    /// bookingAppointmentTimestamp property.
    pub booking_appointment_timestamp: Option<String>,
    /// consumerEmail property.
    pub consumer_email: Option<String>,
    /// consumerPhoneNumber property.
    pub consumer_phone_number: Option<String>,
    /// customerName property.
    pub customer_name: Option<String>,
    /// jobType property.
    pub job_type: Option<String>,
}

/// `GoogleAdsHomeservicesLocalservicesV1MessageLead` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleAdsHomeservicesLocalservicesV1MessageLead {
    /// consumerPhoneNumber property.
    pub consumer_phone_number: Option<String>,
    /// customerName property.
    pub customer_name: Option<String>,
    /// jobType property.
    pub job_type: Option<String>,
    /// postalCode property.
    pub postal_code: Option<String>,
}

/// `GoogleAdsHomeservicesLocalservicesV1SearchAccountReportsResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleAdsHomeservicesLocalservicesV1SearchAccountReportsResponse {
    /// accountReports property.
    pub account_reports: Option<Vec<GoogleAdsHomeservicesLocalservicesV1AccountReport>>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
}

/// `GoogleAdsHomeservicesLocalservicesV1SearchDetailedLeadReportsResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleAdsHomeservicesLocalservicesV1SearchDetailedLeadReportsResponse {
    /// detailedLeadReports property.
    pub detailed_lead_reports: Option<Vec<GoogleAdsHomeservicesLocalservicesV1DetailedLeadReport>>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
}

/// `GoogleAdsHomeservicesLocalservicesV1AggregatorInfo` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleAdsHomeservicesLocalservicesV1AggregatorInfo {
    /// aggregatorProviderId property.
    pub aggregator_provider_id: Option<String>,
}

/// `GoogleAdsHomeservicesLocalservicesV1PhoneLead` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleAdsHomeservicesLocalservicesV1PhoneLead {
    /// chargedCallTimestamp property.
    pub charged_call_timestamp: Option<String>,
    /// chargedConnectedCallDurationSeconds property.
    pub charged_connected_call_duration_seconds: Option<String>,
    /// consumerPhoneNumber property.
    pub consumer_phone_number: Option<String>,
}

/// `GoogleAdsHomeservicesLocalservicesV1DetailedLeadReport` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleAdsHomeservicesLocalservicesV1DetailedLeadReport {
    /// accountId property.
    pub account_id: Option<String>,
    /// aggregatorInfo property.
    pub aggregator_info: Option<GoogleAdsHomeservicesLocalservicesV1AggregatorInfo>,
    /// bookingLead property.
    pub booking_lead: Option<GoogleAdsHomeservicesLocalservicesV1BookingLead>,
    /// businessName property.
    pub business_name: Option<String>,
    /// chargeStatus property.
    pub charge_status: Option<String>,
    /// currencyCode property.
    pub currency_code: Option<String>,
    /// disputeStatus property.
    pub dispute_status: Option<String>,
    /// geo property.
    pub geo: Option<String>,
    /// googleAdsLeadId property.
    pub google_ads_lead_id: Option<String>,
    /// leadCategory property.
    pub lead_category: Option<String>,
    /// leadCreationTimestamp property.
    pub lead_creation_timestamp: Option<String>,
    /// leadId property.
    pub lead_id: Option<String>,
    /// leadPrice property.
    pub lead_price: Option<f64>,
    /// leadType property.
    pub lead_type: Option<String>,
    /// messageLead property.
    pub message_lead: Option<GoogleAdsHomeservicesLocalservicesV1MessageLead>,
    /// phoneLead property.
    pub phone_lead: Option<GoogleAdsHomeservicesLocalservicesV1PhoneLead>,
    /// timezone property.
    pub timezone: Option<GoogleTypeTimeZone>,
}

/// `GoogleAdsHomeservicesLocalservicesV1AccountReport` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GoogleAdsHomeservicesLocalservicesV1AccountReport {
    /// accountId property.
    pub account_id: Option<String>,
    /// aggregatorInfo property.
    pub aggregator_info: Option<GoogleAdsHomeservicesLocalservicesV1AggregatorInfo>,
    /// averageFiveStarRating property.
    pub average_five_star_rating: Option<f64>,
    /// averageWeeklyBudget property.
    pub average_weekly_budget: Option<f64>,
    /// businessName property.
    pub business_name: Option<String>,
    /// currencyCode property.
    pub currency_code: Option<String>,
    /// currentPeriodChargedLeads property.
    pub current_period_charged_leads: Option<String>,
    /// currentPeriodConnectedPhoneCalls property.
    pub current_period_connected_phone_calls: Option<String>,
    /// currentPeriodPhoneCalls property.
    pub current_period_phone_calls: Option<String>,
    /// currentPeriodTotalCost property.
    pub current_period_total_cost: Option<f64>,
    /// impressionsLastTwoDays property.
    pub impressions_last_two_days: Option<String>,
    /// phoneLeadResponsiveness property.
    pub phone_lead_responsiveness: Option<f64>,
    /// previousPeriodChargedLeads property.
    pub previous_period_charged_leads: Option<String>,
    /// previousPeriodConnectedPhoneCalls property.
    pub previous_period_connected_phone_calls: Option<String>,
    /// previousPeriodPhoneCalls property.
    pub previous_period_phone_calls: Option<String>,
    /// previousPeriodTotalCost property.
    pub previous_period_total_cost: Option<f64>,
    /// totalReview property.
    pub total_review: Option<i64>,
}

// =============================================================================
// ARGS TYPES (per-endpoint)
// =============================================================================

/// Arguments for [`localservices.accountReports.search_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct LocalservicesAccountReportsSearchArgs {
    /// Query parameter: `endDate.day`.
    pub end_date_day: Option<String>,
    /// Query parameter: `endDate.month`.
    pub end_date_month: Option<String>,
    /// Query parameter: `endDate.year`.
    pub end_date_year: Option<String>,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `query`.
    pub query: Option<String>,
    /// Query parameter: `startDate.day`.
    pub start_date_day: Option<String>,
    /// Query parameter: `startDate.month`.
    pub start_date_month: Option<String>,
    /// Query parameter: `startDate.year`.
    pub start_date_year: Option<String>,
}

/// Arguments for [`localservices.detailedLeadReports.search_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct LocalservicesDetailedLeadReportsSearchArgs {
    /// Query parameter: `endDate.day`.
    pub end_date_day: Option<String>,
    /// Query parameter: `endDate.month`.
    pub end_date_month: Option<String>,
    /// Query parameter: `endDate.year`.
    pub end_date_year: Option<String>,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `query`.
    pub query: Option<String>,
    /// Query parameter: `startDate.day`.
    pub start_date_day: Option<String>,
    /// Query parameter: `startDate.month`.
    pub start_date_month: Option<String>,
    /// Query parameter: `startDate.year`.
    pub start_date_year: Option<String>,
}

// =============================================================================
// CLIENT FUNCTIONS (per-endpoint)
// =============================================================================

// -----------------------------------------------------------------------------
// GET v1/accountReports:search
// -----------------------------------------------------------------------------

/// GET v1/accountReports:search.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = localservices_account_reports_search_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn localservices_account_reports_search_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &LocalservicesAccountReportsSearchArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleAdsHomeservicesLocalservicesV1SearchAccountReportsResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://localservices.googleapis.com/v1/accountReports:search",);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.end_date_day {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("endDate_day=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.end_date_month {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("endDate_month=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.end_date_year {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("endDate_year=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.query {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("query=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.start_date_day {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("startDate_day=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.start_date_month {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("startDate_month=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.start_date_year {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("startDate_year=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleAdsHomeservicesLocalservicesV1SearchAccountReportsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/detailedLeadReports:search
// -----------------------------------------------------------------------------

/// GET v1/detailedLeadReports:search.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = localservices_detailed_lead_reports_search_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn localservices_detailed_lead_reports_search_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &LocalservicesDetailedLeadReportsSearchArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<GoogleAdsHomeservicesLocalservicesV1SearchDetailedLeadReportsResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url =
        format!("https://localservices.googleapis.com/v1/detailedLeadReports:search",);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.end_date_day {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("endDate_day=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.end_date_month {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("endDate_month=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.end_date_year {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("endDate_year=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.query {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("query=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.start_date_day {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("startDate_day=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.start_date_month {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("startDate_month=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.start_date_year {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("startDate_year=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GoogleAdsHomeservicesLocalservicesV1SearchDetailedLeadReportsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}
