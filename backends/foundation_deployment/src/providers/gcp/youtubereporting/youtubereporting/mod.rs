//! Auto-generated API module for gcp/youtubereporting youtubereporting.
//!
//! Generated by `cargo run --bin ewe_platform gen_api`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp_youtubereporting_youtubereporting `

#![cfg(feature = "gcp_youtubereporting_youtubereporting")]
#![allow(clippy::too_many_arguments, clippy::type_complexity)]
#![allow(
    clippy::missing_errors_doc,
    clippy::doc_markdown,
    clippy::useless_format
)]
#![allow(unused_imports)]

use foundation_core::valtron::{TaskIterator, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{ClientRequestBuilder, SimpleHttpClient};
use foundation_macros::JsonHash;
use serde::{Deserialize, Serialize};

// Import shared types used by this module
use super::shared::Empty;

use super::shared::ApiResponse;

// =============================================================================
// TYPE DECLARATIONS
// =============================================================================

/// `Job` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Job {
    /// createTime property.
    pub create_time: Option<String>,
    /// expireTime property.
    pub expire_time: Option<String>,
    /// id property.
    pub id: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// reportTypeId property.
    pub report_type_id: Option<String>,
    /// systemManaged property.
    pub system_managed: Option<bool>,
}

/// `Report` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Report {
    /// createTime property.
    pub create_time: Option<String>,
    /// downloadUrl property.
    pub download_url: Option<String>,
    /// endTime property.
    pub end_time: Option<String>,
    /// id property.
    pub id: Option<String>,
    /// jobExpireTime property.
    pub job_expire_time: Option<String>,
    /// jobId property.
    pub job_id: Option<String>,
    /// startTime property.
    pub start_time: Option<String>,
}

/// `GdataMedia` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GdataMedia {
    /// algorithm property.
    pub algorithm: Option<String>,
    /// bigstoreObjectRef property.
    pub bigstore_object_ref: Option<String>,
    /// blobRef property.
    pub blob_ref: Option<String>,
    /// blobstore2Info property.
    pub blobstore2_info: Option<GdataBlobstore2Info>,
    /// compositeMedia property.
    pub composite_media: Option<Vec<GdataCompositeMedia>>,
    /// contentType property.
    pub content_type: Option<String>,
    /// contentTypeInfo property.
    pub content_type_info: Option<GdataContentTypeInfo>,
    /// cosmoBinaryReference property.
    pub cosmo_binary_reference: Option<String>,
    /// crc32cHash property.
    pub crc32c_hash: Option<i64>,
    /// diffChecksumsResponse property.
    pub diff_checksums_response: Option<GdataDiffChecksumsResponse>,
    /// diffDownloadResponse property.
    pub diff_download_response: Option<GdataDiffDownloadResponse>,
    /// diffUploadRequest property.
    pub diff_upload_request: Option<GdataDiffUploadRequest>,
    /// diffUploadResponse property.
    pub diff_upload_response: Option<GdataDiffUploadResponse>,
    /// diffVersionResponse property.
    pub diff_version_response: Option<GdataDiffVersionResponse>,
    /// downloadParameters property.
    pub download_parameters: Option<GdataDownloadParameters>,
    /// filename property.
    pub filename: Option<String>,
    /// hash property.
    pub hash: Option<String>,
    /// hashVerified property.
    pub hash_verified: Option<bool>,
    /// inline property.
    pub inline: Option<String>,
    /// isPotentialRetry property.
    pub is_potential_retry: Option<bool>,
    /// length property.
    pub length: Option<String>,
    /// md5Hash property.
    pub md5_hash: Option<String>,
    /// mediaId property.
    pub media_id: Option<String>,
    /// objectId property.
    pub object_id: Option<GdataObjectId>,
    /// path property.
    pub path: Option<String>,
    /// referenceType property.
    pub reference_type: Option<String>,
    /// sha1Hash property.
    pub sha1_hash: Option<String>,
    /// sha256Hash property.
    pub sha256_hash: Option<String>,
    /// timestamp property.
    pub timestamp: Option<String>,
    /// token property.
    pub token: Option<String>,
}

/// `ReportType` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ReportType {
    /// deprecateTime property.
    pub deprecate_time: Option<String>,
    /// id property.
    pub id: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// systemManaged property.
    pub system_managed: Option<bool>,
}

/// `GdataDiffUploadResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GdataDiffUploadResponse {
    /// objectVersion property.
    pub object_version: Option<String>,
    /// originalObject property.
    pub original_object: Option<GdataCompositeMedia>,
}

/// `ListReportsResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ListReportsResponse {
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// reports property.
    pub reports: Option<Vec<Report>>,
}

/// `ListJobsResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ListJobsResponse {
    /// jobs property.
    pub jobs: Option<Vec<Job>>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
}

/// `GdataDiffDownloadResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GdataDiffDownloadResponse {
    /// objectLocation property.
    pub object_location: Option<GdataCompositeMedia>,
}

/// `GdataDiffVersionResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GdataDiffVersionResponse {
    /// objectSizeBytes property.
    pub object_size_bytes: Option<String>,
    /// objectVersion property.
    pub object_version: Option<String>,
}

/// `GdataDownloadParameters` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GdataDownloadParameters {
    /// allowGzipCompression property.
    pub allow_gzip_compression: Option<bool>,
    /// ignoreRange property.
    pub ignore_range: Option<bool>,
}

/// `GdataObjectId` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GdataObjectId {
    /// bucketName property.
    pub bucket_name: Option<String>,
    /// generation property.
    pub generation: Option<String>,
    /// objectName property.
    pub object_name: Option<String>,
}

/// `GdataBlobstore2Info` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GdataBlobstore2Info {
    /// blobGeneration property.
    pub blob_generation: Option<String>,
    /// blobId property.
    pub blob_id: Option<String>,
    /// downloadExternalReadToken property.
    pub download_external_read_token: Option<String>,
    /// downloadReadHandle property.
    pub download_read_handle: Option<String>,
    /// readToken property.
    pub read_token: Option<String>,
    /// uploadFragmentListCreationInfo property.
    pub upload_fragment_list_creation_info: Option<String>,
    /// uploadMetadataContainer property.
    pub upload_metadata_container: Option<String>,
}

/// `GdataContentTypeInfo` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GdataContentTypeInfo {
    /// bestGuess property.
    pub best_guess: Option<String>,
    /// fromBytes property.
    pub from_bytes: Option<String>,
    /// fromFileName property.
    pub from_file_name: Option<String>,
    /// fromHeader property.
    pub from_header: Option<String>,
    /// fromUrlPath property.
    pub from_url_path: Option<String>,
}

/// `ListReportTypesResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ListReportTypesResponse {
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// reportTypes property.
    pub report_types: Option<Vec<ReportType>>,
}

/// `GdataCompositeMedia` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GdataCompositeMedia {
    /// blobRef property.
    pub blob_ref: Option<String>,
    /// blobstore2Info property.
    pub blobstore2_info: Option<GdataBlobstore2Info>,
    /// cosmoBinaryReference property.
    pub cosmo_binary_reference: Option<String>,
    /// crc32cHash property.
    pub crc32c_hash: Option<i64>,
    /// inline property.
    pub inline: Option<String>,
    /// length property.
    pub length: Option<String>,
    /// md5Hash property.
    pub md5_hash: Option<String>,
    /// objectId property.
    pub object_id: Option<GdataObjectId>,
    /// path property.
    pub path: Option<String>,
    /// referenceType property.
    pub reference_type: Option<String>,
    /// sha1Hash property.
    pub sha1_hash: Option<String>,
}

/// `GdataDiffChecksumsResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GdataDiffChecksumsResponse {
    /// checksumsLocation property.
    pub checksums_location: Option<GdataCompositeMedia>,
    /// chunkSizeBytes property.
    pub chunk_size_bytes: Option<String>,
    /// objectLocation property.
    pub object_location: Option<GdataCompositeMedia>,
    /// objectSizeBytes property.
    pub object_size_bytes: Option<String>,
    /// objectVersion property.
    pub object_version: Option<String>,
}

/// `GdataDiffUploadRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GdataDiffUploadRequest {
    /// checksumsInfo property.
    pub checksums_info: Option<GdataCompositeMedia>,
    /// objectInfo property.
    pub object_info: Option<GdataCompositeMedia>,
    /// objectVersion property.
    pub object_version: Option<String>,
}

// =============================================================================
// ARGS TYPES (per-endpoint)
// =============================================================================

/// Arguments for [`youtubereporting.jobs.create_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct YoutubereportingJobsCreateArgs {
    /// Query parameter: `onBehalfOfContentOwner`.
    pub on_behalf_of_content_owner: Option<String>,
    /// Request body.
    pub body: Job,
}

/// Arguments for [`youtubereporting.jobs.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct YoutubereportingJobsDeleteArgs {
    /// Path parameter: `jobId`.
    pub job_id: String,
    /// Query parameter: `onBehalfOfContentOwner`.
    pub on_behalf_of_content_owner: Option<String>,
}

/// Arguments for [`youtubereporting.jobs.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct YoutubereportingJobsGetArgs {
    /// Path parameter: `jobId`.
    pub job_id: String,
    /// Query parameter: `onBehalfOfContentOwner`.
    pub on_behalf_of_content_owner: Option<String>,
}

/// Arguments for [`youtubereporting.jobs.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct YoutubereportingJobsListArgs {
    /// Query parameter: `includeSystemManaged`.
    pub include_system_managed: Option<String>,
    /// Query parameter: `onBehalfOfContentOwner`.
    pub on_behalf_of_content_owner: Option<String>,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`youtubereporting.jobs.reports.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct YoutubereportingJobsReportsGetArgs {
    /// Path parameter: `jobId`.
    pub job_id: String,
    /// Path parameter: `reportId`.
    pub report_id: String,
    /// Query parameter: `onBehalfOfContentOwner`.
    pub on_behalf_of_content_owner: Option<String>,
}

/// Arguments for [`youtubereporting.jobs.reports.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct YoutubereportingJobsReportsListArgs {
    /// Path parameter: `jobId`.
    pub job_id: String,
    /// Query parameter: `createdAfter`.
    pub created_after: Option<String>,
    /// Query parameter: `onBehalfOfContentOwner`.
    pub on_behalf_of_content_owner: Option<String>,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `startTimeAtOrAfter`.
    pub start_time_at_or_after: Option<String>,
    /// Query parameter: `startTimeBefore`.
    pub start_time_before: Option<String>,
}

/// Arguments for [`youtubereporting.media.download_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct YoutubereportingMediaDownloadArgs {
    /// Path parameter: `resourceName`.
    pub resource_name: String,
}

/// Arguments for [`youtubereporting.reportTypes.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct YoutubereportingReportTypesListArgs {
    /// Query parameter: `includeSystemManaged`.
    pub include_system_managed: Option<String>,
    /// Query parameter: `onBehalfOfContentOwner`.
    pub on_behalf_of_content_owner: Option<String>,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

// =============================================================================
// CLIENT FUNCTIONS (per-endpoint)
// =============================================================================

// -----------------------------------------------------------------------------
// POST v1/jobs
// -----------------------------------------------------------------------------

/// POST v1/jobs.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = youtubereporting_jobs_create_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn youtubereporting_jobs_create_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &YoutubereportingJobsCreateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Job>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://youtubereporting.googleapis.com/v1/jobs",);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.on_behalf_of_content_owner {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("onBehalfOfContentOwner=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Job = serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                    super::shared::ApiError::ParseFailed(e.to_string())
                })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE v1/jobs/{jobId}
// -----------------------------------------------------------------------------

/// DELETE v1/jobs/{jobId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = youtubereporting_jobs_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn youtubereporting_jobs_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &YoutubereportingJobsDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://youtubereporting.googleapis.com/v1/jobs/{}",
        args.job_id,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.on_behalf_of_content_owner {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("onBehalfOfContentOwner=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Empty =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/jobs/{jobId}
// -----------------------------------------------------------------------------

/// GET v1/jobs/{jobId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = youtubereporting_jobs_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn youtubereporting_jobs_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &YoutubereportingJobsGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Job>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://youtubereporting.googleapis.com/v1/jobs/{}",
        args.job_id,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.on_behalf_of_content_owner {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("onBehalfOfContentOwner=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Job = serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                    super::shared::ApiError::ParseFailed(e.to_string())
                })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/jobs
// -----------------------------------------------------------------------------

/// GET v1/jobs.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = youtubereporting_jobs_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn youtubereporting_jobs_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &YoutubereportingJobsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListJobsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://youtubereporting.googleapis.com/v1/jobs",);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.include_system_managed {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("includeSystemManaged=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.on_behalf_of_content_owner {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("onBehalfOfContentOwner=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListJobsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/jobs/{jobId}/reports/{reportId}
// -----------------------------------------------------------------------------

/// GET v1/jobs/{jobId}/reports/{reportId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = youtubereporting_jobs_reports_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn youtubereporting_jobs_reports_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &YoutubereportingJobsReportsGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Report>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://youtubereporting.googleapis.com/v1/jobs/{}/reports/{}",
        args.job_id, args.report_id,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.on_behalf_of_content_owner {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("onBehalfOfContentOwner=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Report =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/jobs/{jobId}/reports
// -----------------------------------------------------------------------------

/// GET v1/jobs/{jobId}/reports.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = youtubereporting_jobs_reports_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn youtubereporting_jobs_reports_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &YoutubereportingJobsReportsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListReportsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://youtubereporting.googleapis.com/v1/jobs/{}/reports",
        args.job_id,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.created_after {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("createdAfter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.on_behalf_of_content_owner {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("onBehalfOfContentOwner=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.start_time_at_or_after {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("startTimeAtOrAfter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.start_time_before {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("startTimeBefore=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListReportsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/media/{+resourceName}
// -----------------------------------------------------------------------------

/// GET v1/media/{+resourceName}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = youtubereporting_media_download_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn youtubereporting_media_download_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &YoutubereportingMediaDownloadArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GdataMedia>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://youtubereporting.googleapis.com/v1/media/{}",
        args.resource_name,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GdataMedia =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/reportTypes
// -----------------------------------------------------------------------------

/// GET v1/reportTypes.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = youtubereporting_report_types_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn youtubereporting_report_types_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &YoutubereportingReportTypesListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListReportTypesResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://youtubereporting.googleapis.com/v1/reportTypes",);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.include_system_managed {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("includeSystemManaged=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.on_behalf_of_content_owner {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("onBehalfOfContentOwner=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListReportTypesResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}
