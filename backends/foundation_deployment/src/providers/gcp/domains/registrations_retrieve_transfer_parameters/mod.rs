//! Auto-generated API module for gcp/domains registrations:retrieveTransferParameters.
//!
//! Generated by `cargo run --bin ewe_platform gen_api`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp_domains_registrations_retrieve_transfer_parameters `

#![cfg(feature = "gcp_domains_registrations_retrieve_transfer_parameters")]
#![allow(clippy::too_many_arguments, clippy::type_complexity)]
#![allow(
    clippy::missing_errors_doc,
    clippy::doc_markdown,
    clippy::useless_format
)]
#![allow(unused_imports)]

use foundation_core::valtron::{TaskIterator, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{ClientRequestBuilder, SimpleHttpClient};
use foundation_macros::JsonHash;
use serde::{Deserialize, Serialize};

use super::shared::ApiResponse;

// =============================================================================
// TYPE DECLARATIONS
// =============================================================================

/// `Money` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Money {
    /// currencyCode property.
    pub currency_code: Option<String>,
    /// nanos property.
    pub nanos: Option<i64>,
    /// units property.
    pub units: Option<String>,
}

/// `RetrieveTransferParametersResponse` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct RetrieveTransferParametersResponse {
    /// transferParameters property.
    pub transfer_parameters: Option<TransferParameters>,
}

/// `TransferParameters` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct TransferParameters {
    /// currentRegistrar property.
    pub current_registrar: Option<String>,
    /// currentRegistrarUri property.
    pub current_registrar_uri: Option<String>,
    /// domainName property.
    pub domain_name: Option<String>,
    /// nameServers property.
    pub name_servers: Option<Vec<String>>,
    /// supportedPrivacy property.
    pub supported_privacy: Option<Vec<String>>,
    /// transferLockState property.
    pub transfer_lock_state: Option<String>,
    /// yearlyPrice property.
    pub yearly_price: Option<Money>,
}

// =============================================================================
// ARGS TYPES (per-endpoint)
// =============================================================================

/// Arguments for [`domains.projects.locations.registrations.retrieveTransferParameters_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DomainsProjectsLocationsRegistrationsRetrieveTransferParametersArgs {
    /// Path parameter: `location`.
    pub location: String,
    /// Query parameter: `domainName`.
    pub domain_name: Option<String>,
}

// =============================================================================
// CLIENT FUNCTIONS (per-endpoint)
// =============================================================================

// -----------------------------------------------------------------------------
// GET v1/projects/{projectsId}/locations/{locationsId}/registrations:retrieveTransferParameters
// -----------------------------------------------------------------------------

/// GET v1/projects/{projectsId}/locations/{locationsId}/registrations:retrieveTransferParameters.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = domains_projects_locations_registrations_retrieve_transfer_parameters_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn domains_projects_locations_registrations_retrieve_transfer_parameters_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<RetrieveTransferParametersResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://domains.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/registrations:retrieveTransferParameters",
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: RetrieveTransferParametersResponse = serde_json::from_str(&body)
                    .map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}
