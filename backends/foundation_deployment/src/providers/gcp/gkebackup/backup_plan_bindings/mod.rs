//! Auto-generated API module for gcp/gkebackup backupPlanBindings.
//!
//! Generated by `cargo run --bin ewe_platform gen_api`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp_gkebackup_backup_plan_bindings `

#![cfg(feature = "gcp_gkebackup_backup_plan_bindings")]
#![allow(clippy::too_many_arguments, clippy::type_complexity)]
#![allow(
    clippy::missing_errors_doc,
    clippy::doc_markdown,
    clippy::useless_format
)]

use foundation_core::valtron::{execute, StreamIterator, TaskIterator, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{ClientRequestBuilder, SimpleHttpClient};
use foundation_macros::JsonHash;
use serde::{Deserialize, Serialize};

use super::shared::{ApiError, ApiPending, ApiResponse};

// =============================================================================
// TYPE DECLARATIONS
// =============================================================================

/// `BackupPlanBinding` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct BackupPlanBinding {
    /// backupPlan property.
    pub backup_plan: Option<String>,
    /// backupPlanDetails property.
    pub backup_plan_details: Option<BackupPlanDetails>,
    /// cluster property.
    pub cluster: Option<String>,
    /// createTime property.
    pub create_time: Option<String>,
    /// etag property.
    pub etag: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// uid property.
    pub uid: Option<String>,
    /// updateTime property.
    pub update_time: Option<String>,
}

/// `BackupConfigDetails` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct BackupConfigDetails {
    /// allNamespaces property.
    pub all_namespaces: Option<bool>,
    /// encryptionKey property.
    pub encryption_key: Option<EncryptionKey>,
    /// includeSecrets property.
    pub include_secrets: Option<bool>,
    /// includeVolumeData property.
    pub include_volume_data: Option<bool>,
    /// selectedApplications property.
    pub selected_applications: Option<NamespacedNames>,
    /// selectedNamespaces property.
    pub selected_namespaces: Option<Namespaces>,
}

/// `EncryptionKey` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct EncryptionKey {
    /// gcpKmsEncryptionKey property.
    pub gcp_kms_encryption_key: Option<String>,
}

/// `NamespacedNames` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct NamespacedNames {
    /// namespacedNames property.
    pub namespaced_names: Option<Vec<NamespacedName>>,
}

/// `Namespaces` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Namespaces {
    /// namespaces property.
    pub namespaces: Option<Vec<String>>,
}

/// `ListBackupPlanBindingsResponse` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ListBackupPlanBindingsResponse {
    /// backupPlanBindings property.
    pub backup_plan_bindings: Option<Vec<BackupPlanBinding>>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// unreachable property.
    pub unreachable: Option<Vec<String>>,
}

/// `BackupPlanDetails` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct BackupPlanDetails {
    /// backupConfigDetails property.
    pub backup_config_details: Option<BackupConfigDetails>,
    /// lastSuccessfulBackup property.
    pub last_successful_backup: Option<String>,
    /// lastSuccessfulBackupTime property.
    pub last_successful_backup_time: Option<String>,
    /// nextScheduledBackupTime property.
    pub next_scheduled_backup_time: Option<String>,
    /// protectedPodCount property.
    pub protected_pod_count: Option<i64>,
    /// retentionPolicyDetails property.
    pub retention_policy_details: Option<RetentionPolicyDetails>,
    /// rpoRiskLevel property.
    pub rpo_risk_level: Option<i64>,
    /// state property.
    pub state: Option<String>,
}

/// `RetentionPolicyDetails` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct RetentionPolicyDetails {
    /// backupDeleteLockDays property.
    pub backup_delete_lock_days: Option<i64>,
    /// backupRetainDays property.
    pub backup_retain_days: Option<i64>,
}

/// `NamespacedName` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct NamespacedName {
    /// name property.
    pub name: Option<String>,
    /// namespace property.
    pub namespace: Option<String>,
}

// =============================================================================
// ARGS TYPES (per-endpoint)
// =============================================================================

/// Arguments for [`gkebackup.projects.locations.backupChannels.backupPlanBindings.get_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GkebackupProjectsLocationsBackupChannelsBackupPlanBindingsGetArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`gkebackup.projects.locations.backupChannels.backupPlanBindings.list_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GkebackupProjectsLocationsBackupChannelsBackupPlanBindingsListArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

// =============================================================================
// CLIENT FUNCTIONS (per-endpoint)
// =============================================================================

// -----------------------------------------------------------------------------
// GET v1/projects/{projectsId}/locations/{locationsId}/backupChannels/{backupChannelsId}/backupPlanBindings/{backupPlanBindingsId}
// -----------------------------------------------------------------------------

/// GET v1/projects/{projectsId}/locations/{locationsId}/backupChannels/{backupChannelsId}/backupPlanBindings/{backupPlanBindingsId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = gkebackup_projects_locations_backup_channels_backup_plan_bindings_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn gkebackup_projects_locations_backup_channels_backup_plan_bindings_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<BackupPlanBinding>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://gkebackup.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/backupChannels/{{backupChannelsId}}/backupPlanBindings/{{backupPlanBindingsId}}",
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: BackupPlanBinding =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/projects/{projectsId}/locations/{locationsId}/backupChannels/{backupChannelsId}/backupPlanBindings
// -----------------------------------------------------------------------------

/// GET v1/projects/{projectsId}/locations/{locationsId}/backupChannels/{backupChannelsId}/backupPlanBindings.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = gkebackup_projects_locations_backup_channels_backup_plan_bindings_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn gkebackup_projects_locations_backup_channels_backup_plan_bindings_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListBackupPlanBindingsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://gkebackup.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/backupChannels/{{backupChannelsId}}/backupPlanBindings",
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListBackupPlanBindingsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}
