//! Auto-generated API module for gcp/mybusinessverifications VoiceOfMerchantState.
//!
//! Generated by `cargo run --bin ewe_platform gen_api`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp_mybusinessverifications_voice_of_merchant_state `

#![cfg(feature = "gcp_mybusinessverifications_voice_of_merchant_state")]
#![allow(clippy::too_many_arguments, clippy::type_complexity)]
#![allow(
    clippy::missing_errors_doc,
    clippy::doc_markdown,
    clippy::useless_format
)]
#![allow(unused_imports)]

use foundation_core::valtron::{TaskIterator, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{ClientRequestBuilder, SimpleHttpClient};
use foundation_macros::JsonHash;
use serde::{Deserialize, Serialize};

use super::shared::ApiResponse;

// =============================================================================
// TYPE DECLARATIONS
// =============================================================================

/// `ResolveOwnershipConflict` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ResolveOwnershipConflict {}

/// `Verify` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Verify {
    /// hasPendingVerification property.
    pub has_pending_verification: Option<bool>,
}

/// `WaitForVoiceOfMerchant` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct WaitForVoiceOfMerchant {}

/// `ComplyWithGuidelines` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ComplyWithGuidelines {
    /// recommendationReason property.
    pub recommendation_reason: Option<String>,
}

/// `VoiceOfMerchantState` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct VoiceOfMerchantState {
    /// complyWithGuidelines property.
    pub comply_with_guidelines: Option<ComplyWithGuidelines>,
    /// hasBusinessAuthority property.
    pub has_business_authority: Option<bool>,
    /// hasVoiceOfMerchant property.
    pub has_voice_of_merchant: Option<bool>,
    /// resolveOwnershipConflict property.
    pub resolve_ownership_conflict: Option<ResolveOwnershipConflict>,
    /// verify property.
    pub verify: Option<Verify>,
    /// waitForVoiceOfMerchant property.
    pub wait_for_voice_of_merchant: Option<WaitForVoiceOfMerchant>,
}

// =============================================================================
// ARGS TYPES (per-endpoint)
// =============================================================================

/// Arguments for [`mybusinessverifications.locations.getVoiceOfMerchantState_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MybusinessverificationsLocationsGetVoiceOfMerchantStateArgs {
    /// Path parameter: `name`.
    pub name: String,
}

// =============================================================================
// CLIENT FUNCTIONS (per-endpoint)
// =============================================================================

// -----------------------------------------------------------------------------
// GET v1/locations/{locationsId}/VoiceOfMerchantState
// -----------------------------------------------------------------------------

/// GET v1/locations/{locationsId}/VoiceOfMerchantState.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = mybusinessverifications_locations_get_voice_of_merchant_state_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn mybusinessverifications_locations_get_voice_of_merchant_state_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<VoiceOfMerchantState>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://mybusinessverifications.googleapis.com/v1/locations/{{locationsId}}/VoiceOfMerchantState",
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: VoiceOfMerchantState =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}
