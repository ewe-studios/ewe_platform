//! Auto-generated API module for gcp/monitoring serviceLevelObjectives.
//!
//! Generated by `cargo run --bin ewe_platform gen_api`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp_monitoring_service_level_objectives `

#![cfg(feature = "gcp_monitoring_service_level_objectives")]
#![allow(clippy::too_many_arguments, clippy::type_complexity)]
#![allow(
    clippy::missing_errors_doc,
    clippy::doc_markdown,
    clippy::useless_format
)]

use foundation_core::valtron::{execute, StreamIterator, TaskIterator, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{ClientRequestBuilder, SimpleHttpClient};
use foundation_macros::JsonHash;
use serde::{Deserialize, Serialize};

// Import shared types used by this module
use super::shared::Empty;

use super::shared::{ApiError, ApiPending, ApiResponse};

// =============================================================================
// TYPE DECLARATIONS
// =============================================================================

/// `WindowsBasedSli` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct WindowsBasedSli {
    /// goodBadMetricFilter property.
    pub good_bad_metric_filter: Option<String>,
    /// goodTotalRatioThreshold property.
    pub good_total_ratio_threshold: Option<PerformanceThreshold>,
    /// metricMeanInRange property.
    pub metric_mean_in_range: Option<MetricRange>,
    /// metricSumInRange property.
    pub metric_sum_in_range: Option<MetricRange>,
    /// windowPeriod property.
    pub window_period: Option<String>,
}

/// `AvailabilityCriteria` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct AvailabilityCriteria {}

/// `ServiceLevelObjective` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ServiceLevelObjective {
    /// calendarPeriod property.
    pub calendar_period: Option<String>,
    /// displayName property.
    pub display_name: Option<String>,
    /// goal property.
    pub goal: Option<f64>,
    /// name property.
    pub name: Option<String>,
    /// rollingPeriod property.
    pub rolling_period: Option<String>,
    /// serviceLevelIndicator property.
    pub service_level_indicator: Option<ServiceLevelIndicator>,
    /// userLabels property.
    pub user_labels: Option<serde_json::Value>,
}

/// `LatencyCriteria` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct LatencyCriteria {
    /// threshold property.
    pub threshold: Option<String>,
}

/// `ListServiceLevelObjectivesResponse` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ListServiceLevelObjectivesResponse {
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// serviceLevelObjectives property.
    pub service_level_objectives: Option<Vec<ServiceLevelObjective>>,
}

/// `ServiceLevelIndicator` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ServiceLevelIndicator {
    /// basicSli property.
    pub basic_sli: Option<BasicSli>,
    /// requestBased property.
    pub request_based: Option<RequestBasedSli>,
    /// windowsBased property.
    pub windows_based: Option<WindowsBasedSli>,
}

/// `RequestBasedSli` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct RequestBasedSli {
    /// distributionCut property.
    pub distribution_cut: Option<DistributionCut>,
    /// goodTotalRatio property.
    pub good_total_ratio: Option<TimeSeriesRatio>,
}

/// `MetricRange` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct MetricRange {
    /// range property.
    pub range: Option<GoogleMonitoringV3Range>,
    /// timeSeries property.
    pub time_series: Option<String>,
}

/// `TimeSeriesRatio` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct TimeSeriesRatio {
    /// badServiceFilter property.
    pub bad_service_filter: Option<String>,
    /// goodServiceFilter property.
    pub good_service_filter: Option<String>,
    /// totalServiceFilter property.
    pub total_service_filter: Option<String>,
}

/// `BasicSli` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct BasicSli {
    /// availability property.
    pub availability: Option<AvailabilityCriteria>,
    /// latency property.
    pub latency: Option<LatencyCriteria>,
    /// location property.
    pub location: Option<Vec<String>>,
    /// method property.
    pub method: Option<Vec<String>>,
    /// version property.
    pub version: Option<Vec<String>>,
}

/// `PerformanceThreshold` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct PerformanceThreshold {
    /// basicSliPerformance property.
    pub basic_sli_performance: Option<BasicSli>,
    /// performance property.
    pub performance: Option<RequestBasedSli>,
    /// threshold property.
    pub threshold: Option<f64>,
}

/// `GoogleMonitoringV3Range` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct GoogleMonitoringV3Range {
    /// max property.
    pub max: Option<f64>,
    /// min property.
    pub min: Option<f64>,
}

/// `DistributionCut` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct DistributionCut {
    /// distributionFilter property.
    pub distribution_filter: Option<String>,
    /// range property.
    pub range: Option<GoogleMonitoringV3Range>,
}

// =============================================================================
// ARGS TYPES (per-endpoint)
// =============================================================================

/// Arguments for [`monitoring.services.serviceLevelObjectives.create_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MonitoringServicesServiceLevelObjectivesCreateArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `serviceLevelObjectiveId`.
    pub service_level_objective_id: Option<String>,
}

/// Arguments for [`monitoring.services.serviceLevelObjectives.delete_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MonitoringServicesServiceLevelObjectivesDeleteArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`monitoring.services.serviceLevelObjectives.get_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MonitoringServicesServiceLevelObjectivesGetArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `view`.
    pub view: Option<String>,
}

/// Arguments for [`monitoring.services.serviceLevelObjectives.list_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MonitoringServicesServiceLevelObjectivesListArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `view`.
    pub view: Option<String>,
}

/// Arguments for [`monitoring.services.serviceLevelObjectives.patch_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MonitoringServicesServiceLevelObjectivesPatchArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `updateMask`.
    pub update_mask: Option<String>,
}

// =============================================================================
// CLIENT FUNCTIONS (per-endpoint)
// =============================================================================

// -----------------------------------------------------------------------------
// POST v3/{v3Id}/{v3Id1}/services/{servicesId}/serviceLevelObjectives
// -----------------------------------------------------------------------------

/// POST v3/{v3Id}/{v3Id1}/services/{servicesId}/serviceLevelObjectives.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = monitoring_services_service_level_objectives_create_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn monitoring_services_service_level_objectives_create_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ServiceLevelObjective>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://monitoring.googleapis.com/v3/{{v3Id}}/{{v3Id1}}/services/{{servicesId}}/serviceLevelObjectives",
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ServiceLevelObjective =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE v3/{v3Id}/{v3Id1}/services/{servicesId}/serviceLevelObjectives/{serviceLevelObjectivesId}
// -----------------------------------------------------------------------------

/// DELETE v3/{v3Id}/{v3Id1}/services/{servicesId}/serviceLevelObjectives/{serviceLevelObjectivesId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = monitoring_services_service_level_objectives_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn monitoring_services_service_level_objectives_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://monitoring.googleapis.com/v3/{{v3Id}}/{{v3Id1}}/services/{{servicesId}}/serviceLevelObjectives/{{serviceLevelObjectivesId}}",
    );

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Empty =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v3/{v3Id}/{v3Id1}/services/{servicesId}/serviceLevelObjectives/{serviceLevelObjectivesId}
// -----------------------------------------------------------------------------

/// GET v3/{v3Id}/{v3Id1}/services/{servicesId}/serviceLevelObjectives/{serviceLevelObjectivesId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = monitoring_services_service_level_objectives_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn monitoring_services_service_level_objectives_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ServiceLevelObjective>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://monitoring.googleapis.com/v3/{{v3Id}}/{{v3Id1}}/services/{{servicesId}}/serviceLevelObjectives/{{serviceLevelObjectivesId}}",
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ServiceLevelObjective =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v3/{v3Id}/{v3Id1}/services/{servicesId}/serviceLevelObjectives
// -----------------------------------------------------------------------------

/// GET v3/{v3Id}/{v3Id1}/services/{servicesId}/serviceLevelObjectives.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = monitoring_services_service_level_objectives_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn monitoring_services_service_level_objectives_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<ListServiceLevelObjectivesResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://monitoring.googleapis.com/v3/{{v3Id}}/{{v3Id1}}/services/{{servicesId}}/serviceLevelObjectives",
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListServiceLevelObjectivesResponse = serde_json::from_str(&body)
                    .map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH v3/{v3Id}/{v3Id1}/services/{servicesId}/serviceLevelObjectives/{serviceLevelObjectivesId}
// -----------------------------------------------------------------------------

/// PATCH v3/{v3Id}/{v3Id1}/services/{servicesId}/serviceLevelObjectives/{serviceLevelObjectivesId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = monitoring_services_service_level_objectives_patch_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn monitoring_services_service_level_objectives_patch_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ServiceLevelObjective>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://monitoring.googleapis.com/v3/{{v3Id}}/{{v3Id1}}/services/{{servicesId}}/serviceLevelObjectives/{{serviceLevelObjectivesId}}",
    );

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ServiceLevelObjective =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}
