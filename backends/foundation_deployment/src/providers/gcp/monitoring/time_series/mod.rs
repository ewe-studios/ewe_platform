//! Auto-generated API module for gcp/monitoring timeSeries.
//!
//! Generated by `cargo run --bin ewe_platform gen_api`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp_monitoring_time_series `

#![cfg(feature = "gcp_monitoring_time_series")]
#![allow(clippy::too_many_arguments, clippy::type_complexity)]
#![allow(
    clippy::missing_errors_doc,
    clippy::doc_markdown,
    clippy::useless_format
)]

use foundation_core::valtron::{execute, StreamIterator, TaskIterator, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{ClientRequestBuilder, SimpleHttpClient};
use foundation_macros::JsonHash;
use serde::{Deserialize, Serialize};

// Import shared types used by this module
use super::shared::Empty;

use super::shared::{ApiError, ApiPending, ApiResponse};

// =============================================================================
// TYPE DECLARATIONS
// =============================================================================

/// `Explicit` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Explicit {
    /// bounds property.
    pub bounds: Option<Vec<f64>>,
}

/// `Distribution` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Distribution {
    /// bucketCounts property.
    pub bucket_counts: Option<Vec<String>>,
    /// bucketOptions property.
    pub bucket_options: Option<BucketOptions>,
    /// count property.
    pub count: Option<String>,
    /// exemplars property.
    pub exemplars: Option<Vec<Exemplar>>,
    /// mean property.
    pub mean: Option<f64>,
    /// range property.
    pub range: Option<Range>,
    /// sumOfSquaredDeviation property.
    pub sum_of_squared_deviation: Option<f64>,
}

/// `Linear` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Linear {
    /// numFiniteBuckets property.
    pub num_finite_buckets: Option<i64>,
    /// offset property.
    pub offset: Option<f64>,
    /// width property.
    pub width: Option<f64>,
}

/// `Point` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Point {
    /// interval property.
    pub interval: Option<TimeInterval>,
    /// value property.
    pub value: Option<TypedValue>,
}

/// `BucketOptions` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct BucketOptions {
    /// explicitBuckets property.
    pub explicit_buckets: Option<Explicit>,
    /// exponentialBuckets property.
    pub exponential_buckets: Option<Exponential>,
    /// linearBuckets property.
    pub linear_buckets: Option<Linear>,
}

/// `TimeSeries` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct TimeSeries {
    /// description property.
    pub description: Option<String>,
    /// metadata property.
    pub metadata: Option<MonitoredResourceMetadata>,
    /// metric property.
    pub metric: Option<Metric>,
    /// metricKind property.
    pub metric_kind: Option<String>,
    /// points property.
    pub points: Option<Vec<Point>>,
    /// resource property.
    pub resource: Option<MonitoredResource>,
    /// unit property.
    pub unit: Option<String>,
    /// valueType property.
    pub value_type: Option<String>,
}

/// `MonitoredResource` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct MonitoredResource {
    /// labels property.
    pub labels: Option<serde_json::Value>,
    /// type property.
    pub r#type: Option<String>,
}

/// `Exemplar` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Exemplar {
    /// attachments property.
    pub attachments: Option<Vec<serde_json::Value>>,
    /// timestamp property.
    pub timestamp: Option<String>,
    /// value property.
    pub value: Option<f64>,
}

/// `MonitoredResourceMetadata` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct MonitoredResourceMetadata {
    /// systemLabels property.
    pub system_labels: Option<serde_json::Value>,
    /// userLabels property.
    pub user_labels: Option<serde_json::Value>,
}

/// `Metric` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Metric {
    /// labels property.
    pub labels: Option<serde_json::Value>,
    /// type property.
    pub r#type: Option<String>,
}

/// `ListTimeSeriesResponse` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ListTimeSeriesResponse {
    /// executionErrors property.
    pub execution_errors: Option<Vec<Status>>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// timeSeries property.
    pub time_series: Option<Vec<TimeSeries>>,
    /// unit property.
    pub unit: Option<String>,
    /// unreachable property.
    pub unreachable: Option<Vec<String>>,
}

/// `Range` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Range {
    /// max property.
    pub max: Option<f64>,
    /// min property.
    pub min: Option<f64>,
}

/// `Exponential` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Exponential {
    /// growthFactor property.
    pub growth_factor: Option<f64>,
    /// numFiniteBuckets property.
    pub num_finite_buckets: Option<i64>,
    /// scale property.
    pub scale: Option<f64>,
}

/// `Status` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Status {
    /// code property.
    pub code: Option<i64>,
    /// details property.
    pub details: Option<Vec<serde_json::Value>>,
    /// message property.
    pub message: Option<String>,
}

/// `TimeInterval` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct TimeInterval {
    /// endTime property.
    pub end_time: Option<String>,
    /// startTime property.
    pub start_time: Option<String>,
}

/// `TypedValue` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct TypedValue {
    /// boolValue property.
    pub bool_value: Option<bool>,
    /// distributionValue property.
    pub distribution_value: Option<Distribution>,
    /// doubleValue property.
    pub double_value: Option<f64>,
    /// int64Value property.
    pub int64_value: Option<String>,
    /// stringValue property.
    pub string_value: Option<String>,
}

// =============================================================================
// ARGS TYPES (per-endpoint)
// =============================================================================

/// Arguments for [`monitoring.folders.timeSeries.list_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MonitoringFoldersTimeSeriesListArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `aggregation.alignmentPeriod`.
    pub aggregation_alignment_period: Option<String>,
    /// Query parameter: `aggregation.crossSeriesReducer`.
    pub aggregation_cross_series_reducer: Option<String>,
    /// Query parameter: `aggregation.groupByFields`.
    pub aggregation_group_by_fields: Option<String>,
    /// Query parameter: `aggregation.perSeriesAligner`.
    pub aggregation_per_series_aligner: Option<String>,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `interval.endTime`.
    pub interval_end_time: Option<String>,
    /// Query parameter: `interval.startTime`.
    pub interval_start_time: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `secondaryAggregation.alignmentPeriod`.
    pub secondary_aggregation_alignment_period: Option<String>,
    /// Query parameter: `secondaryAggregation.crossSeriesReducer`.
    pub secondary_aggregation_cross_series_reducer: Option<String>,
    /// Query parameter: `secondaryAggregation.groupByFields`.
    pub secondary_aggregation_group_by_fields: Option<String>,
    /// Query parameter: `secondaryAggregation.perSeriesAligner`.
    pub secondary_aggregation_per_series_aligner: Option<String>,
    /// Query parameter: `view`.
    pub view: Option<String>,
}

/// Arguments for [`monitoring.organizations.timeSeries.list_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MonitoringOrganizationsTimeSeriesListArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `aggregation.alignmentPeriod`.
    pub aggregation_alignment_period: Option<String>,
    /// Query parameter: `aggregation.crossSeriesReducer`.
    pub aggregation_cross_series_reducer: Option<String>,
    /// Query parameter: `aggregation.groupByFields`.
    pub aggregation_group_by_fields: Option<String>,
    /// Query parameter: `aggregation.perSeriesAligner`.
    pub aggregation_per_series_aligner: Option<String>,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `interval.endTime`.
    pub interval_end_time: Option<String>,
    /// Query parameter: `interval.startTime`.
    pub interval_start_time: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `secondaryAggregation.alignmentPeriod`.
    pub secondary_aggregation_alignment_period: Option<String>,
    /// Query parameter: `secondaryAggregation.crossSeriesReducer`.
    pub secondary_aggregation_cross_series_reducer: Option<String>,
    /// Query parameter: `secondaryAggregation.groupByFields`.
    pub secondary_aggregation_group_by_fields: Option<String>,
    /// Query parameter: `secondaryAggregation.perSeriesAligner`.
    pub secondary_aggregation_per_series_aligner: Option<String>,
    /// Query parameter: `view`.
    pub view: Option<String>,
}

/// Arguments for [`monitoring.projects.timeSeries.create_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MonitoringProjectsTimeSeriesCreateArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`monitoring.projects.timeSeries.list_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct MonitoringProjectsTimeSeriesListArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `aggregation.alignmentPeriod`.
    pub aggregation_alignment_period: Option<String>,
    /// Query parameter: `aggregation.crossSeriesReducer`.
    pub aggregation_cross_series_reducer: Option<String>,
    /// Query parameter: `aggregation.groupByFields`.
    pub aggregation_group_by_fields: Option<String>,
    /// Query parameter: `aggregation.perSeriesAligner`.
    pub aggregation_per_series_aligner: Option<String>,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `interval.endTime`.
    pub interval_end_time: Option<String>,
    /// Query parameter: `interval.startTime`.
    pub interval_start_time: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `secondaryAggregation.alignmentPeriod`.
    pub secondary_aggregation_alignment_period: Option<String>,
    /// Query parameter: `secondaryAggregation.crossSeriesReducer`.
    pub secondary_aggregation_cross_series_reducer: Option<String>,
    /// Query parameter: `secondaryAggregation.groupByFields`.
    pub secondary_aggregation_group_by_fields: Option<String>,
    /// Query parameter: `secondaryAggregation.perSeriesAligner`.
    pub secondary_aggregation_per_series_aligner: Option<String>,
    /// Query parameter: `view`.
    pub view: Option<String>,
}

// =============================================================================
// CLIENT FUNCTIONS (per-endpoint)
// =============================================================================

// -----------------------------------------------------------------------------
// GET v3/folders/{foldersId}/timeSeries
// -----------------------------------------------------------------------------

/// GET v3/folders/{foldersId}/timeSeries.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = monitoring_folders_time_series_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn monitoring_folders_time_series_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListTimeSeriesResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url =
        format!("https://monitoring.googleapis.com/v3/folders/{{foldersId}}/timeSeries",);

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListTimeSeriesResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v3/organizations/{organizationsId}/timeSeries
// -----------------------------------------------------------------------------

/// GET v3/organizations/{organizationsId}/timeSeries.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = monitoring_organizations_time_series_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn monitoring_organizations_time_series_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListTimeSeriesResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://monitoring.googleapis.com/v3/organizations/{{organizationsId}}/timeSeries",
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListTimeSeriesResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v3/projects/{projectsId}/timeSeries
// -----------------------------------------------------------------------------

/// POST v3/projects/{projectsId}/timeSeries.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = monitoring_projects_time_series_create_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn monitoring_projects_time_series_create_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url =
        format!("https://monitoring.googleapis.com/v3/projects/{{projectsId}}/timeSeries",);

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Empty =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v3/projects/{projectsId}/timeSeries
// -----------------------------------------------------------------------------

/// GET v3/projects/{projectsId}/timeSeries.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = monitoring_projects_time_series_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn monitoring_projects_time_series_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListTimeSeriesResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url =
        format!("https://monitoring.googleapis.com/v3/projects/{{projectsId}}/timeSeries",);

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListTimeSeriesResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}
