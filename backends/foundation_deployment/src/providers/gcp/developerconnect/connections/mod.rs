//! Auto-generated API module for gcp/developerconnect connections.
//!
//! Generated by `cargo run --bin ewe_platform gen_api`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp_developerconnect_connections `

#![cfg(feature = "gcp_developerconnect_connections")]
#![allow(clippy::too_many_arguments, clippy::type_complexity)]
#![allow(
    clippy::missing_errors_doc,
    clippy::doc_markdown,
    clippy::useless_format
)]

use foundation_core::valtron::{execute, StreamIterator, TaskIterator, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{ClientRequestBuilder, SimpleHttpClient};
use foundation_macros::JsonHash;
use serde::{Deserialize, Serialize};

// Import shared types used by this module
use super::shared::Operation;

use super::shared::{ApiError, ApiPending, ApiResponse};

// =============================================================================
// TYPE DECLARATIONS
// =============================================================================

/// `GitLabConfig` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct GitLabConfig {
    /// authorizerCredential property.
    pub authorizer_credential: Option<UserCredential>,
    /// readAuthorizerCredential property.
    pub read_authorizer_credential: Option<UserCredential>,
    /// webhookSecretSecretVersion property.
    pub webhook_secret_secret_version: Option<String>,
}

/// `UserCredential` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct UserCredential {
    /// userTokenSecretVersion property.
    pub user_token_secret_version: Option<String>,
    /// username property.
    pub username: Option<String>,
}

/// `OAuthCredential` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct OAuthCredential {
    /// oauthTokenSecretVersion property.
    pub oauth_token_secret_version: Option<String>,
    /// username property.
    pub username: Option<String>,
}

/// `ListConnectionsResponse` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ListConnectionsResponse {
    /// connections property.
    pub connections: Option<Vec<Connection>>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// unreachable property.
    pub unreachable: Option<Vec<String>>,
}

/// `GenericHTTPEndpointConfig` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct GenericHTTPEndpointConfig {
    /// basicAuthentication property.
    pub basic_authentication: Option<BasicAuthentication>,
    /// bearerTokenAuthentication property.
    pub bearer_token_authentication: Option<BearerTokenAuthentication>,
    /// hostUri property.
    pub host_uri: Option<String>,
    /// serviceDirectoryConfig property.
    pub service_directory_config: Option<ServiceDirectoryConfig>,
    /// sslCaCertificate property.
    pub ssl_ca_certificate: Option<String>,
}

/// `FetchLinkableGitRepositoriesResponse` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct FetchLinkableGitRepositoriesResponse {
    /// linkableGitRepositories property.
    pub linkable_git_repositories: Option<Vec<LinkableGitRepository>>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
}

/// `GitProxyConfig` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct GitProxyConfig {
    /// enabled property.
    pub enabled: Option<bool>,
    /// httpProxyBaseUri property.
    pub http_proxy_base_uri: Option<String>,
}

/// `CryptoKeyConfig` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct CryptoKeyConfig {
    /// keyReference property.
    pub key_reference: Option<String>,
}

/// `BitbucketDataCenterConfig` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct BitbucketDataCenterConfig {
    /// authorizerCredential property.
    pub authorizer_credential: Option<UserCredential>,
    /// hostUri property.
    pub host_uri: Option<String>,
    /// readAuthorizerCredential property.
    pub read_authorizer_credential: Option<UserCredential>,
    /// serverVersion property.
    pub server_version: Option<String>,
    /// serviceDirectoryConfig property.
    pub service_directory_config: Option<ServiceDirectoryConfig>,
    /// sslCaCertificate property.
    pub ssl_ca_certificate: Option<String>,
    /// webhookSecretSecretVersion property.
    pub webhook_secret_secret_version: Option<String>,
}

/// `ServiceDirectoryConfig` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ServiceDirectoryConfig {
    /// service property.
    pub service: Option<String>,
}

/// `FetchGitHubInstallationsResponse` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct FetchGitHubInstallationsResponse {
    /// installations property.
    pub installations: Option<Vec<Installation>>,
}

/// `GitHubConfig` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct GitHubConfig {
    /// appInstallationId property.
    pub app_installation_id: Option<String>,
    /// authorizerCredential property.
    pub authorizer_credential: Option<OAuthCredential>,
    /// githubApp property.
    pub github_app: Option<String>,
    /// installationUri property.
    pub installation_uri: Option<String>,
}

/// `InstallationState` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct InstallationState {
    /// actionUri property.
    pub action_uri: Option<String>,
    /// message property.
    pub message: Option<String>,
    /// stage property.
    pub stage: Option<String>,
}

/// `Installation` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Installation {
    /// id property.
    pub id: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// type property.
    pub r#type: Option<String>,
}

/// `BasicAuthentication` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct BasicAuthentication {
    /// passwordSecretVersion property.
    pub password_secret_version: Option<String>,
    /// username property.
    pub username: Option<String>,
}

/// `GitHubEnterpriseConfig` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct GitHubEnterpriseConfig {
    /// appId property.
    pub app_id: Option<String>,
    /// appInstallationId property.
    pub app_installation_id: Option<String>,
    /// appSlug property.
    pub app_slug: Option<String>,
    /// hostUri property.
    pub host_uri: Option<String>,
    /// installationUri property.
    pub installation_uri: Option<String>,
    /// organization property.
    pub organization: Option<String>,
    /// privateKeySecretVersion property.
    pub private_key_secret_version: Option<String>,
    /// serverVersion property.
    pub server_version: Option<String>,
    /// serviceDirectoryConfig property.
    pub service_directory_config: Option<ServiceDirectoryConfig>,
    /// sslCaCertificate property.
    pub ssl_ca_certificate: Option<String>,
    /// webhookSecretSecretVersion property.
    pub webhook_secret_secret_version: Option<String>,
}

/// `BitbucketCloudConfig` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct BitbucketCloudConfig {
    /// authorizerCredential property.
    pub authorizer_credential: Option<UserCredential>,
    /// readAuthorizerCredential property.
    pub read_authorizer_credential: Option<UserCredential>,
    /// webhookSecretSecretVersion property.
    pub webhook_secret_secret_version: Option<String>,
    /// workspace property.
    pub workspace: Option<String>,
}

/// `Status` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Status {
    /// code property.
    pub code: Option<i64>,
    /// details property.
    pub details: Option<Vec<serde_json::Value>>,
    /// message property.
    pub message: Option<String>,
}

/// `LinkableGitRepository` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct LinkableGitRepository {
    /// cloneUri property.
    pub clone_uri: Option<String>,
}

/// `Connection` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Connection {
    /// annotations property.
    pub annotations: Option<serde_json::Value>,
    /// bitbucketCloudConfig property.
    pub bitbucket_cloud_config: Option<BitbucketCloudConfig>,
    /// bitbucketDataCenterConfig property.
    pub bitbucket_data_center_config: Option<BitbucketDataCenterConfig>,
    /// createTime property.
    pub create_time: Option<String>,
    /// cryptoKeyConfig property.
    pub crypto_key_config: Option<CryptoKeyConfig>,
    /// deleteTime property.
    pub delete_time: Option<String>,
    /// disabled property.
    pub disabled: Option<bool>,
    /// etag property.
    pub etag: Option<String>,
    /// gitProxyConfig property.
    pub git_proxy_config: Option<GitProxyConfig>,
    /// githubConfig property.
    pub github_config: Option<GitHubConfig>,
    /// githubEnterpriseConfig property.
    pub github_enterprise_config: Option<GitHubEnterpriseConfig>,
    /// gitlabConfig property.
    pub gitlab_config: Option<GitLabConfig>,
    /// gitlabEnterpriseConfig property.
    pub gitlab_enterprise_config: Option<GitLabEnterpriseConfig>,
    /// httpConfig property.
    pub http_config: Option<GenericHTTPEndpointConfig>,
    /// installationState property.
    pub installation_state: Option<InstallationState>,
    /// labels property.
    pub labels: Option<serde_json::Value>,
    /// name property.
    pub name: Option<String>,
    /// reconciling property.
    pub reconciling: Option<bool>,
    /// secureSourceManagerInstanceConfig property.
    pub secure_source_manager_instance_config: Option<SecureSourceManagerInstanceConfig>,
    /// uid property.
    pub uid: Option<String>,
    /// updateTime property.
    pub update_time: Option<String>,
}

/// `SecureSourceManagerInstanceConfig` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct SecureSourceManagerInstanceConfig {
    /// instance property.
    pub instance: Option<String>,
}

/// `GitLabEnterpriseConfig` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct GitLabEnterpriseConfig {
    /// authorizerCredential property.
    pub authorizer_credential: Option<UserCredential>,
    /// hostUri property.
    pub host_uri: Option<String>,
    /// readAuthorizerCredential property.
    pub read_authorizer_credential: Option<UserCredential>,
    /// serverVersion property.
    pub server_version: Option<String>,
    /// serviceDirectoryConfig property.
    pub service_directory_config: Option<ServiceDirectoryConfig>,
    /// sslCaCertificate property.
    pub ssl_ca_certificate: Option<String>,
    /// webhookSecretSecretVersion property.
    pub webhook_secret_secret_version: Option<String>,
}

/// `BearerTokenAuthentication` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct BearerTokenAuthentication {
    /// tokenSecretVersion property.
    pub token_secret_version: Option<String>,
}

// =============================================================================
// ARGS TYPES (per-endpoint)
// =============================================================================

/// Arguments for [`developerconnect.projects.locations.connections.create_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DeveloperconnectProjectsLocationsConnectionsCreateArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `connectionId`.
    pub connection_id: Option<String>,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Query parameter: `validateOnly`.
    pub validate_only: Option<String>,
}

/// Arguments for [`developerconnect.projects.locations.connections.delete_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DeveloperconnectProjectsLocationsConnectionsDeleteArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `etag`.
    pub etag: Option<String>,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Query parameter: `validateOnly`.
    pub validate_only: Option<String>,
}

/// Arguments for [`developerconnect.projects.locations.connections.fetchGitHubInstallations_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DeveloperconnectProjectsLocationsConnectionsFetchGitHubInstallationsArgs {
    /// Path parameter: `connection`.
    pub connection: String,
}

/// Arguments for [`developerconnect.projects.locations.connections.fetchLinkableGitRepositories_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DeveloperconnectProjectsLocationsConnectionsFetchLinkableGitRepositoriesArgs {
    /// Path parameter: `connection`.
    pub connection: String,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`developerconnect.projects.locations.connections.get_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DeveloperconnectProjectsLocationsConnectionsGetArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`developerconnect.projects.locations.connections.list_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DeveloperconnectProjectsLocationsConnectionsListArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`developerconnect.projects.locations.connections.patch_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DeveloperconnectProjectsLocationsConnectionsPatchArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `allowMissing`.
    pub allow_missing: Option<String>,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Query parameter: `updateMask`.
    pub update_mask: Option<String>,
    /// Query parameter: `validateOnly`.
    pub validate_only: Option<String>,
}

// =============================================================================
// CLIENT FUNCTIONS (per-endpoint)
// =============================================================================

// -----------------------------------------------------------------------------
// POST v1/projects/{projectsId}/locations/{locationsId}/connections
// -----------------------------------------------------------------------------

/// POST v1/projects/{projectsId}/locations/{locationsId}/connections.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = developerconnect_projects_locations_connections_create_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn developerconnect_projects_locations_connections_create_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://developerconnect.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/connections",
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE v1/projects/{projectsId}/locations/{locationsId}/connections/{connectionsId}
// -----------------------------------------------------------------------------

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/connections/{connectionsId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = developerconnect_projects_locations_connections_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn developerconnect_projects_locations_connections_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://developerconnect.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/connections/{{connectionsId}}",
    );

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/projects/{projectsId}/locations/{locationsId}/connections/{connectionsId}:fetchGitHubInstallations
// -----------------------------------------------------------------------------

/// GET v1/projects/{projectsId}/locations/{locationsId}/connections/{connectionsId}:fetchGitHubInstallations.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = developerconnect_projects_locations_connections_fetch_git_hub_installations_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn developerconnect_projects_locations_connections_fetch_git_hub_installations_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<FetchGitHubInstallationsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://developerconnect.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/connections/{{connectionsId}}:fetchGitHubInstallations",
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: FetchGitHubInstallationsResponse = serde_json::from_str(&body)
                    .map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/projects/{projectsId}/locations/{locationsId}/connections/{connectionsId}:fetchLinkableGitRepositories
// -----------------------------------------------------------------------------

/// GET v1/projects/{projectsId}/locations/{locationsId}/connections/{connectionsId}:fetchLinkableGitRepositories.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = developerconnect_projects_locations_connections_fetch_linkable_git_repositories_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn developerconnect_projects_locations_connections_fetch_linkable_git_repositories_request<
    R,
    F,
>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<FetchLinkableGitRepositoriesResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://developerconnect.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/connections/{{connectionsId}}:fetchLinkableGitRepositories",
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: FetchLinkableGitRepositoriesResponse = serde_json::from_str(&body)
                    .map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/projects/{projectsId}/locations/{locationsId}/connections/{connectionsId}
// -----------------------------------------------------------------------------

/// GET v1/projects/{projectsId}/locations/{locationsId}/connections/{connectionsId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = developerconnect_projects_locations_connections_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn developerconnect_projects_locations_connections_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Connection>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://developerconnect.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/connections/{{connectionsId}}",
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Connection =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/projects/{projectsId}/locations/{locationsId}/connections
// -----------------------------------------------------------------------------

/// GET v1/projects/{projectsId}/locations/{locationsId}/connections.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = developerconnect_projects_locations_connections_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn developerconnect_projects_locations_connections_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListConnectionsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://developerconnect.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/connections",
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListConnectionsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH v1/projects/{projectsId}/locations/{locationsId}/connections/{connectionsId}
// -----------------------------------------------------------------------------

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/connections/{connectionsId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = developerconnect_projects_locations_connections_patch_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn developerconnect_projects_locations_connections_patch_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://developerconnect.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/connections/{{connectionsId}}",
    );

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}
