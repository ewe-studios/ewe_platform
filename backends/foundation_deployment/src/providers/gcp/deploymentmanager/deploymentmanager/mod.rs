//! Auto-generated API module for gcp/deploymentmanager deploymentmanager.
//!
//! Generated by `cargo run --bin ewe_platform gen_api`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp_deploymentmanager_deploymentmanager `

#![cfg(feature = "gcp_deploymentmanager_deploymentmanager")]
#![allow(clippy::too_many_arguments, clippy::type_complexity)]
#![allow(
    clippy::missing_errors_doc,
    clippy::doc_markdown,
    clippy::useless_format
)]
#![allow(unused_imports)]

use foundation_core::valtron::{TaskIterator, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{ClientRequestBuilder, SimpleHttpClient};
use foundation_macros::JsonHash;
use serde::{Deserialize, Serialize};

// Import shared types used by this module
use super::shared::Operation;

use super::shared::ApiResponse;

// =============================================================================
// TYPE DECLARATIONS
// =============================================================================

/// `Deployment` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Deployment {
    /// description property.
    pub description: Option<String>,
    /// fingerprint property.
    pub fingerprint: Option<String>,
    /// id property.
    pub id: Option<String>,
    /// insertTime property.
    pub insert_time: Option<String>,
    /// labels property.
    pub labels: Option<Vec<DeploymentLabelEntry>>,
    /// manifest property.
    pub manifest: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// operation property.
    pub operation: Option<Operation>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// target property.
    pub target: Option<TargetConfiguration>,
    /// update property.
    pub update: Option<DeploymentUpdate>,
    /// updateTime property.
    pub update_time: Option<String>,
}

/// `ResourceAccessControl` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ResourceAccessControl {
    /// gcpIamPolicy property.
    pub gcp_iam_policy: Option<String>,
}

/// `DeploymentLabelEntry` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct DeploymentLabelEntry {
    /// key property.
    pub key: Option<String>,
    /// value property.
    pub value: Option<String>,
}

/// `DeploymentsListResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct DeploymentsListResponse {
    /// deployments property.
    pub deployments: Option<Vec<Deployment>>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
}

/// `ResourceUpdate` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ResourceUpdate {
    /// accessControl property.
    pub access_control: Option<ResourceAccessControl>,
    /// error property.
    pub error: Option<std::collections::HashMap<String, serde_json::Value>>,
    /// finalProperties property.
    pub final_properties: Option<String>,
    /// intent property.
    pub intent: Option<String>,
    /// manifest property.
    pub manifest: Option<String>,
    /// properties property.
    pub properties: Option<String>,
    /// state property.
    pub state: Option<String>,
    /// warnings property.
    pub warnings: Option<Vec<std::collections::HashMap<String, serde_json::Value>>>,
}

/// `GetVersionOperationMetadataSbomInfo` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GetVersionOperationMetadataSbomInfo {
    /// currentComponentVersions property.
    pub current_component_versions: Option<serde_json::Value>,
    /// targetComponentVersions property.
    pub target_component_versions: Option<serde_json::Value>,
}

/// `TargetConfiguration` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct TargetConfiguration {
    /// config property.
    pub config: Option<ConfigFile>,
    /// imports property.
    pub imports: Option<Vec<ImportFile>>,
}

/// `Policy` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Policy {
    /// auditConfigs property.
    pub audit_configs: Option<Vec<AuditConfig>>,
    /// bindings property.
    pub bindings: Option<Vec<Binding>>,
    /// etag property.
    pub etag: Option<String>,
    /// version property.
    pub version: Option<i64>,
}

/// `OperationsListResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct OperationsListResponse {
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// operations property.
    pub operations: Option<Vec<Operation>>,
}

/// `TestPermissionsRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct TestPermissionsRequest {
    /// permissions property.
    pub permissions: Option<Vec<String>>,
}

/// `Expr` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Expr {
    /// description property.
    pub description: Option<String>,
    /// expression property.
    pub expression: Option<String>,
    /// location property.
    pub location: Option<String>,
    /// title property.
    pub title: Option<String>,
}

/// `ErrorInfo` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ErrorInfo {
    /// domain property.
    pub domain: Option<String>,
    /// metadatas property.
    pub metadatas: Option<serde_json::Value>,
    /// reason property.
    pub reason: Option<String>,
}

/// `ConfigFile` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ConfigFile {
    /// content property.
    pub content: Option<String>,
}

/// `Help` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Help {
    /// links property.
    pub links: Option<Vec<HelpLink>>,
}

/// `Binding` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Binding {
    /// condition property.
    pub condition: Option<Expr>,
    /// members property.
    pub members: Option<Vec<String>>,
    /// role property.
    pub role: Option<String>,
}

/// `QuotaExceededInfo` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct QuotaExceededInfo {
    /// dimensions property.
    pub dimensions: Option<serde_json::Value>,
    /// futureLimit property.
    pub future_limit: Option<f64>,
    /// limit property.
    pub limit: Option<f64>,
    /// limitName property.
    pub limit_name: Option<String>,
    /// metricName property.
    pub metric_name: Option<String>,
    /// rolloutStatus property.
    pub rollout_status: Option<String>,
}

/// `Type` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Type {
    /// id property.
    pub id: Option<String>,
    /// insertTime property.
    pub insert_time: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// operation property.
    pub operation: Option<Operation>,
    /// selfLink property.
    pub self_link: Option<String>,
}

/// `FirewallPolicyRuleOperationMetadata` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct FirewallPolicyRuleOperationMetadata {
    /// allocatedPriority property.
    pub allocated_priority: Option<i64>,
}

/// `GetVersionOperationMetadata` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GetVersionOperationMetadata {
    /// inlineSbomInfo property.
    pub inline_sbom_info: Option<GetVersionOperationMetadataSbomInfo>,
}

/// `SetCommonInstanceMetadataOperationMetadata` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SetCommonInstanceMetadataOperationMetadata {
    /// clientOperationId property.
    pub client_operation_id: Option<String>,
    /// perLocationOperations property.
    pub per_location_operations: Option<serde_json::Value>,
}

/// `DeploymentUpdate` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct DeploymentUpdate {
    /// description property.
    pub description: Option<String>,
    /// labels property.
    pub labels: Option<Vec<DeploymentUpdateLabelEntry>>,
    /// manifest property.
    pub manifest: Option<String>,
}

/// `Manifest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Manifest {
    /// config property.
    pub config: Option<ConfigFile>,
    /// expandedConfig property.
    pub expanded_config: Option<String>,
    /// id property.
    pub id: Option<String>,
    /// imports property.
    pub imports: Option<Vec<ImportFile>>,
    /// insertTime property.
    pub insert_time: Option<String>,
    /// layout property.
    pub layout: Option<String>,
    /// manifestSizeBytes property.
    pub manifest_size_bytes: Option<String>,
    /// manifestSizeLimitBytes property.
    pub manifest_size_limit_bytes: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
}

/// `ManifestsListResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ManifestsListResponse {
    /// manifests property.
    pub manifests: Option<Vec<Manifest>>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
}

/// `LocalizedMessage` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct LocalizedMessage {
    /// locale property.
    pub locale: Option<String>,
    /// message property.
    pub message: Option<String>,
}

/// `DeploymentsCancelPreviewRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct DeploymentsCancelPreviewRequest {
    /// fingerprint property.
    pub fingerprint: Option<String>,
}

/// `HelpLink` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct HelpLink {
    /// description property.
    pub description: Option<String>,
    /// url property.
    pub url: Option<String>,
}

/// `ImportFile` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ImportFile {
    /// content property.
    pub content: Option<String>,
    /// name property.
    pub name: Option<String>,
}

/// `TestPermissionsResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct TestPermissionsResponse {
    /// permissions property.
    pub permissions: Option<Vec<String>>,
}

/// `TypesListResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct TypesListResponse {
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// types property.
    pub types: Option<Vec<Type>>,
}

/// `SetAutoscalerLinkOperationMetadata` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SetAutoscalerLinkOperationMetadata {
    /// zonalIgmIds property.
    pub zonal_igm_ids: Option<Vec<String>>,
    /// zoneToIgmIds property.
    pub zone_to_igm_ids: Option<serde_json::Value>,
}

/// `DeploymentsStopRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct DeploymentsStopRequest {
    /// fingerprint property.
    pub fingerprint: Option<String>,
}

/// `DebugInfo` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct DebugInfo {
    /// detail property.
    pub detail: Option<String>,
    /// stackEntries property.
    pub stack_entries: Option<Vec<String>>,
}

/// `ResourcesListResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ResourcesListResponse {
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// resources property.
    pub resources: Option<Vec<Resource>>,
}

/// `DeploymentUpdateLabelEntry` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct DeploymentUpdateLabelEntry {
    /// key property.
    pub key: Option<String>,
    /// value property.
    pub value: Option<String>,
}

/// `AuditConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AuditConfig {
    /// auditLogConfigs property.
    pub audit_log_configs: Option<Vec<AuditLogConfig>>,
    /// service property.
    pub service: Option<String>,
}

/// `Resource` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Resource {
    /// accessControl property.
    pub access_control: Option<ResourceAccessControl>,
    /// finalProperties property.
    pub final_properties: Option<String>,
    /// id property.
    pub id: Option<String>,
    /// insertTime property.
    pub insert_time: Option<String>,
    /// manifest property.
    pub manifest: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// properties property.
    pub properties: Option<String>,
    /// type property.
    pub r#type: Option<String>,
    /// update property.
    pub update: Option<ResourceUpdate>,
    /// updateTime property.
    pub update_time: Option<String>,
    /// url property.
    pub url: Option<String>,
    /// warnings property.
    pub warnings: Option<Vec<std::collections::HashMap<String, serde_json::Value>>>,
}

/// `GlobalSetPolicyRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GlobalSetPolicyRequest {
    /// bindings property.
    pub bindings: Option<Vec<Binding>>,
    /// etag property.
    pub etag: Option<String>,
    /// policy property.
    pub policy: Option<Policy>,
    /// updateMask property.
    pub update_mask: Option<String>,
}

/// `InstancesBulkInsertOperationMetadata` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InstancesBulkInsertOperationMetadata {
    /// machineType property.
    pub machine_type: Option<String>,
    /// perLocationStatus property.
    pub per_location_status: Option<serde_json::Value>,
}

/// `AuditLogConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AuditLogConfig {
    /// exemptedMembers property.
    pub exempted_members: Option<Vec<String>>,
    /// logType property.
    pub log_type: Option<String>,
}

// =============================================================================
// ARGS TYPES (per-endpoint)
// =============================================================================

/// Arguments for [`deploymentmanager.deployments.cancelPreview_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DeploymentmanagerDeploymentsCancelPreviewArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `deployment`.
    pub deployment: String,
    /// Request body.
    pub body: DeploymentsCancelPreviewRequest,
}

/// Arguments for [`deploymentmanager.deployments.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DeploymentmanagerDeploymentsDeleteArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `deployment`.
    pub deployment: String,
    /// Query parameter: `deletePolicy`.
    pub delete_policy: Option<String>,
    /// Query parameter: `header.bypassBillingFilter`.
    pub header_bypass_billing_filter: Option<String>,
}

/// Arguments for [`deploymentmanager.deployments.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DeploymentmanagerDeploymentsGetArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `deployment`.
    pub deployment: String,
    /// Query parameter: `header.bypassBillingFilter`.
    pub header_bypass_billing_filter: Option<String>,
}

/// Arguments for [`deploymentmanager.deployments.getIamPolicy_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DeploymentmanagerDeploymentsGetIamPolicyArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `resource`.
    pub resource: String,
    /// Query parameter: `header.bypassBillingFilter`.
    pub header_bypass_billing_filter: Option<String>,
    /// Query parameter: `optionsRequestedPolicyVersion`.
    pub options_requested_policy_version: Option<String>,
}

/// Arguments for [`deploymentmanager.deployments.insert_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DeploymentmanagerDeploymentsInsertArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Query parameter: `createPolicy`.
    pub create_policy: Option<String>,
    /// Query parameter: `header.bypassBillingFilter`.
    pub header_bypass_billing_filter: Option<String>,
    /// Query parameter: `preview`.
    pub preview: Option<String>,
    /// Request body.
    pub body: Deployment,
}

/// Arguments for [`deploymentmanager.deployments.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DeploymentmanagerDeploymentsListArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`deploymentmanager.deployments.patch_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DeploymentmanagerDeploymentsPatchArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `deployment`.
    pub deployment: String,
    /// Query parameter: `createPolicy`.
    pub create_policy: Option<String>,
    /// Query parameter: `deletePolicy`.
    pub delete_policy: Option<String>,
    /// Query parameter: `header.bypassBillingFilter`.
    pub header_bypass_billing_filter: Option<String>,
    /// Query parameter: `preview`.
    pub preview: Option<String>,
    /// Request body.
    pub body: Deployment,
}

/// Arguments for [`deploymentmanager.deployments.setIamPolicy_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DeploymentmanagerDeploymentsSetIamPolicyArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `resource`.
    pub resource: String,
    /// Request body.
    pub body: GlobalSetPolicyRequest,
}

/// Arguments for [`deploymentmanager.deployments.stop_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DeploymentmanagerDeploymentsStopArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `deployment`.
    pub deployment: String,
    /// Request body.
    pub body: DeploymentsStopRequest,
}

/// Arguments for [`deploymentmanager.deployments.testIamPermissions_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DeploymentmanagerDeploymentsTestIamPermissionsArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `resource`.
    pub resource: String,
    /// Query parameter: `header.bypassBillingFilter`.
    pub header_bypass_billing_filter: Option<String>,
    /// Request body.
    pub body: TestPermissionsRequest,
}

/// Arguments for [`deploymentmanager.deployments.update_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DeploymentmanagerDeploymentsUpdateArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `deployment`.
    pub deployment: String,
    /// Query parameter: `createPolicy`.
    pub create_policy: Option<String>,
    /// Query parameter: `deletePolicy`.
    pub delete_policy: Option<String>,
    /// Query parameter: `header.bypassBillingFilter`.
    pub header_bypass_billing_filter: Option<String>,
    /// Query parameter: `preview`.
    pub preview: Option<String>,
    /// Request body.
    pub body: Deployment,
}

/// Arguments for [`deploymentmanager.manifests.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DeploymentmanagerManifestsGetArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `deployment`.
    pub deployment: String,
    /// Path parameter: `manifest`.
    pub manifest: String,
    /// Query parameter: `header.bypassBillingFilter`.
    pub header_bypass_billing_filter: Option<String>,
}

/// Arguments for [`deploymentmanager.manifests.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DeploymentmanagerManifestsListArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `deployment`.
    pub deployment: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`deploymentmanager.operations.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DeploymentmanagerOperationsGetArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `operation`.
    pub operation: String,
    /// Query parameter: `header.bypassBillingFilter`.
    pub header_bypass_billing_filter: Option<String>,
}

/// Arguments for [`deploymentmanager.operations.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DeploymentmanagerOperationsListArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`deploymentmanager.resources.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DeploymentmanagerResourcesGetArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `deployment`.
    pub deployment: String,
    /// Path parameter: `resource`.
    pub resource: String,
    /// Query parameter: `header.bypassBillingFilter`.
    pub header_bypass_billing_filter: Option<String>,
}

/// Arguments for [`deploymentmanager.resources.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DeploymentmanagerResourcesListArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `deployment`.
    pub deployment: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`deploymentmanager.types.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DeploymentmanagerTypesListArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

// =============================================================================
// CLIENT FUNCTIONS (per-endpoint)
// =============================================================================

// -----------------------------------------------------------------------------
// POST deploymentmanager/v2/projects/{project}/global/deployments/{deployment}/cancelPreview
// -----------------------------------------------------------------------------

/// POST deploymentmanager/v2/projects/{project}/global/deployments/{deployment}/cancelPreview.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = deploymentmanager_deployments_cancel_preview_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn deploymentmanager_deployments_cancel_preview_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DeploymentmanagerDeploymentsCancelPreviewArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://deploymentmanager.googleapis.com/deploymentmanager/v2/projects/{}/global/deployments/{}/cancelPreview",
        args.project,
        args.deployment,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE deploymentmanager/v2/projects/{project}/global/deployments/{deployment}
// -----------------------------------------------------------------------------

/// DELETE deploymentmanager/v2/projects/{project}/global/deployments/{deployment}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = deploymentmanager_deployments_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn deploymentmanager_deployments_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DeploymentmanagerDeploymentsDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://deploymentmanager.googleapis.com/deploymentmanager/v2/projects/{}/global/deployments/{}",
        args.project,
        args.deployment,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.delete_policy {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("deletePolicy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.header_bypass_billing_filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("header_bypassBillingFilter=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET deploymentmanager/v2/projects/{project}/global/deployments/{deployment}
// -----------------------------------------------------------------------------

/// GET deploymentmanager/v2/projects/{project}/global/deployments/{deployment}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = deploymentmanager_deployments_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn deploymentmanager_deployments_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DeploymentmanagerDeploymentsGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Deployment>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://deploymentmanager.googleapis.com/deploymentmanager/v2/projects/{}/global/deployments/{}",
        args.project,
        args.deployment,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.header_bypass_billing_filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("header_bypassBillingFilter=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Deployment =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET deploymentmanager/v2/projects/{project}/global/deployments/{resource}/getIamPolicy
// -----------------------------------------------------------------------------

/// GET deploymentmanager/v2/projects/{project}/global/deployments/{resource}/getIamPolicy.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = deploymentmanager_deployments_get_iam_policy_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn deploymentmanager_deployments_get_iam_policy_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DeploymentmanagerDeploymentsGetIamPolicyArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://deploymentmanager.googleapis.com/deploymentmanager/v2/projects/{}/global/deployments/{}/getIamPolicy",
        args.project,
        args.resource,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.header_bypass_billing_filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("header_bypassBillingFilter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.options_requested_policy_version {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("optionsRequestedPolicyVersion=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Policy =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST deploymentmanager/v2/projects/{project}/global/deployments
// -----------------------------------------------------------------------------

/// POST deploymentmanager/v2/projects/{project}/global/deployments.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = deploymentmanager_deployments_insert_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn deploymentmanager_deployments_insert_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DeploymentmanagerDeploymentsInsertArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://deploymentmanager.googleapis.com/deploymentmanager/v2/projects/{}/global/deployments",
        args.project,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.create_policy {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("createPolicy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.header_bypass_billing_filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("header_bypassBillingFilter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.preview {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("preview=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET deploymentmanager/v2/projects/{project}/global/deployments
// -----------------------------------------------------------------------------

/// GET deploymentmanager/v2/projects/{project}/global/deployments.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = deploymentmanager_deployments_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn deploymentmanager_deployments_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DeploymentmanagerDeploymentsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<DeploymentsListResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://deploymentmanager.googleapis.com/deploymentmanager/v2/projects/{}/global/deployments",
        args.project,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: DeploymentsListResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH deploymentmanager/v2/projects/{project}/global/deployments/{deployment}
// -----------------------------------------------------------------------------

/// PATCH deploymentmanager/v2/projects/{project}/global/deployments/{deployment}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = deploymentmanager_deployments_patch_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn deploymentmanager_deployments_patch_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DeploymentmanagerDeploymentsPatchArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://deploymentmanager.googleapis.com/deploymentmanager/v2/projects/{}/global/deployments/{}",
        args.project,
        args.deployment,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.create_policy {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("createPolicy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.delete_policy {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("deletePolicy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.header_bypass_billing_filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("header_bypassBillingFilter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.preview {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("preview=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST deploymentmanager/v2/projects/{project}/global/deployments/{resource}/setIamPolicy
// -----------------------------------------------------------------------------

/// POST deploymentmanager/v2/projects/{project}/global/deployments/{resource}/setIamPolicy.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = deploymentmanager_deployments_set_iam_policy_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn deploymentmanager_deployments_set_iam_policy_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DeploymentmanagerDeploymentsSetIamPolicyArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://deploymentmanager.googleapis.com/deploymentmanager/v2/projects/{}/global/deployments/{}/setIamPolicy",
        args.project,
        args.resource,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Policy =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST deploymentmanager/v2/projects/{project}/global/deployments/{deployment}/stop
// -----------------------------------------------------------------------------

/// POST deploymentmanager/v2/projects/{project}/global/deployments/{deployment}/stop.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = deploymentmanager_deployments_stop_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn deploymentmanager_deployments_stop_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DeploymentmanagerDeploymentsStopArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://deploymentmanager.googleapis.com/deploymentmanager/v2/projects/{}/global/deployments/{}/stop",
        args.project,
        args.deployment,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST deploymentmanager/v2/projects/{project}/global/deployments/{resource}/testIamPermissions
// -----------------------------------------------------------------------------

/// POST deploymentmanager/v2/projects/{project}/global/deployments/{resource}/testIamPermissions.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = deploymentmanager_deployments_test_iam_permissions_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn deploymentmanager_deployments_test_iam_permissions_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DeploymentmanagerDeploymentsTestIamPermissionsArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TestPermissionsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://deploymentmanager.googleapis.com/deploymentmanager/v2/projects/{}/global/deployments/{}/testIamPermissions",
        args.project,
        args.resource,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.header_bypass_billing_filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("header_bypassBillingFilter=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: TestPermissionsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PUT deploymentmanager/v2/projects/{project}/global/deployments/{deployment}
// -----------------------------------------------------------------------------

/// PUT deploymentmanager/v2/projects/{project}/global/deployments/{deployment}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = deploymentmanager_deployments_update_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn deploymentmanager_deployments_update_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DeploymentmanagerDeploymentsUpdateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://deploymentmanager.googleapis.com/deploymentmanager/v2/projects/{}/global/deployments/{}",
        args.project,
        args.deployment,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.create_policy {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("createPolicy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.delete_policy {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("deletePolicy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.header_bypass_billing_filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("header_bypassBillingFilter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.preview {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("preview=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .put(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET deploymentmanager/v2/projects/{project}/global/deployments/{deployment}/manifests/{manifest}
// -----------------------------------------------------------------------------

/// GET deploymentmanager/v2/projects/{project}/global/deployments/{deployment}/manifests/{manifest}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = deploymentmanager_manifests_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn deploymentmanager_manifests_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DeploymentmanagerManifestsGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Manifest>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://deploymentmanager.googleapis.com/deploymentmanager/v2/projects/{}/global/deployments/{}/manifests/{}",
        args.project,
        args.deployment,
        args.manifest,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.header_bypass_billing_filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("header_bypassBillingFilter=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Manifest =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET deploymentmanager/v2/projects/{project}/global/deployments/{deployment}/manifests
// -----------------------------------------------------------------------------

/// GET deploymentmanager/v2/projects/{project}/global/deployments/{deployment}/manifests.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = deploymentmanager_manifests_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn deploymentmanager_manifests_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DeploymentmanagerManifestsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ManifestsListResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://deploymentmanager.googleapis.com/deploymentmanager/v2/projects/{}/global/deployments/{}/manifests",
        args.project,
        args.deployment,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ManifestsListResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET deploymentmanager/v2/projects/{project}/global/operations/{operation}
// -----------------------------------------------------------------------------

/// GET deploymentmanager/v2/projects/{project}/global/operations/{operation}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = deploymentmanager_operations_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn deploymentmanager_operations_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DeploymentmanagerOperationsGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://deploymentmanager.googleapis.com/deploymentmanager/v2/projects/{}/global/operations/{}",
        args.project,
        args.operation,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.header_bypass_billing_filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("header_bypassBillingFilter=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET deploymentmanager/v2/projects/{project}/global/operations
// -----------------------------------------------------------------------------

/// GET deploymentmanager/v2/projects/{project}/global/operations.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = deploymentmanager_operations_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn deploymentmanager_operations_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DeploymentmanagerOperationsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<OperationsListResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://deploymentmanager.googleapis.com/deploymentmanager/v2/projects/{}/global/operations",
        args.project,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: OperationsListResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET deploymentmanager/v2/projects/{project}/global/deployments/{deployment}/resources/{resource}
// -----------------------------------------------------------------------------

/// GET deploymentmanager/v2/projects/{project}/global/deployments/{deployment}/resources/{resource}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = deploymentmanager_resources_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn deploymentmanager_resources_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DeploymentmanagerResourcesGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Resource>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://deploymentmanager.googleapis.com/deploymentmanager/v2/projects/{}/global/deployments/{}/resources/{}",
        args.project,
        args.deployment,
        args.resource,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.header_bypass_billing_filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("header_bypassBillingFilter=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Resource =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET deploymentmanager/v2/projects/{project}/global/deployments/{deployment}/resources
// -----------------------------------------------------------------------------

/// GET deploymentmanager/v2/projects/{project}/global/deployments/{deployment}/resources.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = deploymentmanager_resources_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn deploymentmanager_resources_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DeploymentmanagerResourcesListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ResourcesListResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://deploymentmanager.googleapis.com/deploymentmanager/v2/projects/{}/global/deployments/{}/resources",
        args.project,
        args.deployment,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ResourcesListResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET deploymentmanager/v2/projects/{project}/global/types
// -----------------------------------------------------------------------------

/// GET deploymentmanager/v2/projects/{project}/global/types.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = deploymentmanager_types_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn deploymentmanager_types_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DeploymentmanagerTypesListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TypesListResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://deploymentmanager.googleapis.com/deploymentmanager/v2/projects/{}/global/types",
        args.project,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.filter {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("filter=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.max_results {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("maxResults=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.order_by {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("orderBy=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: TypesListResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}
