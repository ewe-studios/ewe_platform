//! Auto-generated API module for gcp/dataform compilationResults.
//!
//! Generated by `cargo run --bin ewe_platform gen_api`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp_dataform_compilation_results `

#![cfg(feature = "gcp_dataform_compilation_results")]
#![allow(clippy::too_many_arguments, clippy::type_complexity)]
#![allow(
    clippy::missing_errors_doc,
    clippy::doc_markdown,
    clippy::useless_format
)]

use foundation_core::valtron::{execute, StreamIterator, TaskIterator, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{ClientRequestBuilder, SimpleHttpClient};
use foundation_macros::JsonHash;
use serde::{Deserialize, Serialize};

use super::shared::{ApiError, ApiPending, ApiResponse};

// =============================================================================
// TYPE DECLARATIONS
// =============================================================================

/// `CompilationResult` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct CompilationResult {
    /// codeCompilationConfig property.
    pub code_compilation_config: Option<CodeCompilationConfig>,
    /// compilationErrors property.
    pub compilation_errors: Option<Vec<CompilationError>>,
    /// createTime property.
    pub create_time: Option<String>,
    /// dataEncryptionState property.
    pub data_encryption_state: Option<DataEncryptionState>,
    /// dataformCoreVersion property.
    pub dataform_core_version: Option<String>,
    /// gitCommitish property.
    pub git_commitish: Option<String>,
    /// internalMetadata property.
    pub internal_metadata: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// privateResourceMetadata property.
    pub private_resource_metadata: Option<PrivateResourceMetadata>,
    /// releaseConfig property.
    pub release_config: Option<String>,
    /// resolvedGitCommitSha property.
    pub resolved_git_commit_sha: Option<String>,
    /// workspace property.
    pub workspace: Option<String>,
}

/// `Assertion` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Assertion {
    /// dependencyTargets property.
    pub dependency_targets: Option<Vec<Target>>,
    /// disabled property.
    pub disabled: Option<bool>,
    /// parentAction property.
    pub parent_action: Option<Target>,
    /// relationDescriptor property.
    pub relation_descriptor: Option<RelationDescriptor>,
    /// selectQuery property.
    pub select_query: Option<String>,
    /// tags property.
    pub tags: Option<Vec<String>>,
}

/// `Operations` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Operations {
    /// dependencyTargets property.
    pub dependency_targets: Option<Vec<Target>>,
    /// disabled property.
    pub disabled: Option<bool>,
    /// hasOutput property.
    pub has_output: Option<bool>,
    /// queries property.
    pub queries: Option<Vec<String>>,
    /// relationDescriptor property.
    pub relation_descriptor: Option<RelationDescriptor>,
    /// tags property.
    pub tags: Option<Vec<String>>,
}

/// `RelationDescriptor` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct RelationDescriptor {
    /// bigqueryLabels property.
    pub bigquery_labels: Option<serde_json::Value>,
    /// columns property.
    pub columns: Option<Vec<ColumnDescriptor>>,
    /// description property.
    pub description: Option<String>,
}

/// `Notebook` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Notebook {
    /// contents property.
    pub contents: Option<String>,
    /// dependencyTargets property.
    pub dependency_targets: Option<Vec<Target>>,
    /// disabled property.
    pub disabled: Option<bool>,
    /// tags property.
    pub tags: Option<Vec<String>>,
}

/// `SqlDefinition` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct SqlDefinition {
    /// errorTable property.
    pub error_table: Option<ErrorTable>,
    /// load property.
    pub load: Option<LoadConfig>,
    /// query property.
    pub query: Option<String>,
}

/// `ColumnDescriptor` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ColumnDescriptor {
    /// bigqueryPolicyTags property.
    pub bigquery_policy_tags: Option<Vec<String>>,
    /// description property.
    pub description: Option<String>,
    /// path property.
    pub path: Option<Vec<String>>,
}

/// `ErrorTable` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ErrorTable {
    /// retentionDays property.
    pub retention_days: Option<i64>,
    /// target property.
    pub target: Option<Target>,
}

/// `DataPreparation` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct DataPreparation {
    /// contentsSql property.
    pub contents_sql: Option<SqlDefinition>,
    /// contentsYaml property.
    pub contents_yaml: Option<String>,
    /// dependencyTargets property.
    pub dependency_targets: Option<Vec<Target>>,
    /// disabled property.
    pub disabled: Option<bool>,
    /// tags property.
    pub tags: Option<Vec<String>>,
}

/// `SimpleLoadMode` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct SimpleLoadMode {}

/// `CompilationResultAction` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct CompilationResultAction {
    /// assertion property.
    pub assertion: Option<Assertion>,
    /// canonicalTarget property.
    pub canonical_target: Option<Target>,
    /// dataPreparation property.
    pub data_preparation: Option<DataPreparation>,
    /// declaration property.
    pub declaration: Option<Declaration>,
    /// filePath property.
    pub file_path: Option<String>,
    /// internalMetadata property.
    pub internal_metadata: Option<String>,
    /// notebook property.
    pub notebook: Option<Notebook>,
    /// operations property.
    pub operations: Option<Operations>,
    /// relation property.
    pub relation: Option<Relation>,
    /// target property.
    pub target: Option<Target>,
}

/// `IncrementalLoadMode` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct IncrementalLoadMode {
    /// column property.
    pub column: Option<String>,
}

/// `CodeCompilationConfig` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct CodeCompilationConfig {
    /// assertionSchema property.
    pub assertion_schema: Option<String>,
    /// builtinAssertionNamePrefix property.
    pub builtin_assertion_name_prefix: Option<String>,
    /// databaseSuffix property.
    pub database_suffix: Option<String>,
    /// defaultDatabase property.
    pub default_database: Option<String>,
    /// defaultLocation property.
    pub default_location: Option<String>,
    /// defaultNotebookRuntimeOptions property.
    pub default_notebook_runtime_options: Option<NotebookRuntimeOptions>,
    /// defaultSchema property.
    pub default_schema: Option<String>,
    /// schemaSuffix property.
    pub schema_suffix: Option<String>,
    /// tablePrefix property.
    pub table_prefix: Option<String>,
    /// vars property.
    pub vars: Option<serde_json::Value>,
}

/// `CompilationError` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct CompilationError {
    /// actionTarget property.
    pub action_target: Option<Target>,
    /// message property.
    pub message: Option<String>,
    /// path property.
    pub path: Option<String>,
    /// stack property.
    pub stack: Option<String>,
}

/// `LoadConfig` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct LoadConfig {
    /// append property.
    pub append: Option<SimpleLoadMode>,
    /// maximum property.
    pub maximum: Option<IncrementalLoadMode>,
    /// replace property.
    pub replace: Option<SimpleLoadMode>,
    /// unique property.
    pub unique: Option<IncrementalLoadMode>,
}

/// `NotebookRuntimeOptions` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct NotebookRuntimeOptions {
    /// aiPlatformNotebookRuntimeTemplate property.
    pub ai_platform_notebook_runtime_template: Option<String>,
    /// gcsOutputBucket property.
    pub gcs_output_bucket: Option<String>,
}

/// `DataEncryptionState` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct DataEncryptionState {
    /// kmsKeyVersionName property.
    pub kms_key_version_name: Option<String>,
}

/// `ListCompilationResultsResponse` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ListCompilationResultsResponse {
    /// compilationResults property.
    pub compilation_results: Option<Vec<CompilationResult>>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// unreachable property.
    pub unreachable: Option<Vec<String>>,
}

/// `QueryCompilationResultActionsResponse` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct QueryCompilationResultActionsResponse {
    /// compilationResultActions property.
    pub compilation_result_actions: Option<Vec<CompilationResultAction>>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
}

/// `Target` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Target {
    /// database property.
    pub database: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// schema property.
    pub schema: Option<String>,
}

/// `Declaration` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Declaration {
    /// relationDescriptor property.
    pub relation_descriptor: Option<RelationDescriptor>,
}

/// `IncrementalTableConfig` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct IncrementalTableConfig {
    /// incrementalPostOperations property.
    pub incremental_post_operations: Option<Vec<String>>,
    /// incrementalPreOperations property.
    pub incremental_pre_operations: Option<Vec<String>>,
    /// incrementalSelectQuery property.
    pub incremental_select_query: Option<String>,
    /// refreshDisabled property.
    pub refresh_disabled: Option<bool>,
    /// uniqueKeyParts property.
    pub unique_key_parts: Option<Vec<String>>,
    /// updatePartitionFilter property.
    pub update_partition_filter: Option<String>,
}

/// `PrivateResourceMetadata` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct PrivateResourceMetadata {
    /// userScoped property.
    pub user_scoped: Option<bool>,
}

/// `Relation` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Relation {
    /// additionalOptions property.
    pub additional_options: Option<serde_json::Value>,
    /// clusterExpressions property.
    pub cluster_expressions: Option<Vec<String>>,
    /// connection property.
    pub connection: Option<String>,
    /// dependencyTargets property.
    pub dependency_targets: Option<Vec<Target>>,
    /// disabled property.
    pub disabled: Option<bool>,
    /// fileFormat property.
    pub file_format: Option<String>,
    /// incrementalTableConfig property.
    pub incremental_table_config: Option<IncrementalTableConfig>,
    /// partitionExpirationDays property.
    pub partition_expiration_days: Option<i64>,
    /// partitionExpression property.
    pub partition_expression: Option<String>,
    /// postOperations property.
    pub post_operations: Option<Vec<String>>,
    /// preOperations property.
    pub pre_operations: Option<Vec<String>>,
    /// relationDescriptor property.
    pub relation_descriptor: Option<RelationDescriptor>,
    /// relationType property.
    pub relation_type: Option<String>,
    /// requirePartitionFilter property.
    pub require_partition_filter: Option<bool>,
    /// selectQuery property.
    pub select_query: Option<String>,
    /// storageUri property.
    pub storage_uri: Option<String>,
    /// tableFormat property.
    pub table_format: Option<String>,
    /// tags property.
    pub tags: Option<Vec<String>>,
}

// =============================================================================
// ARGS TYPES (per-endpoint)
// =============================================================================

/// Arguments for [`dataform.projects.locations.repositories.compilationResults.create_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataformProjectsLocationsRepositoriesCompilationResultsCreateArgs {
    /// Path parameter: `parent`.
    pub parent: String,
}

/// Arguments for [`dataform.projects.locations.repositories.compilationResults.get_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataformProjectsLocationsRepositoriesCompilationResultsGetArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`dataform.projects.locations.repositories.compilationResults.list_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataformProjectsLocationsRepositoriesCompilationResultsListArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`dataform.projects.locations.repositories.compilationResults.query_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DataformProjectsLocationsRepositoriesCompilationResultsQueryArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

// =============================================================================
// CLIENT FUNCTIONS (per-endpoint)
// =============================================================================

// -----------------------------------------------------------------------------
// POST v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}/compilationResults
// -----------------------------------------------------------------------------

/// POST v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}/compilationResults.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataform_projects_locations_repositories_compilation_results_create_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataform_projects_locations_repositories_compilation_results_create_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<CompilationResult>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dataform.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/repositories/{{repositoriesId}}/compilationResults",
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: CompilationResult =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}/compilationResults/{compilationResultsId}
// -----------------------------------------------------------------------------

/// GET v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}/compilationResults/{compilationResultsId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataform_projects_locations_repositories_compilation_results_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataform_projects_locations_repositories_compilation_results_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<CompilationResult>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dataform.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/repositories/{{repositoriesId}}/compilationResults/{{compilationResultsId}}",
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: CompilationResult =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}/compilationResults
// -----------------------------------------------------------------------------

/// GET v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}/compilationResults.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataform_projects_locations_repositories_compilation_results_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataform_projects_locations_repositories_compilation_results_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListCompilationResultsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dataform.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/repositories/{{repositoriesId}}/compilationResults",
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListCompilationResultsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}/compilationResults/{compilationResultsId}:query
// -----------------------------------------------------------------------------

/// GET v1/projects/{projectsId}/locations/{locationsId}/repositories/{repositoriesId}/compilationResults/{compilationResultsId}:query.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dataform_projects_locations_repositories_compilation_results_query_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dataform_projects_locations_repositories_compilation_results_query_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<QueryCompilationResultActionsResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dataform.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/repositories/{{repositoriesId}}/compilationResults/{{compilationResultsId}}:query",
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: QueryCompilationResultActionsResponse = serde_json::from_str(&body)
                    .map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}
