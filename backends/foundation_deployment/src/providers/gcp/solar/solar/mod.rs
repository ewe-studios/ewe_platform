//! Auto-generated API module for gcp/solar solar.
//!
//! Generated by `cargo run --bin ewe_platform gen_api`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp_solar_solar `

#![cfg(feature = "gcp_solar_solar")]
#![allow(clippy::too_many_arguments, clippy::type_complexity)]
#![allow(
    clippy::missing_errors_doc,
    clippy::doc_markdown,
    clippy::useless_format
)]
#![allow(unused_imports)]

use foundation_core::valtron::{TaskIterator, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{ClientRequestBuilder, SimpleHttpClient};
use foundation_macros::JsonHash;
use serde::{Deserialize, Serialize};

use super::shared::ApiResponse;

// =============================================================================
// TYPE DECLARATIONS
// =============================================================================

/// `LatLng` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct LatLng {
    /// latitude property.
    pub latitude: Option<f64>,
    /// longitude property.
    pub longitude: Option<f64>,
}

/// `FinancialAnalysis` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct FinancialAnalysis {
    /// averageKwhPerMonth property.
    pub average_kwh_per_month: Option<f64>,
    /// cashPurchaseSavings property.
    pub cash_purchase_savings: Option<CashPurchaseSavings>,
    /// defaultBill property.
    pub default_bill: Option<bool>,
    /// financedPurchaseSavings property.
    pub financed_purchase_savings: Option<FinancedPurchaseSavings>,
    /// financialDetails property.
    pub financial_details: Option<FinancialDetails>,
    /// leasingSavings property.
    pub leasing_savings: Option<LeasingSavings>,
    /// monthlyBill property.
    pub monthly_bill: Option<Money>,
    /// panelConfigIndex property.
    pub panel_config_index: Option<i64>,
}

/// `RoofSegmentSummary` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct RoofSegmentSummary {
    /// azimuthDegrees property.
    pub azimuth_degrees: Option<f64>,
    /// panelsCount property.
    pub panels_count: Option<i64>,
    /// pitchDegrees property.
    pub pitch_degrees: Option<f64>,
    /// segmentIndex property.
    pub segment_index: Option<i64>,
    /// yearlyEnergyDcKwh property.
    pub yearly_energy_dc_kwh: Option<f64>,
}

/// `LatLngBox` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct LatLngBox {
    /// ne property.
    pub ne: Option<LatLng>,
    /// sw property.
    pub sw: Option<LatLng>,
}

/// `Date` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Date {
    /// day property.
    pub day: Option<i64>,
    /// month property.
    pub month: Option<i64>,
    /// year property.
    pub year: Option<i64>,
}

/// `RoofSegmentSizeAndSunshineStats` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct RoofSegmentSizeAndSunshineStats {
    /// azimuthDegrees property.
    pub azimuth_degrees: Option<f64>,
    /// boundingBox property.
    pub bounding_box: Option<LatLngBox>,
    /// center property.
    pub center: Option<LatLng>,
    /// pitchDegrees property.
    pub pitch_degrees: Option<f64>,
    /// planeHeightAtCenterMeters property.
    pub plane_height_at_center_meters: Option<f64>,
    /// stats property.
    pub stats: Option<SizeAndSunshineStats>,
}

/// `HttpBody` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct HttpBody {
    /// contentType property.
    pub content_type: Option<String>,
    /// data property.
    pub data: Option<String>,
    /// extensions property.
    pub extensions: Option<Vec<serde_json::Value>>,
}

/// `FinancedPurchaseSavings` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct FinancedPurchaseSavings {
    /// annualLoanPayment property.
    pub annual_loan_payment: Option<Money>,
    /// loanInterestRate property.
    pub loan_interest_rate: Option<f64>,
    /// rebateValue property.
    pub rebate_value: Option<Money>,
    /// savings property.
    pub savings: Option<SavingsOverTime>,
}

/// `LeasingSavings` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct LeasingSavings {
    /// annualLeasingCost property.
    pub annual_leasing_cost: Option<Money>,
    /// leasesAllowed property.
    pub leases_allowed: Option<bool>,
    /// leasesSupported property.
    pub leases_supported: Option<bool>,
    /// savings property.
    pub savings: Option<SavingsOverTime>,
}

/// `Money` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Money {
    /// currencyCode property.
    pub currency_code: Option<String>,
    /// nanos property.
    pub nanos: Option<i64>,
    /// units property.
    pub units: Option<String>,
}

/// `SolarPanelConfig` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SolarPanelConfig {
    /// panelsCount property.
    pub panels_count: Option<i64>,
    /// roofSegmentSummaries property.
    pub roof_segment_summaries: Option<Vec<RoofSegmentSummary>>,
    /// yearlyEnergyDcKwh property.
    pub yearly_energy_dc_kwh: Option<f64>,
}

/// `SolarPanel` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SolarPanel {
    /// center property.
    pub center: Option<LatLng>,
    /// orientation property.
    pub orientation: Option<String>,
    /// segmentIndex property.
    pub segment_index: Option<i64>,
    /// yearlyEnergyDcKwh property.
    pub yearly_energy_dc_kwh: Option<f64>,
}

/// `SavingsOverTime` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SavingsOverTime {
    /// financiallyViable property.
    pub financially_viable: Option<bool>,
    /// presentValueOfSavingsLifetime property.
    pub present_value_of_savings_lifetime: Option<Money>,
    /// presentValueOfSavingsYear20 property.
    pub present_value_of_savings_year20: Option<Money>,
    /// savingsLifetime property.
    pub savings_lifetime: Option<Money>,
    /// savingsYear1 property.
    pub savings_year1: Option<Money>,
    /// savingsYear20 property.
    pub savings_year20: Option<Money>,
}

/// `BuildingInsights` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct BuildingInsights {
    /// administrativeArea property.
    pub administrative_area: Option<String>,
    /// boundingBox property.
    pub bounding_box: Option<LatLngBox>,
    /// center property.
    pub center: Option<LatLng>,
    /// imageryDate property.
    pub imagery_date: Option<Date>,
    /// imageryProcessedDate property.
    pub imagery_processed_date: Option<Date>,
    /// imageryQuality property.
    pub imagery_quality: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// postalCode property.
    pub postal_code: Option<String>,
    /// regionCode property.
    pub region_code: Option<String>,
    /// solarPotential property.
    pub solar_potential: Option<SolarPotential>,
    /// statisticalArea property.
    pub statistical_area: Option<String>,
}

/// `DataLayers` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct DataLayers {
    /// annualFluxUrl property.
    pub annual_flux_url: Option<String>,
    /// dsmUrl property.
    pub dsm_url: Option<String>,
    /// hourlyShadeUrls property.
    pub hourly_shade_urls: Option<Vec<String>>,
    /// imageryDate property.
    pub imagery_date: Option<Date>,
    /// imageryProcessedDate property.
    pub imagery_processed_date: Option<Date>,
    /// imageryQuality property.
    pub imagery_quality: Option<String>,
    /// maskUrl property.
    pub mask_url: Option<String>,
    /// monthlyFluxUrl property.
    pub monthly_flux_url: Option<String>,
    /// rgbUrl property.
    pub rgb_url: Option<String>,
}

/// `SizeAndSunshineStats` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SizeAndSunshineStats {
    /// areaMeters2 property.
    pub area_meters2: Option<f64>,
    /// groundAreaMeters2 property.
    pub ground_area_meters2: Option<f64>,
    /// sunshineQuantiles property.
    pub sunshine_quantiles: Option<Vec<f64>>,
}

/// `FinancialDetails` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct FinancialDetails {
    /// costOfElectricityWithoutSolar property.
    pub cost_of_electricity_without_solar: Option<Money>,
    /// federalIncentive property.
    pub federal_incentive: Option<Money>,
    /// initialAcKwhPerYear property.
    pub initial_ac_kwh_per_year: Option<f64>,
    /// lifetimeSrecTotal property.
    pub lifetime_srec_total: Option<Money>,
    /// netMeteringAllowed property.
    pub net_metering_allowed: Option<bool>,
    /// percentageExportedToGrid property.
    pub percentage_exported_to_grid: Option<f64>,
    /// remainingLifetimeUtilityBill property.
    pub remaining_lifetime_utility_bill: Option<Money>,
    /// solarPercentage property.
    pub solar_percentage: Option<f64>,
    /// stateIncentive property.
    pub state_incentive: Option<Money>,
    /// utilityIncentive property.
    pub utility_incentive: Option<Money>,
}

/// `CashPurchaseSavings` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct CashPurchaseSavings {
    /// outOfPocketCost property.
    pub out_of_pocket_cost: Option<Money>,
    /// paybackYears property.
    pub payback_years: Option<f64>,
    /// rebateValue property.
    pub rebate_value: Option<Money>,
    /// savings property.
    pub savings: Option<SavingsOverTime>,
    /// upfrontCost property.
    pub upfront_cost: Option<Money>,
}

/// `SolarPotential` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SolarPotential {
    /// buildingStats property.
    pub building_stats: Option<SizeAndSunshineStats>,
    /// carbonOffsetFactorKgPerMwh property.
    pub carbon_offset_factor_kg_per_mwh: Option<f64>,
    /// financialAnalyses property.
    pub financial_analyses: Option<Vec<FinancialAnalysis>>,
    /// maxArrayAreaMeters2 property.
    pub max_array_area_meters2: Option<f64>,
    /// maxArrayPanelsCount property.
    pub max_array_panels_count: Option<i64>,
    /// maxSunshineHoursPerYear property.
    pub max_sunshine_hours_per_year: Option<f64>,
    /// panelCapacityWatts property.
    pub panel_capacity_watts: Option<f64>,
    /// panelHeightMeters property.
    pub panel_height_meters: Option<f64>,
    /// panelLifetimeYears property.
    pub panel_lifetime_years: Option<i64>,
    /// panelWidthMeters property.
    pub panel_width_meters: Option<f64>,
    /// roofSegmentStats property.
    pub roof_segment_stats: Option<Vec<RoofSegmentSizeAndSunshineStats>>,
    /// solarPanelConfigs property.
    pub solar_panel_configs: Option<Vec<SolarPanelConfig>>,
    /// solarPanels property.
    pub solar_panels: Option<Vec<SolarPanel>>,
    /// wholeRoofStats property.
    pub whole_roof_stats: Option<SizeAndSunshineStats>,
}

// =============================================================================
// ARGS TYPES (per-endpoint)
// =============================================================================

/// Arguments for [`solar.buildingInsights.findClosest_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct SolarBuildingInsightsFindClosestArgs {
    /// Query parameter: `exactQualityRequired`.
    pub exact_quality_required: Option<String>,
    /// Query parameter: `experiments`.
    pub experiments: Option<String>,
    /// Query parameter: `location.latitude`.
    pub location_latitude: Option<String>,
    /// Query parameter: `location.longitude`.
    pub location_longitude: Option<String>,
    /// Query parameter: `requiredQuality`.
    pub required_quality: Option<String>,
}

/// Arguments for [`solar.dataLayers.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct SolarDataLayersGetArgs {
    /// Query parameter: `exactQualityRequired`.
    pub exact_quality_required: Option<String>,
    /// Query parameter: `experiments`.
    pub experiments: Option<String>,
    /// Query parameter: `location.latitude`.
    pub location_latitude: Option<String>,
    /// Query parameter: `location.longitude`.
    pub location_longitude: Option<String>,
    /// Query parameter: `pixelSizeMeters`.
    pub pixel_size_meters: Option<String>,
    /// Query parameter: `radiusMeters`.
    pub radius_meters: Option<String>,
    /// Query parameter: `requiredQuality`.
    pub required_quality: Option<String>,
    /// Query parameter: `view`.
    pub view: Option<String>,
}

/// Arguments for [`solar.geoTiff.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct SolarGeoTiffGetArgs {
    /// Query parameter: `id`.
    pub id: Option<String>,
}

// =============================================================================
// CLIENT FUNCTIONS (per-endpoint)
// =============================================================================

// -----------------------------------------------------------------------------
// GET v1/buildingInsights:findClosest
// -----------------------------------------------------------------------------

/// GET v1/buildingInsights:findClosest.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = solar_building_insights_find_closest_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn solar_building_insights_find_closest_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &SolarBuildingInsightsFindClosestArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<BuildingInsights>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://solar.googleapis.com/v1/buildingInsights:findClosest",);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.exact_quality_required {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("exactQualityRequired=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.experiments {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("experiments=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.location_latitude {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("location_latitude=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.location_longitude {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("location_longitude=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.required_quality {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requiredQuality=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: BuildingInsights =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/dataLayers:get
// -----------------------------------------------------------------------------

/// GET v1/dataLayers:get.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = solar_data_layers_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn solar_data_layers_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &SolarDataLayersGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<DataLayers>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://solar.googleapis.com/v1/dataLayers:get",);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.exact_quality_required {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("exactQualityRequired=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.experiments {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("experiments=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.location_latitude {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("location_latitude=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.location_longitude {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("location_longitude=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.pixel_size_meters {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pixelSizeMeters=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.radius_meters {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("radiusMeters=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.required_quality {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("requiredQuality=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.view {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("view=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: DataLayers =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/geoTiff:get
// -----------------------------------------------------------------------------

/// GET v1/geoTiff:get.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = solar_geo_tiff_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn solar_geo_tiff_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &SolarGeoTiffGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<HttpBody>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://solar.googleapis.com/v1/geoTiff:get",);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("id=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: HttpBody =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}
