//! Auto-generated API module for gcp/transcoder jobs.
//!
//! Generated by `cargo run --bin ewe_platform gen_api`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp_transcoder_jobs `

#![cfg(feature = "gcp_transcoder_jobs")]
#![allow(clippy::too_many_arguments, clippy::type_complexity)]
#![allow(
    clippy::missing_errors_doc,
    clippy::doc_markdown,
    clippy::useless_format
)]
#![allow(unused_imports)]

use foundation_core::valtron::{TaskIterator, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{ClientRequestBuilder, SimpleHttpClient};
use foundation_macros::JsonHash;
use serde::{Deserialize, Serialize};

// Import shared types used by this module
use super::shared::Empty;

use super::shared::ApiResponse;

// =============================================================================
// TYPE DECLARATIONS
// =============================================================================

/// `Input` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Input {
    /// attributes property.
    pub attributes: Option<InputAttributes>,
    /// key property.
    pub key: Option<String>,
    /// preprocessingConfig property.
    pub preprocessing_config: Option<PreprocessingConfig>,
    /// uri property.
    pub uri: Option<String>,
}

/// `ListJobsResponse` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ListJobsResponse {
    /// jobs property.
    pub jobs: Option<Vec<Job>>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// unreachable property.
    pub unreachable: Option<Vec<String>>,
}

/// `Overlay` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Overlay {
    /// animations property.
    pub animations: Option<Vec<Animation>>,
    /// image property.
    pub image: Option<Image>,
}

/// `H264ColorFormatHLG` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct H264ColorFormatHLG {}

/// `BwdifConfig` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct BwdifConfig {
    /// deinterlaceAllFrames property.
    pub deinterlace_all_frames: Option<bool>,
    /// mode property.
    pub mode: Option<String>,
    /// parity property.
    pub parity: Option<String>,
}

/// `Fairplay` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Fairplay {}

/// `H264CodecSettings` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct H264CodecSettings {
    /// allowOpenGop property.
    pub allow_open_gop: Option<bool>,
    /// aqStrength property.
    pub aq_strength: Option<f64>,
    /// bFrameCount property.
    pub b_frame_count: Option<i64>,
    /// bPyramid property.
    pub b_pyramid: Option<bool>,
    /// bitrateBps property.
    pub bitrate_bps: Option<i64>,
    /// crfLevel property.
    pub crf_level: Option<i64>,
    /// enableTwoPass property.
    pub enable_two_pass: Option<bool>,
    /// entropyCoder property.
    pub entropy_coder: Option<String>,
    /// frameRate property.
    pub frame_rate: Option<f64>,
    /// frameRateConversionStrategy property.
    pub frame_rate_conversion_strategy: Option<String>,
    /// gopDuration property.
    pub gop_duration: Option<String>,
    /// gopFrameCount property.
    pub gop_frame_count: Option<i64>,
    /// heightPixels property.
    pub height_pixels: Option<i64>,
    /// hlg property.
    pub hlg: Option<H264ColorFormatHLG>,
    /// pixelFormat property.
    pub pixel_format: Option<String>,
    /// preset property.
    pub preset: Option<String>,
    /// profile property.
    pub profile: Option<String>,
    /// rateControlMode property.
    pub rate_control_mode: Option<String>,
    /// sdr property.
    pub sdr: Option<H264ColorFormatSDR>,
    /// tune property.
    pub tune: Option<String>,
    /// vbvFullnessBits property.
    pub vbv_fullness_bits: Option<i64>,
    /// vbvSizeBits property.
    pub vbv_size_bits: Option<i64>,
    /// widthPixels property.
    pub width_pixels: Option<i64>,
}

/// `H265ColorFormatHLG` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct H265ColorFormatHLG {}

/// `Aes128Encryption` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Aes128Encryption {}

/// `VideoStream` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct VideoStream {
    /// h264 property.
    pub h264: Option<H264CodecSettings>,
    /// h265 property.
    pub h265: Option<H265CodecSettings>,
    /// vp9 property.
    pub vp9: Option<Vp9CodecSettings>,
}

/// `Audio` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Audio {
    /// highBoost property.
    pub high_boost: Option<bool>,
    /// lowBoost property.
    pub low_boost: Option<bool>,
    /// lufs property.
    pub lufs: Option<f64>,
}

/// `Deinterlace` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Deinterlace {
    /// bwdif property.
    pub bwdif: Option<BwdifConfig>,
    /// yadif property.
    pub yadif: Option<YadifConfig>,
}

/// `AnimationFade` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct AnimationFade {
    /// endTimeOffset property.
    pub end_time_offset: Option<String>,
    /// fadeType property.
    pub fade_type: Option<String>,
    /// startTimeOffset property.
    pub start_time_offset: Option<String>,
    /// xy property.
    pub xy: Option<NormalizedCoordinate>,
}

/// `Manifest` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Manifest {
    /// dash property.
    pub dash: Option<DashConfig>,
    /// fileName property.
    pub file_name: Option<String>,
    /// muxStreams property.
    pub mux_streams: Option<Vec<String>>,
    /// type property.
    pub r#type: Option<String>,
}

/// `Crop` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Crop {
    /// bottomPixels property.
    pub bottom_pixels: Option<i64>,
    /// leftPixels property.
    pub left_pixels: Option<i64>,
    /// rightPixels property.
    pub right_pixels: Option<i64>,
    /// topPixels property.
    pub top_pixels: Option<i64>,
}

/// `Pad` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Pad {
    /// bottomPixels property.
    pub bottom_pixels: Option<i64>,
    /// leftPixels property.
    pub left_pixels: Option<i64>,
    /// rightPixels property.
    pub right_pixels: Option<i64>,
    /// topPixels property.
    pub top_pixels: Option<i64>,
}

/// `Job` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Job {
    /// batchModePriority property.
    pub batch_mode_priority: Option<i64>,
    /// config property.
    pub config: Option<JobConfig>,
    /// createTime property.
    pub create_time: Option<String>,
    /// endTime property.
    pub end_time: Option<String>,
    /// error property.
    pub error: Option<Status>,
    /// fillContentGaps property.
    pub fill_content_gaps: Option<bool>,
    /// inputUri property.
    pub input_uri: Option<String>,
    /// labels property.
    pub labels: Option<serde_json::Value>,
    /// mode property.
    pub mode: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// optimization property.
    pub optimization: Option<String>,
    /// outputUri property.
    pub output_uri: Option<String>,
    /// startTime property.
    pub start_time: Option<String>,
    /// state property.
    pub state: Option<String>,
    /// templateId property.
    pub template_id: Option<String>,
    /// ttlAfterCompletionDays property.
    pub ttl_after_completion_days: Option<i64>,
}

/// `DrmSystems` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct DrmSystems {
    /// clearkey property.
    pub clearkey: Option<Clearkey>,
    /// fairplay property.
    pub fairplay: Option<Fairplay>,
    /// playready property.
    pub playready: Option<Playready>,
    /// widevine property.
    pub widevine: Option<Widevine>,
}

/// `MpegCommonEncryption` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct MpegCommonEncryption {
    /// scheme property.
    pub scheme: Option<String>,
}

/// `Fmp4Config` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Fmp4Config {
    /// codecTag property.
    pub codec_tag: Option<String>,
}

/// `SpriteSheet` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct SpriteSheet {
    /// columnCount property.
    pub column_count: Option<i64>,
    /// endTimeOffset property.
    pub end_time_offset: Option<String>,
    /// filePrefix property.
    pub file_prefix: Option<String>,
    /// format property.
    pub format: Option<String>,
    /// interval property.
    pub interval: Option<String>,
    /// quality property.
    pub quality: Option<i64>,
    /// rowCount property.
    pub row_count: Option<i64>,
    /// spriteHeightPixels property.
    pub sprite_height_pixels: Option<i64>,
    /// spriteWidthPixels property.
    pub sprite_width_pixels: Option<i64>,
    /// startTimeOffset property.
    pub start_time_offset: Option<String>,
    /// totalCount property.
    pub total_count: Option<i64>,
}

/// `ElementaryStream` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ElementaryStream {
    /// audioStream property.
    pub audio_stream: Option<AudioStream>,
    /// key property.
    pub key: Option<String>,
    /// textStream property.
    pub text_stream: Option<TextStream>,
    /// videoStream property.
    pub video_stream: Option<VideoStream>,
}

/// `Denoise` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Denoise {
    /// strength property.
    pub strength: Option<f64>,
    /// tune property.
    pub tune: Option<String>,
}

/// `EditAtom` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct EditAtom {
    /// endTimeOffset property.
    pub end_time_offset: Option<String>,
    /// inputs property.
    pub inputs: Option<Vec<String>>,
    /// key property.
    pub key: Option<String>,
    /// startTimeOffset property.
    pub start_time_offset: Option<String>,
}

/// `Image` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Image {
    /// alpha property.
    pub alpha: Option<f64>,
    /// resolution property.
    pub resolution: Option<NormalizedCoordinate>,
    /// uri property.
    pub uri: Option<String>,
}

/// `Output` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Output {
    /// uri property.
    pub uri: Option<String>,
}

/// `Encryption` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Encryption {
    /// aes128 property.
    pub aes128: Option<Aes128Encryption>,
    /// drmSystems property.
    pub drm_systems: Option<DrmSystems>,
    /// id property.
    pub id: Option<String>,
    /// mpegCenc property.
    pub mpeg_cenc: Option<MpegCommonEncryption>,
    /// sampleAes property.
    pub sample_aes: Option<SampleAesEncryption>,
    /// secretManagerKeySource property.
    pub secret_manager_key_source: Option<SecretManagerSource>,
}

/// `YadifConfig` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct YadifConfig {
    /// deinterlaceAllFrames property.
    pub deinterlace_all_frames: Option<bool>,
    /// disableSpatialInterlacing property.
    pub disable_spatial_interlacing: Option<bool>,
    /// mode property.
    pub mode: Option<String>,
    /// parity property.
    pub parity: Option<String>,
}

/// `SampleAesEncryption` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct SampleAesEncryption {}

/// `H265ColorFormatSDR` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct H265ColorFormatSDR {}

/// `Clearkey` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Clearkey {}

/// `H264ColorFormatSDR` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct H264ColorFormatSDR {}

/// `Widevine` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Widevine {}

/// `Vp9ColorFormatSDR` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Vp9ColorFormatSDR {}

/// `PubsubDestination` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct PubsubDestination {
    /// topic property.
    pub topic: Option<String>,
}

/// `AudioMapping` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct AudioMapping {
    /// atomKey property.
    pub atom_key: Option<String>,
    /// gainDb property.
    pub gain_db: Option<f64>,
    /// inputChannel property.
    pub input_channel: Option<i64>,
    /// inputKey property.
    pub input_key: Option<String>,
    /// inputTrack property.
    pub input_track: Option<i64>,
    /// outputChannel property.
    pub output_channel: Option<i64>,
}

/// `SegmentSettings` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct SegmentSettings {
    /// individualSegments property.
    pub individual_segments: Option<bool>,
    /// segmentDuration property.
    pub segment_duration: Option<String>,
}

/// `DashConfig` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct DashConfig {
    /// segmentReferenceScheme property.
    pub segment_reference_scheme: Option<String>,
}

/// `Deblock` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Deblock {
    /// enabled property.
    pub enabled: Option<bool>,
    /// strength property.
    pub strength: Option<f64>,
}

/// `InputAttributes` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct InputAttributes {
    /// trackDefinitions property.
    pub track_definitions: Option<Vec<TrackDefinition>>,
}

/// `Playready` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Playready {}

/// `NormalizedCoordinate` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct NormalizedCoordinate {
    /// x property.
    pub x: Option<f64>,
    /// y property.
    pub y: Option<f64>,
}

/// `Color` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Color {
    /// brightness property.
    pub brightness: Option<f64>,
    /// contrast property.
    pub contrast: Option<f64>,
    /// saturation property.
    pub saturation: Option<f64>,
}

/// `Vp9CodecSettings` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Vp9CodecSettings {
    /// bitrateBps property.
    pub bitrate_bps: Option<i64>,
    /// crfLevel property.
    pub crf_level: Option<i64>,
    /// frameRate property.
    pub frame_rate: Option<f64>,
    /// frameRateConversionStrategy property.
    pub frame_rate_conversion_strategy: Option<String>,
    /// gopDuration property.
    pub gop_duration: Option<String>,
    /// gopFrameCount property.
    pub gop_frame_count: Option<i64>,
    /// heightPixels property.
    pub height_pixels: Option<i64>,
    /// hlg property.
    pub hlg: Option<Vp9ColorFormatHLG>,
    /// pixelFormat property.
    pub pixel_format: Option<String>,
    /// profile property.
    pub profile: Option<String>,
    /// rateControlMode property.
    pub rate_control_mode: Option<String>,
    /// sdr property.
    pub sdr: Option<Vp9ColorFormatSDR>,
    /// widthPixels property.
    pub width_pixels: Option<i64>,
}

/// `JobConfig` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct JobConfig {
    /// adBreaks property.
    pub ad_breaks: Option<Vec<AdBreak>>,
    /// editList property.
    pub edit_list: Option<Vec<EditAtom>>,
    /// elementaryStreams property.
    pub elementary_streams: Option<Vec<ElementaryStream>>,
    /// encryptions property.
    pub encryptions: Option<Vec<Encryption>>,
    /// inputs property.
    pub inputs: Option<Vec<Input>>,
    /// manifests property.
    pub manifests: Option<Vec<Manifest>>,
    /// muxStreams property.
    pub mux_streams: Option<Vec<MuxStream>>,
    /// output property.
    pub output: Option<Output>,
    /// overlays property.
    pub overlays: Option<Vec<Overlay>>,
    /// pubsubDestination property.
    pub pubsub_destination: Option<PubsubDestination>,
    /// spriteSheets property.
    pub sprite_sheets: Option<Vec<SpriteSheet>>,
}

/// `AudioStream` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct AudioStream {
    /// bitrateBps property.
    pub bitrate_bps: Option<i64>,
    /// channelCount property.
    pub channel_count: Option<i64>,
    /// channelLayout property.
    pub channel_layout: Option<Vec<String>>,
    /// codec property.
    pub codec: Option<String>,
    /// displayName property.
    pub display_name: Option<String>,
    /// languageCode property.
    pub language_code: Option<String>,
    /// mapping property.
    pub mapping: Option<Vec<AudioMapping>>,
    /// sampleRateHertz property.
    pub sample_rate_hertz: Option<i64>,
}

/// `AnimationEnd` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct AnimationEnd {
    /// startTimeOffset property.
    pub start_time_offset: Option<String>,
}

/// `PreprocessingConfig` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct PreprocessingConfig {
    /// audio property.
    pub audio: Option<Audio>,
    /// color property.
    pub color: Option<Color>,
    /// crop property.
    pub crop: Option<Crop>,
    /// deblock property.
    pub deblock: Option<Deblock>,
    /// deinterlace property.
    pub deinterlace: Option<Deinterlace>,
    /// denoise property.
    pub denoise: Option<Denoise>,
    /// pad property.
    pub pad: Option<Pad>,
}

/// `AnimationStatic` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct AnimationStatic {
    /// startTimeOffset property.
    pub start_time_offset: Option<String>,
    /// xy property.
    pub xy: Option<NormalizedCoordinate>,
}

/// `Status` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Status {
    /// code property.
    pub code: Option<i64>,
    /// details property.
    pub details: Option<Vec<serde_json::Value>>,
    /// message property.
    pub message: Option<String>,
}

/// `MuxStream` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct MuxStream {
    /// container property.
    pub container: Option<String>,
    /// elementaryStreams property.
    pub elementary_streams: Option<Vec<String>>,
    /// encryptionId property.
    pub encryption_id: Option<String>,
    /// fileName property.
    pub file_name: Option<String>,
    /// fmp4 property.
    pub fmp4: Option<Fmp4Config>,
    /// key property.
    pub key: Option<String>,
    /// segmentSettings property.
    pub segment_settings: Option<SegmentSettings>,
}

/// `SecretManagerSource` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct SecretManagerSource {
    /// secretVersion property.
    pub secret_version: Option<String>,
}

/// `AdBreak` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct AdBreak {
    /// startTimeOffset property.
    pub start_time_offset: Option<String>,
}

/// `H265ColorFormatHDR10` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct H265ColorFormatHDR10 {}

/// `Vp9ColorFormatHLG` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Vp9ColorFormatHLG {}

/// `TextStream` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct TextStream {
    /// codec property.
    pub codec: Option<String>,
    /// displayName property.
    pub display_name: Option<String>,
    /// languageCode property.
    pub language_code: Option<String>,
    /// mapping property.
    pub mapping: Option<Vec<TextMapping>>,
}

/// `TrackDefinition` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct TrackDefinition {
    /// detectLanguages property.
    pub detect_languages: Option<bool>,
    /// detectedLanguages property.
    pub detected_languages: Option<Vec<String>>,
    /// inputTrack property.
    pub input_track: Option<i64>,
    /// languages property.
    pub languages: Option<Vec<String>>,
}

/// `H265CodecSettings` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct H265CodecSettings {
    /// allowOpenGop property.
    pub allow_open_gop: Option<bool>,
    /// aqStrength property.
    pub aq_strength: Option<f64>,
    /// bFrameCount property.
    pub b_frame_count: Option<i64>,
    /// bPyramid property.
    pub b_pyramid: Option<bool>,
    /// bitrateBps property.
    pub bitrate_bps: Option<i64>,
    /// crfLevel property.
    pub crf_level: Option<i64>,
    /// enableTwoPass property.
    pub enable_two_pass: Option<bool>,
    /// frameRate property.
    pub frame_rate: Option<f64>,
    /// frameRateConversionStrategy property.
    pub frame_rate_conversion_strategy: Option<String>,
    /// gopDuration property.
    pub gop_duration: Option<String>,
    /// gopFrameCount property.
    pub gop_frame_count: Option<i64>,
    /// hdr10 property.
    pub hdr10: Option<H265ColorFormatHDR10>,
    /// heightPixels property.
    pub height_pixels: Option<i64>,
    /// hlg property.
    pub hlg: Option<H265ColorFormatHLG>,
    /// pixelFormat property.
    pub pixel_format: Option<String>,
    /// preset property.
    pub preset: Option<String>,
    /// profile property.
    pub profile: Option<String>,
    /// rateControlMode property.
    pub rate_control_mode: Option<String>,
    /// sdr property.
    pub sdr: Option<H265ColorFormatSDR>,
    /// tune property.
    pub tune: Option<String>,
    /// vbvFullnessBits property.
    pub vbv_fullness_bits: Option<i64>,
    /// vbvSizeBits property.
    pub vbv_size_bits: Option<i64>,
    /// widthPixels property.
    pub width_pixels: Option<i64>,
}

/// `Animation` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Animation {
    /// animationEnd property.
    pub animation_end: Option<AnimationEnd>,
    /// animationFade property.
    pub animation_fade: Option<AnimationFade>,
    /// animationStatic property.
    pub animation_static: Option<AnimationStatic>,
}

/// `TextMapping` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct TextMapping {
    /// atomKey property.
    pub atom_key: Option<String>,
    /// inputKey property.
    pub input_key: Option<String>,
    /// inputTrack property.
    pub input_track: Option<i64>,
}

// =============================================================================
// ARGS TYPES (per-endpoint)
// =============================================================================

/// Arguments for [`transcoder.projects.locations.jobs.create_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct TranscoderProjectsLocationsJobsCreateArgs {
    /// Path parameter: `parent`.
    pub parent: String,
}

/// Arguments for [`transcoder.projects.locations.jobs.delete_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct TranscoderProjectsLocationsJobsDeleteArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `allowMissing`.
    pub allow_missing: Option<String>,
}

/// Arguments for [`transcoder.projects.locations.jobs.get_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct TranscoderProjectsLocationsJobsGetArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`transcoder.projects.locations.jobs.list_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct TranscoderProjectsLocationsJobsListArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

// =============================================================================
// CLIENT FUNCTIONS (per-endpoint)
// =============================================================================

// -----------------------------------------------------------------------------
// POST v1/projects/{projectsId}/locations/{locationsId}/jobs
// -----------------------------------------------------------------------------

/// POST v1/projects/{projectsId}/locations/{locationsId}/jobs.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = transcoder_projects_locations_jobs_create_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn transcoder_projects_locations_jobs_create_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Job>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://transcoder.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/jobs",
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Job = serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                    super::shared::ApiError::ParseFailed(e.to_string())
                })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE v1/projects/{projectsId}/locations/{locationsId}/jobs/{jobsId}
// -----------------------------------------------------------------------------

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/jobs/{jobsId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = transcoder_projects_locations_jobs_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn transcoder_projects_locations_jobs_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://transcoder.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/jobs/{{jobsId}}",
    );

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Empty =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/projects/{projectsId}/locations/{locationsId}/jobs/{jobsId}
// -----------------------------------------------------------------------------

/// GET v1/projects/{projectsId}/locations/{locationsId}/jobs/{jobsId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = transcoder_projects_locations_jobs_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn transcoder_projects_locations_jobs_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Job>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://transcoder.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/jobs/{{jobsId}}",
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Job = serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                    super::shared::ApiError::ParseFailed(e.to_string())
                })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/projects/{projectsId}/locations/{locationsId}/jobs
// -----------------------------------------------------------------------------

/// GET v1/projects/{projectsId}/locations/{locationsId}/jobs.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = transcoder_projects_locations_jobs_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn transcoder_projects_locations_jobs_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListJobsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://transcoder.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/jobs",
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListJobsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}
