//! Auto-generated API module for gcp/kgsearch kgsearch.
//!
//! Generated by `cargo run --bin ewe_platform gen_api`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp_kgsearch_kgsearch `

#![cfg(feature = "gcp_kgsearch_kgsearch")]
#![allow(clippy::too_many_arguments, clippy::type_complexity)]
#![allow(
    clippy::missing_errors_doc,
    clippy::doc_markdown,
    clippy::useless_format
)]
#![allow(unused_imports)]

use foundation_core::valtron::{TaskIterator, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{ClientRequestBuilder, SimpleHttpClient};
use foundation_macros::JsonHash;
use serde::{Deserialize, Serialize};

use super::shared::ApiResponse;

// =============================================================================
// TYPE DECLARATIONS
// =============================================================================

/// `SearchResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SearchResponse {
    /// @context property.
    pub context: Option<serde_json::Value>,
    /// @type property.
    pub r#type: Option<serde_json::Value>,
    /// itemListElement property.
    pub item_list_element: Option<Vec<serde_json::Value>>,
}

// =============================================================================
// ARGS TYPES (per-endpoint)
// =============================================================================

/// Arguments for [`kgsearch.entities.search_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct KgsearchEntitiesSearchArgs {
    /// Query parameter: `ids`.
    pub ids: Option<String>,
    /// Query parameter: `indent`.
    pub indent: Option<String>,
    /// Query parameter: `languages`.
    pub languages: Option<String>,
    /// Query parameter: `limit`.
    pub limit: Option<String>,
    /// Query parameter: `prefix`.
    pub prefix: Option<String>,
    /// Query parameter: `query`.
    pub query: Option<String>,
    /// Query parameter: `types`.
    pub types: Option<String>,
}

// =============================================================================
// CLIENT FUNCTIONS (per-endpoint)
// =============================================================================

// -----------------------------------------------------------------------------
// GET v1/entities:search
// -----------------------------------------------------------------------------

/// GET v1/entities:search.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = kgsearch_entities_search_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn kgsearch_entities_search_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &KgsearchEntitiesSearchArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SearchResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://kgsearch.googleapis.com/v1/entities:search",);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.ids {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("ids=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.indent {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("indent=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.languages {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("languages=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.limit {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("limit=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.prefix {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("prefix=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.query {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("query=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.types {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("types=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: SearchResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}
