//! Auto-generated API module for gcp/oracledatabase dbSystems.
//!
//! Generated by `cargo run --bin ewe_platform gen_api`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp_oracledatabase_db_systems `

#![cfg(feature = "gcp_oracledatabase_db_systems")]
#![allow(clippy::too_many_arguments, clippy::type_complexity)]
#![allow(
    clippy::missing_errors_doc,
    clippy::doc_markdown,
    clippy::useless_format
)]
#![allow(unused_imports)]

use foundation_core::valtron::{TaskIterator, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{ClientRequestBuilder, SimpleHttpClient};
use foundation_macros::JsonHash;
use serde::{Deserialize, Serialize};

// Import shared types used by this module
use super::shared::Operation;

use super::shared::ApiResponse;

// =============================================================================
// TYPE DECLARATIONS
// =============================================================================

/// `TimeZone` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct TimeZone {
    /// id property.
    pub id: Option<String>,
    /// version property.
    pub version: Option<String>,
}

/// `DbHome` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct DbHome {
    /// database property.
    pub database: Option<Database>,
    /// dbVersion property.
    pub db_version: Option<String>,
    /// displayName property.
    pub display_name: Option<String>,
    /// isUnifiedAuditingEnabled property.
    pub is_unified_auditing_enabled: Option<bool>,
}

/// `Status` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Status {
    /// code property.
    pub code: Option<i64>,
    /// details property.
    pub details: Option<Vec<serde_json::Value>>,
    /// message property.
    pub message: Option<String>,
}

/// `DbSystemProperties` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct DbSystemProperties {
    /// computeCount property.
    pub compute_count: Option<i64>,
    /// computeModel property.
    pub compute_model: Option<String>,
    /// dataCollectionOptions property.
    pub data_collection_options: Option<DataCollectionOptionsDbSystem>,
    /// dataStorageSizeGb property.
    pub data_storage_size_gb: Option<i64>,
    /// databaseEdition property.
    pub database_edition: Option<String>,
    /// dbHome property.
    pub db_home: Option<DbHome>,
    /// dbSystemOptions property.
    pub db_system_options: Option<DbSystemOptions>,
    /// domain property.
    pub domain: Option<String>,
    /// hostname property.
    pub hostname: Option<String>,
    /// hostnamePrefix property.
    pub hostname_prefix: Option<String>,
    /// initialDataStorageSizeGb property.
    pub initial_data_storage_size_gb: Option<i64>,
    /// licenseModel property.
    pub license_model: Option<String>,
    /// lifecycleState property.
    pub lifecycle_state: Option<String>,
    /// memorySizeGb property.
    pub memory_size_gb: Option<i64>,
    /// nodeCount property.
    pub node_count: Option<i64>,
    /// ocid property.
    pub ocid: Option<String>,
    /// privateIp property.
    pub private_ip: Option<String>,
    /// recoStorageSizeGb property.
    pub reco_storage_size_gb: Option<i64>,
    /// shape property.
    pub shape: Option<String>,
    /// sshPublicKeys property.
    pub ssh_public_keys: Option<Vec<String>>,
    /// timeZone property.
    pub time_zone: Option<TimeZone>,
}

/// `BackupDestinationDetails` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct BackupDestinationDetails {
    /// type property.
    pub r#type: Option<String>,
}

/// `DbSystem` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct DbSystem {
    /// createTime property.
    pub create_time: Option<String>,
    /// displayName property.
    pub display_name: Option<String>,
    /// entitlementId property.
    pub entitlement_id: Option<String>,
    /// gcpOracleZone property.
    pub gcp_oracle_zone: Option<String>,
    /// labels property.
    pub labels: Option<serde_json::Value>,
    /// name property.
    pub name: Option<String>,
    /// ociUrl property.
    pub oci_url: Option<String>,
    /// odbNetwork property.
    pub odb_network: Option<String>,
    /// odbSubnet property.
    pub odb_subnet: Option<String>,
    /// properties property.
    pub properties: Option<DbSystemProperties>,
}

/// `DbSystemOptions` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct DbSystemOptions {
    /// storageManagement property.
    pub storage_management: Option<String>,
}

/// `ListDbSystemsResponse` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ListDbSystemsResponse {
    /// dbSystems property.
    pub db_systems: Option<Vec<DbSystem>>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
}

/// `DatabaseProperties` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct DatabaseProperties {
    /// databaseManagementConfig property.
    pub database_management_config: Option<DatabaseManagementConfig>,
    /// dbBackupConfig property.
    pub db_backup_config: Option<DbBackupConfig>,
    /// dbVersion property.
    pub db_version: Option<String>,
    /// state property.
    pub state: Option<String>,
}

/// `DbBackupConfig` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct DbBackupConfig {
    /// autoBackupEnabled property.
    pub auto_backup_enabled: Option<bool>,
    /// autoFullBackupDay property.
    pub auto_full_backup_day: Option<String>,
    /// autoFullBackupWindow property.
    pub auto_full_backup_window: Option<String>,
    /// autoIncrementalBackupWindow property.
    pub auto_incremental_backup_window: Option<String>,
    /// backupDeletionPolicy property.
    pub backup_deletion_policy: Option<String>,
    /// backupDestinationDetails property.
    pub backup_destination_details: Option<Vec<BackupDestinationDetails>>,
    /// retentionPeriodDays property.
    pub retention_period_days: Option<i64>,
}

/// `Database` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Database {
    /// adminPassword property.
    pub admin_password: Option<String>,
    /// adminPasswordSecretVersion property.
    pub admin_password_secret_version: Option<String>,
    /// characterSet property.
    pub character_set: Option<String>,
    /// createTime property.
    pub create_time: Option<String>,
    /// databaseId property.
    pub database_id: Option<String>,
    /// dbHomeName property.
    pub db_home_name: Option<String>,
    /// dbName property.
    pub db_name: Option<String>,
    /// dbUniqueName property.
    pub db_unique_name: Option<String>,
    /// gcpOracleZone property.
    pub gcp_oracle_zone: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// ncharacterSet property.
    pub ncharacter_set: Option<String>,
    /// ociUrl property.
    pub oci_url: Option<String>,
    /// opsInsightsStatus property.
    pub ops_insights_status: Option<String>,
    /// pluggableDatabaseId property.
    pub pluggable_database_id: Option<String>,
    /// pluggableDatabaseName property.
    pub pluggable_database_name: Option<String>,
    /// properties property.
    pub properties: Option<DatabaseProperties>,
    /// tdeWalletPassword property.
    pub tde_wallet_password: Option<String>,
    /// tdeWalletPasswordSecretVersion property.
    pub tde_wallet_password_secret_version: Option<String>,
}

/// `DatabaseManagementConfig` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct DatabaseManagementConfig {
    /// managementState property.
    pub management_state: Option<String>,
    /// managementType property.
    pub management_type: Option<String>,
}

/// `DataCollectionOptionsDbSystem` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct DataCollectionOptionsDbSystem {
    /// isDiagnosticsEventsEnabled property.
    pub is_diagnostics_events_enabled: Option<bool>,
    /// isIncidentLogsEnabled property.
    pub is_incident_logs_enabled: Option<bool>,
}

// =============================================================================
// ARGS TYPES (per-endpoint)
// =============================================================================

/// Arguments for [`oracledatabase.projects.locations.dbSystems.create_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct OracledatabaseProjectsLocationsDbSystemsCreateArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `dbSystemId`.
    pub db_system_id: Option<String>,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
}

/// Arguments for [`oracledatabase.projects.locations.dbSystems.delete_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct OracledatabaseProjectsLocationsDbSystemsDeleteArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
}

/// Arguments for [`oracledatabase.projects.locations.dbSystems.get_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct OracledatabaseProjectsLocationsDbSystemsGetArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`oracledatabase.projects.locations.dbSystems.list_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct OracledatabaseProjectsLocationsDbSystemsListArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

// =============================================================================
// CLIENT FUNCTIONS (per-endpoint)
// =============================================================================

// -----------------------------------------------------------------------------
// POST v1/projects/{projectsId}/locations/{locationsId}/dbSystems
// -----------------------------------------------------------------------------

/// POST v1/projects/{projectsId}/locations/{locationsId}/dbSystems.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = oracledatabase_projects_locations_db_systems_create_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn oracledatabase_projects_locations_db_systems_create_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://oracledatabase.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/dbSystems",
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE v1/projects/{projectsId}/locations/{locationsId}/dbSystems/{dbSystemsId}
// -----------------------------------------------------------------------------

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/dbSystems/{dbSystemsId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = oracledatabase_projects_locations_db_systems_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn oracledatabase_projects_locations_db_systems_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://oracledatabase.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/dbSystems/{{dbSystemsId}}",
    );

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/projects/{projectsId}/locations/{locationsId}/dbSystems/{dbSystemsId}
// -----------------------------------------------------------------------------

/// GET v1/projects/{projectsId}/locations/{locationsId}/dbSystems/{dbSystemsId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = oracledatabase_projects_locations_db_systems_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn oracledatabase_projects_locations_db_systems_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<DbSystem>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://oracledatabase.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/dbSystems/{{dbSystemsId}}",
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: DbSystem =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/projects/{projectsId}/locations/{locationsId}/dbSystems
// -----------------------------------------------------------------------------

/// GET v1/projects/{projectsId}/locations/{locationsId}/dbSystems.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = oracledatabase_projects_locations_db_systems_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn oracledatabase_projects_locations_db_systems_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListDbSystemsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://oracledatabase.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/dbSystems",
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListDbSystemsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}
