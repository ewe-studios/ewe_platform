//! Auto-generated API module for gcp/admob mediationReport:generate.
//!
//! Generated by `cargo run --bin ewe_platform gen_api`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp_admob_mediation_report_generate `

#![cfg(feature = "gcp_admob_mediation_report_generate")]
#![allow(clippy::too_many_arguments, clippy::type_complexity)]
#![allow(
    clippy::missing_errors_doc,
    clippy::doc_markdown,
    clippy::useless_format
)]

use foundation_core::valtron::{execute, StreamIterator, TaskIterator, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{ClientRequestBuilder, SimpleHttpClient};
use foundation_macros::JsonHash;
use serde::{Deserialize, Serialize};

use super::shared::{ApiError, ApiPending, ApiResponse};

// =============================================================================
// TYPE DECLARATIONS
// =============================================================================

/// `DateRange` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct DateRange {
    /// endDate property.
    pub end_date: Option<Date>,
    /// startDate property.
    pub start_date: Option<Date>,
}

/// `ReportRow` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ReportRow {
    /// dimensionValues property.
    pub dimension_values: Option<serde_json::Value>,
    /// metricValues property.
    pub metric_values: Option<serde_json::Value>,
}

/// `LocalizationSettings` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct LocalizationSettings {
    /// currencyCode property.
    pub currency_code: Option<String>,
    /// languageCode property.
    pub language_code: Option<String>,
}

/// `GenerateMediationReportResponse` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct GenerateMediationReportResponse {
    /// footer property.
    pub footer: Option<ReportFooter>,
    /// header property.
    pub header: Option<ReportHeader>,
    /// row property.
    pub row: Option<ReportRow>,
}

/// `ReportWarning` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ReportWarning {
    /// description property.
    pub description: Option<String>,
    /// type property.
    pub r#type: Option<String>,
}

/// `ReportFooter` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ReportFooter {
    /// matchingRowCount property.
    pub matching_row_count: Option<String>,
    /// warnings property.
    pub warnings: Option<Vec<ReportWarning>>,
}

/// `Date` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Date {
    /// day property.
    pub day: Option<i64>,
    /// month property.
    pub month: Option<i64>,
    /// year property.
    pub year: Option<i64>,
}

/// `ReportHeader` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ReportHeader {
    /// dateRange property.
    pub date_range: Option<DateRange>,
    /// localizationSettings property.
    pub localization_settings: Option<LocalizationSettings>,
    /// reportingTimeZone property.
    pub reporting_time_zone: Option<String>,
}

// =============================================================================
// ARGS TYPES (per-endpoint)
// =============================================================================

/// Arguments for [`admob.accounts.mediationReport.generate_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AdmobAccountsMediationReportGenerateArgs {
    /// Path parameter: `parent`.
    pub parent: String,
}

// =============================================================================
// CLIENT FUNCTIONS (per-endpoint)
// =============================================================================

// -----------------------------------------------------------------------------
// POST v1/accounts/{accountsId}/mediationReport:generate
// -----------------------------------------------------------------------------

/// POST v1/accounts/{accountsId}/mediationReport:generate.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = admob_accounts_mediation_report_generate_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn admob_accounts_mediation_report_generate_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GenerateMediationReportResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://admob.googleapis.com/v1/accounts/{{accountsId}}/mediationReport:generate",
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GenerateMediationReportResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}
