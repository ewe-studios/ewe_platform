//! Auto-generated API module for gcp/admob admob.
//!
//! Generated by `cargo run --bin ewe_platform gen_api`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp_admob_admob `

#![cfg(feature = "gcp_admob_admob")]
#![allow(clippy::too_many_arguments, clippy::type_complexity)]
#![allow(
    clippy::missing_errors_doc,
    clippy::doc_markdown,
    clippy::useless_format
)]
#![allow(unused_imports)]

use foundation_core::valtron::{TaskIterator, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{ClientRequestBuilder, SimpleHttpClient};
use foundation_macros::JsonHash;
use serde::{Deserialize, Serialize};

use super::shared::ApiResponse;

// =============================================================================
// TYPE DECLARATIONS
// =============================================================================

/// `ListAppsResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ListAppsResponse {
    /// apps property.
    pub apps: Option<Vec<App>>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
}

/// `GenerateMediationReportRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GenerateMediationReportRequest {
    /// reportSpec property.
    pub report_spec: Option<MediationReportSpec>,
}

/// `GenerateMediationReportResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GenerateMediationReportResponse {
    /// footer property.
    pub footer: Option<ReportFooter>,
    /// header property.
    pub header: Option<ReportHeader>,
    /// row property.
    pub row: Option<ReportRow>,
}

/// `App` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct App {
    /// appApprovalState property.
    pub app_approval_state: Option<String>,
    /// appId property.
    pub app_id: Option<String>,
    /// linkedAppInfo property.
    pub linked_app_info: Option<AppLinkedAppInfo>,
    /// manualAppInfo property.
    pub manual_app_info: Option<AppManualAppInfo>,
    /// name property.
    pub name: Option<String>,
    /// platform property.
    pub platform: Option<String>,
}

/// `GenerateNetworkReportResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GenerateNetworkReportResponse {
    /// footer property.
    pub footer: Option<ReportFooter>,
    /// header property.
    pub header: Option<ReportHeader>,
    /// row property.
    pub row: Option<ReportRow>,
}

/// `AppLinkedAppInfo` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AppLinkedAppInfo {
    /// appStoreId property.
    pub app_store_id: Option<String>,
    /// displayName property.
    pub display_name: Option<String>,
}

/// `NetworkReportSpecDimensionFilter` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct NetworkReportSpecDimensionFilter {
    /// dimension property.
    pub dimension: Option<String>,
    /// matchesAny property.
    pub matches_any: Option<StringList>,
}

/// `NetworkReportSpecSortCondition` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct NetworkReportSpecSortCondition {
    /// dimension property.
    pub dimension: Option<String>,
    /// metric property.
    pub metric: Option<String>,
    /// order property.
    pub order: Option<String>,
}

/// `LocalizationSettings` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct LocalizationSettings {
    /// currencyCode property.
    pub currency_code: Option<String>,
    /// languageCode property.
    pub language_code: Option<String>,
}

/// `AdUnit` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AdUnit {
    /// adFormat property.
    pub ad_format: Option<String>,
    /// adTypes property.
    pub ad_types: Option<Vec<String>>,
    /// adUnitId property.
    pub ad_unit_id: Option<String>,
    /// appId property.
    pub app_id: Option<String>,
    /// displayName property.
    pub display_name: Option<String>,
    /// name property.
    pub name: Option<String>,
}

/// `MediationReportSpec` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct MediationReportSpec {
    /// dateRange property.
    pub date_range: Option<DateRange>,
    /// dimensionFilters property.
    pub dimension_filters: Option<Vec<MediationReportSpecDimensionFilter>>,
    /// dimensions property.
    pub dimensions: Option<Vec<String>>,
    /// localizationSettings property.
    pub localization_settings: Option<LocalizationSettings>,
    /// maxReportRows property.
    pub max_report_rows: Option<i64>,
    /// metrics property.
    pub metrics: Option<Vec<String>>,
    /// sortConditions property.
    pub sort_conditions: Option<Vec<MediationReportSpecSortCondition>>,
    /// timeZone property.
    pub time_zone: Option<String>,
}

/// `ListAdUnitsResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ListAdUnitsResponse {
    /// adUnits property.
    pub ad_units: Option<Vec<AdUnit>>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
}

/// `ReportRow` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ReportRow {
    /// dimensionValues property.
    pub dimension_values: Option<serde_json::Value>,
    /// metricValues property.
    pub metric_values: Option<serde_json::Value>,
}

/// `MediationReportSpecDimensionFilter` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct MediationReportSpecDimensionFilter {
    /// dimension property.
    pub dimension: Option<String>,
    /// matchesAny property.
    pub matches_any: Option<StringList>,
}

/// `Date` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Date {
    /// day property.
    pub day: Option<i64>,
    /// month property.
    pub month: Option<i64>,
    /// year property.
    pub year: Option<i64>,
}

/// `ReportWarning` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ReportWarning {
    /// description property.
    pub description: Option<String>,
    /// type property.
    pub r#type: Option<String>,
}

/// `DateRange` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct DateRange {
    /// endDate property.
    pub end_date: Option<Date>,
    /// startDate property.
    pub start_date: Option<Date>,
}

/// `ListPublisherAccountsResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ListPublisherAccountsResponse {
    /// account property.
    pub account: Option<Vec<PublisherAccount>>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
}

/// `StringList` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct StringList {
    /// values property.
    pub values: Option<Vec<String>>,
}

/// `PublisherAccount` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct PublisherAccount {
    /// currencyCode property.
    pub currency_code: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// publisherId property.
    pub publisher_id: Option<String>,
    /// reportingTimeZone property.
    pub reporting_time_zone: Option<String>,
}

/// `NetworkReportSpec` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct NetworkReportSpec {
    /// dateRange property.
    pub date_range: Option<DateRange>,
    /// dimensionFilters property.
    pub dimension_filters: Option<Vec<NetworkReportSpecDimensionFilter>>,
    /// dimensions property.
    pub dimensions: Option<Vec<String>>,
    /// localizationSettings property.
    pub localization_settings: Option<LocalizationSettings>,
    /// maxReportRows property.
    pub max_report_rows: Option<i64>,
    /// metrics property.
    pub metrics: Option<Vec<String>>,
    /// sortConditions property.
    pub sort_conditions: Option<Vec<NetworkReportSpecSortCondition>>,
    /// timeZone property.
    pub time_zone: Option<String>,
}

/// `MediationReportSpecSortCondition` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct MediationReportSpecSortCondition {
    /// dimension property.
    pub dimension: Option<String>,
    /// metric property.
    pub metric: Option<String>,
    /// order property.
    pub order: Option<String>,
}

/// `AppManualAppInfo` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AppManualAppInfo {
    /// displayName property.
    pub display_name: Option<String>,
}

/// `GenerateNetworkReportRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct GenerateNetworkReportRequest {
    /// reportSpec property.
    pub report_spec: Option<NetworkReportSpec>,
}

/// `ReportFooter` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ReportFooter {
    /// matchingRowCount property.
    pub matching_row_count: Option<String>,
    /// warnings property.
    pub warnings: Option<Vec<ReportWarning>>,
}

/// `ReportHeader` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ReportHeader {
    /// dateRange property.
    pub date_range: Option<DateRange>,
    /// localizationSettings property.
    pub localization_settings: Option<LocalizationSettings>,
    /// reportingTimeZone property.
    pub reporting_time_zone: Option<String>,
}

// =============================================================================
// ARGS TYPES (per-endpoint)
// =============================================================================

/// Arguments for [`admob.accounts.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct AdmobAccountsGetArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`admob.accounts.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct AdmobAccountsListArgs {
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`admob.accounts.adUnits.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct AdmobAccountsAdUnitsListArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`admob.accounts.apps.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct AdmobAccountsAppsListArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`admob.accounts.mediationReport.generate_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct AdmobAccountsMediationReportGenerateArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Request body.
    pub body: GenerateMediationReportRequest,
}

/// Arguments for [`admob.accounts.networkReport.generate_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct AdmobAccountsNetworkReportGenerateArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Request body.
    pub body: GenerateNetworkReportRequest,
}

// =============================================================================
// CLIENT FUNCTIONS (per-endpoint)
// =============================================================================

// -----------------------------------------------------------------------------
// GET v1/{+name}
// -----------------------------------------------------------------------------

/// GET v1/{+name}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = admob_accounts_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn admob_accounts_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &AdmobAccountsGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<PublisherAccount>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://admob.googleapis.com/v1/{}", args.name,);

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: PublisherAccount =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/accounts
// -----------------------------------------------------------------------------

/// GET v1/accounts.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = admob_accounts_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn admob_accounts_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &AdmobAccountsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListPublisherAccountsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://admob.googleapis.com/v1/accounts",);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListPublisherAccountsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+parent}/adUnits
// -----------------------------------------------------------------------------

/// GET v1/{+parent}/adUnits.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = admob_accounts_ad_units_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn admob_accounts_ad_units_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &AdmobAccountsAdUnitsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListAdUnitsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://admob.googleapis.com/v1/{}/adUnits", args.parent,);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListAdUnitsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/{+parent}/apps
// -----------------------------------------------------------------------------

/// GET v1/{+parent}/apps.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = admob_accounts_apps_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn admob_accounts_apps_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &AdmobAccountsAppsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListAppsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://admob.googleapis.com/v1/{}/apps", args.parent,);

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.page_size {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageSize=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.page_token {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("pageToken=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListAppsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/{+parent}/mediationReport:generate
// -----------------------------------------------------------------------------

/// POST v1/{+parent}/mediationReport:generate.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = admob_accounts_mediation_report_generate_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn admob_accounts_mediation_report_generate_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &AdmobAccountsMediationReportGenerateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GenerateMediationReportResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://admob.googleapis.com/v1/{}/mediationReport:generate",
        args.parent,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GenerateMediationReportResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/{+parent}/networkReport:generate
// -----------------------------------------------------------------------------

/// POST v1/{+parent}/networkReport:generate.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = admob_accounts_network_report_generate_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn admob_accounts_network_report_generate_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &AdmobAccountsNetworkReportGenerateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<GenerateNetworkReportResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://admob.googleapis.com/v1/{}/networkReport:generate",
        args.parent,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: GenerateNetworkReportResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}
