//! Auto-generated API module for gcp/verifiedaccess verifiedaccess.
//!
//! Generated by `cargo run --bin ewe_platform gen_api`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp_verifiedaccess_verifiedaccess `

#![cfg(feature = "gcp_verifiedaccess_verifiedaccess")]
#![allow(clippy::too_many_arguments, clippy::type_complexity)]
#![allow(
    clippy::missing_errors_doc,
    clippy::doc_markdown,
    clippy::useless_format
)]
#![allow(unused_imports)]

use foundation_core::valtron::{TaskIterator, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{ClientRequestBuilder, SimpleHttpClient};
use foundation_macros::JsonHash;
use serde::{Deserialize, Serialize};

// Import shared types used by this module
use super::shared::Empty;

use super::shared::ApiResponse;

// =============================================================================
// TYPE DECLARATIONS
// =============================================================================

/// `VerifyChallengeResponseResult` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct VerifyChallengeResponseResult {
    /// attestedDeviceId property.
    pub attested_device_id: Option<String>,
    /// customerId property.
    pub customer_id: Option<String>,
    /// deviceEnrollmentId property.
    pub device_enrollment_id: Option<String>,
    /// devicePermanentId property.
    pub device_permanent_id: Option<String>,
    /// deviceSignal property.
    pub device_signal: Option<String>,
    /// deviceSignals property.
    pub device_signals: Option<DeviceSignals>,
    /// keyTrustLevel property.
    pub key_trust_level: Option<String>,
    /// profileCustomerId property.
    pub profile_customer_id: Option<String>,
    /// profileKeyTrustLevel property.
    pub profile_key_trust_level: Option<String>,
    /// profilePermanentId property.
    pub profile_permanent_id: Option<String>,
    /// signedPublicKeyAndChallenge property.
    pub signed_public_key_and_challenge: Option<String>,
    /// virtualDeviceId property.
    pub virtual_device_id: Option<String>,
    /// virtualProfileId property.
    pub virtual_profile_id: Option<String>,
}

/// `Challenge` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Challenge {
    /// challenge property.
    pub challenge: Option<String>,
}

/// `DeviceSignals` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct DeviceSignals {
    /// allowScreenLock property.
    pub allow_screen_lock: Option<bool>,
    /// antivirus property.
    pub antivirus: Option<Antivirus>,
    /// browserVersion property.
    pub browser_version: Option<String>,
    /// builtInDnsClientEnabled property.
    pub built_in_dns_client_enabled: Option<bool>,
    /// chromeRemoteDesktopAppBlocked property.
    pub chrome_remote_desktop_app_blocked: Option<bool>,
    /// crowdStrikeAgent property.
    pub crowd_strike_agent: Option<CrowdStrikeAgent>,
    /// deviceAffiliationIds property.
    pub device_affiliation_ids: Option<Vec<String>>,
    /// deviceEnrollmentDomain property.
    pub device_enrollment_domain: Option<String>,
    /// deviceManufacturer property.
    pub device_manufacturer: Option<String>,
    /// deviceModel property.
    pub device_model: Option<String>,
    /// diskEncryption property.
    pub disk_encryption: Option<String>,
    /// displayName property.
    pub display_name: Option<String>,
    /// hostname property.
    pub hostname: Option<String>,
    /// imei property.
    pub imei: Option<Vec<String>>,
    /// macAddresses property.
    pub mac_addresses: Option<Vec<String>>,
    /// meid property.
    pub meid: Option<Vec<String>>,
    /// operatingSystem property.
    pub operating_system: Option<String>,
    /// osFirewall property.
    pub os_firewall: Option<String>,
    /// osVersion property.
    pub os_version: Option<String>,
    /// passwordProtectionWarningTrigger property.
    pub password_protection_warning_trigger: Option<String>,
    /// profileAffiliationIds property.
    pub profile_affiliation_ids: Option<Vec<String>>,
    /// profileEnrollmentDomain property.
    pub profile_enrollment_domain: Option<String>,
    /// realtimeUrlCheckMode property.
    pub realtime_url_check_mode: Option<String>,
    /// safeBrowsingProtectionLevel property.
    pub safe_browsing_protection_level: Option<String>,
    /// screenLockSecured property.
    pub screen_lock_secured: Option<String>,
    /// secureBootMode property.
    pub secure_boot_mode: Option<String>,
    /// serialNumber property.
    pub serial_number: Option<String>,
    /// siteIsolationEnabled property.
    pub site_isolation_enabled: Option<bool>,
    /// systemDnsServers property.
    pub system_dns_servers: Option<Vec<String>>,
    /// thirdPartyBlockingEnabled property.
    pub third_party_blocking_enabled: Option<bool>,
    /// trigger property.
    pub trigger: Option<String>,
    /// windowsMachineDomain property.
    pub windows_machine_domain: Option<String>,
    /// windowsUserDomain property.
    pub windows_user_domain: Option<String>,
}

/// `VerifyChallengeResponseRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct VerifyChallengeResponseRequest {
    /// challengeResponse property.
    pub challenge_response: Option<String>,
    /// expectedIdentity property.
    pub expected_identity: Option<String>,
}

/// `Antivirus` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Antivirus {
    /// state property.
    pub state: Option<String>,
}

/// `CrowdStrikeAgent` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct CrowdStrikeAgent {
    /// agentId property.
    pub agent_id: Option<String>,
    /// customerId property.
    pub customer_id: Option<String>,
}

// =============================================================================
// ARGS TYPES (per-endpoint)
// =============================================================================

/// Arguments for [`verifiedaccess.challenge.generate_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct VerifiedaccessChallengeGenerateArgs {
    /// Request body.
    pub body: Empty,
}

/// Arguments for [`verifiedaccess.challenge.verify_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct VerifiedaccessChallengeVerifyArgs {
    /// Request body.
    pub body: VerifyChallengeResponseRequest,
}

// =============================================================================
// CLIENT FUNCTIONS (per-endpoint)
// =============================================================================

// -----------------------------------------------------------------------------
// POST v2/challenge:generate
// -----------------------------------------------------------------------------

/// POST v2/challenge:generate.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = verifiedaccess_challenge_generate_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn verifiedaccess_challenge_generate_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &VerifiedaccessChallengeGenerateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Challenge>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://verifiedaccess.googleapis.com/v2/challenge:generate",);

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Challenge =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v2/challenge:verify
// -----------------------------------------------------------------------------

/// POST v2/challenge:verify.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = verifiedaccess_challenge_verify_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn verifiedaccess_challenge_verify_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &VerifiedaccessChallengeVerifyArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<VerifyChallengeResponseResult>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://verifiedaccess.googleapis.com/v2/challenge:verify",);

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: VerifyChallengeResponseResult =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}
