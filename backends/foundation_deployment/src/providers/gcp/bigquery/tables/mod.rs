//! Auto-generated API module for gcp/bigquery tables.
//!
//! Generated by `cargo run --bin ewe_platform gen_api`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp_bigquery_tables `

#![cfg(feature = "gcp_bigquery_tables")]
#![allow(clippy::too_many_arguments, clippy::type_complexity)]
#![allow(
    clippy::missing_errors_doc,
    clippy::doc_markdown,
    clippy::useless_format
)]

use foundation_core::valtron::{execute, StreamIterator, TaskIterator, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{ClientRequestBuilder, SimpleHttpClient};
use foundation_macros::JsonHash;
use serde::{Deserialize, Serialize};

// Import shared types used by this module
use super::shared::Policy;
use super::shared::TestIamPermissionsResponse;

use super::shared::{ApiError, ApiPending, ApiResponse};

// =============================================================================
// TYPE DECLARATIONS
// =============================================================================

/// `TableReference` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct TableReference {
    /// datasetId property.
    pub dataset_id: Option<String>,
    /// projectId property.
    pub project_id: Option<String>,
    /// tableId property.
    pub table_id: Option<String>,
}

/// `ForeignTypeInfo` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ForeignTypeInfo {
    /// typeSystem property.
    pub type_system: Option<String>,
}

/// `PartitionedColumn` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct PartitionedColumn {
    /// field property.
    pub field: Option<String>,
}

/// `EncryptionConfiguration` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct EncryptionConfiguration {
    /// kmsKeyName property.
    pub kms_key_name: Option<String>,
}

/// `Clustering` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Clustering {
    /// fields property.
    pub fields: Option<Vec<String>>,
}

/// `JoinRestrictionPolicy` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct JoinRestrictionPolicy {
    /// joinAllowedColumns property.
    pub join_allowed_columns: Option<Vec<String>>,
    /// joinCondition property.
    pub join_condition: Option<String>,
}

/// `ExternalCatalogTableOptions` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ExternalCatalogTableOptions {
    /// connectionId property.
    pub connection_id: Option<String>,
    /// parameters property.
    pub parameters: Option<serde_json::Value>,
    /// storageDescriptor property.
    pub storage_descriptor: Option<StorageDescriptor>,
}

/// `ExternalDataConfiguration` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ExternalDataConfiguration {
    /// autodetect property.
    pub autodetect: Option<bool>,
    /// avroOptions property.
    pub avro_options: Option<AvroOptions>,
    /// bigtableOptions property.
    pub bigtable_options: Option<BigtableOptions>,
    /// compression property.
    pub compression: Option<String>,
    /// connectionId property.
    pub connection_id: Option<String>,
    /// csvOptions property.
    pub csv_options: Option<CsvOptions>,
    /// dateFormat property.
    pub date_format: Option<String>,
    /// datetimeFormat property.
    pub datetime_format: Option<String>,
    /// decimalTargetTypes property.
    pub decimal_target_types: Option<Vec<String>>,
    /// fileSetSpecType property.
    pub file_set_spec_type: Option<String>,
    /// googleSheetsOptions property.
    pub google_sheets_options: Option<GoogleSheetsOptions>,
    /// hivePartitioningOptions property.
    pub hive_partitioning_options: Option<HivePartitioningOptions>,
    /// ignoreUnknownValues property.
    pub ignore_unknown_values: Option<bool>,
    /// jsonExtension property.
    pub json_extension: Option<String>,
    /// jsonOptions property.
    pub json_options: Option<JsonOptions>,
    /// maxBadRecords property.
    pub max_bad_records: Option<i64>,
    /// metadataCacheMode property.
    pub metadata_cache_mode: Option<String>,
    /// objectMetadata property.
    pub object_metadata: Option<String>,
    /// parquetOptions property.
    pub parquet_options: Option<ParquetOptions>,
    /// referenceFileSchemaUri property.
    pub reference_file_schema_uri: Option<String>,
    /// schema property.
    pub schema: Option<TableSchema>,
    /// sourceFormat property.
    pub source_format: Option<String>,
    /// sourceUris property.
    pub source_uris: Option<Vec<String>>,
    /// timeFormat property.
    pub time_format: Option<String>,
    /// timeZone property.
    pub time_zone: Option<String>,
    /// timestampFormat property.
    pub timestamp_format: Option<String>,
    /// timestampTargetPrecision property.
    pub timestamp_target_precision: Option<Vec<i64>>,
}

/// `MaterializedViewDefinition` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct MaterializedViewDefinition {
    /// allowNonIncrementalDefinition property.
    pub allow_non_incremental_definition: Option<bool>,
    /// enableRefresh property.
    pub enable_refresh: Option<bool>,
    /// lastRefreshTime property.
    pub last_refresh_time: Option<String>,
    /// maxStaleness property.
    pub max_staleness: Option<String>,
    /// query property.
    pub query: Option<String>,
    /// refreshIntervalMs property.
    pub refresh_interval_ms: Option<String>,
}

/// `MaterializedViewStatus` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct MaterializedViewStatus {
    /// lastRefreshStatus property.
    pub last_refresh_status: Option<ErrorProto>,
    /// refreshWatermark property.
    pub refresh_watermark: Option<String>,
}

/// `TableList` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct TableList {
    /// etag property.
    pub etag: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// tables property.
    pub tables: Option<Vec<std::collections::HashMap<String, serde_json::Value>>>,
    /// totalItems property.
    pub total_items: Option<i64>,
}

/// `PartitioningDefinition` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct PartitioningDefinition {
    /// partitionedColumn property.
    pub partitioned_column: Option<Vec<PartitionedColumn>>,
}

/// `TableSchema` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct TableSchema {
    /// fields property.
    pub fields: Option<Vec<TableFieldSchema>>,
    /// foreignTypeInfo property.
    pub foreign_type_info: Option<ForeignTypeInfo>,
}

/// `Streamingbuffer` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Streamingbuffer {
    /// estimatedBytes property.
    pub estimated_bytes: Option<String>,
    /// estimatedRows property.
    pub estimated_rows: Option<String>,
    /// oldestEntryTime property.
    pub oldest_entry_time: Option<String>,
}

/// `TableConstraints` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct TableConstraints {
    /// foreignKeys property.
    pub foreign_keys: Option<Vec<std::collections::HashMap<String, serde_json::Value>>>,
    /// primaryKey property.
    pub primary_key: Option<std::collections::HashMap<String, serde_json::Value>>,
}

/// `ForeignViewDefinition` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ForeignViewDefinition {
    /// dialect property.
    pub dialect: Option<String>,
    /// query property.
    pub query: Option<String>,
}

/// `CloneDefinition` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct CloneDefinition {
    /// baseTableReference property.
    pub base_table_reference: Option<TableReference>,
    /// cloneTime property.
    pub clone_time: Option<String>,
}

/// `UserDefinedFunctionResource` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct UserDefinedFunctionResource {
    /// inlineCode property.
    pub inline_code: Option<String>,
    /// resourceUri property.
    pub resource_uri: Option<String>,
}

/// `DataPolicyOption` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct DataPolicyOption {
    /// name property.
    pub name: Option<String>,
}

/// `SnapshotDefinition` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct SnapshotDefinition {
    /// baseTableReference property.
    pub base_table_reference: Option<TableReference>,
    /// snapshotTime property.
    pub snapshot_time: Option<String>,
}

/// `BigtableOptions` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct BigtableOptions {
    /// columnFamilies property.
    pub column_families: Option<Vec<BigtableColumnFamily>>,
    /// ignoreUnspecifiedColumnFamilies property.
    pub ignore_unspecified_column_families: Option<bool>,
    /// outputColumnFamiliesAsJson property.
    pub output_column_families_as_json: Option<bool>,
    /// readRowkeyAsString property.
    pub read_rowkey_as_string: Option<bool>,
}

/// `ModelDefinition` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ModelDefinition {
    /// modelOptions property.
    pub model_options: Option<std::collections::HashMap<String, serde_json::Value>>,
    /// trainingRuns property.
    pub training_runs: Option<Vec<BqmlTrainingRun>>,
}

/// `BqmlIterationResult` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct BqmlIterationResult {
    /// durationMs property.
    pub duration_ms: Option<String>,
    /// evalLoss property.
    pub eval_loss: Option<f64>,
    /// index property.
    pub index: Option<i64>,
    /// learnRate property.
    pub learn_rate: Option<f64>,
    /// trainingLoss property.
    pub training_loss: Option<f64>,
}

/// `CsvOptions` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct CsvOptions {
    /// allowJaggedRows property.
    pub allow_jagged_rows: Option<bool>,
    /// allowQuotedNewlines property.
    pub allow_quoted_newlines: Option<bool>,
    /// encoding property.
    pub encoding: Option<String>,
    /// fieldDelimiter property.
    pub field_delimiter: Option<String>,
    /// nullMarker property.
    pub null_marker: Option<String>,
    /// nullMarkers property.
    pub null_markers: Option<Vec<String>>,
    /// preserveAsciiControlCharacters property.
    pub preserve_ascii_control_characters: Option<bool>,
    /// quote property.
    pub quote: Option<String>,
    /// skipLeadingRows property.
    pub skip_leading_rows: Option<String>,
    /// sourceColumnMatch property.
    pub source_column_match: Option<String>,
}

/// `ParquetOptions` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ParquetOptions {
    /// enableListInference property.
    pub enable_list_inference: Option<bool>,
    /// enumAsString property.
    pub enum_as_string: Option<bool>,
    /// mapTargetType property.
    pub map_target_type: Option<String>,
}

/// `ErrorProto` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ErrorProto {
    /// debugInfo property.
    pub debug_info: Option<String>,
    /// location property.
    pub location: Option<String>,
    /// message property.
    pub message: Option<String>,
    /// reason property.
    pub reason: Option<String>,
}

/// `GoogleSheetsOptions` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct GoogleSheetsOptions {
    /// range property.
    pub range: Option<String>,
    /// skipLeadingRows property.
    pub skip_leading_rows: Option<String>,
}

/// `DifferentialPrivacyPolicy` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct DifferentialPrivacyPolicy {
    /// deltaBudget property.
    pub delta_budget: Option<f64>,
    /// deltaBudgetRemaining property.
    pub delta_budget_remaining: Option<f64>,
    /// deltaPerQuery property.
    pub delta_per_query: Option<f64>,
    /// epsilonBudget property.
    pub epsilon_budget: Option<f64>,
    /// epsilonBudgetRemaining property.
    pub epsilon_budget_remaining: Option<f64>,
    /// maxEpsilonPerQuery property.
    pub max_epsilon_per_query: Option<f64>,
    /// maxGroupsContributed property.
    pub max_groups_contributed: Option<String>,
    /// privacyUnitColumn property.
    pub privacy_unit_column: Option<String>,
}

/// `AggregationThresholdPolicy` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct AggregationThresholdPolicy {
    /// privacyUnitColumns property.
    pub privacy_unit_columns: Option<Vec<String>>,
    /// threshold property.
    pub threshold: Option<String>,
}

/// `StorageDescriptor` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct StorageDescriptor {
    /// inputFormat property.
    pub input_format: Option<String>,
    /// locationUri property.
    pub location_uri: Option<String>,
    /// outputFormat property.
    pub output_format: Option<String>,
    /// serdeInfo property.
    pub serde_info: Option<SerDeInfo>,
}

/// `JsonOptions` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct JsonOptions {
    /// encoding property.
    pub encoding: Option<String>,
}

/// `SerDeInfo` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct SerDeInfo {
    /// name property.
    pub name: Option<String>,
    /// parameters property.
    pub parameters: Option<serde_json::Value>,
    /// serializationLibrary property.
    pub serialization_library: Option<String>,
}

/// `ViewDefinition` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ViewDefinition {
    /// foreignDefinitions property.
    pub foreign_definitions: Option<Vec<ForeignViewDefinition>>,
    /// privacyPolicy property.
    pub privacy_policy: Option<PrivacyPolicy>,
    /// query property.
    pub query: Option<String>,
    /// useExplicitColumnNames property.
    pub use_explicit_column_names: Option<bool>,
    /// useLegacySql property.
    pub use_legacy_sql: Option<bool>,
    /// userDefinedFunctionResources property.
    pub user_defined_function_resources: Option<Vec<UserDefinedFunctionResource>>,
}

/// `HivePartitioningOptions` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct HivePartitioningOptions {
    /// fields property.
    pub fields: Option<Vec<String>>,
    /// mode property.
    pub mode: Option<String>,
    /// requirePartitionFilter property.
    pub require_partition_filter: Option<bool>,
    /// sourceUriPrefix property.
    pub source_uri_prefix: Option<String>,
}

/// `TimePartitioning` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct TimePartitioning {
    /// expirationMs property.
    pub expiration_ms: Option<String>,
    /// field property.
    pub field: Option<String>,
    /// requirePartitionFilter property.
    pub require_partition_filter: Option<bool>,
    /// type property.
    pub r#type: Option<String>,
}

/// `Binding` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Binding {
    /// condition property.
    pub condition: Option<Expr>,
    /// members property.
    pub members: Option<Vec<String>>,
    /// role property.
    pub role: Option<String>,
}

/// `Expr` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Expr {
    /// description property.
    pub description: Option<String>,
    /// expression property.
    pub expression: Option<String>,
    /// location property.
    pub location: Option<String>,
    /// title property.
    pub title: Option<String>,
}

/// `TableReplicationInfo` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct TableReplicationInfo {
    /// replicatedSourceLastRefreshTime property.
    pub replicated_source_last_refresh_time: Option<String>,
    /// replicationError property.
    pub replication_error: Option<ErrorProto>,
    /// replicationIntervalMs property.
    pub replication_interval_ms: Option<String>,
    /// replicationStatus property.
    pub replication_status: Option<String>,
    /// sourceTable property.
    pub source_table: Option<TableReference>,
}

/// `BigtableColumn` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct BigtableColumn {
    /// encoding property.
    pub encoding: Option<String>,
    /// fieldName property.
    pub field_name: Option<String>,
    /// onlyReadLatest property.
    pub only_read_latest: Option<bool>,
    /// protoConfig property.
    pub proto_config: Option<BigtableProtoConfig>,
    /// qualifierEncoded property.
    pub qualifier_encoded: Option<String>,
    /// qualifierString property.
    pub qualifier_string: Option<String>,
    /// type property.
    pub r#type: Option<String>,
}

/// `BigtableProtoConfig` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct BigtableProtoConfig {
    /// protoMessageName property.
    pub proto_message_name: Option<String>,
    /// schemaBundleId property.
    pub schema_bundle_id: Option<String>,
}

/// `AvroOptions` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct AvroOptions {
    /// useAvroLogicalTypes property.
    pub use_avro_logical_types: Option<bool>,
}

/// `GeneratedExpressionInfo` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct GeneratedExpressionInfo {
    /// asynchronous property.
    pub asynchronous: Option<bool>,
    /// generationExpression property.
    pub generation_expression: Option<String>,
    /// stored property.
    pub stored: Option<bool>,
}

/// `BigLakeConfiguration` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct BigLakeConfiguration {
    /// connectionId property.
    pub connection_id: Option<String>,
    /// fileFormat property.
    pub file_format: Option<String>,
    /// storageUri property.
    pub storage_uri: Option<String>,
    /// tableFormat property.
    pub table_format: Option<String>,
}

/// `AuditConfig` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct AuditConfig {
    /// auditLogConfigs property.
    pub audit_log_configs: Option<Vec<AuditLogConfig>>,
    /// service property.
    pub service: Option<String>,
}

/// `PrivacyPolicy` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct PrivacyPolicy {
    /// aggregationThresholdPolicy property.
    pub aggregation_threshold_policy: Option<AggregationThresholdPolicy>,
    /// differentialPrivacyPolicy property.
    pub differential_privacy_policy: Option<DifferentialPrivacyPolicy>,
    /// joinRestrictionPolicy property.
    pub join_restriction_policy: Option<JoinRestrictionPolicy>,
}

/// `TableFieldSchema` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct TableFieldSchema {
    /// categories property.
    pub categories: Option<std::collections::HashMap<String, serde_json::Value>>,
    /// collation property.
    pub collation: Option<String>,
    /// dataPolicies property.
    pub data_policies: Option<Vec<DataPolicyOption>>,
    /// defaultValueExpression property.
    pub default_value_expression: Option<String>,
    /// description property.
    pub description: Option<String>,
    /// fields property.
    pub fields: Option<Vec<TableFieldSchema>>,
    /// foreignTypeDefinition property.
    pub foreign_type_definition: Option<String>,
    /// generatedColumn property.
    pub generated_column: Option<GeneratedColumn>,
    /// maxLength property.
    pub max_length: Option<String>,
    /// mode property.
    pub mode: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// policyTags property.
    pub policy_tags: Option<std::collections::HashMap<String, serde_json::Value>>,
    /// precision property.
    pub precision: Option<String>,
    /// rangeElementType property.
    pub range_element_type: Option<std::collections::HashMap<String, serde_json::Value>>,
    /// roundingMode property.
    pub rounding_mode: Option<String>,
    /// scale property.
    pub scale: Option<String>,
    /// timestampPrecision property.
    pub timestamp_precision: Option<String>,
    /// type property.
    pub r#type: Option<String>,
}

/// `RestrictionConfig` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct RestrictionConfig {
    /// type property.
    pub r#type: Option<String>,
}

/// `RangePartitioning` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct RangePartitioning {
    /// field property.
    pub field: Option<String>,
    /// range property.
    pub range: Option<std::collections::HashMap<String, serde_json::Value>>,
}

/// `GeneratedColumn` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct GeneratedColumn {
    /// generatedExpressionInfo property.
    pub generated_expression_info: Option<GeneratedExpressionInfo>,
    /// generatedMode property.
    pub generated_mode: Option<String>,
}

/// `Table` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Table {
    /// biglakeConfiguration property.
    pub biglake_configuration: Option<BigLakeConfiguration>,
    /// cloneDefinition property.
    pub clone_definition: Option<CloneDefinition>,
    /// clustering property.
    pub clustering: Option<Clustering>,
    /// creationTime property.
    pub creation_time: Option<String>,
    /// defaultCollation property.
    pub default_collation: Option<String>,
    /// defaultRoundingMode property.
    pub default_rounding_mode: Option<String>,
    /// description property.
    pub description: Option<String>,
    /// encryptionConfiguration property.
    pub encryption_configuration: Option<EncryptionConfiguration>,
    /// etag property.
    pub etag: Option<String>,
    /// expirationTime property.
    pub expiration_time: Option<String>,
    /// externalCatalogTableOptions property.
    pub external_catalog_table_options: Option<ExternalCatalogTableOptions>,
    /// externalDataConfiguration property.
    pub external_data_configuration: Option<ExternalDataConfiguration>,
    /// friendlyName property.
    pub friendly_name: Option<String>,
    /// id property.
    pub id: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// labels property.
    pub labels: Option<serde_json::Value>,
    /// lastModifiedTime property.
    pub last_modified_time: Option<String>,
    /// location property.
    pub location: Option<String>,
    /// managedTableType property.
    pub managed_table_type: Option<String>,
    /// materializedView property.
    pub materialized_view: Option<MaterializedViewDefinition>,
    /// materializedViewStatus property.
    pub materialized_view_status: Option<MaterializedViewStatus>,
    /// maxStaleness property.
    pub max_staleness: Option<String>,
    /// model property.
    pub model: Option<ModelDefinition>,
    /// numActiveLogicalBytes property.
    pub num_active_logical_bytes: Option<String>,
    /// numActivePhysicalBytes property.
    pub num_active_physical_bytes: Option<String>,
    /// numBytes property.
    pub num_bytes: Option<String>,
    /// numCurrentPhysicalBytes property.
    pub num_current_physical_bytes: Option<String>,
    /// numLongTermBytes property.
    pub num_long_term_bytes: Option<String>,
    /// numLongTermLogicalBytes property.
    pub num_long_term_logical_bytes: Option<String>,
    /// numLongTermPhysicalBytes property.
    pub num_long_term_physical_bytes: Option<String>,
    /// numPartitions property.
    pub num_partitions: Option<String>,
    /// numPhysicalBytes property.
    pub num_physical_bytes: Option<String>,
    /// numRows property.
    pub num_rows: Option<String>,
    /// numTimeTravelPhysicalBytes property.
    pub num_time_travel_physical_bytes: Option<String>,
    /// numTotalLogicalBytes property.
    pub num_total_logical_bytes: Option<String>,
    /// numTotalPhysicalBytes property.
    pub num_total_physical_bytes: Option<String>,
    /// partitionDefinition property.
    pub partition_definition: Option<PartitioningDefinition>,
    /// rangePartitioning property.
    pub range_partitioning: Option<RangePartitioning>,
    /// replicas property.
    pub replicas: Option<Vec<TableReference>>,
    /// requirePartitionFilter property.
    pub require_partition_filter: Option<bool>,
    /// resourceTags property.
    pub resource_tags: Option<serde_json::Value>,
    /// restrictions property.
    pub restrictions: Option<RestrictionConfig>,
    /// schema property.
    pub schema: Option<TableSchema>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// snapshotDefinition property.
    pub snapshot_definition: Option<SnapshotDefinition>,
    /// streamingBuffer property.
    pub streaming_buffer: Option<Streamingbuffer>,
    /// tableConstraints property.
    pub table_constraints: Option<TableConstraints>,
    /// tableReference property.
    pub table_reference: Option<TableReference>,
    /// tableReplicationInfo property.
    pub table_replication_info: Option<TableReplicationInfo>,
    /// timePartitioning property.
    pub time_partitioning: Option<TimePartitioning>,
    /// type property.
    pub r#type: Option<String>,
    /// view property.
    pub view: Option<ViewDefinition>,
}

/// `BqmlTrainingRun` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct BqmlTrainingRun {
    /// iterationResults property.
    pub iteration_results: Option<Vec<BqmlIterationResult>>,
    /// startTime property.
    pub start_time: Option<String>,
    /// state property.
    pub state: Option<String>,
    /// trainingOptions property.
    pub training_options: Option<std::collections::HashMap<String, serde_json::Value>>,
}

/// `BigtableColumnFamily` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct BigtableColumnFamily {
    /// columns property.
    pub columns: Option<Vec<BigtableColumn>>,
    /// encoding property.
    pub encoding: Option<String>,
    /// familyId property.
    pub family_id: Option<String>,
    /// onlyReadLatest property.
    pub only_read_latest: Option<bool>,
    /// protoConfig property.
    pub proto_config: Option<BigtableProtoConfig>,
    /// type property.
    pub r#type: Option<String>,
}

/// `AuditLogConfig` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct AuditLogConfig {
    /// exemptedMembers property.
    pub exempted_members: Option<Vec<String>>,
    /// logType property.
    pub log_type: Option<String>,
}

// =============================================================================
// ARGS TYPES (per-endpoint)
// =============================================================================

/// Arguments for [`bigquery.tables.delete_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BigqueryTablesDeleteArgs {
    /// Path parameter: `projectId`.
    pub project_id: String,
    /// Path parameter: `datasetId`.
    pub dataset_id: String,
    /// Path parameter: `tableId`.
    pub table_id: String,
}

/// Arguments for [`bigquery.tables.get_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BigqueryTablesGetArgs {
    /// Path parameter: `projectId`.
    pub project_id: String,
    /// Path parameter: `datasetId`.
    pub dataset_id: String,
    /// Path parameter: `tableId`.
    pub table_id: String,
    /// Query parameter: `selectedFields`.
    pub selected_fields: Option<String>,
    /// Query parameter: `view`.
    pub view: Option<String>,
}

/// Arguments for [`bigquery.tables.getIamPolicy_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BigqueryTablesGetIamPolicyArgs {
    /// Path parameter: `resource`.
    pub resource: String,
}

/// Arguments for [`bigquery.tables.insert_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BigqueryTablesInsertArgs {
    /// Path parameter: `projectId`.
    pub project_id: String,
    /// Path parameter: `datasetId`.
    pub dataset_id: String,
}

/// Arguments for [`bigquery.tables.list_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BigqueryTablesListArgs {
    /// Path parameter: `projectId`.
    pub project_id: String,
    /// Path parameter: `datasetId`.
    pub dataset_id: String,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`bigquery.tables.patch_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BigqueryTablesPatchArgs {
    /// Path parameter: `projectId`.
    pub project_id: String,
    /// Path parameter: `datasetId`.
    pub dataset_id: String,
    /// Path parameter: `tableId`.
    pub table_id: String,
    /// Query parameter: `autodetect_schema`.
    pub autodetect_schema: Option<String>,
}

/// Arguments for [`bigquery.tables.setIamPolicy_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BigqueryTablesSetIamPolicyArgs {
    /// Path parameter: `resource`.
    pub resource: String,
}

/// Arguments for [`bigquery.tables.testIamPermissions_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BigqueryTablesTestIamPermissionsArgs {
    /// Path parameter: `resource`.
    pub resource: String,
}

/// Arguments for [`bigquery.tables.update_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BigqueryTablesUpdateArgs {
    /// Path parameter: `projectId`.
    pub project_id: String,
    /// Path parameter: `datasetId`.
    pub dataset_id: String,
    /// Path parameter: `tableId`.
    pub table_id: String,
    /// Query parameter: `autodetect_schema`.
    pub autodetect_schema: Option<String>,
}

// =============================================================================
// CLIENT FUNCTIONS (per-endpoint)
// =============================================================================

// -----------------------------------------------------------------------------
// DELETE projects/{projectsId}/datasets/{datasetsId}/tables/{tablesId}
// -----------------------------------------------------------------------------

/// DELETE projects/{projectsId}/datasets/{datasetsId}/tables/{tablesId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = bigquery_tables_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn bigquery_tables_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://bigquery.googleapis.com/bigquery/v2/projects/{{projectsId}}/datasets/{{datasetsId}}/tables/{{tablesId}}",
    );

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream: _,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{projectsId}/datasets/{datasetsId}/tables/{tablesId}
// -----------------------------------------------------------------------------

/// GET projects/{projectsId}/datasets/{datasetsId}/tables/{tablesId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = bigquery_tables_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn bigquery_tables_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Table>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://bigquery.googleapis.com/bigquery/v2/projects/{{projectsId}}/datasets/{{datasetsId}}/tables/{{tablesId}}",
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Table =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{projectsId}/datasets/{datasetsId}/tables/{tablesId}:getIamPolicy
// -----------------------------------------------------------------------------

/// POST projects/{projectsId}/datasets/{datasetsId}/tables/{tablesId}:getIamPolicy.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = bigquery_tables_get_iam_policy_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn bigquery_tables_get_iam_policy_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://bigquery.googleapis.com/bigquery/v2/projects/{{projectsId}}/datasets/{{datasetsId}}/tables/{{tablesId}}:getIamPolicy",
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Policy =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{projectsId}/datasets/{datasetsId}/tables
// -----------------------------------------------------------------------------

/// POST projects/{projectsId}/datasets/{datasetsId}/tables.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = bigquery_tables_insert_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn bigquery_tables_insert_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Table>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://bigquery.googleapis.com/bigquery/v2/projects/{{projectsId}}/datasets/{{datasetsId}}/tables",
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Table =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{projectsId}/datasets/{datasetsId}/tables
// -----------------------------------------------------------------------------

/// GET projects/{projectsId}/datasets/{datasetsId}/tables.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = bigquery_tables_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn bigquery_tables_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TableList>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://bigquery.googleapis.com/bigquery/v2/projects/{{projectsId}}/datasets/{{datasetsId}}/tables",
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: TableList =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH projects/{projectsId}/datasets/{datasetsId}/tables/{tablesId}
// -----------------------------------------------------------------------------

/// PATCH projects/{projectsId}/datasets/{datasetsId}/tables/{tablesId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = bigquery_tables_patch_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn bigquery_tables_patch_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Table>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://bigquery.googleapis.com/bigquery/v2/projects/{{projectsId}}/datasets/{{datasetsId}}/tables/{{tablesId}}",
    );

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Table =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{projectsId}/datasets/{datasetsId}/tables/{tablesId}:setIamPolicy
// -----------------------------------------------------------------------------

/// POST projects/{projectsId}/datasets/{datasetsId}/tables/{tablesId}:setIamPolicy.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = bigquery_tables_set_iam_policy_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn bigquery_tables_set_iam_policy_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://bigquery.googleapis.com/bigquery/v2/projects/{{projectsId}}/datasets/{{datasetsId}}/tables/{{tablesId}}:setIamPolicy",
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Policy =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{projectsId}/datasets/{datasetsId}/tables/{tablesId}:testIamPermissions
// -----------------------------------------------------------------------------

/// POST projects/{projectsId}/datasets/{datasetsId}/tables/{tablesId}:testIamPermissions.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = bigquery_tables_test_iam_permissions_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn bigquery_tables_test_iam_permissions_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TestIamPermissionsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://bigquery.googleapis.com/bigquery/v2/projects/{{projectsId}}/datasets/{{datasetsId}}/tables/{{tablesId}}:testIamPermissions",
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: TestIamPermissionsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PUT projects/{projectsId}/datasets/{datasetsId}/tables/{tablesId}
// -----------------------------------------------------------------------------

/// PUT projects/{projectsId}/datasets/{datasetsId}/tables/{tablesId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = bigquery_tables_update_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn bigquery_tables_update_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Table>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://bigquery.googleapis.com/bigquery/v2/projects/{{projectsId}}/datasets/{{datasetsId}}/tables/{{tablesId}}",
    );

    let mut builder = client
        .put(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Table =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}
