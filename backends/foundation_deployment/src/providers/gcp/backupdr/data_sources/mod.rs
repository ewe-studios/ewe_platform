//! Auto-generated API module for gcp/backupdr dataSources.
//!
//! Generated by `cargo run --bin ewe_platform gen_api`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp_backupdr_data_sources `

#![cfg(feature = "gcp_backupdr_data_sources")]
#![allow(clippy::too_many_arguments, clippy::type_complexity)]
#![allow(
    clippy::missing_errors_doc,
    clippy::doc_markdown,
    clippy::useless_format
)]
#![allow(unused_imports)]

use foundation_core::valtron::{TaskIterator, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{ClientRequestBuilder, SimpleHttpClient};
use foundation_macros::JsonHash;
use serde::{Deserialize, Serialize};

// Import shared types used by this module
use super::shared::Operation;

use super::shared::ApiResponse;

// =============================================================================
// TYPE DECLARATIONS
// =============================================================================

/// `ComputeInstanceDataSourceProperties` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ComputeInstanceDataSourceProperties {
    /// description property.
    pub description: Option<String>,
    /// machineType property.
    pub machine_type: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// totalDiskCount property.
    pub total_disk_count: Option<String>,
    /// totalDiskSizeGb property.
    pub total_disk_size_gb: Option<String>,
}

/// `DataSourceGcpResource` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct DataSourceGcpResource {
    /// alloyDbClusterDatasourceProperties property.
    pub alloy_db_cluster_datasource_properties: Option<AlloyDBClusterDataSourceProperties>,
    /// cloudSqlInstanceDatasourceProperties property.
    pub cloud_sql_instance_datasource_properties: Option<CloudSqlInstanceDataSourceProperties>,
    /// computeInstanceDatasourceProperties property.
    pub compute_instance_datasource_properties: Option<ComputeInstanceDataSourceProperties>,
    /// diskDatasourceProperties property.
    pub disk_datasource_properties: Option<DiskDataSourceProperties>,
    /// filestoreInstanceDatasourceProperties property.
    pub filestore_instance_datasource_properties: Option<FilestoreInstanceDataSourceProperties>,
    /// gcpResourcename property.
    pub gcp_resourcename: Option<String>,
    /// location property.
    pub location: Option<String>,
    /// type property.
    pub r#type: Option<String>,
}

/// `CloudSqlInstanceDataSourceProperties` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct CloudSqlInstanceDataSourceProperties {
    /// databaseInstalledVersion property.
    pub database_installed_version: Option<String>,
    /// instanceCreateTime property.
    pub instance_create_time: Option<String>,
    /// instanceTier property.
    pub instance_tier: Option<String>,
    /// name property.
    pub name: Option<String>,
}

/// `ListDataSourcesResponse` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ListDataSourcesResponse {
    /// dataSources property.
    pub data_sources: Option<Vec<DataSource>>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// unreachable property.
    pub unreachable: Option<Vec<String>>,
}

/// `DiskDataSourceProperties` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct DiskDataSourceProperties {
    /// description property.
    pub description: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// sizeGb property.
    pub size_gb: Option<String>,
    /// type property.
    pub r#type: Option<String>,
}

/// `BackupConfigInfo` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct BackupConfigInfo {
    /// backupApplianceBackupConfig property.
    pub backup_appliance_backup_config: Option<BackupApplianceBackupConfig>,
    /// gcpBackupConfig property.
    pub gcp_backup_config: Option<GcpBackupConfig>,
    /// lastBackupError property.
    pub last_backup_error: Option<Status>,
    /// lastBackupState property.
    pub last_backup_state: Option<String>,
    /// lastSuccessfulBackupConsistencyTime property.
    pub last_successful_backup_consistency_time: Option<String>,
}

/// `AlloyDBClusterDataSourceProperties` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct AlloyDBClusterDataSourceProperties {
    /// clusterUid property.
    pub cluster_uid: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// pitrWindows property.
    pub pitr_windows: Option<Vec<AlloyDbPitrWindow>>,
}

/// `FetchAccessTokenResponse` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct FetchAccessTokenResponse {
    /// expireTime property.
    pub expire_time: Option<String>,
    /// readLocation property.
    pub read_location: Option<String>,
    /// token property.
    pub token: Option<String>,
    /// writeLocation property.
    pub write_location: Option<String>,
}

/// `GcpBackupConfig` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct GcpBackupConfig {
    /// backupPlan property.
    pub backup_plan: Option<String>,
    /// backupPlanAssociation property.
    pub backup_plan_association: Option<String>,
    /// backupPlanDescription property.
    pub backup_plan_description: Option<String>,
    /// backupPlanRevisionId property.
    pub backup_plan_revision_id: Option<String>,
    /// backupPlanRevisionName property.
    pub backup_plan_revision_name: Option<String>,
    /// backupPlanRules property.
    pub backup_plan_rules: Option<Vec<String>>,
}

/// `AlloyDbPitrWindow` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct AlloyDbPitrWindow {
    /// endTime property.
    pub end_time: Option<String>,
    /// logRetentionDays property.
    pub log_retention_days: Option<String>,
    /// startTime property.
    pub start_time: Option<String>,
}

/// `DataSource` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct DataSource {
    /// backupBlockedByVaultAccessRestriction property.
    pub backup_blocked_by_vault_access_restriction: Option<bool>,
    /// backupConfigInfo property.
    pub backup_config_info: Option<BackupConfigInfo>,
    /// backupCount property.
    pub backup_count: Option<String>,
    /// configState property.
    pub config_state: Option<String>,
    /// createTime property.
    pub create_time: Option<String>,
    /// dataSourceBackupApplianceApplication property.
    pub data_source_backup_appliance_application: Option<DataSourceBackupApplianceApplication>,
    /// dataSourceGcpResource property.
    pub data_source_gcp_resource: Option<DataSourceGcpResource>,
    /// etag property.
    pub etag: Option<String>,
    /// labels property.
    pub labels: Option<serde_json::Value>,
    /// name property.
    pub name: Option<String>,
    /// state property.
    pub state: Option<String>,
    /// totalStoredBytes property.
    pub total_stored_bytes: Option<String>,
    /// updateTime property.
    pub update_time: Option<String>,
}

/// `DataSourceBackupApplianceApplication` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct DataSourceBackupApplianceApplication {
    /// applianceId property.
    pub appliance_id: Option<String>,
    /// applicationId property.
    pub application_id: Option<String>,
    /// applicationName property.
    pub application_name: Option<String>,
    /// backupAppliance property.
    pub backup_appliance: Option<String>,
    /// hostId property.
    pub host_id: Option<String>,
    /// hostname property.
    pub hostname: Option<String>,
    /// type property.
    pub r#type: Option<String>,
}

/// `Status` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Status {
    /// code property.
    pub code: Option<i64>,
    /// details property.
    pub details: Option<Vec<serde_json::Value>>,
    /// message property.
    pub message: Option<String>,
}

/// `InitiateBackupResponse` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct InitiateBackupResponse {
    /// backup property.
    pub backup: Option<String>,
    /// baseBackupGenerationId property.
    pub base_backup_generation_id: Option<i64>,
    /// newBackupGenerationId property.
    pub new_backup_generation_id: Option<i64>,
}

/// `FilestoreInstanceDataSourceProperties` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct FilestoreInstanceDataSourceProperties {
    /// instanceCreateTime property.
    pub instance_create_time: Option<String>,
    /// name property.
    pub name: Option<String>,
}

/// `BackupApplianceBackupConfig` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct BackupApplianceBackupConfig {
    /// applicationName property.
    pub application_name: Option<String>,
    /// backupApplianceId property.
    pub backup_appliance_id: Option<String>,
    /// backupApplianceName property.
    pub backup_appliance_name: Option<String>,
    /// hostName property.
    pub host_name: Option<String>,
    /// slaId property.
    pub sla_id: Option<String>,
    /// slpName property.
    pub slp_name: Option<String>,
    /// sltName property.
    pub slt_name: Option<String>,
}

// =============================================================================
// ARGS TYPES (per-endpoint)
// =============================================================================

/// Arguments for [`backupdr.projects.locations.backupVaults.dataSources.abandonBackup_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BackupdrProjectsLocationsBackupVaultsDataSourcesAbandonBackupArgs {
    /// Path parameter: `dataSource`.
    pub data_source: String,
}

/// Arguments for [`backupdr.projects.locations.backupVaults.dataSources.fetchAccessToken_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BackupdrProjectsLocationsBackupVaultsDataSourcesFetchAccessTokenArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`backupdr.projects.locations.backupVaults.dataSources.finalizeBackup_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BackupdrProjectsLocationsBackupVaultsDataSourcesFinalizeBackupArgs {
    /// Path parameter: `dataSource`.
    pub data_source: String,
}

/// Arguments for [`backupdr.projects.locations.backupVaults.dataSources.get_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BackupdrProjectsLocationsBackupVaultsDataSourcesGetArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`backupdr.projects.locations.backupVaults.dataSources.initiateBackup_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BackupdrProjectsLocationsBackupVaultsDataSourcesInitiateBackupArgs {
    /// Path parameter: `dataSource`.
    pub data_source: String,
}

/// Arguments for [`backupdr.projects.locations.backupVaults.dataSources.list_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BackupdrProjectsLocationsBackupVaultsDataSourcesListArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`backupdr.projects.locations.backupVaults.dataSources.patch_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BackupdrProjectsLocationsBackupVaultsDataSourcesPatchArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `allowMissing`.
    pub allow_missing: Option<String>,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
    /// Query parameter: `updateMask`.
    pub update_mask: Option<String>,
}

/// Arguments for [`backupdr.projects.locations.backupVaults.dataSources.remove_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BackupdrProjectsLocationsBackupVaultsDataSourcesRemoveArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`backupdr.projects.locations.backupVaults.dataSources.setInternalStatus_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BackupdrProjectsLocationsBackupVaultsDataSourcesSetInternalStatusArgs {
    /// Path parameter: `dataSource`.
    pub data_source: String,
}

// =============================================================================
// CLIENT FUNCTIONS (per-endpoint)
// =============================================================================

// -----------------------------------------------------------------------------
// POST v1/projects/{projectsId}/locations/{locationsId}/backupVaults/{backupVaultsId}/dataSources/{dataSourcesId}:abandonBackup
// -----------------------------------------------------------------------------

/// POST v1/projects/{projectsId}/locations/{locationsId}/backupVaults/{backupVaultsId}/dataSources/{dataSourcesId}:abandonBackup.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = backupdr_projects_locations_backup_vaults_data_sources_abandon_backup_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn backupdr_projects_locations_backup_vaults_data_sources_abandon_backup_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://backupdr.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/backupVaults/{{backupVaultsId}}/dataSources/{{dataSourcesId}}:abandonBackup",
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/projects/{projectsId}/locations/{locationsId}/backupVaults/{backupVaultsId}/dataSources/{dataSourcesId}:fetchAccessToken
// -----------------------------------------------------------------------------

/// POST v1/projects/{projectsId}/locations/{locationsId}/backupVaults/{backupVaultsId}/dataSources/{dataSourcesId}:fetchAccessToken.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = backupdr_projects_locations_backup_vaults_data_sources_fetch_access_token_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn backupdr_projects_locations_backup_vaults_data_sources_fetch_access_token_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<FetchAccessTokenResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://backupdr.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/backupVaults/{{backupVaultsId}}/dataSources/{{dataSourcesId}}:fetchAccessToken",
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: FetchAccessTokenResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/projects/{projectsId}/locations/{locationsId}/backupVaults/{backupVaultsId}/dataSources/{dataSourcesId}:finalizeBackup
// -----------------------------------------------------------------------------

/// POST v1/projects/{projectsId}/locations/{locationsId}/backupVaults/{backupVaultsId}/dataSources/{dataSourcesId}:finalizeBackup.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = backupdr_projects_locations_backup_vaults_data_sources_finalize_backup_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn backupdr_projects_locations_backup_vaults_data_sources_finalize_backup_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://backupdr.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/backupVaults/{{backupVaultsId}}/dataSources/{{dataSourcesId}}:finalizeBackup",
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/projects/{projectsId}/locations/{locationsId}/backupVaults/{backupVaultsId}/dataSources/{dataSourcesId}
// -----------------------------------------------------------------------------

/// GET v1/projects/{projectsId}/locations/{locationsId}/backupVaults/{backupVaultsId}/dataSources/{dataSourcesId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = backupdr_projects_locations_backup_vaults_data_sources_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn backupdr_projects_locations_backup_vaults_data_sources_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<DataSource>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://backupdr.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/backupVaults/{{backupVaultsId}}/dataSources/{{dataSourcesId}}",
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: DataSource =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/projects/{projectsId}/locations/{locationsId}/backupVaults/{backupVaultsId}/dataSources/{dataSourcesId}:initiateBackup
// -----------------------------------------------------------------------------

/// POST v1/projects/{projectsId}/locations/{locationsId}/backupVaults/{backupVaultsId}/dataSources/{dataSourcesId}:initiateBackup.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = backupdr_projects_locations_backup_vaults_data_sources_initiate_backup_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn backupdr_projects_locations_backup_vaults_data_sources_initiate_backup_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<InitiateBackupResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://backupdr.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/backupVaults/{{backupVaultsId}}/dataSources/{{dataSourcesId}}:initiateBackup",
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: InitiateBackupResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/projects/{projectsId}/locations/{locationsId}/backupVaults/{backupVaultsId}/dataSources
// -----------------------------------------------------------------------------

/// GET v1/projects/{projectsId}/locations/{locationsId}/backupVaults/{backupVaultsId}/dataSources.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = backupdr_projects_locations_backup_vaults_data_sources_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn backupdr_projects_locations_backup_vaults_data_sources_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListDataSourcesResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://backupdr.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/backupVaults/{{backupVaultsId}}/dataSources",
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListDataSourcesResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH v1/projects/{projectsId}/locations/{locationsId}/backupVaults/{backupVaultsId}/dataSources/{dataSourcesId}
// -----------------------------------------------------------------------------

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/backupVaults/{backupVaultsId}/dataSources/{dataSourcesId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = backupdr_projects_locations_backup_vaults_data_sources_patch_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn backupdr_projects_locations_backup_vaults_data_sources_patch_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://backupdr.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/backupVaults/{{backupVaultsId}}/dataSources/{{dataSourcesId}}",
    );

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/projects/{projectsId}/locations/{locationsId}/backupVaults/{backupVaultsId}/dataSources/{dataSourcesId}:remove
// -----------------------------------------------------------------------------

/// POST v1/projects/{projectsId}/locations/{locationsId}/backupVaults/{backupVaultsId}/dataSources/{dataSourcesId}:remove.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = backupdr_projects_locations_backup_vaults_data_sources_remove_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn backupdr_projects_locations_backup_vaults_data_sources_remove_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://backupdr.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/backupVaults/{{backupVaultsId}}/dataSources/{{dataSourcesId}}:remove",
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/projects/{projectsId}/locations/{locationsId}/backupVaults/{backupVaultsId}/dataSources/{dataSourcesId}:setInternalStatus
// -----------------------------------------------------------------------------

/// POST v1/projects/{projectsId}/locations/{locationsId}/backupVaults/{backupVaultsId}/dataSources/{dataSourcesId}:setInternalStatus.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = backupdr_projects_locations_backup_vaults_data_sources_set_internal_status_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn backupdr_projects_locations_backup_vaults_data_sources_set_internal_status_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://backupdr.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/backupVaults/{{backupVaultsId}}/dataSources/{{dataSourcesId}}:setInternalStatus",
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}
