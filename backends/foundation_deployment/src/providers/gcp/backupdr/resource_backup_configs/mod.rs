//! Auto-generated API module for gcp/backupdr resourceBackupConfigs.
//!
//! Generated by `cargo run --bin ewe_platform gen_api`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp_backupdr_resource_backup_configs `

#![cfg(feature = "gcp_backupdr_resource_backup_configs")]
#![allow(clippy::too_many_arguments, clippy::type_complexity)]
#![allow(
    clippy::missing_errors_doc,
    clippy::doc_markdown,
    clippy::useless_format
)]

use foundation_core::valtron::{execute, StreamIterator, TaskIterator, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{ClientRequestBuilder, SimpleHttpClient};
use foundation_macros::JsonHash;
use serde::{Deserialize, Serialize};

use super::shared::{ApiError, ApiPending, ApiResponse};

// =============================================================================
// TYPE DECLARATIONS
// =============================================================================

/// `PitrSettings` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct PitrSettings {
    /// retentionDays property.
    pub retention_days: Option<i64>,
}

/// `BackupConfigDetails` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct BackupConfigDetails {
    /// applicableResource property.
    pub applicable_resource: Option<String>,
    /// backupConfigSource property.
    pub backup_config_source: Option<String>,
    /// backupConfigSourceDisplayName property.
    pub backup_config_source_display_name: Option<String>,
    /// backupDrPlanConfig property.
    pub backup_dr_plan_config: Option<BackupDrPlanConfig>,
    /// backupDrTemplateConfig property.
    pub backup_dr_template_config: Option<BackupDrTemplateConfig>,
    /// backupLocations property.
    pub backup_locations: Option<Vec<BackupLocation>>,
    /// backupVault property.
    pub backup_vault: Option<String>,
    /// latestSuccessfulBackupTime property.
    pub latest_successful_backup_time: Option<String>,
    /// pitrSettings property.
    pub pitr_settings: Option<PitrSettings>,
    /// state property.
    pub state: Option<String>,
    /// type property.
    pub r#type: Option<String>,
}

/// `ResourceBackupConfig` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ResourceBackupConfig {
    /// backupConfigsDetails property.
    pub backup_configs_details: Option<Vec<BackupConfigDetails>>,
    /// backupConfigured property.
    pub backup_configured: Option<bool>,
    /// name property.
    pub name: Option<String>,
    /// targetResource property.
    pub target_resource: Option<String>,
    /// targetResourceDisplayName property.
    pub target_resource_display_name: Option<String>,
    /// targetResourceLabels property.
    pub target_resource_labels: Option<serde_json::Value>,
    /// targetResourceType property.
    pub target_resource_type: Option<String>,
    /// uid property.
    pub uid: Option<String>,
    /// vaulted property.
    pub vaulted: Option<bool>,
}

/// `BackupDrPlanConfig` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct BackupDrPlanConfig {
    /// backupDrPlanRules property.
    pub backup_dr_plan_rules: Option<Vec<BackupDrPlanRule>>,
}

/// `BackupLocation` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct BackupLocation {
    /// locationId property.
    pub location_id: Option<String>,
    /// type property.
    pub r#type: Option<String>,
}

/// `BackupDrPlanRule` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct BackupDrPlanRule {
    /// lastSuccessfulBackupTime property.
    pub last_successful_backup_time: Option<String>,
    /// ruleId property.
    pub rule_id: Option<String>,
}

/// `ListResourceBackupConfigsResponse` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ListResourceBackupConfigsResponse {
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// resourceBackupConfigs property.
    pub resource_backup_configs: Option<Vec<ResourceBackupConfig>>,
}

/// `BackupDrTemplateConfig` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct BackupDrTemplateConfig {
    /// firstPartyManagementUri property.
    pub first_party_management_uri: Option<String>,
    /// thirdPartyManagementUri property.
    pub third_party_management_uri: Option<String>,
}

// =============================================================================
// ARGS TYPES (per-endpoint)
// =============================================================================

/// Arguments for [`backupdr.projects.locations.resourceBackupConfigs.list_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BackupdrProjectsLocationsResourceBackupConfigsListArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

// =============================================================================
// CLIENT FUNCTIONS (per-endpoint)
// =============================================================================

// -----------------------------------------------------------------------------
// GET v1/projects/{projectsId}/locations/{locationsId}/resourceBackupConfigs
// -----------------------------------------------------------------------------

/// GET v1/projects/{projectsId}/locations/{locationsId}/resourceBackupConfigs.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = backupdr_projects_locations_resource_backup_configs_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn backupdr_projects_locations_resource_backup_configs_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListResourceBackupConfigsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://backupdr.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/resourceBackupConfigs",
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListResourceBackupConfigsResponse = serde_json::from_str(&body)
                    .map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}
