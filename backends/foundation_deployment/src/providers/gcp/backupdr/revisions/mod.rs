//! Auto-generated API module for gcp/backupdr revisions.
//!
//! Generated by `cargo run --bin ewe_platform gen_api`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp_backupdr_revisions `

#![cfg(feature = "gcp_backupdr_revisions")]
#![allow(clippy::too_many_arguments, clippy::type_complexity)]
#![allow(
    clippy::missing_errors_doc,
    clippy::doc_markdown,
    clippy::useless_format
)]
#![allow(unused_imports)]

use foundation_core::valtron::{TaskIterator, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{ClientRequestBuilder, SimpleHttpClient};
use foundation_macros::JsonHash;
use serde::{Deserialize, Serialize};

use super::shared::ApiResponse;

// =============================================================================
// TYPE DECLARATIONS
// =============================================================================

/// `BackupPlanRevision` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct BackupPlanRevision {
    /// backupPlanSnapshot property.
    pub backup_plan_snapshot: Option<BackupPlan>,
    /// createTime property.
    pub create_time: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// revisionId property.
    pub revision_id: Option<String>,
    /// state property.
    pub state: Option<String>,
}

/// `ComputeInstanceBackupPlanProperties` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ComputeInstanceBackupPlanProperties {
    /// guestFlush property.
    pub guest_flush: Option<bool>,
}

/// `StandardSchedule` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct StandardSchedule {
    /// backupWindow property.
    pub backup_window: Option<BackupWindow>,
    /// daysOfMonth property.
    pub days_of_month: Option<Vec<i64>>,
    /// daysOfWeek property.
    pub days_of_week: Option<Vec<String>>,
    /// hourlyFrequency property.
    pub hourly_frequency: Option<i64>,
    /// months property.
    pub months: Option<Vec<String>>,
    /// recurrenceType property.
    pub recurrence_type: Option<String>,
    /// timeZone property.
    pub time_zone: Option<String>,
    /// weekDayOfMonth property.
    pub week_day_of_month: Option<WeekDayOfMonth>,
}

/// `ListBackupPlanRevisionsResponse` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ListBackupPlanRevisionsResponse {
    /// backupPlanRevisions property.
    pub backup_plan_revisions: Option<Vec<BackupPlanRevision>>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// unreachable property.
    pub unreachable: Option<Vec<String>>,
}

/// `BackupWindow` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct BackupWindow {
    /// endHourOfDay property.
    pub end_hour_of_day: Option<i64>,
    /// startHourOfDay property.
    pub start_hour_of_day: Option<i64>,
}

/// `BackupRule` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct BackupRule {
    /// backupRetentionDays property.
    pub backup_retention_days: Option<i64>,
    /// ruleId property.
    pub rule_id: Option<String>,
    /// standardSchedule property.
    pub standard_schedule: Option<StandardSchedule>,
}

/// `BackupPlan` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct BackupPlan {
    /// backupRules property.
    pub backup_rules: Option<Vec<BackupRule>>,
    /// backupVault property.
    pub backup_vault: Option<String>,
    /// backupVaultServiceAccount property.
    pub backup_vault_service_account: Option<String>,
    /// computeInstanceBackupPlanProperties property.
    pub compute_instance_backup_plan_properties: Option<ComputeInstanceBackupPlanProperties>,
    /// createTime property.
    pub create_time: Option<String>,
    /// description property.
    pub description: Option<String>,
    /// diskBackupPlanProperties property.
    pub disk_backup_plan_properties: Option<DiskBackupPlanProperties>,
    /// etag property.
    pub etag: Option<String>,
    /// labels property.
    pub labels: Option<serde_json::Value>,
    /// logRetentionDays property.
    pub log_retention_days: Option<String>,
    /// maxCustomOnDemandRetentionDays property.
    pub max_custom_on_demand_retention_days: Option<i64>,
    /// name property.
    pub name: Option<String>,
    /// resourceType property.
    pub resource_type: Option<String>,
    /// revisionId property.
    pub revision_id: Option<String>,
    /// revisionName property.
    pub revision_name: Option<String>,
    /// state property.
    pub state: Option<String>,
    /// supportedResourceTypes property.
    pub supported_resource_types: Option<Vec<String>>,
    /// updateTime property.
    pub update_time: Option<String>,
}

/// `DiskBackupPlanProperties` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct DiskBackupPlanProperties {
    /// guestFlush property.
    pub guest_flush: Option<bool>,
}

/// `WeekDayOfMonth` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct WeekDayOfMonth {
    /// dayOfWeek property.
    pub day_of_week: Option<String>,
    /// weekOfMonth property.
    pub week_of_month: Option<String>,
}

// =============================================================================
// ARGS TYPES (per-endpoint)
// =============================================================================

/// Arguments for [`backupdr.projects.locations.backupPlans.revisions.get_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BackupdrProjectsLocationsBackupPlansRevisionsGetArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`backupdr.projects.locations.backupPlans.revisions.list_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct BackupdrProjectsLocationsBackupPlansRevisionsListArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

// =============================================================================
// CLIENT FUNCTIONS (per-endpoint)
// =============================================================================

// -----------------------------------------------------------------------------
// GET v1/projects/{projectsId}/locations/{locationsId}/backupPlans/{backupPlansId}/revisions/{revisionsId}
// -----------------------------------------------------------------------------

/// GET v1/projects/{projectsId}/locations/{locationsId}/backupPlans/{backupPlansId}/revisions/{revisionsId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = backupdr_projects_locations_backup_plans_revisions_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn backupdr_projects_locations_backup_plans_revisions_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<BackupPlanRevision>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://backupdr.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/backupPlans/{{backupPlansId}}/revisions/{{revisionsId}}",
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: BackupPlanRevision =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/projects/{projectsId}/locations/{locationsId}/backupPlans/{backupPlansId}/revisions
// -----------------------------------------------------------------------------

/// GET v1/projects/{projectsId}/locations/{locationsId}/backupPlans/{backupPlansId}/revisions.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = backupdr_projects_locations_backup_plans_revisions_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn backupdr_projects_locations_backup_plans_revisions_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListBackupPlanRevisionsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://backupdr.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/backupPlans/{{backupPlansId}}/revisions",
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListBackupPlanRevisionsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}
