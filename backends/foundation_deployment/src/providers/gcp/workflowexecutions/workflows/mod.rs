//! Auto-generated API module for gcp/workflowexecutions workflows.
//!
//! Generated by `cargo run --bin ewe_platform gen_api`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp_workflowexecutions_workflows `

#![cfg(feature = "gcp_workflowexecutions_workflows")]
#![allow(clippy::too_many_arguments, clippy::type_complexity)]
#![allow(
    clippy::missing_errors_doc,
    clippy::doc_markdown,
    clippy::useless_format
)]

use foundation_core::valtron::{execute, StreamIterator, TaskIterator, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{ClientRequestBuilder, SimpleHttpClient};
use foundation_macros::JsonHash;
use serde::{Deserialize, Serialize};

// Import shared types used by this module
use super::shared::Execution;

use super::shared::{ApiError, ApiPending, ApiResponse};

// =============================================================================
// TYPE DECLARATIONS
// =============================================================================

/// `Status` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Status {
    /// currentSteps property.
    pub current_steps: Option<Vec<Step>>,
}

/// `StackTraceElement` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct StackTraceElement {
    /// position property.
    pub position: Option<Position>,
    /// routine property.
    pub routine: Option<String>,
    /// step property.
    pub step: Option<String>,
}

/// `Error` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Error {
    /// context property.
    pub context: Option<String>,
    /// payload property.
    pub payload: Option<String>,
    /// stackTrace property.
    pub stack_trace: Option<StackTrace>,
}

/// `Step` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Step {
    /// routine property.
    pub routine: Option<String>,
    /// step property.
    pub step: Option<String>,
}

/// `Position` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Position {
    /// column property.
    pub column: Option<String>,
    /// length property.
    pub length: Option<String>,
    /// line property.
    pub line: Option<String>,
}

/// `StateError` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct StateError {
    /// details property.
    pub details: Option<String>,
    /// type property.
    pub r#type: Option<String>,
}

/// `StackTrace` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct StackTrace {
    /// elements property.
    pub elements: Option<Vec<StackTraceElement>>,
}

// =============================================================================
// ARGS TYPES (per-endpoint)
// =============================================================================

/// Arguments for [`workflowexecutions.projects.locations.workflows.triggerPubsubExecution_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct WorkflowexecutionsProjectsLocationsWorkflowsTriggerPubsubExecutionArgs {
    /// Path parameter: `workflow`.
    pub workflow: String,
}

// =============================================================================
// CLIENT FUNCTIONS (per-endpoint)
// =============================================================================

// -----------------------------------------------------------------------------
// POST v1/projects/{projectsId}/locations/{locationsId}/workflows/{workflowsId}:triggerPubsubExecution
// -----------------------------------------------------------------------------

/// POST v1/projects/{projectsId}/locations/{locationsId}/workflows/{workflowsId}:triggerPubsubExecution.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = workflowexecutions_projects_locations_workflows_trigger_pubsub_execution_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn workflowexecutions_projects_locations_workflows_trigger_pubsub_execution_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Execution>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://workflowexecutions.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/workflows/{{workflowsId}}:triggerPubsubExecution",
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Execution =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}
