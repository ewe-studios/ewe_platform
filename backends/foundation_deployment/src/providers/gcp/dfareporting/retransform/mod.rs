//! Auto-generated API module for gcp/dfareporting retransform.
//!
//! Generated by `cargo run --bin ewe_platform gen_api`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp_dfareporting_retransform `

#![cfg(feature = "gcp_dfareporting_retransform")]
#![allow(clippy::too_many_arguments, clippy::type_complexity)]
#![allow(
    clippy::missing_errors_doc,
    clippy::doc_markdown,
    clippy::useless_format
)]

use foundation_core::valtron::{execute, StreamIterator, TaskIterator, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{ClientRequestBuilder, SimpleHttpClient};
use foundation_macros::JsonHash;
use serde::{Deserialize, Serialize};

// Import shared types used by this module
use super::shared::DynamicFeed;

use super::shared::{ApiError, ApiPending, ApiResponse};

// =============================================================================
// TYPE DECLARATIONS
// =============================================================================

/// `IngestionErrorRecord` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct IngestionErrorRecord {
    /// errors property.
    pub errors: Option<Vec<FieldError>>,
    /// recordId property.
    pub record_id: Option<String>,
}

/// `ContentSourceMetaData` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ContentSourceMetaData {
    /// charset property.
    pub charset: Option<String>,
    /// fieldNames property.
    pub field_names: Option<Vec<String>>,
    /// rowNumber property.
    pub row_number: Option<i64>,
    /// separator property.
    pub separator: Option<String>,
}

/// `LastModifiedInfo` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct LastModifiedInfo {
    /// time property.
    pub time: Option<String>,
}

/// `FeedField` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct FeedField {
    /// defaultValue property.
    pub default_value: Option<String>,
    /// filterable property.
    pub filterable: Option<bool>,
    /// id property.
    pub id: Option<i64>,
    /// name property.
    pub name: Option<String>,
    /// renderable property.
    pub renderable: Option<bool>,
    /// required property.
    pub required: Option<bool>,
    /// type property.
    pub r#type: Option<String>,
}

/// `ContentSource` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ContentSource {
    /// contentSourceName property.
    pub content_source_name: Option<String>,
    /// createInfo property.
    pub create_info: Option<LastModifiedInfo>,
    /// lastModifiedInfo property.
    pub last_modified_info: Option<LastModifiedInfo>,
    /// metaData property.
    pub meta_data: Option<ContentSourceMetaData>,
    /// resourceLink property.
    pub resource_link: Option<String>,
    /// resourceType property.
    pub resource_type: Option<String>,
}

/// `FeedIngestionStatus` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct FeedIngestionStatus {
    /// ingestionErrorRecords property.
    pub ingestion_error_records: Option<Vec<IngestionErrorRecord>>,
    /// ingestionStatus property.
    pub ingestion_status: Option<IngestionStatus>,
    /// state property.
    pub state: Option<String>,
}

/// `FieldError` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct FieldError {
    /// fieldId property.
    pub field_id: Option<i64>,
    /// fieldName property.
    pub field_name: Option<String>,
    /// fieldValues property.
    pub field_values: Option<Vec<String>>,
    /// ingestionError property.
    pub ingestion_error: Option<String>,
    /// isError property.
    pub is_error: Option<bool>,
}

/// `Element` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Element {
    /// activeFieldId property.
    pub active_field_id: Option<i64>,
    /// createInfo property.
    pub create_info: Option<LastModifiedInfo>,
    /// defaultFieldId property.
    pub default_field_id: Option<i64>,
    /// elementName property.
    pub element_name: Option<String>,
    /// endTimestampFieldId property.
    pub end_timestamp_field_id: Option<i64>,
    /// externalIdFieldId property.
    pub external_id_field_id: Option<i64>,
    /// feedFields property.
    pub feed_fields: Option<Vec<FeedField>>,
    /// isLocalTimestamp property.
    pub is_local_timestamp: Option<bool>,
    /// lastModifiedInfo property.
    pub last_modified_info: Option<LastModifiedInfo>,
    /// proximityTargetingFieldId property.
    pub proximity_targeting_field_id: Option<i64>,
    /// reportingLabelFieldId property.
    pub reporting_label_field_id: Option<i64>,
    /// startTimestampFieldId property.
    pub start_timestamp_field_id: Option<i64>,
}

/// `FeedSchedule` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct FeedSchedule {
    /// repeatValue property.
    pub repeat_value: Option<String>,
    /// scheduleEnabled property.
    pub schedule_enabled: Option<bool>,
    /// startHour property.
    pub start_hour: Option<String>,
    /// startMinute property.
    pub start_minute: Option<String>,
    /// timeZone property.
    pub time_zone: Option<String>,
}

/// `IngestionStatus` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct IngestionStatus {
    /// numActiveRows property.
    pub num_active_rows: Option<String>,
    /// numRowsProcessed property.
    pub num_rows_processed: Option<String>,
    /// numRowsTotal property.
    pub num_rows_total: Option<String>,
    /// numRowsWithErrors property.
    pub num_rows_with_errors: Option<String>,
    /// numWarningsTotal property.
    pub num_warnings_total: Option<String>,
}

// =============================================================================
// ARGS TYPES (per-endpoint)
// =============================================================================

/// Arguments for [`dfareporting.dynamicFeeds.retransform_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingDynamicFeedsRetransformArgs {
    /// Path parameter: `dynamicFeedId`.
    pub dynamic_feed_id: String,
}

// =============================================================================
// CLIENT FUNCTIONS (per-endpoint)
// =============================================================================

// -----------------------------------------------------------------------------
// POST studio/dynamicFeeds/{dynamicFeedsId}/retransform
// -----------------------------------------------------------------------------

/// POST studio/dynamicFeeds/{dynamicFeedsId}/retransform.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dfareporting_dynamic_feeds_retransform_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dfareporting_dynamic_feeds_retransform_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<DynamicFeed>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/studio/dynamicFeeds/{{dynamicFeedsId}}/retransform",
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: DynamicFeed =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}
