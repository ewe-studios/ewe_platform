//! Auto-generated API module for gcp/dfareporting generatetags.
//!
//! Generated by `cargo run --bin ewe_platform gen_api`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp_dfareporting_generatetags `

#![cfg(feature = "gcp_dfareporting_generatetags")]
#![allow(clippy::too_many_arguments, clippy::type_complexity)]
#![allow(
    clippy::missing_errors_doc,
    clippy::doc_markdown,
    clippy::useless_format
)]

use foundation_core::valtron::{execute, StreamIterator, TaskIterator, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{ClientRequestBuilder, SimpleHttpClient};
use foundation_macros::JsonHash;
use serde::{Deserialize, Serialize};

use super::shared::{ApiError, ApiPending, ApiResponse};

// =============================================================================
// TYPE DECLARATIONS
// =============================================================================

/// `TagData` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct TagData {
    /// adId property.
    pub ad_id: Option<String>,
    /// clickTag property.
    pub click_tag: Option<String>,
    /// creativeId property.
    pub creative_id: Option<String>,
    /// format property.
    pub format: Option<String>,
    /// impressionTag property.
    pub impression_tag: Option<String>,
}

/// `PlacementTag` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct PlacementTag {
    /// placementId property.
    pub placement_id: Option<String>,
    /// tagDatas property.
    pub tag_datas: Option<Vec<TagData>>,
}

/// `PlacementsGenerateTagsResponse` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct PlacementsGenerateTagsResponse {
    /// kind property.
    pub kind: Option<String>,
    /// placementTags property.
    pub placement_tags: Option<Vec<PlacementTag>>,
}

// =============================================================================
// ARGS TYPES (per-endpoint)
// =============================================================================

/// Arguments for [`dfareporting.placements.generatetags_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingPlacementsGeneratetagsArgs {
    /// Path parameter: `profileId`.
    pub profile_id: String,
    /// Query parameter: `campaignId`.
    pub campaign_id: Option<String>,
    /// Query parameter: `placementIds`.
    pub placement_ids: Option<String>,
    /// Query parameter: `tagFormats`.
    pub tag_formats: Option<String>,
    /// Query parameter: `tagProperties.dcDbmMacroIncluded`.
    pub tag_properties_dc_dbm_macro_included: Option<String>,
    /// Query parameter: `tagProperties.gppMacrosIncluded`.
    pub tag_properties_gpp_macros_included: Option<String>,
    /// Query parameter: `tagProperties.tcfGdprMacrosIncluded`.
    pub tag_properties_tcf_gdpr_macros_included: Option<String>,
}

// =============================================================================
// CLIENT FUNCTIONS (per-endpoint)
// =============================================================================

// -----------------------------------------------------------------------------
// POST userprofiles/{userprofilesId}/placements/generatetags
// -----------------------------------------------------------------------------

/// POST userprofiles/{userprofilesId}/placements/generatetags.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dfareporting_placements_generatetags_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dfareporting_placements_generatetags_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<PlacementsGenerateTagsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/userprofiles/{{userprofilesId}}/placements/generatetags",
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: PlacementsGenerateTagsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}
