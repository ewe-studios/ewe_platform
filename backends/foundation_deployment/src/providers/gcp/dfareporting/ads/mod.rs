//! Auto-generated API module for gcp/dfareporting ads.
//!
//! Generated by `cargo run --bin ewe_platform gen_api`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp_dfareporting_ads `

#![cfg(feature = "gcp_dfareporting_ads")]
#![allow(clippy::too_many_arguments, clippy::type_complexity)]
#![allow(
    clippy::missing_errors_doc,
    clippy::doc_markdown,
    clippy::useless_format
)]
#![allow(unused_imports)]

use foundation_core::valtron::{TaskIterator, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{ClientRequestBuilder, SimpleHttpClient};
use foundation_macros::JsonHash;
use serde::{Deserialize, Serialize};

use super::shared::ApiResponse;

// =============================================================================
// TYPE DECLARATIONS
// =============================================================================

/// `ConnectionType` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ConnectionType {
    /// id property.
    pub id: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// name property.
    pub name: Option<String>,
}

/// `Language` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Language {
    /// id property.
    pub id: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// languageCode property.
    pub language_code: Option<String>,
    /// name property.
    pub name: Option<String>,
}

/// `GeoTargeting` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct GeoTargeting {
    /// cities property.
    pub cities: Option<Vec<City>>,
    /// countries property.
    pub countries: Option<Vec<Country>>,
    /// excludeCountries property.
    pub exclude_countries: Option<bool>,
    /// metros property.
    pub metros: Option<Vec<Metro>>,
    /// postalCodes property.
    pub postal_codes: Option<Vec<PostalCode>>,
    /// regions property.
    pub regions: Option<Vec<Region>>,
}

/// `Browser` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Browser {
    /// browserVersionId property.
    pub browser_version_id: Option<String>,
    /// dartId property.
    pub dart_id: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// majorVersion property.
    pub major_version: Option<String>,
    /// minorVersion property.
    pub minor_version: Option<String>,
    /// name property.
    pub name: Option<String>,
}

/// `Region` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Region {
    /// countryCode property.
    pub country_code: Option<String>,
    /// countryDartId property.
    pub country_dart_id: Option<String>,
    /// dartId property.
    pub dart_id: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// regionCode property.
    pub region_code: Option<String>,
}

/// `PlatformType` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct PlatformType {
    /// id property.
    pub id: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// name property.
    pub name: Option<String>,
}

/// `Metro` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Metro {
    /// countryCode property.
    pub country_code: Option<String>,
    /// countryDartId property.
    pub country_dart_id: Option<String>,
    /// dartId property.
    pub dart_id: Option<String>,
    /// dmaId property.
    pub dma_id: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// metroCode property.
    pub metro_code: Option<String>,
    /// name property.
    pub name: Option<String>,
}

/// `ListTargetingExpression` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ListTargetingExpression {
    /// expression property.
    pub expression: Option<String>,
}

/// `LastModifiedInfo` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct LastModifiedInfo {
    /// time property.
    pub time: Option<String>,
}

/// `CompanionClickThroughOverride` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct CompanionClickThroughOverride {
    /// clickThroughUrl property.
    pub click_through_url: Option<ClickThroughUrl>,
    /// creativeId property.
    pub creative_id: Option<String>,
}

/// `CreativeGroupAssignment` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct CreativeGroupAssignment {
    /// creativeGroupId property.
    pub creative_group_id: Option<String>,
    /// creativeGroupNumber property.
    pub creative_group_number: Option<String>,
}

/// `City` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct City {
    /// countryCode property.
    pub country_code: Option<String>,
    /// countryDartId property.
    pub country_dart_id: Option<String>,
    /// dartId property.
    pub dart_id: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// metroCode property.
    pub metro_code: Option<String>,
    /// metroDmaId property.
    pub metro_dma_id: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// regionCode property.
    pub region_code: Option<String>,
    /// regionDartId property.
    pub region_dart_id: Option<String>,
}

/// `DayPartTargeting` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct DayPartTargeting {
    /// daysOfWeek property.
    pub days_of_week: Option<Vec<String>>,
    /// hoursOfDay property.
    pub hours_of_day: Option<Vec<i64>>,
    /// userLocalTime property.
    pub user_local_time: Option<bool>,
}

/// `PostalCode` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct PostalCode {
    /// code property.
    pub code: Option<String>,
    /// countryCode property.
    pub country_code: Option<String>,
    /// countryDartId property.
    pub country_dart_id: Option<String>,
    /// id property.
    pub id: Option<String>,
    /// kind property.
    pub kind: Option<String>,
}

/// `DimensionValue` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct DimensionValue {
    /// dimensionName property.
    pub dimension_name: Option<String>,
    /// etag property.
    pub etag: Option<String>,
    /// id property.
    pub id: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// matchType property.
    pub match_type: Option<String>,
    /// value property.
    pub value: Option<String>,
}

/// `TechnologyTargeting` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct TechnologyTargeting {
    /// browsers property.
    pub browsers: Option<Vec<Browser>>,
    /// connectionTypes property.
    pub connection_types: Option<Vec<ConnectionType>>,
    /// mobileCarriers property.
    pub mobile_carriers: Option<Vec<MobileCarrier>>,
    /// operatingSystemVersions property.
    pub operating_system_versions: Option<Vec<OperatingSystemVersion>>,
    /// operatingSystems property.
    pub operating_systems: Option<Vec<OperatingSystem>>,
    /// platformTypes property.
    pub platform_types: Option<Vec<PlatformType>>,
}

/// `Size` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Size {
    /// height property.
    pub height: Option<i64>,
    /// iab property.
    pub iab: Option<bool>,
    /// id property.
    pub id: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// width property.
    pub width: Option<i64>,
}

/// `MobileCarrier` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct MobileCarrier {
    /// countryCode property.
    pub country_code: Option<String>,
    /// countryDartId property.
    pub country_dart_id: Option<String>,
    /// id property.
    pub id: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// name property.
    pub name: Option<String>,
}

/// `CreativeRotation` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct CreativeRotation {
    /// creativeAssignments property.
    pub creative_assignments: Option<Vec<CreativeAssignment>>,
    /// creativeOptimizationConfigurationId property.
    pub creative_optimization_configuration_id: Option<String>,
    /// type property.
    pub r#type: Option<String>,
    /// weightCalculationStrategy property.
    pub weight_calculation_strategy: Option<String>,
}

/// `Ad` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Ad {
    /// accountId property.
    pub account_id: Option<String>,
    /// active property.
    pub active: Option<bool>,
    /// advertiserId property.
    pub advertiser_id: Option<String>,
    /// advertiserIdDimensionValue property.
    pub advertiser_id_dimension_value: Option<DimensionValue>,
    /// archived property.
    pub archived: Option<bool>,
    /// audienceSegmentId property.
    pub audience_segment_id: Option<String>,
    /// campaignId property.
    pub campaign_id: Option<String>,
    /// campaignIdDimensionValue property.
    pub campaign_id_dimension_value: Option<DimensionValue>,
    /// clickThroughUrl property.
    pub click_through_url: Option<ClickThroughUrl>,
    /// clickThroughUrlSuffixProperties property.
    pub click_through_url_suffix_properties: Option<ClickThroughUrlSuffixProperties>,
    /// comments property.
    pub comments: Option<String>,
    /// compatibility property.
    pub compatibility: Option<String>,
    /// contextualKeywordTargeting property.
    pub contextual_keyword_targeting: Option<ContextualKeywordTargeting>,
    /// createInfo property.
    pub create_info: Option<LastModifiedInfo>,
    /// creativeGroupAssignments property.
    pub creative_group_assignments: Option<Vec<CreativeGroupAssignment>>,
    /// creativeRotation property.
    pub creative_rotation: Option<CreativeRotation>,
    /// dayPartTargeting property.
    pub day_part_targeting: Option<DayPartTargeting>,
    /// defaultClickThroughEventTagProperties property.
    pub default_click_through_event_tag_properties: Option<DefaultClickThroughEventTagProperties>,
    /// deliverySchedule property.
    pub delivery_schedule: Option<DeliverySchedule>,
    /// dynamicClickTracker property.
    pub dynamic_click_tracker: Option<bool>,
    /// endTime property.
    pub end_time: Option<String>,
    /// eventTagOverrides property.
    pub event_tag_overrides: Option<Vec<EventTagOverride>>,
    /// geoTargeting property.
    pub geo_targeting: Option<GeoTargeting>,
    /// id property.
    pub id: Option<String>,
    /// idDimensionValue property.
    pub id_dimension_value: Option<DimensionValue>,
    /// keyValueTargetingExpression property.
    pub key_value_targeting_expression: Option<KeyValueTargetingExpression>,
    /// kind property.
    pub kind: Option<String>,
    /// languageTargeting property.
    pub language_targeting: Option<LanguageTargeting>,
    /// lastModifiedInfo property.
    pub last_modified_info: Option<LastModifiedInfo>,
    /// name property.
    pub name: Option<String>,
    /// placementAssignments property.
    pub placement_assignments: Option<Vec<PlacementAssignment>>,
    /// remarketingListExpression property.
    pub remarketing_list_expression: Option<ListTargetingExpression>,
    /// size property.
    pub size: Option<Size>,
    /// sslCompliant property.
    pub ssl_compliant: Option<bool>,
    /// sslRequired property.
    pub ssl_required: Option<bool>,
    /// startTime property.
    pub start_time: Option<String>,
    /// subaccountId property.
    pub subaccount_id: Option<String>,
    /// targetingTemplateId property.
    pub targeting_template_id: Option<String>,
    /// technologyTargeting property.
    pub technology_targeting: Option<TechnologyTargeting>,
    /// type property.
    pub r#type: Option<String>,
}

/// `LanguageTargeting` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct LanguageTargeting {
    /// languages property.
    pub languages: Option<Vec<Language>>,
}

/// `ClickThroughUrlSuffixProperties` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ClickThroughUrlSuffixProperties {
    /// clickThroughUrlSuffix property.
    pub click_through_url_suffix: Option<String>,
    /// overrideInheritedSuffix property.
    pub override_inherited_suffix: Option<bool>,
}

/// `Country` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Country {
    /// countryCode property.
    pub country_code: Option<String>,
    /// dartId property.
    pub dart_id: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// sslEnabled property.
    pub ssl_enabled: Option<bool>,
    /// tvDataProviders property.
    pub tv_data_providers: Option<Vec<String>>,
}

/// `AdsListResponse` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct AdsListResponse {
    /// ads property.
    pub ads: Option<Vec<Ad>>,
    /// kind property.
    pub kind: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
}

/// `DeliverySchedule` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct DeliverySchedule {
    /// frequencyCap property.
    pub frequency_cap: Option<FrequencyCap>,
    /// hardCutoff property.
    pub hard_cutoff: Option<bool>,
    /// impressionRatio property.
    pub impression_ratio: Option<String>,
    /// priority property.
    pub priority: Option<String>,
}

/// `ContextualKeyword` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ContextualKeyword {
    /// keyword property.
    pub keyword: Option<String>,
}

/// `CreativeAssignment` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct CreativeAssignment {
    /// active property.
    pub active: Option<bool>,
    /// applyEventTags property.
    pub apply_event_tags: Option<bool>,
    /// clickThroughUrl property.
    pub click_through_url: Option<ClickThroughUrl>,
    /// companionCreativeOverrides property.
    pub companion_creative_overrides: Option<Vec<CompanionClickThroughOverride>>,
    /// creativeGroupAssignments property.
    pub creative_group_assignments: Option<Vec<CreativeGroupAssignment>>,
    /// creativeId property.
    pub creative_id: Option<String>,
    /// creativeIdDimensionValue property.
    pub creative_id_dimension_value: Option<DimensionValue>,
    /// endTime property.
    pub end_time: Option<String>,
    /// richMediaExitOverrides property.
    pub rich_media_exit_overrides: Option<Vec<RichMediaExitOverride>>,
    /// sequence property.
    pub sequence: Option<i64>,
    /// sslCompliant property.
    pub ssl_compliant: Option<bool>,
    /// startTime property.
    pub start_time: Option<String>,
    /// weight property.
    pub weight: Option<i64>,
}

/// `ContextualKeywordTargeting` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ContextualKeywordTargeting {
    /// keywords property.
    pub keywords: Option<Vec<ContextualKeyword>>,
}

/// `EventTagOverride` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct EventTagOverride {
    /// enabled property.
    pub enabled: Option<bool>,
    /// id property.
    pub id: Option<String>,
}

/// `KeyValueTargetingExpression` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct KeyValueTargetingExpression {
    /// expression property.
    pub expression: Option<String>,
}

/// `PlacementAssignment` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct PlacementAssignment {
    /// active property.
    pub active: Option<bool>,
    /// placementId property.
    pub placement_id: Option<String>,
    /// placementIdDimensionValue property.
    pub placement_id_dimension_value: Option<DimensionValue>,
    /// sslRequired property.
    pub ssl_required: Option<bool>,
}

/// `ClickThroughUrl` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ClickThroughUrl {
    /// computedClickThroughUrl property.
    pub computed_click_through_url: Option<String>,
    /// customClickThroughUrl property.
    pub custom_click_through_url: Option<String>,
    /// defaultLandingPage property.
    pub default_landing_page: Option<bool>,
    /// landingPageId property.
    pub landing_page_id: Option<String>,
}

/// `DefaultClickThroughEventTagProperties` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct DefaultClickThroughEventTagProperties {
    /// defaultClickThroughEventTagId property.
    pub default_click_through_event_tag_id: Option<String>,
    /// overrideInheritedEventTag property.
    pub override_inherited_event_tag: Option<bool>,
}

/// `OperatingSystem` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct OperatingSystem {
    /// dartId property.
    pub dart_id: Option<String>,
    /// desktop property.
    pub desktop: Option<bool>,
    /// kind property.
    pub kind: Option<String>,
    /// mobile property.
    pub mobile: Option<bool>,
    /// name property.
    pub name: Option<String>,
}

/// `FrequencyCap` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct FrequencyCap {
    /// duration property.
    pub duration: Option<String>,
    /// impressions property.
    pub impressions: Option<String>,
}

/// `OperatingSystemVersion` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct OperatingSystemVersion {
    /// id property.
    pub id: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// majorVersion property.
    pub major_version: Option<String>,
    /// minorVersion property.
    pub minor_version: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// operatingSystem property.
    pub operating_system: Option<OperatingSystem>,
}

/// `RichMediaExitOverride` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct RichMediaExitOverride {
    /// clickThroughUrl property.
    pub click_through_url: Option<ClickThroughUrl>,
    /// enabled property.
    pub enabled: Option<bool>,
    /// exitId property.
    pub exit_id: Option<String>,
}

// =============================================================================
// ARGS TYPES (per-endpoint)
// =============================================================================

/// Arguments for [`dfareporting.ads.get_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingAdsGetArgs {
    /// Path parameter: `profileId`.
    pub profile_id: String,
    /// Path parameter: `id`.
    pub id: String,
}

/// Arguments for [`dfareporting.ads.insert_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingAdsInsertArgs {
    /// Path parameter: `profileId`.
    pub profile_id: String,
}

/// Arguments for [`dfareporting.ads.list_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingAdsListArgs {
    /// Path parameter: `profileId`.
    pub profile_id: String,
    /// Query parameter: `active`.
    pub active: Option<String>,
    /// Query parameter: `advertiserId`.
    pub advertiser_id: Option<String>,
    /// Query parameter: `archived`.
    pub archived: Option<String>,
    /// Query parameter: `audienceSegmentIds`.
    pub audience_segment_ids: Option<String>,
    /// Query parameter: `campaignIds`.
    pub campaign_ids: Option<String>,
    /// Query parameter: `compatibility`.
    pub compatibility: Option<String>,
    /// Query parameter: `creativeIds`.
    pub creative_ids: Option<String>,
    /// Query parameter: `creativeOptimizationConfigurationIds`.
    pub creative_optimization_configuration_ids: Option<String>,
    /// Query parameter: `dynamicClickTracker`.
    pub dynamic_click_tracker: Option<String>,
    /// Query parameter: `ids`.
    pub ids: Option<String>,
    /// Query parameter: `landingPageIds`.
    pub landing_page_ids: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `overriddenEventTagId`.
    pub overridden_event_tag_id: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `placementIds`.
    pub placement_ids: Option<String>,
    /// Query parameter: `remarketingListIds`.
    pub remarketing_list_ids: Option<String>,
    /// Query parameter: `searchString`.
    pub search_string: Option<String>,
    /// Query parameter: `sizeIds`.
    pub size_ids: Option<String>,
    /// Query parameter: `sortField`.
    pub sort_field: Option<String>,
    /// Query parameter: `sortOrder`.
    pub sort_order: Option<String>,
    /// Query parameter: `sslCompliant`.
    pub ssl_compliant: Option<String>,
    /// Query parameter: `sslRequired`.
    pub ssl_required: Option<String>,
    /// Query parameter: `type`.
    pub r#type: Option<String>,
}

/// Arguments for [`dfareporting.ads.patch_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingAdsPatchArgs {
    /// Path parameter: `profileId`.
    pub profile_id: String,
    /// Query parameter: `id`.
    pub id: Option<String>,
}

/// Arguments for [`dfareporting.ads.update_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingAdsUpdateArgs {
    /// Path parameter: `profileId`.
    pub profile_id: String,
}

// =============================================================================
// CLIENT FUNCTIONS (per-endpoint)
// =============================================================================

// -----------------------------------------------------------------------------
// GET userprofiles/{userprofilesId}/ads/{adsId}
// -----------------------------------------------------------------------------

/// GET userprofiles/{userprofilesId}/ads/{adsId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dfareporting_ads_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dfareporting_ads_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Ad>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/userprofiles/{{userprofilesId}}/ads/{{adsId}}",
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Ad = serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                    super::shared::ApiError::ParseFailed(e.to_string())
                })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST userprofiles/{userprofilesId}/ads
// -----------------------------------------------------------------------------

/// POST userprofiles/{userprofilesId}/ads.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dfareporting_ads_insert_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dfareporting_ads_insert_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Ad>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/userprofiles/{{userprofilesId}}/ads",
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Ad = serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                    super::shared::ApiError::ParseFailed(e.to_string())
                })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET userprofiles/{userprofilesId}/ads
// -----------------------------------------------------------------------------

/// GET userprofiles/{userprofilesId}/ads.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dfareporting_ads_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dfareporting_ads_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<AdsListResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/userprofiles/{{userprofilesId}}/ads",
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: AdsListResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH userprofiles/{userprofilesId}/ads
// -----------------------------------------------------------------------------

/// PATCH userprofiles/{userprofilesId}/ads.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dfareporting_ads_patch_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dfareporting_ads_patch_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Ad>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/userprofiles/{{userprofilesId}}/ads",
    );

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Ad = serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                    super::shared::ApiError::ParseFailed(e.to_string())
                })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PUT userprofiles/{userprofilesId}/ads
// -----------------------------------------------------------------------------

/// PUT userprofiles/{userprofilesId}/ads.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dfareporting_ads_update_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dfareporting_ads_update_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Ad>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/userprofiles/{{userprofilesId}}/ads",
    );

    let mut builder = client
        .put(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Ad = serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                    super::shared::ApiError::ParseFailed(e.to_string())
                })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}
