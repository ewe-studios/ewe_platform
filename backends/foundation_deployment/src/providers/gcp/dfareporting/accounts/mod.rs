//! Auto-generated API module for gcp/dfareporting accounts.
//!
//! Generated by `cargo run --bin ewe_platform gen_api`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp_dfareporting_accounts `

#![cfg(feature = "gcp_dfareporting_accounts")]
#![allow(clippy::too_many_arguments, clippy::type_complexity)]
#![allow(
    clippy::missing_errors_doc,
    clippy::doc_markdown,
    clippy::useless_format
)]

use foundation_core::valtron::{execute, StreamIterator, TaskIterator, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{ClientRequestBuilder, SimpleHttpClient};
use foundation_macros::JsonHash;
use serde::{Deserialize, Serialize};

use super::shared::{ApiError, ApiPending, ApiResponse};

// =============================================================================
// TYPE DECLARATIONS
// =============================================================================

/// `Account` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Account {
    /// accountPermissionIds property.
    pub account_permission_ids: Option<Vec<String>>,
    /// accountProfile property.
    pub account_profile: Option<String>,
    /// active property.
    pub active: Option<bool>,
    /// activeAdsLimitTier property.
    pub active_ads_limit_tier: Option<String>,
    /// activeViewOptOut property.
    pub active_view_opt_out: Option<bool>,
    /// availablePermissionIds property.
    pub available_permission_ids: Option<Vec<String>>,
    /// countryId property.
    pub country_id: Option<String>,
    /// currencyId property.
    pub currency_id: Option<String>,
    /// defaultCreativeSizeId property.
    pub default_creative_size_id: Option<String>,
    /// description property.
    pub description: Option<String>,
    /// id property.
    pub id: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// locale property.
    pub locale: Option<String>,
    /// maximumImageSize property.
    pub maximum_image_size: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// nielsenOcrEnabled property.
    pub nielsen_ocr_enabled: Option<bool>,
    /// reportsConfiguration property.
    pub reports_configuration: Option<ReportsConfiguration>,
    /// shareReportsWithTwitter property.
    pub share_reports_with_twitter: Option<bool>,
    /// teaserSizeLimit property.
    pub teaser_size_limit: Option<String>,
}

/// `LookbackConfiguration` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct LookbackConfiguration {
    /// clickDuration property.
    pub click_duration: Option<i64>,
    /// postImpressionActivitiesDuration property.
    pub post_impression_activities_duration: Option<i64>,
}

/// `AccountsListResponse` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct AccountsListResponse {
    /// accounts property.
    pub accounts: Option<Vec<Account>>,
    /// kind property.
    pub kind: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
}

/// `ReportsConfiguration` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ReportsConfiguration {
    /// exposureToConversionEnabled property.
    pub exposure_to_conversion_enabled: Option<bool>,
    /// lookbackConfiguration property.
    pub lookback_configuration: Option<LookbackConfiguration>,
    /// reportGenerationTimeZoneId property.
    pub report_generation_time_zone_id: Option<String>,
}

// =============================================================================
// ARGS TYPES (per-endpoint)
// =============================================================================

/// Arguments for [`dfareporting.accounts.get_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingAccountsGetArgs {
    /// Path parameter: `profileId`.
    pub profile_id: String,
    /// Path parameter: `id`.
    pub id: String,
}

/// Arguments for [`dfareporting.accounts.list_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingAccountsListArgs {
    /// Path parameter: `profileId`.
    pub profile_id: String,
    /// Query parameter: `active`.
    pub active: Option<String>,
    /// Query parameter: `ids`.
    pub ids: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `searchString`.
    pub search_string: Option<String>,
    /// Query parameter: `sortField`.
    pub sort_field: Option<String>,
    /// Query parameter: `sortOrder`.
    pub sort_order: Option<String>,
}

/// Arguments for [`dfareporting.accounts.patch_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingAccountsPatchArgs {
    /// Path parameter: `profileId`.
    pub profile_id: String,
    /// Query parameter: `id`.
    pub id: Option<String>,
}

/// Arguments for [`dfareporting.accounts.update_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DfareportingAccountsUpdateArgs {
    /// Path parameter: `profileId`.
    pub profile_id: String,
}

// =============================================================================
// CLIENT FUNCTIONS (per-endpoint)
// =============================================================================

// -----------------------------------------------------------------------------
// GET userprofiles/{userprofilesId}/accounts/{accountsId}
// -----------------------------------------------------------------------------

/// GET userprofiles/{userprofilesId}/accounts/{accountsId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dfareporting_accounts_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dfareporting_accounts_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Account>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/userprofiles/{{userprofilesId}}/accounts/{{accountsId}}",
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Account =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET userprofiles/{userprofilesId}/accounts
// -----------------------------------------------------------------------------

/// GET userprofiles/{userprofilesId}/accounts.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dfareporting_accounts_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dfareporting_accounts_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<AccountsListResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/userprofiles/{{userprofilesId}}/accounts",
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: AccountsListResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH userprofiles/{userprofilesId}/accounts
// -----------------------------------------------------------------------------

/// PATCH userprofiles/{userprofilesId}/accounts.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dfareporting_accounts_patch_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dfareporting_accounts_patch_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Account>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/userprofiles/{{userprofilesId}}/accounts",
    );

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Account =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PUT userprofiles/{userprofilesId}/accounts
// -----------------------------------------------------------------------------

/// PUT userprofiles/{userprofilesId}/accounts.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = dfareporting_accounts_update_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn dfareporting_accounts_update_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Account>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://dfareporting.googleapis.com/dfareporting/v5/userprofiles/{{userprofilesId}}/accounts",
    );

    let mut builder = client
        .put(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Account =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}
