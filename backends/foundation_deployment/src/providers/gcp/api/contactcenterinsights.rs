//! ContactcenterinsightsProvider - State-aware contactcenterinsights API client.
//!
//! WHY: Users need state-aware API clients that automatically track
//!      resource changes in the state store.
//!
//! WHAT: Provider wrapping ProviderClient<S> with methods for
//!       contactcenterinsights API endpoints that auto-store results.
//!
//! HOW: Each method wraps the task with StoreStateIdentifierTask
//!      for automatic state persistence on success.

#![cfg(feature = "gcp")]

use crate::providers::gcp::clients::contactcenterinsights::{
    contactcenterinsights_projects_locations_bulk_delete_feedback_labels_builder, contactcenterinsights_projects_locations_bulk_delete_feedback_labels_task,
    contactcenterinsights_projects_locations_bulk_download_feedback_labels_builder, contactcenterinsights_projects_locations_bulk_download_feedback_labels_task,
    contactcenterinsights_projects_locations_bulk_upload_feedback_labels_builder, contactcenterinsights_projects_locations_bulk_upload_feedback_labels_task,
    contactcenterinsights_projects_locations_generative_insights_builder, contactcenterinsights_projects_locations_generative_insights_task,
    contactcenterinsights_projects_locations_query_metrics_builder, contactcenterinsights_projects_locations_query_metrics_task,
    contactcenterinsights_projects_locations_query_performance_overview_builder, contactcenterinsights_projects_locations_query_performance_overview_task,
    contactcenterinsights_projects_locations_test_correlation_config_builder, contactcenterinsights_projects_locations_test_correlation_config_task,
    contactcenterinsights_projects_locations_update_correlation_config_builder, contactcenterinsights_projects_locations_update_correlation_config_task,
    contactcenterinsights_projects_locations_update_settings_builder, contactcenterinsights_projects_locations_update_settings_task,
    contactcenterinsights_projects_locations_analysis_rules_create_builder, contactcenterinsights_projects_locations_analysis_rules_create_task,
    contactcenterinsights_projects_locations_analysis_rules_delete_builder, contactcenterinsights_projects_locations_analysis_rules_delete_task,
    contactcenterinsights_projects_locations_analysis_rules_patch_builder, contactcenterinsights_projects_locations_analysis_rules_patch_task,
    contactcenterinsights_projects_locations_assessment_rules_create_builder, contactcenterinsights_projects_locations_assessment_rules_create_task,
    contactcenterinsights_projects_locations_assessment_rules_delete_builder, contactcenterinsights_projects_locations_assessment_rules_delete_task,
    contactcenterinsights_projects_locations_assessment_rules_patch_builder, contactcenterinsights_projects_locations_assessment_rules_patch_task,
    contactcenterinsights_projects_locations_authorized_view_sets_create_builder, contactcenterinsights_projects_locations_authorized_view_sets_create_task,
    contactcenterinsights_projects_locations_authorized_view_sets_delete_builder, contactcenterinsights_projects_locations_authorized_view_sets_delete_task,
    contactcenterinsights_projects_locations_authorized_view_sets_patch_builder, contactcenterinsights_projects_locations_authorized_view_sets_patch_task,
    contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_create_builder, contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_create_task,
    contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_delete_builder, contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_delete_task,
    contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_generative_insights_builder, contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_generative_insights_task,
    contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_patch_builder, contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_patch_task,
    contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_query_metrics_builder, contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_query_metrics_task,
    contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_query_performance_overview_builder, contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_query_performance_overview_task,
    contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_set_iam_policy_builder, contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_set_iam_policy_task,
    contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_test_iam_permissions_builder, contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_test_iam_permissions_task,
    contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_delete_builder, contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_delete_task,
    contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_appeal_builder, contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_appeal_task,
    contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_create_builder, contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_create_task,
    contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_delete_builder, contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_delete_task,
    contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_finalize_builder, contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_finalize_task,
    contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_publish_builder, contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_publish_task,
    contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_notes_create_builder, contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_notes_create_task,
    contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_notes_delete_builder, contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_notes_delete_task,
    contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_notes_patch_builder, contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_notes_patch_task,
    contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_feedback_labels_create_builder, contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_feedback_labels_create_task,
    contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_feedback_labels_delete_builder, contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_feedback_labels_delete_task,
    contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_feedback_labels_patch_builder, contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_feedback_labels_patch_task,
    contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_operations_cancel_builder, contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_operations_cancel_task,
    contactcenterinsights_projects_locations_auto_labeling_rules_create_builder, contactcenterinsights_projects_locations_auto_labeling_rules_create_task,
    contactcenterinsights_projects_locations_auto_labeling_rules_delete_builder, contactcenterinsights_projects_locations_auto_labeling_rules_delete_task,
    contactcenterinsights_projects_locations_auto_labeling_rules_patch_builder, contactcenterinsights_projects_locations_auto_labeling_rules_patch_task,
    contactcenterinsights_projects_locations_auto_labeling_rules_test_builder, contactcenterinsights_projects_locations_auto_labeling_rules_test_task,
    contactcenterinsights_projects_locations_conversations_bulk_analyze_builder, contactcenterinsights_projects_locations_conversations_bulk_analyze_task,
    contactcenterinsights_projects_locations_conversations_bulk_delete_builder, contactcenterinsights_projects_locations_conversations_bulk_delete_task,
    contactcenterinsights_projects_locations_conversations_create_builder, contactcenterinsights_projects_locations_conversations_create_task,
    contactcenterinsights_projects_locations_conversations_delete_builder, contactcenterinsights_projects_locations_conversations_delete_task,
    contactcenterinsights_projects_locations_conversations_ingest_builder, contactcenterinsights_projects_locations_conversations_ingest_task,
    contactcenterinsights_projects_locations_conversations_patch_builder, contactcenterinsights_projects_locations_conversations_patch_task,
    contactcenterinsights_projects_locations_conversations_sample_builder, contactcenterinsights_projects_locations_conversations_sample_task,
    contactcenterinsights_projects_locations_conversations_upload_builder, contactcenterinsights_projects_locations_conversations_upload_task,
    contactcenterinsights_projects_locations_conversations_analyses_create_builder, contactcenterinsights_projects_locations_conversations_analyses_create_task,
    contactcenterinsights_projects_locations_conversations_analyses_delete_builder, contactcenterinsights_projects_locations_conversations_analyses_delete_task,
    contactcenterinsights_projects_locations_conversations_assessments_appeal_builder, contactcenterinsights_projects_locations_conversations_assessments_appeal_task,
    contactcenterinsights_projects_locations_conversations_assessments_create_builder, contactcenterinsights_projects_locations_conversations_assessments_create_task,
    contactcenterinsights_projects_locations_conversations_assessments_delete_builder, contactcenterinsights_projects_locations_conversations_assessments_delete_task,
    contactcenterinsights_projects_locations_conversations_assessments_finalize_builder, contactcenterinsights_projects_locations_conversations_assessments_finalize_task,
    contactcenterinsights_projects_locations_conversations_assessments_publish_builder, contactcenterinsights_projects_locations_conversations_assessments_publish_task,
    contactcenterinsights_projects_locations_conversations_assessments_notes_create_builder, contactcenterinsights_projects_locations_conversations_assessments_notes_create_task,
    contactcenterinsights_projects_locations_conversations_assessments_notes_delete_builder, contactcenterinsights_projects_locations_conversations_assessments_notes_delete_task,
    contactcenterinsights_projects_locations_conversations_assessments_notes_patch_builder, contactcenterinsights_projects_locations_conversations_assessments_notes_patch_task,
    contactcenterinsights_projects_locations_conversations_feedback_labels_create_builder, contactcenterinsights_projects_locations_conversations_feedback_labels_create_task,
    contactcenterinsights_projects_locations_conversations_feedback_labels_delete_builder, contactcenterinsights_projects_locations_conversations_feedback_labels_delete_task,
    contactcenterinsights_projects_locations_conversations_feedback_labels_patch_builder, contactcenterinsights_projects_locations_conversations_feedback_labels_patch_task,
    contactcenterinsights_projects_locations_conversations_segments_bulk_analyze_builder, contactcenterinsights_projects_locations_conversations_segments_bulk_analyze_task,
    contactcenterinsights_projects_locations_dashboards_create_builder, contactcenterinsights_projects_locations_dashboards_create_task,
    contactcenterinsights_projects_locations_dashboards_delete_builder, contactcenterinsights_projects_locations_dashboards_delete_task,
    contactcenterinsights_projects_locations_dashboards_patch_builder, contactcenterinsights_projects_locations_dashboards_patch_task,
    contactcenterinsights_projects_locations_dashboards_charts_create_builder, contactcenterinsights_projects_locations_dashboards_charts_create_task,
    contactcenterinsights_projects_locations_dashboards_charts_delete_builder, contactcenterinsights_projects_locations_dashboards_charts_delete_task,
    contactcenterinsights_projects_locations_dashboards_charts_patch_builder, contactcenterinsights_projects_locations_dashboards_charts_patch_task,
    contactcenterinsights_projects_locations_datasets_bulk_delete_feedback_labels_builder, contactcenterinsights_projects_locations_datasets_bulk_delete_feedback_labels_task,
    contactcenterinsights_projects_locations_datasets_bulk_download_feedback_labels_builder, contactcenterinsights_projects_locations_datasets_bulk_download_feedback_labels_task,
    contactcenterinsights_projects_locations_datasets_bulk_upload_feedback_labels_builder, contactcenterinsights_projects_locations_datasets_bulk_upload_feedback_labels_task,
    contactcenterinsights_projects_locations_datasets_create_builder, contactcenterinsights_projects_locations_datasets_create_task,
    contactcenterinsights_projects_locations_datasets_delete_builder, contactcenterinsights_projects_locations_datasets_delete_task,
    contactcenterinsights_projects_locations_datasets_patch_builder, contactcenterinsights_projects_locations_datasets_patch_task,
    contactcenterinsights_projects_locations_datasets_conversations_bulk_delete_builder, contactcenterinsights_projects_locations_datasets_conversations_bulk_delete_task,
    contactcenterinsights_projects_locations_datasets_conversations_calculate_stats_builder, contactcenterinsights_projects_locations_datasets_conversations_calculate_stats_task,
    contactcenterinsights_projects_locations_datasets_conversations_delete_builder, contactcenterinsights_projects_locations_datasets_conversations_delete_task,
    contactcenterinsights_projects_locations_datasets_conversations_ingest_builder, contactcenterinsights_projects_locations_datasets_conversations_ingest_task,
    contactcenterinsights_projects_locations_datasets_conversations_sample_builder, contactcenterinsights_projects_locations_datasets_conversations_sample_task,
    contactcenterinsights_projects_locations_datasets_conversations_feedback_labels_create_builder, contactcenterinsights_projects_locations_datasets_conversations_feedback_labels_create_task,
    contactcenterinsights_projects_locations_datasets_conversations_feedback_labels_delete_builder, contactcenterinsights_projects_locations_datasets_conversations_feedback_labels_delete_task,
    contactcenterinsights_projects_locations_datasets_conversations_feedback_labels_patch_builder, contactcenterinsights_projects_locations_datasets_conversations_feedback_labels_patch_task,
    contactcenterinsights_projects_locations_datasets_insightsdata_export_builder, contactcenterinsights_projects_locations_datasets_insightsdata_export_task,
    contactcenterinsights_projects_locations_encryption_spec_initialize_builder, contactcenterinsights_projects_locations_encryption_spec_initialize_task,
    contactcenterinsights_projects_locations_insightsdata_export_builder, contactcenterinsights_projects_locations_insightsdata_export_task,
    contactcenterinsights_projects_locations_issue_models_create_builder, contactcenterinsights_projects_locations_issue_models_create_task,
    contactcenterinsights_projects_locations_issue_models_delete_builder, contactcenterinsights_projects_locations_issue_models_delete_task,
    contactcenterinsights_projects_locations_issue_models_deploy_builder, contactcenterinsights_projects_locations_issue_models_deploy_task,
    contactcenterinsights_projects_locations_issue_models_export_builder, contactcenterinsights_projects_locations_issue_models_export_task,
    contactcenterinsights_projects_locations_issue_models_import_builder, contactcenterinsights_projects_locations_issue_models_import_task,
    contactcenterinsights_projects_locations_issue_models_patch_builder, contactcenterinsights_projects_locations_issue_models_patch_task,
    contactcenterinsights_projects_locations_issue_models_undeploy_builder, contactcenterinsights_projects_locations_issue_models_undeploy_task,
    contactcenterinsights_projects_locations_issue_models_issues_create_builder, contactcenterinsights_projects_locations_issue_models_issues_create_task,
    contactcenterinsights_projects_locations_issue_models_issues_delete_builder, contactcenterinsights_projects_locations_issue_models_issues_delete_task,
    contactcenterinsights_projects_locations_issue_models_issues_patch_builder, contactcenterinsights_projects_locations_issue_models_issues_patch_task,
    contactcenterinsights_projects_locations_operations_cancel_builder, contactcenterinsights_projects_locations_operations_cancel_task,
    contactcenterinsights_projects_locations_phrase_matchers_create_builder, contactcenterinsights_projects_locations_phrase_matchers_create_task,
    contactcenterinsights_projects_locations_phrase_matchers_delete_builder, contactcenterinsights_projects_locations_phrase_matchers_delete_task,
    contactcenterinsights_projects_locations_phrase_matchers_patch_builder, contactcenterinsights_projects_locations_phrase_matchers_patch_task,
    contactcenterinsights_projects_locations_qa_question_tags_create_builder, contactcenterinsights_projects_locations_qa_question_tags_create_task,
    contactcenterinsights_projects_locations_qa_question_tags_delete_builder, contactcenterinsights_projects_locations_qa_question_tags_delete_task,
    contactcenterinsights_projects_locations_qa_question_tags_patch_builder, contactcenterinsights_projects_locations_qa_question_tags_patch_task,
    contactcenterinsights_projects_locations_qa_scorecards_create_builder, contactcenterinsights_projects_locations_qa_scorecards_create_task,
    contactcenterinsights_projects_locations_qa_scorecards_delete_builder, contactcenterinsights_projects_locations_qa_scorecards_delete_task,
    contactcenterinsights_projects_locations_qa_scorecards_patch_builder, contactcenterinsights_projects_locations_qa_scorecards_patch_task,
    contactcenterinsights_projects_locations_qa_scorecards_revisions_create_builder, contactcenterinsights_projects_locations_qa_scorecards_revisions_create_task,
    contactcenterinsights_projects_locations_qa_scorecards_revisions_delete_builder, contactcenterinsights_projects_locations_qa_scorecards_revisions_delete_task,
    contactcenterinsights_projects_locations_qa_scorecards_revisions_deploy_builder, contactcenterinsights_projects_locations_qa_scorecards_revisions_deploy_task,
    contactcenterinsights_projects_locations_qa_scorecards_revisions_tune_qa_scorecard_revision_builder, contactcenterinsights_projects_locations_qa_scorecards_revisions_tune_qa_scorecard_revision_task,
    contactcenterinsights_projects_locations_qa_scorecards_revisions_undeploy_builder, contactcenterinsights_projects_locations_qa_scorecards_revisions_undeploy_task,
    contactcenterinsights_projects_locations_qa_scorecards_revisions_qa_questions_create_builder, contactcenterinsights_projects_locations_qa_scorecards_revisions_qa_questions_create_task,
    contactcenterinsights_projects_locations_qa_scorecards_revisions_qa_questions_delete_builder, contactcenterinsights_projects_locations_qa_scorecards_revisions_qa_questions_delete_task,
    contactcenterinsights_projects_locations_qa_scorecards_revisions_qa_questions_patch_builder, contactcenterinsights_projects_locations_qa_scorecards_revisions_qa_questions_patch_task,
    contactcenterinsights_projects_locations_views_create_builder, contactcenterinsights_projects_locations_views_create_task,
    contactcenterinsights_projects_locations_views_delete_builder, contactcenterinsights_projects_locations_views_delete_task,
    contactcenterinsights_projects_locations_views_patch_builder, contactcenterinsights_projects_locations_views_patch_task,
};
use crate::providers::gcp::clients::types::{ApiError, ApiPending};
use crate::providers::gcp::clients::contactcenterinsights::GoogleCloudContactcenterinsightsV1AnalysisRule;
use crate::providers::gcp::clients::contactcenterinsights::GoogleCloudContactcenterinsightsV1Assessment;
use crate::providers::gcp::clients::contactcenterinsights::GoogleCloudContactcenterinsightsV1AssessmentRule;
use crate::providers::gcp::clients::contactcenterinsights::GoogleCloudContactcenterinsightsV1AuthorizedView;
use crate::providers::gcp::clients::contactcenterinsights::GoogleCloudContactcenterinsightsV1AuthorizedViewSet;
use crate::providers::gcp::clients::contactcenterinsights::GoogleCloudContactcenterinsightsV1AutoLabelingRule;
use crate::providers::gcp::clients::contactcenterinsights::GoogleCloudContactcenterinsightsV1CalculateStatsResponse;
use crate::providers::gcp::clients::contactcenterinsights::GoogleCloudContactcenterinsightsV1Chart;
use crate::providers::gcp::clients::contactcenterinsights::GoogleCloudContactcenterinsightsV1Conversation;
use crate::providers::gcp::clients::contactcenterinsights::GoogleCloudContactcenterinsightsV1CorrelationConfig;
use crate::providers::gcp::clients::contactcenterinsights::GoogleCloudContactcenterinsightsV1Dashboard;
use crate::providers::gcp::clients::contactcenterinsights::GoogleCloudContactcenterinsightsV1Dataset;
use crate::providers::gcp::clients::contactcenterinsights::GoogleCloudContactcenterinsightsV1FeedbackLabel;
use crate::providers::gcp::clients::contactcenterinsights::GoogleCloudContactcenterinsightsV1Issue;
use crate::providers::gcp::clients::contactcenterinsights::GoogleCloudContactcenterinsightsV1IssueModel;
use crate::providers::gcp::clients::contactcenterinsights::GoogleCloudContactcenterinsightsV1Note;
use crate::providers::gcp::clients::contactcenterinsights::GoogleCloudContactcenterinsightsV1PhraseMatcher;
use crate::providers::gcp::clients::contactcenterinsights::GoogleCloudContactcenterinsightsV1QaQuestion;
use crate::providers::gcp::clients::contactcenterinsights::GoogleCloudContactcenterinsightsV1QaQuestionTag;
use crate::providers::gcp::clients::contactcenterinsights::GoogleCloudContactcenterinsightsV1QaScorecard;
use crate::providers::gcp::clients::contactcenterinsights::GoogleCloudContactcenterinsightsV1QaScorecardRevision;
use crate::providers::gcp::clients::contactcenterinsights::GoogleCloudContactcenterinsightsV1Settings;
use crate::providers::gcp::clients::contactcenterinsights::GoogleCloudContactcenterinsightsV1TestAutoLabelingRuleResponse;
use crate::providers::gcp::clients::contactcenterinsights::GoogleCloudContactcenterinsightsV1View;
use crate::providers::gcp::clients::contactcenterinsights::GoogleIamV1Policy;
use crate::providers::gcp::clients::contactcenterinsights::GoogleIamV1TestIamPermissionsResponse;
use crate::providers::gcp::clients::contactcenterinsights::GoogleLongrunningOperation;
use crate::providers::gcp::clients::contactcenterinsights::GoogleProtobufEmpty;
use crate::providers::gcp::clients::contactcenterinsights::ContactcenterinsightsProjectsLocationsAnalysisRulesCreateArgs;
use crate::providers::gcp::clients::contactcenterinsights::ContactcenterinsightsProjectsLocationsAnalysisRulesDeleteArgs;
use crate::providers::gcp::clients::contactcenterinsights::ContactcenterinsightsProjectsLocationsAnalysisRulesPatchArgs;
use crate::providers::gcp::clients::contactcenterinsights::ContactcenterinsightsProjectsLocationsAssessmentRulesCreateArgs;
use crate::providers::gcp::clients::contactcenterinsights::ContactcenterinsightsProjectsLocationsAssessmentRulesDeleteArgs;
use crate::providers::gcp::clients::contactcenterinsights::ContactcenterinsightsProjectsLocationsAssessmentRulesPatchArgs;
use crate::providers::gcp::clients::contactcenterinsights::ContactcenterinsightsProjectsLocationsAuthorizedViewSetsAuthorizedViewsConversationsAssessmentsAppealArgs;
use crate::providers::gcp::clients::contactcenterinsights::ContactcenterinsightsProjectsLocationsAuthorizedViewSetsAuthorizedViewsConversationsAssessmentsCreateArgs;
use crate::providers::gcp::clients::contactcenterinsights::ContactcenterinsightsProjectsLocationsAuthorizedViewSetsAuthorizedViewsConversationsAssessmentsDeleteArgs;
use crate::providers::gcp::clients::contactcenterinsights::ContactcenterinsightsProjectsLocationsAuthorizedViewSetsAuthorizedViewsConversationsAssessmentsFinalizeArgs;
use crate::providers::gcp::clients::contactcenterinsights::ContactcenterinsightsProjectsLocationsAuthorizedViewSetsAuthorizedViewsConversationsAssessmentsNotesCreateArgs;
use crate::providers::gcp::clients::contactcenterinsights::ContactcenterinsightsProjectsLocationsAuthorizedViewSetsAuthorizedViewsConversationsAssessmentsNotesDeleteArgs;
use crate::providers::gcp::clients::contactcenterinsights::ContactcenterinsightsProjectsLocationsAuthorizedViewSetsAuthorizedViewsConversationsAssessmentsNotesPatchArgs;
use crate::providers::gcp::clients::contactcenterinsights::ContactcenterinsightsProjectsLocationsAuthorizedViewSetsAuthorizedViewsConversationsAssessmentsPublishArgs;
use crate::providers::gcp::clients::contactcenterinsights::ContactcenterinsightsProjectsLocationsAuthorizedViewSetsAuthorizedViewsConversationsDeleteArgs;
use crate::providers::gcp::clients::contactcenterinsights::ContactcenterinsightsProjectsLocationsAuthorizedViewSetsAuthorizedViewsConversationsFeedbackLabelsCreateArgs;
use crate::providers::gcp::clients::contactcenterinsights::ContactcenterinsightsProjectsLocationsAuthorizedViewSetsAuthorizedViewsConversationsFeedbackLabelsDeleteArgs;
use crate::providers::gcp::clients::contactcenterinsights::ContactcenterinsightsProjectsLocationsAuthorizedViewSetsAuthorizedViewsConversationsFeedbackLabelsPatchArgs;
use crate::providers::gcp::clients::contactcenterinsights::ContactcenterinsightsProjectsLocationsAuthorizedViewSetsAuthorizedViewsCreateArgs;
use crate::providers::gcp::clients::contactcenterinsights::ContactcenterinsightsProjectsLocationsAuthorizedViewSetsAuthorizedViewsDeleteArgs;
use crate::providers::gcp::clients::contactcenterinsights::ContactcenterinsightsProjectsLocationsAuthorizedViewSetsAuthorizedViewsGenerativeInsightsArgs;
use crate::providers::gcp::clients::contactcenterinsights::ContactcenterinsightsProjectsLocationsAuthorizedViewSetsAuthorizedViewsOperationsCancelArgs;
use crate::providers::gcp::clients::contactcenterinsights::ContactcenterinsightsProjectsLocationsAuthorizedViewSetsAuthorizedViewsPatchArgs;
use crate::providers::gcp::clients::contactcenterinsights::ContactcenterinsightsProjectsLocationsAuthorizedViewSetsAuthorizedViewsQueryMetricsArgs;
use crate::providers::gcp::clients::contactcenterinsights::ContactcenterinsightsProjectsLocationsAuthorizedViewSetsAuthorizedViewsQueryPerformanceOverviewArgs;
use crate::providers::gcp::clients::contactcenterinsights::ContactcenterinsightsProjectsLocationsAuthorizedViewSetsAuthorizedViewsSetIamPolicyArgs;
use crate::providers::gcp::clients::contactcenterinsights::ContactcenterinsightsProjectsLocationsAuthorizedViewSetsAuthorizedViewsTestIamPermissionsArgs;
use crate::providers::gcp::clients::contactcenterinsights::ContactcenterinsightsProjectsLocationsAuthorizedViewSetsCreateArgs;
use crate::providers::gcp::clients::contactcenterinsights::ContactcenterinsightsProjectsLocationsAuthorizedViewSetsDeleteArgs;
use crate::providers::gcp::clients::contactcenterinsights::ContactcenterinsightsProjectsLocationsAuthorizedViewSetsPatchArgs;
use crate::providers::gcp::clients::contactcenterinsights::ContactcenterinsightsProjectsLocationsAutoLabelingRulesCreateArgs;
use crate::providers::gcp::clients::contactcenterinsights::ContactcenterinsightsProjectsLocationsAutoLabelingRulesDeleteArgs;
use crate::providers::gcp::clients::contactcenterinsights::ContactcenterinsightsProjectsLocationsAutoLabelingRulesPatchArgs;
use crate::providers::gcp::clients::contactcenterinsights::ContactcenterinsightsProjectsLocationsAutoLabelingRulesTestArgs;
use crate::providers::gcp::clients::contactcenterinsights::ContactcenterinsightsProjectsLocationsBulkDeleteFeedbackLabelsArgs;
use crate::providers::gcp::clients::contactcenterinsights::ContactcenterinsightsProjectsLocationsBulkDownloadFeedbackLabelsArgs;
use crate::providers::gcp::clients::contactcenterinsights::ContactcenterinsightsProjectsLocationsBulkUploadFeedbackLabelsArgs;
use crate::providers::gcp::clients::contactcenterinsights::ContactcenterinsightsProjectsLocationsConversationsAnalysesCreateArgs;
use crate::providers::gcp::clients::contactcenterinsights::ContactcenterinsightsProjectsLocationsConversationsAnalysesDeleteArgs;
use crate::providers::gcp::clients::contactcenterinsights::ContactcenterinsightsProjectsLocationsConversationsAssessmentsAppealArgs;
use crate::providers::gcp::clients::contactcenterinsights::ContactcenterinsightsProjectsLocationsConversationsAssessmentsCreateArgs;
use crate::providers::gcp::clients::contactcenterinsights::ContactcenterinsightsProjectsLocationsConversationsAssessmentsDeleteArgs;
use crate::providers::gcp::clients::contactcenterinsights::ContactcenterinsightsProjectsLocationsConversationsAssessmentsFinalizeArgs;
use crate::providers::gcp::clients::contactcenterinsights::ContactcenterinsightsProjectsLocationsConversationsAssessmentsNotesCreateArgs;
use crate::providers::gcp::clients::contactcenterinsights::ContactcenterinsightsProjectsLocationsConversationsAssessmentsNotesDeleteArgs;
use crate::providers::gcp::clients::contactcenterinsights::ContactcenterinsightsProjectsLocationsConversationsAssessmentsNotesPatchArgs;
use crate::providers::gcp::clients::contactcenterinsights::ContactcenterinsightsProjectsLocationsConversationsAssessmentsPublishArgs;
use crate::providers::gcp::clients::contactcenterinsights::ContactcenterinsightsProjectsLocationsConversationsBulkAnalyzeArgs;
use crate::providers::gcp::clients::contactcenterinsights::ContactcenterinsightsProjectsLocationsConversationsBulkDeleteArgs;
use crate::providers::gcp::clients::contactcenterinsights::ContactcenterinsightsProjectsLocationsConversationsCreateArgs;
use crate::providers::gcp::clients::contactcenterinsights::ContactcenterinsightsProjectsLocationsConversationsDeleteArgs;
use crate::providers::gcp::clients::contactcenterinsights::ContactcenterinsightsProjectsLocationsConversationsFeedbackLabelsCreateArgs;
use crate::providers::gcp::clients::contactcenterinsights::ContactcenterinsightsProjectsLocationsConversationsFeedbackLabelsDeleteArgs;
use crate::providers::gcp::clients::contactcenterinsights::ContactcenterinsightsProjectsLocationsConversationsFeedbackLabelsPatchArgs;
use crate::providers::gcp::clients::contactcenterinsights::ContactcenterinsightsProjectsLocationsConversationsIngestArgs;
use crate::providers::gcp::clients::contactcenterinsights::ContactcenterinsightsProjectsLocationsConversationsPatchArgs;
use crate::providers::gcp::clients::contactcenterinsights::ContactcenterinsightsProjectsLocationsConversationsSampleArgs;
use crate::providers::gcp::clients::contactcenterinsights::ContactcenterinsightsProjectsLocationsConversationsSegmentsBulkAnalyzeArgs;
use crate::providers::gcp::clients::contactcenterinsights::ContactcenterinsightsProjectsLocationsConversationsUploadArgs;
use crate::providers::gcp::clients::contactcenterinsights::ContactcenterinsightsProjectsLocationsDashboardsChartsCreateArgs;
use crate::providers::gcp::clients::contactcenterinsights::ContactcenterinsightsProjectsLocationsDashboardsChartsDeleteArgs;
use crate::providers::gcp::clients::contactcenterinsights::ContactcenterinsightsProjectsLocationsDashboardsChartsPatchArgs;
use crate::providers::gcp::clients::contactcenterinsights::ContactcenterinsightsProjectsLocationsDashboardsCreateArgs;
use crate::providers::gcp::clients::contactcenterinsights::ContactcenterinsightsProjectsLocationsDashboardsDeleteArgs;
use crate::providers::gcp::clients::contactcenterinsights::ContactcenterinsightsProjectsLocationsDashboardsPatchArgs;
use crate::providers::gcp::clients::contactcenterinsights::ContactcenterinsightsProjectsLocationsDatasetsBulkDeleteFeedbackLabelsArgs;
use crate::providers::gcp::clients::contactcenterinsights::ContactcenterinsightsProjectsLocationsDatasetsBulkDownloadFeedbackLabelsArgs;
use crate::providers::gcp::clients::contactcenterinsights::ContactcenterinsightsProjectsLocationsDatasetsBulkUploadFeedbackLabelsArgs;
use crate::providers::gcp::clients::contactcenterinsights::ContactcenterinsightsProjectsLocationsDatasetsConversationsBulkDeleteArgs;
use crate::providers::gcp::clients::contactcenterinsights::ContactcenterinsightsProjectsLocationsDatasetsConversationsCalculateStatsArgs;
use crate::providers::gcp::clients::contactcenterinsights::ContactcenterinsightsProjectsLocationsDatasetsConversationsDeleteArgs;
use crate::providers::gcp::clients::contactcenterinsights::ContactcenterinsightsProjectsLocationsDatasetsConversationsFeedbackLabelsCreateArgs;
use crate::providers::gcp::clients::contactcenterinsights::ContactcenterinsightsProjectsLocationsDatasetsConversationsFeedbackLabelsDeleteArgs;
use crate::providers::gcp::clients::contactcenterinsights::ContactcenterinsightsProjectsLocationsDatasetsConversationsFeedbackLabelsPatchArgs;
use crate::providers::gcp::clients::contactcenterinsights::ContactcenterinsightsProjectsLocationsDatasetsConversationsIngestArgs;
use crate::providers::gcp::clients::contactcenterinsights::ContactcenterinsightsProjectsLocationsDatasetsConversationsSampleArgs;
use crate::providers::gcp::clients::contactcenterinsights::ContactcenterinsightsProjectsLocationsDatasetsCreateArgs;
use crate::providers::gcp::clients::contactcenterinsights::ContactcenterinsightsProjectsLocationsDatasetsDeleteArgs;
use crate::providers::gcp::clients::contactcenterinsights::ContactcenterinsightsProjectsLocationsDatasetsInsightsdataExportArgs;
use crate::providers::gcp::clients::contactcenterinsights::ContactcenterinsightsProjectsLocationsDatasetsPatchArgs;
use crate::providers::gcp::clients::contactcenterinsights::ContactcenterinsightsProjectsLocationsEncryptionSpecInitializeArgs;
use crate::providers::gcp::clients::contactcenterinsights::ContactcenterinsightsProjectsLocationsGenerativeInsightsArgs;
use crate::providers::gcp::clients::contactcenterinsights::ContactcenterinsightsProjectsLocationsInsightsdataExportArgs;
use crate::providers::gcp::clients::contactcenterinsights::ContactcenterinsightsProjectsLocationsIssueModelsCreateArgs;
use crate::providers::gcp::clients::contactcenterinsights::ContactcenterinsightsProjectsLocationsIssueModelsDeleteArgs;
use crate::providers::gcp::clients::contactcenterinsights::ContactcenterinsightsProjectsLocationsIssueModelsDeployArgs;
use crate::providers::gcp::clients::contactcenterinsights::ContactcenterinsightsProjectsLocationsIssueModelsExportArgs;
use crate::providers::gcp::clients::contactcenterinsights::ContactcenterinsightsProjectsLocationsIssueModelsImportArgs;
use crate::providers::gcp::clients::contactcenterinsights::ContactcenterinsightsProjectsLocationsIssueModelsIssuesCreateArgs;
use crate::providers::gcp::clients::contactcenterinsights::ContactcenterinsightsProjectsLocationsIssueModelsIssuesDeleteArgs;
use crate::providers::gcp::clients::contactcenterinsights::ContactcenterinsightsProjectsLocationsIssueModelsIssuesPatchArgs;
use crate::providers::gcp::clients::contactcenterinsights::ContactcenterinsightsProjectsLocationsIssueModelsPatchArgs;
use crate::providers::gcp::clients::contactcenterinsights::ContactcenterinsightsProjectsLocationsIssueModelsUndeployArgs;
use crate::providers::gcp::clients::contactcenterinsights::ContactcenterinsightsProjectsLocationsOperationsCancelArgs;
use crate::providers::gcp::clients::contactcenterinsights::ContactcenterinsightsProjectsLocationsPhraseMatchersCreateArgs;
use crate::providers::gcp::clients::contactcenterinsights::ContactcenterinsightsProjectsLocationsPhraseMatchersDeleteArgs;
use crate::providers::gcp::clients::contactcenterinsights::ContactcenterinsightsProjectsLocationsPhraseMatchersPatchArgs;
use crate::providers::gcp::clients::contactcenterinsights::ContactcenterinsightsProjectsLocationsQaQuestionTagsCreateArgs;
use crate::providers::gcp::clients::contactcenterinsights::ContactcenterinsightsProjectsLocationsQaQuestionTagsDeleteArgs;
use crate::providers::gcp::clients::contactcenterinsights::ContactcenterinsightsProjectsLocationsQaQuestionTagsPatchArgs;
use crate::providers::gcp::clients::contactcenterinsights::ContactcenterinsightsProjectsLocationsQaScorecardsCreateArgs;
use crate::providers::gcp::clients::contactcenterinsights::ContactcenterinsightsProjectsLocationsQaScorecardsDeleteArgs;
use crate::providers::gcp::clients::contactcenterinsights::ContactcenterinsightsProjectsLocationsQaScorecardsPatchArgs;
use crate::providers::gcp::clients::contactcenterinsights::ContactcenterinsightsProjectsLocationsQaScorecardsRevisionsCreateArgs;
use crate::providers::gcp::clients::contactcenterinsights::ContactcenterinsightsProjectsLocationsQaScorecardsRevisionsDeleteArgs;
use crate::providers::gcp::clients::contactcenterinsights::ContactcenterinsightsProjectsLocationsQaScorecardsRevisionsDeployArgs;
use crate::providers::gcp::clients::contactcenterinsights::ContactcenterinsightsProjectsLocationsQaScorecardsRevisionsQaQuestionsCreateArgs;
use crate::providers::gcp::clients::contactcenterinsights::ContactcenterinsightsProjectsLocationsQaScorecardsRevisionsQaQuestionsDeleteArgs;
use crate::providers::gcp::clients::contactcenterinsights::ContactcenterinsightsProjectsLocationsQaScorecardsRevisionsQaQuestionsPatchArgs;
use crate::providers::gcp::clients::contactcenterinsights::ContactcenterinsightsProjectsLocationsQaScorecardsRevisionsTuneQaScorecardRevisionArgs;
use crate::providers::gcp::clients::contactcenterinsights::ContactcenterinsightsProjectsLocationsQaScorecardsRevisionsUndeployArgs;
use crate::providers::gcp::clients::contactcenterinsights::ContactcenterinsightsProjectsLocationsQueryMetricsArgs;
use crate::providers::gcp::clients::contactcenterinsights::ContactcenterinsightsProjectsLocationsQueryPerformanceOverviewArgs;
use crate::providers::gcp::clients::contactcenterinsights::ContactcenterinsightsProjectsLocationsTestCorrelationConfigArgs;
use crate::providers::gcp::clients::contactcenterinsights::ContactcenterinsightsProjectsLocationsUpdateCorrelationConfigArgs;
use crate::providers::gcp::clients::contactcenterinsights::ContactcenterinsightsProjectsLocationsUpdateSettingsArgs;
use crate::providers::gcp::clients::contactcenterinsights::ContactcenterinsightsProjectsLocationsViewsCreateArgs;
use crate::providers::gcp::clients::contactcenterinsights::ContactcenterinsightsProjectsLocationsViewsDeleteArgs;
use crate::providers::gcp::clients::contactcenterinsights::ContactcenterinsightsProjectsLocationsViewsPatchArgs;
use crate::provider_client::{ProviderClient, ProviderError};
use foundation_core::valtron::{execute, StreamIterator};
use foundation_core::wire::simple_http::client::SimpleHttpClient;
use foundation_db::state::store_state_task::StoreStateIdentifierTask;
use std::sync::Arc;

/// ContactcenterinsightsProvider with automatic state tracking.
///
/// # Type Parameters
///
/// * `S` - StateStore implementation (FileStateStore, SqliteStateStore, etc.)
///
/// # Example
///
/// ```rust
/// let state_store = FileStateStore::new("/path", "my-project", "dev");
/// let client = ProviderClient::new("my-project", "dev", state_store);
/// let http_client = SimpleHttpClient::new(...);
/// let provider = ContactcenterinsightsProvider::new(client, http_client);
/// ```
#[derive(Clone)]
pub struct ContactcenterinsightsProvider<S>
where
    S: foundation_db::state::traits::StateStore + Send + Sync + 'static,
{
    client: ProviderClient<S>,
    http_client: Arc<SimpleHttpClient>,
}

impl<S> ContactcenterinsightsProvider<S>
where
    S: foundation_db::state::traits::StateStore + Send + Sync + 'static,
{
    /// Create new ContactcenterinsightsProvider.
    pub fn new(client: ProviderClient<S>, http_client: SimpleHttpClient) -> Self {
        Self {
            client,
            http_client: Arc::new(http_client),
        }
    }

    /// Contactcenterinsights projects locations bulk delete feedback labels.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn contactcenterinsights_projects_locations_bulk_delete_feedback_labels(
        &self,
        args: &ContactcenterinsightsProjectsLocationsBulkDeleteFeedbackLabelsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = contactcenterinsights_projects_locations_bulk_delete_feedback_labels_builder(
            &self.http_client,
            &args.parent,
        )
        .map_err(ProviderError::Api)?;

        let task = contactcenterinsights_projects_locations_bulk_delete_feedback_labels_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Contactcenterinsights projects locations bulk download feedback labels.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn contactcenterinsights_projects_locations_bulk_download_feedback_labels(
        &self,
        args: &ContactcenterinsightsProjectsLocationsBulkDownloadFeedbackLabelsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = contactcenterinsights_projects_locations_bulk_download_feedback_labels_builder(
            &self.http_client,
            &args.parent,
        )
        .map_err(ProviderError::Api)?;

        let task = contactcenterinsights_projects_locations_bulk_download_feedback_labels_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Contactcenterinsights projects locations bulk upload feedback labels.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn contactcenterinsights_projects_locations_bulk_upload_feedback_labels(
        &self,
        args: &ContactcenterinsightsProjectsLocationsBulkUploadFeedbackLabelsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = contactcenterinsights_projects_locations_bulk_upload_feedback_labels_builder(
            &self.http_client,
            &args.parent,
        )
        .map_err(ProviderError::Api)?;

        let task = contactcenterinsights_projects_locations_bulk_upload_feedback_labels_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Contactcenterinsights projects locations generative insights.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn contactcenterinsights_projects_locations_generative_insights(
        &self,
        args: &ContactcenterinsightsProjectsLocationsGenerativeInsightsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = contactcenterinsights_projects_locations_generative_insights_builder(
            &self.http_client,
            &args.location,
        )
        .map_err(ProviderError::Api)?;

        let task = contactcenterinsights_projects_locations_generative_insights_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Contactcenterinsights projects locations query metrics.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn contactcenterinsights_projects_locations_query_metrics(
        &self,
        args: &ContactcenterinsightsProjectsLocationsQueryMetricsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = contactcenterinsights_projects_locations_query_metrics_builder(
            &self.http_client,
            &args.location,
        )
        .map_err(ProviderError::Api)?;

        let task = contactcenterinsights_projects_locations_query_metrics_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Contactcenterinsights projects locations query performance overview.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn contactcenterinsights_projects_locations_query_performance_overview(
        &self,
        args: &ContactcenterinsightsProjectsLocationsQueryPerformanceOverviewArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = contactcenterinsights_projects_locations_query_performance_overview_builder(
            &self.http_client,
            &args.parent,
        )
        .map_err(ProviderError::Api)?;

        let task = contactcenterinsights_projects_locations_query_performance_overview_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Contactcenterinsights projects locations test correlation config.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn contactcenterinsights_projects_locations_test_correlation_config(
        &self,
        args: &ContactcenterinsightsProjectsLocationsTestCorrelationConfigArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = contactcenterinsights_projects_locations_test_correlation_config_builder(
            &self.http_client,
            &args.location,
        )
        .map_err(ProviderError::Api)?;

        let task = contactcenterinsights_projects_locations_test_correlation_config_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Contactcenterinsights projects locations update correlation config.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudContactcenterinsightsV1CorrelationConfig result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn contactcenterinsights_projects_locations_update_correlation_config(
        &self,
        args: &ContactcenterinsightsProjectsLocationsUpdateCorrelationConfigArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudContactcenterinsightsV1CorrelationConfig, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = contactcenterinsights_projects_locations_update_correlation_config_builder(
            &self.http_client,
            &args.name,
            &args.updateMask,
        )
        .map_err(ProviderError::Api)?;

        let task = contactcenterinsights_projects_locations_update_correlation_config_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Contactcenterinsights projects locations update settings.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudContactcenterinsightsV1Settings result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn contactcenterinsights_projects_locations_update_settings(
        &self,
        args: &ContactcenterinsightsProjectsLocationsUpdateSettingsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudContactcenterinsightsV1Settings, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = contactcenterinsights_projects_locations_update_settings_builder(
            &self.http_client,
            &args.name,
            &args.updateMask,
        )
        .map_err(ProviderError::Api)?;

        let task = contactcenterinsights_projects_locations_update_settings_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Contactcenterinsights projects locations analysis rules create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudContactcenterinsightsV1AnalysisRule result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn contactcenterinsights_projects_locations_analysis_rules_create(
        &self,
        args: &ContactcenterinsightsProjectsLocationsAnalysisRulesCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudContactcenterinsightsV1AnalysisRule, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = contactcenterinsights_projects_locations_analysis_rules_create_builder(
            &self.http_client,
            &args.parent,
        )
        .map_err(ProviderError::Api)?;

        let task = contactcenterinsights_projects_locations_analysis_rules_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Contactcenterinsights projects locations analysis rules delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn contactcenterinsights_projects_locations_analysis_rules_delete(
        &self,
        args: &ContactcenterinsightsProjectsLocationsAnalysisRulesDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = contactcenterinsights_projects_locations_analysis_rules_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = contactcenterinsights_projects_locations_analysis_rules_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Contactcenterinsights projects locations analysis rules patch.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudContactcenterinsightsV1AnalysisRule result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn contactcenterinsights_projects_locations_analysis_rules_patch(
        &self,
        args: &ContactcenterinsightsProjectsLocationsAnalysisRulesPatchArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudContactcenterinsightsV1AnalysisRule, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = contactcenterinsights_projects_locations_analysis_rules_patch_builder(
            &self.http_client,
            &args.name,
            &args.updateMask,
        )
        .map_err(ProviderError::Api)?;

        let task = contactcenterinsights_projects_locations_analysis_rules_patch_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Contactcenterinsights projects locations assessment rules create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudContactcenterinsightsV1AssessmentRule result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn contactcenterinsights_projects_locations_assessment_rules_create(
        &self,
        args: &ContactcenterinsightsProjectsLocationsAssessmentRulesCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudContactcenterinsightsV1AssessmentRule, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = contactcenterinsights_projects_locations_assessment_rules_create_builder(
            &self.http_client,
            &args.parent,
            &args.assessmentRuleId,
        )
        .map_err(ProviderError::Api)?;

        let task = contactcenterinsights_projects_locations_assessment_rules_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Contactcenterinsights projects locations assessment rules delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn contactcenterinsights_projects_locations_assessment_rules_delete(
        &self,
        args: &ContactcenterinsightsProjectsLocationsAssessmentRulesDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = contactcenterinsights_projects_locations_assessment_rules_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = contactcenterinsights_projects_locations_assessment_rules_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Contactcenterinsights projects locations assessment rules patch.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudContactcenterinsightsV1AssessmentRule result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn contactcenterinsights_projects_locations_assessment_rules_patch(
        &self,
        args: &ContactcenterinsightsProjectsLocationsAssessmentRulesPatchArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudContactcenterinsightsV1AssessmentRule, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = contactcenterinsights_projects_locations_assessment_rules_patch_builder(
            &self.http_client,
            &args.name,
            &args.updateMask,
        )
        .map_err(ProviderError::Api)?;

        let task = contactcenterinsights_projects_locations_assessment_rules_patch_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Contactcenterinsights projects locations authorized view sets create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudContactcenterinsightsV1AuthorizedViewSet result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn contactcenterinsights_projects_locations_authorized_view_sets_create(
        &self,
        args: &ContactcenterinsightsProjectsLocationsAuthorizedViewSetsCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudContactcenterinsightsV1AuthorizedViewSet, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = contactcenterinsights_projects_locations_authorized_view_sets_create_builder(
            &self.http_client,
            &args.parent,
            &args.authorizedViewSetId,
        )
        .map_err(ProviderError::Api)?;

        let task = contactcenterinsights_projects_locations_authorized_view_sets_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Contactcenterinsights projects locations authorized view sets delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn contactcenterinsights_projects_locations_authorized_view_sets_delete(
        &self,
        args: &ContactcenterinsightsProjectsLocationsAuthorizedViewSetsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = contactcenterinsights_projects_locations_authorized_view_sets_delete_builder(
            &self.http_client,
            &args.name,
            &args.force,
        )
        .map_err(ProviderError::Api)?;

        let task = contactcenterinsights_projects_locations_authorized_view_sets_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Contactcenterinsights projects locations authorized view sets patch.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudContactcenterinsightsV1AuthorizedViewSet result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn contactcenterinsights_projects_locations_authorized_view_sets_patch(
        &self,
        args: &ContactcenterinsightsProjectsLocationsAuthorizedViewSetsPatchArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudContactcenterinsightsV1AuthorizedViewSet, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = contactcenterinsights_projects_locations_authorized_view_sets_patch_builder(
            &self.http_client,
            &args.name,
            &args.updateMask,
        )
        .map_err(ProviderError::Api)?;

        let task = contactcenterinsights_projects_locations_authorized_view_sets_patch_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Contactcenterinsights projects locations authorized view sets authorized views create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudContactcenterinsightsV1AuthorizedView result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_create(
        &self,
        args: &ContactcenterinsightsProjectsLocationsAuthorizedViewSetsAuthorizedViewsCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudContactcenterinsightsV1AuthorizedView, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_create_builder(
            &self.http_client,
            &args.parent,
            &args.authorizedViewId,
        )
        .map_err(ProviderError::Api)?;

        let task = contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Contactcenterinsights projects locations authorized view sets authorized views delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_delete(
        &self,
        args: &ContactcenterinsightsProjectsLocationsAuthorizedViewSetsAuthorizedViewsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Contactcenterinsights projects locations authorized view sets authorized views generative insights.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_generative_insights(
        &self,
        args: &ContactcenterinsightsProjectsLocationsAuthorizedViewSetsAuthorizedViewsGenerativeInsightsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_generative_insights_builder(
            &self.http_client,
            &args.location,
        )
        .map_err(ProviderError::Api)?;

        let task = contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_generative_insights_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Contactcenterinsights projects locations authorized view sets authorized views patch.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudContactcenterinsightsV1AuthorizedView result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_patch(
        &self,
        args: &ContactcenterinsightsProjectsLocationsAuthorizedViewSetsAuthorizedViewsPatchArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudContactcenterinsightsV1AuthorizedView, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_patch_builder(
            &self.http_client,
            &args.name,
            &args.updateMask,
        )
        .map_err(ProviderError::Api)?;

        let task = contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_patch_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Contactcenterinsights projects locations authorized view sets authorized views query metrics.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_query_metrics(
        &self,
        args: &ContactcenterinsightsProjectsLocationsAuthorizedViewSetsAuthorizedViewsQueryMetricsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_query_metrics_builder(
            &self.http_client,
            &args.location,
        )
        .map_err(ProviderError::Api)?;

        let task = contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_query_metrics_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Contactcenterinsights projects locations authorized view sets authorized views query performance overview.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_query_performance_overview(
        &self,
        args: &ContactcenterinsightsProjectsLocationsAuthorizedViewSetsAuthorizedViewsQueryPerformanceOverviewArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_query_performance_overview_builder(
            &self.http_client,
            &args.parent,
        )
        .map_err(ProviderError::Api)?;

        let task = contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_query_performance_overview_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Contactcenterinsights projects locations authorized view sets authorized views set iam policy.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleIamV1Policy result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_set_iam_policy(
        &self,
        args: &ContactcenterinsightsProjectsLocationsAuthorizedViewSetsAuthorizedViewsSetIamPolicyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleIamV1Policy, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_set_iam_policy_builder(
            &self.http_client,
            &args.resource,
        )
        .map_err(ProviderError::Api)?;

        let task = contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_set_iam_policy_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Contactcenterinsights projects locations authorized view sets authorized views test iam permissions.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleIamV1TestIamPermissionsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_test_iam_permissions(
        &self,
        args: &ContactcenterinsightsProjectsLocationsAuthorizedViewSetsAuthorizedViewsTestIamPermissionsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleIamV1TestIamPermissionsResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_test_iam_permissions_builder(
            &self.http_client,
            &args.resource,
        )
        .map_err(ProviderError::Api)?;

        let task = contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_test_iam_permissions_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Contactcenterinsights projects locations authorized view sets authorized views conversations delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_delete(
        &self,
        args: &ContactcenterinsightsProjectsLocationsAuthorizedViewSetsAuthorizedViewsConversationsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_delete_builder(
            &self.http_client,
            &args.name,
            &args.force,
        )
        .map_err(ProviderError::Api)?;

        let task = contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Contactcenterinsights projects locations authorized view sets authorized views conversations assessments appeal.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudContactcenterinsightsV1Assessment result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_appeal(
        &self,
        args: &ContactcenterinsightsProjectsLocationsAuthorizedViewSetsAuthorizedViewsConversationsAssessmentsAppealArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudContactcenterinsightsV1Assessment, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_appeal_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_appeal_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Contactcenterinsights projects locations authorized view sets authorized views conversations assessments create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudContactcenterinsightsV1Assessment result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_create(
        &self,
        args: &ContactcenterinsightsProjectsLocationsAuthorizedViewSetsAuthorizedViewsConversationsAssessmentsCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudContactcenterinsightsV1Assessment, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_create_builder(
            &self.http_client,
            &args.parent,
        )
        .map_err(ProviderError::Api)?;

        let task = contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Contactcenterinsights projects locations authorized view sets authorized views conversations assessments delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_delete(
        &self,
        args: &ContactcenterinsightsProjectsLocationsAuthorizedViewSetsAuthorizedViewsConversationsAssessmentsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_delete_builder(
            &self.http_client,
            &args.name,
            &args.force,
        )
        .map_err(ProviderError::Api)?;

        let task = contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Contactcenterinsights projects locations authorized view sets authorized views conversations assessments finalize.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudContactcenterinsightsV1Assessment result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_finalize(
        &self,
        args: &ContactcenterinsightsProjectsLocationsAuthorizedViewSetsAuthorizedViewsConversationsAssessmentsFinalizeArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudContactcenterinsightsV1Assessment, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_finalize_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_finalize_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Contactcenterinsights projects locations authorized view sets authorized views conversations assessments publish.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudContactcenterinsightsV1Assessment result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_publish(
        &self,
        args: &ContactcenterinsightsProjectsLocationsAuthorizedViewSetsAuthorizedViewsConversationsAssessmentsPublishArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudContactcenterinsightsV1Assessment, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_publish_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_publish_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Contactcenterinsights projects locations authorized view sets authorized views conversations assessments notes create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudContactcenterinsightsV1Note result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_notes_create(
        &self,
        args: &ContactcenterinsightsProjectsLocationsAuthorizedViewSetsAuthorizedViewsConversationsAssessmentsNotesCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudContactcenterinsightsV1Note, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_notes_create_builder(
            &self.http_client,
            &args.parent,
        )
        .map_err(ProviderError::Api)?;

        let task = contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_notes_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Contactcenterinsights projects locations authorized view sets authorized views conversations assessments notes delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_notes_delete(
        &self,
        args: &ContactcenterinsightsProjectsLocationsAuthorizedViewSetsAuthorizedViewsConversationsAssessmentsNotesDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_notes_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_notes_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Contactcenterinsights projects locations authorized view sets authorized views conversations assessments notes patch.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudContactcenterinsightsV1Note result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_notes_patch(
        &self,
        args: &ContactcenterinsightsProjectsLocationsAuthorizedViewSetsAuthorizedViewsConversationsAssessmentsNotesPatchArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudContactcenterinsightsV1Note, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_notes_patch_builder(
            &self.http_client,
            &args.name,
            &args.updateMask,
        )
        .map_err(ProviderError::Api)?;

        let task = contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_assessments_notes_patch_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Contactcenterinsights projects locations authorized view sets authorized views conversations feedback labels create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudContactcenterinsightsV1FeedbackLabel result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_feedback_labels_create(
        &self,
        args: &ContactcenterinsightsProjectsLocationsAuthorizedViewSetsAuthorizedViewsConversationsFeedbackLabelsCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudContactcenterinsightsV1FeedbackLabel, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_feedback_labels_create_builder(
            &self.http_client,
            &args.parent,
            &args.feedbackLabelId,
        )
        .map_err(ProviderError::Api)?;

        let task = contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_feedback_labels_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Contactcenterinsights projects locations authorized view sets authorized views conversations feedback labels delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_feedback_labels_delete(
        &self,
        args: &ContactcenterinsightsProjectsLocationsAuthorizedViewSetsAuthorizedViewsConversationsFeedbackLabelsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_feedback_labels_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_feedback_labels_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Contactcenterinsights projects locations authorized view sets authorized views conversations feedback labels patch.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudContactcenterinsightsV1FeedbackLabel result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_feedback_labels_patch(
        &self,
        args: &ContactcenterinsightsProjectsLocationsAuthorizedViewSetsAuthorizedViewsConversationsFeedbackLabelsPatchArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudContactcenterinsightsV1FeedbackLabel, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_feedback_labels_patch_builder(
            &self.http_client,
            &args.name,
            &args.updateMask,
        )
        .map_err(ProviderError::Api)?;

        let task = contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_conversations_feedback_labels_patch_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Contactcenterinsights projects locations authorized view sets authorized views operations cancel.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_operations_cancel(
        &self,
        args: &ContactcenterinsightsProjectsLocationsAuthorizedViewSetsAuthorizedViewsOperationsCancelArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_operations_cancel_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = contactcenterinsights_projects_locations_authorized_view_sets_authorized_views_operations_cancel_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Contactcenterinsights projects locations auto labeling rules create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudContactcenterinsightsV1AutoLabelingRule result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn contactcenterinsights_projects_locations_auto_labeling_rules_create(
        &self,
        args: &ContactcenterinsightsProjectsLocationsAutoLabelingRulesCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudContactcenterinsightsV1AutoLabelingRule, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = contactcenterinsights_projects_locations_auto_labeling_rules_create_builder(
            &self.http_client,
            &args.parent,
            &args.autoLabelingRuleId,
        )
        .map_err(ProviderError::Api)?;

        let task = contactcenterinsights_projects_locations_auto_labeling_rules_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Contactcenterinsights projects locations auto labeling rules delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn contactcenterinsights_projects_locations_auto_labeling_rules_delete(
        &self,
        args: &ContactcenterinsightsProjectsLocationsAutoLabelingRulesDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = contactcenterinsights_projects_locations_auto_labeling_rules_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = contactcenterinsights_projects_locations_auto_labeling_rules_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Contactcenterinsights projects locations auto labeling rules patch.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudContactcenterinsightsV1AutoLabelingRule result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn contactcenterinsights_projects_locations_auto_labeling_rules_patch(
        &self,
        args: &ContactcenterinsightsProjectsLocationsAutoLabelingRulesPatchArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudContactcenterinsightsV1AutoLabelingRule, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = contactcenterinsights_projects_locations_auto_labeling_rules_patch_builder(
            &self.http_client,
            &args.name,
            &args.updateMask,
        )
        .map_err(ProviderError::Api)?;

        let task = contactcenterinsights_projects_locations_auto_labeling_rules_patch_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Contactcenterinsights projects locations auto labeling rules test.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudContactcenterinsightsV1TestAutoLabelingRuleResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn contactcenterinsights_projects_locations_auto_labeling_rules_test(
        &self,
        args: &ContactcenterinsightsProjectsLocationsAutoLabelingRulesTestArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudContactcenterinsightsV1TestAutoLabelingRuleResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = contactcenterinsights_projects_locations_auto_labeling_rules_test_builder(
            &self.http_client,
            &args.parent,
        )
        .map_err(ProviderError::Api)?;

        let task = contactcenterinsights_projects_locations_auto_labeling_rules_test_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Contactcenterinsights projects locations conversations bulk analyze.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn contactcenterinsights_projects_locations_conversations_bulk_analyze(
        &self,
        args: &ContactcenterinsightsProjectsLocationsConversationsBulkAnalyzeArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = contactcenterinsights_projects_locations_conversations_bulk_analyze_builder(
            &self.http_client,
            &args.parent,
        )
        .map_err(ProviderError::Api)?;

        let task = contactcenterinsights_projects_locations_conversations_bulk_analyze_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Contactcenterinsights projects locations conversations bulk delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn contactcenterinsights_projects_locations_conversations_bulk_delete(
        &self,
        args: &ContactcenterinsightsProjectsLocationsConversationsBulkDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = contactcenterinsights_projects_locations_conversations_bulk_delete_builder(
            &self.http_client,
            &args.parent,
        )
        .map_err(ProviderError::Api)?;

        let task = contactcenterinsights_projects_locations_conversations_bulk_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Contactcenterinsights projects locations conversations create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudContactcenterinsightsV1Conversation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn contactcenterinsights_projects_locations_conversations_create(
        &self,
        args: &ContactcenterinsightsProjectsLocationsConversationsCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudContactcenterinsightsV1Conversation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = contactcenterinsights_projects_locations_conversations_create_builder(
            &self.http_client,
            &args.parent,
            &args.conversationId,
        )
        .map_err(ProviderError::Api)?;

        let task = contactcenterinsights_projects_locations_conversations_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Contactcenterinsights projects locations conversations delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn contactcenterinsights_projects_locations_conversations_delete(
        &self,
        args: &ContactcenterinsightsProjectsLocationsConversationsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = contactcenterinsights_projects_locations_conversations_delete_builder(
            &self.http_client,
            &args.name,
            &args.force,
        )
        .map_err(ProviderError::Api)?;

        let task = contactcenterinsights_projects_locations_conversations_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Contactcenterinsights projects locations conversations ingest.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn contactcenterinsights_projects_locations_conversations_ingest(
        &self,
        args: &ContactcenterinsightsProjectsLocationsConversationsIngestArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = contactcenterinsights_projects_locations_conversations_ingest_builder(
            &self.http_client,
            &args.parent,
        )
        .map_err(ProviderError::Api)?;

        let task = contactcenterinsights_projects_locations_conversations_ingest_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Contactcenterinsights projects locations conversations patch.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudContactcenterinsightsV1Conversation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn contactcenterinsights_projects_locations_conversations_patch(
        &self,
        args: &ContactcenterinsightsProjectsLocationsConversationsPatchArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudContactcenterinsightsV1Conversation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = contactcenterinsights_projects_locations_conversations_patch_builder(
            &self.http_client,
            &args.name,
            &args.allowMissing,
            &args.conversationAutoLabelingUpdateConfig.allowAutoLabelingUpdate,
            &args.updateMask,
        )
        .map_err(ProviderError::Api)?;

        let task = contactcenterinsights_projects_locations_conversations_patch_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Contactcenterinsights projects locations conversations sample.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn contactcenterinsights_projects_locations_conversations_sample(
        &self,
        args: &ContactcenterinsightsProjectsLocationsConversationsSampleArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = contactcenterinsights_projects_locations_conversations_sample_builder(
            &self.http_client,
            &args.parent,
        )
        .map_err(ProviderError::Api)?;

        let task = contactcenterinsights_projects_locations_conversations_sample_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Contactcenterinsights projects locations conversations upload.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn contactcenterinsights_projects_locations_conversations_upload(
        &self,
        args: &ContactcenterinsightsProjectsLocationsConversationsUploadArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = contactcenterinsights_projects_locations_conversations_upload_builder(
            &self.http_client,
            &args.parent,
        )
        .map_err(ProviderError::Api)?;

        let task = contactcenterinsights_projects_locations_conversations_upload_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Contactcenterinsights projects locations conversations analyses create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn contactcenterinsights_projects_locations_conversations_analyses_create(
        &self,
        args: &ContactcenterinsightsProjectsLocationsConversationsAnalysesCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = contactcenterinsights_projects_locations_conversations_analyses_create_builder(
            &self.http_client,
            &args.parent,
        )
        .map_err(ProviderError::Api)?;

        let task = contactcenterinsights_projects_locations_conversations_analyses_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Contactcenterinsights projects locations conversations analyses delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn contactcenterinsights_projects_locations_conversations_analyses_delete(
        &self,
        args: &ContactcenterinsightsProjectsLocationsConversationsAnalysesDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = contactcenterinsights_projects_locations_conversations_analyses_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = contactcenterinsights_projects_locations_conversations_analyses_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Contactcenterinsights projects locations conversations assessments appeal.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudContactcenterinsightsV1Assessment result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn contactcenterinsights_projects_locations_conversations_assessments_appeal(
        &self,
        args: &ContactcenterinsightsProjectsLocationsConversationsAssessmentsAppealArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudContactcenterinsightsV1Assessment, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = contactcenterinsights_projects_locations_conversations_assessments_appeal_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = contactcenterinsights_projects_locations_conversations_assessments_appeal_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Contactcenterinsights projects locations conversations assessments create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudContactcenterinsightsV1Assessment result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn contactcenterinsights_projects_locations_conversations_assessments_create(
        &self,
        args: &ContactcenterinsightsProjectsLocationsConversationsAssessmentsCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudContactcenterinsightsV1Assessment, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = contactcenterinsights_projects_locations_conversations_assessments_create_builder(
            &self.http_client,
            &args.parent,
        )
        .map_err(ProviderError::Api)?;

        let task = contactcenterinsights_projects_locations_conversations_assessments_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Contactcenterinsights projects locations conversations assessments delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn contactcenterinsights_projects_locations_conversations_assessments_delete(
        &self,
        args: &ContactcenterinsightsProjectsLocationsConversationsAssessmentsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = contactcenterinsights_projects_locations_conversations_assessments_delete_builder(
            &self.http_client,
            &args.name,
            &args.force,
        )
        .map_err(ProviderError::Api)?;

        let task = contactcenterinsights_projects_locations_conversations_assessments_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Contactcenterinsights projects locations conversations assessments finalize.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudContactcenterinsightsV1Assessment result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn contactcenterinsights_projects_locations_conversations_assessments_finalize(
        &self,
        args: &ContactcenterinsightsProjectsLocationsConversationsAssessmentsFinalizeArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudContactcenterinsightsV1Assessment, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = contactcenterinsights_projects_locations_conversations_assessments_finalize_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = contactcenterinsights_projects_locations_conversations_assessments_finalize_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Contactcenterinsights projects locations conversations assessments publish.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudContactcenterinsightsV1Assessment result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn contactcenterinsights_projects_locations_conversations_assessments_publish(
        &self,
        args: &ContactcenterinsightsProjectsLocationsConversationsAssessmentsPublishArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudContactcenterinsightsV1Assessment, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = contactcenterinsights_projects_locations_conversations_assessments_publish_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = contactcenterinsights_projects_locations_conversations_assessments_publish_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Contactcenterinsights projects locations conversations assessments notes create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudContactcenterinsightsV1Note result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn contactcenterinsights_projects_locations_conversations_assessments_notes_create(
        &self,
        args: &ContactcenterinsightsProjectsLocationsConversationsAssessmentsNotesCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudContactcenterinsightsV1Note, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = contactcenterinsights_projects_locations_conversations_assessments_notes_create_builder(
            &self.http_client,
            &args.parent,
        )
        .map_err(ProviderError::Api)?;

        let task = contactcenterinsights_projects_locations_conversations_assessments_notes_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Contactcenterinsights projects locations conversations assessments notes delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn contactcenterinsights_projects_locations_conversations_assessments_notes_delete(
        &self,
        args: &ContactcenterinsightsProjectsLocationsConversationsAssessmentsNotesDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = contactcenterinsights_projects_locations_conversations_assessments_notes_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = contactcenterinsights_projects_locations_conversations_assessments_notes_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Contactcenterinsights projects locations conversations assessments notes patch.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudContactcenterinsightsV1Note result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn contactcenterinsights_projects_locations_conversations_assessments_notes_patch(
        &self,
        args: &ContactcenterinsightsProjectsLocationsConversationsAssessmentsNotesPatchArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudContactcenterinsightsV1Note, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = contactcenterinsights_projects_locations_conversations_assessments_notes_patch_builder(
            &self.http_client,
            &args.name,
            &args.updateMask,
        )
        .map_err(ProviderError::Api)?;

        let task = contactcenterinsights_projects_locations_conversations_assessments_notes_patch_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Contactcenterinsights projects locations conversations feedback labels create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudContactcenterinsightsV1FeedbackLabel result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn contactcenterinsights_projects_locations_conversations_feedback_labels_create(
        &self,
        args: &ContactcenterinsightsProjectsLocationsConversationsFeedbackLabelsCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudContactcenterinsightsV1FeedbackLabel, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = contactcenterinsights_projects_locations_conversations_feedback_labels_create_builder(
            &self.http_client,
            &args.parent,
            &args.feedbackLabelId,
        )
        .map_err(ProviderError::Api)?;

        let task = contactcenterinsights_projects_locations_conversations_feedback_labels_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Contactcenterinsights projects locations conversations feedback labels delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn contactcenterinsights_projects_locations_conversations_feedback_labels_delete(
        &self,
        args: &ContactcenterinsightsProjectsLocationsConversationsFeedbackLabelsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = contactcenterinsights_projects_locations_conversations_feedback_labels_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = contactcenterinsights_projects_locations_conversations_feedback_labels_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Contactcenterinsights projects locations conversations feedback labels patch.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudContactcenterinsightsV1FeedbackLabel result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn contactcenterinsights_projects_locations_conversations_feedback_labels_patch(
        &self,
        args: &ContactcenterinsightsProjectsLocationsConversationsFeedbackLabelsPatchArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudContactcenterinsightsV1FeedbackLabel, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = contactcenterinsights_projects_locations_conversations_feedback_labels_patch_builder(
            &self.http_client,
            &args.name,
            &args.updateMask,
        )
        .map_err(ProviderError::Api)?;

        let task = contactcenterinsights_projects_locations_conversations_feedback_labels_patch_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Contactcenterinsights projects locations conversations segments bulk analyze.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn contactcenterinsights_projects_locations_conversations_segments_bulk_analyze(
        &self,
        args: &ContactcenterinsightsProjectsLocationsConversationsSegmentsBulkAnalyzeArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = contactcenterinsights_projects_locations_conversations_segments_bulk_analyze_builder(
            &self.http_client,
            &args.parent,
        )
        .map_err(ProviderError::Api)?;

        let task = contactcenterinsights_projects_locations_conversations_segments_bulk_analyze_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Contactcenterinsights projects locations dashboards create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudContactcenterinsightsV1Dashboard result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn contactcenterinsights_projects_locations_dashboards_create(
        &self,
        args: &ContactcenterinsightsProjectsLocationsDashboardsCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudContactcenterinsightsV1Dashboard, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = contactcenterinsights_projects_locations_dashboards_create_builder(
            &self.http_client,
            &args.parent,
            &args.dashboardId,
        )
        .map_err(ProviderError::Api)?;

        let task = contactcenterinsights_projects_locations_dashboards_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Contactcenterinsights projects locations dashboards delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn contactcenterinsights_projects_locations_dashboards_delete(
        &self,
        args: &ContactcenterinsightsProjectsLocationsDashboardsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = contactcenterinsights_projects_locations_dashboards_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = contactcenterinsights_projects_locations_dashboards_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Contactcenterinsights projects locations dashboards patch.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudContactcenterinsightsV1Dashboard result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn contactcenterinsights_projects_locations_dashboards_patch(
        &self,
        args: &ContactcenterinsightsProjectsLocationsDashboardsPatchArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudContactcenterinsightsV1Dashboard, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = contactcenterinsights_projects_locations_dashboards_patch_builder(
            &self.http_client,
            &args.name,
            &args.updateMask,
        )
        .map_err(ProviderError::Api)?;

        let task = contactcenterinsights_projects_locations_dashboards_patch_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Contactcenterinsights projects locations dashboards charts create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudContactcenterinsightsV1Chart result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn contactcenterinsights_projects_locations_dashboards_charts_create(
        &self,
        args: &ContactcenterinsightsProjectsLocationsDashboardsChartsCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudContactcenterinsightsV1Chart, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = contactcenterinsights_projects_locations_dashboards_charts_create_builder(
            &self.http_client,
            &args.parent,
            &args.chartId,
        )
        .map_err(ProviderError::Api)?;

        let task = contactcenterinsights_projects_locations_dashboards_charts_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Contactcenterinsights projects locations dashboards charts delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn contactcenterinsights_projects_locations_dashboards_charts_delete(
        &self,
        args: &ContactcenterinsightsProjectsLocationsDashboardsChartsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = contactcenterinsights_projects_locations_dashboards_charts_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = contactcenterinsights_projects_locations_dashboards_charts_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Contactcenterinsights projects locations dashboards charts patch.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudContactcenterinsightsV1Chart result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn contactcenterinsights_projects_locations_dashboards_charts_patch(
        &self,
        args: &ContactcenterinsightsProjectsLocationsDashboardsChartsPatchArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudContactcenterinsightsV1Chart, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = contactcenterinsights_projects_locations_dashboards_charts_patch_builder(
            &self.http_client,
            &args.name,
            &args.updateMask,
        )
        .map_err(ProviderError::Api)?;

        let task = contactcenterinsights_projects_locations_dashboards_charts_patch_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Contactcenterinsights projects locations datasets bulk delete feedback labels.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn contactcenterinsights_projects_locations_datasets_bulk_delete_feedback_labels(
        &self,
        args: &ContactcenterinsightsProjectsLocationsDatasetsBulkDeleteFeedbackLabelsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = contactcenterinsights_projects_locations_datasets_bulk_delete_feedback_labels_builder(
            &self.http_client,
            &args.parent,
        )
        .map_err(ProviderError::Api)?;

        let task = contactcenterinsights_projects_locations_datasets_bulk_delete_feedback_labels_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Contactcenterinsights projects locations datasets bulk download feedback labels.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn contactcenterinsights_projects_locations_datasets_bulk_download_feedback_labels(
        &self,
        args: &ContactcenterinsightsProjectsLocationsDatasetsBulkDownloadFeedbackLabelsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = contactcenterinsights_projects_locations_datasets_bulk_download_feedback_labels_builder(
            &self.http_client,
            &args.parent,
        )
        .map_err(ProviderError::Api)?;

        let task = contactcenterinsights_projects_locations_datasets_bulk_download_feedback_labels_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Contactcenterinsights projects locations datasets bulk upload feedback labels.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn contactcenterinsights_projects_locations_datasets_bulk_upload_feedback_labels(
        &self,
        args: &ContactcenterinsightsProjectsLocationsDatasetsBulkUploadFeedbackLabelsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = contactcenterinsights_projects_locations_datasets_bulk_upload_feedback_labels_builder(
            &self.http_client,
            &args.parent,
        )
        .map_err(ProviderError::Api)?;

        let task = contactcenterinsights_projects_locations_datasets_bulk_upload_feedback_labels_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Contactcenterinsights projects locations datasets create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudContactcenterinsightsV1Dataset result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn contactcenterinsights_projects_locations_datasets_create(
        &self,
        args: &ContactcenterinsightsProjectsLocationsDatasetsCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudContactcenterinsightsV1Dataset, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = contactcenterinsights_projects_locations_datasets_create_builder(
            &self.http_client,
            &args.parent,
            &args.datasetId,
        )
        .map_err(ProviderError::Api)?;

        let task = contactcenterinsights_projects_locations_datasets_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Contactcenterinsights projects locations datasets delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn contactcenterinsights_projects_locations_datasets_delete(
        &self,
        args: &ContactcenterinsightsProjectsLocationsDatasetsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = contactcenterinsights_projects_locations_datasets_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = contactcenterinsights_projects_locations_datasets_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Contactcenterinsights projects locations datasets patch.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudContactcenterinsightsV1Dataset result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn contactcenterinsights_projects_locations_datasets_patch(
        &self,
        args: &ContactcenterinsightsProjectsLocationsDatasetsPatchArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudContactcenterinsightsV1Dataset, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = contactcenterinsights_projects_locations_datasets_patch_builder(
            &self.http_client,
            &args.name,
            &args.updateMask,
        )
        .map_err(ProviderError::Api)?;

        let task = contactcenterinsights_projects_locations_datasets_patch_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Contactcenterinsights projects locations datasets conversations bulk delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn contactcenterinsights_projects_locations_datasets_conversations_bulk_delete(
        &self,
        args: &ContactcenterinsightsProjectsLocationsDatasetsConversationsBulkDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = contactcenterinsights_projects_locations_datasets_conversations_bulk_delete_builder(
            &self.http_client,
            &args.parent,
        )
        .map_err(ProviderError::Api)?;

        let task = contactcenterinsights_projects_locations_datasets_conversations_bulk_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Contactcenterinsights projects locations datasets conversations calculate stats.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudContactcenterinsightsV1CalculateStatsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn contactcenterinsights_projects_locations_datasets_conversations_calculate_stats(
        &self,
        args: &ContactcenterinsightsProjectsLocationsDatasetsConversationsCalculateStatsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudContactcenterinsightsV1CalculateStatsResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = contactcenterinsights_projects_locations_datasets_conversations_calculate_stats_builder(
            &self.http_client,
            &args.location,
        )
        .map_err(ProviderError::Api)?;

        let task = contactcenterinsights_projects_locations_datasets_conversations_calculate_stats_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Contactcenterinsights projects locations datasets conversations delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn contactcenterinsights_projects_locations_datasets_conversations_delete(
        &self,
        args: &ContactcenterinsightsProjectsLocationsDatasetsConversationsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = contactcenterinsights_projects_locations_datasets_conversations_delete_builder(
            &self.http_client,
            &args.name,
            &args.force,
        )
        .map_err(ProviderError::Api)?;

        let task = contactcenterinsights_projects_locations_datasets_conversations_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Contactcenterinsights projects locations datasets conversations ingest.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn contactcenterinsights_projects_locations_datasets_conversations_ingest(
        &self,
        args: &ContactcenterinsightsProjectsLocationsDatasetsConversationsIngestArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = contactcenterinsights_projects_locations_datasets_conversations_ingest_builder(
            &self.http_client,
            &args.parent,
        )
        .map_err(ProviderError::Api)?;

        let task = contactcenterinsights_projects_locations_datasets_conversations_ingest_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Contactcenterinsights projects locations datasets conversations sample.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn contactcenterinsights_projects_locations_datasets_conversations_sample(
        &self,
        args: &ContactcenterinsightsProjectsLocationsDatasetsConversationsSampleArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = contactcenterinsights_projects_locations_datasets_conversations_sample_builder(
            &self.http_client,
            &args.parent,
        )
        .map_err(ProviderError::Api)?;

        let task = contactcenterinsights_projects_locations_datasets_conversations_sample_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Contactcenterinsights projects locations datasets conversations feedback labels create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudContactcenterinsightsV1FeedbackLabel result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn contactcenterinsights_projects_locations_datasets_conversations_feedback_labels_create(
        &self,
        args: &ContactcenterinsightsProjectsLocationsDatasetsConversationsFeedbackLabelsCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudContactcenterinsightsV1FeedbackLabel, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = contactcenterinsights_projects_locations_datasets_conversations_feedback_labels_create_builder(
            &self.http_client,
            &args.parent,
            &args.feedbackLabelId,
        )
        .map_err(ProviderError::Api)?;

        let task = contactcenterinsights_projects_locations_datasets_conversations_feedback_labels_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Contactcenterinsights projects locations datasets conversations feedback labels delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn contactcenterinsights_projects_locations_datasets_conversations_feedback_labels_delete(
        &self,
        args: &ContactcenterinsightsProjectsLocationsDatasetsConversationsFeedbackLabelsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = contactcenterinsights_projects_locations_datasets_conversations_feedback_labels_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = contactcenterinsights_projects_locations_datasets_conversations_feedback_labels_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Contactcenterinsights projects locations datasets conversations feedback labels patch.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudContactcenterinsightsV1FeedbackLabel result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn contactcenterinsights_projects_locations_datasets_conversations_feedback_labels_patch(
        &self,
        args: &ContactcenterinsightsProjectsLocationsDatasetsConversationsFeedbackLabelsPatchArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudContactcenterinsightsV1FeedbackLabel, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = contactcenterinsights_projects_locations_datasets_conversations_feedback_labels_patch_builder(
            &self.http_client,
            &args.name,
            &args.updateMask,
        )
        .map_err(ProviderError::Api)?;

        let task = contactcenterinsights_projects_locations_datasets_conversations_feedback_labels_patch_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Contactcenterinsights projects locations datasets insightsdata export.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn contactcenterinsights_projects_locations_datasets_insightsdata_export(
        &self,
        args: &ContactcenterinsightsProjectsLocationsDatasetsInsightsdataExportArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = contactcenterinsights_projects_locations_datasets_insightsdata_export_builder(
            &self.http_client,
            &args.parent,
        )
        .map_err(ProviderError::Api)?;

        let task = contactcenterinsights_projects_locations_datasets_insightsdata_export_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Contactcenterinsights projects locations encryption spec initialize.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn contactcenterinsights_projects_locations_encryption_spec_initialize(
        &self,
        args: &ContactcenterinsightsProjectsLocationsEncryptionSpecInitializeArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = contactcenterinsights_projects_locations_encryption_spec_initialize_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = contactcenterinsights_projects_locations_encryption_spec_initialize_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Contactcenterinsights projects locations insightsdata export.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn contactcenterinsights_projects_locations_insightsdata_export(
        &self,
        args: &ContactcenterinsightsProjectsLocationsInsightsdataExportArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = contactcenterinsights_projects_locations_insightsdata_export_builder(
            &self.http_client,
            &args.parent,
        )
        .map_err(ProviderError::Api)?;

        let task = contactcenterinsights_projects_locations_insightsdata_export_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Contactcenterinsights projects locations issue models create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn contactcenterinsights_projects_locations_issue_models_create(
        &self,
        args: &ContactcenterinsightsProjectsLocationsIssueModelsCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = contactcenterinsights_projects_locations_issue_models_create_builder(
            &self.http_client,
            &args.parent,
        )
        .map_err(ProviderError::Api)?;

        let task = contactcenterinsights_projects_locations_issue_models_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Contactcenterinsights projects locations issue models delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn contactcenterinsights_projects_locations_issue_models_delete(
        &self,
        args: &ContactcenterinsightsProjectsLocationsIssueModelsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = contactcenterinsights_projects_locations_issue_models_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = contactcenterinsights_projects_locations_issue_models_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Contactcenterinsights projects locations issue models deploy.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn contactcenterinsights_projects_locations_issue_models_deploy(
        &self,
        args: &ContactcenterinsightsProjectsLocationsIssueModelsDeployArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = contactcenterinsights_projects_locations_issue_models_deploy_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = contactcenterinsights_projects_locations_issue_models_deploy_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Contactcenterinsights projects locations issue models export.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn contactcenterinsights_projects_locations_issue_models_export(
        &self,
        args: &ContactcenterinsightsProjectsLocationsIssueModelsExportArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = contactcenterinsights_projects_locations_issue_models_export_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = contactcenterinsights_projects_locations_issue_models_export_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Contactcenterinsights projects locations issue models import.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn contactcenterinsights_projects_locations_issue_models_import(
        &self,
        args: &ContactcenterinsightsProjectsLocationsIssueModelsImportArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = contactcenterinsights_projects_locations_issue_models_import_builder(
            &self.http_client,
            &args.parent,
        )
        .map_err(ProviderError::Api)?;

        let task = contactcenterinsights_projects_locations_issue_models_import_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Contactcenterinsights projects locations issue models patch.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudContactcenterinsightsV1IssueModel result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn contactcenterinsights_projects_locations_issue_models_patch(
        &self,
        args: &ContactcenterinsightsProjectsLocationsIssueModelsPatchArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudContactcenterinsightsV1IssueModel, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = contactcenterinsights_projects_locations_issue_models_patch_builder(
            &self.http_client,
            &args.name,
            &args.updateMask,
        )
        .map_err(ProviderError::Api)?;

        let task = contactcenterinsights_projects_locations_issue_models_patch_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Contactcenterinsights projects locations issue models undeploy.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn contactcenterinsights_projects_locations_issue_models_undeploy(
        &self,
        args: &ContactcenterinsightsProjectsLocationsIssueModelsUndeployArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = contactcenterinsights_projects_locations_issue_models_undeploy_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = contactcenterinsights_projects_locations_issue_models_undeploy_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Contactcenterinsights projects locations issue models issues create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn contactcenterinsights_projects_locations_issue_models_issues_create(
        &self,
        args: &ContactcenterinsightsProjectsLocationsIssueModelsIssuesCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = contactcenterinsights_projects_locations_issue_models_issues_create_builder(
            &self.http_client,
            &args.parent,
        )
        .map_err(ProviderError::Api)?;

        let task = contactcenterinsights_projects_locations_issue_models_issues_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Contactcenterinsights projects locations issue models issues delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn contactcenterinsights_projects_locations_issue_models_issues_delete(
        &self,
        args: &ContactcenterinsightsProjectsLocationsIssueModelsIssuesDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = contactcenterinsights_projects_locations_issue_models_issues_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = contactcenterinsights_projects_locations_issue_models_issues_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Contactcenterinsights projects locations issue models issues patch.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudContactcenterinsightsV1Issue result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn contactcenterinsights_projects_locations_issue_models_issues_patch(
        &self,
        args: &ContactcenterinsightsProjectsLocationsIssueModelsIssuesPatchArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudContactcenterinsightsV1Issue, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = contactcenterinsights_projects_locations_issue_models_issues_patch_builder(
            &self.http_client,
            &args.name,
            &args.updateMask,
        )
        .map_err(ProviderError::Api)?;

        let task = contactcenterinsights_projects_locations_issue_models_issues_patch_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Contactcenterinsights projects locations operations cancel.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn contactcenterinsights_projects_locations_operations_cancel(
        &self,
        args: &ContactcenterinsightsProjectsLocationsOperationsCancelArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = contactcenterinsights_projects_locations_operations_cancel_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = contactcenterinsights_projects_locations_operations_cancel_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Contactcenterinsights projects locations phrase matchers create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudContactcenterinsightsV1PhraseMatcher result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn contactcenterinsights_projects_locations_phrase_matchers_create(
        &self,
        args: &ContactcenterinsightsProjectsLocationsPhraseMatchersCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudContactcenterinsightsV1PhraseMatcher, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = contactcenterinsights_projects_locations_phrase_matchers_create_builder(
            &self.http_client,
            &args.parent,
        )
        .map_err(ProviderError::Api)?;

        let task = contactcenterinsights_projects_locations_phrase_matchers_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Contactcenterinsights projects locations phrase matchers delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn contactcenterinsights_projects_locations_phrase_matchers_delete(
        &self,
        args: &ContactcenterinsightsProjectsLocationsPhraseMatchersDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = contactcenterinsights_projects_locations_phrase_matchers_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = contactcenterinsights_projects_locations_phrase_matchers_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Contactcenterinsights projects locations phrase matchers patch.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudContactcenterinsightsV1PhraseMatcher result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn contactcenterinsights_projects_locations_phrase_matchers_patch(
        &self,
        args: &ContactcenterinsightsProjectsLocationsPhraseMatchersPatchArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudContactcenterinsightsV1PhraseMatcher, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = contactcenterinsights_projects_locations_phrase_matchers_patch_builder(
            &self.http_client,
            &args.name,
            &args.updateMask,
        )
        .map_err(ProviderError::Api)?;

        let task = contactcenterinsights_projects_locations_phrase_matchers_patch_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Contactcenterinsights projects locations qa question tags create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudContactcenterinsightsV1QaQuestionTag result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn contactcenterinsights_projects_locations_qa_question_tags_create(
        &self,
        args: &ContactcenterinsightsProjectsLocationsQaQuestionTagsCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudContactcenterinsightsV1QaQuestionTag, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = contactcenterinsights_projects_locations_qa_question_tags_create_builder(
            &self.http_client,
            &args.parent,
            &args.qaQuestionTagId,
        )
        .map_err(ProviderError::Api)?;

        let task = contactcenterinsights_projects_locations_qa_question_tags_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Contactcenterinsights projects locations qa question tags delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn contactcenterinsights_projects_locations_qa_question_tags_delete(
        &self,
        args: &ContactcenterinsightsProjectsLocationsQaQuestionTagsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = contactcenterinsights_projects_locations_qa_question_tags_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = contactcenterinsights_projects_locations_qa_question_tags_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Contactcenterinsights projects locations qa question tags patch.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn contactcenterinsights_projects_locations_qa_question_tags_patch(
        &self,
        args: &ContactcenterinsightsProjectsLocationsQaQuestionTagsPatchArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = contactcenterinsights_projects_locations_qa_question_tags_patch_builder(
            &self.http_client,
            &args.name,
            &args.updateMask,
        )
        .map_err(ProviderError::Api)?;

        let task = contactcenterinsights_projects_locations_qa_question_tags_patch_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Contactcenterinsights projects locations qa scorecards create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudContactcenterinsightsV1QaScorecard result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn contactcenterinsights_projects_locations_qa_scorecards_create(
        &self,
        args: &ContactcenterinsightsProjectsLocationsQaScorecardsCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudContactcenterinsightsV1QaScorecard, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = contactcenterinsights_projects_locations_qa_scorecards_create_builder(
            &self.http_client,
            &args.parent,
            &args.qaScorecardId,
        )
        .map_err(ProviderError::Api)?;

        let task = contactcenterinsights_projects_locations_qa_scorecards_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Contactcenterinsights projects locations qa scorecards delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn contactcenterinsights_projects_locations_qa_scorecards_delete(
        &self,
        args: &ContactcenterinsightsProjectsLocationsQaScorecardsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = contactcenterinsights_projects_locations_qa_scorecards_delete_builder(
            &self.http_client,
            &args.name,
            &args.force,
        )
        .map_err(ProviderError::Api)?;

        let task = contactcenterinsights_projects_locations_qa_scorecards_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Contactcenterinsights projects locations qa scorecards patch.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudContactcenterinsightsV1QaScorecard result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn contactcenterinsights_projects_locations_qa_scorecards_patch(
        &self,
        args: &ContactcenterinsightsProjectsLocationsQaScorecardsPatchArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudContactcenterinsightsV1QaScorecard, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = contactcenterinsights_projects_locations_qa_scorecards_patch_builder(
            &self.http_client,
            &args.name,
            &args.updateMask,
        )
        .map_err(ProviderError::Api)?;

        let task = contactcenterinsights_projects_locations_qa_scorecards_patch_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Contactcenterinsights projects locations qa scorecards revisions create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudContactcenterinsightsV1QaScorecardRevision result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn contactcenterinsights_projects_locations_qa_scorecards_revisions_create(
        &self,
        args: &ContactcenterinsightsProjectsLocationsQaScorecardsRevisionsCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudContactcenterinsightsV1QaScorecardRevision, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = contactcenterinsights_projects_locations_qa_scorecards_revisions_create_builder(
            &self.http_client,
            &args.parent,
            &args.qaScorecardRevisionId,
        )
        .map_err(ProviderError::Api)?;

        let task = contactcenterinsights_projects_locations_qa_scorecards_revisions_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Contactcenterinsights projects locations qa scorecards revisions delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn contactcenterinsights_projects_locations_qa_scorecards_revisions_delete(
        &self,
        args: &ContactcenterinsightsProjectsLocationsQaScorecardsRevisionsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = contactcenterinsights_projects_locations_qa_scorecards_revisions_delete_builder(
            &self.http_client,
            &args.name,
            &args.force,
        )
        .map_err(ProviderError::Api)?;

        let task = contactcenterinsights_projects_locations_qa_scorecards_revisions_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Contactcenterinsights projects locations qa scorecards revisions deploy.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudContactcenterinsightsV1QaScorecardRevision result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn contactcenterinsights_projects_locations_qa_scorecards_revisions_deploy(
        &self,
        args: &ContactcenterinsightsProjectsLocationsQaScorecardsRevisionsDeployArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudContactcenterinsightsV1QaScorecardRevision, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = contactcenterinsights_projects_locations_qa_scorecards_revisions_deploy_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = contactcenterinsights_projects_locations_qa_scorecards_revisions_deploy_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Contactcenterinsights projects locations qa scorecards revisions tune qa scorecard revision.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn contactcenterinsights_projects_locations_qa_scorecards_revisions_tune_qa_scorecard_revision(
        &self,
        args: &ContactcenterinsightsProjectsLocationsQaScorecardsRevisionsTuneQaScorecardRevisionArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = contactcenterinsights_projects_locations_qa_scorecards_revisions_tune_qa_scorecard_revision_builder(
            &self.http_client,
            &args.parent,
        )
        .map_err(ProviderError::Api)?;

        let task = contactcenterinsights_projects_locations_qa_scorecards_revisions_tune_qa_scorecard_revision_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Contactcenterinsights projects locations qa scorecards revisions undeploy.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudContactcenterinsightsV1QaScorecardRevision result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn contactcenterinsights_projects_locations_qa_scorecards_revisions_undeploy(
        &self,
        args: &ContactcenterinsightsProjectsLocationsQaScorecardsRevisionsUndeployArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudContactcenterinsightsV1QaScorecardRevision, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = contactcenterinsights_projects_locations_qa_scorecards_revisions_undeploy_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = contactcenterinsights_projects_locations_qa_scorecards_revisions_undeploy_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Contactcenterinsights projects locations qa scorecards revisions qa questions create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudContactcenterinsightsV1QaQuestion result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn contactcenterinsights_projects_locations_qa_scorecards_revisions_qa_questions_create(
        &self,
        args: &ContactcenterinsightsProjectsLocationsQaScorecardsRevisionsQaQuestionsCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudContactcenterinsightsV1QaQuestion, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = contactcenterinsights_projects_locations_qa_scorecards_revisions_qa_questions_create_builder(
            &self.http_client,
            &args.parent,
            &args.qaQuestionId,
        )
        .map_err(ProviderError::Api)?;

        let task = contactcenterinsights_projects_locations_qa_scorecards_revisions_qa_questions_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Contactcenterinsights projects locations qa scorecards revisions qa questions delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn contactcenterinsights_projects_locations_qa_scorecards_revisions_qa_questions_delete(
        &self,
        args: &ContactcenterinsightsProjectsLocationsQaScorecardsRevisionsQaQuestionsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = contactcenterinsights_projects_locations_qa_scorecards_revisions_qa_questions_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = contactcenterinsights_projects_locations_qa_scorecards_revisions_qa_questions_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Contactcenterinsights projects locations qa scorecards revisions qa questions patch.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudContactcenterinsightsV1QaQuestion result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn contactcenterinsights_projects_locations_qa_scorecards_revisions_qa_questions_patch(
        &self,
        args: &ContactcenterinsightsProjectsLocationsQaScorecardsRevisionsQaQuestionsPatchArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudContactcenterinsightsV1QaQuestion, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = contactcenterinsights_projects_locations_qa_scorecards_revisions_qa_questions_patch_builder(
            &self.http_client,
            &args.name,
            &args.updateMask,
        )
        .map_err(ProviderError::Api)?;

        let task = contactcenterinsights_projects_locations_qa_scorecards_revisions_qa_questions_patch_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Contactcenterinsights projects locations views create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudContactcenterinsightsV1View result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn contactcenterinsights_projects_locations_views_create(
        &self,
        args: &ContactcenterinsightsProjectsLocationsViewsCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudContactcenterinsightsV1View, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = contactcenterinsights_projects_locations_views_create_builder(
            &self.http_client,
            &args.parent,
        )
        .map_err(ProviderError::Api)?;

        let task = contactcenterinsights_projects_locations_views_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Contactcenterinsights projects locations views delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn contactcenterinsights_projects_locations_views_delete(
        &self,
        args: &ContactcenterinsightsProjectsLocationsViewsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = contactcenterinsights_projects_locations_views_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = contactcenterinsights_projects_locations_views_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Contactcenterinsights projects locations views patch.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudContactcenterinsightsV1View result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn contactcenterinsights_projects_locations_views_patch(
        &self,
        args: &ContactcenterinsightsProjectsLocationsViewsPatchArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudContactcenterinsightsV1View, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = contactcenterinsights_projects_locations_views_patch_builder(
            &self.http_client,
            &args.name,
            &args.updateMask,
        )
        .map_err(ProviderError::Api)?;

        let task = contactcenterinsights_projects_locations_views_patch_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

}
