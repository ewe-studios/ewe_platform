//! ApigeeProvider - State-aware apigee API client.
//!
//! WHY: Users need state-aware API clients that automatically track
//!      resource changes in the state store.
//!
//! WHAT: Provider wrapping ProviderClient<S> with methods for
//!       apigee API endpoints that auto-store results.
//!
//! HOW: Each method wraps the task with StoreStateIdentifierTask
//!      for automatic state persistence on success.

#![cfg(feature = "gcp")]

use crate::providers::gcp::clients::apigee::{
    apigee_hybrid_issuers_list_builder, apigee_hybrid_issuers_list_task,
    apigee_organizations_create_builder, apigee_organizations_create_task,
    apigee_organizations_delete_builder, apigee_organizations_delete_task,
    apigee_organizations_get_builder, apigee_organizations_get_task,
    apigee_organizations_get_control_plane_access_builder, apigee_organizations_get_control_plane_access_task,
    apigee_organizations_get_deployed_ingress_config_builder, apigee_organizations_get_deployed_ingress_config_task,
    apigee_organizations_get_project_mapping_builder, apigee_organizations_get_project_mapping_task,
    apigee_organizations_get_runtime_config_builder, apigee_organizations_get_runtime_config_task,
    apigee_organizations_get_security_settings_builder, apigee_organizations_get_security_settings_task,
    apigee_organizations_get_sync_authorization_builder, apigee_organizations_get_sync_authorization_task,
    apigee_organizations_list_builder, apigee_organizations_list_task,
    apigee_organizations_set_addons_builder, apigee_organizations_set_addons_task,
    apigee_organizations_set_sync_authorization_builder, apigee_organizations_set_sync_authorization_task,
    apigee_organizations_update_builder, apigee_organizations_update_task,
    apigee_organizations_update_control_plane_access_builder, apigee_organizations_update_control_plane_access_task,
    apigee_organizations_update_security_settings_builder, apigee_organizations_update_security_settings_task,
    apigee_organizations_analytics_datastores_create_builder, apigee_organizations_analytics_datastores_create_task,
    apigee_organizations_analytics_datastores_delete_builder, apigee_organizations_analytics_datastores_delete_task,
    apigee_organizations_analytics_datastores_get_builder, apigee_organizations_analytics_datastores_get_task,
    apigee_organizations_analytics_datastores_list_builder, apigee_organizations_analytics_datastores_list_task,
    apigee_organizations_analytics_datastores_test_builder, apigee_organizations_analytics_datastores_test_task,
    apigee_organizations_analytics_datastores_update_builder, apigee_organizations_analytics_datastores_update_task,
    apigee_organizations_apim_service_extensions_create_builder, apigee_organizations_apim_service_extensions_create_task,
    apigee_organizations_apim_service_extensions_delete_builder, apigee_organizations_apim_service_extensions_delete_task,
    apigee_organizations_apim_service_extensions_get_builder, apigee_organizations_apim_service_extensions_get_task,
    apigee_organizations_apim_service_extensions_list_builder, apigee_organizations_apim_service_extensions_list_task,
    apigee_organizations_apim_service_extensions_patch_builder, apigee_organizations_apim_service_extensions_patch_task,
    apigee_organizations_apiproducts_attributes_builder, apigee_organizations_apiproducts_attributes_task,
    apigee_organizations_apiproducts_create_builder, apigee_organizations_apiproducts_create_task,
    apigee_organizations_apiproducts_delete_builder, apigee_organizations_apiproducts_delete_task,
    apigee_organizations_apiproducts_get_builder, apigee_organizations_apiproducts_get_task,
    apigee_organizations_apiproducts_list_builder, apigee_organizations_apiproducts_list_task,
    apigee_organizations_apiproducts_move_builder, apigee_organizations_apiproducts_move_task,
    apigee_organizations_apiproducts_update_builder, apigee_organizations_apiproducts_update_task,
    apigee_organizations_apiproducts_attributes_delete_builder, apigee_organizations_apiproducts_attributes_delete_task,
    apigee_organizations_apiproducts_attributes_get_builder, apigee_organizations_apiproducts_attributes_get_task,
    apigee_organizations_apiproducts_attributes_list_builder, apigee_organizations_apiproducts_attributes_list_task,
    apigee_organizations_apiproducts_attributes_update_api_product_attribute_builder, apigee_organizations_apiproducts_attributes_update_api_product_attribute_task,
    apigee_organizations_apiproducts_rateplans_create_builder, apigee_organizations_apiproducts_rateplans_create_task,
    apigee_organizations_apiproducts_rateplans_delete_builder, apigee_organizations_apiproducts_rateplans_delete_task,
    apigee_organizations_apiproducts_rateplans_get_builder, apigee_organizations_apiproducts_rateplans_get_task,
    apigee_organizations_apiproducts_rateplans_list_builder, apigee_organizations_apiproducts_rateplans_list_task,
    apigee_organizations_apiproducts_rateplans_update_builder, apigee_organizations_apiproducts_rateplans_update_task,
    apigee_organizations_apis_create_builder, apigee_organizations_apis_create_task,
    apigee_organizations_apis_delete_builder, apigee_organizations_apis_delete_task,
    apigee_organizations_apis_get_builder, apigee_organizations_apis_get_task,
    apigee_organizations_apis_list_builder, apigee_organizations_apis_list_task,
    apigee_organizations_apis_move_builder, apigee_organizations_apis_move_task,
    apigee_organizations_apis_patch_builder, apigee_organizations_apis_patch_task,
    apigee_organizations_apis_debugsessions_list_builder, apigee_organizations_apis_debugsessions_list_task,
    apigee_organizations_apis_deployments_list_builder, apigee_organizations_apis_deployments_list_task,
    apigee_organizations_apis_keyvaluemaps_create_builder, apigee_organizations_apis_keyvaluemaps_create_task,
    apigee_organizations_apis_keyvaluemaps_delete_builder, apigee_organizations_apis_keyvaluemaps_delete_task,
    apigee_organizations_apis_keyvaluemaps_get_builder, apigee_organizations_apis_keyvaluemaps_get_task,
    apigee_organizations_apis_keyvaluemaps_update_builder, apigee_organizations_apis_keyvaluemaps_update_task,
    apigee_organizations_apis_keyvaluemaps_entries_create_builder, apigee_organizations_apis_keyvaluemaps_entries_create_task,
    apigee_organizations_apis_keyvaluemaps_entries_delete_builder, apigee_organizations_apis_keyvaluemaps_entries_delete_task,
    apigee_organizations_apis_keyvaluemaps_entries_get_builder, apigee_organizations_apis_keyvaluemaps_entries_get_task,
    apigee_organizations_apis_keyvaluemaps_entries_list_builder, apigee_organizations_apis_keyvaluemaps_entries_list_task,
    apigee_organizations_apis_keyvaluemaps_entries_update_builder, apigee_organizations_apis_keyvaluemaps_entries_update_task,
    apigee_organizations_apis_revisions_delete_builder, apigee_organizations_apis_revisions_delete_task,
    apigee_organizations_apis_revisions_get_builder, apigee_organizations_apis_revisions_get_task,
    apigee_organizations_apis_revisions_update_api_proxy_revision_builder, apigee_organizations_apis_revisions_update_api_proxy_revision_task,
    apigee_organizations_apis_revisions_deployments_list_builder, apigee_organizations_apis_revisions_deployments_list_task,
    apigee_organizations_appgroups_create_builder, apigee_organizations_appgroups_create_task,
    apigee_organizations_appgroups_delete_builder, apigee_organizations_appgroups_delete_task,
    apigee_organizations_appgroups_get_builder, apigee_organizations_appgroups_get_task,
    apigee_organizations_appgroups_get_balance_builder, apigee_organizations_appgroups_get_balance_task,
    apigee_organizations_appgroups_get_monetization_config_builder, apigee_organizations_appgroups_get_monetization_config_task,
    apigee_organizations_appgroups_list_builder, apigee_organizations_appgroups_list_task,
    apigee_organizations_appgroups_update_builder, apigee_organizations_appgroups_update_task,
    apigee_organizations_appgroups_update_monetization_config_builder, apigee_organizations_appgroups_update_monetization_config_task,
    apigee_organizations_appgroups_apps_create_builder, apigee_organizations_appgroups_apps_create_task,
    apigee_organizations_appgroups_apps_delete_builder, apigee_organizations_appgroups_apps_delete_task,
    apigee_organizations_appgroups_apps_get_builder, apigee_organizations_appgroups_apps_get_task,
    apigee_organizations_appgroups_apps_list_builder, apigee_organizations_appgroups_apps_list_task,
    apigee_organizations_appgroups_apps_update_builder, apigee_organizations_appgroups_apps_update_task,
    apigee_organizations_appgroups_apps_keys_create_builder, apigee_organizations_appgroups_apps_keys_create_task,
    apigee_organizations_appgroups_apps_keys_delete_builder, apigee_organizations_appgroups_apps_keys_delete_task,
    apigee_organizations_appgroups_apps_keys_get_builder, apigee_organizations_appgroups_apps_keys_get_task,
    apigee_organizations_appgroups_apps_keys_update_app_group_app_key_builder, apigee_organizations_appgroups_apps_keys_update_app_group_app_key_task,
    apigee_organizations_appgroups_apps_keys_apiproducts_delete_builder, apigee_organizations_appgroups_apps_keys_apiproducts_delete_task,
    apigee_organizations_appgroups_apps_keys_apiproducts_update_app_group_app_key_api_product_builder, apigee_organizations_appgroups_apps_keys_apiproducts_update_app_group_app_key_api_product_task,
    apigee_organizations_appgroups_balance_adjust_builder, apigee_organizations_appgroups_balance_adjust_task,
    apigee_organizations_appgroups_balance_credit_builder, apigee_organizations_appgroups_balance_credit_task,
    apigee_organizations_appgroups_subscriptions_create_builder, apigee_organizations_appgroups_subscriptions_create_task,
    apigee_organizations_appgroups_subscriptions_expire_builder, apigee_organizations_appgroups_subscriptions_expire_task,
    apigee_organizations_appgroups_subscriptions_get_builder, apigee_organizations_appgroups_subscriptions_get_task,
    apigee_organizations_appgroups_subscriptions_list_builder, apigee_organizations_appgroups_subscriptions_list_task,
    apigee_organizations_apps_get_builder, apigee_organizations_apps_get_task,
    apigee_organizations_apps_list_builder, apigee_organizations_apps_list_task,
    apigee_organizations_datacollectors_create_builder, apigee_organizations_datacollectors_create_task,
    apigee_organizations_datacollectors_delete_builder, apigee_organizations_datacollectors_delete_task,
    apigee_organizations_datacollectors_get_builder, apigee_organizations_datacollectors_get_task,
    apigee_organizations_datacollectors_list_builder, apigee_organizations_datacollectors_list_task,
    apigee_organizations_datacollectors_patch_builder, apigee_organizations_datacollectors_patch_task,
    apigee_organizations_deployments_list_builder, apigee_organizations_deployments_list_task,
    apigee_organizations_developers_attributes_builder, apigee_organizations_developers_attributes_task,
    apigee_organizations_developers_create_builder, apigee_organizations_developers_create_task,
    apigee_organizations_developers_delete_builder, apigee_organizations_developers_delete_task,
    apigee_organizations_developers_get_builder, apigee_organizations_developers_get_task,
    apigee_organizations_developers_get_balance_builder, apigee_organizations_developers_get_balance_task,
    apigee_organizations_developers_get_monetization_config_builder, apigee_organizations_developers_get_monetization_config_task,
    apigee_organizations_developers_list_builder, apigee_organizations_developers_list_task,
    apigee_organizations_developers_set_developer_status_builder, apigee_organizations_developers_set_developer_status_task,
    apigee_organizations_developers_update_builder, apigee_organizations_developers_update_task,
    apigee_organizations_developers_update_monetization_config_builder, apigee_organizations_developers_update_monetization_config_task,
    apigee_organizations_developers_apps_attributes_builder, apigee_organizations_developers_apps_attributes_task,
    apigee_organizations_developers_apps_create_builder, apigee_organizations_developers_apps_create_task,
    apigee_organizations_developers_apps_delete_builder, apigee_organizations_developers_apps_delete_task,
    apigee_organizations_developers_apps_generate_key_pair_or_update_developer_app_status_builder, apigee_organizations_developers_apps_generate_key_pair_or_update_developer_app_status_task,
    apigee_organizations_developers_apps_get_builder, apigee_organizations_developers_apps_get_task,
    apigee_organizations_developers_apps_list_builder, apigee_organizations_developers_apps_list_task,
    apigee_organizations_developers_apps_update_builder, apigee_organizations_developers_apps_update_task,
    apigee_organizations_developers_apps_attributes_delete_builder, apigee_organizations_developers_apps_attributes_delete_task,
    apigee_organizations_developers_apps_attributes_get_builder, apigee_organizations_developers_apps_attributes_get_task,
    apigee_organizations_developers_apps_attributes_list_builder, apigee_organizations_developers_apps_attributes_list_task,
    apigee_organizations_developers_apps_attributes_update_developer_app_attribute_builder, apigee_organizations_developers_apps_attributes_update_developer_app_attribute_task,
    apigee_organizations_developers_apps_keys_create_builder, apigee_organizations_developers_apps_keys_create_task,
    apigee_organizations_developers_apps_keys_delete_builder, apigee_organizations_developers_apps_keys_delete_task,
    apigee_organizations_developers_apps_keys_get_builder, apigee_organizations_developers_apps_keys_get_task,
    apigee_organizations_developers_apps_keys_replace_developer_app_key_builder, apigee_organizations_developers_apps_keys_replace_developer_app_key_task,
    apigee_organizations_developers_apps_keys_update_developer_app_key_builder, apigee_organizations_developers_apps_keys_update_developer_app_key_task,
    apigee_organizations_developers_apps_keys_apiproducts_delete_builder, apigee_organizations_developers_apps_keys_apiproducts_delete_task,
    apigee_organizations_developers_apps_keys_apiproducts_update_developer_app_key_api_product_builder, apigee_organizations_developers_apps_keys_apiproducts_update_developer_app_key_api_product_task,
    apigee_organizations_developers_apps_keys_create_create_builder, apigee_organizations_developers_apps_keys_create_create_task,
    apigee_organizations_developers_attributes_delete_builder, apigee_organizations_developers_attributes_delete_task,
    apigee_organizations_developers_attributes_get_builder, apigee_organizations_developers_attributes_get_task,
    apigee_organizations_developers_attributes_list_builder, apigee_organizations_developers_attributes_list_task,
    apigee_organizations_developers_attributes_update_developer_attribute_builder, apigee_organizations_developers_attributes_update_developer_attribute_task,
    apigee_organizations_developers_balance_adjust_builder, apigee_organizations_developers_balance_adjust_task,
    apigee_organizations_developers_balance_credit_builder, apigee_organizations_developers_balance_credit_task,
    apigee_organizations_developers_subscriptions_create_builder, apigee_organizations_developers_subscriptions_create_task,
    apigee_organizations_developers_subscriptions_expire_builder, apigee_organizations_developers_subscriptions_expire_task,
    apigee_organizations_developers_subscriptions_get_builder, apigee_organizations_developers_subscriptions_get_task,
    apigee_organizations_developers_subscriptions_list_builder, apigee_organizations_developers_subscriptions_list_task,
    apigee_organizations_dns_zones_create_builder, apigee_organizations_dns_zones_create_task,
    apigee_organizations_dns_zones_delete_builder, apigee_organizations_dns_zones_delete_task,
    apigee_organizations_dns_zones_get_builder, apigee_organizations_dns_zones_get_task,
    apigee_organizations_dns_zones_list_builder, apigee_organizations_dns_zones_list_task,
    apigee_organizations_endpoint_attachments_create_builder, apigee_organizations_endpoint_attachments_create_task,
    apigee_organizations_endpoint_attachments_delete_builder, apigee_organizations_endpoint_attachments_delete_task,
    apigee_organizations_endpoint_attachments_get_builder, apigee_organizations_endpoint_attachments_get_task,
    apigee_organizations_endpoint_attachments_list_builder, apigee_organizations_endpoint_attachments_list_task,
    apigee_organizations_envgroups_create_builder, apigee_organizations_envgroups_create_task,
    apigee_organizations_envgroups_delete_builder, apigee_organizations_envgroups_delete_task,
    apigee_organizations_envgroups_get_builder, apigee_organizations_envgroups_get_task,
    apigee_organizations_envgroups_get_deployed_ingress_config_builder, apigee_organizations_envgroups_get_deployed_ingress_config_task,
    apigee_organizations_envgroups_list_builder, apigee_organizations_envgroups_list_task,
    apigee_organizations_envgroups_patch_builder, apigee_organizations_envgroups_patch_task,
    apigee_organizations_envgroups_attachments_create_builder, apigee_organizations_envgroups_attachments_create_task,
    apigee_organizations_envgroups_attachments_delete_builder, apigee_organizations_envgroups_attachments_delete_task,
    apigee_organizations_envgroups_attachments_get_builder, apigee_organizations_envgroups_attachments_get_task,
    apigee_organizations_envgroups_attachments_list_builder, apigee_organizations_envgroups_attachments_list_task,
    apigee_organizations_environments_create_builder, apigee_organizations_environments_create_task,
    apigee_organizations_environments_delete_builder, apigee_organizations_environments_delete_task,
    apigee_organizations_environments_get_builder, apigee_organizations_environments_get_task,
    apigee_organizations_environments_get_addons_config_builder, apigee_organizations_environments_get_addons_config_task,
    apigee_organizations_environments_get_api_security_runtime_config_builder, apigee_organizations_environments_get_api_security_runtime_config_task,
    apigee_organizations_environments_get_debugmask_builder, apigee_organizations_environments_get_debugmask_task,
    apigee_organizations_environments_get_deployed_config_builder, apigee_organizations_environments_get_deployed_config_task,
    apigee_organizations_environments_get_iam_policy_builder, apigee_organizations_environments_get_iam_policy_task,
    apigee_organizations_environments_get_security_actions_config_builder, apigee_organizations_environments_get_security_actions_config_task,
    apigee_organizations_environments_get_trace_config_builder, apigee_organizations_environments_get_trace_config_task,
    apigee_organizations_environments_modify_environment_builder, apigee_organizations_environments_modify_environment_task,
    apigee_organizations_environments_set_iam_policy_builder, apigee_organizations_environments_set_iam_policy_task,
    apigee_organizations_environments_subscribe_builder, apigee_organizations_environments_subscribe_task,
    apigee_organizations_environments_test_iam_permissions_builder, apigee_organizations_environments_test_iam_permissions_task,
    apigee_organizations_environments_unsubscribe_builder, apigee_organizations_environments_unsubscribe_task,
    apigee_organizations_environments_update_builder, apigee_organizations_environments_update_task,
    apigee_organizations_environments_update_debugmask_builder, apigee_organizations_environments_update_debugmask_task,
    apigee_organizations_environments_update_environment_builder, apigee_organizations_environments_update_environment_task,
    apigee_organizations_environments_update_security_actions_config_builder, apigee_organizations_environments_update_security_actions_config_task,
    apigee_organizations_environments_update_trace_config_builder, apigee_organizations_environments_update_trace_config_task,
    apigee_organizations_environments_addons_config_set_addon_enablement_builder, apigee_organizations_environments_addons_config_set_addon_enablement_task,
    apigee_organizations_environments_analytics_admin_get_schemav2_builder, apigee_organizations_environments_analytics_admin_get_schemav2_task,
    apigee_organizations_environments_analytics_exports_create_builder, apigee_organizations_environments_analytics_exports_create_task,
    apigee_organizations_environments_analytics_exports_get_builder, apigee_organizations_environments_analytics_exports_get_task,
    apigee_organizations_environments_analytics_exports_list_builder, apigee_organizations_environments_analytics_exports_list_task,
    apigee_organizations_environments_apis_deployments_list_builder, apigee_organizations_environments_apis_deployments_list_task,
    apigee_organizations_environments_apis_revisions_deploy_builder, apigee_organizations_environments_apis_revisions_deploy_task,
    apigee_organizations_environments_apis_revisions_get_deployments_builder, apigee_organizations_environments_apis_revisions_get_deployments_task,
    apigee_organizations_environments_apis_revisions_undeploy_builder, apigee_organizations_environments_apis_revisions_undeploy_task,
    apigee_organizations_environments_apis_revisions_debugsessions_create_builder, apigee_organizations_environments_apis_revisions_debugsessions_create_task,
    apigee_organizations_environments_apis_revisions_debugsessions_delete_data_builder, apigee_organizations_environments_apis_revisions_debugsessions_delete_data_task,
    apigee_organizations_environments_apis_revisions_debugsessions_get_builder, apigee_organizations_environments_apis_revisions_debugsessions_get_task,
    apigee_organizations_environments_apis_revisions_debugsessions_list_builder, apigee_organizations_environments_apis_revisions_debugsessions_list_task,
    apigee_organizations_environments_apis_revisions_debugsessions_data_get_builder, apigee_organizations_environments_apis_revisions_debugsessions_data_get_task,
    apigee_organizations_environments_apis_revisions_deployments_generate_deploy_change_report_builder, apigee_organizations_environments_apis_revisions_deployments_generate_deploy_change_report_task,
    apigee_organizations_environments_apis_revisions_deployments_generate_undeploy_change_report_builder, apigee_organizations_environments_apis_revisions_deployments_generate_undeploy_change_report_task,
    apigee_organizations_environments_archive_deployments_create_builder, apigee_organizations_environments_archive_deployments_create_task,
    apigee_organizations_environments_archive_deployments_delete_builder, apigee_organizations_environments_archive_deployments_delete_task,
    apigee_organizations_environments_archive_deployments_generate_download_url_builder, apigee_organizations_environments_archive_deployments_generate_download_url_task,
    apigee_organizations_environments_archive_deployments_generate_upload_url_builder, apigee_organizations_environments_archive_deployments_generate_upload_url_task,
    apigee_organizations_environments_archive_deployments_get_builder, apigee_organizations_environments_archive_deployments_get_task,
    apigee_organizations_environments_archive_deployments_list_builder, apigee_organizations_environments_archive_deployments_list_task,
    apigee_organizations_environments_archive_deployments_patch_builder, apigee_organizations_environments_archive_deployments_patch_task,
    apigee_organizations_environments_caches_delete_builder, apigee_organizations_environments_caches_delete_task,
    apigee_organizations_environments_deployments_get_builder, apigee_organizations_environments_deployments_get_task,
    apigee_organizations_environments_deployments_get_iam_policy_builder, apigee_organizations_environments_deployments_get_iam_policy_task,
    apigee_organizations_environments_deployments_list_builder, apigee_organizations_environments_deployments_list_task,
    apigee_organizations_environments_deployments_set_iam_policy_builder, apigee_organizations_environments_deployments_set_iam_policy_task,
    apigee_organizations_environments_deployments_test_iam_permissions_builder, apigee_organizations_environments_deployments_test_iam_permissions_task,
    apigee_organizations_environments_flowhooks_attach_shared_flow_to_flow_hook_builder, apigee_organizations_environments_flowhooks_attach_shared_flow_to_flow_hook_task,
    apigee_organizations_environments_flowhooks_detach_shared_flow_from_flow_hook_builder, apigee_organizations_environments_flowhooks_detach_shared_flow_from_flow_hook_task,
    apigee_organizations_environments_flowhooks_get_builder, apigee_organizations_environments_flowhooks_get_task,
    apigee_organizations_environments_keystores_create_builder, apigee_organizations_environments_keystores_create_task,
    apigee_organizations_environments_keystores_delete_builder, apigee_organizations_environments_keystores_delete_task,
    apigee_organizations_environments_keystores_get_builder, apigee_organizations_environments_keystores_get_task,
    apigee_organizations_environments_keystores_aliases_create_builder, apigee_organizations_environments_keystores_aliases_create_task,
    apigee_organizations_environments_keystores_aliases_csr_builder, apigee_organizations_environments_keystores_aliases_csr_task,
    apigee_organizations_environments_keystores_aliases_delete_builder, apigee_organizations_environments_keystores_aliases_delete_task,
    apigee_organizations_environments_keystores_aliases_get_builder, apigee_organizations_environments_keystores_aliases_get_task,
    apigee_organizations_environments_keystores_aliases_get_certificate_builder, apigee_organizations_environments_keystores_aliases_get_certificate_task,
    apigee_organizations_environments_keystores_aliases_update_builder, apigee_organizations_environments_keystores_aliases_update_task,
    apigee_organizations_environments_keyvaluemaps_create_builder, apigee_organizations_environments_keyvaluemaps_create_task,
    apigee_organizations_environments_keyvaluemaps_delete_builder, apigee_organizations_environments_keyvaluemaps_delete_task,
    apigee_organizations_environments_keyvaluemaps_get_builder, apigee_organizations_environments_keyvaluemaps_get_task,
    apigee_organizations_environments_keyvaluemaps_update_builder, apigee_organizations_environments_keyvaluemaps_update_task,
    apigee_organizations_environments_keyvaluemaps_entries_create_builder, apigee_organizations_environments_keyvaluemaps_entries_create_task,
    apigee_organizations_environments_keyvaluemaps_entries_delete_builder, apigee_organizations_environments_keyvaluemaps_entries_delete_task,
    apigee_organizations_environments_keyvaluemaps_entries_get_builder, apigee_organizations_environments_keyvaluemaps_entries_get_task,
    apigee_organizations_environments_keyvaluemaps_entries_list_builder, apigee_organizations_environments_keyvaluemaps_entries_list_task,
    apigee_organizations_environments_keyvaluemaps_entries_update_builder, apigee_organizations_environments_keyvaluemaps_entries_update_task,
    apigee_organizations_environments_optimized_stats_get_builder, apigee_organizations_environments_optimized_stats_get_task,
    apigee_organizations_environments_queries_create_builder, apigee_organizations_environments_queries_create_task,
    apigee_organizations_environments_queries_get_builder, apigee_organizations_environments_queries_get_task,
    apigee_organizations_environments_queries_get_result_builder, apigee_organizations_environments_queries_get_result_task,
    apigee_organizations_environments_queries_get_resulturl_builder, apigee_organizations_environments_queries_get_resulturl_task,
    apigee_organizations_environments_queries_list_builder, apigee_organizations_environments_queries_list_task,
    apigee_organizations_environments_references_create_builder, apigee_organizations_environments_references_create_task,
    apigee_organizations_environments_references_delete_builder, apigee_organizations_environments_references_delete_task,
    apigee_organizations_environments_references_get_builder, apigee_organizations_environments_references_get_task,
    apigee_organizations_environments_references_update_builder, apigee_organizations_environments_references_update_task,
    apigee_organizations_environments_resourcefiles_create_builder, apigee_organizations_environments_resourcefiles_create_task,
    apigee_organizations_environments_resourcefiles_delete_builder, apigee_organizations_environments_resourcefiles_delete_task,
    apigee_organizations_environments_resourcefiles_get_builder, apigee_organizations_environments_resourcefiles_get_task,
    apigee_organizations_environments_resourcefiles_list_builder, apigee_organizations_environments_resourcefiles_list_task,
    apigee_organizations_environments_resourcefiles_list_environment_resources_builder, apigee_organizations_environments_resourcefiles_list_environment_resources_task,
    apigee_organizations_environments_resourcefiles_update_builder, apigee_organizations_environments_resourcefiles_update_task,
    apigee_organizations_environments_security_actions_create_builder, apigee_organizations_environments_security_actions_create_task,
    apigee_organizations_environments_security_actions_delete_builder, apigee_organizations_environments_security_actions_delete_task,
    apigee_organizations_environments_security_actions_disable_builder, apigee_organizations_environments_security_actions_disable_task,
    apigee_organizations_environments_security_actions_enable_builder, apigee_organizations_environments_security_actions_enable_task,
    apigee_organizations_environments_security_actions_get_builder, apigee_organizations_environments_security_actions_get_task,
    apigee_organizations_environments_security_actions_list_builder, apigee_organizations_environments_security_actions_list_task,
    apigee_organizations_environments_security_actions_patch_builder, apigee_organizations_environments_security_actions_patch_task,
    apigee_organizations_environments_security_incidents_batch_update_builder, apigee_organizations_environments_security_incidents_batch_update_task,
    apigee_organizations_environments_security_incidents_get_builder, apigee_organizations_environments_security_incidents_get_task,
    apigee_organizations_environments_security_incidents_list_builder, apigee_organizations_environments_security_incidents_list_task,
    apigee_organizations_environments_security_incidents_patch_builder, apigee_organizations_environments_security_incidents_patch_task,
    apigee_organizations_environments_security_reports_create_builder, apigee_organizations_environments_security_reports_create_task,
    apigee_organizations_environments_security_reports_get_builder, apigee_organizations_environments_security_reports_get_task,
    apigee_organizations_environments_security_reports_get_result_builder, apigee_organizations_environments_security_reports_get_result_task,
    apigee_organizations_environments_security_reports_get_result_view_builder, apigee_organizations_environments_security_reports_get_result_view_task,
    apigee_organizations_environments_security_reports_list_builder, apigee_organizations_environments_security_reports_list_task,
    apigee_organizations_environments_security_stats_query_tabular_stats_builder, apigee_organizations_environments_security_stats_query_tabular_stats_task,
    apigee_organizations_environments_security_stats_query_time_series_stats_builder, apigee_organizations_environments_security_stats_query_time_series_stats_task,
    apigee_organizations_environments_sharedflows_deployments_list_builder, apigee_organizations_environments_sharedflows_deployments_list_task,
    apigee_organizations_environments_sharedflows_revisions_deploy_builder, apigee_organizations_environments_sharedflows_revisions_deploy_task,
    apigee_organizations_environments_sharedflows_revisions_get_deployments_builder, apigee_organizations_environments_sharedflows_revisions_get_deployments_task,
    apigee_organizations_environments_sharedflows_revisions_undeploy_builder, apigee_organizations_environments_sharedflows_revisions_undeploy_task,
    apigee_organizations_environments_stats_get_builder, apigee_organizations_environments_stats_get_task,
    apigee_organizations_environments_targetservers_create_builder, apigee_organizations_environments_targetservers_create_task,
    apigee_organizations_environments_targetservers_delete_builder, apigee_organizations_environments_targetservers_delete_task,
    apigee_organizations_environments_targetservers_get_builder, apigee_organizations_environments_targetservers_get_task,
    apigee_organizations_environments_targetservers_update_builder, apigee_organizations_environments_targetservers_update_task,
    apigee_organizations_environments_trace_config_overrides_create_builder, apigee_organizations_environments_trace_config_overrides_create_task,
    apigee_organizations_environments_trace_config_overrides_delete_builder, apigee_organizations_environments_trace_config_overrides_delete_task,
    apigee_organizations_environments_trace_config_overrides_get_builder, apigee_organizations_environments_trace_config_overrides_get_task,
    apigee_organizations_environments_trace_config_overrides_list_builder, apigee_organizations_environments_trace_config_overrides_list_task,
    apigee_organizations_environments_trace_config_overrides_patch_builder, apigee_organizations_environments_trace_config_overrides_patch_task,
    apigee_organizations_host_queries_create_builder, apigee_organizations_host_queries_create_task,
    apigee_organizations_host_queries_get_builder, apigee_organizations_host_queries_get_task,
    apigee_organizations_host_queries_get_result_builder, apigee_organizations_host_queries_get_result_task,
    apigee_organizations_host_queries_get_result_view_builder, apigee_organizations_host_queries_get_result_view_task,
    apigee_organizations_host_queries_list_builder, apigee_organizations_host_queries_list_task,
    apigee_organizations_host_security_reports_create_builder, apigee_organizations_host_security_reports_create_task,
    apigee_organizations_host_security_reports_get_builder, apigee_organizations_host_security_reports_get_task,
    apigee_organizations_host_security_reports_get_result_builder, apigee_organizations_host_security_reports_get_result_task,
    apigee_organizations_host_security_reports_get_result_view_builder, apigee_organizations_host_security_reports_get_result_view_task,
    apigee_organizations_host_security_reports_list_builder, apigee_organizations_host_security_reports_list_task,
    apigee_organizations_host_stats_get_builder, apigee_organizations_host_stats_get_task,
    apigee_organizations_instances_create_builder, apigee_organizations_instances_create_task,
    apigee_organizations_instances_delete_builder, apigee_organizations_instances_delete_task,
    apigee_organizations_instances_get_builder, apigee_organizations_instances_get_task,
    apigee_organizations_instances_list_builder, apigee_organizations_instances_list_task,
    apigee_organizations_instances_patch_builder, apigee_organizations_instances_patch_task,
    apigee_organizations_instances_report_status_builder, apigee_organizations_instances_report_status_task,
    apigee_organizations_instances_attachments_create_builder, apigee_organizations_instances_attachments_create_task,
    apigee_organizations_instances_attachments_delete_builder, apigee_organizations_instances_attachments_delete_task,
    apigee_organizations_instances_attachments_get_builder, apigee_organizations_instances_attachments_get_task,
    apigee_organizations_instances_attachments_list_builder, apigee_organizations_instances_attachments_list_task,
    apigee_organizations_instances_canaryevaluations_create_builder, apigee_organizations_instances_canaryevaluations_create_task,
    apigee_organizations_instances_canaryevaluations_get_builder, apigee_organizations_instances_canaryevaluations_get_task,
    apigee_organizations_instances_nat_addresses_activate_builder, apigee_organizations_instances_nat_addresses_activate_task,
    apigee_organizations_instances_nat_addresses_create_builder, apigee_organizations_instances_nat_addresses_create_task,
    apigee_organizations_instances_nat_addresses_delete_builder, apigee_organizations_instances_nat_addresses_delete_task,
    apigee_organizations_instances_nat_addresses_get_builder, apigee_organizations_instances_nat_addresses_get_task,
    apigee_organizations_instances_nat_addresses_list_builder, apigee_organizations_instances_nat_addresses_list_task,
    apigee_organizations_keyvaluemaps_create_builder, apigee_organizations_keyvaluemaps_create_task,
    apigee_organizations_keyvaluemaps_delete_builder, apigee_organizations_keyvaluemaps_delete_task,
    apigee_organizations_keyvaluemaps_get_builder, apigee_organizations_keyvaluemaps_get_task,
    apigee_organizations_keyvaluemaps_update_builder, apigee_organizations_keyvaluemaps_update_task,
    apigee_organizations_keyvaluemaps_entries_create_builder, apigee_organizations_keyvaluemaps_entries_create_task,
    apigee_organizations_keyvaluemaps_entries_delete_builder, apigee_organizations_keyvaluemaps_entries_delete_task,
    apigee_organizations_keyvaluemaps_entries_get_builder, apigee_organizations_keyvaluemaps_entries_get_task,
    apigee_organizations_keyvaluemaps_entries_list_builder, apigee_organizations_keyvaluemaps_entries_list_task,
    apigee_organizations_keyvaluemaps_entries_update_builder, apigee_organizations_keyvaluemaps_entries_update_task,
    apigee_organizations_operations_get_builder, apigee_organizations_operations_get_task,
    apigee_organizations_operations_list_builder, apigee_organizations_operations_list_task,
    apigee_organizations_optimized_host_stats_get_builder, apigee_organizations_optimized_host_stats_get_task,
    apigee_organizations_reports_create_builder, apigee_organizations_reports_create_task,
    apigee_organizations_reports_delete_builder, apigee_organizations_reports_delete_task,
    apigee_organizations_reports_get_builder, apigee_organizations_reports_get_task,
    apigee_organizations_reports_list_builder, apigee_organizations_reports_list_task,
    apigee_organizations_reports_update_builder, apigee_organizations_reports_update_task,
    apigee_organizations_security_assessment_results_batch_compute_builder, apigee_organizations_security_assessment_results_batch_compute_task,
    apigee_organizations_security_feedback_create_builder, apigee_organizations_security_feedback_create_task,
    apigee_organizations_security_feedback_delete_builder, apigee_organizations_security_feedback_delete_task,
    apigee_organizations_security_feedback_get_builder, apigee_organizations_security_feedback_get_task,
    apigee_organizations_security_feedback_list_builder, apigee_organizations_security_feedback_list_task,
    apigee_organizations_security_feedback_patch_builder, apigee_organizations_security_feedback_patch_task,
    apigee_organizations_security_monitoring_conditions_create_builder, apigee_organizations_security_monitoring_conditions_create_task,
    apigee_organizations_security_monitoring_conditions_delete_builder, apigee_organizations_security_monitoring_conditions_delete_task,
    apigee_organizations_security_monitoring_conditions_get_builder, apigee_organizations_security_monitoring_conditions_get_task,
    apigee_organizations_security_monitoring_conditions_list_builder, apigee_organizations_security_monitoring_conditions_list_task,
    apigee_organizations_security_monitoring_conditions_patch_builder, apigee_organizations_security_monitoring_conditions_patch_task,
    apigee_organizations_security_profiles_create_builder, apigee_organizations_security_profiles_create_task,
    apigee_organizations_security_profiles_delete_builder, apigee_organizations_security_profiles_delete_task,
    apigee_organizations_security_profiles_get_builder, apigee_organizations_security_profiles_get_task,
    apigee_organizations_security_profiles_list_builder, apigee_organizations_security_profiles_list_task,
    apigee_organizations_security_profiles_list_revisions_builder, apigee_organizations_security_profiles_list_revisions_task,
    apigee_organizations_security_profiles_patch_builder, apigee_organizations_security_profiles_patch_task,
    apigee_organizations_security_profiles_environments_compute_environment_scores_builder, apigee_organizations_security_profiles_environments_compute_environment_scores_task,
    apigee_organizations_security_profiles_environments_create_builder, apigee_organizations_security_profiles_environments_create_task,
    apigee_organizations_security_profiles_environments_delete_builder, apigee_organizations_security_profiles_environments_delete_task,
    apigee_organizations_security_profiles_v2_create_builder, apigee_organizations_security_profiles_v2_create_task,
    apigee_organizations_security_profiles_v2_delete_builder, apigee_organizations_security_profiles_v2_delete_task,
    apigee_organizations_security_profiles_v2_get_builder, apigee_organizations_security_profiles_v2_get_task,
    apigee_organizations_security_profiles_v2_list_builder, apigee_organizations_security_profiles_v2_list_task,
    apigee_organizations_security_profiles_v2_patch_builder, apigee_organizations_security_profiles_v2_patch_task,
    apigee_organizations_sharedflows_create_builder, apigee_organizations_sharedflows_create_task,
    apigee_organizations_sharedflows_delete_builder, apigee_organizations_sharedflows_delete_task,
    apigee_organizations_sharedflows_get_builder, apigee_organizations_sharedflows_get_task,
    apigee_organizations_sharedflows_list_builder, apigee_organizations_sharedflows_list_task,
    apigee_organizations_sharedflows_move_builder, apigee_organizations_sharedflows_move_task,
    apigee_organizations_sharedflows_deployments_list_builder, apigee_organizations_sharedflows_deployments_list_task,
    apigee_organizations_sharedflows_revisions_delete_builder, apigee_organizations_sharedflows_revisions_delete_task,
    apigee_organizations_sharedflows_revisions_get_builder, apigee_organizations_sharedflows_revisions_get_task,
    apigee_organizations_sharedflows_revisions_update_shared_flow_revision_builder, apigee_organizations_sharedflows_revisions_update_shared_flow_revision_task,
    apigee_organizations_sharedflows_revisions_deployments_list_builder, apigee_organizations_sharedflows_revisions_deployments_list_task,
    apigee_organizations_sites_apicategories_create_builder, apigee_organizations_sites_apicategories_create_task,
    apigee_organizations_sites_apicategories_delete_builder, apigee_organizations_sites_apicategories_delete_task,
    apigee_organizations_sites_apicategories_get_builder, apigee_organizations_sites_apicategories_get_task,
    apigee_organizations_sites_apicategories_list_builder, apigee_organizations_sites_apicategories_list_task,
    apigee_organizations_sites_apicategories_patch_builder, apigee_organizations_sites_apicategories_patch_task,
    apigee_organizations_sites_apidocs_create_builder, apigee_organizations_sites_apidocs_create_task,
    apigee_organizations_sites_apidocs_delete_builder, apigee_organizations_sites_apidocs_delete_task,
    apigee_organizations_sites_apidocs_get_builder, apigee_organizations_sites_apidocs_get_task,
    apigee_organizations_sites_apidocs_get_documentation_builder, apigee_organizations_sites_apidocs_get_documentation_task,
    apigee_organizations_sites_apidocs_list_builder, apigee_organizations_sites_apidocs_list_task,
    apigee_organizations_sites_apidocs_update_builder, apigee_organizations_sites_apidocs_update_task,
    apigee_organizations_sites_apidocs_update_documentation_builder, apigee_organizations_sites_apidocs_update_documentation_task,
    apigee_organizations_spaces_create_builder, apigee_organizations_spaces_create_task,
    apigee_organizations_spaces_delete_builder, apigee_organizations_spaces_delete_task,
    apigee_organizations_spaces_get_builder, apigee_organizations_spaces_get_task,
    apigee_organizations_spaces_get_iam_policy_builder, apigee_organizations_spaces_get_iam_policy_task,
    apigee_organizations_spaces_list_builder, apigee_organizations_spaces_list_task,
    apigee_organizations_spaces_patch_builder, apigee_organizations_spaces_patch_task,
    apigee_organizations_spaces_set_iam_policy_builder, apigee_organizations_spaces_set_iam_policy_task,
    apigee_organizations_spaces_test_iam_permissions_builder, apigee_organizations_spaces_test_iam_permissions_task,
    apigee_projects_provision_organization_builder, apigee_projects_provision_organization_task,
};
use crate::providers::gcp::clients::types::{ApiError, ApiPending};
use crate::providers::gcp::clients::apigee::GoogleApiHttpBody;
use crate::providers::gcp::clients::apigee::GoogleCloudApigeeV1AddonsConfig;
use crate::providers::gcp::clients::apigee::GoogleCloudApigeeV1Alias;
use crate::providers::gcp::clients::apigee::GoogleCloudApigeeV1ApiCategoryResponse;
use crate::providers::gcp::clients::apigee::GoogleCloudApigeeV1ApiDocDocumentationResponse;
use crate::providers::gcp::clients::apigee::GoogleCloudApigeeV1ApiDocResponse;
use crate::providers::gcp::clients::apigee::GoogleCloudApigeeV1ApiProduct;
use crate::providers::gcp::clients::apigee::GoogleCloudApigeeV1ApiProxy;
use crate::providers::gcp::clients::apigee::GoogleCloudApigeeV1ApiProxyRevision;
use crate::providers::gcp::clients::apigee::GoogleCloudApigeeV1ApiSecurityRuntimeConfig;
use crate::providers::gcp::clients::apigee::GoogleCloudApigeeV1ApimServiceExtension;
use crate::providers::gcp::clients::apigee::GoogleCloudApigeeV1App;
use crate::providers::gcp::clients::apigee::GoogleCloudApigeeV1AppGroup;
use crate::providers::gcp::clients::apigee::GoogleCloudApigeeV1AppGroupApp;
use crate::providers::gcp::clients::apigee::GoogleCloudApigeeV1AppGroupAppKey;
use crate::providers::gcp::clients::apigee::GoogleCloudApigeeV1AppGroupBalance;
use crate::providers::gcp::clients::apigee::GoogleCloudApigeeV1AppGroupMonetizationConfig;
use crate::providers::gcp::clients::apigee::GoogleCloudApigeeV1AppGroupSubscription;
use crate::providers::gcp::clients::apigee::GoogleCloudApigeeV1ArchiveDeployment;
use crate::providers::gcp::clients::apigee::GoogleCloudApigeeV1AsyncQuery;
use crate::providers::gcp::clients::apigee::GoogleCloudApigeeV1AsyncQueryResultView;
use crate::providers::gcp::clients::apigee::GoogleCloudApigeeV1Attribute;
use crate::providers::gcp::clients::apigee::GoogleCloudApigeeV1Attributes;
use crate::providers::gcp::clients::apigee::GoogleCloudApigeeV1BatchComputeSecurityAssessmentResultsResponse;
use crate::providers::gcp::clients::apigee::GoogleCloudApigeeV1BatchUpdateSecurityIncidentsResponse;
use crate::providers::gcp::clients::apigee::GoogleCloudApigeeV1CanaryEvaluation;
use crate::providers::gcp::clients::apigee::GoogleCloudApigeeV1ComputeEnvironmentScoresResponse;
use crate::providers::gcp::clients::apigee::GoogleCloudApigeeV1ControlPlaneAccess;
use crate::providers::gcp::clients::apigee::GoogleCloudApigeeV1CustomReport;
use crate::providers::gcp::clients::apigee::GoogleCloudApigeeV1DataCollector;
use crate::providers::gcp::clients::apigee::GoogleCloudApigeeV1Datastore;
use crate::providers::gcp::clients::apigee::GoogleCloudApigeeV1DebugMask;
use crate::providers::gcp::clients::apigee::GoogleCloudApigeeV1DebugSession;
use crate::providers::gcp::clients::apigee::GoogleCloudApigeeV1DebugSessionTransaction;
use crate::providers::gcp::clients::apigee::GoogleCloudApigeeV1DeleteCustomReportResponse;
use crate::providers::gcp::clients::apigee::GoogleCloudApigeeV1DeleteResponse;
use crate::providers::gcp::clients::apigee::GoogleCloudApigeeV1Deployment;
use crate::providers::gcp::clients::apigee::GoogleCloudApigeeV1DeploymentChangeReport;
use crate::providers::gcp::clients::apigee::GoogleCloudApigeeV1Developer;
use crate::providers::gcp::clients::apigee::GoogleCloudApigeeV1DeveloperApp;
use crate::providers::gcp::clients::apigee::GoogleCloudApigeeV1DeveloperAppKey;
use crate::providers::gcp::clients::apigee::GoogleCloudApigeeV1DeveloperBalance;
use crate::providers::gcp::clients::apigee::GoogleCloudApigeeV1DeveloperMonetizationConfig;
use crate::providers::gcp::clients::apigee::GoogleCloudApigeeV1DeveloperSubscription;
use crate::providers::gcp::clients::apigee::GoogleCloudApigeeV1DnsZone;
use crate::providers::gcp::clients::apigee::GoogleCloudApigeeV1EndpointAttachment;
use crate::providers::gcp::clients::apigee::GoogleCloudApigeeV1Environment;
use crate::providers::gcp::clients::apigee::GoogleCloudApigeeV1EnvironmentConfig;
use crate::providers::gcp::clients::apigee::GoogleCloudApigeeV1EnvironmentGroup;
use crate::providers::gcp::clients::apigee::GoogleCloudApigeeV1EnvironmentGroupAttachment;
use crate::providers::gcp::clients::apigee::GoogleCloudApigeeV1EnvironmentGroupConfig;
use crate::providers::gcp::clients::apigee::GoogleCloudApigeeV1Export;
use crate::providers::gcp::clients::apigee::GoogleCloudApigeeV1FlowHook;
use crate::providers::gcp::clients::apigee::GoogleCloudApigeeV1GenerateDownloadUrlResponse;
use crate::providers::gcp::clients::apigee::GoogleCloudApigeeV1GenerateUploadUrlResponse;
use crate::providers::gcp::clients::apigee::GoogleCloudApigeeV1GetAsyncQueryResultUrlResponse;
use crate::providers::gcp::clients::apigee::GoogleCloudApigeeV1IngressConfig;
use crate::providers::gcp::clients::apigee::GoogleCloudApigeeV1Instance;
use crate::providers::gcp::clients::apigee::GoogleCloudApigeeV1InstanceAttachment;
use crate::providers::gcp::clients::apigee::GoogleCloudApigeeV1KeyValueEntry;
use crate::providers::gcp::clients::apigee::GoogleCloudApigeeV1KeyValueMap;
use crate::providers::gcp::clients::apigee::GoogleCloudApigeeV1Keystore;
use crate::providers::gcp::clients::apigee::GoogleCloudApigeeV1ListApiCategoriesResponse;
use crate::providers::gcp::clients::apigee::GoogleCloudApigeeV1ListApiDebugSessionsResponse;
use crate::providers::gcp::clients::apigee::GoogleCloudApigeeV1ListApiDocsResponse;
use crate::providers::gcp::clients::apigee::GoogleCloudApigeeV1ListApiProductsResponse;
use crate::providers::gcp::clients::apigee::GoogleCloudApigeeV1ListApiProxiesResponse;
use crate::providers::gcp::clients::apigee::GoogleCloudApigeeV1ListApimServiceExtensionsResponse;
use crate::providers::gcp::clients::apigee::GoogleCloudApigeeV1ListAppGroupAppsResponse;
use crate::providers::gcp::clients::apigee::GoogleCloudApigeeV1ListAppGroupSubscriptionsResponse;
use crate::providers::gcp::clients::apigee::GoogleCloudApigeeV1ListAppGroupsResponse;
use crate::providers::gcp::clients::apigee::GoogleCloudApigeeV1ListAppsResponse;
use crate::providers::gcp::clients::apigee::GoogleCloudApigeeV1ListArchiveDeploymentsResponse;
use crate::providers::gcp::clients::apigee::GoogleCloudApigeeV1ListAsyncQueriesResponse;
use crate::providers::gcp::clients::apigee::GoogleCloudApigeeV1ListCustomReportsResponse;
use crate::providers::gcp::clients::apigee::GoogleCloudApigeeV1ListDataCollectorsResponse;
use crate::providers::gcp::clients::apigee::GoogleCloudApigeeV1ListDatastoresResponse;
use crate::providers::gcp::clients::apigee::GoogleCloudApigeeV1ListDebugSessionsResponse;
use crate::providers::gcp::clients::apigee::GoogleCloudApigeeV1ListDeploymentsResponse;
use crate::providers::gcp::clients::apigee::GoogleCloudApigeeV1ListDeveloperAppsResponse;
use crate::providers::gcp::clients::apigee::GoogleCloudApigeeV1ListDeveloperSubscriptionsResponse;
use crate::providers::gcp::clients::apigee::GoogleCloudApigeeV1ListDnsZonesResponse;
use crate::providers::gcp::clients::apigee::GoogleCloudApigeeV1ListEndpointAttachmentsResponse;
use crate::providers::gcp::clients::apigee::GoogleCloudApigeeV1ListEnvironmentGroupAttachmentsResponse;
use crate::providers::gcp::clients::apigee::GoogleCloudApigeeV1ListEnvironmentGroupsResponse;
use crate::providers::gcp::clients::apigee::GoogleCloudApigeeV1ListEnvironmentResourcesResponse;
use crate::providers::gcp::clients::apigee::GoogleCloudApigeeV1ListExportsResponse;
use crate::providers::gcp::clients::apigee::GoogleCloudApigeeV1ListHybridIssuersResponse;
use crate::providers::gcp::clients::apigee::GoogleCloudApigeeV1ListInstanceAttachmentsResponse;
use crate::providers::gcp::clients::apigee::GoogleCloudApigeeV1ListInstancesResponse;
use crate::providers::gcp::clients::apigee::GoogleCloudApigeeV1ListKeyValueEntriesResponse;
use crate::providers::gcp::clients::apigee::GoogleCloudApigeeV1ListNatAddressesResponse;
use crate::providers::gcp::clients::apigee::GoogleCloudApigeeV1ListOfDevelopersResponse;
use crate::providers::gcp::clients::apigee::GoogleCloudApigeeV1ListOrganizationsResponse;
use crate::providers::gcp::clients::apigee::GoogleCloudApigeeV1ListRatePlansResponse;
use crate::providers::gcp::clients::apigee::GoogleCloudApigeeV1ListSecurityActionsResponse;
use crate::providers::gcp::clients::apigee::GoogleCloudApigeeV1ListSecurityFeedbackResponse;
use crate::providers::gcp::clients::apigee::GoogleCloudApigeeV1ListSecurityIncidentsResponse;
use crate::providers::gcp::clients::apigee::GoogleCloudApigeeV1ListSecurityMonitoringConditionsResponse;
use crate::providers::gcp::clients::apigee::GoogleCloudApigeeV1ListSecurityProfileRevisionsResponse;
use crate::providers::gcp::clients::apigee::GoogleCloudApigeeV1ListSecurityProfilesResponse;
use crate::providers::gcp::clients::apigee::GoogleCloudApigeeV1ListSecurityProfilesV2Response;
use crate::providers::gcp::clients::apigee::GoogleCloudApigeeV1ListSecurityReportsResponse;
use crate::providers::gcp::clients::apigee::GoogleCloudApigeeV1ListSharedFlowsResponse;
use crate::providers::gcp::clients::apigee::GoogleCloudApigeeV1ListSpacesResponse;
use crate::providers::gcp::clients::apigee::GoogleCloudApigeeV1ListTraceConfigOverridesResponse;
use crate::providers::gcp::clients::apigee::GoogleCloudApigeeV1NatAddress;
use crate::providers::gcp::clients::apigee::GoogleCloudApigeeV1OptimizedStats;
use crate::providers::gcp::clients::apigee::GoogleCloudApigeeV1Organization;
use crate::providers::gcp::clients::apigee::GoogleCloudApigeeV1OrganizationProjectMapping;
use crate::providers::gcp::clients::apigee::GoogleCloudApigeeV1QueryTabularStatsResponse;
use crate::providers::gcp::clients::apigee::GoogleCloudApigeeV1QueryTimeSeriesStatsResponse;
use crate::providers::gcp::clients::apigee::GoogleCloudApigeeV1RatePlan;
use crate::providers::gcp::clients::apigee::GoogleCloudApigeeV1Reference;
use crate::providers::gcp::clients::apigee::GoogleCloudApigeeV1ReportInstanceStatusResponse;
use crate::providers::gcp::clients::apigee::GoogleCloudApigeeV1ResourceFile;
use crate::providers::gcp::clients::apigee::GoogleCloudApigeeV1RuntimeConfig;
use crate::providers::gcp::clients::apigee::GoogleCloudApigeeV1Schema;
use crate::providers::gcp::clients::apigee::GoogleCloudApigeeV1SecurityAction;
use crate::providers::gcp::clients::apigee::GoogleCloudApigeeV1SecurityActionsConfig;
use crate::providers::gcp::clients::apigee::GoogleCloudApigeeV1SecurityFeedback;
use crate::providers::gcp::clients::apigee::GoogleCloudApigeeV1SecurityIncident;
use crate::providers::gcp::clients::apigee::GoogleCloudApigeeV1SecurityMonitoringCondition;
use crate::providers::gcp::clients::apigee::GoogleCloudApigeeV1SecurityProfile;
use crate::providers::gcp::clients::apigee::GoogleCloudApigeeV1SecurityProfileEnvironmentAssociation;
use crate::providers::gcp::clients::apigee::GoogleCloudApigeeV1SecurityProfileV2;
use crate::providers::gcp::clients::apigee::GoogleCloudApigeeV1SecurityReport;
use crate::providers::gcp::clients::apigee::GoogleCloudApigeeV1SecurityReportResultView;
use crate::providers::gcp::clients::apigee::GoogleCloudApigeeV1SecuritySettings;
use crate::providers::gcp::clients::apigee::GoogleCloudApigeeV1SharedFlow;
use crate::providers::gcp::clients::apigee::GoogleCloudApigeeV1SharedFlowRevision;
use crate::providers::gcp::clients::apigee::GoogleCloudApigeeV1Space;
use crate::providers::gcp::clients::apigee::GoogleCloudApigeeV1Stats;
use crate::providers::gcp::clients::apigee::GoogleCloudApigeeV1Subscription;
use crate::providers::gcp::clients::apigee::GoogleCloudApigeeV1SyncAuthorization;
use crate::providers::gcp::clients::apigee::GoogleCloudApigeeV1TargetServer;
use crate::providers::gcp::clients::apigee::GoogleCloudApigeeV1TestDatastoreResponse;
use crate::providers::gcp::clients::apigee::GoogleCloudApigeeV1TraceConfig;
use crate::providers::gcp::clients::apigee::GoogleCloudApigeeV1TraceConfigOverride;
use crate::providers::gcp::clients::apigee::GoogleIamV1Policy;
use crate::providers::gcp::clients::apigee::GoogleIamV1TestIamPermissionsResponse;
use crate::providers::gcp::clients::apigee::GoogleLongrunningListOperationsResponse;
use crate::providers::gcp::clients::apigee::GoogleLongrunningOperation;
use crate::providers::gcp::clients::apigee::GoogleProtobufEmpty;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsAnalyticsDatastoresCreateArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsAnalyticsDatastoresDeleteArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsAnalyticsDatastoresGetArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsAnalyticsDatastoresListArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsAnalyticsDatastoresTestArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsAnalyticsDatastoresUpdateArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsApimServiceExtensionsCreateArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsApimServiceExtensionsDeleteArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsApimServiceExtensionsGetArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsApimServiceExtensionsListArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsApimServiceExtensionsPatchArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsApiproductsAttributesArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsApiproductsAttributesDeleteArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsApiproductsAttributesGetArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsApiproductsAttributesListArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsApiproductsAttributesUpdateApiProductAttributeArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsApiproductsCreateArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsApiproductsDeleteArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsApiproductsGetArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsApiproductsListArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsApiproductsMoveArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsApiproductsRateplansCreateArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsApiproductsRateplansDeleteArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsApiproductsRateplansGetArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsApiproductsRateplansListArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsApiproductsRateplansUpdateArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsApiproductsUpdateArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsApisCreateArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsApisDebugsessionsListArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsApisDeleteArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsApisDeploymentsListArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsApisGetArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsApisKeyvaluemapsCreateArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsApisKeyvaluemapsDeleteArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsApisKeyvaluemapsEntriesCreateArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsApisKeyvaluemapsEntriesDeleteArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsApisKeyvaluemapsEntriesGetArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsApisKeyvaluemapsEntriesListArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsApisKeyvaluemapsEntriesUpdateArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsApisKeyvaluemapsGetArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsApisKeyvaluemapsUpdateArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsApisListArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsApisMoveArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsApisPatchArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsApisRevisionsDeleteArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsApisRevisionsDeploymentsListArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsApisRevisionsGetArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsApisRevisionsUpdateApiProxyRevisionArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsAppgroupsAppsCreateArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsAppgroupsAppsDeleteArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsAppgroupsAppsGetArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsAppgroupsAppsKeysApiproductsDeleteArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsAppgroupsAppsKeysApiproductsUpdateAppGroupAppKeyApiProductArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsAppgroupsAppsKeysCreateArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsAppgroupsAppsKeysDeleteArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsAppgroupsAppsKeysGetArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsAppgroupsAppsKeysUpdateAppGroupAppKeyArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsAppgroupsAppsListArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsAppgroupsAppsUpdateArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsAppgroupsBalanceAdjustArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsAppgroupsBalanceCreditArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsAppgroupsCreateArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsAppgroupsDeleteArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsAppgroupsGetArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsAppgroupsGetBalanceArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsAppgroupsGetMonetizationConfigArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsAppgroupsListArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsAppgroupsSubscriptionsCreateArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsAppgroupsSubscriptionsExpireArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsAppgroupsSubscriptionsGetArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsAppgroupsSubscriptionsListArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsAppgroupsUpdateArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsAppgroupsUpdateMonetizationConfigArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsAppsGetArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsAppsListArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsCreateArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsDatacollectorsCreateArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsDatacollectorsDeleteArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsDatacollectorsGetArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsDatacollectorsListArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsDatacollectorsPatchArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsDeleteArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsDeploymentsListArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsDevelopersAppsAttributesArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsDevelopersAppsAttributesDeleteArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsDevelopersAppsAttributesGetArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsDevelopersAppsAttributesListArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsDevelopersAppsAttributesUpdateDeveloperAppAttributeArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsDevelopersAppsCreateArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsDevelopersAppsDeleteArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsDevelopersAppsGenerateKeyPairOrUpdateDeveloperAppStatusArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsDevelopersAppsGetArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsDevelopersAppsKeysApiproductsDeleteArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsDevelopersAppsKeysApiproductsUpdateDeveloperAppKeyApiProductArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsDevelopersAppsKeysCreateArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsDevelopersAppsKeysCreateCreateArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsDevelopersAppsKeysDeleteArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsDevelopersAppsKeysGetArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsDevelopersAppsKeysReplaceDeveloperAppKeyArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsDevelopersAppsKeysUpdateDeveloperAppKeyArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsDevelopersAppsListArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsDevelopersAppsUpdateArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsDevelopersAttributesArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsDevelopersAttributesDeleteArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsDevelopersAttributesGetArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsDevelopersAttributesListArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsDevelopersAttributesUpdateDeveloperAttributeArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsDevelopersBalanceAdjustArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsDevelopersBalanceCreditArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsDevelopersCreateArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsDevelopersDeleteArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsDevelopersGetArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsDevelopersGetBalanceArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsDevelopersGetMonetizationConfigArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsDevelopersListArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsDevelopersSetDeveloperStatusArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsDevelopersSubscriptionsCreateArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsDevelopersSubscriptionsExpireArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsDevelopersSubscriptionsGetArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsDevelopersSubscriptionsListArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsDevelopersUpdateArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsDevelopersUpdateMonetizationConfigArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsDnsZonesCreateArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsDnsZonesDeleteArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsDnsZonesGetArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsDnsZonesListArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsEndpointAttachmentsCreateArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsEndpointAttachmentsDeleteArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsEndpointAttachmentsGetArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsEndpointAttachmentsListArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsEnvgroupsAttachmentsCreateArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsEnvgroupsAttachmentsDeleteArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsEnvgroupsAttachmentsGetArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsEnvgroupsAttachmentsListArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsEnvgroupsCreateArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsEnvgroupsDeleteArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsEnvgroupsGetArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsEnvgroupsGetDeployedIngressConfigArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsEnvgroupsListArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsEnvgroupsPatchArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsEnvironmentsAddonsConfigSetAddonEnablementArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsEnvironmentsAnalyticsAdminGetSchemav2Args;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsEnvironmentsAnalyticsExportsCreateArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsEnvironmentsAnalyticsExportsGetArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsEnvironmentsAnalyticsExportsListArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsEnvironmentsApisDeploymentsListArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsEnvironmentsApisRevisionsDebugsessionsCreateArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsEnvironmentsApisRevisionsDebugsessionsDataGetArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsEnvironmentsApisRevisionsDebugsessionsDeleteDataArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsEnvironmentsApisRevisionsDebugsessionsGetArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsEnvironmentsApisRevisionsDebugsessionsListArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsEnvironmentsApisRevisionsDeployArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsEnvironmentsApisRevisionsDeploymentsGenerateDeployChangeReportArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsEnvironmentsApisRevisionsDeploymentsGenerateUndeployChangeReportArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsEnvironmentsApisRevisionsGetDeploymentsArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsEnvironmentsApisRevisionsUndeployArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsEnvironmentsArchiveDeploymentsCreateArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsEnvironmentsArchiveDeploymentsDeleteArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsEnvironmentsArchiveDeploymentsGenerateDownloadUrlArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsEnvironmentsArchiveDeploymentsGenerateUploadUrlArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsEnvironmentsArchiveDeploymentsGetArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsEnvironmentsArchiveDeploymentsListArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsEnvironmentsArchiveDeploymentsPatchArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsEnvironmentsCachesDeleteArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsEnvironmentsCreateArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsEnvironmentsDeleteArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsEnvironmentsDeploymentsGetArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsEnvironmentsDeploymentsGetIamPolicyArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsEnvironmentsDeploymentsListArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsEnvironmentsDeploymentsSetIamPolicyArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsEnvironmentsDeploymentsTestIamPermissionsArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsEnvironmentsFlowhooksAttachSharedFlowToFlowHookArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsEnvironmentsFlowhooksDetachSharedFlowFromFlowHookArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsEnvironmentsFlowhooksGetArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsEnvironmentsGetAddonsConfigArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsEnvironmentsGetApiSecurityRuntimeConfigArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsEnvironmentsGetArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsEnvironmentsGetDebugmaskArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsEnvironmentsGetDeployedConfigArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsEnvironmentsGetIamPolicyArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsEnvironmentsGetSecurityActionsConfigArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsEnvironmentsGetTraceConfigArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsEnvironmentsKeystoresAliasesCreateArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsEnvironmentsKeystoresAliasesCsrArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsEnvironmentsKeystoresAliasesDeleteArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsEnvironmentsKeystoresAliasesGetArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsEnvironmentsKeystoresAliasesGetCertificateArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsEnvironmentsKeystoresAliasesUpdateArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsEnvironmentsKeystoresCreateArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsEnvironmentsKeystoresDeleteArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsEnvironmentsKeystoresGetArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsEnvironmentsKeyvaluemapsCreateArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsEnvironmentsKeyvaluemapsDeleteArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsEnvironmentsKeyvaluemapsEntriesCreateArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsEnvironmentsKeyvaluemapsEntriesDeleteArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsEnvironmentsKeyvaluemapsEntriesGetArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsEnvironmentsKeyvaluemapsEntriesListArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsEnvironmentsKeyvaluemapsEntriesUpdateArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsEnvironmentsKeyvaluemapsGetArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsEnvironmentsKeyvaluemapsUpdateArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsEnvironmentsModifyEnvironmentArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsEnvironmentsOptimizedStatsGetArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsEnvironmentsQueriesCreateArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsEnvironmentsQueriesGetArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsEnvironmentsQueriesGetResultArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsEnvironmentsQueriesGetResulturlArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsEnvironmentsQueriesListArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsEnvironmentsReferencesCreateArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsEnvironmentsReferencesDeleteArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsEnvironmentsReferencesGetArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsEnvironmentsReferencesUpdateArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsEnvironmentsResourcefilesCreateArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsEnvironmentsResourcefilesDeleteArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsEnvironmentsResourcefilesGetArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsEnvironmentsResourcefilesListArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsEnvironmentsResourcefilesListEnvironmentResourcesArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsEnvironmentsResourcefilesUpdateArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsEnvironmentsSecurityActionsCreateArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsEnvironmentsSecurityActionsDeleteArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsEnvironmentsSecurityActionsDisableArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsEnvironmentsSecurityActionsEnableArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsEnvironmentsSecurityActionsGetArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsEnvironmentsSecurityActionsListArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsEnvironmentsSecurityActionsPatchArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsEnvironmentsSecurityIncidentsBatchUpdateArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsEnvironmentsSecurityIncidentsGetArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsEnvironmentsSecurityIncidentsListArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsEnvironmentsSecurityIncidentsPatchArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsEnvironmentsSecurityReportsCreateArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsEnvironmentsSecurityReportsGetArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsEnvironmentsSecurityReportsGetResultArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsEnvironmentsSecurityReportsGetResultViewArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsEnvironmentsSecurityReportsListArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsEnvironmentsSecurityStatsQueryTabularStatsArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsEnvironmentsSecurityStatsQueryTimeSeriesStatsArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsEnvironmentsSetIamPolicyArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsEnvironmentsSharedflowsDeploymentsListArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsEnvironmentsSharedflowsRevisionsDeployArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsEnvironmentsSharedflowsRevisionsGetDeploymentsArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsEnvironmentsSharedflowsRevisionsUndeployArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsEnvironmentsStatsGetArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsEnvironmentsSubscribeArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsEnvironmentsTargetserversCreateArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsEnvironmentsTargetserversDeleteArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsEnvironmentsTargetserversGetArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsEnvironmentsTargetserversUpdateArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsEnvironmentsTestIamPermissionsArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsEnvironmentsTraceConfigOverridesCreateArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsEnvironmentsTraceConfigOverridesDeleteArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsEnvironmentsTraceConfigOverridesGetArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsEnvironmentsTraceConfigOverridesListArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsEnvironmentsTraceConfigOverridesPatchArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsEnvironmentsUnsubscribeArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsEnvironmentsUpdateArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsEnvironmentsUpdateDebugmaskArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsEnvironmentsUpdateEnvironmentArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsEnvironmentsUpdateSecurityActionsConfigArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsEnvironmentsUpdateTraceConfigArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsGetArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsGetControlPlaneAccessArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsGetDeployedIngressConfigArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsGetProjectMappingArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsGetRuntimeConfigArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsGetSecuritySettingsArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsGetSyncAuthorizationArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsHostQueriesCreateArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsHostQueriesGetArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsHostQueriesGetResultArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsHostQueriesGetResultViewArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsHostQueriesListArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsHostSecurityReportsCreateArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsHostSecurityReportsGetArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsHostSecurityReportsGetResultArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsHostSecurityReportsGetResultViewArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsHostSecurityReportsListArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsHostStatsGetArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsInstancesAttachmentsCreateArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsInstancesAttachmentsDeleteArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsInstancesAttachmentsGetArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsInstancesAttachmentsListArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsInstancesCanaryevaluationsCreateArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsInstancesCanaryevaluationsGetArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsInstancesCreateArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsInstancesDeleteArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsInstancesGetArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsInstancesListArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsInstancesNatAddressesActivateArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsInstancesNatAddressesCreateArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsInstancesNatAddressesDeleteArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsInstancesNatAddressesGetArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsInstancesNatAddressesListArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsInstancesPatchArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsInstancesReportStatusArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsKeyvaluemapsCreateArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsKeyvaluemapsDeleteArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsKeyvaluemapsEntriesCreateArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsKeyvaluemapsEntriesDeleteArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsKeyvaluemapsEntriesGetArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsKeyvaluemapsEntriesListArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsKeyvaluemapsEntriesUpdateArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsKeyvaluemapsGetArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsKeyvaluemapsUpdateArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsOperationsGetArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsOperationsListArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsOptimizedHostStatsGetArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsReportsCreateArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsReportsDeleteArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsReportsGetArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsReportsListArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsReportsUpdateArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsSecurityAssessmentResultsBatchComputeArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsSecurityFeedbackCreateArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsSecurityFeedbackDeleteArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsSecurityFeedbackGetArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsSecurityFeedbackListArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsSecurityFeedbackPatchArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsSecurityMonitoringConditionsCreateArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsSecurityMonitoringConditionsDeleteArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsSecurityMonitoringConditionsGetArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsSecurityMonitoringConditionsListArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsSecurityMonitoringConditionsPatchArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsSecurityProfilesCreateArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsSecurityProfilesDeleteArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsSecurityProfilesEnvironmentsComputeEnvironmentScoresArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsSecurityProfilesEnvironmentsCreateArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsSecurityProfilesEnvironmentsDeleteArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsSecurityProfilesGetArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsSecurityProfilesListArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsSecurityProfilesListRevisionsArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsSecurityProfilesPatchArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsSecurityProfilesV2CreateArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsSecurityProfilesV2DeleteArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsSecurityProfilesV2GetArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsSecurityProfilesV2ListArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsSecurityProfilesV2PatchArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsSetAddonsArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsSetSyncAuthorizationArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsSharedflowsCreateArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsSharedflowsDeleteArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsSharedflowsDeploymentsListArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsSharedflowsGetArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsSharedflowsListArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsSharedflowsMoveArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsSharedflowsRevisionsDeleteArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsSharedflowsRevisionsDeploymentsListArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsSharedflowsRevisionsGetArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsSharedflowsRevisionsUpdateSharedFlowRevisionArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsSitesApicategoriesCreateArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsSitesApicategoriesDeleteArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsSitesApicategoriesGetArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsSitesApicategoriesListArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsSitesApicategoriesPatchArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsSitesApidocsCreateArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsSitesApidocsDeleteArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsSitesApidocsGetArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsSitesApidocsGetDocumentationArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsSitesApidocsListArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsSitesApidocsUpdateArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsSitesApidocsUpdateDocumentationArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsSpacesCreateArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsSpacesDeleteArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsSpacesGetArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsSpacesGetIamPolicyArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsSpacesListArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsSpacesPatchArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsSpacesSetIamPolicyArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsSpacesTestIamPermissionsArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsUpdateArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsUpdateControlPlaneAccessArgs;
use crate::providers::gcp::clients::apigee::ApigeeOrganizationsUpdateSecuritySettingsArgs;
use crate::providers::gcp::clients::apigee::ApigeeProjectsProvisionOrganizationArgs;
use crate::provider_client::{ProviderClient, ProviderError};
use foundation_core::valtron::{execute, StreamIterator};
use foundation_core::wire::simple_http::client::{SimpleHttpClient, DnsResolver};
use foundation_db::state::store_state_task::StoreStateIdentifierTask;
use std::sync::Arc;

/// ApigeeProvider with automatic state tracking.
///
/// # Type Parameters
///
/// * `S` - StateStore implementation (FileStateStore, SqliteStateStore, etc.)
/// * `R` - DNS resolver type for HTTP client
///
/// # Example
///
/// ```rust
/// let state_store = FileStateStore::new("/path", "my-project", "dev");
/// let http_client = SimpleHttpClient::with_resolver(StaticSocketAddr::new(addr));
/// let client = ProviderClient::new("my-project", "dev", state_store, http_client);
/// let provider = ApigeeProvider::from_provider_client(client);
/// ```
#[derive(Clone)]
pub struct ApigeeProvider<S, R>
where
    S: foundation_db::state::traits::StateStore + Send + Sync + 'static,
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + 'static,
{
    client: ProviderClient<S, R>,
    http_client: Arc<SimpleHttpClient<R>>,
}

impl<S, R> ApigeeProvider<S, R>
where
    S: foundation_db::state::traits::StateStore + Send + Sync + 'static,
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + 'static,
{
    /// Create new ApigeeProvider.
    pub fn new(client: ProviderClient<S, R>, http_client: Arc<SimpleHttpClient<R>>) -> Self {
        Self {
            client,
            http_client,
        }
    }

    /// Create new ApigeeProvider from ProviderClient, extracting the HTTP client.
    ///
    /// This is a convenience method that calls `Self::new()` with `client.http_client()`.
    pub fn from_provider_client(client: ProviderClient<S, R>) -> Self {
        Self::new(client, client.http_client.clone())
    }

    /// Apigee hybrid issuers list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1ListHybridIssuersResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn apigee_hybrid_issuers_list(
        &self,
        args: &ApigeeHybridIssuersListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1ListHybridIssuersResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_hybrid_issuers_list_builder(
            &self.http_client,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_hybrid_issuers_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn apigee_organizations_create(
        &self,
        args: &ApigeeOrganizationsCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_create_builder(
            &self.http_client,
            &args.parent,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn apigee_organizations_delete(
        &self,
        args: &ApigeeOrganizationsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_delete_builder(
            &self.http_client,
            &args.name,
            &args.retention,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1Organization result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn apigee_organizations_get(
        &self,
        args: &ApigeeOrganizationsGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1Organization, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_get_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations get control plane access.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1ControlPlaneAccess result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn apigee_organizations_get_control_plane_access(
        &self,
        args: &ApigeeOrganizationsGetControlPlaneAccessArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1ControlPlaneAccess, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_get_control_plane_access_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_get_control_plane_access_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations get deployed ingress config.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1IngressConfig result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn apigee_organizations_get_deployed_ingress_config(
        &self,
        args: &ApigeeOrganizationsGetDeployedIngressConfigArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1IngressConfig, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_get_deployed_ingress_config_builder(
            &self.http_client,
            &args.name,
            &args.view,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_get_deployed_ingress_config_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations get project mapping.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1OrganizationProjectMapping result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn apigee_organizations_get_project_mapping(
        &self,
        args: &ApigeeOrganizationsGetProjectMappingArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1OrganizationProjectMapping, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_get_project_mapping_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_get_project_mapping_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations get runtime config.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1RuntimeConfig result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn apigee_organizations_get_runtime_config(
        &self,
        args: &ApigeeOrganizationsGetRuntimeConfigArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1RuntimeConfig, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_get_runtime_config_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_get_runtime_config_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations get security settings.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1SecuritySettings result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn apigee_organizations_get_security_settings(
        &self,
        args: &ApigeeOrganizationsGetSecuritySettingsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1SecuritySettings, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_get_security_settings_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_get_security_settings_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations get sync authorization.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1SyncAuthorization result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn apigee_organizations_get_sync_authorization(
        &self,
        args: &ApigeeOrganizationsGetSyncAuthorizationArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1SyncAuthorization, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_get_sync_authorization_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_get_sync_authorization_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1ListOrganizationsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn apigee_organizations_list(
        &self,
        args: &ApigeeOrganizationsListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1ListOrganizationsResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_list_builder(
            &self.http_client,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations set addons.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn apigee_organizations_set_addons(
        &self,
        args: &ApigeeOrganizationsSetAddonsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_set_addons_builder(
            &self.http_client,
            &args.org,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_set_addons_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations set sync authorization.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1SyncAuthorization result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn apigee_organizations_set_sync_authorization(
        &self,
        args: &ApigeeOrganizationsSetSyncAuthorizationArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1SyncAuthorization, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_set_sync_authorization_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_set_sync_authorization_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations update.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1Organization result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn apigee_organizations_update(
        &self,
        args: &ApigeeOrganizationsUpdateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1Organization, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_update_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_update_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations update control plane access.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn apigee_organizations_update_control_plane_access(
        &self,
        args: &ApigeeOrganizationsUpdateControlPlaneAccessArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_update_control_plane_access_builder(
            &self.http_client,
            &args.name,
            &args.updateMask,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_update_control_plane_access_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations update security settings.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1SecuritySettings result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn apigee_organizations_update_security_settings(
        &self,
        args: &ApigeeOrganizationsUpdateSecuritySettingsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1SecuritySettings, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_update_security_settings_builder(
            &self.http_client,
            &args.name,
            &args.updateMask,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_update_security_settings_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations analytics datastores create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1Datastore result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn apigee_organizations_analytics_datastores_create(
        &self,
        args: &ApigeeOrganizationsAnalyticsDatastoresCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1Datastore, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_analytics_datastores_create_builder(
            &self.http_client,
            &args.parent,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_analytics_datastores_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations analytics datastores delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn apigee_organizations_analytics_datastores_delete(
        &self,
        args: &ApigeeOrganizationsAnalyticsDatastoresDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_analytics_datastores_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_analytics_datastores_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations analytics datastores get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1Datastore result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn apigee_organizations_analytics_datastores_get(
        &self,
        args: &ApigeeOrganizationsAnalyticsDatastoresGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1Datastore, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_analytics_datastores_get_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_analytics_datastores_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations analytics datastores list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1ListDatastoresResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn apigee_organizations_analytics_datastores_list(
        &self,
        args: &ApigeeOrganizationsAnalyticsDatastoresListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1ListDatastoresResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_analytics_datastores_list_builder(
            &self.http_client,
            &args.parent,
            &args.targetType,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_analytics_datastores_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations analytics datastores test.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1TestDatastoreResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn apigee_organizations_analytics_datastores_test(
        &self,
        args: &ApigeeOrganizationsAnalyticsDatastoresTestArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1TestDatastoreResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_analytics_datastores_test_builder(
            &self.http_client,
            &args.parent,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_analytics_datastores_test_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations analytics datastores update.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1Datastore result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn apigee_organizations_analytics_datastores_update(
        &self,
        args: &ApigeeOrganizationsAnalyticsDatastoresUpdateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1Datastore, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_analytics_datastores_update_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_analytics_datastores_update_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations apim service extensions create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn apigee_organizations_apim_service_extensions_create(
        &self,
        args: &ApigeeOrganizationsApimServiceExtensionsCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_apim_service_extensions_create_builder(
            &self.http_client,
            &args.parent,
            &args.apimServiceExtensionId,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_apim_service_extensions_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations apim service extensions delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn apigee_organizations_apim_service_extensions_delete(
        &self,
        args: &ApigeeOrganizationsApimServiceExtensionsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_apim_service_extensions_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_apim_service_extensions_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations apim service extensions get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1ApimServiceExtension result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn apigee_organizations_apim_service_extensions_get(
        &self,
        args: &ApigeeOrganizationsApimServiceExtensionsGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1ApimServiceExtension, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_apim_service_extensions_get_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_apim_service_extensions_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations apim service extensions list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1ListApimServiceExtensionsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn apigee_organizations_apim_service_extensions_list(
        &self,
        args: &ApigeeOrganizationsApimServiceExtensionsListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1ListApimServiceExtensionsResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_apim_service_extensions_list_builder(
            &self.http_client,
            &args.parent,
            &args.pageSize,
            &args.pageToken,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_apim_service_extensions_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations apim service extensions patch.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn apigee_organizations_apim_service_extensions_patch(
        &self,
        args: &ApigeeOrganizationsApimServiceExtensionsPatchArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_apim_service_extensions_patch_builder(
            &self.http_client,
            &args.name,
            &args.allowMissing,
            &args.updateMask,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_apim_service_extensions_patch_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations apiproducts attributes.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1Attributes result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn apigee_organizations_apiproducts_attributes(
        &self,
        args: &ApigeeOrganizationsApiproductsAttributesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1Attributes, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_apiproducts_attributes_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_apiproducts_attributes_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations apiproducts create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1ApiProduct result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn apigee_organizations_apiproducts_create(
        &self,
        args: &ApigeeOrganizationsApiproductsCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1ApiProduct, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_apiproducts_create_builder(
            &self.http_client,
            &args.parent,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_apiproducts_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations apiproducts delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1ApiProduct result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn apigee_organizations_apiproducts_delete(
        &self,
        args: &ApigeeOrganizationsApiproductsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1ApiProduct, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_apiproducts_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_apiproducts_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations apiproducts get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1ApiProduct result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn apigee_organizations_apiproducts_get(
        &self,
        args: &ApigeeOrganizationsApiproductsGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1ApiProduct, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_apiproducts_get_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_apiproducts_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations apiproducts list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1ListApiProductsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn apigee_organizations_apiproducts_list(
        &self,
        args: &ApigeeOrganizationsApiproductsListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1ListApiProductsResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_apiproducts_list_builder(
            &self.http_client,
            &args.parent,
            &args.attributename,
            &args.attributevalue,
            &args.count,
            &args.expand,
            &args.space,
            &args.startKey,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_apiproducts_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations apiproducts move.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1ApiProduct result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn apigee_organizations_apiproducts_move(
        &self,
        args: &ApigeeOrganizationsApiproductsMoveArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1ApiProduct, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_apiproducts_move_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_apiproducts_move_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations apiproducts update.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1ApiProduct result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn apigee_organizations_apiproducts_update(
        &self,
        args: &ApigeeOrganizationsApiproductsUpdateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1ApiProduct, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_apiproducts_update_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_apiproducts_update_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations apiproducts attributes delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1Attribute result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn apigee_organizations_apiproducts_attributes_delete(
        &self,
        args: &ApigeeOrganizationsApiproductsAttributesDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1Attribute, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_apiproducts_attributes_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_apiproducts_attributes_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations apiproducts attributes get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1Attribute result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn apigee_organizations_apiproducts_attributes_get(
        &self,
        args: &ApigeeOrganizationsApiproductsAttributesGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1Attribute, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_apiproducts_attributes_get_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_apiproducts_attributes_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations apiproducts attributes list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1Attributes result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn apigee_organizations_apiproducts_attributes_list(
        &self,
        args: &ApigeeOrganizationsApiproductsAttributesListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1Attributes, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_apiproducts_attributes_list_builder(
            &self.http_client,
            &args.parent,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_apiproducts_attributes_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations apiproducts attributes update api product attribute.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1Attribute result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn apigee_organizations_apiproducts_attributes_update_api_product_attribute(
        &self,
        args: &ApigeeOrganizationsApiproductsAttributesUpdateApiProductAttributeArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1Attribute, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_apiproducts_attributes_update_api_product_attribute_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_apiproducts_attributes_update_api_product_attribute_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations apiproducts rateplans create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1RatePlan result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn apigee_organizations_apiproducts_rateplans_create(
        &self,
        args: &ApigeeOrganizationsApiproductsRateplansCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1RatePlan, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_apiproducts_rateplans_create_builder(
            &self.http_client,
            &args.parent,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_apiproducts_rateplans_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations apiproducts rateplans delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1RatePlan result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn apigee_organizations_apiproducts_rateplans_delete(
        &self,
        args: &ApigeeOrganizationsApiproductsRateplansDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1RatePlan, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_apiproducts_rateplans_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_apiproducts_rateplans_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations apiproducts rateplans get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1RatePlan result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn apigee_organizations_apiproducts_rateplans_get(
        &self,
        args: &ApigeeOrganizationsApiproductsRateplansGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1RatePlan, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_apiproducts_rateplans_get_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_apiproducts_rateplans_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations apiproducts rateplans list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1ListRatePlansResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn apigee_organizations_apiproducts_rateplans_list(
        &self,
        args: &ApigeeOrganizationsApiproductsRateplansListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1ListRatePlansResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_apiproducts_rateplans_list_builder(
            &self.http_client,
            &args.parent,
            &args.count,
            &args.expand,
            &args.orderBy,
            &args.startKey,
            &args.state,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_apiproducts_rateplans_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations apiproducts rateplans update.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1RatePlan result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn apigee_organizations_apiproducts_rateplans_update(
        &self,
        args: &ApigeeOrganizationsApiproductsRateplansUpdateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1RatePlan, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_apiproducts_rateplans_update_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_apiproducts_rateplans_update_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations apis create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1ApiProxyRevision result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn apigee_organizations_apis_create(
        &self,
        args: &ApigeeOrganizationsApisCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1ApiProxyRevision, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_apis_create_builder(
            &self.http_client,
            &args.parent,
            &args.action,
            &args.name,
            &args.space,
            &args.validate,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_apis_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations apis delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1ApiProxy result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn apigee_organizations_apis_delete(
        &self,
        args: &ApigeeOrganizationsApisDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1ApiProxy, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_apis_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_apis_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations apis get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1ApiProxy result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn apigee_organizations_apis_get(
        &self,
        args: &ApigeeOrganizationsApisGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1ApiProxy, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_apis_get_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_apis_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations apis list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1ListApiProxiesResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn apigee_organizations_apis_list(
        &self,
        args: &ApigeeOrganizationsApisListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1ListApiProxiesResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_apis_list_builder(
            &self.http_client,
            &args.parent,
            &args.includeMetaData,
            &args.includeRevisions,
            &args.space,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_apis_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations apis move.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1ApiProxy result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn apigee_organizations_apis_move(
        &self,
        args: &ApigeeOrganizationsApisMoveArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1ApiProxy, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_apis_move_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_apis_move_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations apis patch.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1ApiProxy result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn apigee_organizations_apis_patch(
        &self,
        args: &ApigeeOrganizationsApisPatchArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1ApiProxy, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_apis_patch_builder(
            &self.http_client,
            &args.name,
            &args.updateMask,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_apis_patch_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations apis debugsessions list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1ListApiDebugSessionsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn apigee_organizations_apis_debugsessions_list(
        &self,
        args: &ApigeeOrganizationsApisDebugsessionsListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1ListApiDebugSessionsResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_apis_debugsessions_list_builder(
            &self.http_client,
            &args.parent,
            &args.pageSize,
            &args.pageToken,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_apis_debugsessions_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations apis deployments list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1ListDeploymentsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn apigee_organizations_apis_deployments_list(
        &self,
        args: &ApigeeOrganizationsApisDeploymentsListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1ListDeploymentsResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_apis_deployments_list_builder(
            &self.http_client,
            &args.parent,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_apis_deployments_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations apis keyvaluemaps create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1KeyValueMap result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn apigee_organizations_apis_keyvaluemaps_create(
        &self,
        args: &ApigeeOrganizationsApisKeyvaluemapsCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1KeyValueMap, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_apis_keyvaluemaps_create_builder(
            &self.http_client,
            &args.parent,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_apis_keyvaluemaps_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations apis keyvaluemaps delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1KeyValueMap result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn apigee_organizations_apis_keyvaluemaps_delete(
        &self,
        args: &ApigeeOrganizationsApisKeyvaluemapsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1KeyValueMap, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_apis_keyvaluemaps_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_apis_keyvaluemaps_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations apis keyvaluemaps get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1KeyValueMap result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn apigee_organizations_apis_keyvaluemaps_get(
        &self,
        args: &ApigeeOrganizationsApisKeyvaluemapsGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1KeyValueMap, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_apis_keyvaluemaps_get_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_apis_keyvaluemaps_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations apis keyvaluemaps update.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1KeyValueMap result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn apigee_organizations_apis_keyvaluemaps_update(
        &self,
        args: &ApigeeOrganizationsApisKeyvaluemapsUpdateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1KeyValueMap, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_apis_keyvaluemaps_update_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_apis_keyvaluemaps_update_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations apis keyvaluemaps entries create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1KeyValueEntry result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn apigee_organizations_apis_keyvaluemaps_entries_create(
        &self,
        args: &ApigeeOrganizationsApisKeyvaluemapsEntriesCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1KeyValueEntry, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_apis_keyvaluemaps_entries_create_builder(
            &self.http_client,
            &args.parent,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_apis_keyvaluemaps_entries_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations apis keyvaluemaps entries delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1KeyValueEntry result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn apigee_organizations_apis_keyvaluemaps_entries_delete(
        &self,
        args: &ApigeeOrganizationsApisKeyvaluemapsEntriesDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1KeyValueEntry, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_apis_keyvaluemaps_entries_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_apis_keyvaluemaps_entries_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations apis keyvaluemaps entries get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1KeyValueEntry result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn apigee_organizations_apis_keyvaluemaps_entries_get(
        &self,
        args: &ApigeeOrganizationsApisKeyvaluemapsEntriesGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1KeyValueEntry, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_apis_keyvaluemaps_entries_get_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_apis_keyvaluemaps_entries_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations apis keyvaluemaps entries list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1ListKeyValueEntriesResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn apigee_organizations_apis_keyvaluemaps_entries_list(
        &self,
        args: &ApigeeOrganizationsApisKeyvaluemapsEntriesListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1ListKeyValueEntriesResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_apis_keyvaluemaps_entries_list_builder(
            &self.http_client,
            &args.parent,
            &args.pageSize,
            &args.pageToken,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_apis_keyvaluemaps_entries_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations apis keyvaluemaps entries update.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1KeyValueEntry result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn apigee_organizations_apis_keyvaluemaps_entries_update(
        &self,
        args: &ApigeeOrganizationsApisKeyvaluemapsEntriesUpdateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1KeyValueEntry, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_apis_keyvaluemaps_entries_update_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_apis_keyvaluemaps_entries_update_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations apis revisions delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1ApiProxyRevision result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn apigee_organizations_apis_revisions_delete(
        &self,
        args: &ApigeeOrganizationsApisRevisionsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1ApiProxyRevision, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_apis_revisions_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_apis_revisions_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations apis revisions get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleApiHttpBody result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn apigee_organizations_apis_revisions_get(
        &self,
        args: &ApigeeOrganizationsApisRevisionsGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleApiHttpBody, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_apis_revisions_get_builder(
            &self.http_client,
            &args.name,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_apis_revisions_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations apis revisions update api proxy revision.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1ApiProxyRevision result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn apigee_organizations_apis_revisions_update_api_proxy_revision(
        &self,
        args: &ApigeeOrganizationsApisRevisionsUpdateApiProxyRevisionArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1ApiProxyRevision, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_apis_revisions_update_api_proxy_revision_builder(
            &self.http_client,
            &args.name,
            &args.validate,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_apis_revisions_update_api_proxy_revision_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations apis revisions deployments list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1ListDeploymentsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn apigee_organizations_apis_revisions_deployments_list(
        &self,
        args: &ApigeeOrganizationsApisRevisionsDeploymentsListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1ListDeploymentsResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_apis_revisions_deployments_list_builder(
            &self.http_client,
            &args.parent,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_apis_revisions_deployments_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations appgroups create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1AppGroup result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn apigee_organizations_appgroups_create(
        &self,
        args: &ApigeeOrganizationsAppgroupsCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1AppGroup, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_appgroups_create_builder(
            &self.http_client,
            &args.parent,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_appgroups_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations appgroups delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1AppGroup result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn apigee_organizations_appgroups_delete(
        &self,
        args: &ApigeeOrganizationsAppgroupsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1AppGroup, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_appgroups_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_appgroups_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations appgroups get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1AppGroup result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn apigee_organizations_appgroups_get(
        &self,
        args: &ApigeeOrganizationsAppgroupsGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1AppGroup, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_appgroups_get_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_appgroups_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations appgroups get balance.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1AppGroupBalance result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn apigee_organizations_appgroups_get_balance(
        &self,
        args: &ApigeeOrganizationsAppgroupsGetBalanceArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1AppGroupBalance, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_appgroups_get_balance_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_appgroups_get_balance_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations appgroups get monetization config.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1AppGroupMonetizationConfig result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn apigee_organizations_appgroups_get_monetization_config(
        &self,
        args: &ApigeeOrganizationsAppgroupsGetMonetizationConfigArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1AppGroupMonetizationConfig, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_appgroups_get_monetization_config_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_appgroups_get_monetization_config_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations appgroups list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1ListAppGroupsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn apigee_organizations_appgroups_list(
        &self,
        args: &ApigeeOrganizationsAppgroupsListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1ListAppGroupsResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_appgroups_list_builder(
            &self.http_client,
            &args.parent,
            &args.filter,
            &args.pageSize,
            &args.pageToken,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_appgroups_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations appgroups update.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1AppGroup result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn apigee_organizations_appgroups_update(
        &self,
        args: &ApigeeOrganizationsAppgroupsUpdateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1AppGroup, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_appgroups_update_builder(
            &self.http_client,
            &args.name,
            &args.action,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_appgroups_update_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations appgroups update monetization config.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1AppGroupMonetizationConfig result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn apigee_organizations_appgroups_update_monetization_config(
        &self,
        args: &ApigeeOrganizationsAppgroupsUpdateMonetizationConfigArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1AppGroupMonetizationConfig, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_appgroups_update_monetization_config_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_appgroups_update_monetization_config_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations appgroups apps create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1AppGroupApp result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn apigee_organizations_appgroups_apps_create(
        &self,
        args: &ApigeeOrganizationsAppgroupsAppsCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1AppGroupApp, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_appgroups_apps_create_builder(
            &self.http_client,
            &args.parent,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_appgroups_apps_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations appgroups apps delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1AppGroupApp result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn apigee_organizations_appgroups_apps_delete(
        &self,
        args: &ApigeeOrganizationsAppgroupsAppsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1AppGroupApp, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_appgroups_apps_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_appgroups_apps_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations appgroups apps get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1AppGroupApp result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn apigee_organizations_appgroups_apps_get(
        &self,
        args: &ApigeeOrganizationsAppgroupsAppsGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1AppGroupApp, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_appgroups_apps_get_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_appgroups_apps_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations appgroups apps list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1ListAppGroupAppsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn apigee_organizations_appgroups_apps_list(
        &self,
        args: &ApigeeOrganizationsAppgroupsAppsListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1ListAppGroupAppsResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_appgroups_apps_list_builder(
            &self.http_client,
            &args.parent,
            &args.pageSize,
            &args.pageToken,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_appgroups_apps_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations appgroups apps update.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1AppGroupApp result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn apigee_organizations_appgroups_apps_update(
        &self,
        args: &ApigeeOrganizationsAppgroupsAppsUpdateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1AppGroupApp, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_appgroups_apps_update_builder(
            &self.http_client,
            &args.name,
            &args.action,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_appgroups_apps_update_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations appgroups apps keys create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1AppGroupAppKey result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn apigee_organizations_appgroups_apps_keys_create(
        &self,
        args: &ApigeeOrganizationsAppgroupsAppsKeysCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1AppGroupAppKey, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_appgroups_apps_keys_create_builder(
            &self.http_client,
            &args.parent,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_appgroups_apps_keys_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations appgroups apps keys delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1AppGroupAppKey result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn apigee_organizations_appgroups_apps_keys_delete(
        &self,
        args: &ApigeeOrganizationsAppgroupsAppsKeysDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1AppGroupAppKey, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_appgroups_apps_keys_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_appgroups_apps_keys_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations appgroups apps keys get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1AppGroupAppKey result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn apigee_organizations_appgroups_apps_keys_get(
        &self,
        args: &ApigeeOrganizationsAppgroupsAppsKeysGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1AppGroupAppKey, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_appgroups_apps_keys_get_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_appgroups_apps_keys_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations appgroups apps keys update app group app key.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1AppGroupAppKey result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn apigee_organizations_appgroups_apps_keys_update_app_group_app_key(
        &self,
        args: &ApigeeOrganizationsAppgroupsAppsKeysUpdateAppGroupAppKeyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1AppGroupAppKey, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_appgroups_apps_keys_update_app_group_app_key_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_appgroups_apps_keys_update_app_group_app_key_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations appgroups apps keys apiproducts delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1AppGroupAppKey result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn apigee_organizations_appgroups_apps_keys_apiproducts_delete(
        &self,
        args: &ApigeeOrganizationsAppgroupsAppsKeysApiproductsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1AppGroupAppKey, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_appgroups_apps_keys_apiproducts_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_appgroups_apps_keys_apiproducts_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations appgroups apps keys apiproducts update app group app key api product.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn apigee_organizations_appgroups_apps_keys_apiproducts_update_app_group_app_key_api_product(
        &self,
        args: &ApigeeOrganizationsAppgroupsAppsKeysApiproductsUpdateAppGroupAppKeyApiProductArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_appgroups_apps_keys_apiproducts_update_app_group_app_key_api_product_builder(
            &self.http_client,
            &args.name,
            &args.action,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_appgroups_apps_keys_apiproducts_update_app_group_app_key_api_product_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations appgroups balance adjust.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1AppGroupBalance result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn apigee_organizations_appgroups_balance_adjust(
        &self,
        args: &ApigeeOrganizationsAppgroupsBalanceAdjustArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1AppGroupBalance, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_appgroups_balance_adjust_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_appgroups_balance_adjust_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations appgroups balance credit.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1AppGroupBalance result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn apigee_organizations_appgroups_balance_credit(
        &self,
        args: &ApigeeOrganizationsAppgroupsBalanceCreditArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1AppGroupBalance, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_appgroups_balance_credit_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_appgroups_balance_credit_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations appgroups subscriptions create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1AppGroupSubscription result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn apigee_organizations_appgroups_subscriptions_create(
        &self,
        args: &ApigeeOrganizationsAppgroupsSubscriptionsCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1AppGroupSubscription, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_appgroups_subscriptions_create_builder(
            &self.http_client,
            &args.parent,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_appgroups_subscriptions_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations appgroups subscriptions expire.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1AppGroupSubscription result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn apigee_organizations_appgroups_subscriptions_expire(
        &self,
        args: &ApigeeOrganizationsAppgroupsSubscriptionsExpireArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1AppGroupSubscription, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_appgroups_subscriptions_expire_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_appgroups_subscriptions_expire_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations appgroups subscriptions get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1AppGroupSubscription result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn apigee_organizations_appgroups_subscriptions_get(
        &self,
        args: &ApigeeOrganizationsAppgroupsSubscriptionsGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1AppGroupSubscription, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_appgroups_subscriptions_get_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_appgroups_subscriptions_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations appgroups subscriptions list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1ListAppGroupSubscriptionsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn apigee_organizations_appgroups_subscriptions_list(
        &self,
        args: &ApigeeOrganizationsAppgroupsSubscriptionsListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1ListAppGroupSubscriptionsResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_appgroups_subscriptions_list_builder(
            &self.http_client,
            &args.parent,
            &args.pageSize,
            &args.pageToken,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_appgroups_subscriptions_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations apps get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1App result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn apigee_organizations_apps_get(
        &self,
        args: &ApigeeOrganizationsAppsGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1App, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_apps_get_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_apps_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations apps list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1ListAppsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn apigee_organizations_apps_list(
        &self,
        args: &ApigeeOrganizationsAppsListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1ListAppsResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_apps_list_builder(
            &self.http_client,
            &args.parent,
            &args.apiProduct,
            &args.apptype,
            &args.expand,
            &args.filter,
            &args.ids,
            &args.includeCred,
            &args.keyStatus,
            &args.pageSize,
            &args.pageToken,
            &args.rows,
            &args.startKey,
            &args.status,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_apps_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations datacollectors create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1DataCollector result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn apigee_organizations_datacollectors_create(
        &self,
        args: &ApigeeOrganizationsDatacollectorsCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1DataCollector, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_datacollectors_create_builder(
            &self.http_client,
            &args.parent,
            &args.dataCollectorId,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_datacollectors_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations datacollectors delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn apigee_organizations_datacollectors_delete(
        &self,
        args: &ApigeeOrganizationsDatacollectorsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_datacollectors_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_datacollectors_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations datacollectors get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1DataCollector result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn apigee_organizations_datacollectors_get(
        &self,
        args: &ApigeeOrganizationsDatacollectorsGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1DataCollector, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_datacollectors_get_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_datacollectors_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations datacollectors list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1ListDataCollectorsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn apigee_organizations_datacollectors_list(
        &self,
        args: &ApigeeOrganizationsDatacollectorsListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1ListDataCollectorsResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_datacollectors_list_builder(
            &self.http_client,
            &args.parent,
            &args.pageSize,
            &args.pageToken,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_datacollectors_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations datacollectors patch.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1DataCollector result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn apigee_organizations_datacollectors_patch(
        &self,
        args: &ApigeeOrganizationsDatacollectorsPatchArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1DataCollector, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_datacollectors_patch_builder(
            &self.http_client,
            &args.name,
            &args.updateMask,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_datacollectors_patch_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations deployments list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1ListDeploymentsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn apigee_organizations_deployments_list(
        &self,
        args: &ApigeeOrganizationsDeploymentsListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1ListDeploymentsResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_deployments_list_builder(
            &self.http_client,
            &args.parent,
            &args.sharedFlows,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_deployments_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations developers attributes.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1Attributes result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn apigee_organizations_developers_attributes(
        &self,
        args: &ApigeeOrganizationsDevelopersAttributesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1Attributes, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_developers_attributes_builder(
            &self.http_client,
            &args.parent,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_developers_attributes_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations developers create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1Developer result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn apigee_organizations_developers_create(
        &self,
        args: &ApigeeOrganizationsDevelopersCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1Developer, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_developers_create_builder(
            &self.http_client,
            &args.parent,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_developers_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations developers delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1Developer result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn apigee_organizations_developers_delete(
        &self,
        args: &ApigeeOrganizationsDevelopersDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1Developer, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_developers_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_developers_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations developers get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1Developer result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn apigee_organizations_developers_get(
        &self,
        args: &ApigeeOrganizationsDevelopersGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1Developer, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_developers_get_builder(
            &self.http_client,
            &args.name,
            &args.action,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_developers_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations developers get balance.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1DeveloperBalance result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn apigee_organizations_developers_get_balance(
        &self,
        args: &ApigeeOrganizationsDevelopersGetBalanceArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1DeveloperBalance, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_developers_get_balance_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_developers_get_balance_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations developers get monetization config.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1DeveloperMonetizationConfig result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn apigee_organizations_developers_get_monetization_config(
        &self,
        args: &ApigeeOrganizationsDevelopersGetMonetizationConfigArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1DeveloperMonetizationConfig, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_developers_get_monetization_config_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_developers_get_monetization_config_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations developers list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1ListOfDevelopersResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn apigee_organizations_developers_list(
        &self,
        args: &ApigeeOrganizationsDevelopersListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1ListOfDevelopersResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_developers_list_builder(
            &self.http_client,
            &args.parent,
            &args.app,
            &args.count,
            &args.expand,
            &args.ids,
            &args.includeCompany,
            &args.startKey,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_developers_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations developers set developer status.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn apigee_organizations_developers_set_developer_status(
        &self,
        args: &ApigeeOrganizationsDevelopersSetDeveloperStatusArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_developers_set_developer_status_builder(
            &self.http_client,
            &args.name,
            &args.action,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_developers_set_developer_status_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations developers update.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1Developer result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn apigee_organizations_developers_update(
        &self,
        args: &ApigeeOrganizationsDevelopersUpdateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1Developer, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_developers_update_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_developers_update_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations developers update monetization config.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1DeveloperMonetizationConfig result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn apigee_organizations_developers_update_monetization_config(
        &self,
        args: &ApigeeOrganizationsDevelopersUpdateMonetizationConfigArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1DeveloperMonetizationConfig, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_developers_update_monetization_config_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_developers_update_monetization_config_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations developers apps attributes.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1Attributes result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn apigee_organizations_developers_apps_attributes(
        &self,
        args: &ApigeeOrganizationsDevelopersAppsAttributesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1Attributes, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_developers_apps_attributes_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_developers_apps_attributes_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations developers apps create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1DeveloperApp result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn apigee_organizations_developers_apps_create(
        &self,
        args: &ApigeeOrganizationsDevelopersAppsCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1DeveloperApp, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_developers_apps_create_builder(
            &self.http_client,
            &args.parent,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_developers_apps_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations developers apps delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1DeveloperApp result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn apigee_organizations_developers_apps_delete(
        &self,
        args: &ApigeeOrganizationsDevelopersAppsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1DeveloperApp, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_developers_apps_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_developers_apps_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations developers apps generate key pair or update developer app status.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1DeveloperApp result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn apigee_organizations_developers_apps_generate_key_pair_or_update_developer_app_status(
        &self,
        args: &ApigeeOrganizationsDevelopersAppsGenerateKeyPairOrUpdateDeveloperAppStatusArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1DeveloperApp, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_developers_apps_generate_key_pair_or_update_developer_app_status_builder(
            &self.http_client,
            &args.name,
            &args.action,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_developers_apps_generate_key_pair_or_update_developer_app_status_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations developers apps get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1DeveloperApp result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn apigee_organizations_developers_apps_get(
        &self,
        args: &ApigeeOrganizationsDevelopersAppsGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1DeveloperApp, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_developers_apps_get_builder(
            &self.http_client,
            &args.name,
            &args.entity,
            &args.query,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_developers_apps_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations developers apps list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1ListDeveloperAppsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn apigee_organizations_developers_apps_list(
        &self,
        args: &ApigeeOrganizationsDevelopersAppsListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1ListDeveloperAppsResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_developers_apps_list_builder(
            &self.http_client,
            &args.parent,
            &args.count,
            &args.expand,
            &args.shallowExpand,
            &args.startKey,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_developers_apps_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations developers apps update.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1DeveloperApp result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn apigee_organizations_developers_apps_update(
        &self,
        args: &ApigeeOrganizationsDevelopersAppsUpdateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1DeveloperApp, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_developers_apps_update_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_developers_apps_update_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations developers apps attributes delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1Attribute result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn apigee_organizations_developers_apps_attributes_delete(
        &self,
        args: &ApigeeOrganizationsDevelopersAppsAttributesDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1Attribute, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_developers_apps_attributes_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_developers_apps_attributes_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations developers apps attributes get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1Attribute result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn apigee_organizations_developers_apps_attributes_get(
        &self,
        args: &ApigeeOrganizationsDevelopersAppsAttributesGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1Attribute, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_developers_apps_attributes_get_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_developers_apps_attributes_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations developers apps attributes list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1Attributes result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn apigee_organizations_developers_apps_attributes_list(
        &self,
        args: &ApigeeOrganizationsDevelopersAppsAttributesListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1Attributes, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_developers_apps_attributes_list_builder(
            &self.http_client,
            &args.parent,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_developers_apps_attributes_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations developers apps attributes update developer app attribute.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1Attribute result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn apigee_organizations_developers_apps_attributes_update_developer_app_attribute(
        &self,
        args: &ApigeeOrganizationsDevelopersAppsAttributesUpdateDeveloperAppAttributeArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1Attribute, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_developers_apps_attributes_update_developer_app_attribute_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_developers_apps_attributes_update_developer_app_attribute_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations developers apps keys create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1DeveloperAppKey result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn apigee_organizations_developers_apps_keys_create(
        &self,
        args: &ApigeeOrganizationsDevelopersAppsKeysCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1DeveloperAppKey, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_developers_apps_keys_create_builder(
            &self.http_client,
            &args.parent,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_developers_apps_keys_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations developers apps keys delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1DeveloperAppKey result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn apigee_organizations_developers_apps_keys_delete(
        &self,
        args: &ApigeeOrganizationsDevelopersAppsKeysDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1DeveloperAppKey, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_developers_apps_keys_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_developers_apps_keys_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations developers apps keys get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1DeveloperAppKey result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn apigee_organizations_developers_apps_keys_get(
        &self,
        args: &ApigeeOrganizationsDevelopersAppsKeysGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1DeveloperAppKey, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_developers_apps_keys_get_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_developers_apps_keys_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations developers apps keys replace developer app key.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1DeveloperAppKey result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn apigee_organizations_developers_apps_keys_replace_developer_app_key(
        &self,
        args: &ApigeeOrganizationsDevelopersAppsKeysReplaceDeveloperAppKeyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1DeveloperAppKey, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_developers_apps_keys_replace_developer_app_key_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_developers_apps_keys_replace_developer_app_key_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations developers apps keys update developer app key.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1DeveloperAppKey result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn apigee_organizations_developers_apps_keys_update_developer_app_key(
        &self,
        args: &ApigeeOrganizationsDevelopersAppsKeysUpdateDeveloperAppKeyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1DeveloperAppKey, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_developers_apps_keys_update_developer_app_key_builder(
            &self.http_client,
            &args.name,
            &args.action,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_developers_apps_keys_update_developer_app_key_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations developers apps keys apiproducts delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1DeveloperAppKey result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn apigee_organizations_developers_apps_keys_apiproducts_delete(
        &self,
        args: &ApigeeOrganizationsDevelopersAppsKeysApiproductsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1DeveloperAppKey, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_developers_apps_keys_apiproducts_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_developers_apps_keys_apiproducts_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations developers apps keys apiproducts update developer app key api product.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn apigee_organizations_developers_apps_keys_apiproducts_update_developer_app_key_api_product(
        &self,
        args: &ApigeeOrganizationsDevelopersAppsKeysApiproductsUpdateDeveloperAppKeyApiProductArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_developers_apps_keys_apiproducts_update_developer_app_key_api_product_builder(
            &self.http_client,
            &args.name,
            &args.action,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_developers_apps_keys_apiproducts_update_developer_app_key_api_product_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations developers apps keys create create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1DeveloperAppKey result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn apigee_organizations_developers_apps_keys_create_create(
        &self,
        args: &ApigeeOrganizationsDevelopersAppsKeysCreateCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1DeveloperAppKey, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_developers_apps_keys_create_create_builder(
            &self.http_client,
            &args.parent,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_developers_apps_keys_create_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations developers attributes delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1Attribute result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn apigee_organizations_developers_attributes_delete(
        &self,
        args: &ApigeeOrganizationsDevelopersAttributesDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1Attribute, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_developers_attributes_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_developers_attributes_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations developers attributes get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1Attribute result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn apigee_organizations_developers_attributes_get(
        &self,
        args: &ApigeeOrganizationsDevelopersAttributesGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1Attribute, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_developers_attributes_get_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_developers_attributes_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations developers attributes list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1Attributes result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn apigee_organizations_developers_attributes_list(
        &self,
        args: &ApigeeOrganizationsDevelopersAttributesListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1Attributes, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_developers_attributes_list_builder(
            &self.http_client,
            &args.parent,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_developers_attributes_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations developers attributes update developer attribute.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1Attribute result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn apigee_organizations_developers_attributes_update_developer_attribute(
        &self,
        args: &ApigeeOrganizationsDevelopersAttributesUpdateDeveloperAttributeArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1Attribute, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_developers_attributes_update_developer_attribute_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_developers_attributes_update_developer_attribute_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations developers balance adjust.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1DeveloperBalance result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn apigee_organizations_developers_balance_adjust(
        &self,
        args: &ApigeeOrganizationsDevelopersBalanceAdjustArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1DeveloperBalance, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_developers_balance_adjust_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_developers_balance_adjust_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations developers balance credit.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1DeveloperBalance result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn apigee_organizations_developers_balance_credit(
        &self,
        args: &ApigeeOrganizationsDevelopersBalanceCreditArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1DeveloperBalance, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_developers_balance_credit_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_developers_balance_credit_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations developers subscriptions create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1DeveloperSubscription result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn apigee_organizations_developers_subscriptions_create(
        &self,
        args: &ApigeeOrganizationsDevelopersSubscriptionsCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1DeveloperSubscription, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_developers_subscriptions_create_builder(
            &self.http_client,
            &args.parent,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_developers_subscriptions_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations developers subscriptions expire.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1DeveloperSubscription result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn apigee_organizations_developers_subscriptions_expire(
        &self,
        args: &ApigeeOrganizationsDevelopersSubscriptionsExpireArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1DeveloperSubscription, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_developers_subscriptions_expire_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_developers_subscriptions_expire_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations developers subscriptions get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1DeveloperSubscription result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn apigee_organizations_developers_subscriptions_get(
        &self,
        args: &ApigeeOrganizationsDevelopersSubscriptionsGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1DeveloperSubscription, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_developers_subscriptions_get_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_developers_subscriptions_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations developers subscriptions list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1ListDeveloperSubscriptionsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn apigee_organizations_developers_subscriptions_list(
        &self,
        args: &ApigeeOrganizationsDevelopersSubscriptionsListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1ListDeveloperSubscriptionsResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_developers_subscriptions_list_builder(
            &self.http_client,
            &args.parent,
            &args.count,
            &args.startKey,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_developers_subscriptions_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations dns zones create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn apigee_organizations_dns_zones_create(
        &self,
        args: &ApigeeOrganizationsDnsZonesCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_dns_zones_create_builder(
            &self.http_client,
            &args.parent,
            &args.dnsZoneId,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_dns_zones_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations dns zones delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn apigee_organizations_dns_zones_delete(
        &self,
        args: &ApigeeOrganizationsDnsZonesDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_dns_zones_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_dns_zones_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations dns zones get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1DnsZone result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn apigee_organizations_dns_zones_get(
        &self,
        args: &ApigeeOrganizationsDnsZonesGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1DnsZone, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_dns_zones_get_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_dns_zones_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations dns zones list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1ListDnsZonesResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn apigee_organizations_dns_zones_list(
        &self,
        args: &ApigeeOrganizationsDnsZonesListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1ListDnsZonesResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_dns_zones_list_builder(
            &self.http_client,
            &args.parent,
            &args.pageSize,
            &args.pageToken,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_dns_zones_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations endpoint attachments create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn apigee_organizations_endpoint_attachments_create(
        &self,
        args: &ApigeeOrganizationsEndpointAttachmentsCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_endpoint_attachments_create_builder(
            &self.http_client,
            &args.parent,
            &args.endpointAttachmentId,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_endpoint_attachments_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations endpoint attachments delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn apigee_organizations_endpoint_attachments_delete(
        &self,
        args: &ApigeeOrganizationsEndpointAttachmentsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_endpoint_attachments_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_endpoint_attachments_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations endpoint attachments get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1EndpointAttachment result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn apigee_organizations_endpoint_attachments_get(
        &self,
        args: &ApigeeOrganizationsEndpointAttachmentsGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1EndpointAttachment, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_endpoint_attachments_get_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_endpoint_attachments_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations endpoint attachments list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1ListEndpointAttachmentsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn apigee_organizations_endpoint_attachments_list(
        &self,
        args: &ApigeeOrganizationsEndpointAttachmentsListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1ListEndpointAttachmentsResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_endpoint_attachments_list_builder(
            &self.http_client,
            &args.parent,
            &args.pageSize,
            &args.pageToken,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_endpoint_attachments_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations envgroups create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn apigee_organizations_envgroups_create(
        &self,
        args: &ApigeeOrganizationsEnvgroupsCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_envgroups_create_builder(
            &self.http_client,
            &args.parent,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_envgroups_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations envgroups delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn apigee_organizations_envgroups_delete(
        &self,
        args: &ApigeeOrganizationsEnvgroupsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_envgroups_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_envgroups_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations envgroups get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1EnvironmentGroup result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn apigee_organizations_envgroups_get(
        &self,
        args: &ApigeeOrganizationsEnvgroupsGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1EnvironmentGroup, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_envgroups_get_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_envgroups_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations envgroups get deployed ingress config.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1EnvironmentGroupConfig result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn apigee_organizations_envgroups_get_deployed_ingress_config(
        &self,
        args: &ApigeeOrganizationsEnvgroupsGetDeployedIngressConfigArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1EnvironmentGroupConfig, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_envgroups_get_deployed_ingress_config_builder(
            &self.http_client,
            &args.name,
            &args.view,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_envgroups_get_deployed_ingress_config_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations envgroups list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1ListEnvironmentGroupsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn apigee_organizations_envgroups_list(
        &self,
        args: &ApigeeOrganizationsEnvgroupsListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1ListEnvironmentGroupsResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_envgroups_list_builder(
            &self.http_client,
            &args.parent,
            &args.pageSize,
            &args.pageToken,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_envgroups_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations envgroups patch.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn apigee_organizations_envgroups_patch(
        &self,
        args: &ApigeeOrganizationsEnvgroupsPatchArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_envgroups_patch_builder(
            &self.http_client,
            &args.name,
            &args.updateMask,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_envgroups_patch_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations envgroups attachments create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn apigee_organizations_envgroups_attachments_create(
        &self,
        args: &ApigeeOrganizationsEnvgroupsAttachmentsCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_envgroups_attachments_create_builder(
            &self.http_client,
            &args.parent,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_envgroups_attachments_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations envgroups attachments delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn apigee_organizations_envgroups_attachments_delete(
        &self,
        args: &ApigeeOrganizationsEnvgroupsAttachmentsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_envgroups_attachments_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_envgroups_attachments_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations envgroups attachments get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1EnvironmentGroupAttachment result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn apigee_organizations_envgroups_attachments_get(
        &self,
        args: &ApigeeOrganizationsEnvgroupsAttachmentsGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1EnvironmentGroupAttachment, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_envgroups_attachments_get_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_envgroups_attachments_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations envgroups attachments list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1ListEnvironmentGroupAttachmentsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn apigee_organizations_envgroups_attachments_list(
        &self,
        args: &ApigeeOrganizationsEnvgroupsAttachmentsListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1ListEnvironmentGroupAttachmentsResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_envgroups_attachments_list_builder(
            &self.http_client,
            &args.parent,
            &args.pageSize,
            &args.pageToken,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_envgroups_attachments_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations environments create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn apigee_organizations_environments_create(
        &self,
        args: &ApigeeOrganizationsEnvironmentsCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_environments_create_builder(
            &self.http_client,
            &args.parent,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_environments_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations environments delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn apigee_organizations_environments_delete(
        &self,
        args: &ApigeeOrganizationsEnvironmentsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_environments_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_environments_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations environments get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1Environment result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn apigee_organizations_environments_get(
        &self,
        args: &ApigeeOrganizationsEnvironmentsGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1Environment, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_environments_get_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_environments_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations environments get addons config.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1AddonsConfig result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn apigee_organizations_environments_get_addons_config(
        &self,
        args: &ApigeeOrganizationsEnvironmentsGetAddonsConfigArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1AddonsConfig, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_environments_get_addons_config_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_environments_get_addons_config_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations environments get api security runtime config.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1ApiSecurityRuntimeConfig result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn apigee_organizations_environments_get_api_security_runtime_config(
        &self,
        args: &ApigeeOrganizationsEnvironmentsGetApiSecurityRuntimeConfigArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1ApiSecurityRuntimeConfig, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_environments_get_api_security_runtime_config_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_environments_get_api_security_runtime_config_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations environments get debugmask.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1DebugMask result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn apigee_organizations_environments_get_debugmask(
        &self,
        args: &ApigeeOrganizationsEnvironmentsGetDebugmaskArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1DebugMask, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_environments_get_debugmask_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_environments_get_debugmask_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations environments get deployed config.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1EnvironmentConfig result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn apigee_organizations_environments_get_deployed_config(
        &self,
        args: &ApigeeOrganizationsEnvironmentsGetDeployedConfigArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1EnvironmentConfig, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_environments_get_deployed_config_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_environments_get_deployed_config_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations environments get iam policy.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleIamV1Policy result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn apigee_organizations_environments_get_iam_policy(
        &self,
        args: &ApigeeOrganizationsEnvironmentsGetIamPolicyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleIamV1Policy, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_environments_get_iam_policy_builder(
            &self.http_client,
            &args.resource,
            &args.options_requestedPolicyVersion,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_environments_get_iam_policy_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations environments get security actions config.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1SecurityActionsConfig result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn apigee_organizations_environments_get_security_actions_config(
        &self,
        args: &ApigeeOrganizationsEnvironmentsGetSecurityActionsConfigArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1SecurityActionsConfig, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_environments_get_security_actions_config_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_environments_get_security_actions_config_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations environments get trace config.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1TraceConfig result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn apigee_organizations_environments_get_trace_config(
        &self,
        args: &ApigeeOrganizationsEnvironmentsGetTraceConfigArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1TraceConfig, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_environments_get_trace_config_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_environments_get_trace_config_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations environments modify environment.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn apigee_organizations_environments_modify_environment(
        &self,
        args: &ApigeeOrganizationsEnvironmentsModifyEnvironmentArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_environments_modify_environment_builder(
            &self.http_client,
            &args.name,
            &args.updateMask,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_environments_modify_environment_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations environments set iam policy.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleIamV1Policy result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn apigee_organizations_environments_set_iam_policy(
        &self,
        args: &ApigeeOrganizationsEnvironmentsSetIamPolicyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleIamV1Policy, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_environments_set_iam_policy_builder(
            &self.http_client,
            &args.resource,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_environments_set_iam_policy_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations environments subscribe.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1Subscription result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn apigee_organizations_environments_subscribe(
        &self,
        args: &ApigeeOrganizationsEnvironmentsSubscribeArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1Subscription, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_environments_subscribe_builder(
            &self.http_client,
            &args.parent,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_environments_subscribe_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations environments test iam permissions.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleIamV1TestIamPermissionsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn apigee_organizations_environments_test_iam_permissions(
        &self,
        args: &ApigeeOrganizationsEnvironmentsTestIamPermissionsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleIamV1TestIamPermissionsResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_environments_test_iam_permissions_builder(
            &self.http_client,
            &args.resource,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_environments_test_iam_permissions_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations environments unsubscribe.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn apigee_organizations_environments_unsubscribe(
        &self,
        args: &ApigeeOrganizationsEnvironmentsUnsubscribeArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_environments_unsubscribe_builder(
            &self.http_client,
            &args.parent,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_environments_unsubscribe_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations environments update.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1Environment result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn apigee_organizations_environments_update(
        &self,
        args: &ApigeeOrganizationsEnvironmentsUpdateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1Environment, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_environments_update_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_environments_update_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations environments update debugmask.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1DebugMask result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn apigee_organizations_environments_update_debugmask(
        &self,
        args: &ApigeeOrganizationsEnvironmentsUpdateDebugmaskArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1DebugMask, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_environments_update_debugmask_builder(
            &self.http_client,
            &args.name,
            &args.replaceRepeatedFields,
            &args.updateMask,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_environments_update_debugmask_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations environments update environment.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1Environment result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn apigee_organizations_environments_update_environment(
        &self,
        args: &ApigeeOrganizationsEnvironmentsUpdateEnvironmentArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1Environment, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_environments_update_environment_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_environments_update_environment_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations environments update security actions config.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1SecurityActionsConfig result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn apigee_organizations_environments_update_security_actions_config(
        &self,
        args: &ApigeeOrganizationsEnvironmentsUpdateSecurityActionsConfigArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1SecurityActionsConfig, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_environments_update_security_actions_config_builder(
            &self.http_client,
            &args.name,
            &args.updateMask,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_environments_update_security_actions_config_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations environments update trace config.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1TraceConfig result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn apigee_organizations_environments_update_trace_config(
        &self,
        args: &ApigeeOrganizationsEnvironmentsUpdateTraceConfigArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1TraceConfig, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_environments_update_trace_config_builder(
            &self.http_client,
            &args.name,
            &args.updateMask,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_environments_update_trace_config_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations environments addons config set addon enablement.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn apigee_organizations_environments_addons_config_set_addon_enablement(
        &self,
        args: &ApigeeOrganizationsEnvironmentsAddonsConfigSetAddonEnablementArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_environments_addons_config_set_addon_enablement_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_environments_addons_config_set_addon_enablement_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations environments analytics admin get schemav2.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1Schema result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn apigee_organizations_environments_analytics_admin_get_schemav2(
        &self,
        args: &ApigeeOrganizationsEnvironmentsAnalyticsAdminGetSchemav2Args,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1Schema, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_environments_analytics_admin_get_schemav2_builder(
            &self.http_client,
            &args.name,
            &args.disableCache,
            &args.type_rs,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_environments_analytics_admin_get_schemav2_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations environments analytics exports create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1Export result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn apigee_organizations_environments_analytics_exports_create(
        &self,
        args: &ApigeeOrganizationsEnvironmentsAnalyticsExportsCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1Export, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_environments_analytics_exports_create_builder(
            &self.http_client,
            &args.parent,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_environments_analytics_exports_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations environments analytics exports get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1Export result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn apigee_organizations_environments_analytics_exports_get(
        &self,
        args: &ApigeeOrganizationsEnvironmentsAnalyticsExportsGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1Export, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_environments_analytics_exports_get_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_environments_analytics_exports_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations environments analytics exports list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1ListExportsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn apigee_organizations_environments_analytics_exports_list(
        &self,
        args: &ApigeeOrganizationsEnvironmentsAnalyticsExportsListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1ListExportsResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_environments_analytics_exports_list_builder(
            &self.http_client,
            &args.parent,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_environments_analytics_exports_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations environments apis deployments list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1ListDeploymentsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn apigee_organizations_environments_apis_deployments_list(
        &self,
        args: &ApigeeOrganizationsEnvironmentsApisDeploymentsListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1ListDeploymentsResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_environments_apis_deployments_list_builder(
            &self.http_client,
            &args.parent,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_environments_apis_deployments_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations environments apis revisions deploy.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1Deployment result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn apigee_organizations_environments_apis_revisions_deploy(
        &self,
        args: &ApigeeOrganizationsEnvironmentsApisRevisionsDeployArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1Deployment, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_environments_apis_revisions_deploy_builder(
            &self.http_client,
            &args.name,
            &args.override_rs,
            &args.sequencedRollout,
            &args.serviceAccount,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_environments_apis_revisions_deploy_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations environments apis revisions get deployments.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1Deployment result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn apigee_organizations_environments_apis_revisions_get_deployments(
        &self,
        args: &ApigeeOrganizationsEnvironmentsApisRevisionsGetDeploymentsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1Deployment, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_environments_apis_revisions_get_deployments_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_environments_apis_revisions_get_deployments_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations environments apis revisions undeploy.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn apigee_organizations_environments_apis_revisions_undeploy(
        &self,
        args: &ApigeeOrganizationsEnvironmentsApisRevisionsUndeployArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_environments_apis_revisions_undeploy_builder(
            &self.http_client,
            &args.name,
            &args.sequencedRollout,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_environments_apis_revisions_undeploy_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations environments apis revisions debugsessions create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1DebugSession result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn apigee_organizations_environments_apis_revisions_debugsessions_create(
        &self,
        args: &ApigeeOrganizationsEnvironmentsApisRevisionsDebugsessionsCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1DebugSession, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_environments_apis_revisions_debugsessions_create_builder(
            &self.http_client,
            &args.parent,
            &args.timeout,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_environments_apis_revisions_debugsessions_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations environments apis revisions debugsessions delete data.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn apigee_organizations_environments_apis_revisions_debugsessions_delete_data(
        &self,
        args: &ApigeeOrganizationsEnvironmentsApisRevisionsDebugsessionsDeleteDataArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_environments_apis_revisions_debugsessions_delete_data_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_environments_apis_revisions_debugsessions_delete_data_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations environments apis revisions debugsessions get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1DebugSession result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn apigee_organizations_environments_apis_revisions_debugsessions_get(
        &self,
        args: &ApigeeOrganizationsEnvironmentsApisRevisionsDebugsessionsGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1DebugSession, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_environments_apis_revisions_debugsessions_get_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_environments_apis_revisions_debugsessions_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations environments apis revisions debugsessions list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1ListDebugSessionsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn apigee_organizations_environments_apis_revisions_debugsessions_list(
        &self,
        args: &ApigeeOrganizationsEnvironmentsApisRevisionsDebugsessionsListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1ListDebugSessionsResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_environments_apis_revisions_debugsessions_list_builder(
            &self.http_client,
            &args.parent,
            &args.pageSize,
            &args.pageToken,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_environments_apis_revisions_debugsessions_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations environments apis revisions debugsessions data get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1DebugSessionTransaction result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn apigee_organizations_environments_apis_revisions_debugsessions_data_get(
        &self,
        args: &ApigeeOrganizationsEnvironmentsApisRevisionsDebugsessionsDataGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1DebugSessionTransaction, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_environments_apis_revisions_debugsessions_data_get_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_environments_apis_revisions_debugsessions_data_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations environments apis revisions deployments generate deploy change report.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1DeploymentChangeReport result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn apigee_organizations_environments_apis_revisions_deployments_generate_deploy_change_report(
        &self,
        args: &ApigeeOrganizationsEnvironmentsApisRevisionsDeploymentsGenerateDeployChangeReportArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1DeploymentChangeReport, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_environments_apis_revisions_deployments_generate_deploy_change_report_builder(
            &self.http_client,
            &args.name,
            &args.override_rs,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_environments_apis_revisions_deployments_generate_deploy_change_report_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations environments apis revisions deployments generate undeploy change report.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1DeploymentChangeReport result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn apigee_organizations_environments_apis_revisions_deployments_generate_undeploy_change_report(
        &self,
        args: &ApigeeOrganizationsEnvironmentsApisRevisionsDeploymentsGenerateUndeployChangeReportArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1DeploymentChangeReport, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_environments_apis_revisions_deployments_generate_undeploy_change_report_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_environments_apis_revisions_deployments_generate_undeploy_change_report_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations environments archive deployments create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn apigee_organizations_environments_archive_deployments_create(
        &self,
        args: &ApigeeOrganizationsEnvironmentsArchiveDeploymentsCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_environments_archive_deployments_create_builder(
            &self.http_client,
            &args.parent,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_environments_archive_deployments_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations environments archive deployments delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn apigee_organizations_environments_archive_deployments_delete(
        &self,
        args: &ApigeeOrganizationsEnvironmentsArchiveDeploymentsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_environments_archive_deployments_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_environments_archive_deployments_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations environments archive deployments generate download url.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1GenerateDownloadUrlResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn apigee_organizations_environments_archive_deployments_generate_download_url(
        &self,
        args: &ApigeeOrganizationsEnvironmentsArchiveDeploymentsGenerateDownloadUrlArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1GenerateDownloadUrlResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_environments_archive_deployments_generate_download_url_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_environments_archive_deployments_generate_download_url_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations environments archive deployments generate upload url.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1GenerateUploadUrlResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn apigee_organizations_environments_archive_deployments_generate_upload_url(
        &self,
        args: &ApigeeOrganizationsEnvironmentsArchiveDeploymentsGenerateUploadUrlArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1GenerateUploadUrlResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_environments_archive_deployments_generate_upload_url_builder(
            &self.http_client,
            &args.parent,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_environments_archive_deployments_generate_upload_url_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations environments archive deployments get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1ArchiveDeployment result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn apigee_organizations_environments_archive_deployments_get(
        &self,
        args: &ApigeeOrganizationsEnvironmentsArchiveDeploymentsGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1ArchiveDeployment, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_environments_archive_deployments_get_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_environments_archive_deployments_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations environments archive deployments list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1ListArchiveDeploymentsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn apigee_organizations_environments_archive_deployments_list(
        &self,
        args: &ApigeeOrganizationsEnvironmentsArchiveDeploymentsListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1ListArchiveDeploymentsResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_environments_archive_deployments_list_builder(
            &self.http_client,
            &args.parent,
            &args.filter,
            &args.pageSize,
            &args.pageToken,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_environments_archive_deployments_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations environments archive deployments patch.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1ArchiveDeployment result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn apigee_organizations_environments_archive_deployments_patch(
        &self,
        args: &ApigeeOrganizationsEnvironmentsArchiveDeploymentsPatchArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1ArchiveDeployment, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_environments_archive_deployments_patch_builder(
            &self.http_client,
            &args.name,
            &args.updateMask,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_environments_archive_deployments_patch_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations environments caches delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn apigee_organizations_environments_caches_delete(
        &self,
        args: &ApigeeOrganizationsEnvironmentsCachesDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_environments_caches_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_environments_caches_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations environments deployments get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1Deployment result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn apigee_organizations_environments_deployments_get(
        &self,
        args: &ApigeeOrganizationsEnvironmentsDeploymentsGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1Deployment, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_environments_deployments_get_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_environments_deployments_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations environments deployments get iam policy.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleIamV1Policy result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn apigee_organizations_environments_deployments_get_iam_policy(
        &self,
        args: &ApigeeOrganizationsEnvironmentsDeploymentsGetIamPolicyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleIamV1Policy, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_environments_deployments_get_iam_policy_builder(
            &self.http_client,
            &args.resource,
            &args.options_requestedPolicyVersion,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_environments_deployments_get_iam_policy_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations environments deployments list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1ListDeploymentsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn apigee_organizations_environments_deployments_list(
        &self,
        args: &ApigeeOrganizationsEnvironmentsDeploymentsListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1ListDeploymentsResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_environments_deployments_list_builder(
            &self.http_client,
            &args.parent,
            &args.sharedFlows,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_environments_deployments_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations environments deployments set iam policy.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleIamV1Policy result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn apigee_organizations_environments_deployments_set_iam_policy(
        &self,
        args: &ApigeeOrganizationsEnvironmentsDeploymentsSetIamPolicyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleIamV1Policy, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_environments_deployments_set_iam_policy_builder(
            &self.http_client,
            &args.resource,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_environments_deployments_set_iam_policy_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations environments deployments test iam permissions.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleIamV1TestIamPermissionsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn apigee_organizations_environments_deployments_test_iam_permissions(
        &self,
        args: &ApigeeOrganizationsEnvironmentsDeploymentsTestIamPermissionsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleIamV1TestIamPermissionsResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_environments_deployments_test_iam_permissions_builder(
            &self.http_client,
            &args.resource,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_environments_deployments_test_iam_permissions_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations environments flowhooks attach shared flow to flow hook.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1FlowHook result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn apigee_organizations_environments_flowhooks_attach_shared_flow_to_flow_hook(
        &self,
        args: &ApigeeOrganizationsEnvironmentsFlowhooksAttachSharedFlowToFlowHookArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1FlowHook, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_environments_flowhooks_attach_shared_flow_to_flow_hook_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_environments_flowhooks_attach_shared_flow_to_flow_hook_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations environments flowhooks detach shared flow from flow hook.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1FlowHook result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn apigee_organizations_environments_flowhooks_detach_shared_flow_from_flow_hook(
        &self,
        args: &ApigeeOrganizationsEnvironmentsFlowhooksDetachSharedFlowFromFlowHookArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1FlowHook, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_environments_flowhooks_detach_shared_flow_from_flow_hook_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_environments_flowhooks_detach_shared_flow_from_flow_hook_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations environments flowhooks get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1FlowHook result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn apigee_organizations_environments_flowhooks_get(
        &self,
        args: &ApigeeOrganizationsEnvironmentsFlowhooksGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1FlowHook, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_environments_flowhooks_get_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_environments_flowhooks_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations environments keystores create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1Keystore result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn apigee_organizations_environments_keystores_create(
        &self,
        args: &ApigeeOrganizationsEnvironmentsKeystoresCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1Keystore, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_environments_keystores_create_builder(
            &self.http_client,
            &args.parent,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_environments_keystores_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations environments keystores delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1Keystore result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn apigee_organizations_environments_keystores_delete(
        &self,
        args: &ApigeeOrganizationsEnvironmentsKeystoresDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1Keystore, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_environments_keystores_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_environments_keystores_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations environments keystores get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1Keystore result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn apigee_organizations_environments_keystores_get(
        &self,
        args: &ApigeeOrganizationsEnvironmentsKeystoresGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1Keystore, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_environments_keystores_get_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_environments_keystores_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations environments keystores aliases create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1Alias result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn apigee_organizations_environments_keystores_aliases_create(
        &self,
        args: &ApigeeOrganizationsEnvironmentsKeystoresAliasesCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1Alias, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_environments_keystores_aliases_create_builder(
            &self.http_client,
            &args.parent,
            &args._password,
            &args.alias,
            &args.format,
            &args.ignoreExpiryValidation,
            &args.ignoreNewlineValidation,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_environments_keystores_aliases_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations environments keystores aliases csr.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleApiHttpBody result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn apigee_organizations_environments_keystores_aliases_csr(
        &self,
        args: &ApigeeOrganizationsEnvironmentsKeystoresAliasesCsrArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleApiHttpBody, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_environments_keystores_aliases_csr_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_environments_keystores_aliases_csr_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations environments keystores aliases delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1Alias result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn apigee_organizations_environments_keystores_aliases_delete(
        &self,
        args: &ApigeeOrganizationsEnvironmentsKeystoresAliasesDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1Alias, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_environments_keystores_aliases_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_environments_keystores_aliases_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations environments keystores aliases get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1Alias result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn apigee_organizations_environments_keystores_aliases_get(
        &self,
        args: &ApigeeOrganizationsEnvironmentsKeystoresAliasesGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1Alias, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_environments_keystores_aliases_get_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_environments_keystores_aliases_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations environments keystores aliases get certificate.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleApiHttpBody result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn apigee_organizations_environments_keystores_aliases_get_certificate(
        &self,
        args: &ApigeeOrganizationsEnvironmentsKeystoresAliasesGetCertificateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleApiHttpBody, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_environments_keystores_aliases_get_certificate_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_environments_keystores_aliases_get_certificate_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations environments keystores aliases update.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1Alias result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn apigee_organizations_environments_keystores_aliases_update(
        &self,
        args: &ApigeeOrganizationsEnvironmentsKeystoresAliasesUpdateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1Alias, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_environments_keystores_aliases_update_builder(
            &self.http_client,
            &args.name,
            &args.ignoreExpiryValidation,
            &args.ignoreNewlineValidation,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_environments_keystores_aliases_update_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations environments keyvaluemaps create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1KeyValueMap result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn apigee_organizations_environments_keyvaluemaps_create(
        &self,
        args: &ApigeeOrganizationsEnvironmentsKeyvaluemapsCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1KeyValueMap, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_environments_keyvaluemaps_create_builder(
            &self.http_client,
            &args.parent,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_environments_keyvaluemaps_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations environments keyvaluemaps delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1KeyValueMap result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn apigee_organizations_environments_keyvaluemaps_delete(
        &self,
        args: &ApigeeOrganizationsEnvironmentsKeyvaluemapsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1KeyValueMap, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_environments_keyvaluemaps_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_environments_keyvaluemaps_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations environments keyvaluemaps get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1KeyValueMap result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn apigee_organizations_environments_keyvaluemaps_get(
        &self,
        args: &ApigeeOrganizationsEnvironmentsKeyvaluemapsGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1KeyValueMap, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_environments_keyvaluemaps_get_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_environments_keyvaluemaps_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations environments keyvaluemaps update.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1KeyValueMap result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn apigee_organizations_environments_keyvaluemaps_update(
        &self,
        args: &ApigeeOrganizationsEnvironmentsKeyvaluemapsUpdateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1KeyValueMap, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_environments_keyvaluemaps_update_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_environments_keyvaluemaps_update_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations environments keyvaluemaps entries create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1KeyValueEntry result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn apigee_organizations_environments_keyvaluemaps_entries_create(
        &self,
        args: &ApigeeOrganizationsEnvironmentsKeyvaluemapsEntriesCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1KeyValueEntry, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_environments_keyvaluemaps_entries_create_builder(
            &self.http_client,
            &args.parent,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_environments_keyvaluemaps_entries_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations environments keyvaluemaps entries delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1KeyValueEntry result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn apigee_organizations_environments_keyvaluemaps_entries_delete(
        &self,
        args: &ApigeeOrganizationsEnvironmentsKeyvaluemapsEntriesDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1KeyValueEntry, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_environments_keyvaluemaps_entries_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_environments_keyvaluemaps_entries_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations environments keyvaluemaps entries get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1KeyValueEntry result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn apigee_organizations_environments_keyvaluemaps_entries_get(
        &self,
        args: &ApigeeOrganizationsEnvironmentsKeyvaluemapsEntriesGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1KeyValueEntry, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_environments_keyvaluemaps_entries_get_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_environments_keyvaluemaps_entries_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations environments keyvaluemaps entries list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1ListKeyValueEntriesResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn apigee_organizations_environments_keyvaluemaps_entries_list(
        &self,
        args: &ApigeeOrganizationsEnvironmentsKeyvaluemapsEntriesListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1ListKeyValueEntriesResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_environments_keyvaluemaps_entries_list_builder(
            &self.http_client,
            &args.parent,
            &args.pageSize,
            &args.pageToken,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_environments_keyvaluemaps_entries_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations environments keyvaluemaps entries update.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1KeyValueEntry result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn apigee_organizations_environments_keyvaluemaps_entries_update(
        &self,
        args: &ApigeeOrganizationsEnvironmentsKeyvaluemapsEntriesUpdateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1KeyValueEntry, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_environments_keyvaluemaps_entries_update_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_environments_keyvaluemaps_entries_update_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations environments optimized stats get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1OptimizedStats result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn apigee_organizations_environments_optimized_stats_get(
        &self,
        args: &ApigeeOrganizationsEnvironmentsOptimizedStatsGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1OptimizedStats, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_environments_optimized_stats_get_builder(
            &self.http_client,
            &args.name,
            &args.accuracy,
            &args.aggTable,
            &args.filter,
            &args.limit,
            &args.offset,
            &args.realtime,
            &args.select,
            &args.sonar,
            &args.sort,
            &args.sortby,
            &args.timeRange,
            &args.timeUnit,
            &args.topk,
            &args.tsAscending,
            &args.tzo,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_environments_optimized_stats_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations environments queries create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1AsyncQuery result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn apigee_organizations_environments_queries_create(
        &self,
        args: &ApigeeOrganizationsEnvironmentsQueriesCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1AsyncQuery, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_environments_queries_create_builder(
            &self.http_client,
            &args.parent,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_environments_queries_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations environments queries get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1AsyncQuery result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn apigee_organizations_environments_queries_get(
        &self,
        args: &ApigeeOrganizationsEnvironmentsQueriesGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1AsyncQuery, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_environments_queries_get_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_environments_queries_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations environments queries get result.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleApiHttpBody result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn apigee_organizations_environments_queries_get_result(
        &self,
        args: &ApigeeOrganizationsEnvironmentsQueriesGetResultArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleApiHttpBody, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_environments_queries_get_result_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_environments_queries_get_result_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations environments queries get resulturl.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1GetAsyncQueryResultUrlResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn apigee_organizations_environments_queries_get_resulturl(
        &self,
        args: &ApigeeOrganizationsEnvironmentsQueriesGetResulturlArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1GetAsyncQueryResultUrlResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_environments_queries_get_resulturl_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_environments_queries_get_resulturl_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations environments queries list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1ListAsyncQueriesResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn apigee_organizations_environments_queries_list(
        &self,
        args: &ApigeeOrganizationsEnvironmentsQueriesListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1ListAsyncQueriesResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_environments_queries_list_builder(
            &self.http_client,
            &args.parent,
            &args.dataset,
            &args.from,
            &args.inclQueriesWithoutReport,
            &args.status,
            &args.submittedBy,
            &args.to,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_environments_queries_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations environments references create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1Reference result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn apigee_organizations_environments_references_create(
        &self,
        args: &ApigeeOrganizationsEnvironmentsReferencesCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1Reference, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_environments_references_create_builder(
            &self.http_client,
            &args.parent,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_environments_references_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations environments references delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1Reference result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn apigee_organizations_environments_references_delete(
        &self,
        args: &ApigeeOrganizationsEnvironmentsReferencesDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1Reference, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_environments_references_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_environments_references_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations environments references get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1Reference result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn apigee_organizations_environments_references_get(
        &self,
        args: &ApigeeOrganizationsEnvironmentsReferencesGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1Reference, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_environments_references_get_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_environments_references_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations environments references update.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1Reference result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn apigee_organizations_environments_references_update(
        &self,
        args: &ApigeeOrganizationsEnvironmentsReferencesUpdateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1Reference, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_environments_references_update_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_environments_references_update_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations environments resourcefiles create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1ResourceFile result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn apigee_organizations_environments_resourcefiles_create(
        &self,
        args: &ApigeeOrganizationsEnvironmentsResourcefilesCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1ResourceFile, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_environments_resourcefiles_create_builder(
            &self.http_client,
            &args.parent,
            &args.name,
            &args.type_rs,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_environments_resourcefiles_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations environments resourcefiles delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1ResourceFile result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn apigee_organizations_environments_resourcefiles_delete(
        &self,
        args: &ApigeeOrganizationsEnvironmentsResourcefilesDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1ResourceFile, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_environments_resourcefiles_delete_builder(
            &self.http_client,
            &args.parent,
            &args.type_rs,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_environments_resourcefiles_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations environments resourcefiles get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleApiHttpBody result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn apigee_organizations_environments_resourcefiles_get(
        &self,
        args: &ApigeeOrganizationsEnvironmentsResourcefilesGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleApiHttpBody, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_environments_resourcefiles_get_builder(
            &self.http_client,
            &args.parent,
            &args.type_rs,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_environments_resourcefiles_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations environments resourcefiles list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1ListEnvironmentResourcesResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn apigee_organizations_environments_resourcefiles_list(
        &self,
        args: &ApigeeOrganizationsEnvironmentsResourcefilesListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1ListEnvironmentResourcesResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_environments_resourcefiles_list_builder(
            &self.http_client,
            &args.parent,
            &args.type_rs,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_environments_resourcefiles_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations environments resourcefiles list environment resources.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1ListEnvironmentResourcesResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn apigee_organizations_environments_resourcefiles_list_environment_resources(
        &self,
        args: &ApigeeOrganizationsEnvironmentsResourcefilesListEnvironmentResourcesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1ListEnvironmentResourcesResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_environments_resourcefiles_list_environment_resources_builder(
            &self.http_client,
            &args.parent,
            &args.type_rs,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_environments_resourcefiles_list_environment_resources_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations environments resourcefiles update.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1ResourceFile result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn apigee_organizations_environments_resourcefiles_update(
        &self,
        args: &ApigeeOrganizationsEnvironmentsResourcefilesUpdateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1ResourceFile, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_environments_resourcefiles_update_builder(
            &self.http_client,
            &args.parent,
            &args.type_rs,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_environments_resourcefiles_update_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations environments security actions create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1SecurityAction result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn apigee_organizations_environments_security_actions_create(
        &self,
        args: &ApigeeOrganizationsEnvironmentsSecurityActionsCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1SecurityAction, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_environments_security_actions_create_builder(
            &self.http_client,
            &args.parent,
            &args.securityActionId,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_environments_security_actions_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations environments security actions delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn apigee_organizations_environments_security_actions_delete(
        &self,
        args: &ApigeeOrganizationsEnvironmentsSecurityActionsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_environments_security_actions_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_environments_security_actions_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations environments security actions disable.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1SecurityAction result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn apigee_organizations_environments_security_actions_disable(
        &self,
        args: &ApigeeOrganizationsEnvironmentsSecurityActionsDisableArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1SecurityAction, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_environments_security_actions_disable_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_environments_security_actions_disable_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations environments security actions enable.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1SecurityAction result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn apigee_organizations_environments_security_actions_enable(
        &self,
        args: &ApigeeOrganizationsEnvironmentsSecurityActionsEnableArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1SecurityAction, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_environments_security_actions_enable_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_environments_security_actions_enable_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations environments security actions get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1SecurityAction result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn apigee_organizations_environments_security_actions_get(
        &self,
        args: &ApigeeOrganizationsEnvironmentsSecurityActionsGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1SecurityAction, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_environments_security_actions_get_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_environments_security_actions_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations environments security actions list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1ListSecurityActionsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn apigee_organizations_environments_security_actions_list(
        &self,
        args: &ApigeeOrganizationsEnvironmentsSecurityActionsListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1ListSecurityActionsResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_environments_security_actions_list_builder(
            &self.http_client,
            &args.parent,
            &args.filter,
            &args.pageSize,
            &args.pageToken,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_environments_security_actions_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations environments security actions patch.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1SecurityAction result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn apigee_organizations_environments_security_actions_patch(
        &self,
        args: &ApigeeOrganizationsEnvironmentsSecurityActionsPatchArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1SecurityAction, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_environments_security_actions_patch_builder(
            &self.http_client,
            &args.name,
            &args.updateMask,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_environments_security_actions_patch_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations environments security incidents batch update.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1BatchUpdateSecurityIncidentsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn apigee_organizations_environments_security_incidents_batch_update(
        &self,
        args: &ApigeeOrganizationsEnvironmentsSecurityIncidentsBatchUpdateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1BatchUpdateSecurityIncidentsResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_environments_security_incidents_batch_update_builder(
            &self.http_client,
            &args.parent,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_environments_security_incidents_batch_update_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations environments security incidents get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1SecurityIncident result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn apigee_organizations_environments_security_incidents_get(
        &self,
        args: &ApigeeOrganizationsEnvironmentsSecurityIncidentsGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1SecurityIncident, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_environments_security_incidents_get_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_environments_security_incidents_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations environments security incidents list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1ListSecurityIncidentsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn apigee_organizations_environments_security_incidents_list(
        &self,
        args: &ApigeeOrganizationsEnvironmentsSecurityIncidentsListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1ListSecurityIncidentsResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_environments_security_incidents_list_builder(
            &self.http_client,
            &args.parent,
            &args.filter,
            &args.pageSize,
            &args.pageToken,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_environments_security_incidents_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations environments security incidents patch.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1SecurityIncident result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn apigee_organizations_environments_security_incidents_patch(
        &self,
        args: &ApigeeOrganizationsEnvironmentsSecurityIncidentsPatchArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1SecurityIncident, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_environments_security_incidents_patch_builder(
            &self.http_client,
            &args.name,
            &args.updateMask,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_environments_security_incidents_patch_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations environments security reports create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1SecurityReport result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn apigee_organizations_environments_security_reports_create(
        &self,
        args: &ApigeeOrganizationsEnvironmentsSecurityReportsCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1SecurityReport, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_environments_security_reports_create_builder(
            &self.http_client,
            &args.parent,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_environments_security_reports_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations environments security reports get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1SecurityReport result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn apigee_organizations_environments_security_reports_get(
        &self,
        args: &ApigeeOrganizationsEnvironmentsSecurityReportsGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1SecurityReport, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_environments_security_reports_get_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_environments_security_reports_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations environments security reports get result.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleApiHttpBody result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn apigee_organizations_environments_security_reports_get_result(
        &self,
        args: &ApigeeOrganizationsEnvironmentsSecurityReportsGetResultArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleApiHttpBody, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_environments_security_reports_get_result_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_environments_security_reports_get_result_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations environments security reports get result view.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1SecurityReportResultView result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn apigee_organizations_environments_security_reports_get_result_view(
        &self,
        args: &ApigeeOrganizationsEnvironmentsSecurityReportsGetResultViewArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1SecurityReportResultView, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_environments_security_reports_get_result_view_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_environments_security_reports_get_result_view_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations environments security reports list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1ListSecurityReportsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn apigee_organizations_environments_security_reports_list(
        &self,
        args: &ApigeeOrganizationsEnvironmentsSecurityReportsListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1ListSecurityReportsResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_environments_security_reports_list_builder(
            &self.http_client,
            &args.parent,
            &args.dataset,
            &args.from,
            &args.pageSize,
            &args.pageToken,
            &args.status,
            &args.submittedBy,
            &args.to,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_environments_security_reports_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations environments security stats query tabular stats.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1QueryTabularStatsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn apigee_organizations_environments_security_stats_query_tabular_stats(
        &self,
        args: &ApigeeOrganizationsEnvironmentsSecurityStatsQueryTabularStatsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1QueryTabularStatsResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_environments_security_stats_query_tabular_stats_builder(
            &self.http_client,
            &args.orgenv,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_environments_security_stats_query_tabular_stats_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations environments security stats query time series stats.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1QueryTimeSeriesStatsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn apigee_organizations_environments_security_stats_query_time_series_stats(
        &self,
        args: &ApigeeOrganizationsEnvironmentsSecurityStatsQueryTimeSeriesStatsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1QueryTimeSeriesStatsResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_environments_security_stats_query_time_series_stats_builder(
            &self.http_client,
            &args.orgenv,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_environments_security_stats_query_time_series_stats_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations environments sharedflows deployments list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1ListDeploymentsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn apigee_organizations_environments_sharedflows_deployments_list(
        &self,
        args: &ApigeeOrganizationsEnvironmentsSharedflowsDeploymentsListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1ListDeploymentsResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_environments_sharedflows_deployments_list_builder(
            &self.http_client,
            &args.parent,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_environments_sharedflows_deployments_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations environments sharedflows revisions deploy.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1Deployment result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn apigee_organizations_environments_sharedflows_revisions_deploy(
        &self,
        args: &ApigeeOrganizationsEnvironmentsSharedflowsRevisionsDeployArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1Deployment, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_environments_sharedflows_revisions_deploy_builder(
            &self.http_client,
            &args.name,
            &args.override_rs,
            &args.serviceAccount,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_environments_sharedflows_revisions_deploy_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations environments sharedflows revisions get deployments.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1Deployment result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn apigee_organizations_environments_sharedflows_revisions_get_deployments(
        &self,
        args: &ApigeeOrganizationsEnvironmentsSharedflowsRevisionsGetDeploymentsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1Deployment, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_environments_sharedflows_revisions_get_deployments_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_environments_sharedflows_revisions_get_deployments_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations environments sharedflows revisions undeploy.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn apigee_organizations_environments_sharedflows_revisions_undeploy(
        &self,
        args: &ApigeeOrganizationsEnvironmentsSharedflowsRevisionsUndeployArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_environments_sharedflows_revisions_undeploy_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_environments_sharedflows_revisions_undeploy_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations environments stats get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1Stats result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn apigee_organizations_environments_stats_get(
        &self,
        args: &ApigeeOrganizationsEnvironmentsStatsGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1Stats, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_environments_stats_get_builder(
            &self.http_client,
            &args.name,
            &args.accuracy,
            &args.aggTable,
            &args.filter,
            &args.limit,
            &args.offset,
            &args.realtime,
            &args.select,
            &args.sonar,
            &args.sort,
            &args.sortby,
            &args.timeRange,
            &args.timeUnit,
            &args.topk,
            &args.tsAscending,
            &args.tzo,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_environments_stats_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations environments targetservers create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1TargetServer result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn apigee_organizations_environments_targetservers_create(
        &self,
        args: &ApigeeOrganizationsEnvironmentsTargetserversCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1TargetServer, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_environments_targetservers_create_builder(
            &self.http_client,
            &args.parent,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_environments_targetservers_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations environments targetservers delete.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1TargetServer result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn apigee_organizations_environments_targetservers_delete(
        &self,
        args: &ApigeeOrganizationsEnvironmentsTargetserversDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1TargetServer, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_environments_targetservers_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_environments_targetservers_delete_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations environments targetservers get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1TargetServer result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn apigee_organizations_environments_targetservers_get(
        &self,
        args: &ApigeeOrganizationsEnvironmentsTargetserversGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1TargetServer, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_environments_targetservers_get_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_environments_targetservers_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations environments targetservers update.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1TargetServer result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn apigee_organizations_environments_targetservers_update(
        &self,
        args: &ApigeeOrganizationsEnvironmentsTargetserversUpdateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1TargetServer, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_environments_targetservers_update_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_environments_targetservers_update_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations environments trace config overrides create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1TraceConfigOverride result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn apigee_organizations_environments_trace_config_overrides_create(
        &self,
        args: &ApigeeOrganizationsEnvironmentsTraceConfigOverridesCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1TraceConfigOverride, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_environments_trace_config_overrides_create_builder(
            &self.http_client,
            &args.parent,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_environments_trace_config_overrides_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations environments trace config overrides delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn apigee_organizations_environments_trace_config_overrides_delete(
        &self,
        args: &ApigeeOrganizationsEnvironmentsTraceConfigOverridesDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_environments_trace_config_overrides_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_environments_trace_config_overrides_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations environments trace config overrides get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1TraceConfigOverride result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn apigee_organizations_environments_trace_config_overrides_get(
        &self,
        args: &ApigeeOrganizationsEnvironmentsTraceConfigOverridesGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1TraceConfigOverride, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_environments_trace_config_overrides_get_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_environments_trace_config_overrides_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations environments trace config overrides list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1ListTraceConfigOverridesResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn apigee_organizations_environments_trace_config_overrides_list(
        &self,
        args: &ApigeeOrganizationsEnvironmentsTraceConfigOverridesListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1ListTraceConfigOverridesResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_environments_trace_config_overrides_list_builder(
            &self.http_client,
            &args.parent,
            &args.pageSize,
            &args.pageToken,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_environments_trace_config_overrides_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations environments trace config overrides patch.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1TraceConfigOverride result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn apigee_organizations_environments_trace_config_overrides_patch(
        &self,
        args: &ApigeeOrganizationsEnvironmentsTraceConfigOverridesPatchArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1TraceConfigOverride, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_environments_trace_config_overrides_patch_builder(
            &self.http_client,
            &args.name,
            &args.updateMask,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_environments_trace_config_overrides_patch_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations host queries create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1AsyncQuery result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn apigee_organizations_host_queries_create(
        &self,
        args: &ApigeeOrganizationsHostQueriesCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1AsyncQuery, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_host_queries_create_builder(
            &self.http_client,
            &args.parent,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_host_queries_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations host queries get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1AsyncQuery result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn apigee_organizations_host_queries_get(
        &self,
        args: &ApigeeOrganizationsHostQueriesGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1AsyncQuery, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_host_queries_get_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_host_queries_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations host queries get result.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleApiHttpBody result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn apigee_organizations_host_queries_get_result(
        &self,
        args: &ApigeeOrganizationsHostQueriesGetResultArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleApiHttpBody, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_host_queries_get_result_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_host_queries_get_result_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations host queries get result view.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1AsyncQueryResultView result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn apigee_organizations_host_queries_get_result_view(
        &self,
        args: &ApigeeOrganizationsHostQueriesGetResultViewArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1AsyncQueryResultView, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_host_queries_get_result_view_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_host_queries_get_result_view_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations host queries list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1ListAsyncQueriesResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn apigee_organizations_host_queries_list(
        &self,
        args: &ApigeeOrganizationsHostQueriesListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1ListAsyncQueriesResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_host_queries_list_builder(
            &self.http_client,
            &args.parent,
            &args.dataset,
            &args.envgroupHostname,
            &args.from,
            &args.inclQueriesWithoutReport,
            &args.status,
            &args.submittedBy,
            &args.to,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_host_queries_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations host security reports create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1SecurityReport result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn apigee_organizations_host_security_reports_create(
        &self,
        args: &ApigeeOrganizationsHostSecurityReportsCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1SecurityReport, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_host_security_reports_create_builder(
            &self.http_client,
            &args.parent,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_host_security_reports_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations host security reports get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1SecurityReport result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn apigee_organizations_host_security_reports_get(
        &self,
        args: &ApigeeOrganizationsHostSecurityReportsGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1SecurityReport, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_host_security_reports_get_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_host_security_reports_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations host security reports get result.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleApiHttpBody result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn apigee_organizations_host_security_reports_get_result(
        &self,
        args: &ApigeeOrganizationsHostSecurityReportsGetResultArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleApiHttpBody, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_host_security_reports_get_result_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_host_security_reports_get_result_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations host security reports get result view.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1SecurityReportResultView result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn apigee_organizations_host_security_reports_get_result_view(
        &self,
        args: &ApigeeOrganizationsHostSecurityReportsGetResultViewArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1SecurityReportResultView, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_host_security_reports_get_result_view_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_host_security_reports_get_result_view_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations host security reports list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1ListSecurityReportsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn apigee_organizations_host_security_reports_list(
        &self,
        args: &ApigeeOrganizationsHostSecurityReportsListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1ListSecurityReportsResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_host_security_reports_list_builder(
            &self.http_client,
            &args.parent,
            &args.dataset,
            &args.envgroupHostname,
            &args.from,
            &args.pageSize,
            &args.pageToken,
            &args.status,
            &args.submittedBy,
            &args.to,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_host_security_reports_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations host stats get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1Stats result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn apigee_organizations_host_stats_get(
        &self,
        args: &ApigeeOrganizationsHostStatsGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1Stats, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_host_stats_get_builder(
            &self.http_client,
            &args.name,
            &args.accuracy,
            &args.envgroupHostname,
            &args.filter,
            &args.limit,
            &args.offset,
            &args.realtime,
            &args.select,
            &args.sort,
            &args.sortby,
            &args.timeRange,
            &args.timeUnit,
            &args.topk,
            &args.tsAscending,
            &args.tzo,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_host_stats_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations instances create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn apigee_organizations_instances_create(
        &self,
        args: &ApigeeOrganizationsInstancesCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_instances_create_builder(
            &self.http_client,
            &args.parent,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_instances_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations instances delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn apigee_organizations_instances_delete(
        &self,
        args: &ApigeeOrganizationsInstancesDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_instances_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_instances_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations instances get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1Instance result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn apigee_organizations_instances_get(
        &self,
        args: &ApigeeOrganizationsInstancesGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1Instance, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_instances_get_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_instances_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations instances list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1ListInstancesResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn apigee_organizations_instances_list(
        &self,
        args: &ApigeeOrganizationsInstancesListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1ListInstancesResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_instances_list_builder(
            &self.http_client,
            &args.parent,
            &args.pageSize,
            &args.pageToken,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_instances_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations instances patch.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn apigee_organizations_instances_patch(
        &self,
        args: &ApigeeOrganizationsInstancesPatchArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_instances_patch_builder(
            &self.http_client,
            &args.name,
            &args.updateMask,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_instances_patch_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations instances report status.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1ReportInstanceStatusResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn apigee_organizations_instances_report_status(
        &self,
        args: &ApigeeOrganizationsInstancesReportStatusArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1ReportInstanceStatusResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_instances_report_status_builder(
            &self.http_client,
            &args.instance,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_instances_report_status_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations instances attachments create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn apigee_organizations_instances_attachments_create(
        &self,
        args: &ApigeeOrganizationsInstancesAttachmentsCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_instances_attachments_create_builder(
            &self.http_client,
            &args.parent,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_instances_attachments_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations instances attachments delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn apigee_organizations_instances_attachments_delete(
        &self,
        args: &ApigeeOrganizationsInstancesAttachmentsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_instances_attachments_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_instances_attachments_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations instances attachments get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1InstanceAttachment result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn apigee_organizations_instances_attachments_get(
        &self,
        args: &ApigeeOrganizationsInstancesAttachmentsGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1InstanceAttachment, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_instances_attachments_get_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_instances_attachments_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations instances attachments list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1ListInstanceAttachmentsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn apigee_organizations_instances_attachments_list(
        &self,
        args: &ApigeeOrganizationsInstancesAttachmentsListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1ListInstanceAttachmentsResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_instances_attachments_list_builder(
            &self.http_client,
            &args.parent,
            &args.pageSize,
            &args.pageToken,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_instances_attachments_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations instances canaryevaluations create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn apigee_organizations_instances_canaryevaluations_create(
        &self,
        args: &ApigeeOrganizationsInstancesCanaryevaluationsCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_instances_canaryevaluations_create_builder(
            &self.http_client,
            &args.parent,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_instances_canaryevaluations_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations instances canaryevaluations get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1CanaryEvaluation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn apigee_organizations_instances_canaryevaluations_get(
        &self,
        args: &ApigeeOrganizationsInstancesCanaryevaluationsGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1CanaryEvaluation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_instances_canaryevaluations_get_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_instances_canaryevaluations_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations instances nat addresses activate.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn apigee_organizations_instances_nat_addresses_activate(
        &self,
        args: &ApigeeOrganizationsInstancesNatAddressesActivateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_instances_nat_addresses_activate_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_instances_nat_addresses_activate_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations instances nat addresses create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn apigee_organizations_instances_nat_addresses_create(
        &self,
        args: &ApigeeOrganizationsInstancesNatAddressesCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_instances_nat_addresses_create_builder(
            &self.http_client,
            &args.parent,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_instances_nat_addresses_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations instances nat addresses delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn apigee_organizations_instances_nat_addresses_delete(
        &self,
        args: &ApigeeOrganizationsInstancesNatAddressesDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_instances_nat_addresses_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_instances_nat_addresses_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations instances nat addresses get.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1NatAddress result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn apigee_organizations_instances_nat_addresses_get(
        &self,
        args: &ApigeeOrganizationsInstancesNatAddressesGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1NatAddress, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_instances_nat_addresses_get_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_instances_nat_addresses_get_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations instances nat addresses list.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1ListNatAddressesResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn apigee_organizations_instances_nat_addresses_list(
        &self,
        args: &ApigeeOrganizationsInstancesNatAddressesListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1ListNatAddressesResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_instances_nat_addresses_list_builder(
            &self.http_client,
            &args.parent,
            &args.pageSize,
            &args.pageToken,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_instances_nat_addresses_list_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations keyvaluemaps create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1KeyValueMap result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn apigee_organizations_keyvaluemaps_create(
        &self,
        args: &ApigeeOrganizationsKeyvaluemapsCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1KeyValueMap, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_keyvaluemaps_create_builder(
            &self.http_client,
            &args.parent,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_keyvaluemaps_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations keyvaluemaps delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1KeyValueMap result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn apigee_organizations_keyvaluemaps_delete(
        &self,
        args: &ApigeeOrganizationsKeyvaluemapsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1KeyValueMap, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_keyvaluemaps_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_keyvaluemaps_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations keyvaluemaps get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1KeyValueMap result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn apigee_organizations_keyvaluemaps_get(
        &self,
        args: &ApigeeOrganizationsKeyvaluemapsGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1KeyValueMap, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_keyvaluemaps_get_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_keyvaluemaps_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations keyvaluemaps update.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1KeyValueMap result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn apigee_organizations_keyvaluemaps_update(
        &self,
        args: &ApigeeOrganizationsKeyvaluemapsUpdateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1KeyValueMap, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_keyvaluemaps_update_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_keyvaluemaps_update_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations keyvaluemaps entries create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1KeyValueEntry result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn apigee_organizations_keyvaluemaps_entries_create(
        &self,
        args: &ApigeeOrganizationsKeyvaluemapsEntriesCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1KeyValueEntry, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_keyvaluemaps_entries_create_builder(
            &self.http_client,
            &args.parent,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_keyvaluemaps_entries_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations keyvaluemaps entries delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1KeyValueEntry result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn apigee_organizations_keyvaluemaps_entries_delete(
        &self,
        args: &ApigeeOrganizationsKeyvaluemapsEntriesDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1KeyValueEntry, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_keyvaluemaps_entries_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_keyvaluemaps_entries_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations keyvaluemaps entries get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1KeyValueEntry result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn apigee_organizations_keyvaluemaps_entries_get(
        &self,
        args: &ApigeeOrganizationsKeyvaluemapsEntriesGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1KeyValueEntry, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_keyvaluemaps_entries_get_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_keyvaluemaps_entries_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations keyvaluemaps entries list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1ListKeyValueEntriesResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn apigee_organizations_keyvaluemaps_entries_list(
        &self,
        args: &ApigeeOrganizationsKeyvaluemapsEntriesListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1ListKeyValueEntriesResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_keyvaluemaps_entries_list_builder(
            &self.http_client,
            &args.parent,
            &args.pageSize,
            &args.pageToken,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_keyvaluemaps_entries_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations keyvaluemaps entries update.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1KeyValueEntry result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn apigee_organizations_keyvaluemaps_entries_update(
        &self,
        args: &ApigeeOrganizationsKeyvaluemapsEntriesUpdateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1KeyValueEntry, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_keyvaluemaps_entries_update_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_keyvaluemaps_entries_update_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations operations get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn apigee_organizations_operations_get(
        &self,
        args: &ApigeeOrganizationsOperationsGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_operations_get_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_operations_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations operations list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningListOperationsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn apigee_organizations_operations_list(
        &self,
        args: &ApigeeOrganizationsOperationsListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningListOperationsResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_operations_list_builder(
            &self.http_client,
            &args.name,
            &args.filter,
            &args.pageSize,
            &args.pageToken,
            &args.returnPartialSuccess,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_operations_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations optimized host stats get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1OptimizedStats result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn apigee_organizations_optimized_host_stats_get(
        &self,
        args: &ApigeeOrganizationsOptimizedHostStatsGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1OptimizedStats, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_optimized_host_stats_get_builder(
            &self.http_client,
            &args.name,
            &args.accuracy,
            &args.envgroupHostname,
            &args.filter,
            &args.limit,
            &args.offset,
            &args.realtime,
            &args.select,
            &args.sort,
            &args.sortby,
            &args.timeRange,
            &args.timeUnit,
            &args.topk,
            &args.tsAscending,
            &args.tzo,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_optimized_host_stats_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations reports create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1CustomReport result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn apigee_organizations_reports_create(
        &self,
        args: &ApigeeOrganizationsReportsCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1CustomReport, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_reports_create_builder(
            &self.http_client,
            &args.parent,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_reports_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations reports delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1DeleteCustomReportResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn apigee_organizations_reports_delete(
        &self,
        args: &ApigeeOrganizationsReportsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1DeleteCustomReportResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_reports_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_reports_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations reports get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1CustomReport result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn apigee_organizations_reports_get(
        &self,
        args: &ApigeeOrganizationsReportsGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1CustomReport, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_reports_get_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_reports_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations reports list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1ListCustomReportsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn apigee_organizations_reports_list(
        &self,
        args: &ApigeeOrganizationsReportsListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1ListCustomReportsResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_reports_list_builder(
            &self.http_client,
            &args.parent,
            &args.expand,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_reports_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations reports update.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1CustomReport result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn apigee_organizations_reports_update(
        &self,
        args: &ApigeeOrganizationsReportsUpdateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1CustomReport, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_reports_update_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_reports_update_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations security assessment results batch compute.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1BatchComputeSecurityAssessmentResultsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn apigee_organizations_security_assessment_results_batch_compute(
        &self,
        args: &ApigeeOrganizationsSecurityAssessmentResultsBatchComputeArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1BatchComputeSecurityAssessmentResultsResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_security_assessment_results_batch_compute_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_security_assessment_results_batch_compute_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations security feedback create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1SecurityFeedback result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn apigee_organizations_security_feedback_create(
        &self,
        args: &ApigeeOrganizationsSecurityFeedbackCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1SecurityFeedback, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_security_feedback_create_builder(
            &self.http_client,
            &args.parent,
            &args.securityFeedbackId,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_security_feedback_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations security feedback delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn apigee_organizations_security_feedback_delete(
        &self,
        args: &ApigeeOrganizationsSecurityFeedbackDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_security_feedback_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_security_feedback_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations security feedback get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1SecurityFeedback result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn apigee_organizations_security_feedback_get(
        &self,
        args: &ApigeeOrganizationsSecurityFeedbackGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1SecurityFeedback, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_security_feedback_get_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_security_feedback_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations security feedback list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1ListSecurityFeedbackResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn apigee_organizations_security_feedback_list(
        &self,
        args: &ApigeeOrganizationsSecurityFeedbackListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1ListSecurityFeedbackResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_security_feedback_list_builder(
            &self.http_client,
            &args.parent,
            &args.pageSize,
            &args.pageToken,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_security_feedback_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations security feedback patch.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1SecurityFeedback result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn apigee_organizations_security_feedback_patch(
        &self,
        args: &ApigeeOrganizationsSecurityFeedbackPatchArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1SecurityFeedback, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_security_feedback_patch_builder(
            &self.http_client,
            &args.name,
            &args.updateMask,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_security_feedback_patch_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations security monitoring conditions create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1SecurityMonitoringCondition result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn apigee_organizations_security_monitoring_conditions_create(
        &self,
        args: &ApigeeOrganizationsSecurityMonitoringConditionsCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1SecurityMonitoringCondition, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_security_monitoring_conditions_create_builder(
            &self.http_client,
            &args.parent,
            &args.securityMonitoringConditionId,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_security_monitoring_conditions_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations security monitoring conditions delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn apigee_organizations_security_monitoring_conditions_delete(
        &self,
        args: &ApigeeOrganizationsSecurityMonitoringConditionsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_security_monitoring_conditions_delete_builder(
            &self.http_client,
            &args.name,
            &args.riskAssessmentType,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_security_monitoring_conditions_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations security monitoring conditions get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1SecurityMonitoringCondition result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn apigee_organizations_security_monitoring_conditions_get(
        &self,
        args: &ApigeeOrganizationsSecurityMonitoringConditionsGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1SecurityMonitoringCondition, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_security_monitoring_conditions_get_builder(
            &self.http_client,
            &args.name,
            &args.riskAssessmentType,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_security_monitoring_conditions_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations security monitoring conditions list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1ListSecurityMonitoringConditionsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn apigee_organizations_security_monitoring_conditions_list(
        &self,
        args: &ApigeeOrganizationsSecurityMonitoringConditionsListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1ListSecurityMonitoringConditionsResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_security_monitoring_conditions_list_builder(
            &self.http_client,
            &args.parent,
            &args.filter,
            &args.pageSize,
            &args.pageToken,
            &args.riskAssessmentType,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_security_monitoring_conditions_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations security monitoring conditions patch.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1SecurityMonitoringCondition result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn apigee_organizations_security_monitoring_conditions_patch(
        &self,
        args: &ApigeeOrganizationsSecurityMonitoringConditionsPatchArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1SecurityMonitoringCondition, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_security_monitoring_conditions_patch_builder(
            &self.http_client,
            &args.name,
            &args.updateMask,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_security_monitoring_conditions_patch_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations security profiles create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1SecurityProfile result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn apigee_organizations_security_profiles_create(
        &self,
        args: &ApigeeOrganizationsSecurityProfilesCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1SecurityProfile, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_security_profiles_create_builder(
            &self.http_client,
            &args.parent,
            &args.securityProfileId,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_security_profiles_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations security profiles delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn apigee_organizations_security_profiles_delete(
        &self,
        args: &ApigeeOrganizationsSecurityProfilesDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_security_profiles_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_security_profiles_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations security profiles get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1SecurityProfile result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn apigee_organizations_security_profiles_get(
        &self,
        args: &ApigeeOrganizationsSecurityProfilesGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1SecurityProfile, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_security_profiles_get_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_security_profiles_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations security profiles list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1ListSecurityProfilesResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn apigee_organizations_security_profiles_list(
        &self,
        args: &ApigeeOrganizationsSecurityProfilesListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1ListSecurityProfilesResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_security_profiles_list_builder(
            &self.http_client,
            &args.parent,
            &args.pageSize,
            &args.pageToken,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_security_profiles_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations security profiles list revisions.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1ListSecurityProfileRevisionsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn apigee_organizations_security_profiles_list_revisions(
        &self,
        args: &ApigeeOrganizationsSecurityProfilesListRevisionsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1ListSecurityProfileRevisionsResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_security_profiles_list_revisions_builder(
            &self.http_client,
            &args.name,
            &args.pageSize,
            &args.pageToken,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_security_profiles_list_revisions_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations security profiles patch.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1SecurityProfile result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn apigee_organizations_security_profiles_patch(
        &self,
        args: &ApigeeOrganizationsSecurityProfilesPatchArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1SecurityProfile, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_security_profiles_patch_builder(
            &self.http_client,
            &args.name,
            &args.updateMask,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_security_profiles_patch_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations security profiles environments compute environment scores.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1ComputeEnvironmentScoresResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn apigee_organizations_security_profiles_environments_compute_environment_scores(
        &self,
        args: &ApigeeOrganizationsSecurityProfilesEnvironmentsComputeEnvironmentScoresArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1ComputeEnvironmentScoresResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_security_profiles_environments_compute_environment_scores_builder(
            &self.http_client,
            &args.profileEnvironment,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_security_profiles_environments_compute_environment_scores_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations security profiles environments create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1SecurityProfileEnvironmentAssociation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn apigee_organizations_security_profiles_environments_create(
        &self,
        args: &ApigeeOrganizationsSecurityProfilesEnvironmentsCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1SecurityProfileEnvironmentAssociation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_security_profiles_environments_create_builder(
            &self.http_client,
            &args.parent,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_security_profiles_environments_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations security profiles environments delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn apigee_organizations_security_profiles_environments_delete(
        &self,
        args: &ApigeeOrganizationsSecurityProfilesEnvironmentsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_security_profiles_environments_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_security_profiles_environments_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations security profiles v2 create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1SecurityProfileV2 result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn apigee_organizations_security_profiles_v2_create(
        &self,
        args: &ApigeeOrganizationsSecurityProfilesV2CreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1SecurityProfileV2, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_security_profiles_v2_create_builder(
            &self.http_client,
            &args.parent,
            &args.securityProfileV2Id,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_security_profiles_v2_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations security profiles v2 delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn apigee_organizations_security_profiles_v2_delete(
        &self,
        args: &ApigeeOrganizationsSecurityProfilesV2DeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_security_profiles_v2_delete_builder(
            &self.http_client,
            &args.name,
            &args.riskAssessmentType,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_security_profiles_v2_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations security profiles v2 get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1SecurityProfileV2 result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn apigee_organizations_security_profiles_v2_get(
        &self,
        args: &ApigeeOrganizationsSecurityProfilesV2GetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1SecurityProfileV2, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_security_profiles_v2_get_builder(
            &self.http_client,
            &args.name,
            &args.riskAssessmentType,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_security_profiles_v2_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations security profiles v2 list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1ListSecurityProfilesV2Response result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn apigee_organizations_security_profiles_v2_list(
        &self,
        args: &ApigeeOrganizationsSecurityProfilesV2ListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1ListSecurityProfilesV2Response, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_security_profiles_v2_list_builder(
            &self.http_client,
            &args.parent,
            &args.pageSize,
            &args.pageToken,
            &args.riskAssessmentType,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_security_profiles_v2_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations security profiles v2 patch.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1SecurityProfileV2 result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn apigee_organizations_security_profiles_v2_patch(
        &self,
        args: &ApigeeOrganizationsSecurityProfilesV2PatchArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1SecurityProfileV2, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_security_profiles_v2_patch_builder(
            &self.http_client,
            &args.name,
            &args.updateMask,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_security_profiles_v2_patch_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations sharedflows create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1SharedFlowRevision result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn apigee_organizations_sharedflows_create(
        &self,
        args: &ApigeeOrganizationsSharedflowsCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1SharedFlowRevision, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_sharedflows_create_builder(
            &self.http_client,
            &args.parent,
            &args.action,
            &args.name,
            &args.space,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_sharedflows_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations sharedflows delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1SharedFlow result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn apigee_organizations_sharedflows_delete(
        &self,
        args: &ApigeeOrganizationsSharedflowsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1SharedFlow, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_sharedflows_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_sharedflows_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations sharedflows get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1SharedFlow result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn apigee_organizations_sharedflows_get(
        &self,
        args: &ApigeeOrganizationsSharedflowsGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1SharedFlow, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_sharedflows_get_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_sharedflows_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations sharedflows list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1ListSharedFlowsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn apigee_organizations_sharedflows_list(
        &self,
        args: &ApigeeOrganizationsSharedflowsListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1ListSharedFlowsResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_sharedflows_list_builder(
            &self.http_client,
            &args.parent,
            &args.includeMetaData,
            &args.includeRevisions,
            &args.space,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_sharedflows_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations sharedflows move.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1SharedFlow result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn apigee_organizations_sharedflows_move(
        &self,
        args: &ApigeeOrganizationsSharedflowsMoveArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1SharedFlow, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_sharedflows_move_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_sharedflows_move_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations sharedflows deployments list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1ListDeploymentsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn apigee_organizations_sharedflows_deployments_list(
        &self,
        args: &ApigeeOrganizationsSharedflowsDeploymentsListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1ListDeploymentsResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_sharedflows_deployments_list_builder(
            &self.http_client,
            &args.parent,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_sharedflows_deployments_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations sharedflows revisions delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1SharedFlowRevision result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn apigee_organizations_sharedflows_revisions_delete(
        &self,
        args: &ApigeeOrganizationsSharedflowsRevisionsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1SharedFlowRevision, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_sharedflows_revisions_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_sharedflows_revisions_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations sharedflows revisions get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleApiHttpBody result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn apigee_organizations_sharedflows_revisions_get(
        &self,
        args: &ApigeeOrganizationsSharedflowsRevisionsGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleApiHttpBody, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_sharedflows_revisions_get_builder(
            &self.http_client,
            &args.name,
            &args.format,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_sharedflows_revisions_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations sharedflows revisions update shared flow revision.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1SharedFlowRevision result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn apigee_organizations_sharedflows_revisions_update_shared_flow_revision(
        &self,
        args: &ApigeeOrganizationsSharedflowsRevisionsUpdateSharedFlowRevisionArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1SharedFlowRevision, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_sharedflows_revisions_update_shared_flow_revision_builder(
            &self.http_client,
            &args.name,
            &args.validate,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_sharedflows_revisions_update_shared_flow_revision_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations sharedflows revisions deployments list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1ListDeploymentsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn apigee_organizations_sharedflows_revisions_deployments_list(
        &self,
        args: &ApigeeOrganizationsSharedflowsRevisionsDeploymentsListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1ListDeploymentsResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_sharedflows_revisions_deployments_list_builder(
            &self.http_client,
            &args.parent,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_sharedflows_revisions_deployments_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations sites apicategories create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1ApiCategoryResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn apigee_organizations_sites_apicategories_create(
        &self,
        args: &ApigeeOrganizationsSitesApicategoriesCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1ApiCategoryResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_sites_apicategories_create_builder(
            &self.http_client,
            &args.parent,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_sites_apicategories_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations sites apicategories delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1DeleteResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn apigee_organizations_sites_apicategories_delete(
        &self,
        args: &ApigeeOrganizationsSitesApicategoriesDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1DeleteResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_sites_apicategories_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_sites_apicategories_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations sites apicategories get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1ApiCategoryResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn apigee_organizations_sites_apicategories_get(
        &self,
        args: &ApigeeOrganizationsSitesApicategoriesGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1ApiCategoryResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_sites_apicategories_get_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_sites_apicategories_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations sites apicategories list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1ListApiCategoriesResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn apigee_organizations_sites_apicategories_list(
        &self,
        args: &ApigeeOrganizationsSitesApicategoriesListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1ListApiCategoriesResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_sites_apicategories_list_builder(
            &self.http_client,
            &args.parent,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_sites_apicategories_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations sites apicategories patch.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1ApiCategoryResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn apigee_organizations_sites_apicategories_patch(
        &self,
        args: &ApigeeOrganizationsSitesApicategoriesPatchArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1ApiCategoryResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_sites_apicategories_patch_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_sites_apicategories_patch_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations sites apidocs create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1ApiDocResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn apigee_organizations_sites_apidocs_create(
        &self,
        args: &ApigeeOrganizationsSitesApidocsCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1ApiDocResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_sites_apidocs_create_builder(
            &self.http_client,
            &args.parent,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_sites_apidocs_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations sites apidocs delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1DeleteResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn apigee_organizations_sites_apidocs_delete(
        &self,
        args: &ApigeeOrganizationsSitesApidocsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1DeleteResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_sites_apidocs_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_sites_apidocs_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations sites apidocs get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1ApiDocResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn apigee_organizations_sites_apidocs_get(
        &self,
        args: &ApigeeOrganizationsSitesApidocsGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1ApiDocResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_sites_apidocs_get_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_sites_apidocs_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations sites apidocs get documentation.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1ApiDocDocumentationResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn apigee_organizations_sites_apidocs_get_documentation(
        &self,
        args: &ApigeeOrganizationsSitesApidocsGetDocumentationArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1ApiDocDocumentationResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_sites_apidocs_get_documentation_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_sites_apidocs_get_documentation_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations sites apidocs list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1ListApiDocsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn apigee_organizations_sites_apidocs_list(
        &self,
        args: &ApigeeOrganizationsSitesApidocsListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1ListApiDocsResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_sites_apidocs_list_builder(
            &self.http_client,
            &args.parent,
            &args.pageSize,
            &args.pageToken,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_sites_apidocs_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations sites apidocs update.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1ApiDocResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn apigee_organizations_sites_apidocs_update(
        &self,
        args: &ApigeeOrganizationsSitesApidocsUpdateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1ApiDocResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_sites_apidocs_update_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_sites_apidocs_update_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations sites apidocs update documentation.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1ApiDocDocumentationResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn apigee_organizations_sites_apidocs_update_documentation(
        &self,
        args: &ApigeeOrganizationsSitesApidocsUpdateDocumentationArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1ApiDocDocumentationResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_sites_apidocs_update_documentation_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_sites_apidocs_update_documentation_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations spaces create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1Space result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn apigee_organizations_spaces_create(
        &self,
        args: &ApigeeOrganizationsSpacesCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1Space, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_spaces_create_builder(
            &self.http_client,
            &args.parent,
            &args.spaceId,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_spaces_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations spaces delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn apigee_organizations_spaces_delete(
        &self,
        args: &ApigeeOrganizationsSpacesDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_spaces_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_spaces_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations spaces get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1Space result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn apigee_organizations_spaces_get(
        &self,
        args: &ApigeeOrganizationsSpacesGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1Space, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_spaces_get_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_spaces_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations spaces get iam policy.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleIamV1Policy result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn apigee_organizations_spaces_get_iam_policy(
        &self,
        args: &ApigeeOrganizationsSpacesGetIamPolicyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleIamV1Policy, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_spaces_get_iam_policy_builder(
            &self.http_client,
            &args.resource,
            &args.options_requestedPolicyVersion,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_spaces_get_iam_policy_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations spaces list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1ListSpacesResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn apigee_organizations_spaces_list(
        &self,
        args: &ApigeeOrganizationsSpacesListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1ListSpacesResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_spaces_list_builder(
            &self.http_client,
            &args.parent,
            &args.pageSize,
            &args.pageToken,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_spaces_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations spaces patch.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudApigeeV1Space result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn apigee_organizations_spaces_patch(
        &self,
        args: &ApigeeOrganizationsSpacesPatchArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudApigeeV1Space, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_spaces_patch_builder(
            &self.http_client,
            &args.name,
            &args.updateMask,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_spaces_patch_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations spaces set iam policy.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleIamV1Policy result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn apigee_organizations_spaces_set_iam_policy(
        &self,
        args: &ApigeeOrganizationsSpacesSetIamPolicyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleIamV1Policy, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_spaces_set_iam_policy_builder(
            &self.http_client,
            &args.resource,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_spaces_set_iam_policy_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee organizations spaces test iam permissions.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleIamV1TestIamPermissionsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn apigee_organizations_spaces_test_iam_permissions(
        &self,
        args: &ApigeeOrganizationsSpacesTestIamPermissionsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleIamV1TestIamPermissionsResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_organizations_spaces_test_iam_permissions_builder(
            &self.http_client,
            &args.resource,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_organizations_spaces_test_iam_permissions_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Apigee projects provision organization.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn apigee_projects_provision_organization(
        &self,
        args: &ApigeeProjectsProvisionOrganizationArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = apigee_projects_provision_organization_builder(
            &self.http_client,
            &args.project,
        )
        .map_err(ProviderError::Api)?;

        let task = apigee_projects_provision_organization_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

}
