//! DataplexProvider - State-aware dataplex API client.
//!
//! WHY: Users need state-aware API clients that automatically track
//!      resource changes in the state store.
//!
//! WHAT: Provider wrapping ProviderClient<S> with methods for
//!       dataplex API endpoints that auto-store results.
//!
//! HOW: Each method wraps the task with StoreStateIdentifierTask
//!      for automatic state persistence on success.

#![cfg(feature = "gcp")]

use crate::providers::gcp::clients::dataplex::{
    dataplex_organizations_locations_encryption_configs_create_builder, dataplex_organizations_locations_encryption_configs_create_task,
    dataplex_organizations_locations_encryption_configs_delete_builder, dataplex_organizations_locations_encryption_configs_delete_task,
    dataplex_organizations_locations_encryption_configs_get_builder, dataplex_organizations_locations_encryption_configs_get_task,
    dataplex_organizations_locations_encryption_configs_get_iam_policy_builder, dataplex_organizations_locations_encryption_configs_get_iam_policy_task,
    dataplex_organizations_locations_encryption_configs_list_builder, dataplex_organizations_locations_encryption_configs_list_task,
    dataplex_organizations_locations_encryption_configs_patch_builder, dataplex_organizations_locations_encryption_configs_patch_task,
    dataplex_organizations_locations_encryption_configs_set_iam_policy_builder, dataplex_organizations_locations_encryption_configs_set_iam_policy_task,
    dataplex_organizations_locations_encryption_configs_test_iam_permissions_builder, dataplex_organizations_locations_encryption_configs_test_iam_permissions_task,
    dataplex_organizations_locations_operations_cancel_builder, dataplex_organizations_locations_operations_cancel_task,
    dataplex_organizations_locations_operations_delete_builder, dataplex_organizations_locations_operations_delete_task,
    dataplex_organizations_locations_operations_get_builder, dataplex_organizations_locations_operations_get_task,
    dataplex_organizations_locations_operations_list_builder, dataplex_organizations_locations_operations_list_task,
    dataplex_projects_locations_get_builder, dataplex_projects_locations_get_task,
    dataplex_projects_locations_list_builder, dataplex_projects_locations_list_task,
    dataplex_projects_locations_lookup_context_builder, dataplex_projects_locations_lookup_context_task,
    dataplex_projects_locations_lookup_entry_builder, dataplex_projects_locations_lookup_entry_task,
    dataplex_projects_locations_lookup_entry_links_builder, dataplex_projects_locations_lookup_entry_links_task,
    dataplex_projects_locations_modify_entry_builder, dataplex_projects_locations_modify_entry_task,
    dataplex_projects_locations_search_entries_builder, dataplex_projects_locations_search_entries_task,
    dataplex_projects_locations_aspect_types_create_builder, dataplex_projects_locations_aspect_types_create_task,
    dataplex_projects_locations_aspect_types_delete_builder, dataplex_projects_locations_aspect_types_delete_task,
    dataplex_projects_locations_aspect_types_get_builder, dataplex_projects_locations_aspect_types_get_task,
    dataplex_projects_locations_aspect_types_get_iam_policy_builder, dataplex_projects_locations_aspect_types_get_iam_policy_task,
    dataplex_projects_locations_aspect_types_list_builder, dataplex_projects_locations_aspect_types_list_task,
    dataplex_projects_locations_aspect_types_patch_builder, dataplex_projects_locations_aspect_types_patch_task,
    dataplex_projects_locations_aspect_types_set_iam_policy_builder, dataplex_projects_locations_aspect_types_set_iam_policy_task,
    dataplex_projects_locations_aspect_types_test_iam_permissions_builder, dataplex_projects_locations_aspect_types_test_iam_permissions_task,
    dataplex_projects_locations_change_requests_get_iam_policy_builder, dataplex_projects_locations_change_requests_get_iam_policy_task,
    dataplex_projects_locations_change_requests_set_iam_policy_builder, dataplex_projects_locations_change_requests_set_iam_policy_task,
    dataplex_projects_locations_change_requests_test_iam_permissions_builder, dataplex_projects_locations_change_requests_test_iam_permissions_task,
    dataplex_projects_locations_data_attribute_bindings_create_builder, dataplex_projects_locations_data_attribute_bindings_create_task,
    dataplex_projects_locations_data_attribute_bindings_delete_builder, dataplex_projects_locations_data_attribute_bindings_delete_task,
    dataplex_projects_locations_data_attribute_bindings_get_builder, dataplex_projects_locations_data_attribute_bindings_get_task,
    dataplex_projects_locations_data_attribute_bindings_get_iam_policy_builder, dataplex_projects_locations_data_attribute_bindings_get_iam_policy_task,
    dataplex_projects_locations_data_attribute_bindings_list_builder, dataplex_projects_locations_data_attribute_bindings_list_task,
    dataplex_projects_locations_data_attribute_bindings_patch_builder, dataplex_projects_locations_data_attribute_bindings_patch_task,
    dataplex_projects_locations_data_attribute_bindings_set_iam_policy_builder, dataplex_projects_locations_data_attribute_bindings_set_iam_policy_task,
    dataplex_projects_locations_data_attribute_bindings_test_iam_permissions_builder, dataplex_projects_locations_data_attribute_bindings_test_iam_permissions_task,
    dataplex_projects_locations_data_domains_get_iam_policy_builder, dataplex_projects_locations_data_domains_get_iam_policy_task,
    dataplex_projects_locations_data_domains_set_iam_policy_builder, dataplex_projects_locations_data_domains_set_iam_policy_task,
    dataplex_projects_locations_data_domains_test_iam_permissions_builder, dataplex_projects_locations_data_domains_test_iam_permissions_task,
    dataplex_projects_locations_data_products_create_builder, dataplex_projects_locations_data_products_create_task,
    dataplex_projects_locations_data_products_delete_builder, dataplex_projects_locations_data_products_delete_task,
    dataplex_projects_locations_data_products_get_builder, dataplex_projects_locations_data_products_get_task,
    dataplex_projects_locations_data_products_get_iam_policy_builder, dataplex_projects_locations_data_products_get_iam_policy_task,
    dataplex_projects_locations_data_products_list_builder, dataplex_projects_locations_data_products_list_task,
    dataplex_projects_locations_data_products_patch_builder, dataplex_projects_locations_data_products_patch_task,
    dataplex_projects_locations_data_products_set_iam_policy_builder, dataplex_projects_locations_data_products_set_iam_policy_task,
    dataplex_projects_locations_data_products_test_iam_permissions_builder, dataplex_projects_locations_data_products_test_iam_permissions_task,
    dataplex_projects_locations_data_products_data_assets_create_builder, dataplex_projects_locations_data_products_data_assets_create_task,
    dataplex_projects_locations_data_products_data_assets_delete_builder, dataplex_projects_locations_data_products_data_assets_delete_task,
    dataplex_projects_locations_data_products_data_assets_get_builder, dataplex_projects_locations_data_products_data_assets_get_task,
    dataplex_projects_locations_data_products_data_assets_list_builder, dataplex_projects_locations_data_products_data_assets_list_task,
    dataplex_projects_locations_data_products_data_assets_patch_builder, dataplex_projects_locations_data_products_data_assets_patch_task,
    dataplex_projects_locations_data_scans_create_builder, dataplex_projects_locations_data_scans_create_task,
    dataplex_projects_locations_data_scans_delete_builder, dataplex_projects_locations_data_scans_delete_task,
    dataplex_projects_locations_data_scans_generate_data_quality_rules_builder, dataplex_projects_locations_data_scans_generate_data_quality_rules_task,
    dataplex_projects_locations_data_scans_get_builder, dataplex_projects_locations_data_scans_get_task,
    dataplex_projects_locations_data_scans_get_iam_policy_builder, dataplex_projects_locations_data_scans_get_iam_policy_task,
    dataplex_projects_locations_data_scans_list_builder, dataplex_projects_locations_data_scans_list_task,
    dataplex_projects_locations_data_scans_patch_builder, dataplex_projects_locations_data_scans_patch_task,
    dataplex_projects_locations_data_scans_run_builder, dataplex_projects_locations_data_scans_run_task,
    dataplex_projects_locations_data_scans_set_iam_policy_builder, dataplex_projects_locations_data_scans_set_iam_policy_task,
    dataplex_projects_locations_data_scans_test_iam_permissions_builder, dataplex_projects_locations_data_scans_test_iam_permissions_task,
    dataplex_projects_locations_data_scans_jobs_generate_data_quality_rules_builder, dataplex_projects_locations_data_scans_jobs_generate_data_quality_rules_task,
    dataplex_projects_locations_data_scans_jobs_get_builder, dataplex_projects_locations_data_scans_jobs_get_task,
    dataplex_projects_locations_data_scans_jobs_list_builder, dataplex_projects_locations_data_scans_jobs_list_task,
    dataplex_projects_locations_data_taxonomies_create_builder, dataplex_projects_locations_data_taxonomies_create_task,
    dataplex_projects_locations_data_taxonomies_delete_builder, dataplex_projects_locations_data_taxonomies_delete_task,
    dataplex_projects_locations_data_taxonomies_get_builder, dataplex_projects_locations_data_taxonomies_get_task,
    dataplex_projects_locations_data_taxonomies_get_iam_policy_builder, dataplex_projects_locations_data_taxonomies_get_iam_policy_task,
    dataplex_projects_locations_data_taxonomies_list_builder, dataplex_projects_locations_data_taxonomies_list_task,
    dataplex_projects_locations_data_taxonomies_patch_builder, dataplex_projects_locations_data_taxonomies_patch_task,
    dataplex_projects_locations_data_taxonomies_set_iam_policy_builder, dataplex_projects_locations_data_taxonomies_set_iam_policy_task,
    dataplex_projects_locations_data_taxonomies_test_iam_permissions_builder, dataplex_projects_locations_data_taxonomies_test_iam_permissions_task,
    dataplex_projects_locations_data_taxonomies_attributes_create_builder, dataplex_projects_locations_data_taxonomies_attributes_create_task,
    dataplex_projects_locations_data_taxonomies_attributes_delete_builder, dataplex_projects_locations_data_taxonomies_attributes_delete_task,
    dataplex_projects_locations_data_taxonomies_attributes_get_builder, dataplex_projects_locations_data_taxonomies_attributes_get_task,
    dataplex_projects_locations_data_taxonomies_attributes_get_iam_policy_builder, dataplex_projects_locations_data_taxonomies_attributes_get_iam_policy_task,
    dataplex_projects_locations_data_taxonomies_attributes_list_builder, dataplex_projects_locations_data_taxonomies_attributes_list_task,
    dataplex_projects_locations_data_taxonomies_attributes_patch_builder, dataplex_projects_locations_data_taxonomies_attributes_patch_task,
    dataplex_projects_locations_data_taxonomies_attributes_set_iam_policy_builder, dataplex_projects_locations_data_taxonomies_attributes_set_iam_policy_task,
    dataplex_projects_locations_data_taxonomies_attributes_test_iam_permissions_builder, dataplex_projects_locations_data_taxonomies_attributes_test_iam_permissions_task,
    dataplex_projects_locations_entry_groups_create_builder, dataplex_projects_locations_entry_groups_create_task,
    dataplex_projects_locations_entry_groups_delete_builder, dataplex_projects_locations_entry_groups_delete_task,
    dataplex_projects_locations_entry_groups_get_builder, dataplex_projects_locations_entry_groups_get_task,
    dataplex_projects_locations_entry_groups_get_iam_policy_builder, dataplex_projects_locations_entry_groups_get_iam_policy_task,
    dataplex_projects_locations_entry_groups_list_builder, dataplex_projects_locations_entry_groups_list_task,
    dataplex_projects_locations_entry_groups_patch_builder, dataplex_projects_locations_entry_groups_patch_task,
    dataplex_projects_locations_entry_groups_set_iam_policy_builder, dataplex_projects_locations_entry_groups_set_iam_policy_task,
    dataplex_projects_locations_entry_groups_test_iam_permissions_builder, dataplex_projects_locations_entry_groups_test_iam_permissions_task,
    dataplex_projects_locations_entry_groups_entries_create_builder, dataplex_projects_locations_entry_groups_entries_create_task,
    dataplex_projects_locations_entry_groups_entries_delete_builder, dataplex_projects_locations_entry_groups_entries_delete_task,
    dataplex_projects_locations_entry_groups_entries_get_builder, dataplex_projects_locations_entry_groups_entries_get_task,
    dataplex_projects_locations_entry_groups_entries_list_builder, dataplex_projects_locations_entry_groups_entries_list_task,
    dataplex_projects_locations_entry_groups_entries_patch_builder, dataplex_projects_locations_entry_groups_entries_patch_task,
    dataplex_projects_locations_entry_groups_entry_links_create_builder, dataplex_projects_locations_entry_groups_entry_links_create_task,
    dataplex_projects_locations_entry_groups_entry_links_delete_builder, dataplex_projects_locations_entry_groups_entry_links_delete_task,
    dataplex_projects_locations_entry_groups_entry_links_get_builder, dataplex_projects_locations_entry_groups_entry_links_get_task,
    dataplex_projects_locations_entry_groups_entry_links_patch_builder, dataplex_projects_locations_entry_groups_entry_links_patch_task,
    dataplex_projects_locations_entry_link_types_get_iam_policy_builder, dataplex_projects_locations_entry_link_types_get_iam_policy_task,
    dataplex_projects_locations_entry_link_types_set_iam_policy_builder, dataplex_projects_locations_entry_link_types_set_iam_policy_task,
    dataplex_projects_locations_entry_link_types_test_iam_permissions_builder, dataplex_projects_locations_entry_link_types_test_iam_permissions_task,
    dataplex_projects_locations_entry_types_create_builder, dataplex_projects_locations_entry_types_create_task,
    dataplex_projects_locations_entry_types_delete_builder, dataplex_projects_locations_entry_types_delete_task,
    dataplex_projects_locations_entry_types_get_builder, dataplex_projects_locations_entry_types_get_task,
    dataplex_projects_locations_entry_types_get_iam_policy_builder, dataplex_projects_locations_entry_types_get_iam_policy_task,
    dataplex_projects_locations_entry_types_list_builder, dataplex_projects_locations_entry_types_list_task,
    dataplex_projects_locations_entry_types_patch_builder, dataplex_projects_locations_entry_types_patch_task,
    dataplex_projects_locations_entry_types_set_iam_policy_builder, dataplex_projects_locations_entry_types_set_iam_policy_task,
    dataplex_projects_locations_entry_types_test_iam_permissions_builder, dataplex_projects_locations_entry_types_test_iam_permissions_task,
    dataplex_projects_locations_glossaries_create_builder, dataplex_projects_locations_glossaries_create_task,
    dataplex_projects_locations_glossaries_delete_builder, dataplex_projects_locations_glossaries_delete_task,
    dataplex_projects_locations_glossaries_get_builder, dataplex_projects_locations_glossaries_get_task,
    dataplex_projects_locations_glossaries_get_iam_policy_builder, dataplex_projects_locations_glossaries_get_iam_policy_task,
    dataplex_projects_locations_glossaries_list_builder, dataplex_projects_locations_glossaries_list_task,
    dataplex_projects_locations_glossaries_patch_builder, dataplex_projects_locations_glossaries_patch_task,
    dataplex_projects_locations_glossaries_set_iam_policy_builder, dataplex_projects_locations_glossaries_set_iam_policy_task,
    dataplex_projects_locations_glossaries_test_iam_permissions_builder, dataplex_projects_locations_glossaries_test_iam_permissions_task,
    dataplex_projects_locations_glossaries_categories_create_builder, dataplex_projects_locations_glossaries_categories_create_task,
    dataplex_projects_locations_glossaries_categories_delete_builder, dataplex_projects_locations_glossaries_categories_delete_task,
    dataplex_projects_locations_glossaries_categories_get_builder, dataplex_projects_locations_glossaries_categories_get_task,
    dataplex_projects_locations_glossaries_categories_get_iam_policy_builder, dataplex_projects_locations_glossaries_categories_get_iam_policy_task,
    dataplex_projects_locations_glossaries_categories_list_builder, dataplex_projects_locations_glossaries_categories_list_task,
    dataplex_projects_locations_glossaries_categories_patch_builder, dataplex_projects_locations_glossaries_categories_patch_task,
    dataplex_projects_locations_glossaries_categories_set_iam_policy_builder, dataplex_projects_locations_glossaries_categories_set_iam_policy_task,
    dataplex_projects_locations_glossaries_categories_test_iam_permissions_builder, dataplex_projects_locations_glossaries_categories_test_iam_permissions_task,
    dataplex_projects_locations_glossaries_terms_create_builder, dataplex_projects_locations_glossaries_terms_create_task,
    dataplex_projects_locations_glossaries_terms_delete_builder, dataplex_projects_locations_glossaries_terms_delete_task,
    dataplex_projects_locations_glossaries_terms_get_builder, dataplex_projects_locations_glossaries_terms_get_task,
    dataplex_projects_locations_glossaries_terms_get_iam_policy_builder, dataplex_projects_locations_glossaries_terms_get_iam_policy_task,
    dataplex_projects_locations_glossaries_terms_list_builder, dataplex_projects_locations_glossaries_terms_list_task,
    dataplex_projects_locations_glossaries_terms_patch_builder, dataplex_projects_locations_glossaries_terms_patch_task,
    dataplex_projects_locations_glossaries_terms_set_iam_policy_builder, dataplex_projects_locations_glossaries_terms_set_iam_policy_task,
    dataplex_projects_locations_glossaries_terms_test_iam_permissions_builder, dataplex_projects_locations_glossaries_terms_test_iam_permissions_task,
    dataplex_projects_locations_governance_rules_get_iam_policy_builder, dataplex_projects_locations_governance_rules_get_iam_policy_task,
    dataplex_projects_locations_governance_rules_set_iam_policy_builder, dataplex_projects_locations_governance_rules_set_iam_policy_task,
    dataplex_projects_locations_governance_rules_test_iam_permissions_builder, dataplex_projects_locations_governance_rules_test_iam_permissions_task,
    dataplex_projects_locations_lakes_create_builder, dataplex_projects_locations_lakes_create_task,
    dataplex_projects_locations_lakes_delete_builder, dataplex_projects_locations_lakes_delete_task,
    dataplex_projects_locations_lakes_get_builder, dataplex_projects_locations_lakes_get_task,
    dataplex_projects_locations_lakes_get_iam_policy_builder, dataplex_projects_locations_lakes_get_iam_policy_task,
    dataplex_projects_locations_lakes_list_builder, dataplex_projects_locations_lakes_list_task,
    dataplex_projects_locations_lakes_patch_builder, dataplex_projects_locations_lakes_patch_task,
    dataplex_projects_locations_lakes_set_iam_policy_builder, dataplex_projects_locations_lakes_set_iam_policy_task,
    dataplex_projects_locations_lakes_test_iam_permissions_builder, dataplex_projects_locations_lakes_test_iam_permissions_task,
    dataplex_projects_locations_lakes_actions_list_builder, dataplex_projects_locations_lakes_actions_list_task,
    dataplex_projects_locations_lakes_tasks_create_builder, dataplex_projects_locations_lakes_tasks_create_task,
    dataplex_projects_locations_lakes_tasks_delete_builder, dataplex_projects_locations_lakes_tasks_delete_task,
    dataplex_projects_locations_lakes_tasks_get_builder, dataplex_projects_locations_lakes_tasks_get_task,
    dataplex_projects_locations_lakes_tasks_get_iam_policy_builder, dataplex_projects_locations_lakes_tasks_get_iam_policy_task,
    dataplex_projects_locations_lakes_tasks_list_builder, dataplex_projects_locations_lakes_tasks_list_task,
    dataplex_projects_locations_lakes_tasks_patch_builder, dataplex_projects_locations_lakes_tasks_patch_task,
    dataplex_projects_locations_lakes_tasks_run_builder, dataplex_projects_locations_lakes_tasks_run_task,
    dataplex_projects_locations_lakes_tasks_set_iam_policy_builder, dataplex_projects_locations_lakes_tasks_set_iam_policy_task,
    dataplex_projects_locations_lakes_tasks_test_iam_permissions_builder, dataplex_projects_locations_lakes_tasks_test_iam_permissions_task,
    dataplex_projects_locations_lakes_tasks_jobs_cancel_builder, dataplex_projects_locations_lakes_tasks_jobs_cancel_task,
    dataplex_projects_locations_lakes_tasks_jobs_get_builder, dataplex_projects_locations_lakes_tasks_jobs_get_task,
    dataplex_projects_locations_lakes_tasks_jobs_list_builder, dataplex_projects_locations_lakes_tasks_jobs_list_task,
    dataplex_projects_locations_lakes_zones_create_builder, dataplex_projects_locations_lakes_zones_create_task,
    dataplex_projects_locations_lakes_zones_delete_builder, dataplex_projects_locations_lakes_zones_delete_task,
    dataplex_projects_locations_lakes_zones_get_builder, dataplex_projects_locations_lakes_zones_get_task,
    dataplex_projects_locations_lakes_zones_get_iam_policy_builder, dataplex_projects_locations_lakes_zones_get_iam_policy_task,
    dataplex_projects_locations_lakes_zones_list_builder, dataplex_projects_locations_lakes_zones_list_task,
    dataplex_projects_locations_lakes_zones_patch_builder, dataplex_projects_locations_lakes_zones_patch_task,
    dataplex_projects_locations_lakes_zones_set_iam_policy_builder, dataplex_projects_locations_lakes_zones_set_iam_policy_task,
    dataplex_projects_locations_lakes_zones_test_iam_permissions_builder, dataplex_projects_locations_lakes_zones_test_iam_permissions_task,
    dataplex_projects_locations_lakes_zones_actions_list_builder, dataplex_projects_locations_lakes_zones_actions_list_task,
    dataplex_projects_locations_lakes_zones_assets_create_builder, dataplex_projects_locations_lakes_zones_assets_create_task,
    dataplex_projects_locations_lakes_zones_assets_delete_builder, dataplex_projects_locations_lakes_zones_assets_delete_task,
    dataplex_projects_locations_lakes_zones_assets_get_builder, dataplex_projects_locations_lakes_zones_assets_get_task,
    dataplex_projects_locations_lakes_zones_assets_get_iam_policy_builder, dataplex_projects_locations_lakes_zones_assets_get_iam_policy_task,
    dataplex_projects_locations_lakes_zones_assets_list_builder, dataplex_projects_locations_lakes_zones_assets_list_task,
    dataplex_projects_locations_lakes_zones_assets_patch_builder, dataplex_projects_locations_lakes_zones_assets_patch_task,
    dataplex_projects_locations_lakes_zones_assets_set_iam_policy_builder, dataplex_projects_locations_lakes_zones_assets_set_iam_policy_task,
    dataplex_projects_locations_lakes_zones_assets_test_iam_permissions_builder, dataplex_projects_locations_lakes_zones_assets_test_iam_permissions_task,
    dataplex_projects_locations_lakes_zones_assets_actions_list_builder, dataplex_projects_locations_lakes_zones_assets_actions_list_task,
    dataplex_projects_locations_lakes_zones_entities_create_builder, dataplex_projects_locations_lakes_zones_entities_create_task,
    dataplex_projects_locations_lakes_zones_entities_delete_builder, dataplex_projects_locations_lakes_zones_entities_delete_task,
    dataplex_projects_locations_lakes_zones_entities_get_builder, dataplex_projects_locations_lakes_zones_entities_get_task,
    dataplex_projects_locations_lakes_zones_entities_list_builder, dataplex_projects_locations_lakes_zones_entities_list_task,
    dataplex_projects_locations_lakes_zones_entities_update_builder, dataplex_projects_locations_lakes_zones_entities_update_task,
    dataplex_projects_locations_lakes_zones_entities_partitions_create_builder, dataplex_projects_locations_lakes_zones_entities_partitions_create_task,
    dataplex_projects_locations_lakes_zones_entities_partitions_delete_builder, dataplex_projects_locations_lakes_zones_entities_partitions_delete_task,
    dataplex_projects_locations_lakes_zones_entities_partitions_get_builder, dataplex_projects_locations_lakes_zones_entities_partitions_get_task,
    dataplex_projects_locations_lakes_zones_entities_partitions_list_builder, dataplex_projects_locations_lakes_zones_entities_partitions_list_task,
    dataplex_projects_locations_metadata_feeds_create_builder, dataplex_projects_locations_metadata_feeds_create_task,
    dataplex_projects_locations_metadata_feeds_delete_builder, dataplex_projects_locations_metadata_feeds_delete_task,
    dataplex_projects_locations_metadata_feeds_get_builder, dataplex_projects_locations_metadata_feeds_get_task,
    dataplex_projects_locations_metadata_feeds_list_builder, dataplex_projects_locations_metadata_feeds_list_task,
    dataplex_projects_locations_metadata_feeds_patch_builder, dataplex_projects_locations_metadata_feeds_patch_task,
    dataplex_projects_locations_metadata_jobs_cancel_builder, dataplex_projects_locations_metadata_jobs_cancel_task,
    dataplex_projects_locations_metadata_jobs_create_builder, dataplex_projects_locations_metadata_jobs_create_task,
    dataplex_projects_locations_metadata_jobs_get_builder, dataplex_projects_locations_metadata_jobs_get_task,
    dataplex_projects_locations_metadata_jobs_list_builder, dataplex_projects_locations_metadata_jobs_list_task,
    dataplex_projects_locations_operations_cancel_builder, dataplex_projects_locations_operations_cancel_task,
    dataplex_projects_locations_operations_delete_builder, dataplex_projects_locations_operations_delete_task,
    dataplex_projects_locations_operations_get_builder, dataplex_projects_locations_operations_get_task,
    dataplex_projects_locations_operations_list_builder, dataplex_projects_locations_operations_list_task,
    dataplex_projects_locations_policy_intents_get_iam_policy_builder, dataplex_projects_locations_policy_intents_get_iam_policy_task,
    dataplex_projects_locations_policy_intents_set_iam_policy_builder, dataplex_projects_locations_policy_intents_set_iam_policy_task,
    dataplex_projects_locations_policy_intents_test_iam_permissions_builder, dataplex_projects_locations_policy_intents_test_iam_permissions_task,
};
use crate::providers::gcp::clients::types::{ApiError, ApiPending};
use crate::providers::gcp::clients::dataplex::Empty;
use crate::providers::gcp::clients::dataplex::GoogleCloudDataplexV1AspectType;
use crate::providers::gcp::clients::dataplex::GoogleCloudDataplexV1Asset;
use crate::providers::gcp::clients::dataplex::GoogleCloudDataplexV1DataAsset;
use crate::providers::gcp::clients::dataplex::GoogleCloudDataplexV1DataAttribute;
use crate::providers::gcp::clients::dataplex::GoogleCloudDataplexV1DataAttributeBinding;
use crate::providers::gcp::clients::dataplex::GoogleCloudDataplexV1DataProduct;
use crate::providers::gcp::clients::dataplex::GoogleCloudDataplexV1DataScan;
use crate::providers::gcp::clients::dataplex::GoogleCloudDataplexV1DataScanJob;
use crate::providers::gcp::clients::dataplex::GoogleCloudDataplexV1DataTaxonomy;
use crate::providers::gcp::clients::dataplex::GoogleCloudDataplexV1EncryptionConfig;
use crate::providers::gcp::clients::dataplex::GoogleCloudDataplexV1Entity;
use crate::providers::gcp::clients::dataplex::GoogleCloudDataplexV1Entry;
use crate::providers::gcp::clients::dataplex::GoogleCloudDataplexV1EntryGroup;
use crate::providers::gcp::clients::dataplex::GoogleCloudDataplexV1EntryLink;
use crate::providers::gcp::clients::dataplex::GoogleCloudDataplexV1EntryType;
use crate::providers::gcp::clients::dataplex::GoogleCloudDataplexV1GenerateDataQualityRulesResponse;
use crate::providers::gcp::clients::dataplex::GoogleCloudDataplexV1Glossary;
use crate::providers::gcp::clients::dataplex::GoogleCloudDataplexV1GlossaryCategory;
use crate::providers::gcp::clients::dataplex::GoogleCloudDataplexV1GlossaryTerm;
use crate::providers::gcp::clients::dataplex::GoogleCloudDataplexV1Job;
use crate::providers::gcp::clients::dataplex::GoogleCloudDataplexV1Lake;
use crate::providers::gcp::clients::dataplex::GoogleCloudDataplexV1ListActionsResponse;
use crate::providers::gcp::clients::dataplex::GoogleCloudDataplexV1ListAspectTypesResponse;
use crate::providers::gcp::clients::dataplex::GoogleCloudDataplexV1ListAssetsResponse;
use crate::providers::gcp::clients::dataplex::GoogleCloudDataplexV1ListDataAssetsResponse;
use crate::providers::gcp::clients::dataplex::GoogleCloudDataplexV1ListDataAttributeBindingsResponse;
use crate::providers::gcp::clients::dataplex::GoogleCloudDataplexV1ListDataAttributesResponse;
use crate::providers::gcp::clients::dataplex::GoogleCloudDataplexV1ListDataProductsResponse;
use crate::providers::gcp::clients::dataplex::GoogleCloudDataplexV1ListDataScanJobsResponse;
use crate::providers::gcp::clients::dataplex::GoogleCloudDataplexV1ListDataScansResponse;
use crate::providers::gcp::clients::dataplex::GoogleCloudDataplexV1ListDataTaxonomiesResponse;
use crate::providers::gcp::clients::dataplex::GoogleCloudDataplexV1ListEncryptionConfigsResponse;
use crate::providers::gcp::clients::dataplex::GoogleCloudDataplexV1ListEntitiesResponse;
use crate::providers::gcp::clients::dataplex::GoogleCloudDataplexV1ListEntriesResponse;
use crate::providers::gcp::clients::dataplex::GoogleCloudDataplexV1ListEntryGroupsResponse;
use crate::providers::gcp::clients::dataplex::GoogleCloudDataplexV1ListEntryTypesResponse;
use crate::providers::gcp::clients::dataplex::GoogleCloudDataplexV1ListGlossariesResponse;
use crate::providers::gcp::clients::dataplex::GoogleCloudDataplexV1ListGlossaryCategoriesResponse;
use crate::providers::gcp::clients::dataplex::GoogleCloudDataplexV1ListGlossaryTermsResponse;
use crate::providers::gcp::clients::dataplex::GoogleCloudDataplexV1ListJobsResponse;
use crate::providers::gcp::clients::dataplex::GoogleCloudDataplexV1ListLakesResponse;
use crate::providers::gcp::clients::dataplex::GoogleCloudDataplexV1ListMetadataFeedsResponse;
use crate::providers::gcp::clients::dataplex::GoogleCloudDataplexV1ListMetadataJobsResponse;
use crate::providers::gcp::clients::dataplex::GoogleCloudDataplexV1ListPartitionsResponse;
use crate::providers::gcp::clients::dataplex::GoogleCloudDataplexV1ListTasksResponse;
use crate::providers::gcp::clients::dataplex::GoogleCloudDataplexV1ListZonesResponse;
use crate::providers::gcp::clients::dataplex::GoogleCloudDataplexV1LookupContextResponse;
use crate::providers::gcp::clients::dataplex::GoogleCloudDataplexV1LookupEntryLinksResponse;
use crate::providers::gcp::clients::dataplex::GoogleCloudDataplexV1MetadataFeed;
use crate::providers::gcp::clients::dataplex::GoogleCloudDataplexV1MetadataJob;
use crate::providers::gcp::clients::dataplex::GoogleCloudDataplexV1Partition;
use crate::providers::gcp::clients::dataplex::GoogleCloudDataplexV1RunDataScanResponse;
use crate::providers::gcp::clients::dataplex::GoogleCloudDataplexV1RunTaskResponse;
use crate::providers::gcp::clients::dataplex::GoogleCloudDataplexV1SearchEntriesResponse;
use crate::providers::gcp::clients::dataplex::GoogleCloudDataplexV1Task;
use crate::providers::gcp::clients::dataplex::GoogleCloudDataplexV1Zone;
use crate::providers::gcp::clients::dataplex::GoogleCloudLocationListLocationsResponse;
use crate::providers::gcp::clients::dataplex::GoogleCloudLocationLocation;
use crate::providers::gcp::clients::dataplex::GoogleIamV1Policy;
use crate::providers::gcp::clients::dataplex::GoogleIamV1TestIamPermissionsResponse;
use crate::providers::gcp::clients::dataplex::GoogleLongrunningListOperationsResponse;
use crate::providers::gcp::clients::dataplex::GoogleLongrunningOperation;
use crate::providers::gcp::clients::dataplex::DataplexOrganizationsLocationsEncryptionConfigsCreateArgs;
use crate::providers::gcp::clients::dataplex::DataplexOrganizationsLocationsEncryptionConfigsDeleteArgs;
use crate::providers::gcp::clients::dataplex::DataplexOrganizationsLocationsEncryptionConfigsGetArgs;
use crate::providers::gcp::clients::dataplex::DataplexOrganizationsLocationsEncryptionConfigsGetIamPolicyArgs;
use crate::providers::gcp::clients::dataplex::DataplexOrganizationsLocationsEncryptionConfigsListArgs;
use crate::providers::gcp::clients::dataplex::DataplexOrganizationsLocationsEncryptionConfigsPatchArgs;
use crate::providers::gcp::clients::dataplex::DataplexOrganizationsLocationsEncryptionConfigsSetIamPolicyArgs;
use crate::providers::gcp::clients::dataplex::DataplexOrganizationsLocationsEncryptionConfigsTestIamPermissionsArgs;
use crate::providers::gcp::clients::dataplex::DataplexOrganizationsLocationsOperationsCancelArgs;
use crate::providers::gcp::clients::dataplex::DataplexOrganizationsLocationsOperationsDeleteArgs;
use crate::providers::gcp::clients::dataplex::DataplexOrganizationsLocationsOperationsGetArgs;
use crate::providers::gcp::clients::dataplex::DataplexOrganizationsLocationsOperationsListArgs;
use crate::providers::gcp::clients::dataplex::DataplexProjectsLocationsAspectTypesCreateArgs;
use crate::providers::gcp::clients::dataplex::DataplexProjectsLocationsAspectTypesDeleteArgs;
use crate::providers::gcp::clients::dataplex::DataplexProjectsLocationsAspectTypesGetArgs;
use crate::providers::gcp::clients::dataplex::DataplexProjectsLocationsAspectTypesGetIamPolicyArgs;
use crate::providers::gcp::clients::dataplex::DataplexProjectsLocationsAspectTypesListArgs;
use crate::providers::gcp::clients::dataplex::DataplexProjectsLocationsAspectTypesPatchArgs;
use crate::providers::gcp::clients::dataplex::DataplexProjectsLocationsAspectTypesSetIamPolicyArgs;
use crate::providers::gcp::clients::dataplex::DataplexProjectsLocationsAspectTypesTestIamPermissionsArgs;
use crate::providers::gcp::clients::dataplex::DataplexProjectsLocationsChangeRequestsGetIamPolicyArgs;
use crate::providers::gcp::clients::dataplex::DataplexProjectsLocationsChangeRequestsSetIamPolicyArgs;
use crate::providers::gcp::clients::dataplex::DataplexProjectsLocationsChangeRequestsTestIamPermissionsArgs;
use crate::providers::gcp::clients::dataplex::DataplexProjectsLocationsDataAttributeBindingsCreateArgs;
use crate::providers::gcp::clients::dataplex::DataplexProjectsLocationsDataAttributeBindingsDeleteArgs;
use crate::providers::gcp::clients::dataplex::DataplexProjectsLocationsDataAttributeBindingsGetArgs;
use crate::providers::gcp::clients::dataplex::DataplexProjectsLocationsDataAttributeBindingsGetIamPolicyArgs;
use crate::providers::gcp::clients::dataplex::DataplexProjectsLocationsDataAttributeBindingsListArgs;
use crate::providers::gcp::clients::dataplex::DataplexProjectsLocationsDataAttributeBindingsPatchArgs;
use crate::providers::gcp::clients::dataplex::DataplexProjectsLocationsDataAttributeBindingsSetIamPolicyArgs;
use crate::providers::gcp::clients::dataplex::DataplexProjectsLocationsDataAttributeBindingsTestIamPermissionsArgs;
use crate::providers::gcp::clients::dataplex::DataplexProjectsLocationsDataDomainsGetIamPolicyArgs;
use crate::providers::gcp::clients::dataplex::DataplexProjectsLocationsDataDomainsSetIamPolicyArgs;
use crate::providers::gcp::clients::dataplex::DataplexProjectsLocationsDataDomainsTestIamPermissionsArgs;
use crate::providers::gcp::clients::dataplex::DataplexProjectsLocationsDataProductsCreateArgs;
use crate::providers::gcp::clients::dataplex::DataplexProjectsLocationsDataProductsDataAssetsCreateArgs;
use crate::providers::gcp::clients::dataplex::DataplexProjectsLocationsDataProductsDataAssetsDeleteArgs;
use crate::providers::gcp::clients::dataplex::DataplexProjectsLocationsDataProductsDataAssetsGetArgs;
use crate::providers::gcp::clients::dataplex::DataplexProjectsLocationsDataProductsDataAssetsListArgs;
use crate::providers::gcp::clients::dataplex::DataplexProjectsLocationsDataProductsDataAssetsPatchArgs;
use crate::providers::gcp::clients::dataplex::DataplexProjectsLocationsDataProductsDeleteArgs;
use crate::providers::gcp::clients::dataplex::DataplexProjectsLocationsDataProductsGetArgs;
use crate::providers::gcp::clients::dataplex::DataplexProjectsLocationsDataProductsGetIamPolicyArgs;
use crate::providers::gcp::clients::dataplex::DataplexProjectsLocationsDataProductsListArgs;
use crate::providers::gcp::clients::dataplex::DataplexProjectsLocationsDataProductsPatchArgs;
use crate::providers::gcp::clients::dataplex::DataplexProjectsLocationsDataProductsSetIamPolicyArgs;
use crate::providers::gcp::clients::dataplex::DataplexProjectsLocationsDataProductsTestIamPermissionsArgs;
use crate::providers::gcp::clients::dataplex::DataplexProjectsLocationsDataScansCreateArgs;
use crate::providers::gcp::clients::dataplex::DataplexProjectsLocationsDataScansDeleteArgs;
use crate::providers::gcp::clients::dataplex::DataplexProjectsLocationsDataScansGenerateDataQualityRulesArgs;
use crate::providers::gcp::clients::dataplex::DataplexProjectsLocationsDataScansGetArgs;
use crate::providers::gcp::clients::dataplex::DataplexProjectsLocationsDataScansGetIamPolicyArgs;
use crate::providers::gcp::clients::dataplex::DataplexProjectsLocationsDataScansJobsGenerateDataQualityRulesArgs;
use crate::providers::gcp::clients::dataplex::DataplexProjectsLocationsDataScansJobsGetArgs;
use crate::providers::gcp::clients::dataplex::DataplexProjectsLocationsDataScansJobsListArgs;
use crate::providers::gcp::clients::dataplex::DataplexProjectsLocationsDataScansListArgs;
use crate::providers::gcp::clients::dataplex::DataplexProjectsLocationsDataScansPatchArgs;
use crate::providers::gcp::clients::dataplex::DataplexProjectsLocationsDataScansRunArgs;
use crate::providers::gcp::clients::dataplex::DataplexProjectsLocationsDataScansSetIamPolicyArgs;
use crate::providers::gcp::clients::dataplex::DataplexProjectsLocationsDataScansTestIamPermissionsArgs;
use crate::providers::gcp::clients::dataplex::DataplexProjectsLocationsDataTaxonomiesAttributesCreateArgs;
use crate::providers::gcp::clients::dataplex::DataplexProjectsLocationsDataTaxonomiesAttributesDeleteArgs;
use crate::providers::gcp::clients::dataplex::DataplexProjectsLocationsDataTaxonomiesAttributesGetArgs;
use crate::providers::gcp::clients::dataplex::DataplexProjectsLocationsDataTaxonomiesAttributesGetIamPolicyArgs;
use crate::providers::gcp::clients::dataplex::DataplexProjectsLocationsDataTaxonomiesAttributesListArgs;
use crate::providers::gcp::clients::dataplex::DataplexProjectsLocationsDataTaxonomiesAttributesPatchArgs;
use crate::providers::gcp::clients::dataplex::DataplexProjectsLocationsDataTaxonomiesAttributesSetIamPolicyArgs;
use crate::providers::gcp::clients::dataplex::DataplexProjectsLocationsDataTaxonomiesAttributesTestIamPermissionsArgs;
use crate::providers::gcp::clients::dataplex::DataplexProjectsLocationsDataTaxonomiesCreateArgs;
use crate::providers::gcp::clients::dataplex::DataplexProjectsLocationsDataTaxonomiesDeleteArgs;
use crate::providers::gcp::clients::dataplex::DataplexProjectsLocationsDataTaxonomiesGetArgs;
use crate::providers::gcp::clients::dataplex::DataplexProjectsLocationsDataTaxonomiesGetIamPolicyArgs;
use crate::providers::gcp::clients::dataplex::DataplexProjectsLocationsDataTaxonomiesListArgs;
use crate::providers::gcp::clients::dataplex::DataplexProjectsLocationsDataTaxonomiesPatchArgs;
use crate::providers::gcp::clients::dataplex::DataplexProjectsLocationsDataTaxonomiesSetIamPolicyArgs;
use crate::providers::gcp::clients::dataplex::DataplexProjectsLocationsDataTaxonomiesTestIamPermissionsArgs;
use crate::providers::gcp::clients::dataplex::DataplexProjectsLocationsEntryGroupsCreateArgs;
use crate::providers::gcp::clients::dataplex::DataplexProjectsLocationsEntryGroupsDeleteArgs;
use crate::providers::gcp::clients::dataplex::DataplexProjectsLocationsEntryGroupsEntriesCreateArgs;
use crate::providers::gcp::clients::dataplex::DataplexProjectsLocationsEntryGroupsEntriesDeleteArgs;
use crate::providers::gcp::clients::dataplex::DataplexProjectsLocationsEntryGroupsEntriesGetArgs;
use crate::providers::gcp::clients::dataplex::DataplexProjectsLocationsEntryGroupsEntriesListArgs;
use crate::providers::gcp::clients::dataplex::DataplexProjectsLocationsEntryGroupsEntriesPatchArgs;
use crate::providers::gcp::clients::dataplex::DataplexProjectsLocationsEntryGroupsEntryLinksCreateArgs;
use crate::providers::gcp::clients::dataplex::DataplexProjectsLocationsEntryGroupsEntryLinksDeleteArgs;
use crate::providers::gcp::clients::dataplex::DataplexProjectsLocationsEntryGroupsEntryLinksGetArgs;
use crate::providers::gcp::clients::dataplex::DataplexProjectsLocationsEntryGroupsEntryLinksPatchArgs;
use crate::providers::gcp::clients::dataplex::DataplexProjectsLocationsEntryGroupsGetArgs;
use crate::providers::gcp::clients::dataplex::DataplexProjectsLocationsEntryGroupsGetIamPolicyArgs;
use crate::providers::gcp::clients::dataplex::DataplexProjectsLocationsEntryGroupsListArgs;
use crate::providers::gcp::clients::dataplex::DataplexProjectsLocationsEntryGroupsPatchArgs;
use crate::providers::gcp::clients::dataplex::DataplexProjectsLocationsEntryGroupsSetIamPolicyArgs;
use crate::providers::gcp::clients::dataplex::DataplexProjectsLocationsEntryGroupsTestIamPermissionsArgs;
use crate::providers::gcp::clients::dataplex::DataplexProjectsLocationsEntryLinkTypesGetIamPolicyArgs;
use crate::providers::gcp::clients::dataplex::DataplexProjectsLocationsEntryLinkTypesSetIamPolicyArgs;
use crate::providers::gcp::clients::dataplex::DataplexProjectsLocationsEntryLinkTypesTestIamPermissionsArgs;
use crate::providers::gcp::clients::dataplex::DataplexProjectsLocationsEntryTypesCreateArgs;
use crate::providers::gcp::clients::dataplex::DataplexProjectsLocationsEntryTypesDeleteArgs;
use crate::providers::gcp::clients::dataplex::DataplexProjectsLocationsEntryTypesGetArgs;
use crate::providers::gcp::clients::dataplex::DataplexProjectsLocationsEntryTypesGetIamPolicyArgs;
use crate::providers::gcp::clients::dataplex::DataplexProjectsLocationsEntryTypesListArgs;
use crate::providers::gcp::clients::dataplex::DataplexProjectsLocationsEntryTypesPatchArgs;
use crate::providers::gcp::clients::dataplex::DataplexProjectsLocationsEntryTypesSetIamPolicyArgs;
use crate::providers::gcp::clients::dataplex::DataplexProjectsLocationsEntryTypesTestIamPermissionsArgs;
use crate::providers::gcp::clients::dataplex::DataplexProjectsLocationsGetArgs;
use crate::providers::gcp::clients::dataplex::DataplexProjectsLocationsGlossariesCategoriesCreateArgs;
use crate::providers::gcp::clients::dataplex::DataplexProjectsLocationsGlossariesCategoriesDeleteArgs;
use crate::providers::gcp::clients::dataplex::DataplexProjectsLocationsGlossariesCategoriesGetArgs;
use crate::providers::gcp::clients::dataplex::DataplexProjectsLocationsGlossariesCategoriesGetIamPolicyArgs;
use crate::providers::gcp::clients::dataplex::DataplexProjectsLocationsGlossariesCategoriesListArgs;
use crate::providers::gcp::clients::dataplex::DataplexProjectsLocationsGlossariesCategoriesPatchArgs;
use crate::providers::gcp::clients::dataplex::DataplexProjectsLocationsGlossariesCategoriesSetIamPolicyArgs;
use crate::providers::gcp::clients::dataplex::DataplexProjectsLocationsGlossariesCategoriesTestIamPermissionsArgs;
use crate::providers::gcp::clients::dataplex::DataplexProjectsLocationsGlossariesCreateArgs;
use crate::providers::gcp::clients::dataplex::DataplexProjectsLocationsGlossariesDeleteArgs;
use crate::providers::gcp::clients::dataplex::DataplexProjectsLocationsGlossariesGetArgs;
use crate::providers::gcp::clients::dataplex::DataplexProjectsLocationsGlossariesGetIamPolicyArgs;
use crate::providers::gcp::clients::dataplex::DataplexProjectsLocationsGlossariesListArgs;
use crate::providers::gcp::clients::dataplex::DataplexProjectsLocationsGlossariesPatchArgs;
use crate::providers::gcp::clients::dataplex::DataplexProjectsLocationsGlossariesSetIamPolicyArgs;
use crate::providers::gcp::clients::dataplex::DataplexProjectsLocationsGlossariesTermsCreateArgs;
use crate::providers::gcp::clients::dataplex::DataplexProjectsLocationsGlossariesTermsDeleteArgs;
use crate::providers::gcp::clients::dataplex::DataplexProjectsLocationsGlossariesTermsGetArgs;
use crate::providers::gcp::clients::dataplex::DataplexProjectsLocationsGlossariesTermsGetIamPolicyArgs;
use crate::providers::gcp::clients::dataplex::DataplexProjectsLocationsGlossariesTermsListArgs;
use crate::providers::gcp::clients::dataplex::DataplexProjectsLocationsGlossariesTermsPatchArgs;
use crate::providers::gcp::clients::dataplex::DataplexProjectsLocationsGlossariesTermsSetIamPolicyArgs;
use crate::providers::gcp::clients::dataplex::DataplexProjectsLocationsGlossariesTermsTestIamPermissionsArgs;
use crate::providers::gcp::clients::dataplex::DataplexProjectsLocationsGlossariesTestIamPermissionsArgs;
use crate::providers::gcp::clients::dataplex::DataplexProjectsLocationsGovernanceRulesGetIamPolicyArgs;
use crate::providers::gcp::clients::dataplex::DataplexProjectsLocationsGovernanceRulesSetIamPolicyArgs;
use crate::providers::gcp::clients::dataplex::DataplexProjectsLocationsGovernanceRulesTestIamPermissionsArgs;
use crate::providers::gcp::clients::dataplex::DataplexProjectsLocationsLakesActionsListArgs;
use crate::providers::gcp::clients::dataplex::DataplexProjectsLocationsLakesCreateArgs;
use crate::providers::gcp::clients::dataplex::DataplexProjectsLocationsLakesDeleteArgs;
use crate::providers::gcp::clients::dataplex::DataplexProjectsLocationsLakesGetArgs;
use crate::providers::gcp::clients::dataplex::DataplexProjectsLocationsLakesGetIamPolicyArgs;
use crate::providers::gcp::clients::dataplex::DataplexProjectsLocationsLakesListArgs;
use crate::providers::gcp::clients::dataplex::DataplexProjectsLocationsLakesPatchArgs;
use crate::providers::gcp::clients::dataplex::DataplexProjectsLocationsLakesSetIamPolicyArgs;
use crate::providers::gcp::clients::dataplex::DataplexProjectsLocationsLakesTasksCreateArgs;
use crate::providers::gcp::clients::dataplex::DataplexProjectsLocationsLakesTasksDeleteArgs;
use crate::providers::gcp::clients::dataplex::DataplexProjectsLocationsLakesTasksGetArgs;
use crate::providers::gcp::clients::dataplex::DataplexProjectsLocationsLakesTasksGetIamPolicyArgs;
use crate::providers::gcp::clients::dataplex::DataplexProjectsLocationsLakesTasksJobsCancelArgs;
use crate::providers::gcp::clients::dataplex::DataplexProjectsLocationsLakesTasksJobsGetArgs;
use crate::providers::gcp::clients::dataplex::DataplexProjectsLocationsLakesTasksJobsListArgs;
use crate::providers::gcp::clients::dataplex::DataplexProjectsLocationsLakesTasksListArgs;
use crate::providers::gcp::clients::dataplex::DataplexProjectsLocationsLakesTasksPatchArgs;
use crate::providers::gcp::clients::dataplex::DataplexProjectsLocationsLakesTasksRunArgs;
use crate::providers::gcp::clients::dataplex::DataplexProjectsLocationsLakesTasksSetIamPolicyArgs;
use crate::providers::gcp::clients::dataplex::DataplexProjectsLocationsLakesTasksTestIamPermissionsArgs;
use crate::providers::gcp::clients::dataplex::DataplexProjectsLocationsLakesTestIamPermissionsArgs;
use crate::providers::gcp::clients::dataplex::DataplexProjectsLocationsLakesZonesActionsListArgs;
use crate::providers::gcp::clients::dataplex::DataplexProjectsLocationsLakesZonesAssetsActionsListArgs;
use crate::providers::gcp::clients::dataplex::DataplexProjectsLocationsLakesZonesAssetsCreateArgs;
use crate::providers::gcp::clients::dataplex::DataplexProjectsLocationsLakesZonesAssetsDeleteArgs;
use crate::providers::gcp::clients::dataplex::DataplexProjectsLocationsLakesZonesAssetsGetArgs;
use crate::providers::gcp::clients::dataplex::DataplexProjectsLocationsLakesZonesAssetsGetIamPolicyArgs;
use crate::providers::gcp::clients::dataplex::DataplexProjectsLocationsLakesZonesAssetsListArgs;
use crate::providers::gcp::clients::dataplex::DataplexProjectsLocationsLakesZonesAssetsPatchArgs;
use crate::providers::gcp::clients::dataplex::DataplexProjectsLocationsLakesZonesAssetsSetIamPolicyArgs;
use crate::providers::gcp::clients::dataplex::DataplexProjectsLocationsLakesZonesAssetsTestIamPermissionsArgs;
use crate::providers::gcp::clients::dataplex::DataplexProjectsLocationsLakesZonesCreateArgs;
use crate::providers::gcp::clients::dataplex::DataplexProjectsLocationsLakesZonesDeleteArgs;
use crate::providers::gcp::clients::dataplex::DataplexProjectsLocationsLakesZonesEntitiesCreateArgs;
use crate::providers::gcp::clients::dataplex::DataplexProjectsLocationsLakesZonesEntitiesDeleteArgs;
use crate::providers::gcp::clients::dataplex::DataplexProjectsLocationsLakesZonesEntitiesGetArgs;
use crate::providers::gcp::clients::dataplex::DataplexProjectsLocationsLakesZonesEntitiesListArgs;
use crate::providers::gcp::clients::dataplex::DataplexProjectsLocationsLakesZonesEntitiesPartitionsCreateArgs;
use crate::providers::gcp::clients::dataplex::DataplexProjectsLocationsLakesZonesEntitiesPartitionsDeleteArgs;
use crate::providers::gcp::clients::dataplex::DataplexProjectsLocationsLakesZonesEntitiesPartitionsGetArgs;
use crate::providers::gcp::clients::dataplex::DataplexProjectsLocationsLakesZonesEntitiesPartitionsListArgs;
use crate::providers::gcp::clients::dataplex::DataplexProjectsLocationsLakesZonesEntitiesUpdateArgs;
use crate::providers::gcp::clients::dataplex::DataplexProjectsLocationsLakesZonesGetArgs;
use crate::providers::gcp::clients::dataplex::DataplexProjectsLocationsLakesZonesGetIamPolicyArgs;
use crate::providers::gcp::clients::dataplex::DataplexProjectsLocationsLakesZonesListArgs;
use crate::providers::gcp::clients::dataplex::DataplexProjectsLocationsLakesZonesPatchArgs;
use crate::providers::gcp::clients::dataplex::DataplexProjectsLocationsLakesZonesSetIamPolicyArgs;
use crate::providers::gcp::clients::dataplex::DataplexProjectsLocationsLakesZonesTestIamPermissionsArgs;
use crate::providers::gcp::clients::dataplex::DataplexProjectsLocationsListArgs;
use crate::providers::gcp::clients::dataplex::DataplexProjectsLocationsLookupContextArgs;
use crate::providers::gcp::clients::dataplex::DataplexProjectsLocationsLookupEntryArgs;
use crate::providers::gcp::clients::dataplex::DataplexProjectsLocationsLookupEntryLinksArgs;
use crate::providers::gcp::clients::dataplex::DataplexProjectsLocationsMetadataFeedsCreateArgs;
use crate::providers::gcp::clients::dataplex::DataplexProjectsLocationsMetadataFeedsDeleteArgs;
use crate::providers::gcp::clients::dataplex::DataplexProjectsLocationsMetadataFeedsGetArgs;
use crate::providers::gcp::clients::dataplex::DataplexProjectsLocationsMetadataFeedsListArgs;
use crate::providers::gcp::clients::dataplex::DataplexProjectsLocationsMetadataFeedsPatchArgs;
use crate::providers::gcp::clients::dataplex::DataplexProjectsLocationsMetadataJobsCancelArgs;
use crate::providers::gcp::clients::dataplex::DataplexProjectsLocationsMetadataJobsCreateArgs;
use crate::providers::gcp::clients::dataplex::DataplexProjectsLocationsMetadataJobsGetArgs;
use crate::providers::gcp::clients::dataplex::DataplexProjectsLocationsMetadataJobsListArgs;
use crate::providers::gcp::clients::dataplex::DataplexProjectsLocationsModifyEntryArgs;
use crate::providers::gcp::clients::dataplex::DataplexProjectsLocationsOperationsCancelArgs;
use crate::providers::gcp::clients::dataplex::DataplexProjectsLocationsOperationsDeleteArgs;
use crate::providers::gcp::clients::dataplex::DataplexProjectsLocationsOperationsGetArgs;
use crate::providers::gcp::clients::dataplex::DataplexProjectsLocationsOperationsListArgs;
use crate::providers::gcp::clients::dataplex::DataplexProjectsLocationsPolicyIntentsGetIamPolicyArgs;
use crate::providers::gcp::clients::dataplex::DataplexProjectsLocationsPolicyIntentsSetIamPolicyArgs;
use crate::providers::gcp::clients::dataplex::DataplexProjectsLocationsPolicyIntentsTestIamPermissionsArgs;
use crate::providers::gcp::clients::dataplex::DataplexProjectsLocationsSearchEntriesArgs;
use crate::provider_client::{ProviderClient, ProviderError};
use foundation_core::valtron::{execute, StreamIterator};
use foundation_core::wire::simple_http::client::SimpleHttpClient;
use foundation_db::state::store_state_task::StoreStateIdentifierTask;
use std::sync::Arc;

/// DataplexProvider with automatic state tracking.
///
/// # Type Parameters
///
/// * `S` - StateStore implementation (FileStateStore, SqliteStateStore, etc.)
///
/// # Example
///
/// ```rust
/// let state_store = FileStateStore::new("/path", "my-project", "dev");
/// let client = ProviderClient::new("my-project", "dev", state_store);
/// let http_client = SimpleHttpClient::new(...);
/// let provider = DataplexProvider::new(client, http_client);
/// ```
#[derive(Clone)]
pub struct DataplexProvider<S>
where
    S: foundation_db::state::traits::StateStore + Send + Sync + 'static,
{
    client: ProviderClient<S>,
    http_client: Arc<SimpleHttpClient>,
}

impl<S> DataplexProvider<S>
where
    S: foundation_db::state::traits::StateStore + Send + Sync + 'static,
{
    /// Create new DataplexProvider.
    pub fn new(client: ProviderClient<S>, http_client: SimpleHttpClient) -> Self {
        Self {
            client,
            http_client: Arc::new(http_client),
        }
    }

    /// Dataplex organizations locations encryption configs create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn dataplex_organizations_locations_encryption_configs_create(
        &self,
        args: &DataplexOrganizationsLocationsEncryptionConfigsCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dataplex_organizations_locations_encryption_configs_create_builder(
            &self.http_client,
            &args.parent,
            &args.encryptionConfigId,
        )
        .map_err(ProviderError::Api)?;

        let task = dataplex_organizations_locations_encryption_configs_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dataplex organizations locations encryption configs delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn dataplex_organizations_locations_encryption_configs_delete(
        &self,
        args: &DataplexOrganizationsLocationsEncryptionConfigsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dataplex_organizations_locations_encryption_configs_delete_builder(
            &self.http_client,
            &args.name,
            &args.etag,
        )
        .map_err(ProviderError::Api)?;

        let task = dataplex_organizations_locations_encryption_configs_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dataplex organizations locations encryption configs get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudDataplexV1EncryptionConfig result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn dataplex_organizations_locations_encryption_configs_get(
        &self,
        args: &DataplexOrganizationsLocationsEncryptionConfigsGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudDataplexV1EncryptionConfig, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dataplex_organizations_locations_encryption_configs_get_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = dataplex_organizations_locations_encryption_configs_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dataplex organizations locations encryption configs get iam policy.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleIamV1Policy result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn dataplex_organizations_locations_encryption_configs_get_iam_policy(
        &self,
        args: &DataplexOrganizationsLocationsEncryptionConfigsGetIamPolicyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleIamV1Policy, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dataplex_organizations_locations_encryption_configs_get_iam_policy_builder(
            &self.http_client,
            &args.resource,
            &args.options.requestedPolicyVersion,
        )
        .map_err(ProviderError::Api)?;

        let task = dataplex_organizations_locations_encryption_configs_get_iam_policy_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dataplex organizations locations encryption configs list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudDataplexV1ListEncryptionConfigsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn dataplex_organizations_locations_encryption_configs_list(
        &self,
        args: &DataplexOrganizationsLocationsEncryptionConfigsListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudDataplexV1ListEncryptionConfigsResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dataplex_organizations_locations_encryption_configs_list_builder(
            &self.http_client,
            &args.parent,
            &args.filter,
            &args.orderBy,
            &args.pageSize,
            &args.pageToken,
        )
        .map_err(ProviderError::Api)?;

        let task = dataplex_organizations_locations_encryption_configs_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dataplex organizations locations encryption configs patch.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn dataplex_organizations_locations_encryption_configs_patch(
        &self,
        args: &DataplexOrganizationsLocationsEncryptionConfigsPatchArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dataplex_organizations_locations_encryption_configs_patch_builder(
            &self.http_client,
            &args.name,
            &args.updateMask,
        )
        .map_err(ProviderError::Api)?;

        let task = dataplex_organizations_locations_encryption_configs_patch_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dataplex organizations locations encryption configs set iam policy.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleIamV1Policy result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn dataplex_organizations_locations_encryption_configs_set_iam_policy(
        &self,
        args: &DataplexOrganizationsLocationsEncryptionConfigsSetIamPolicyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleIamV1Policy, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dataplex_organizations_locations_encryption_configs_set_iam_policy_builder(
            &self.http_client,
            &args.resource,
        )
        .map_err(ProviderError::Api)?;

        let task = dataplex_organizations_locations_encryption_configs_set_iam_policy_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dataplex organizations locations encryption configs test iam permissions.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleIamV1TestIamPermissionsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn dataplex_organizations_locations_encryption_configs_test_iam_permissions(
        &self,
        args: &DataplexOrganizationsLocationsEncryptionConfigsTestIamPermissionsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleIamV1TestIamPermissionsResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dataplex_organizations_locations_encryption_configs_test_iam_permissions_builder(
            &self.http_client,
            &args.resource,
        )
        .map_err(ProviderError::Api)?;

        let task = dataplex_organizations_locations_encryption_configs_test_iam_permissions_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dataplex organizations locations operations cancel.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Empty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn dataplex_organizations_locations_operations_cancel(
        &self,
        args: &DataplexOrganizationsLocationsOperationsCancelArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Empty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dataplex_organizations_locations_operations_cancel_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = dataplex_organizations_locations_operations_cancel_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dataplex organizations locations operations delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Empty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn dataplex_organizations_locations_operations_delete(
        &self,
        args: &DataplexOrganizationsLocationsOperationsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Empty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dataplex_organizations_locations_operations_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = dataplex_organizations_locations_operations_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dataplex organizations locations operations get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn dataplex_organizations_locations_operations_get(
        &self,
        args: &DataplexOrganizationsLocationsOperationsGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dataplex_organizations_locations_operations_get_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = dataplex_organizations_locations_operations_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dataplex organizations locations operations list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningListOperationsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn dataplex_organizations_locations_operations_list(
        &self,
        args: &DataplexOrganizationsLocationsOperationsListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningListOperationsResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dataplex_organizations_locations_operations_list_builder(
            &self.http_client,
            &args.name,
            &args.filter,
            &args.pageSize,
            &args.pageToken,
            &args.returnPartialSuccess,
        )
        .map_err(ProviderError::Api)?;

        let task = dataplex_organizations_locations_operations_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dataplex projects locations get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudLocationLocation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn dataplex_projects_locations_get(
        &self,
        args: &DataplexProjectsLocationsGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudLocationLocation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dataplex_projects_locations_get_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = dataplex_projects_locations_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dataplex projects locations list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudLocationListLocationsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn dataplex_projects_locations_list(
        &self,
        args: &DataplexProjectsLocationsListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudLocationListLocationsResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dataplex_projects_locations_list_builder(
            &self.http_client,
            &args.name,
            &args.extraLocationTypes,
            &args.filter,
            &args.pageSize,
            &args.pageToken,
        )
        .map_err(ProviderError::Api)?;

        let task = dataplex_projects_locations_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dataplex projects locations lookup context.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudDataplexV1LookupContextResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn dataplex_projects_locations_lookup_context(
        &self,
        args: &DataplexProjectsLocationsLookupContextArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudDataplexV1LookupContextResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dataplex_projects_locations_lookup_context_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = dataplex_projects_locations_lookup_context_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dataplex projects locations lookup entry.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudDataplexV1Entry result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn dataplex_projects_locations_lookup_entry(
        &self,
        args: &DataplexProjectsLocationsLookupEntryArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudDataplexV1Entry, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dataplex_projects_locations_lookup_entry_builder(
            &self.http_client,
            &args.name,
            &args.aspectTypes,
            &args.entry,
            &args.paths,
            &args.view,
        )
        .map_err(ProviderError::Api)?;

        let task = dataplex_projects_locations_lookup_entry_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dataplex projects locations lookup entry links.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudDataplexV1LookupEntryLinksResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn dataplex_projects_locations_lookup_entry_links(
        &self,
        args: &DataplexProjectsLocationsLookupEntryLinksArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudDataplexV1LookupEntryLinksResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dataplex_projects_locations_lookup_entry_links_builder(
            &self.http_client,
            &args.name,
            &args.entry,
            &args.entryLinkTypes,
            &args.entryMode,
            &args.pageSize,
            &args.pageToken,
        )
        .map_err(ProviderError::Api)?;

        let task = dataplex_projects_locations_lookup_entry_links_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dataplex projects locations modify entry.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudDataplexV1Entry result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn dataplex_projects_locations_modify_entry(
        &self,
        args: &DataplexProjectsLocationsModifyEntryArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudDataplexV1Entry, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dataplex_projects_locations_modify_entry_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = dataplex_projects_locations_modify_entry_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dataplex projects locations search entries.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudDataplexV1SearchEntriesResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn dataplex_projects_locations_search_entries(
        &self,
        args: &DataplexProjectsLocationsSearchEntriesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudDataplexV1SearchEntriesResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dataplex_projects_locations_search_entries_builder(
            &self.http_client,
            &args.name,
            &args.orderBy,
            &args.pageSize,
            &args.pageToken,
            &args.query,
            &args.scope,
            &args.semanticSearch,
        )
        .map_err(ProviderError::Api)?;

        let task = dataplex_projects_locations_search_entries_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dataplex projects locations aspect types create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn dataplex_projects_locations_aspect_types_create(
        &self,
        args: &DataplexProjectsLocationsAspectTypesCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dataplex_projects_locations_aspect_types_create_builder(
            &self.http_client,
            &args.parent,
            &args.aspectTypeId,
            &args.validateOnly,
        )
        .map_err(ProviderError::Api)?;

        let task = dataplex_projects_locations_aspect_types_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dataplex projects locations aspect types delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn dataplex_projects_locations_aspect_types_delete(
        &self,
        args: &DataplexProjectsLocationsAspectTypesDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dataplex_projects_locations_aspect_types_delete_builder(
            &self.http_client,
            &args.name,
            &args.etag,
        )
        .map_err(ProviderError::Api)?;

        let task = dataplex_projects_locations_aspect_types_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dataplex projects locations aspect types get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudDataplexV1AspectType result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn dataplex_projects_locations_aspect_types_get(
        &self,
        args: &DataplexProjectsLocationsAspectTypesGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudDataplexV1AspectType, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dataplex_projects_locations_aspect_types_get_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = dataplex_projects_locations_aspect_types_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dataplex projects locations aspect types get iam policy.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleIamV1Policy result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn dataplex_projects_locations_aspect_types_get_iam_policy(
        &self,
        args: &DataplexProjectsLocationsAspectTypesGetIamPolicyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleIamV1Policy, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dataplex_projects_locations_aspect_types_get_iam_policy_builder(
            &self.http_client,
            &args.resource,
            &args.options.requestedPolicyVersion,
        )
        .map_err(ProviderError::Api)?;

        let task = dataplex_projects_locations_aspect_types_get_iam_policy_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dataplex projects locations aspect types list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudDataplexV1ListAspectTypesResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn dataplex_projects_locations_aspect_types_list(
        &self,
        args: &DataplexProjectsLocationsAspectTypesListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudDataplexV1ListAspectTypesResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dataplex_projects_locations_aspect_types_list_builder(
            &self.http_client,
            &args.parent,
            &args.filter,
            &args.orderBy,
            &args.pageSize,
            &args.pageToken,
        )
        .map_err(ProviderError::Api)?;

        let task = dataplex_projects_locations_aspect_types_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dataplex projects locations aspect types patch.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn dataplex_projects_locations_aspect_types_patch(
        &self,
        args: &DataplexProjectsLocationsAspectTypesPatchArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dataplex_projects_locations_aspect_types_patch_builder(
            &self.http_client,
            &args.name,
            &args.updateMask,
            &args.validateOnly,
        )
        .map_err(ProviderError::Api)?;

        let task = dataplex_projects_locations_aspect_types_patch_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dataplex projects locations aspect types set iam policy.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleIamV1Policy result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn dataplex_projects_locations_aspect_types_set_iam_policy(
        &self,
        args: &DataplexProjectsLocationsAspectTypesSetIamPolicyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleIamV1Policy, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dataplex_projects_locations_aspect_types_set_iam_policy_builder(
            &self.http_client,
            &args.resource,
        )
        .map_err(ProviderError::Api)?;

        let task = dataplex_projects_locations_aspect_types_set_iam_policy_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dataplex projects locations aspect types test iam permissions.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleIamV1TestIamPermissionsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn dataplex_projects_locations_aspect_types_test_iam_permissions(
        &self,
        args: &DataplexProjectsLocationsAspectTypesTestIamPermissionsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleIamV1TestIamPermissionsResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dataplex_projects_locations_aspect_types_test_iam_permissions_builder(
            &self.http_client,
            &args.resource,
        )
        .map_err(ProviderError::Api)?;

        let task = dataplex_projects_locations_aspect_types_test_iam_permissions_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dataplex projects locations change requests get iam policy.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleIamV1Policy result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn dataplex_projects_locations_change_requests_get_iam_policy(
        &self,
        args: &DataplexProjectsLocationsChangeRequestsGetIamPolicyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleIamV1Policy, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dataplex_projects_locations_change_requests_get_iam_policy_builder(
            &self.http_client,
            &args.resource,
            &args.options.requestedPolicyVersion,
        )
        .map_err(ProviderError::Api)?;

        let task = dataplex_projects_locations_change_requests_get_iam_policy_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dataplex projects locations change requests set iam policy.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleIamV1Policy result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn dataplex_projects_locations_change_requests_set_iam_policy(
        &self,
        args: &DataplexProjectsLocationsChangeRequestsSetIamPolicyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleIamV1Policy, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dataplex_projects_locations_change_requests_set_iam_policy_builder(
            &self.http_client,
            &args.resource,
        )
        .map_err(ProviderError::Api)?;

        let task = dataplex_projects_locations_change_requests_set_iam_policy_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dataplex projects locations change requests test iam permissions.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleIamV1TestIamPermissionsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn dataplex_projects_locations_change_requests_test_iam_permissions(
        &self,
        args: &DataplexProjectsLocationsChangeRequestsTestIamPermissionsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleIamV1TestIamPermissionsResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dataplex_projects_locations_change_requests_test_iam_permissions_builder(
            &self.http_client,
            &args.resource,
        )
        .map_err(ProviderError::Api)?;

        let task = dataplex_projects_locations_change_requests_test_iam_permissions_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dataplex projects locations data attribute bindings create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn dataplex_projects_locations_data_attribute_bindings_create(
        &self,
        args: &DataplexProjectsLocationsDataAttributeBindingsCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dataplex_projects_locations_data_attribute_bindings_create_builder(
            &self.http_client,
            &args.parent,
            &args.dataAttributeBindingId,
            &args.validateOnly,
        )
        .map_err(ProviderError::Api)?;

        let task = dataplex_projects_locations_data_attribute_bindings_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dataplex projects locations data attribute bindings delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn dataplex_projects_locations_data_attribute_bindings_delete(
        &self,
        args: &DataplexProjectsLocationsDataAttributeBindingsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dataplex_projects_locations_data_attribute_bindings_delete_builder(
            &self.http_client,
            &args.name,
            &args.etag,
        )
        .map_err(ProviderError::Api)?;

        let task = dataplex_projects_locations_data_attribute_bindings_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dataplex projects locations data attribute bindings get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudDataplexV1DataAttributeBinding result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn dataplex_projects_locations_data_attribute_bindings_get(
        &self,
        args: &DataplexProjectsLocationsDataAttributeBindingsGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudDataplexV1DataAttributeBinding, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dataplex_projects_locations_data_attribute_bindings_get_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = dataplex_projects_locations_data_attribute_bindings_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dataplex projects locations data attribute bindings get iam policy.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleIamV1Policy result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn dataplex_projects_locations_data_attribute_bindings_get_iam_policy(
        &self,
        args: &DataplexProjectsLocationsDataAttributeBindingsGetIamPolicyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleIamV1Policy, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dataplex_projects_locations_data_attribute_bindings_get_iam_policy_builder(
            &self.http_client,
            &args.resource,
            &args.options.requestedPolicyVersion,
        )
        .map_err(ProviderError::Api)?;

        let task = dataplex_projects_locations_data_attribute_bindings_get_iam_policy_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dataplex projects locations data attribute bindings list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudDataplexV1ListDataAttributeBindingsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn dataplex_projects_locations_data_attribute_bindings_list(
        &self,
        args: &DataplexProjectsLocationsDataAttributeBindingsListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudDataplexV1ListDataAttributeBindingsResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dataplex_projects_locations_data_attribute_bindings_list_builder(
            &self.http_client,
            &args.parent,
            &args.filter,
            &args.orderBy,
            &args.pageSize,
            &args.pageToken,
        )
        .map_err(ProviderError::Api)?;

        let task = dataplex_projects_locations_data_attribute_bindings_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dataplex projects locations data attribute bindings patch.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn dataplex_projects_locations_data_attribute_bindings_patch(
        &self,
        args: &DataplexProjectsLocationsDataAttributeBindingsPatchArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dataplex_projects_locations_data_attribute_bindings_patch_builder(
            &self.http_client,
            &args.name,
            &args.updateMask,
            &args.validateOnly,
        )
        .map_err(ProviderError::Api)?;

        let task = dataplex_projects_locations_data_attribute_bindings_patch_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dataplex projects locations data attribute bindings set iam policy.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleIamV1Policy result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn dataplex_projects_locations_data_attribute_bindings_set_iam_policy(
        &self,
        args: &DataplexProjectsLocationsDataAttributeBindingsSetIamPolicyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleIamV1Policy, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dataplex_projects_locations_data_attribute_bindings_set_iam_policy_builder(
            &self.http_client,
            &args.resource,
        )
        .map_err(ProviderError::Api)?;

        let task = dataplex_projects_locations_data_attribute_bindings_set_iam_policy_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dataplex projects locations data attribute bindings test iam permissions.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleIamV1TestIamPermissionsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn dataplex_projects_locations_data_attribute_bindings_test_iam_permissions(
        &self,
        args: &DataplexProjectsLocationsDataAttributeBindingsTestIamPermissionsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleIamV1TestIamPermissionsResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dataplex_projects_locations_data_attribute_bindings_test_iam_permissions_builder(
            &self.http_client,
            &args.resource,
        )
        .map_err(ProviderError::Api)?;

        let task = dataplex_projects_locations_data_attribute_bindings_test_iam_permissions_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dataplex projects locations data domains get iam policy.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleIamV1Policy result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn dataplex_projects_locations_data_domains_get_iam_policy(
        &self,
        args: &DataplexProjectsLocationsDataDomainsGetIamPolicyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleIamV1Policy, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dataplex_projects_locations_data_domains_get_iam_policy_builder(
            &self.http_client,
            &args.resource,
            &args.options.requestedPolicyVersion,
        )
        .map_err(ProviderError::Api)?;

        let task = dataplex_projects_locations_data_domains_get_iam_policy_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dataplex projects locations data domains set iam policy.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleIamV1Policy result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn dataplex_projects_locations_data_domains_set_iam_policy(
        &self,
        args: &DataplexProjectsLocationsDataDomainsSetIamPolicyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleIamV1Policy, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dataplex_projects_locations_data_domains_set_iam_policy_builder(
            &self.http_client,
            &args.resource,
        )
        .map_err(ProviderError::Api)?;

        let task = dataplex_projects_locations_data_domains_set_iam_policy_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dataplex projects locations data domains test iam permissions.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleIamV1TestIamPermissionsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn dataplex_projects_locations_data_domains_test_iam_permissions(
        &self,
        args: &DataplexProjectsLocationsDataDomainsTestIamPermissionsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleIamV1TestIamPermissionsResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dataplex_projects_locations_data_domains_test_iam_permissions_builder(
            &self.http_client,
            &args.resource,
        )
        .map_err(ProviderError::Api)?;

        let task = dataplex_projects_locations_data_domains_test_iam_permissions_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dataplex projects locations data products create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn dataplex_projects_locations_data_products_create(
        &self,
        args: &DataplexProjectsLocationsDataProductsCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dataplex_projects_locations_data_products_create_builder(
            &self.http_client,
            &args.parent,
            &args.dataProductId,
            &args.validateOnly,
        )
        .map_err(ProviderError::Api)?;

        let task = dataplex_projects_locations_data_products_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dataplex projects locations data products delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn dataplex_projects_locations_data_products_delete(
        &self,
        args: &DataplexProjectsLocationsDataProductsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dataplex_projects_locations_data_products_delete_builder(
            &self.http_client,
            &args.name,
            &args.etag,
            &args.validateOnly,
        )
        .map_err(ProviderError::Api)?;

        let task = dataplex_projects_locations_data_products_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dataplex projects locations data products get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudDataplexV1DataProduct result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn dataplex_projects_locations_data_products_get(
        &self,
        args: &DataplexProjectsLocationsDataProductsGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudDataplexV1DataProduct, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dataplex_projects_locations_data_products_get_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = dataplex_projects_locations_data_products_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dataplex projects locations data products get iam policy.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleIamV1Policy result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn dataplex_projects_locations_data_products_get_iam_policy(
        &self,
        args: &DataplexProjectsLocationsDataProductsGetIamPolicyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleIamV1Policy, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dataplex_projects_locations_data_products_get_iam_policy_builder(
            &self.http_client,
            &args.resource,
            &args.options.requestedPolicyVersion,
        )
        .map_err(ProviderError::Api)?;

        let task = dataplex_projects_locations_data_products_get_iam_policy_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dataplex projects locations data products list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudDataplexV1ListDataProductsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn dataplex_projects_locations_data_products_list(
        &self,
        args: &DataplexProjectsLocationsDataProductsListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudDataplexV1ListDataProductsResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dataplex_projects_locations_data_products_list_builder(
            &self.http_client,
            &args.parent,
            &args.filter,
            &args.orderBy,
            &args.pageSize,
            &args.pageToken,
        )
        .map_err(ProviderError::Api)?;

        let task = dataplex_projects_locations_data_products_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dataplex projects locations data products patch.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn dataplex_projects_locations_data_products_patch(
        &self,
        args: &DataplexProjectsLocationsDataProductsPatchArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dataplex_projects_locations_data_products_patch_builder(
            &self.http_client,
            &args.name,
            &args.updateMask,
            &args.validateOnly,
        )
        .map_err(ProviderError::Api)?;

        let task = dataplex_projects_locations_data_products_patch_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dataplex projects locations data products set iam policy.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleIamV1Policy result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn dataplex_projects_locations_data_products_set_iam_policy(
        &self,
        args: &DataplexProjectsLocationsDataProductsSetIamPolicyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleIamV1Policy, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dataplex_projects_locations_data_products_set_iam_policy_builder(
            &self.http_client,
            &args.resource,
        )
        .map_err(ProviderError::Api)?;

        let task = dataplex_projects_locations_data_products_set_iam_policy_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dataplex projects locations data products test iam permissions.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleIamV1TestIamPermissionsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn dataplex_projects_locations_data_products_test_iam_permissions(
        &self,
        args: &DataplexProjectsLocationsDataProductsTestIamPermissionsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleIamV1TestIamPermissionsResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dataplex_projects_locations_data_products_test_iam_permissions_builder(
            &self.http_client,
            &args.resource,
        )
        .map_err(ProviderError::Api)?;

        let task = dataplex_projects_locations_data_products_test_iam_permissions_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dataplex projects locations data products data assets create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn dataplex_projects_locations_data_products_data_assets_create(
        &self,
        args: &DataplexProjectsLocationsDataProductsDataAssetsCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dataplex_projects_locations_data_products_data_assets_create_builder(
            &self.http_client,
            &args.parent,
            &args.dataAssetId,
            &args.validateOnly,
        )
        .map_err(ProviderError::Api)?;

        let task = dataplex_projects_locations_data_products_data_assets_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dataplex projects locations data products data assets delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn dataplex_projects_locations_data_products_data_assets_delete(
        &self,
        args: &DataplexProjectsLocationsDataProductsDataAssetsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dataplex_projects_locations_data_products_data_assets_delete_builder(
            &self.http_client,
            &args.name,
            &args.etag,
            &args.validateOnly,
        )
        .map_err(ProviderError::Api)?;

        let task = dataplex_projects_locations_data_products_data_assets_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dataplex projects locations data products data assets get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudDataplexV1DataAsset result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn dataplex_projects_locations_data_products_data_assets_get(
        &self,
        args: &DataplexProjectsLocationsDataProductsDataAssetsGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudDataplexV1DataAsset, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dataplex_projects_locations_data_products_data_assets_get_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = dataplex_projects_locations_data_products_data_assets_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dataplex projects locations data products data assets list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudDataplexV1ListDataAssetsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn dataplex_projects_locations_data_products_data_assets_list(
        &self,
        args: &DataplexProjectsLocationsDataProductsDataAssetsListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudDataplexV1ListDataAssetsResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dataplex_projects_locations_data_products_data_assets_list_builder(
            &self.http_client,
            &args.parent,
            &args.filter,
            &args.orderBy,
            &args.pageSize,
            &args.pageToken,
        )
        .map_err(ProviderError::Api)?;

        let task = dataplex_projects_locations_data_products_data_assets_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dataplex projects locations data products data assets patch.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn dataplex_projects_locations_data_products_data_assets_patch(
        &self,
        args: &DataplexProjectsLocationsDataProductsDataAssetsPatchArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dataplex_projects_locations_data_products_data_assets_patch_builder(
            &self.http_client,
            &args.name,
            &args.updateMask,
            &args.validateOnly,
        )
        .map_err(ProviderError::Api)?;

        let task = dataplex_projects_locations_data_products_data_assets_patch_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dataplex projects locations data scans create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn dataplex_projects_locations_data_scans_create(
        &self,
        args: &DataplexProjectsLocationsDataScansCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dataplex_projects_locations_data_scans_create_builder(
            &self.http_client,
            &args.parent,
            &args.dataScanId,
            &args.validateOnly,
        )
        .map_err(ProviderError::Api)?;

        let task = dataplex_projects_locations_data_scans_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dataplex projects locations data scans delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn dataplex_projects_locations_data_scans_delete(
        &self,
        args: &DataplexProjectsLocationsDataScansDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dataplex_projects_locations_data_scans_delete_builder(
            &self.http_client,
            &args.name,
            &args.force,
        )
        .map_err(ProviderError::Api)?;

        let task = dataplex_projects_locations_data_scans_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dataplex projects locations data scans generate data quality rules.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudDataplexV1GenerateDataQualityRulesResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn dataplex_projects_locations_data_scans_generate_data_quality_rules(
        &self,
        args: &DataplexProjectsLocationsDataScansGenerateDataQualityRulesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudDataplexV1GenerateDataQualityRulesResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dataplex_projects_locations_data_scans_generate_data_quality_rules_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = dataplex_projects_locations_data_scans_generate_data_quality_rules_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dataplex projects locations data scans get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudDataplexV1DataScan result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn dataplex_projects_locations_data_scans_get(
        &self,
        args: &DataplexProjectsLocationsDataScansGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudDataplexV1DataScan, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dataplex_projects_locations_data_scans_get_builder(
            &self.http_client,
            &args.name,
            &args.view,
        )
        .map_err(ProviderError::Api)?;

        let task = dataplex_projects_locations_data_scans_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dataplex projects locations data scans get iam policy.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleIamV1Policy result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn dataplex_projects_locations_data_scans_get_iam_policy(
        &self,
        args: &DataplexProjectsLocationsDataScansGetIamPolicyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleIamV1Policy, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dataplex_projects_locations_data_scans_get_iam_policy_builder(
            &self.http_client,
            &args.resource,
            &args.options.requestedPolicyVersion,
        )
        .map_err(ProviderError::Api)?;

        let task = dataplex_projects_locations_data_scans_get_iam_policy_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dataplex projects locations data scans list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudDataplexV1ListDataScansResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn dataplex_projects_locations_data_scans_list(
        &self,
        args: &DataplexProjectsLocationsDataScansListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudDataplexV1ListDataScansResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dataplex_projects_locations_data_scans_list_builder(
            &self.http_client,
            &args.parent,
            &args.filter,
            &args.orderBy,
            &args.pageSize,
            &args.pageToken,
        )
        .map_err(ProviderError::Api)?;

        let task = dataplex_projects_locations_data_scans_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dataplex projects locations data scans patch.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn dataplex_projects_locations_data_scans_patch(
        &self,
        args: &DataplexProjectsLocationsDataScansPatchArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dataplex_projects_locations_data_scans_patch_builder(
            &self.http_client,
            &args.name,
            &args.updateMask,
            &args.validateOnly,
        )
        .map_err(ProviderError::Api)?;

        let task = dataplex_projects_locations_data_scans_patch_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dataplex projects locations data scans run.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudDataplexV1RunDataScanResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn dataplex_projects_locations_data_scans_run(
        &self,
        args: &DataplexProjectsLocationsDataScansRunArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudDataplexV1RunDataScanResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dataplex_projects_locations_data_scans_run_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = dataplex_projects_locations_data_scans_run_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dataplex projects locations data scans set iam policy.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleIamV1Policy result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn dataplex_projects_locations_data_scans_set_iam_policy(
        &self,
        args: &DataplexProjectsLocationsDataScansSetIamPolicyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleIamV1Policy, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dataplex_projects_locations_data_scans_set_iam_policy_builder(
            &self.http_client,
            &args.resource,
        )
        .map_err(ProviderError::Api)?;

        let task = dataplex_projects_locations_data_scans_set_iam_policy_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dataplex projects locations data scans test iam permissions.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleIamV1TestIamPermissionsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn dataplex_projects_locations_data_scans_test_iam_permissions(
        &self,
        args: &DataplexProjectsLocationsDataScansTestIamPermissionsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleIamV1TestIamPermissionsResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dataplex_projects_locations_data_scans_test_iam_permissions_builder(
            &self.http_client,
            &args.resource,
        )
        .map_err(ProviderError::Api)?;

        let task = dataplex_projects_locations_data_scans_test_iam_permissions_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dataplex projects locations data scans jobs generate data quality rules.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudDataplexV1GenerateDataQualityRulesResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn dataplex_projects_locations_data_scans_jobs_generate_data_quality_rules(
        &self,
        args: &DataplexProjectsLocationsDataScansJobsGenerateDataQualityRulesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudDataplexV1GenerateDataQualityRulesResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dataplex_projects_locations_data_scans_jobs_generate_data_quality_rules_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = dataplex_projects_locations_data_scans_jobs_generate_data_quality_rules_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dataplex projects locations data scans jobs get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudDataplexV1DataScanJob result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn dataplex_projects_locations_data_scans_jobs_get(
        &self,
        args: &DataplexProjectsLocationsDataScansJobsGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudDataplexV1DataScanJob, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dataplex_projects_locations_data_scans_jobs_get_builder(
            &self.http_client,
            &args.name,
            &args.view,
        )
        .map_err(ProviderError::Api)?;

        let task = dataplex_projects_locations_data_scans_jobs_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dataplex projects locations data scans jobs list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudDataplexV1ListDataScanJobsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn dataplex_projects_locations_data_scans_jobs_list(
        &self,
        args: &DataplexProjectsLocationsDataScansJobsListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudDataplexV1ListDataScanJobsResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dataplex_projects_locations_data_scans_jobs_list_builder(
            &self.http_client,
            &args.parent,
            &args.filter,
            &args.pageSize,
            &args.pageToken,
        )
        .map_err(ProviderError::Api)?;

        let task = dataplex_projects_locations_data_scans_jobs_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dataplex projects locations data taxonomies create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn dataplex_projects_locations_data_taxonomies_create(
        &self,
        args: &DataplexProjectsLocationsDataTaxonomiesCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dataplex_projects_locations_data_taxonomies_create_builder(
            &self.http_client,
            &args.parent,
            &args.dataTaxonomyId,
            &args.validateOnly,
        )
        .map_err(ProviderError::Api)?;

        let task = dataplex_projects_locations_data_taxonomies_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dataplex projects locations data taxonomies delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn dataplex_projects_locations_data_taxonomies_delete(
        &self,
        args: &DataplexProjectsLocationsDataTaxonomiesDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dataplex_projects_locations_data_taxonomies_delete_builder(
            &self.http_client,
            &args.name,
            &args.etag,
        )
        .map_err(ProviderError::Api)?;

        let task = dataplex_projects_locations_data_taxonomies_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dataplex projects locations data taxonomies get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudDataplexV1DataTaxonomy result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn dataplex_projects_locations_data_taxonomies_get(
        &self,
        args: &DataplexProjectsLocationsDataTaxonomiesGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudDataplexV1DataTaxonomy, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dataplex_projects_locations_data_taxonomies_get_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = dataplex_projects_locations_data_taxonomies_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dataplex projects locations data taxonomies get iam policy.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleIamV1Policy result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn dataplex_projects_locations_data_taxonomies_get_iam_policy(
        &self,
        args: &DataplexProjectsLocationsDataTaxonomiesGetIamPolicyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleIamV1Policy, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dataplex_projects_locations_data_taxonomies_get_iam_policy_builder(
            &self.http_client,
            &args.resource,
            &args.options.requestedPolicyVersion,
        )
        .map_err(ProviderError::Api)?;

        let task = dataplex_projects_locations_data_taxonomies_get_iam_policy_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dataplex projects locations data taxonomies list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudDataplexV1ListDataTaxonomiesResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn dataplex_projects_locations_data_taxonomies_list(
        &self,
        args: &DataplexProjectsLocationsDataTaxonomiesListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudDataplexV1ListDataTaxonomiesResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dataplex_projects_locations_data_taxonomies_list_builder(
            &self.http_client,
            &args.parent,
            &args.filter,
            &args.orderBy,
            &args.pageSize,
            &args.pageToken,
        )
        .map_err(ProviderError::Api)?;

        let task = dataplex_projects_locations_data_taxonomies_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dataplex projects locations data taxonomies patch.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn dataplex_projects_locations_data_taxonomies_patch(
        &self,
        args: &DataplexProjectsLocationsDataTaxonomiesPatchArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dataplex_projects_locations_data_taxonomies_patch_builder(
            &self.http_client,
            &args.name,
            &args.updateMask,
            &args.validateOnly,
        )
        .map_err(ProviderError::Api)?;

        let task = dataplex_projects_locations_data_taxonomies_patch_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dataplex projects locations data taxonomies set iam policy.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleIamV1Policy result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn dataplex_projects_locations_data_taxonomies_set_iam_policy(
        &self,
        args: &DataplexProjectsLocationsDataTaxonomiesSetIamPolicyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleIamV1Policy, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dataplex_projects_locations_data_taxonomies_set_iam_policy_builder(
            &self.http_client,
            &args.resource,
        )
        .map_err(ProviderError::Api)?;

        let task = dataplex_projects_locations_data_taxonomies_set_iam_policy_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dataplex projects locations data taxonomies test iam permissions.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleIamV1TestIamPermissionsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn dataplex_projects_locations_data_taxonomies_test_iam_permissions(
        &self,
        args: &DataplexProjectsLocationsDataTaxonomiesTestIamPermissionsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleIamV1TestIamPermissionsResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dataplex_projects_locations_data_taxonomies_test_iam_permissions_builder(
            &self.http_client,
            &args.resource,
        )
        .map_err(ProviderError::Api)?;

        let task = dataplex_projects_locations_data_taxonomies_test_iam_permissions_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dataplex projects locations data taxonomies attributes create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn dataplex_projects_locations_data_taxonomies_attributes_create(
        &self,
        args: &DataplexProjectsLocationsDataTaxonomiesAttributesCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dataplex_projects_locations_data_taxonomies_attributes_create_builder(
            &self.http_client,
            &args.parent,
            &args.dataAttributeId,
            &args.validateOnly,
        )
        .map_err(ProviderError::Api)?;

        let task = dataplex_projects_locations_data_taxonomies_attributes_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dataplex projects locations data taxonomies attributes delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn dataplex_projects_locations_data_taxonomies_attributes_delete(
        &self,
        args: &DataplexProjectsLocationsDataTaxonomiesAttributesDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dataplex_projects_locations_data_taxonomies_attributes_delete_builder(
            &self.http_client,
            &args.name,
            &args.etag,
        )
        .map_err(ProviderError::Api)?;

        let task = dataplex_projects_locations_data_taxonomies_attributes_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dataplex projects locations data taxonomies attributes get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudDataplexV1DataAttribute result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn dataplex_projects_locations_data_taxonomies_attributes_get(
        &self,
        args: &DataplexProjectsLocationsDataTaxonomiesAttributesGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudDataplexV1DataAttribute, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dataplex_projects_locations_data_taxonomies_attributes_get_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = dataplex_projects_locations_data_taxonomies_attributes_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dataplex projects locations data taxonomies attributes get iam policy.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleIamV1Policy result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn dataplex_projects_locations_data_taxonomies_attributes_get_iam_policy(
        &self,
        args: &DataplexProjectsLocationsDataTaxonomiesAttributesGetIamPolicyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleIamV1Policy, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dataplex_projects_locations_data_taxonomies_attributes_get_iam_policy_builder(
            &self.http_client,
            &args.resource,
            &args.options.requestedPolicyVersion,
        )
        .map_err(ProviderError::Api)?;

        let task = dataplex_projects_locations_data_taxonomies_attributes_get_iam_policy_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dataplex projects locations data taxonomies attributes list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudDataplexV1ListDataAttributesResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn dataplex_projects_locations_data_taxonomies_attributes_list(
        &self,
        args: &DataplexProjectsLocationsDataTaxonomiesAttributesListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudDataplexV1ListDataAttributesResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dataplex_projects_locations_data_taxonomies_attributes_list_builder(
            &self.http_client,
            &args.parent,
            &args.filter,
            &args.orderBy,
            &args.pageSize,
            &args.pageToken,
        )
        .map_err(ProviderError::Api)?;

        let task = dataplex_projects_locations_data_taxonomies_attributes_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dataplex projects locations data taxonomies attributes patch.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn dataplex_projects_locations_data_taxonomies_attributes_patch(
        &self,
        args: &DataplexProjectsLocationsDataTaxonomiesAttributesPatchArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dataplex_projects_locations_data_taxonomies_attributes_patch_builder(
            &self.http_client,
            &args.name,
            &args.updateMask,
            &args.validateOnly,
        )
        .map_err(ProviderError::Api)?;

        let task = dataplex_projects_locations_data_taxonomies_attributes_patch_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dataplex projects locations data taxonomies attributes set iam policy.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleIamV1Policy result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn dataplex_projects_locations_data_taxonomies_attributes_set_iam_policy(
        &self,
        args: &DataplexProjectsLocationsDataTaxonomiesAttributesSetIamPolicyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleIamV1Policy, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dataplex_projects_locations_data_taxonomies_attributes_set_iam_policy_builder(
            &self.http_client,
            &args.resource,
        )
        .map_err(ProviderError::Api)?;

        let task = dataplex_projects_locations_data_taxonomies_attributes_set_iam_policy_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dataplex projects locations data taxonomies attributes test iam permissions.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleIamV1TestIamPermissionsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn dataplex_projects_locations_data_taxonomies_attributes_test_iam_permissions(
        &self,
        args: &DataplexProjectsLocationsDataTaxonomiesAttributesTestIamPermissionsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleIamV1TestIamPermissionsResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dataplex_projects_locations_data_taxonomies_attributes_test_iam_permissions_builder(
            &self.http_client,
            &args.resource,
        )
        .map_err(ProviderError::Api)?;

        let task = dataplex_projects_locations_data_taxonomies_attributes_test_iam_permissions_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dataplex projects locations entry groups create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn dataplex_projects_locations_entry_groups_create(
        &self,
        args: &DataplexProjectsLocationsEntryGroupsCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dataplex_projects_locations_entry_groups_create_builder(
            &self.http_client,
            &args.parent,
            &args.entryGroupId,
            &args.validateOnly,
        )
        .map_err(ProviderError::Api)?;

        let task = dataplex_projects_locations_entry_groups_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dataplex projects locations entry groups delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn dataplex_projects_locations_entry_groups_delete(
        &self,
        args: &DataplexProjectsLocationsEntryGroupsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dataplex_projects_locations_entry_groups_delete_builder(
            &self.http_client,
            &args.name,
            &args.etag,
        )
        .map_err(ProviderError::Api)?;

        let task = dataplex_projects_locations_entry_groups_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dataplex projects locations entry groups get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudDataplexV1EntryGroup result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn dataplex_projects_locations_entry_groups_get(
        &self,
        args: &DataplexProjectsLocationsEntryGroupsGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudDataplexV1EntryGroup, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dataplex_projects_locations_entry_groups_get_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = dataplex_projects_locations_entry_groups_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dataplex projects locations entry groups get iam policy.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleIamV1Policy result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn dataplex_projects_locations_entry_groups_get_iam_policy(
        &self,
        args: &DataplexProjectsLocationsEntryGroupsGetIamPolicyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleIamV1Policy, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dataplex_projects_locations_entry_groups_get_iam_policy_builder(
            &self.http_client,
            &args.resource,
            &args.options.requestedPolicyVersion,
        )
        .map_err(ProviderError::Api)?;

        let task = dataplex_projects_locations_entry_groups_get_iam_policy_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dataplex projects locations entry groups list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudDataplexV1ListEntryGroupsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn dataplex_projects_locations_entry_groups_list(
        &self,
        args: &DataplexProjectsLocationsEntryGroupsListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudDataplexV1ListEntryGroupsResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dataplex_projects_locations_entry_groups_list_builder(
            &self.http_client,
            &args.parent,
            &args.filter,
            &args.orderBy,
            &args.pageSize,
            &args.pageToken,
        )
        .map_err(ProviderError::Api)?;

        let task = dataplex_projects_locations_entry_groups_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dataplex projects locations entry groups patch.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn dataplex_projects_locations_entry_groups_patch(
        &self,
        args: &DataplexProjectsLocationsEntryGroupsPatchArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dataplex_projects_locations_entry_groups_patch_builder(
            &self.http_client,
            &args.name,
            &args.updateMask,
            &args.validateOnly,
        )
        .map_err(ProviderError::Api)?;

        let task = dataplex_projects_locations_entry_groups_patch_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dataplex projects locations entry groups set iam policy.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleIamV1Policy result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn dataplex_projects_locations_entry_groups_set_iam_policy(
        &self,
        args: &DataplexProjectsLocationsEntryGroupsSetIamPolicyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleIamV1Policy, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dataplex_projects_locations_entry_groups_set_iam_policy_builder(
            &self.http_client,
            &args.resource,
        )
        .map_err(ProviderError::Api)?;

        let task = dataplex_projects_locations_entry_groups_set_iam_policy_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dataplex projects locations entry groups test iam permissions.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleIamV1TestIamPermissionsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn dataplex_projects_locations_entry_groups_test_iam_permissions(
        &self,
        args: &DataplexProjectsLocationsEntryGroupsTestIamPermissionsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleIamV1TestIamPermissionsResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dataplex_projects_locations_entry_groups_test_iam_permissions_builder(
            &self.http_client,
            &args.resource,
        )
        .map_err(ProviderError::Api)?;

        let task = dataplex_projects_locations_entry_groups_test_iam_permissions_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dataplex projects locations entry groups entries create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudDataplexV1Entry result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn dataplex_projects_locations_entry_groups_entries_create(
        &self,
        args: &DataplexProjectsLocationsEntryGroupsEntriesCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudDataplexV1Entry, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dataplex_projects_locations_entry_groups_entries_create_builder(
            &self.http_client,
            &args.parent,
            &args.entryId,
        )
        .map_err(ProviderError::Api)?;

        let task = dataplex_projects_locations_entry_groups_entries_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dataplex projects locations entry groups entries delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudDataplexV1Entry result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn dataplex_projects_locations_entry_groups_entries_delete(
        &self,
        args: &DataplexProjectsLocationsEntryGroupsEntriesDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudDataplexV1Entry, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dataplex_projects_locations_entry_groups_entries_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = dataplex_projects_locations_entry_groups_entries_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dataplex projects locations entry groups entries get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudDataplexV1Entry result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn dataplex_projects_locations_entry_groups_entries_get(
        &self,
        args: &DataplexProjectsLocationsEntryGroupsEntriesGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudDataplexV1Entry, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dataplex_projects_locations_entry_groups_entries_get_builder(
            &self.http_client,
            &args.name,
            &args.aspectTypes,
            &args.paths,
            &args.view,
        )
        .map_err(ProviderError::Api)?;

        let task = dataplex_projects_locations_entry_groups_entries_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dataplex projects locations entry groups entries list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudDataplexV1ListEntriesResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn dataplex_projects_locations_entry_groups_entries_list(
        &self,
        args: &DataplexProjectsLocationsEntryGroupsEntriesListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudDataplexV1ListEntriesResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dataplex_projects_locations_entry_groups_entries_list_builder(
            &self.http_client,
            &args.parent,
            &args.filter,
            &args.pageSize,
            &args.pageToken,
        )
        .map_err(ProviderError::Api)?;

        let task = dataplex_projects_locations_entry_groups_entries_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dataplex projects locations entry groups entries patch.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudDataplexV1Entry result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn dataplex_projects_locations_entry_groups_entries_patch(
        &self,
        args: &DataplexProjectsLocationsEntryGroupsEntriesPatchArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudDataplexV1Entry, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dataplex_projects_locations_entry_groups_entries_patch_builder(
            &self.http_client,
            &args.name,
            &args.allowMissing,
            &args.aspectKeys,
            &args.deleteMissingAspects,
            &args.updateMask,
        )
        .map_err(ProviderError::Api)?;

        let task = dataplex_projects_locations_entry_groups_entries_patch_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dataplex projects locations entry groups entry links create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudDataplexV1EntryLink result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn dataplex_projects_locations_entry_groups_entry_links_create(
        &self,
        args: &DataplexProjectsLocationsEntryGroupsEntryLinksCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudDataplexV1EntryLink, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dataplex_projects_locations_entry_groups_entry_links_create_builder(
            &self.http_client,
            &args.parent,
            &args.entryLinkId,
        )
        .map_err(ProviderError::Api)?;

        let task = dataplex_projects_locations_entry_groups_entry_links_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dataplex projects locations entry groups entry links delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudDataplexV1EntryLink result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn dataplex_projects_locations_entry_groups_entry_links_delete(
        &self,
        args: &DataplexProjectsLocationsEntryGroupsEntryLinksDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudDataplexV1EntryLink, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dataplex_projects_locations_entry_groups_entry_links_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = dataplex_projects_locations_entry_groups_entry_links_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dataplex projects locations entry groups entry links get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudDataplexV1EntryLink result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn dataplex_projects_locations_entry_groups_entry_links_get(
        &self,
        args: &DataplexProjectsLocationsEntryGroupsEntryLinksGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudDataplexV1EntryLink, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dataplex_projects_locations_entry_groups_entry_links_get_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = dataplex_projects_locations_entry_groups_entry_links_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dataplex projects locations entry groups entry links patch.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudDataplexV1EntryLink result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn dataplex_projects_locations_entry_groups_entry_links_patch(
        &self,
        args: &DataplexProjectsLocationsEntryGroupsEntryLinksPatchArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudDataplexV1EntryLink, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dataplex_projects_locations_entry_groups_entry_links_patch_builder(
            &self.http_client,
            &args.name,
            &args.allowMissing,
            &args.aspectKeys,
        )
        .map_err(ProviderError::Api)?;

        let task = dataplex_projects_locations_entry_groups_entry_links_patch_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dataplex projects locations entry link types get iam policy.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleIamV1Policy result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn dataplex_projects_locations_entry_link_types_get_iam_policy(
        &self,
        args: &DataplexProjectsLocationsEntryLinkTypesGetIamPolicyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleIamV1Policy, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dataplex_projects_locations_entry_link_types_get_iam_policy_builder(
            &self.http_client,
            &args.resource,
            &args.options.requestedPolicyVersion,
        )
        .map_err(ProviderError::Api)?;

        let task = dataplex_projects_locations_entry_link_types_get_iam_policy_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dataplex projects locations entry link types set iam policy.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleIamV1Policy result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn dataplex_projects_locations_entry_link_types_set_iam_policy(
        &self,
        args: &DataplexProjectsLocationsEntryLinkTypesSetIamPolicyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleIamV1Policy, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dataplex_projects_locations_entry_link_types_set_iam_policy_builder(
            &self.http_client,
            &args.resource,
        )
        .map_err(ProviderError::Api)?;

        let task = dataplex_projects_locations_entry_link_types_set_iam_policy_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dataplex projects locations entry link types test iam permissions.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleIamV1TestIamPermissionsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn dataplex_projects_locations_entry_link_types_test_iam_permissions(
        &self,
        args: &DataplexProjectsLocationsEntryLinkTypesTestIamPermissionsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleIamV1TestIamPermissionsResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dataplex_projects_locations_entry_link_types_test_iam_permissions_builder(
            &self.http_client,
            &args.resource,
        )
        .map_err(ProviderError::Api)?;

        let task = dataplex_projects_locations_entry_link_types_test_iam_permissions_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dataplex projects locations entry types create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn dataplex_projects_locations_entry_types_create(
        &self,
        args: &DataplexProjectsLocationsEntryTypesCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dataplex_projects_locations_entry_types_create_builder(
            &self.http_client,
            &args.parent,
            &args.entryTypeId,
            &args.validateOnly,
        )
        .map_err(ProviderError::Api)?;

        let task = dataplex_projects_locations_entry_types_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dataplex projects locations entry types delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn dataplex_projects_locations_entry_types_delete(
        &self,
        args: &DataplexProjectsLocationsEntryTypesDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dataplex_projects_locations_entry_types_delete_builder(
            &self.http_client,
            &args.name,
            &args.etag,
        )
        .map_err(ProviderError::Api)?;

        let task = dataplex_projects_locations_entry_types_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dataplex projects locations entry types get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudDataplexV1EntryType result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn dataplex_projects_locations_entry_types_get(
        &self,
        args: &DataplexProjectsLocationsEntryTypesGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudDataplexV1EntryType, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dataplex_projects_locations_entry_types_get_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = dataplex_projects_locations_entry_types_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dataplex projects locations entry types get iam policy.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleIamV1Policy result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn dataplex_projects_locations_entry_types_get_iam_policy(
        &self,
        args: &DataplexProjectsLocationsEntryTypesGetIamPolicyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleIamV1Policy, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dataplex_projects_locations_entry_types_get_iam_policy_builder(
            &self.http_client,
            &args.resource,
            &args.options.requestedPolicyVersion,
        )
        .map_err(ProviderError::Api)?;

        let task = dataplex_projects_locations_entry_types_get_iam_policy_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dataplex projects locations entry types list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudDataplexV1ListEntryTypesResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn dataplex_projects_locations_entry_types_list(
        &self,
        args: &DataplexProjectsLocationsEntryTypesListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudDataplexV1ListEntryTypesResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dataplex_projects_locations_entry_types_list_builder(
            &self.http_client,
            &args.parent,
            &args.filter,
            &args.orderBy,
            &args.pageSize,
            &args.pageToken,
        )
        .map_err(ProviderError::Api)?;

        let task = dataplex_projects_locations_entry_types_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dataplex projects locations entry types patch.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn dataplex_projects_locations_entry_types_patch(
        &self,
        args: &DataplexProjectsLocationsEntryTypesPatchArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dataplex_projects_locations_entry_types_patch_builder(
            &self.http_client,
            &args.name,
            &args.updateMask,
            &args.validateOnly,
        )
        .map_err(ProviderError::Api)?;

        let task = dataplex_projects_locations_entry_types_patch_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dataplex projects locations entry types set iam policy.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleIamV1Policy result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn dataplex_projects_locations_entry_types_set_iam_policy(
        &self,
        args: &DataplexProjectsLocationsEntryTypesSetIamPolicyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleIamV1Policy, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dataplex_projects_locations_entry_types_set_iam_policy_builder(
            &self.http_client,
            &args.resource,
        )
        .map_err(ProviderError::Api)?;

        let task = dataplex_projects_locations_entry_types_set_iam_policy_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dataplex projects locations entry types test iam permissions.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleIamV1TestIamPermissionsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn dataplex_projects_locations_entry_types_test_iam_permissions(
        &self,
        args: &DataplexProjectsLocationsEntryTypesTestIamPermissionsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleIamV1TestIamPermissionsResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dataplex_projects_locations_entry_types_test_iam_permissions_builder(
            &self.http_client,
            &args.resource,
        )
        .map_err(ProviderError::Api)?;

        let task = dataplex_projects_locations_entry_types_test_iam_permissions_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dataplex projects locations glossaries create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn dataplex_projects_locations_glossaries_create(
        &self,
        args: &DataplexProjectsLocationsGlossariesCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dataplex_projects_locations_glossaries_create_builder(
            &self.http_client,
            &args.parent,
            &args.glossaryId,
            &args.validateOnly,
        )
        .map_err(ProviderError::Api)?;

        let task = dataplex_projects_locations_glossaries_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dataplex projects locations glossaries delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn dataplex_projects_locations_glossaries_delete(
        &self,
        args: &DataplexProjectsLocationsGlossariesDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dataplex_projects_locations_glossaries_delete_builder(
            &self.http_client,
            &args.name,
            &args.etag,
        )
        .map_err(ProviderError::Api)?;

        let task = dataplex_projects_locations_glossaries_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dataplex projects locations glossaries get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudDataplexV1Glossary result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn dataplex_projects_locations_glossaries_get(
        &self,
        args: &DataplexProjectsLocationsGlossariesGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudDataplexV1Glossary, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dataplex_projects_locations_glossaries_get_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = dataplex_projects_locations_glossaries_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dataplex projects locations glossaries get iam policy.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleIamV1Policy result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn dataplex_projects_locations_glossaries_get_iam_policy(
        &self,
        args: &DataplexProjectsLocationsGlossariesGetIamPolicyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleIamV1Policy, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dataplex_projects_locations_glossaries_get_iam_policy_builder(
            &self.http_client,
            &args.resource,
            &args.options.requestedPolicyVersion,
        )
        .map_err(ProviderError::Api)?;

        let task = dataplex_projects_locations_glossaries_get_iam_policy_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dataplex projects locations glossaries list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudDataplexV1ListGlossariesResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn dataplex_projects_locations_glossaries_list(
        &self,
        args: &DataplexProjectsLocationsGlossariesListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudDataplexV1ListGlossariesResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dataplex_projects_locations_glossaries_list_builder(
            &self.http_client,
            &args.parent,
            &args.filter,
            &args.orderBy,
            &args.pageSize,
            &args.pageToken,
        )
        .map_err(ProviderError::Api)?;

        let task = dataplex_projects_locations_glossaries_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dataplex projects locations glossaries patch.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn dataplex_projects_locations_glossaries_patch(
        &self,
        args: &DataplexProjectsLocationsGlossariesPatchArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dataplex_projects_locations_glossaries_patch_builder(
            &self.http_client,
            &args.name,
            &args.updateMask,
            &args.validateOnly,
        )
        .map_err(ProviderError::Api)?;

        let task = dataplex_projects_locations_glossaries_patch_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dataplex projects locations glossaries set iam policy.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleIamV1Policy result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn dataplex_projects_locations_glossaries_set_iam_policy(
        &self,
        args: &DataplexProjectsLocationsGlossariesSetIamPolicyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleIamV1Policy, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dataplex_projects_locations_glossaries_set_iam_policy_builder(
            &self.http_client,
            &args.resource,
        )
        .map_err(ProviderError::Api)?;

        let task = dataplex_projects_locations_glossaries_set_iam_policy_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dataplex projects locations glossaries test iam permissions.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleIamV1TestIamPermissionsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn dataplex_projects_locations_glossaries_test_iam_permissions(
        &self,
        args: &DataplexProjectsLocationsGlossariesTestIamPermissionsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleIamV1TestIamPermissionsResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dataplex_projects_locations_glossaries_test_iam_permissions_builder(
            &self.http_client,
            &args.resource,
        )
        .map_err(ProviderError::Api)?;

        let task = dataplex_projects_locations_glossaries_test_iam_permissions_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dataplex projects locations glossaries categories create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudDataplexV1GlossaryCategory result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn dataplex_projects_locations_glossaries_categories_create(
        &self,
        args: &DataplexProjectsLocationsGlossariesCategoriesCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudDataplexV1GlossaryCategory, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dataplex_projects_locations_glossaries_categories_create_builder(
            &self.http_client,
            &args.parent,
            &args.categoryId,
        )
        .map_err(ProviderError::Api)?;

        let task = dataplex_projects_locations_glossaries_categories_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dataplex projects locations glossaries categories delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Empty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn dataplex_projects_locations_glossaries_categories_delete(
        &self,
        args: &DataplexProjectsLocationsGlossariesCategoriesDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Empty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dataplex_projects_locations_glossaries_categories_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = dataplex_projects_locations_glossaries_categories_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dataplex projects locations glossaries categories get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudDataplexV1GlossaryCategory result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn dataplex_projects_locations_glossaries_categories_get(
        &self,
        args: &DataplexProjectsLocationsGlossariesCategoriesGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudDataplexV1GlossaryCategory, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dataplex_projects_locations_glossaries_categories_get_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = dataplex_projects_locations_glossaries_categories_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dataplex projects locations glossaries categories get iam policy.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleIamV1Policy result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn dataplex_projects_locations_glossaries_categories_get_iam_policy(
        &self,
        args: &DataplexProjectsLocationsGlossariesCategoriesGetIamPolicyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleIamV1Policy, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dataplex_projects_locations_glossaries_categories_get_iam_policy_builder(
            &self.http_client,
            &args.resource,
            &args.options.requestedPolicyVersion,
        )
        .map_err(ProviderError::Api)?;

        let task = dataplex_projects_locations_glossaries_categories_get_iam_policy_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dataplex projects locations glossaries categories list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudDataplexV1ListGlossaryCategoriesResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn dataplex_projects_locations_glossaries_categories_list(
        &self,
        args: &DataplexProjectsLocationsGlossariesCategoriesListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudDataplexV1ListGlossaryCategoriesResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dataplex_projects_locations_glossaries_categories_list_builder(
            &self.http_client,
            &args.parent,
            &args.filter,
            &args.orderBy,
            &args.pageSize,
            &args.pageToken,
        )
        .map_err(ProviderError::Api)?;

        let task = dataplex_projects_locations_glossaries_categories_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dataplex projects locations glossaries categories patch.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudDataplexV1GlossaryCategory result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn dataplex_projects_locations_glossaries_categories_patch(
        &self,
        args: &DataplexProjectsLocationsGlossariesCategoriesPatchArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudDataplexV1GlossaryCategory, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dataplex_projects_locations_glossaries_categories_patch_builder(
            &self.http_client,
            &args.name,
            &args.updateMask,
        )
        .map_err(ProviderError::Api)?;

        let task = dataplex_projects_locations_glossaries_categories_patch_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dataplex projects locations glossaries categories set iam policy.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleIamV1Policy result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn dataplex_projects_locations_glossaries_categories_set_iam_policy(
        &self,
        args: &DataplexProjectsLocationsGlossariesCategoriesSetIamPolicyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleIamV1Policy, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dataplex_projects_locations_glossaries_categories_set_iam_policy_builder(
            &self.http_client,
            &args.resource,
        )
        .map_err(ProviderError::Api)?;

        let task = dataplex_projects_locations_glossaries_categories_set_iam_policy_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dataplex projects locations glossaries categories test iam permissions.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleIamV1TestIamPermissionsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn dataplex_projects_locations_glossaries_categories_test_iam_permissions(
        &self,
        args: &DataplexProjectsLocationsGlossariesCategoriesTestIamPermissionsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleIamV1TestIamPermissionsResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dataplex_projects_locations_glossaries_categories_test_iam_permissions_builder(
            &self.http_client,
            &args.resource,
        )
        .map_err(ProviderError::Api)?;

        let task = dataplex_projects_locations_glossaries_categories_test_iam_permissions_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dataplex projects locations glossaries terms create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudDataplexV1GlossaryTerm result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn dataplex_projects_locations_glossaries_terms_create(
        &self,
        args: &DataplexProjectsLocationsGlossariesTermsCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudDataplexV1GlossaryTerm, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dataplex_projects_locations_glossaries_terms_create_builder(
            &self.http_client,
            &args.parent,
            &args.termId,
        )
        .map_err(ProviderError::Api)?;

        let task = dataplex_projects_locations_glossaries_terms_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dataplex projects locations glossaries terms delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Empty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn dataplex_projects_locations_glossaries_terms_delete(
        &self,
        args: &DataplexProjectsLocationsGlossariesTermsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Empty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dataplex_projects_locations_glossaries_terms_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = dataplex_projects_locations_glossaries_terms_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dataplex projects locations glossaries terms get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudDataplexV1GlossaryTerm result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn dataplex_projects_locations_glossaries_terms_get(
        &self,
        args: &DataplexProjectsLocationsGlossariesTermsGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudDataplexV1GlossaryTerm, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dataplex_projects_locations_glossaries_terms_get_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = dataplex_projects_locations_glossaries_terms_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dataplex projects locations glossaries terms get iam policy.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleIamV1Policy result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn dataplex_projects_locations_glossaries_terms_get_iam_policy(
        &self,
        args: &DataplexProjectsLocationsGlossariesTermsGetIamPolicyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleIamV1Policy, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dataplex_projects_locations_glossaries_terms_get_iam_policy_builder(
            &self.http_client,
            &args.resource,
            &args.options.requestedPolicyVersion,
        )
        .map_err(ProviderError::Api)?;

        let task = dataplex_projects_locations_glossaries_terms_get_iam_policy_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dataplex projects locations glossaries terms list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudDataplexV1ListGlossaryTermsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn dataplex_projects_locations_glossaries_terms_list(
        &self,
        args: &DataplexProjectsLocationsGlossariesTermsListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudDataplexV1ListGlossaryTermsResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dataplex_projects_locations_glossaries_terms_list_builder(
            &self.http_client,
            &args.parent,
            &args.filter,
            &args.orderBy,
            &args.pageSize,
            &args.pageToken,
        )
        .map_err(ProviderError::Api)?;

        let task = dataplex_projects_locations_glossaries_terms_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dataplex projects locations glossaries terms patch.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudDataplexV1GlossaryTerm result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn dataplex_projects_locations_glossaries_terms_patch(
        &self,
        args: &DataplexProjectsLocationsGlossariesTermsPatchArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudDataplexV1GlossaryTerm, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dataplex_projects_locations_glossaries_terms_patch_builder(
            &self.http_client,
            &args.name,
            &args.updateMask,
        )
        .map_err(ProviderError::Api)?;

        let task = dataplex_projects_locations_glossaries_terms_patch_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dataplex projects locations glossaries terms set iam policy.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleIamV1Policy result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn dataplex_projects_locations_glossaries_terms_set_iam_policy(
        &self,
        args: &DataplexProjectsLocationsGlossariesTermsSetIamPolicyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleIamV1Policy, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dataplex_projects_locations_glossaries_terms_set_iam_policy_builder(
            &self.http_client,
            &args.resource,
        )
        .map_err(ProviderError::Api)?;

        let task = dataplex_projects_locations_glossaries_terms_set_iam_policy_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dataplex projects locations glossaries terms test iam permissions.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleIamV1TestIamPermissionsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn dataplex_projects_locations_glossaries_terms_test_iam_permissions(
        &self,
        args: &DataplexProjectsLocationsGlossariesTermsTestIamPermissionsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleIamV1TestIamPermissionsResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dataplex_projects_locations_glossaries_terms_test_iam_permissions_builder(
            &self.http_client,
            &args.resource,
        )
        .map_err(ProviderError::Api)?;

        let task = dataplex_projects_locations_glossaries_terms_test_iam_permissions_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dataplex projects locations governance rules get iam policy.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleIamV1Policy result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn dataplex_projects_locations_governance_rules_get_iam_policy(
        &self,
        args: &DataplexProjectsLocationsGovernanceRulesGetIamPolicyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleIamV1Policy, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dataplex_projects_locations_governance_rules_get_iam_policy_builder(
            &self.http_client,
            &args.resource,
            &args.options.requestedPolicyVersion,
        )
        .map_err(ProviderError::Api)?;

        let task = dataplex_projects_locations_governance_rules_get_iam_policy_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dataplex projects locations governance rules set iam policy.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleIamV1Policy result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn dataplex_projects_locations_governance_rules_set_iam_policy(
        &self,
        args: &DataplexProjectsLocationsGovernanceRulesSetIamPolicyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleIamV1Policy, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dataplex_projects_locations_governance_rules_set_iam_policy_builder(
            &self.http_client,
            &args.resource,
        )
        .map_err(ProviderError::Api)?;

        let task = dataplex_projects_locations_governance_rules_set_iam_policy_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dataplex projects locations governance rules test iam permissions.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleIamV1TestIamPermissionsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn dataplex_projects_locations_governance_rules_test_iam_permissions(
        &self,
        args: &DataplexProjectsLocationsGovernanceRulesTestIamPermissionsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleIamV1TestIamPermissionsResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dataplex_projects_locations_governance_rules_test_iam_permissions_builder(
            &self.http_client,
            &args.resource,
        )
        .map_err(ProviderError::Api)?;

        let task = dataplex_projects_locations_governance_rules_test_iam_permissions_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dataplex projects locations lakes create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn dataplex_projects_locations_lakes_create(
        &self,
        args: &DataplexProjectsLocationsLakesCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dataplex_projects_locations_lakes_create_builder(
            &self.http_client,
            &args.parent,
            &args.lakeId,
            &args.validateOnly,
        )
        .map_err(ProviderError::Api)?;

        let task = dataplex_projects_locations_lakes_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dataplex projects locations lakes delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn dataplex_projects_locations_lakes_delete(
        &self,
        args: &DataplexProjectsLocationsLakesDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dataplex_projects_locations_lakes_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = dataplex_projects_locations_lakes_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dataplex projects locations lakes get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudDataplexV1Lake result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn dataplex_projects_locations_lakes_get(
        &self,
        args: &DataplexProjectsLocationsLakesGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudDataplexV1Lake, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dataplex_projects_locations_lakes_get_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = dataplex_projects_locations_lakes_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dataplex projects locations lakes get iam policy.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleIamV1Policy result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn dataplex_projects_locations_lakes_get_iam_policy(
        &self,
        args: &DataplexProjectsLocationsLakesGetIamPolicyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleIamV1Policy, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dataplex_projects_locations_lakes_get_iam_policy_builder(
            &self.http_client,
            &args.resource,
            &args.options.requestedPolicyVersion,
        )
        .map_err(ProviderError::Api)?;

        let task = dataplex_projects_locations_lakes_get_iam_policy_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dataplex projects locations lakes list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudDataplexV1ListLakesResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn dataplex_projects_locations_lakes_list(
        &self,
        args: &DataplexProjectsLocationsLakesListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudDataplexV1ListLakesResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dataplex_projects_locations_lakes_list_builder(
            &self.http_client,
            &args.parent,
            &args.filter,
            &args.orderBy,
            &args.pageSize,
            &args.pageToken,
        )
        .map_err(ProviderError::Api)?;

        let task = dataplex_projects_locations_lakes_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dataplex projects locations lakes patch.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn dataplex_projects_locations_lakes_patch(
        &self,
        args: &DataplexProjectsLocationsLakesPatchArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dataplex_projects_locations_lakes_patch_builder(
            &self.http_client,
            &args.name,
            &args.updateMask,
            &args.validateOnly,
        )
        .map_err(ProviderError::Api)?;

        let task = dataplex_projects_locations_lakes_patch_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dataplex projects locations lakes set iam policy.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleIamV1Policy result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn dataplex_projects_locations_lakes_set_iam_policy(
        &self,
        args: &DataplexProjectsLocationsLakesSetIamPolicyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleIamV1Policy, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dataplex_projects_locations_lakes_set_iam_policy_builder(
            &self.http_client,
            &args.resource,
        )
        .map_err(ProviderError::Api)?;

        let task = dataplex_projects_locations_lakes_set_iam_policy_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dataplex projects locations lakes test iam permissions.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleIamV1TestIamPermissionsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn dataplex_projects_locations_lakes_test_iam_permissions(
        &self,
        args: &DataplexProjectsLocationsLakesTestIamPermissionsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleIamV1TestIamPermissionsResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dataplex_projects_locations_lakes_test_iam_permissions_builder(
            &self.http_client,
            &args.resource,
        )
        .map_err(ProviderError::Api)?;

        let task = dataplex_projects_locations_lakes_test_iam_permissions_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dataplex projects locations lakes actions list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudDataplexV1ListActionsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn dataplex_projects_locations_lakes_actions_list(
        &self,
        args: &DataplexProjectsLocationsLakesActionsListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudDataplexV1ListActionsResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dataplex_projects_locations_lakes_actions_list_builder(
            &self.http_client,
            &args.parent,
            &args.pageSize,
            &args.pageToken,
        )
        .map_err(ProviderError::Api)?;

        let task = dataplex_projects_locations_lakes_actions_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dataplex projects locations lakes tasks create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn dataplex_projects_locations_lakes_tasks_create(
        &self,
        args: &DataplexProjectsLocationsLakesTasksCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dataplex_projects_locations_lakes_tasks_create_builder(
            &self.http_client,
            &args.parent,
            &args.taskId,
            &args.validateOnly,
        )
        .map_err(ProviderError::Api)?;

        let task = dataplex_projects_locations_lakes_tasks_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dataplex projects locations lakes tasks delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn dataplex_projects_locations_lakes_tasks_delete(
        &self,
        args: &DataplexProjectsLocationsLakesTasksDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dataplex_projects_locations_lakes_tasks_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = dataplex_projects_locations_lakes_tasks_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dataplex projects locations lakes tasks get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudDataplexV1Task result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn dataplex_projects_locations_lakes_tasks_get(
        &self,
        args: &DataplexProjectsLocationsLakesTasksGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudDataplexV1Task, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dataplex_projects_locations_lakes_tasks_get_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = dataplex_projects_locations_lakes_tasks_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dataplex projects locations lakes tasks get iam policy.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleIamV1Policy result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn dataplex_projects_locations_lakes_tasks_get_iam_policy(
        &self,
        args: &DataplexProjectsLocationsLakesTasksGetIamPolicyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleIamV1Policy, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dataplex_projects_locations_lakes_tasks_get_iam_policy_builder(
            &self.http_client,
            &args.resource,
            &args.options.requestedPolicyVersion,
        )
        .map_err(ProviderError::Api)?;

        let task = dataplex_projects_locations_lakes_tasks_get_iam_policy_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dataplex projects locations lakes tasks list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudDataplexV1ListTasksResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn dataplex_projects_locations_lakes_tasks_list(
        &self,
        args: &DataplexProjectsLocationsLakesTasksListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudDataplexV1ListTasksResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dataplex_projects_locations_lakes_tasks_list_builder(
            &self.http_client,
            &args.parent,
            &args.filter,
            &args.orderBy,
            &args.pageSize,
            &args.pageToken,
        )
        .map_err(ProviderError::Api)?;

        let task = dataplex_projects_locations_lakes_tasks_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dataplex projects locations lakes tasks patch.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn dataplex_projects_locations_lakes_tasks_patch(
        &self,
        args: &DataplexProjectsLocationsLakesTasksPatchArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dataplex_projects_locations_lakes_tasks_patch_builder(
            &self.http_client,
            &args.name,
            &args.updateMask,
            &args.validateOnly,
        )
        .map_err(ProviderError::Api)?;

        let task = dataplex_projects_locations_lakes_tasks_patch_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dataplex projects locations lakes tasks run.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudDataplexV1RunTaskResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn dataplex_projects_locations_lakes_tasks_run(
        &self,
        args: &DataplexProjectsLocationsLakesTasksRunArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudDataplexV1RunTaskResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dataplex_projects_locations_lakes_tasks_run_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = dataplex_projects_locations_lakes_tasks_run_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dataplex projects locations lakes tasks set iam policy.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleIamV1Policy result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn dataplex_projects_locations_lakes_tasks_set_iam_policy(
        &self,
        args: &DataplexProjectsLocationsLakesTasksSetIamPolicyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleIamV1Policy, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dataplex_projects_locations_lakes_tasks_set_iam_policy_builder(
            &self.http_client,
            &args.resource,
        )
        .map_err(ProviderError::Api)?;

        let task = dataplex_projects_locations_lakes_tasks_set_iam_policy_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dataplex projects locations lakes tasks test iam permissions.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleIamV1TestIamPermissionsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn dataplex_projects_locations_lakes_tasks_test_iam_permissions(
        &self,
        args: &DataplexProjectsLocationsLakesTasksTestIamPermissionsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleIamV1TestIamPermissionsResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dataplex_projects_locations_lakes_tasks_test_iam_permissions_builder(
            &self.http_client,
            &args.resource,
        )
        .map_err(ProviderError::Api)?;

        let task = dataplex_projects_locations_lakes_tasks_test_iam_permissions_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dataplex projects locations lakes tasks jobs cancel.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Empty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn dataplex_projects_locations_lakes_tasks_jobs_cancel(
        &self,
        args: &DataplexProjectsLocationsLakesTasksJobsCancelArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Empty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dataplex_projects_locations_lakes_tasks_jobs_cancel_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = dataplex_projects_locations_lakes_tasks_jobs_cancel_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dataplex projects locations lakes tasks jobs get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudDataplexV1Job result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn dataplex_projects_locations_lakes_tasks_jobs_get(
        &self,
        args: &DataplexProjectsLocationsLakesTasksJobsGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudDataplexV1Job, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dataplex_projects_locations_lakes_tasks_jobs_get_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = dataplex_projects_locations_lakes_tasks_jobs_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dataplex projects locations lakes tasks jobs list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudDataplexV1ListJobsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn dataplex_projects_locations_lakes_tasks_jobs_list(
        &self,
        args: &DataplexProjectsLocationsLakesTasksJobsListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudDataplexV1ListJobsResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dataplex_projects_locations_lakes_tasks_jobs_list_builder(
            &self.http_client,
            &args.parent,
            &args.pageSize,
            &args.pageToken,
        )
        .map_err(ProviderError::Api)?;

        let task = dataplex_projects_locations_lakes_tasks_jobs_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dataplex projects locations lakes zones create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn dataplex_projects_locations_lakes_zones_create(
        &self,
        args: &DataplexProjectsLocationsLakesZonesCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dataplex_projects_locations_lakes_zones_create_builder(
            &self.http_client,
            &args.parent,
            &args.validateOnly,
            &args.zoneId,
        )
        .map_err(ProviderError::Api)?;

        let task = dataplex_projects_locations_lakes_zones_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dataplex projects locations lakes zones delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn dataplex_projects_locations_lakes_zones_delete(
        &self,
        args: &DataplexProjectsLocationsLakesZonesDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dataplex_projects_locations_lakes_zones_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = dataplex_projects_locations_lakes_zones_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dataplex projects locations lakes zones get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudDataplexV1Zone result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn dataplex_projects_locations_lakes_zones_get(
        &self,
        args: &DataplexProjectsLocationsLakesZonesGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudDataplexV1Zone, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dataplex_projects_locations_lakes_zones_get_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = dataplex_projects_locations_lakes_zones_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dataplex projects locations lakes zones get iam policy.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleIamV1Policy result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn dataplex_projects_locations_lakes_zones_get_iam_policy(
        &self,
        args: &DataplexProjectsLocationsLakesZonesGetIamPolicyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleIamV1Policy, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dataplex_projects_locations_lakes_zones_get_iam_policy_builder(
            &self.http_client,
            &args.resource,
            &args.options.requestedPolicyVersion,
        )
        .map_err(ProviderError::Api)?;

        let task = dataplex_projects_locations_lakes_zones_get_iam_policy_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dataplex projects locations lakes zones list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudDataplexV1ListZonesResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn dataplex_projects_locations_lakes_zones_list(
        &self,
        args: &DataplexProjectsLocationsLakesZonesListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudDataplexV1ListZonesResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dataplex_projects_locations_lakes_zones_list_builder(
            &self.http_client,
            &args.parent,
            &args.filter,
            &args.orderBy,
            &args.pageSize,
            &args.pageToken,
        )
        .map_err(ProviderError::Api)?;

        let task = dataplex_projects_locations_lakes_zones_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dataplex projects locations lakes zones patch.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn dataplex_projects_locations_lakes_zones_patch(
        &self,
        args: &DataplexProjectsLocationsLakesZonesPatchArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dataplex_projects_locations_lakes_zones_patch_builder(
            &self.http_client,
            &args.name,
            &args.updateMask,
            &args.validateOnly,
        )
        .map_err(ProviderError::Api)?;

        let task = dataplex_projects_locations_lakes_zones_patch_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dataplex projects locations lakes zones set iam policy.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleIamV1Policy result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn dataplex_projects_locations_lakes_zones_set_iam_policy(
        &self,
        args: &DataplexProjectsLocationsLakesZonesSetIamPolicyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleIamV1Policy, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dataplex_projects_locations_lakes_zones_set_iam_policy_builder(
            &self.http_client,
            &args.resource,
        )
        .map_err(ProviderError::Api)?;

        let task = dataplex_projects_locations_lakes_zones_set_iam_policy_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dataplex projects locations lakes zones test iam permissions.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleIamV1TestIamPermissionsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn dataplex_projects_locations_lakes_zones_test_iam_permissions(
        &self,
        args: &DataplexProjectsLocationsLakesZonesTestIamPermissionsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleIamV1TestIamPermissionsResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dataplex_projects_locations_lakes_zones_test_iam_permissions_builder(
            &self.http_client,
            &args.resource,
        )
        .map_err(ProviderError::Api)?;

        let task = dataplex_projects_locations_lakes_zones_test_iam_permissions_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dataplex projects locations lakes zones actions list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudDataplexV1ListActionsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn dataplex_projects_locations_lakes_zones_actions_list(
        &self,
        args: &DataplexProjectsLocationsLakesZonesActionsListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudDataplexV1ListActionsResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dataplex_projects_locations_lakes_zones_actions_list_builder(
            &self.http_client,
            &args.parent,
            &args.pageSize,
            &args.pageToken,
        )
        .map_err(ProviderError::Api)?;

        let task = dataplex_projects_locations_lakes_zones_actions_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dataplex projects locations lakes zones assets create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn dataplex_projects_locations_lakes_zones_assets_create(
        &self,
        args: &DataplexProjectsLocationsLakesZonesAssetsCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dataplex_projects_locations_lakes_zones_assets_create_builder(
            &self.http_client,
            &args.parent,
            &args.assetId,
            &args.validateOnly,
        )
        .map_err(ProviderError::Api)?;

        let task = dataplex_projects_locations_lakes_zones_assets_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dataplex projects locations lakes zones assets delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn dataplex_projects_locations_lakes_zones_assets_delete(
        &self,
        args: &DataplexProjectsLocationsLakesZonesAssetsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dataplex_projects_locations_lakes_zones_assets_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = dataplex_projects_locations_lakes_zones_assets_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dataplex projects locations lakes zones assets get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudDataplexV1Asset result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn dataplex_projects_locations_lakes_zones_assets_get(
        &self,
        args: &DataplexProjectsLocationsLakesZonesAssetsGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudDataplexV1Asset, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dataplex_projects_locations_lakes_zones_assets_get_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = dataplex_projects_locations_lakes_zones_assets_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dataplex projects locations lakes zones assets get iam policy.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleIamV1Policy result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn dataplex_projects_locations_lakes_zones_assets_get_iam_policy(
        &self,
        args: &DataplexProjectsLocationsLakesZonesAssetsGetIamPolicyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleIamV1Policy, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dataplex_projects_locations_lakes_zones_assets_get_iam_policy_builder(
            &self.http_client,
            &args.resource,
            &args.options.requestedPolicyVersion,
        )
        .map_err(ProviderError::Api)?;

        let task = dataplex_projects_locations_lakes_zones_assets_get_iam_policy_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dataplex projects locations lakes zones assets list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudDataplexV1ListAssetsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn dataplex_projects_locations_lakes_zones_assets_list(
        &self,
        args: &DataplexProjectsLocationsLakesZonesAssetsListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudDataplexV1ListAssetsResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dataplex_projects_locations_lakes_zones_assets_list_builder(
            &self.http_client,
            &args.parent,
            &args.filter,
            &args.orderBy,
            &args.pageSize,
            &args.pageToken,
        )
        .map_err(ProviderError::Api)?;

        let task = dataplex_projects_locations_lakes_zones_assets_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dataplex projects locations lakes zones assets patch.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn dataplex_projects_locations_lakes_zones_assets_patch(
        &self,
        args: &DataplexProjectsLocationsLakesZonesAssetsPatchArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dataplex_projects_locations_lakes_zones_assets_patch_builder(
            &self.http_client,
            &args.name,
            &args.updateMask,
            &args.validateOnly,
        )
        .map_err(ProviderError::Api)?;

        let task = dataplex_projects_locations_lakes_zones_assets_patch_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dataplex projects locations lakes zones assets set iam policy.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleIamV1Policy result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn dataplex_projects_locations_lakes_zones_assets_set_iam_policy(
        &self,
        args: &DataplexProjectsLocationsLakesZonesAssetsSetIamPolicyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleIamV1Policy, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dataplex_projects_locations_lakes_zones_assets_set_iam_policy_builder(
            &self.http_client,
            &args.resource,
        )
        .map_err(ProviderError::Api)?;

        let task = dataplex_projects_locations_lakes_zones_assets_set_iam_policy_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dataplex projects locations lakes zones assets test iam permissions.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleIamV1TestIamPermissionsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn dataplex_projects_locations_lakes_zones_assets_test_iam_permissions(
        &self,
        args: &DataplexProjectsLocationsLakesZonesAssetsTestIamPermissionsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleIamV1TestIamPermissionsResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dataplex_projects_locations_lakes_zones_assets_test_iam_permissions_builder(
            &self.http_client,
            &args.resource,
        )
        .map_err(ProviderError::Api)?;

        let task = dataplex_projects_locations_lakes_zones_assets_test_iam_permissions_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dataplex projects locations lakes zones assets actions list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudDataplexV1ListActionsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn dataplex_projects_locations_lakes_zones_assets_actions_list(
        &self,
        args: &DataplexProjectsLocationsLakesZonesAssetsActionsListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudDataplexV1ListActionsResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dataplex_projects_locations_lakes_zones_assets_actions_list_builder(
            &self.http_client,
            &args.parent,
            &args.pageSize,
            &args.pageToken,
        )
        .map_err(ProviderError::Api)?;

        let task = dataplex_projects_locations_lakes_zones_assets_actions_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dataplex projects locations lakes zones entities create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudDataplexV1Entity result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn dataplex_projects_locations_lakes_zones_entities_create(
        &self,
        args: &DataplexProjectsLocationsLakesZonesEntitiesCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudDataplexV1Entity, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dataplex_projects_locations_lakes_zones_entities_create_builder(
            &self.http_client,
            &args.parent,
            &args.validateOnly,
        )
        .map_err(ProviderError::Api)?;

        let task = dataplex_projects_locations_lakes_zones_entities_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dataplex projects locations lakes zones entities delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Empty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn dataplex_projects_locations_lakes_zones_entities_delete(
        &self,
        args: &DataplexProjectsLocationsLakesZonesEntitiesDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Empty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dataplex_projects_locations_lakes_zones_entities_delete_builder(
            &self.http_client,
            &args.name,
            &args.etag,
        )
        .map_err(ProviderError::Api)?;

        let task = dataplex_projects_locations_lakes_zones_entities_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dataplex projects locations lakes zones entities get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudDataplexV1Entity result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn dataplex_projects_locations_lakes_zones_entities_get(
        &self,
        args: &DataplexProjectsLocationsLakesZonesEntitiesGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudDataplexV1Entity, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dataplex_projects_locations_lakes_zones_entities_get_builder(
            &self.http_client,
            &args.name,
            &args.view,
        )
        .map_err(ProviderError::Api)?;

        let task = dataplex_projects_locations_lakes_zones_entities_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dataplex projects locations lakes zones entities list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudDataplexV1ListEntitiesResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn dataplex_projects_locations_lakes_zones_entities_list(
        &self,
        args: &DataplexProjectsLocationsLakesZonesEntitiesListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudDataplexV1ListEntitiesResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dataplex_projects_locations_lakes_zones_entities_list_builder(
            &self.http_client,
            &args.parent,
            &args.filter,
            &args.pageSize,
            &args.pageToken,
            &args.view,
        )
        .map_err(ProviderError::Api)?;

        let task = dataplex_projects_locations_lakes_zones_entities_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dataplex projects locations lakes zones entities update.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudDataplexV1Entity result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn dataplex_projects_locations_lakes_zones_entities_update(
        &self,
        args: &DataplexProjectsLocationsLakesZonesEntitiesUpdateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudDataplexV1Entity, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dataplex_projects_locations_lakes_zones_entities_update_builder(
            &self.http_client,
            &args.name,
            &args.validateOnly,
        )
        .map_err(ProviderError::Api)?;

        let task = dataplex_projects_locations_lakes_zones_entities_update_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dataplex projects locations lakes zones entities partitions create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudDataplexV1Partition result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn dataplex_projects_locations_lakes_zones_entities_partitions_create(
        &self,
        args: &DataplexProjectsLocationsLakesZonesEntitiesPartitionsCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudDataplexV1Partition, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dataplex_projects_locations_lakes_zones_entities_partitions_create_builder(
            &self.http_client,
            &args.parent,
            &args.validateOnly,
        )
        .map_err(ProviderError::Api)?;

        let task = dataplex_projects_locations_lakes_zones_entities_partitions_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dataplex projects locations lakes zones entities partitions delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Empty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn dataplex_projects_locations_lakes_zones_entities_partitions_delete(
        &self,
        args: &DataplexProjectsLocationsLakesZonesEntitiesPartitionsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Empty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dataplex_projects_locations_lakes_zones_entities_partitions_delete_builder(
            &self.http_client,
            &args.name,
            &args.etag,
        )
        .map_err(ProviderError::Api)?;

        let task = dataplex_projects_locations_lakes_zones_entities_partitions_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dataplex projects locations lakes zones entities partitions get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudDataplexV1Partition result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn dataplex_projects_locations_lakes_zones_entities_partitions_get(
        &self,
        args: &DataplexProjectsLocationsLakesZonesEntitiesPartitionsGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudDataplexV1Partition, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dataplex_projects_locations_lakes_zones_entities_partitions_get_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = dataplex_projects_locations_lakes_zones_entities_partitions_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dataplex projects locations lakes zones entities partitions list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudDataplexV1ListPartitionsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn dataplex_projects_locations_lakes_zones_entities_partitions_list(
        &self,
        args: &DataplexProjectsLocationsLakesZonesEntitiesPartitionsListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudDataplexV1ListPartitionsResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dataplex_projects_locations_lakes_zones_entities_partitions_list_builder(
            &self.http_client,
            &args.parent,
            &args.filter,
            &args.pageSize,
            &args.pageToken,
        )
        .map_err(ProviderError::Api)?;

        let task = dataplex_projects_locations_lakes_zones_entities_partitions_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dataplex projects locations metadata feeds create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn dataplex_projects_locations_metadata_feeds_create(
        &self,
        args: &DataplexProjectsLocationsMetadataFeedsCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dataplex_projects_locations_metadata_feeds_create_builder(
            &self.http_client,
            &args.parent,
            &args.metadataFeedId,
            &args.validateOnly,
        )
        .map_err(ProviderError::Api)?;

        let task = dataplex_projects_locations_metadata_feeds_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dataplex projects locations metadata feeds delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn dataplex_projects_locations_metadata_feeds_delete(
        &self,
        args: &DataplexProjectsLocationsMetadataFeedsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dataplex_projects_locations_metadata_feeds_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = dataplex_projects_locations_metadata_feeds_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dataplex projects locations metadata feeds get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudDataplexV1MetadataFeed result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn dataplex_projects_locations_metadata_feeds_get(
        &self,
        args: &DataplexProjectsLocationsMetadataFeedsGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudDataplexV1MetadataFeed, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dataplex_projects_locations_metadata_feeds_get_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = dataplex_projects_locations_metadata_feeds_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dataplex projects locations metadata feeds list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudDataplexV1ListMetadataFeedsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn dataplex_projects_locations_metadata_feeds_list(
        &self,
        args: &DataplexProjectsLocationsMetadataFeedsListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudDataplexV1ListMetadataFeedsResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dataplex_projects_locations_metadata_feeds_list_builder(
            &self.http_client,
            &args.parent,
            &args.filter,
            &args.orderBy,
            &args.pageSize,
            &args.pageToken,
        )
        .map_err(ProviderError::Api)?;

        let task = dataplex_projects_locations_metadata_feeds_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dataplex projects locations metadata feeds patch.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn dataplex_projects_locations_metadata_feeds_patch(
        &self,
        args: &DataplexProjectsLocationsMetadataFeedsPatchArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dataplex_projects_locations_metadata_feeds_patch_builder(
            &self.http_client,
            &args.name,
            &args.updateMask,
            &args.validateOnly,
        )
        .map_err(ProviderError::Api)?;

        let task = dataplex_projects_locations_metadata_feeds_patch_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dataplex projects locations metadata jobs cancel.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Empty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn dataplex_projects_locations_metadata_jobs_cancel(
        &self,
        args: &DataplexProjectsLocationsMetadataJobsCancelArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Empty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dataplex_projects_locations_metadata_jobs_cancel_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = dataplex_projects_locations_metadata_jobs_cancel_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dataplex projects locations metadata jobs create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn dataplex_projects_locations_metadata_jobs_create(
        &self,
        args: &DataplexProjectsLocationsMetadataJobsCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dataplex_projects_locations_metadata_jobs_create_builder(
            &self.http_client,
            &args.parent,
            &args.metadataJobId,
            &args.validateOnly,
        )
        .map_err(ProviderError::Api)?;

        let task = dataplex_projects_locations_metadata_jobs_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dataplex projects locations metadata jobs get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudDataplexV1MetadataJob result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn dataplex_projects_locations_metadata_jobs_get(
        &self,
        args: &DataplexProjectsLocationsMetadataJobsGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudDataplexV1MetadataJob, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dataplex_projects_locations_metadata_jobs_get_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = dataplex_projects_locations_metadata_jobs_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dataplex projects locations metadata jobs list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudDataplexV1ListMetadataJobsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn dataplex_projects_locations_metadata_jobs_list(
        &self,
        args: &DataplexProjectsLocationsMetadataJobsListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudDataplexV1ListMetadataJobsResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dataplex_projects_locations_metadata_jobs_list_builder(
            &self.http_client,
            &args.parent,
            &args.filter,
            &args.orderBy,
            &args.pageSize,
            &args.pageToken,
        )
        .map_err(ProviderError::Api)?;

        let task = dataplex_projects_locations_metadata_jobs_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dataplex projects locations operations cancel.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Empty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn dataplex_projects_locations_operations_cancel(
        &self,
        args: &DataplexProjectsLocationsOperationsCancelArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Empty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dataplex_projects_locations_operations_cancel_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = dataplex_projects_locations_operations_cancel_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dataplex projects locations operations delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Empty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn dataplex_projects_locations_operations_delete(
        &self,
        args: &DataplexProjectsLocationsOperationsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Empty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dataplex_projects_locations_operations_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = dataplex_projects_locations_operations_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dataplex projects locations operations get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn dataplex_projects_locations_operations_get(
        &self,
        args: &DataplexProjectsLocationsOperationsGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dataplex_projects_locations_operations_get_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = dataplex_projects_locations_operations_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dataplex projects locations operations list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningListOperationsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn dataplex_projects_locations_operations_list(
        &self,
        args: &DataplexProjectsLocationsOperationsListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningListOperationsResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dataplex_projects_locations_operations_list_builder(
            &self.http_client,
            &args.name,
            &args.filter,
            &args.pageSize,
            &args.pageToken,
            &args.returnPartialSuccess,
        )
        .map_err(ProviderError::Api)?;

        let task = dataplex_projects_locations_operations_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dataplex projects locations policy intents get iam policy.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleIamV1Policy result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn dataplex_projects_locations_policy_intents_get_iam_policy(
        &self,
        args: &DataplexProjectsLocationsPolicyIntentsGetIamPolicyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleIamV1Policy, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dataplex_projects_locations_policy_intents_get_iam_policy_builder(
            &self.http_client,
            &args.resource,
            &args.options.requestedPolicyVersion,
        )
        .map_err(ProviderError::Api)?;

        let task = dataplex_projects_locations_policy_intents_get_iam_policy_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dataplex projects locations policy intents set iam policy.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleIamV1Policy result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn dataplex_projects_locations_policy_intents_set_iam_policy(
        &self,
        args: &DataplexProjectsLocationsPolicyIntentsSetIamPolicyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleIamV1Policy, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dataplex_projects_locations_policy_intents_set_iam_policy_builder(
            &self.http_client,
            &args.resource,
        )
        .map_err(ProviderError::Api)?;

        let task = dataplex_projects_locations_policy_intents_set_iam_policy_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Dataplex projects locations policy intents test iam permissions.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleIamV1TestIamPermissionsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn dataplex_projects_locations_policy_intents_test_iam_permissions(
        &self,
        args: &DataplexProjectsLocationsPolicyIntentsTestIamPermissionsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleIamV1TestIamPermissionsResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = dataplex_projects_locations_policy_intents_test_iam_permissions_builder(
            &self.http_client,
            &args.resource,
        )
        .map_err(ProviderError::Api)?;

        let task = dataplex_projects_locations_policy_intents_test_iam_permissions_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

}
