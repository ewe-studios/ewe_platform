//! NetworksecurityProvider - State-aware networksecurity API client.
//!
//! WHY: Users need state-aware API clients that automatically track
//!      resource changes in the state store.
//!
//! WHAT: Provider wrapping ProviderClient<S> with methods for
//!       networksecurity API endpoints that auto-store results.
//!
//! HOW: Each method wraps the task with StoreStateIdentifierTask
//!      for automatic state persistence on success.

#![cfg(feature = "gcp")]

use crate::providers::gcp::clients::networksecurity::{
    networksecurity_organizations_locations_address_groups_add_items_builder, networksecurity_organizations_locations_address_groups_add_items_task,
    networksecurity_organizations_locations_address_groups_clone_items_builder, networksecurity_organizations_locations_address_groups_clone_items_task,
    networksecurity_organizations_locations_address_groups_create_builder, networksecurity_organizations_locations_address_groups_create_task,
    networksecurity_organizations_locations_address_groups_delete_builder, networksecurity_organizations_locations_address_groups_delete_task,
    networksecurity_organizations_locations_address_groups_get_builder, networksecurity_organizations_locations_address_groups_get_task,
    networksecurity_organizations_locations_address_groups_list_builder, networksecurity_organizations_locations_address_groups_list_task,
    networksecurity_organizations_locations_address_groups_list_references_builder, networksecurity_organizations_locations_address_groups_list_references_task,
    networksecurity_organizations_locations_address_groups_patch_builder, networksecurity_organizations_locations_address_groups_patch_task,
    networksecurity_organizations_locations_address_groups_remove_items_builder, networksecurity_organizations_locations_address_groups_remove_items_task,
    networksecurity_organizations_locations_firewall_endpoints_create_builder, networksecurity_organizations_locations_firewall_endpoints_create_task,
    networksecurity_organizations_locations_firewall_endpoints_delete_builder, networksecurity_organizations_locations_firewall_endpoints_delete_task,
    networksecurity_organizations_locations_firewall_endpoints_get_builder, networksecurity_organizations_locations_firewall_endpoints_get_task,
    networksecurity_organizations_locations_firewall_endpoints_list_builder, networksecurity_organizations_locations_firewall_endpoints_list_task,
    networksecurity_organizations_locations_firewall_endpoints_patch_builder, networksecurity_organizations_locations_firewall_endpoints_patch_task,
    networksecurity_organizations_locations_operations_cancel_builder, networksecurity_organizations_locations_operations_cancel_task,
    networksecurity_organizations_locations_operations_delete_builder, networksecurity_organizations_locations_operations_delete_task,
    networksecurity_organizations_locations_operations_get_builder, networksecurity_organizations_locations_operations_get_task,
    networksecurity_organizations_locations_operations_list_builder, networksecurity_organizations_locations_operations_list_task,
    networksecurity_organizations_locations_security_profile_groups_create_builder, networksecurity_organizations_locations_security_profile_groups_create_task,
    networksecurity_organizations_locations_security_profile_groups_delete_builder, networksecurity_organizations_locations_security_profile_groups_delete_task,
    networksecurity_organizations_locations_security_profile_groups_get_builder, networksecurity_organizations_locations_security_profile_groups_get_task,
    networksecurity_organizations_locations_security_profile_groups_list_builder, networksecurity_organizations_locations_security_profile_groups_list_task,
    networksecurity_organizations_locations_security_profile_groups_patch_builder, networksecurity_organizations_locations_security_profile_groups_patch_task,
    networksecurity_organizations_locations_security_profiles_create_builder, networksecurity_organizations_locations_security_profiles_create_task,
    networksecurity_organizations_locations_security_profiles_delete_builder, networksecurity_organizations_locations_security_profiles_delete_task,
    networksecurity_organizations_locations_security_profiles_get_builder, networksecurity_organizations_locations_security_profiles_get_task,
    networksecurity_organizations_locations_security_profiles_list_builder, networksecurity_organizations_locations_security_profiles_list_task,
    networksecurity_organizations_locations_security_profiles_patch_builder, networksecurity_organizations_locations_security_profiles_patch_task,
    networksecurity_projects_locations_get_builder, networksecurity_projects_locations_get_task,
    networksecurity_projects_locations_list_builder, networksecurity_projects_locations_list_task,
    networksecurity_projects_locations_address_groups_add_items_builder, networksecurity_projects_locations_address_groups_add_items_task,
    networksecurity_projects_locations_address_groups_clone_items_builder, networksecurity_projects_locations_address_groups_clone_items_task,
    networksecurity_projects_locations_address_groups_create_builder, networksecurity_projects_locations_address_groups_create_task,
    networksecurity_projects_locations_address_groups_delete_builder, networksecurity_projects_locations_address_groups_delete_task,
    networksecurity_projects_locations_address_groups_get_builder, networksecurity_projects_locations_address_groups_get_task,
    networksecurity_projects_locations_address_groups_get_iam_policy_builder, networksecurity_projects_locations_address_groups_get_iam_policy_task,
    networksecurity_projects_locations_address_groups_list_builder, networksecurity_projects_locations_address_groups_list_task,
    networksecurity_projects_locations_address_groups_list_references_builder, networksecurity_projects_locations_address_groups_list_references_task,
    networksecurity_projects_locations_address_groups_patch_builder, networksecurity_projects_locations_address_groups_patch_task,
    networksecurity_projects_locations_address_groups_remove_items_builder, networksecurity_projects_locations_address_groups_remove_items_task,
    networksecurity_projects_locations_address_groups_set_iam_policy_builder, networksecurity_projects_locations_address_groups_set_iam_policy_task,
    networksecurity_projects_locations_address_groups_test_iam_permissions_builder, networksecurity_projects_locations_address_groups_test_iam_permissions_task,
    networksecurity_projects_locations_authorization_policies_create_builder, networksecurity_projects_locations_authorization_policies_create_task,
    networksecurity_projects_locations_authorization_policies_delete_builder, networksecurity_projects_locations_authorization_policies_delete_task,
    networksecurity_projects_locations_authorization_policies_get_builder, networksecurity_projects_locations_authorization_policies_get_task,
    networksecurity_projects_locations_authorization_policies_get_iam_policy_builder, networksecurity_projects_locations_authorization_policies_get_iam_policy_task,
    networksecurity_projects_locations_authorization_policies_list_builder, networksecurity_projects_locations_authorization_policies_list_task,
    networksecurity_projects_locations_authorization_policies_patch_builder, networksecurity_projects_locations_authorization_policies_patch_task,
    networksecurity_projects_locations_authorization_policies_set_iam_policy_builder, networksecurity_projects_locations_authorization_policies_set_iam_policy_task,
    networksecurity_projects_locations_authorization_policies_test_iam_permissions_builder, networksecurity_projects_locations_authorization_policies_test_iam_permissions_task,
    networksecurity_projects_locations_authz_policies_create_builder, networksecurity_projects_locations_authz_policies_create_task,
    networksecurity_projects_locations_authz_policies_delete_builder, networksecurity_projects_locations_authz_policies_delete_task,
    networksecurity_projects_locations_authz_policies_get_builder, networksecurity_projects_locations_authz_policies_get_task,
    networksecurity_projects_locations_authz_policies_get_iam_policy_builder, networksecurity_projects_locations_authz_policies_get_iam_policy_task,
    networksecurity_projects_locations_authz_policies_list_builder, networksecurity_projects_locations_authz_policies_list_task,
    networksecurity_projects_locations_authz_policies_patch_builder, networksecurity_projects_locations_authz_policies_patch_task,
    networksecurity_projects_locations_authz_policies_set_iam_policy_builder, networksecurity_projects_locations_authz_policies_set_iam_policy_task,
    networksecurity_projects_locations_authz_policies_test_iam_permissions_builder, networksecurity_projects_locations_authz_policies_test_iam_permissions_task,
    networksecurity_projects_locations_backend_authentication_configs_create_builder, networksecurity_projects_locations_backend_authentication_configs_create_task,
    networksecurity_projects_locations_backend_authentication_configs_delete_builder, networksecurity_projects_locations_backend_authentication_configs_delete_task,
    networksecurity_projects_locations_backend_authentication_configs_get_builder, networksecurity_projects_locations_backend_authentication_configs_get_task,
    networksecurity_projects_locations_backend_authentication_configs_list_builder, networksecurity_projects_locations_backend_authentication_configs_list_task,
    networksecurity_projects_locations_backend_authentication_configs_patch_builder, networksecurity_projects_locations_backend_authentication_configs_patch_task,
    networksecurity_projects_locations_client_tls_policies_create_builder, networksecurity_projects_locations_client_tls_policies_create_task,
    networksecurity_projects_locations_client_tls_policies_delete_builder, networksecurity_projects_locations_client_tls_policies_delete_task,
    networksecurity_projects_locations_client_tls_policies_get_builder, networksecurity_projects_locations_client_tls_policies_get_task,
    networksecurity_projects_locations_client_tls_policies_get_iam_policy_builder, networksecurity_projects_locations_client_tls_policies_get_iam_policy_task,
    networksecurity_projects_locations_client_tls_policies_list_builder, networksecurity_projects_locations_client_tls_policies_list_task,
    networksecurity_projects_locations_client_tls_policies_patch_builder, networksecurity_projects_locations_client_tls_policies_patch_task,
    networksecurity_projects_locations_client_tls_policies_set_iam_policy_builder, networksecurity_projects_locations_client_tls_policies_set_iam_policy_task,
    networksecurity_projects_locations_client_tls_policies_test_iam_permissions_builder, networksecurity_projects_locations_client_tls_policies_test_iam_permissions_task,
    networksecurity_projects_locations_dns_threat_detectors_create_builder, networksecurity_projects_locations_dns_threat_detectors_create_task,
    networksecurity_projects_locations_dns_threat_detectors_delete_builder, networksecurity_projects_locations_dns_threat_detectors_delete_task,
    networksecurity_projects_locations_dns_threat_detectors_get_builder, networksecurity_projects_locations_dns_threat_detectors_get_task,
    networksecurity_projects_locations_dns_threat_detectors_list_builder, networksecurity_projects_locations_dns_threat_detectors_list_task,
    networksecurity_projects_locations_dns_threat_detectors_patch_builder, networksecurity_projects_locations_dns_threat_detectors_patch_task,
    networksecurity_projects_locations_firewall_endpoint_associations_create_builder, networksecurity_projects_locations_firewall_endpoint_associations_create_task,
    networksecurity_projects_locations_firewall_endpoint_associations_delete_builder, networksecurity_projects_locations_firewall_endpoint_associations_delete_task,
    networksecurity_projects_locations_firewall_endpoint_associations_get_builder, networksecurity_projects_locations_firewall_endpoint_associations_get_task,
    networksecurity_projects_locations_firewall_endpoint_associations_list_builder, networksecurity_projects_locations_firewall_endpoint_associations_list_task,
    networksecurity_projects_locations_firewall_endpoint_associations_patch_builder, networksecurity_projects_locations_firewall_endpoint_associations_patch_task,
    networksecurity_projects_locations_gateway_security_policies_create_builder, networksecurity_projects_locations_gateway_security_policies_create_task,
    networksecurity_projects_locations_gateway_security_policies_delete_builder, networksecurity_projects_locations_gateway_security_policies_delete_task,
    networksecurity_projects_locations_gateway_security_policies_get_builder, networksecurity_projects_locations_gateway_security_policies_get_task,
    networksecurity_projects_locations_gateway_security_policies_list_builder, networksecurity_projects_locations_gateway_security_policies_list_task,
    networksecurity_projects_locations_gateway_security_policies_patch_builder, networksecurity_projects_locations_gateway_security_policies_patch_task,
    networksecurity_projects_locations_gateway_security_policies_rules_create_builder, networksecurity_projects_locations_gateway_security_policies_rules_create_task,
    networksecurity_projects_locations_gateway_security_policies_rules_delete_builder, networksecurity_projects_locations_gateway_security_policies_rules_delete_task,
    networksecurity_projects_locations_gateway_security_policies_rules_get_builder, networksecurity_projects_locations_gateway_security_policies_rules_get_task,
    networksecurity_projects_locations_gateway_security_policies_rules_list_builder, networksecurity_projects_locations_gateway_security_policies_rules_list_task,
    networksecurity_projects_locations_gateway_security_policies_rules_patch_builder, networksecurity_projects_locations_gateway_security_policies_rules_patch_task,
    networksecurity_projects_locations_intercept_deployment_groups_create_builder, networksecurity_projects_locations_intercept_deployment_groups_create_task,
    networksecurity_projects_locations_intercept_deployment_groups_delete_builder, networksecurity_projects_locations_intercept_deployment_groups_delete_task,
    networksecurity_projects_locations_intercept_deployment_groups_get_builder, networksecurity_projects_locations_intercept_deployment_groups_get_task,
    networksecurity_projects_locations_intercept_deployment_groups_list_builder, networksecurity_projects_locations_intercept_deployment_groups_list_task,
    networksecurity_projects_locations_intercept_deployment_groups_patch_builder, networksecurity_projects_locations_intercept_deployment_groups_patch_task,
    networksecurity_projects_locations_intercept_deployments_create_builder, networksecurity_projects_locations_intercept_deployments_create_task,
    networksecurity_projects_locations_intercept_deployments_delete_builder, networksecurity_projects_locations_intercept_deployments_delete_task,
    networksecurity_projects_locations_intercept_deployments_get_builder, networksecurity_projects_locations_intercept_deployments_get_task,
    networksecurity_projects_locations_intercept_deployments_list_builder, networksecurity_projects_locations_intercept_deployments_list_task,
    networksecurity_projects_locations_intercept_deployments_patch_builder, networksecurity_projects_locations_intercept_deployments_patch_task,
    networksecurity_projects_locations_intercept_endpoint_group_associations_create_builder, networksecurity_projects_locations_intercept_endpoint_group_associations_create_task,
    networksecurity_projects_locations_intercept_endpoint_group_associations_delete_builder, networksecurity_projects_locations_intercept_endpoint_group_associations_delete_task,
    networksecurity_projects_locations_intercept_endpoint_group_associations_get_builder, networksecurity_projects_locations_intercept_endpoint_group_associations_get_task,
    networksecurity_projects_locations_intercept_endpoint_group_associations_list_builder, networksecurity_projects_locations_intercept_endpoint_group_associations_list_task,
    networksecurity_projects_locations_intercept_endpoint_group_associations_patch_builder, networksecurity_projects_locations_intercept_endpoint_group_associations_patch_task,
    networksecurity_projects_locations_intercept_endpoint_groups_create_builder, networksecurity_projects_locations_intercept_endpoint_groups_create_task,
    networksecurity_projects_locations_intercept_endpoint_groups_delete_builder, networksecurity_projects_locations_intercept_endpoint_groups_delete_task,
    networksecurity_projects_locations_intercept_endpoint_groups_get_builder, networksecurity_projects_locations_intercept_endpoint_groups_get_task,
    networksecurity_projects_locations_intercept_endpoint_groups_list_builder, networksecurity_projects_locations_intercept_endpoint_groups_list_task,
    networksecurity_projects_locations_intercept_endpoint_groups_patch_builder, networksecurity_projects_locations_intercept_endpoint_groups_patch_task,
    networksecurity_projects_locations_mirroring_deployment_groups_create_builder, networksecurity_projects_locations_mirroring_deployment_groups_create_task,
    networksecurity_projects_locations_mirroring_deployment_groups_delete_builder, networksecurity_projects_locations_mirroring_deployment_groups_delete_task,
    networksecurity_projects_locations_mirroring_deployment_groups_get_builder, networksecurity_projects_locations_mirroring_deployment_groups_get_task,
    networksecurity_projects_locations_mirroring_deployment_groups_list_builder, networksecurity_projects_locations_mirroring_deployment_groups_list_task,
    networksecurity_projects_locations_mirroring_deployment_groups_patch_builder, networksecurity_projects_locations_mirroring_deployment_groups_patch_task,
    networksecurity_projects_locations_mirroring_deployments_create_builder, networksecurity_projects_locations_mirroring_deployments_create_task,
    networksecurity_projects_locations_mirroring_deployments_delete_builder, networksecurity_projects_locations_mirroring_deployments_delete_task,
    networksecurity_projects_locations_mirroring_deployments_get_builder, networksecurity_projects_locations_mirroring_deployments_get_task,
    networksecurity_projects_locations_mirroring_deployments_list_builder, networksecurity_projects_locations_mirroring_deployments_list_task,
    networksecurity_projects_locations_mirroring_deployments_patch_builder, networksecurity_projects_locations_mirroring_deployments_patch_task,
    networksecurity_projects_locations_mirroring_endpoint_group_associations_create_builder, networksecurity_projects_locations_mirroring_endpoint_group_associations_create_task,
    networksecurity_projects_locations_mirroring_endpoint_group_associations_delete_builder, networksecurity_projects_locations_mirroring_endpoint_group_associations_delete_task,
    networksecurity_projects_locations_mirroring_endpoint_group_associations_get_builder, networksecurity_projects_locations_mirroring_endpoint_group_associations_get_task,
    networksecurity_projects_locations_mirroring_endpoint_group_associations_list_builder, networksecurity_projects_locations_mirroring_endpoint_group_associations_list_task,
    networksecurity_projects_locations_mirroring_endpoint_group_associations_patch_builder, networksecurity_projects_locations_mirroring_endpoint_group_associations_patch_task,
    networksecurity_projects_locations_mirroring_endpoint_groups_create_builder, networksecurity_projects_locations_mirroring_endpoint_groups_create_task,
    networksecurity_projects_locations_mirroring_endpoint_groups_delete_builder, networksecurity_projects_locations_mirroring_endpoint_groups_delete_task,
    networksecurity_projects_locations_mirroring_endpoint_groups_get_builder, networksecurity_projects_locations_mirroring_endpoint_groups_get_task,
    networksecurity_projects_locations_mirroring_endpoint_groups_list_builder, networksecurity_projects_locations_mirroring_endpoint_groups_list_task,
    networksecurity_projects_locations_mirroring_endpoint_groups_patch_builder, networksecurity_projects_locations_mirroring_endpoint_groups_patch_task,
    networksecurity_projects_locations_operations_cancel_builder, networksecurity_projects_locations_operations_cancel_task,
    networksecurity_projects_locations_operations_delete_builder, networksecurity_projects_locations_operations_delete_task,
    networksecurity_projects_locations_operations_get_builder, networksecurity_projects_locations_operations_get_task,
    networksecurity_projects_locations_operations_list_builder, networksecurity_projects_locations_operations_list_task,
    networksecurity_projects_locations_server_tls_policies_create_builder, networksecurity_projects_locations_server_tls_policies_create_task,
    networksecurity_projects_locations_server_tls_policies_delete_builder, networksecurity_projects_locations_server_tls_policies_delete_task,
    networksecurity_projects_locations_server_tls_policies_get_builder, networksecurity_projects_locations_server_tls_policies_get_task,
    networksecurity_projects_locations_server_tls_policies_get_iam_policy_builder, networksecurity_projects_locations_server_tls_policies_get_iam_policy_task,
    networksecurity_projects_locations_server_tls_policies_list_builder, networksecurity_projects_locations_server_tls_policies_list_task,
    networksecurity_projects_locations_server_tls_policies_patch_builder, networksecurity_projects_locations_server_tls_policies_patch_task,
    networksecurity_projects_locations_server_tls_policies_set_iam_policy_builder, networksecurity_projects_locations_server_tls_policies_set_iam_policy_task,
    networksecurity_projects_locations_server_tls_policies_test_iam_permissions_builder, networksecurity_projects_locations_server_tls_policies_test_iam_permissions_task,
    networksecurity_projects_locations_tls_inspection_policies_create_builder, networksecurity_projects_locations_tls_inspection_policies_create_task,
    networksecurity_projects_locations_tls_inspection_policies_delete_builder, networksecurity_projects_locations_tls_inspection_policies_delete_task,
    networksecurity_projects_locations_tls_inspection_policies_get_builder, networksecurity_projects_locations_tls_inspection_policies_get_task,
    networksecurity_projects_locations_tls_inspection_policies_list_builder, networksecurity_projects_locations_tls_inspection_policies_list_task,
    networksecurity_projects_locations_tls_inspection_policies_patch_builder, networksecurity_projects_locations_tls_inspection_policies_patch_task,
    networksecurity_projects_locations_url_lists_create_builder, networksecurity_projects_locations_url_lists_create_task,
    networksecurity_projects_locations_url_lists_delete_builder, networksecurity_projects_locations_url_lists_delete_task,
    networksecurity_projects_locations_url_lists_get_builder, networksecurity_projects_locations_url_lists_get_task,
    networksecurity_projects_locations_url_lists_list_builder, networksecurity_projects_locations_url_lists_list_task,
    networksecurity_projects_locations_url_lists_patch_builder, networksecurity_projects_locations_url_lists_patch_task,
};
use crate::providers::gcp::clients::types::{ApiError, ApiPending};
use crate::providers::gcp::clients::networksecurity::AddressGroup;
use crate::providers::gcp::clients::networksecurity::AuthorizationPolicy;
use crate::providers::gcp::clients::networksecurity::AuthzPolicy;
use crate::providers::gcp::clients::networksecurity::BackendAuthenticationConfig;
use crate::providers::gcp::clients::networksecurity::ClientTlsPolicy;
use crate::providers::gcp::clients::networksecurity::DnsThreatDetector;
use crate::providers::gcp::clients::networksecurity::Empty;
use crate::providers::gcp::clients::networksecurity::FirewallEndpoint;
use crate::providers::gcp::clients::networksecurity::FirewallEndpointAssociation;
use crate::providers::gcp::clients::networksecurity::GatewaySecurityPolicy;
use crate::providers::gcp::clients::networksecurity::GatewaySecurityPolicyRule;
use crate::providers::gcp::clients::networksecurity::GoogleIamV1Policy;
use crate::providers::gcp::clients::networksecurity::GoogleIamV1TestIamPermissionsResponse;
use crate::providers::gcp::clients::networksecurity::InterceptDeployment;
use crate::providers::gcp::clients::networksecurity::InterceptDeploymentGroup;
use crate::providers::gcp::clients::networksecurity::InterceptEndpointGroup;
use crate::providers::gcp::clients::networksecurity::InterceptEndpointGroupAssociation;
use crate::providers::gcp::clients::networksecurity::ListAddressGroupReferencesResponse;
use crate::providers::gcp::clients::networksecurity::ListAddressGroupsResponse;
use crate::providers::gcp::clients::networksecurity::ListAuthorizationPoliciesResponse;
use crate::providers::gcp::clients::networksecurity::ListAuthzPoliciesResponse;
use crate::providers::gcp::clients::networksecurity::ListBackendAuthenticationConfigsResponse;
use crate::providers::gcp::clients::networksecurity::ListClientTlsPoliciesResponse;
use crate::providers::gcp::clients::networksecurity::ListDnsThreatDetectorsResponse;
use crate::providers::gcp::clients::networksecurity::ListFirewallEndpointAssociationsResponse;
use crate::providers::gcp::clients::networksecurity::ListFirewallEndpointsResponse;
use crate::providers::gcp::clients::networksecurity::ListGatewaySecurityPoliciesResponse;
use crate::providers::gcp::clients::networksecurity::ListGatewaySecurityPolicyRulesResponse;
use crate::providers::gcp::clients::networksecurity::ListInterceptDeploymentGroupsResponse;
use crate::providers::gcp::clients::networksecurity::ListInterceptDeploymentsResponse;
use crate::providers::gcp::clients::networksecurity::ListInterceptEndpointGroupAssociationsResponse;
use crate::providers::gcp::clients::networksecurity::ListInterceptEndpointGroupsResponse;
use crate::providers::gcp::clients::networksecurity::ListLocationsResponse;
use crate::providers::gcp::clients::networksecurity::ListMirroringDeploymentGroupsResponse;
use crate::providers::gcp::clients::networksecurity::ListMirroringDeploymentsResponse;
use crate::providers::gcp::clients::networksecurity::ListMirroringEndpointGroupAssociationsResponse;
use crate::providers::gcp::clients::networksecurity::ListMirroringEndpointGroupsResponse;
use crate::providers::gcp::clients::networksecurity::ListOperationsResponse;
use crate::providers::gcp::clients::networksecurity::ListSecurityProfileGroupsResponse;
use crate::providers::gcp::clients::networksecurity::ListSecurityProfilesResponse;
use crate::providers::gcp::clients::networksecurity::ListServerTlsPoliciesResponse;
use crate::providers::gcp::clients::networksecurity::ListTlsInspectionPoliciesResponse;
use crate::providers::gcp::clients::networksecurity::ListUrlListsResponse;
use crate::providers::gcp::clients::networksecurity::Location;
use crate::providers::gcp::clients::networksecurity::MirroringDeployment;
use crate::providers::gcp::clients::networksecurity::MirroringDeploymentGroup;
use crate::providers::gcp::clients::networksecurity::MirroringEndpointGroup;
use crate::providers::gcp::clients::networksecurity::MirroringEndpointGroupAssociation;
use crate::providers::gcp::clients::networksecurity::Operation;
use crate::providers::gcp::clients::networksecurity::SecurityProfile;
use crate::providers::gcp::clients::networksecurity::SecurityProfileGroup;
use crate::providers::gcp::clients::networksecurity::ServerTlsPolicy;
use crate::providers::gcp::clients::networksecurity::TlsInspectionPolicy;
use crate::providers::gcp::clients::networksecurity::UrlList;
use crate::providers::gcp::clients::networksecurity::NetworksecurityOrganizationsLocationsAddressGroupsAddItemsArgs;
use crate::providers::gcp::clients::networksecurity::NetworksecurityOrganizationsLocationsAddressGroupsCloneItemsArgs;
use crate::providers::gcp::clients::networksecurity::NetworksecurityOrganizationsLocationsAddressGroupsCreateArgs;
use crate::providers::gcp::clients::networksecurity::NetworksecurityOrganizationsLocationsAddressGroupsDeleteArgs;
use crate::providers::gcp::clients::networksecurity::NetworksecurityOrganizationsLocationsAddressGroupsGetArgs;
use crate::providers::gcp::clients::networksecurity::NetworksecurityOrganizationsLocationsAddressGroupsListArgs;
use crate::providers::gcp::clients::networksecurity::NetworksecurityOrganizationsLocationsAddressGroupsListReferencesArgs;
use crate::providers::gcp::clients::networksecurity::NetworksecurityOrganizationsLocationsAddressGroupsPatchArgs;
use crate::providers::gcp::clients::networksecurity::NetworksecurityOrganizationsLocationsAddressGroupsRemoveItemsArgs;
use crate::providers::gcp::clients::networksecurity::NetworksecurityOrganizationsLocationsFirewallEndpointsCreateArgs;
use crate::providers::gcp::clients::networksecurity::NetworksecurityOrganizationsLocationsFirewallEndpointsDeleteArgs;
use crate::providers::gcp::clients::networksecurity::NetworksecurityOrganizationsLocationsFirewallEndpointsGetArgs;
use crate::providers::gcp::clients::networksecurity::NetworksecurityOrganizationsLocationsFirewallEndpointsListArgs;
use crate::providers::gcp::clients::networksecurity::NetworksecurityOrganizationsLocationsFirewallEndpointsPatchArgs;
use crate::providers::gcp::clients::networksecurity::NetworksecurityOrganizationsLocationsOperationsCancelArgs;
use crate::providers::gcp::clients::networksecurity::NetworksecurityOrganizationsLocationsOperationsDeleteArgs;
use crate::providers::gcp::clients::networksecurity::NetworksecurityOrganizationsLocationsOperationsGetArgs;
use crate::providers::gcp::clients::networksecurity::NetworksecurityOrganizationsLocationsOperationsListArgs;
use crate::providers::gcp::clients::networksecurity::NetworksecurityOrganizationsLocationsSecurityProfileGroupsCreateArgs;
use crate::providers::gcp::clients::networksecurity::NetworksecurityOrganizationsLocationsSecurityProfileGroupsDeleteArgs;
use crate::providers::gcp::clients::networksecurity::NetworksecurityOrganizationsLocationsSecurityProfileGroupsGetArgs;
use crate::providers::gcp::clients::networksecurity::NetworksecurityOrganizationsLocationsSecurityProfileGroupsListArgs;
use crate::providers::gcp::clients::networksecurity::NetworksecurityOrganizationsLocationsSecurityProfileGroupsPatchArgs;
use crate::providers::gcp::clients::networksecurity::NetworksecurityOrganizationsLocationsSecurityProfilesCreateArgs;
use crate::providers::gcp::clients::networksecurity::NetworksecurityOrganizationsLocationsSecurityProfilesDeleteArgs;
use crate::providers::gcp::clients::networksecurity::NetworksecurityOrganizationsLocationsSecurityProfilesGetArgs;
use crate::providers::gcp::clients::networksecurity::NetworksecurityOrganizationsLocationsSecurityProfilesListArgs;
use crate::providers::gcp::clients::networksecurity::NetworksecurityOrganizationsLocationsSecurityProfilesPatchArgs;
use crate::providers::gcp::clients::networksecurity::NetworksecurityProjectsLocationsAddressGroupsAddItemsArgs;
use crate::providers::gcp::clients::networksecurity::NetworksecurityProjectsLocationsAddressGroupsCloneItemsArgs;
use crate::providers::gcp::clients::networksecurity::NetworksecurityProjectsLocationsAddressGroupsCreateArgs;
use crate::providers::gcp::clients::networksecurity::NetworksecurityProjectsLocationsAddressGroupsDeleteArgs;
use crate::providers::gcp::clients::networksecurity::NetworksecurityProjectsLocationsAddressGroupsGetArgs;
use crate::providers::gcp::clients::networksecurity::NetworksecurityProjectsLocationsAddressGroupsGetIamPolicyArgs;
use crate::providers::gcp::clients::networksecurity::NetworksecurityProjectsLocationsAddressGroupsListArgs;
use crate::providers::gcp::clients::networksecurity::NetworksecurityProjectsLocationsAddressGroupsListReferencesArgs;
use crate::providers::gcp::clients::networksecurity::NetworksecurityProjectsLocationsAddressGroupsPatchArgs;
use crate::providers::gcp::clients::networksecurity::NetworksecurityProjectsLocationsAddressGroupsRemoveItemsArgs;
use crate::providers::gcp::clients::networksecurity::NetworksecurityProjectsLocationsAddressGroupsSetIamPolicyArgs;
use crate::providers::gcp::clients::networksecurity::NetworksecurityProjectsLocationsAddressGroupsTestIamPermissionsArgs;
use crate::providers::gcp::clients::networksecurity::NetworksecurityProjectsLocationsAuthorizationPoliciesCreateArgs;
use crate::providers::gcp::clients::networksecurity::NetworksecurityProjectsLocationsAuthorizationPoliciesDeleteArgs;
use crate::providers::gcp::clients::networksecurity::NetworksecurityProjectsLocationsAuthorizationPoliciesGetArgs;
use crate::providers::gcp::clients::networksecurity::NetworksecurityProjectsLocationsAuthorizationPoliciesGetIamPolicyArgs;
use crate::providers::gcp::clients::networksecurity::NetworksecurityProjectsLocationsAuthorizationPoliciesListArgs;
use crate::providers::gcp::clients::networksecurity::NetworksecurityProjectsLocationsAuthorizationPoliciesPatchArgs;
use crate::providers::gcp::clients::networksecurity::NetworksecurityProjectsLocationsAuthorizationPoliciesSetIamPolicyArgs;
use crate::providers::gcp::clients::networksecurity::NetworksecurityProjectsLocationsAuthorizationPoliciesTestIamPermissionsArgs;
use crate::providers::gcp::clients::networksecurity::NetworksecurityProjectsLocationsAuthzPoliciesCreateArgs;
use crate::providers::gcp::clients::networksecurity::NetworksecurityProjectsLocationsAuthzPoliciesDeleteArgs;
use crate::providers::gcp::clients::networksecurity::NetworksecurityProjectsLocationsAuthzPoliciesGetArgs;
use crate::providers::gcp::clients::networksecurity::NetworksecurityProjectsLocationsAuthzPoliciesGetIamPolicyArgs;
use crate::providers::gcp::clients::networksecurity::NetworksecurityProjectsLocationsAuthzPoliciesListArgs;
use crate::providers::gcp::clients::networksecurity::NetworksecurityProjectsLocationsAuthzPoliciesPatchArgs;
use crate::providers::gcp::clients::networksecurity::NetworksecurityProjectsLocationsAuthzPoliciesSetIamPolicyArgs;
use crate::providers::gcp::clients::networksecurity::NetworksecurityProjectsLocationsAuthzPoliciesTestIamPermissionsArgs;
use crate::providers::gcp::clients::networksecurity::NetworksecurityProjectsLocationsBackendAuthenticationConfigsCreateArgs;
use crate::providers::gcp::clients::networksecurity::NetworksecurityProjectsLocationsBackendAuthenticationConfigsDeleteArgs;
use crate::providers::gcp::clients::networksecurity::NetworksecurityProjectsLocationsBackendAuthenticationConfigsGetArgs;
use crate::providers::gcp::clients::networksecurity::NetworksecurityProjectsLocationsBackendAuthenticationConfigsListArgs;
use crate::providers::gcp::clients::networksecurity::NetworksecurityProjectsLocationsBackendAuthenticationConfigsPatchArgs;
use crate::providers::gcp::clients::networksecurity::NetworksecurityProjectsLocationsClientTlsPoliciesCreateArgs;
use crate::providers::gcp::clients::networksecurity::NetworksecurityProjectsLocationsClientTlsPoliciesDeleteArgs;
use crate::providers::gcp::clients::networksecurity::NetworksecurityProjectsLocationsClientTlsPoliciesGetArgs;
use crate::providers::gcp::clients::networksecurity::NetworksecurityProjectsLocationsClientTlsPoliciesGetIamPolicyArgs;
use crate::providers::gcp::clients::networksecurity::NetworksecurityProjectsLocationsClientTlsPoliciesListArgs;
use crate::providers::gcp::clients::networksecurity::NetworksecurityProjectsLocationsClientTlsPoliciesPatchArgs;
use crate::providers::gcp::clients::networksecurity::NetworksecurityProjectsLocationsClientTlsPoliciesSetIamPolicyArgs;
use crate::providers::gcp::clients::networksecurity::NetworksecurityProjectsLocationsClientTlsPoliciesTestIamPermissionsArgs;
use crate::providers::gcp::clients::networksecurity::NetworksecurityProjectsLocationsDnsThreatDetectorsCreateArgs;
use crate::providers::gcp::clients::networksecurity::NetworksecurityProjectsLocationsDnsThreatDetectorsDeleteArgs;
use crate::providers::gcp::clients::networksecurity::NetworksecurityProjectsLocationsDnsThreatDetectorsGetArgs;
use crate::providers::gcp::clients::networksecurity::NetworksecurityProjectsLocationsDnsThreatDetectorsListArgs;
use crate::providers::gcp::clients::networksecurity::NetworksecurityProjectsLocationsDnsThreatDetectorsPatchArgs;
use crate::providers::gcp::clients::networksecurity::NetworksecurityProjectsLocationsFirewallEndpointAssociationsCreateArgs;
use crate::providers::gcp::clients::networksecurity::NetworksecurityProjectsLocationsFirewallEndpointAssociationsDeleteArgs;
use crate::providers::gcp::clients::networksecurity::NetworksecurityProjectsLocationsFirewallEndpointAssociationsGetArgs;
use crate::providers::gcp::clients::networksecurity::NetworksecurityProjectsLocationsFirewallEndpointAssociationsListArgs;
use crate::providers::gcp::clients::networksecurity::NetworksecurityProjectsLocationsFirewallEndpointAssociationsPatchArgs;
use crate::providers::gcp::clients::networksecurity::NetworksecurityProjectsLocationsGatewaySecurityPoliciesCreateArgs;
use crate::providers::gcp::clients::networksecurity::NetworksecurityProjectsLocationsGatewaySecurityPoliciesDeleteArgs;
use crate::providers::gcp::clients::networksecurity::NetworksecurityProjectsLocationsGatewaySecurityPoliciesGetArgs;
use crate::providers::gcp::clients::networksecurity::NetworksecurityProjectsLocationsGatewaySecurityPoliciesListArgs;
use crate::providers::gcp::clients::networksecurity::NetworksecurityProjectsLocationsGatewaySecurityPoliciesPatchArgs;
use crate::providers::gcp::clients::networksecurity::NetworksecurityProjectsLocationsGatewaySecurityPoliciesRulesCreateArgs;
use crate::providers::gcp::clients::networksecurity::NetworksecurityProjectsLocationsGatewaySecurityPoliciesRulesDeleteArgs;
use crate::providers::gcp::clients::networksecurity::NetworksecurityProjectsLocationsGatewaySecurityPoliciesRulesGetArgs;
use crate::providers::gcp::clients::networksecurity::NetworksecurityProjectsLocationsGatewaySecurityPoliciesRulesListArgs;
use crate::providers::gcp::clients::networksecurity::NetworksecurityProjectsLocationsGatewaySecurityPoliciesRulesPatchArgs;
use crate::providers::gcp::clients::networksecurity::NetworksecurityProjectsLocationsGetArgs;
use crate::providers::gcp::clients::networksecurity::NetworksecurityProjectsLocationsInterceptDeploymentGroupsCreateArgs;
use crate::providers::gcp::clients::networksecurity::NetworksecurityProjectsLocationsInterceptDeploymentGroupsDeleteArgs;
use crate::providers::gcp::clients::networksecurity::NetworksecurityProjectsLocationsInterceptDeploymentGroupsGetArgs;
use crate::providers::gcp::clients::networksecurity::NetworksecurityProjectsLocationsInterceptDeploymentGroupsListArgs;
use crate::providers::gcp::clients::networksecurity::NetworksecurityProjectsLocationsInterceptDeploymentGroupsPatchArgs;
use crate::providers::gcp::clients::networksecurity::NetworksecurityProjectsLocationsInterceptDeploymentsCreateArgs;
use crate::providers::gcp::clients::networksecurity::NetworksecurityProjectsLocationsInterceptDeploymentsDeleteArgs;
use crate::providers::gcp::clients::networksecurity::NetworksecurityProjectsLocationsInterceptDeploymentsGetArgs;
use crate::providers::gcp::clients::networksecurity::NetworksecurityProjectsLocationsInterceptDeploymentsListArgs;
use crate::providers::gcp::clients::networksecurity::NetworksecurityProjectsLocationsInterceptDeploymentsPatchArgs;
use crate::providers::gcp::clients::networksecurity::NetworksecurityProjectsLocationsInterceptEndpointGroupAssociationsCreateArgs;
use crate::providers::gcp::clients::networksecurity::NetworksecurityProjectsLocationsInterceptEndpointGroupAssociationsDeleteArgs;
use crate::providers::gcp::clients::networksecurity::NetworksecurityProjectsLocationsInterceptEndpointGroupAssociationsGetArgs;
use crate::providers::gcp::clients::networksecurity::NetworksecurityProjectsLocationsInterceptEndpointGroupAssociationsListArgs;
use crate::providers::gcp::clients::networksecurity::NetworksecurityProjectsLocationsInterceptEndpointGroupAssociationsPatchArgs;
use crate::providers::gcp::clients::networksecurity::NetworksecurityProjectsLocationsInterceptEndpointGroupsCreateArgs;
use crate::providers::gcp::clients::networksecurity::NetworksecurityProjectsLocationsInterceptEndpointGroupsDeleteArgs;
use crate::providers::gcp::clients::networksecurity::NetworksecurityProjectsLocationsInterceptEndpointGroupsGetArgs;
use crate::providers::gcp::clients::networksecurity::NetworksecurityProjectsLocationsInterceptEndpointGroupsListArgs;
use crate::providers::gcp::clients::networksecurity::NetworksecurityProjectsLocationsInterceptEndpointGroupsPatchArgs;
use crate::providers::gcp::clients::networksecurity::NetworksecurityProjectsLocationsListArgs;
use crate::providers::gcp::clients::networksecurity::NetworksecurityProjectsLocationsMirroringDeploymentGroupsCreateArgs;
use crate::providers::gcp::clients::networksecurity::NetworksecurityProjectsLocationsMirroringDeploymentGroupsDeleteArgs;
use crate::providers::gcp::clients::networksecurity::NetworksecurityProjectsLocationsMirroringDeploymentGroupsGetArgs;
use crate::providers::gcp::clients::networksecurity::NetworksecurityProjectsLocationsMirroringDeploymentGroupsListArgs;
use crate::providers::gcp::clients::networksecurity::NetworksecurityProjectsLocationsMirroringDeploymentGroupsPatchArgs;
use crate::providers::gcp::clients::networksecurity::NetworksecurityProjectsLocationsMirroringDeploymentsCreateArgs;
use crate::providers::gcp::clients::networksecurity::NetworksecurityProjectsLocationsMirroringDeploymentsDeleteArgs;
use crate::providers::gcp::clients::networksecurity::NetworksecurityProjectsLocationsMirroringDeploymentsGetArgs;
use crate::providers::gcp::clients::networksecurity::NetworksecurityProjectsLocationsMirroringDeploymentsListArgs;
use crate::providers::gcp::clients::networksecurity::NetworksecurityProjectsLocationsMirroringDeploymentsPatchArgs;
use crate::providers::gcp::clients::networksecurity::NetworksecurityProjectsLocationsMirroringEndpointGroupAssociationsCreateArgs;
use crate::providers::gcp::clients::networksecurity::NetworksecurityProjectsLocationsMirroringEndpointGroupAssociationsDeleteArgs;
use crate::providers::gcp::clients::networksecurity::NetworksecurityProjectsLocationsMirroringEndpointGroupAssociationsGetArgs;
use crate::providers::gcp::clients::networksecurity::NetworksecurityProjectsLocationsMirroringEndpointGroupAssociationsListArgs;
use crate::providers::gcp::clients::networksecurity::NetworksecurityProjectsLocationsMirroringEndpointGroupAssociationsPatchArgs;
use crate::providers::gcp::clients::networksecurity::NetworksecurityProjectsLocationsMirroringEndpointGroupsCreateArgs;
use crate::providers::gcp::clients::networksecurity::NetworksecurityProjectsLocationsMirroringEndpointGroupsDeleteArgs;
use crate::providers::gcp::clients::networksecurity::NetworksecurityProjectsLocationsMirroringEndpointGroupsGetArgs;
use crate::providers::gcp::clients::networksecurity::NetworksecurityProjectsLocationsMirroringEndpointGroupsListArgs;
use crate::providers::gcp::clients::networksecurity::NetworksecurityProjectsLocationsMirroringEndpointGroupsPatchArgs;
use crate::providers::gcp::clients::networksecurity::NetworksecurityProjectsLocationsOperationsCancelArgs;
use crate::providers::gcp::clients::networksecurity::NetworksecurityProjectsLocationsOperationsDeleteArgs;
use crate::providers::gcp::clients::networksecurity::NetworksecurityProjectsLocationsOperationsGetArgs;
use crate::providers::gcp::clients::networksecurity::NetworksecurityProjectsLocationsOperationsListArgs;
use crate::providers::gcp::clients::networksecurity::NetworksecurityProjectsLocationsServerTlsPoliciesCreateArgs;
use crate::providers::gcp::clients::networksecurity::NetworksecurityProjectsLocationsServerTlsPoliciesDeleteArgs;
use crate::providers::gcp::clients::networksecurity::NetworksecurityProjectsLocationsServerTlsPoliciesGetArgs;
use crate::providers::gcp::clients::networksecurity::NetworksecurityProjectsLocationsServerTlsPoliciesGetIamPolicyArgs;
use crate::providers::gcp::clients::networksecurity::NetworksecurityProjectsLocationsServerTlsPoliciesListArgs;
use crate::providers::gcp::clients::networksecurity::NetworksecurityProjectsLocationsServerTlsPoliciesPatchArgs;
use crate::providers::gcp::clients::networksecurity::NetworksecurityProjectsLocationsServerTlsPoliciesSetIamPolicyArgs;
use crate::providers::gcp::clients::networksecurity::NetworksecurityProjectsLocationsServerTlsPoliciesTestIamPermissionsArgs;
use crate::providers::gcp::clients::networksecurity::NetworksecurityProjectsLocationsTlsInspectionPoliciesCreateArgs;
use crate::providers::gcp::clients::networksecurity::NetworksecurityProjectsLocationsTlsInspectionPoliciesDeleteArgs;
use crate::providers::gcp::clients::networksecurity::NetworksecurityProjectsLocationsTlsInspectionPoliciesGetArgs;
use crate::providers::gcp::clients::networksecurity::NetworksecurityProjectsLocationsTlsInspectionPoliciesListArgs;
use crate::providers::gcp::clients::networksecurity::NetworksecurityProjectsLocationsTlsInspectionPoliciesPatchArgs;
use crate::providers::gcp::clients::networksecurity::NetworksecurityProjectsLocationsUrlListsCreateArgs;
use crate::providers::gcp::clients::networksecurity::NetworksecurityProjectsLocationsUrlListsDeleteArgs;
use crate::providers::gcp::clients::networksecurity::NetworksecurityProjectsLocationsUrlListsGetArgs;
use crate::providers::gcp::clients::networksecurity::NetworksecurityProjectsLocationsUrlListsListArgs;
use crate::providers::gcp::clients::networksecurity::NetworksecurityProjectsLocationsUrlListsPatchArgs;
use crate::provider_client::{ProviderClient, ProviderError};
use foundation_core::valtron::{execute, StreamIterator};
use foundation_core::wire::simple_http::client::SimpleHttpClient;
use foundation_db::state::store_state_task::StoreStateIdentifierTask;
use std::sync::Arc;

/// NetworksecurityProvider with automatic state tracking.
///
/// # Type Parameters
///
/// * `S` - StateStore implementation (FileStateStore, SqliteStateStore, etc.)
///
/// # Example
///
/// ```rust
/// let state_store = FileStateStore::new("/path", "my-project", "dev");
/// let client = ProviderClient::new("my-project", "dev", state_store);
/// let http_client = SimpleHttpClient::new(...);
/// let provider = NetworksecurityProvider::new(client, http_client);
/// ```
#[derive(Clone)]
pub struct NetworksecurityProvider<S>
where
    S: foundation_db::state::traits::StateStore + Send + Sync + 'static,
{
    client: ProviderClient<S>,
    http_client: Arc<SimpleHttpClient>,
}

impl<S> NetworksecurityProvider<S>
where
    S: foundation_db::state::traits::StateStore + Send + Sync + 'static,
{
    /// Create new NetworksecurityProvider.
    pub fn new(client: ProviderClient<S>, http_client: SimpleHttpClient) -> Self {
        Self {
            client,
            http_client: Arc::new(http_client),
        }
    }

    /// Networksecurity organizations locations address groups add items.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn networksecurity_organizations_locations_address_groups_add_items(
        &self,
        args: &NetworksecurityOrganizationsLocationsAddressGroupsAddItemsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = networksecurity_organizations_locations_address_groups_add_items_builder(
            &self.http_client,
            &args.addressGroup,
        )
        .map_err(ProviderError::Api)?;

        let task = networksecurity_organizations_locations_address_groups_add_items_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Networksecurity organizations locations address groups clone items.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn networksecurity_organizations_locations_address_groups_clone_items(
        &self,
        args: &NetworksecurityOrganizationsLocationsAddressGroupsCloneItemsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = networksecurity_organizations_locations_address_groups_clone_items_builder(
            &self.http_client,
            &args.addressGroup,
        )
        .map_err(ProviderError::Api)?;

        let task = networksecurity_organizations_locations_address_groups_clone_items_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Networksecurity organizations locations address groups create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn networksecurity_organizations_locations_address_groups_create(
        &self,
        args: &NetworksecurityOrganizationsLocationsAddressGroupsCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = networksecurity_organizations_locations_address_groups_create_builder(
            &self.http_client,
            &args.parent,
            &args.addressGroupId,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = networksecurity_organizations_locations_address_groups_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Networksecurity organizations locations address groups delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn networksecurity_organizations_locations_address_groups_delete(
        &self,
        args: &NetworksecurityOrganizationsLocationsAddressGroupsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = networksecurity_organizations_locations_address_groups_delete_builder(
            &self.http_client,
            &args.name,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = networksecurity_organizations_locations_address_groups_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Networksecurity organizations locations address groups get.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the AddressGroup result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn networksecurity_organizations_locations_address_groups_get(
        &self,
        args: &NetworksecurityOrganizationsLocationsAddressGroupsGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<AddressGroup, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = networksecurity_organizations_locations_address_groups_get_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = networksecurity_organizations_locations_address_groups_get_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Networksecurity organizations locations address groups list.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ListAddressGroupsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn networksecurity_organizations_locations_address_groups_list(
        &self,
        args: &NetworksecurityOrganizationsLocationsAddressGroupsListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ListAddressGroupsResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = networksecurity_organizations_locations_address_groups_list_builder(
            &self.http_client,
            &args.parent,
            &args.pageSize,
            &args.pageToken,
            &args.returnPartialSuccess,
        )
        .map_err(ProviderError::Api)?;

        let task = networksecurity_organizations_locations_address_groups_list_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Networksecurity organizations locations address groups list references.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ListAddressGroupReferencesResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn networksecurity_organizations_locations_address_groups_list_references(
        &self,
        args: &NetworksecurityOrganizationsLocationsAddressGroupsListReferencesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ListAddressGroupReferencesResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = networksecurity_organizations_locations_address_groups_list_references_builder(
            &self.http_client,
            &args.addressGroup,
            &args.pageSize,
            &args.pageToken,
        )
        .map_err(ProviderError::Api)?;

        let task = networksecurity_organizations_locations_address_groups_list_references_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Networksecurity organizations locations address groups patch.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn networksecurity_organizations_locations_address_groups_patch(
        &self,
        args: &NetworksecurityOrganizationsLocationsAddressGroupsPatchArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = networksecurity_organizations_locations_address_groups_patch_builder(
            &self.http_client,
            &args.name,
            &args.requestId,
            &args.updateMask,
        )
        .map_err(ProviderError::Api)?;

        let task = networksecurity_organizations_locations_address_groups_patch_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Networksecurity organizations locations address groups remove items.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn networksecurity_organizations_locations_address_groups_remove_items(
        &self,
        args: &NetworksecurityOrganizationsLocationsAddressGroupsRemoveItemsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = networksecurity_organizations_locations_address_groups_remove_items_builder(
            &self.http_client,
            &args.addressGroup,
        )
        .map_err(ProviderError::Api)?;

        let task = networksecurity_organizations_locations_address_groups_remove_items_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Networksecurity organizations locations firewall endpoints create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn networksecurity_organizations_locations_firewall_endpoints_create(
        &self,
        args: &NetworksecurityOrganizationsLocationsFirewallEndpointsCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = networksecurity_organizations_locations_firewall_endpoints_create_builder(
            &self.http_client,
            &args.parent,
            &args.firewallEndpointId,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = networksecurity_organizations_locations_firewall_endpoints_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Networksecurity organizations locations firewall endpoints delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn networksecurity_organizations_locations_firewall_endpoints_delete(
        &self,
        args: &NetworksecurityOrganizationsLocationsFirewallEndpointsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = networksecurity_organizations_locations_firewall_endpoints_delete_builder(
            &self.http_client,
            &args.name,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = networksecurity_organizations_locations_firewall_endpoints_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Networksecurity organizations locations firewall endpoints get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the FirewallEndpoint result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn networksecurity_organizations_locations_firewall_endpoints_get(
        &self,
        args: &NetworksecurityOrganizationsLocationsFirewallEndpointsGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<FirewallEndpoint, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = networksecurity_organizations_locations_firewall_endpoints_get_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = networksecurity_organizations_locations_firewall_endpoints_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Networksecurity organizations locations firewall endpoints list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ListFirewallEndpointsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn networksecurity_organizations_locations_firewall_endpoints_list(
        &self,
        args: &NetworksecurityOrganizationsLocationsFirewallEndpointsListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ListFirewallEndpointsResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = networksecurity_organizations_locations_firewall_endpoints_list_builder(
            &self.http_client,
            &args.parent,
            &args.filter,
            &args.orderBy,
            &args.pageSize,
            &args.pageToken,
        )
        .map_err(ProviderError::Api)?;

        let task = networksecurity_organizations_locations_firewall_endpoints_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Networksecurity organizations locations firewall endpoints patch.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn networksecurity_organizations_locations_firewall_endpoints_patch(
        &self,
        args: &NetworksecurityOrganizationsLocationsFirewallEndpointsPatchArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = networksecurity_organizations_locations_firewall_endpoints_patch_builder(
            &self.http_client,
            &args.name,
            &args.requestId,
            &args.updateMask,
        )
        .map_err(ProviderError::Api)?;

        let task = networksecurity_organizations_locations_firewall_endpoints_patch_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Networksecurity organizations locations operations cancel.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Empty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn networksecurity_organizations_locations_operations_cancel(
        &self,
        args: &NetworksecurityOrganizationsLocationsOperationsCancelArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Empty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = networksecurity_organizations_locations_operations_cancel_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = networksecurity_organizations_locations_operations_cancel_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Networksecurity organizations locations operations delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Empty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn networksecurity_organizations_locations_operations_delete(
        &self,
        args: &NetworksecurityOrganizationsLocationsOperationsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Empty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = networksecurity_organizations_locations_operations_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = networksecurity_organizations_locations_operations_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Networksecurity organizations locations operations get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn networksecurity_organizations_locations_operations_get(
        &self,
        args: &NetworksecurityOrganizationsLocationsOperationsGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = networksecurity_organizations_locations_operations_get_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = networksecurity_organizations_locations_operations_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Networksecurity organizations locations operations list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ListOperationsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn networksecurity_organizations_locations_operations_list(
        &self,
        args: &NetworksecurityOrganizationsLocationsOperationsListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ListOperationsResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = networksecurity_organizations_locations_operations_list_builder(
            &self.http_client,
            &args.name,
            &args.filter,
            &args.pageSize,
            &args.pageToken,
            &args.returnPartialSuccess,
        )
        .map_err(ProviderError::Api)?;

        let task = networksecurity_organizations_locations_operations_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Networksecurity organizations locations security profile groups create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn networksecurity_organizations_locations_security_profile_groups_create(
        &self,
        args: &NetworksecurityOrganizationsLocationsSecurityProfileGroupsCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = networksecurity_organizations_locations_security_profile_groups_create_builder(
            &self.http_client,
            &args.parent,
            &args.securityProfileGroupId,
        )
        .map_err(ProviderError::Api)?;

        let task = networksecurity_organizations_locations_security_profile_groups_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Networksecurity organizations locations security profile groups delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn networksecurity_organizations_locations_security_profile_groups_delete(
        &self,
        args: &NetworksecurityOrganizationsLocationsSecurityProfileGroupsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = networksecurity_organizations_locations_security_profile_groups_delete_builder(
            &self.http_client,
            &args.name,
            &args.etag,
        )
        .map_err(ProviderError::Api)?;

        let task = networksecurity_organizations_locations_security_profile_groups_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Networksecurity organizations locations security profile groups get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the SecurityProfileGroup result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn networksecurity_organizations_locations_security_profile_groups_get(
        &self,
        args: &NetworksecurityOrganizationsLocationsSecurityProfileGroupsGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<SecurityProfileGroup, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = networksecurity_organizations_locations_security_profile_groups_get_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = networksecurity_organizations_locations_security_profile_groups_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Networksecurity organizations locations security profile groups list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ListSecurityProfileGroupsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn networksecurity_organizations_locations_security_profile_groups_list(
        &self,
        args: &NetworksecurityOrganizationsLocationsSecurityProfileGroupsListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ListSecurityProfileGroupsResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = networksecurity_organizations_locations_security_profile_groups_list_builder(
            &self.http_client,
            &args.parent,
            &args.pageSize,
            &args.pageToken,
        )
        .map_err(ProviderError::Api)?;

        let task = networksecurity_organizations_locations_security_profile_groups_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Networksecurity organizations locations security profile groups patch.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn networksecurity_organizations_locations_security_profile_groups_patch(
        &self,
        args: &NetworksecurityOrganizationsLocationsSecurityProfileGroupsPatchArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = networksecurity_organizations_locations_security_profile_groups_patch_builder(
            &self.http_client,
            &args.name,
            &args.updateMask,
        )
        .map_err(ProviderError::Api)?;

        let task = networksecurity_organizations_locations_security_profile_groups_patch_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Networksecurity organizations locations security profiles create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn networksecurity_organizations_locations_security_profiles_create(
        &self,
        args: &NetworksecurityOrganizationsLocationsSecurityProfilesCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = networksecurity_organizations_locations_security_profiles_create_builder(
            &self.http_client,
            &args.parent,
            &args.securityProfileId,
        )
        .map_err(ProviderError::Api)?;

        let task = networksecurity_organizations_locations_security_profiles_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Networksecurity organizations locations security profiles delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn networksecurity_organizations_locations_security_profiles_delete(
        &self,
        args: &NetworksecurityOrganizationsLocationsSecurityProfilesDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = networksecurity_organizations_locations_security_profiles_delete_builder(
            &self.http_client,
            &args.name,
            &args.etag,
        )
        .map_err(ProviderError::Api)?;

        let task = networksecurity_organizations_locations_security_profiles_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Networksecurity organizations locations security profiles get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the SecurityProfile result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn networksecurity_organizations_locations_security_profiles_get(
        &self,
        args: &NetworksecurityOrganizationsLocationsSecurityProfilesGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<SecurityProfile, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = networksecurity_organizations_locations_security_profiles_get_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = networksecurity_organizations_locations_security_profiles_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Networksecurity organizations locations security profiles list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ListSecurityProfilesResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn networksecurity_organizations_locations_security_profiles_list(
        &self,
        args: &NetworksecurityOrganizationsLocationsSecurityProfilesListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ListSecurityProfilesResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = networksecurity_organizations_locations_security_profiles_list_builder(
            &self.http_client,
            &args.parent,
            &args.pageSize,
            &args.pageToken,
        )
        .map_err(ProviderError::Api)?;

        let task = networksecurity_organizations_locations_security_profiles_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Networksecurity organizations locations security profiles patch.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn networksecurity_organizations_locations_security_profiles_patch(
        &self,
        args: &NetworksecurityOrganizationsLocationsSecurityProfilesPatchArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = networksecurity_organizations_locations_security_profiles_patch_builder(
            &self.http_client,
            &args.name,
            &args.updateMask,
        )
        .map_err(ProviderError::Api)?;

        let task = networksecurity_organizations_locations_security_profiles_patch_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Networksecurity projects locations get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Location result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn networksecurity_projects_locations_get(
        &self,
        args: &NetworksecurityProjectsLocationsGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Location, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = networksecurity_projects_locations_get_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = networksecurity_projects_locations_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Networksecurity projects locations list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ListLocationsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn networksecurity_projects_locations_list(
        &self,
        args: &NetworksecurityProjectsLocationsListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ListLocationsResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = networksecurity_projects_locations_list_builder(
            &self.http_client,
            &args.name,
            &args.extraLocationTypes,
            &args.filter,
            &args.pageSize,
            &args.pageToken,
        )
        .map_err(ProviderError::Api)?;

        let task = networksecurity_projects_locations_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Networksecurity projects locations address groups add items.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn networksecurity_projects_locations_address_groups_add_items(
        &self,
        args: &NetworksecurityProjectsLocationsAddressGroupsAddItemsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = networksecurity_projects_locations_address_groups_add_items_builder(
            &self.http_client,
            &args.addressGroup,
        )
        .map_err(ProviderError::Api)?;

        let task = networksecurity_projects_locations_address_groups_add_items_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Networksecurity projects locations address groups clone items.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn networksecurity_projects_locations_address_groups_clone_items(
        &self,
        args: &NetworksecurityProjectsLocationsAddressGroupsCloneItemsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = networksecurity_projects_locations_address_groups_clone_items_builder(
            &self.http_client,
            &args.addressGroup,
        )
        .map_err(ProviderError::Api)?;

        let task = networksecurity_projects_locations_address_groups_clone_items_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Networksecurity projects locations address groups create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn networksecurity_projects_locations_address_groups_create(
        &self,
        args: &NetworksecurityProjectsLocationsAddressGroupsCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = networksecurity_projects_locations_address_groups_create_builder(
            &self.http_client,
            &args.parent,
            &args.addressGroupId,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = networksecurity_projects_locations_address_groups_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Networksecurity projects locations address groups delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn networksecurity_projects_locations_address_groups_delete(
        &self,
        args: &NetworksecurityProjectsLocationsAddressGroupsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = networksecurity_projects_locations_address_groups_delete_builder(
            &self.http_client,
            &args.name,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = networksecurity_projects_locations_address_groups_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Networksecurity projects locations address groups get.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the AddressGroup result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn networksecurity_projects_locations_address_groups_get(
        &self,
        args: &NetworksecurityProjectsLocationsAddressGroupsGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<AddressGroup, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = networksecurity_projects_locations_address_groups_get_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = networksecurity_projects_locations_address_groups_get_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Networksecurity projects locations address groups get iam policy.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleIamV1Policy result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn networksecurity_projects_locations_address_groups_get_iam_policy(
        &self,
        args: &NetworksecurityProjectsLocationsAddressGroupsGetIamPolicyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleIamV1Policy, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = networksecurity_projects_locations_address_groups_get_iam_policy_builder(
            &self.http_client,
            &args.resource,
            &args.options.requestedPolicyVersion,
        )
        .map_err(ProviderError::Api)?;

        let task = networksecurity_projects_locations_address_groups_get_iam_policy_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Networksecurity projects locations address groups list.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ListAddressGroupsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn networksecurity_projects_locations_address_groups_list(
        &self,
        args: &NetworksecurityProjectsLocationsAddressGroupsListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ListAddressGroupsResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = networksecurity_projects_locations_address_groups_list_builder(
            &self.http_client,
            &args.parent,
            &args.pageSize,
            &args.pageToken,
            &args.returnPartialSuccess,
        )
        .map_err(ProviderError::Api)?;

        let task = networksecurity_projects_locations_address_groups_list_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Networksecurity projects locations address groups list references.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ListAddressGroupReferencesResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn networksecurity_projects_locations_address_groups_list_references(
        &self,
        args: &NetworksecurityProjectsLocationsAddressGroupsListReferencesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ListAddressGroupReferencesResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = networksecurity_projects_locations_address_groups_list_references_builder(
            &self.http_client,
            &args.addressGroup,
            &args.pageSize,
            &args.pageToken,
        )
        .map_err(ProviderError::Api)?;

        let task = networksecurity_projects_locations_address_groups_list_references_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Networksecurity projects locations address groups patch.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn networksecurity_projects_locations_address_groups_patch(
        &self,
        args: &NetworksecurityProjectsLocationsAddressGroupsPatchArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = networksecurity_projects_locations_address_groups_patch_builder(
            &self.http_client,
            &args.name,
            &args.requestId,
            &args.updateMask,
        )
        .map_err(ProviderError::Api)?;

        let task = networksecurity_projects_locations_address_groups_patch_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Networksecurity projects locations address groups remove items.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn networksecurity_projects_locations_address_groups_remove_items(
        &self,
        args: &NetworksecurityProjectsLocationsAddressGroupsRemoveItemsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = networksecurity_projects_locations_address_groups_remove_items_builder(
            &self.http_client,
            &args.addressGroup,
        )
        .map_err(ProviderError::Api)?;

        let task = networksecurity_projects_locations_address_groups_remove_items_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Networksecurity projects locations address groups set iam policy.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleIamV1Policy result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn networksecurity_projects_locations_address_groups_set_iam_policy(
        &self,
        args: &NetworksecurityProjectsLocationsAddressGroupsSetIamPolicyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleIamV1Policy, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = networksecurity_projects_locations_address_groups_set_iam_policy_builder(
            &self.http_client,
            &args.resource,
        )
        .map_err(ProviderError::Api)?;

        let task = networksecurity_projects_locations_address_groups_set_iam_policy_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Networksecurity projects locations address groups test iam permissions.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleIamV1TestIamPermissionsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn networksecurity_projects_locations_address_groups_test_iam_permissions(
        &self,
        args: &NetworksecurityProjectsLocationsAddressGroupsTestIamPermissionsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleIamV1TestIamPermissionsResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = networksecurity_projects_locations_address_groups_test_iam_permissions_builder(
            &self.http_client,
            &args.resource,
        )
        .map_err(ProviderError::Api)?;

        let task = networksecurity_projects_locations_address_groups_test_iam_permissions_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Networksecurity projects locations authorization policies create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn networksecurity_projects_locations_authorization_policies_create(
        &self,
        args: &NetworksecurityProjectsLocationsAuthorizationPoliciesCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = networksecurity_projects_locations_authorization_policies_create_builder(
            &self.http_client,
            &args.parent,
            &args.authorizationPolicyId,
        )
        .map_err(ProviderError::Api)?;

        let task = networksecurity_projects_locations_authorization_policies_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Networksecurity projects locations authorization policies delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn networksecurity_projects_locations_authorization_policies_delete(
        &self,
        args: &NetworksecurityProjectsLocationsAuthorizationPoliciesDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = networksecurity_projects_locations_authorization_policies_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = networksecurity_projects_locations_authorization_policies_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Networksecurity projects locations authorization policies get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the AuthorizationPolicy result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn networksecurity_projects_locations_authorization_policies_get(
        &self,
        args: &NetworksecurityProjectsLocationsAuthorizationPoliciesGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<AuthorizationPolicy, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = networksecurity_projects_locations_authorization_policies_get_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = networksecurity_projects_locations_authorization_policies_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Networksecurity projects locations authorization policies get iam policy.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleIamV1Policy result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn networksecurity_projects_locations_authorization_policies_get_iam_policy(
        &self,
        args: &NetworksecurityProjectsLocationsAuthorizationPoliciesGetIamPolicyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleIamV1Policy, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = networksecurity_projects_locations_authorization_policies_get_iam_policy_builder(
            &self.http_client,
            &args.resource,
            &args.options.requestedPolicyVersion,
        )
        .map_err(ProviderError::Api)?;

        let task = networksecurity_projects_locations_authorization_policies_get_iam_policy_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Networksecurity projects locations authorization policies list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ListAuthorizationPoliciesResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn networksecurity_projects_locations_authorization_policies_list(
        &self,
        args: &NetworksecurityProjectsLocationsAuthorizationPoliciesListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ListAuthorizationPoliciesResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = networksecurity_projects_locations_authorization_policies_list_builder(
            &self.http_client,
            &args.parent,
            &args.pageSize,
            &args.pageToken,
        )
        .map_err(ProviderError::Api)?;

        let task = networksecurity_projects_locations_authorization_policies_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Networksecurity projects locations authorization policies patch.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn networksecurity_projects_locations_authorization_policies_patch(
        &self,
        args: &NetworksecurityProjectsLocationsAuthorizationPoliciesPatchArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = networksecurity_projects_locations_authorization_policies_patch_builder(
            &self.http_client,
            &args.name,
            &args.updateMask,
        )
        .map_err(ProviderError::Api)?;

        let task = networksecurity_projects_locations_authorization_policies_patch_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Networksecurity projects locations authorization policies set iam policy.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleIamV1Policy result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn networksecurity_projects_locations_authorization_policies_set_iam_policy(
        &self,
        args: &NetworksecurityProjectsLocationsAuthorizationPoliciesSetIamPolicyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleIamV1Policy, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = networksecurity_projects_locations_authorization_policies_set_iam_policy_builder(
            &self.http_client,
            &args.resource,
        )
        .map_err(ProviderError::Api)?;

        let task = networksecurity_projects_locations_authorization_policies_set_iam_policy_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Networksecurity projects locations authorization policies test iam permissions.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleIamV1TestIamPermissionsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn networksecurity_projects_locations_authorization_policies_test_iam_permissions(
        &self,
        args: &NetworksecurityProjectsLocationsAuthorizationPoliciesTestIamPermissionsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleIamV1TestIamPermissionsResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = networksecurity_projects_locations_authorization_policies_test_iam_permissions_builder(
            &self.http_client,
            &args.resource,
        )
        .map_err(ProviderError::Api)?;

        let task = networksecurity_projects_locations_authorization_policies_test_iam_permissions_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Networksecurity projects locations authz policies create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn networksecurity_projects_locations_authz_policies_create(
        &self,
        args: &NetworksecurityProjectsLocationsAuthzPoliciesCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = networksecurity_projects_locations_authz_policies_create_builder(
            &self.http_client,
            &args.parent,
            &args.authzPolicyId,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = networksecurity_projects_locations_authz_policies_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Networksecurity projects locations authz policies delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn networksecurity_projects_locations_authz_policies_delete(
        &self,
        args: &NetworksecurityProjectsLocationsAuthzPoliciesDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = networksecurity_projects_locations_authz_policies_delete_builder(
            &self.http_client,
            &args.name,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = networksecurity_projects_locations_authz_policies_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Networksecurity projects locations authz policies get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the AuthzPolicy result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn networksecurity_projects_locations_authz_policies_get(
        &self,
        args: &NetworksecurityProjectsLocationsAuthzPoliciesGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<AuthzPolicy, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = networksecurity_projects_locations_authz_policies_get_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = networksecurity_projects_locations_authz_policies_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Networksecurity projects locations authz policies get iam policy.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleIamV1Policy result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn networksecurity_projects_locations_authz_policies_get_iam_policy(
        &self,
        args: &NetworksecurityProjectsLocationsAuthzPoliciesGetIamPolicyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleIamV1Policy, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = networksecurity_projects_locations_authz_policies_get_iam_policy_builder(
            &self.http_client,
            &args.resource,
            &args.options.requestedPolicyVersion,
        )
        .map_err(ProviderError::Api)?;

        let task = networksecurity_projects_locations_authz_policies_get_iam_policy_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Networksecurity projects locations authz policies list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ListAuthzPoliciesResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn networksecurity_projects_locations_authz_policies_list(
        &self,
        args: &NetworksecurityProjectsLocationsAuthzPoliciesListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ListAuthzPoliciesResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = networksecurity_projects_locations_authz_policies_list_builder(
            &self.http_client,
            &args.parent,
            &args.filter,
            &args.orderBy,
            &args.pageSize,
            &args.pageToken,
        )
        .map_err(ProviderError::Api)?;

        let task = networksecurity_projects_locations_authz_policies_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Networksecurity projects locations authz policies patch.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn networksecurity_projects_locations_authz_policies_patch(
        &self,
        args: &NetworksecurityProjectsLocationsAuthzPoliciesPatchArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = networksecurity_projects_locations_authz_policies_patch_builder(
            &self.http_client,
            &args.name,
            &args.requestId,
            &args.updateMask,
        )
        .map_err(ProviderError::Api)?;

        let task = networksecurity_projects_locations_authz_policies_patch_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Networksecurity projects locations authz policies set iam policy.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleIamV1Policy result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn networksecurity_projects_locations_authz_policies_set_iam_policy(
        &self,
        args: &NetworksecurityProjectsLocationsAuthzPoliciesSetIamPolicyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleIamV1Policy, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = networksecurity_projects_locations_authz_policies_set_iam_policy_builder(
            &self.http_client,
            &args.resource,
        )
        .map_err(ProviderError::Api)?;

        let task = networksecurity_projects_locations_authz_policies_set_iam_policy_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Networksecurity projects locations authz policies test iam permissions.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleIamV1TestIamPermissionsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn networksecurity_projects_locations_authz_policies_test_iam_permissions(
        &self,
        args: &NetworksecurityProjectsLocationsAuthzPoliciesTestIamPermissionsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleIamV1TestIamPermissionsResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = networksecurity_projects_locations_authz_policies_test_iam_permissions_builder(
            &self.http_client,
            &args.resource,
        )
        .map_err(ProviderError::Api)?;

        let task = networksecurity_projects_locations_authz_policies_test_iam_permissions_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Networksecurity projects locations backend authentication configs create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn networksecurity_projects_locations_backend_authentication_configs_create(
        &self,
        args: &NetworksecurityProjectsLocationsBackendAuthenticationConfigsCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = networksecurity_projects_locations_backend_authentication_configs_create_builder(
            &self.http_client,
            &args.parent,
            &args.backendAuthenticationConfigId,
        )
        .map_err(ProviderError::Api)?;

        let task = networksecurity_projects_locations_backend_authentication_configs_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Networksecurity projects locations backend authentication configs delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn networksecurity_projects_locations_backend_authentication_configs_delete(
        &self,
        args: &NetworksecurityProjectsLocationsBackendAuthenticationConfigsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = networksecurity_projects_locations_backend_authentication_configs_delete_builder(
            &self.http_client,
            &args.name,
            &args.etag,
        )
        .map_err(ProviderError::Api)?;

        let task = networksecurity_projects_locations_backend_authentication_configs_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Networksecurity projects locations backend authentication configs get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the BackendAuthenticationConfig result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn networksecurity_projects_locations_backend_authentication_configs_get(
        &self,
        args: &NetworksecurityProjectsLocationsBackendAuthenticationConfigsGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<BackendAuthenticationConfig, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = networksecurity_projects_locations_backend_authentication_configs_get_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = networksecurity_projects_locations_backend_authentication_configs_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Networksecurity projects locations backend authentication configs list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ListBackendAuthenticationConfigsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn networksecurity_projects_locations_backend_authentication_configs_list(
        &self,
        args: &NetworksecurityProjectsLocationsBackendAuthenticationConfigsListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ListBackendAuthenticationConfigsResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = networksecurity_projects_locations_backend_authentication_configs_list_builder(
            &self.http_client,
            &args.parent,
            &args.pageSize,
            &args.pageToken,
        )
        .map_err(ProviderError::Api)?;

        let task = networksecurity_projects_locations_backend_authentication_configs_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Networksecurity projects locations backend authentication configs patch.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn networksecurity_projects_locations_backend_authentication_configs_patch(
        &self,
        args: &NetworksecurityProjectsLocationsBackendAuthenticationConfigsPatchArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = networksecurity_projects_locations_backend_authentication_configs_patch_builder(
            &self.http_client,
            &args.name,
            &args.updateMask,
        )
        .map_err(ProviderError::Api)?;

        let task = networksecurity_projects_locations_backend_authentication_configs_patch_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Networksecurity projects locations client tls policies create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn networksecurity_projects_locations_client_tls_policies_create(
        &self,
        args: &NetworksecurityProjectsLocationsClientTlsPoliciesCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = networksecurity_projects_locations_client_tls_policies_create_builder(
            &self.http_client,
            &args.parent,
            &args.clientTlsPolicyId,
        )
        .map_err(ProviderError::Api)?;

        let task = networksecurity_projects_locations_client_tls_policies_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Networksecurity projects locations client tls policies delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn networksecurity_projects_locations_client_tls_policies_delete(
        &self,
        args: &NetworksecurityProjectsLocationsClientTlsPoliciesDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = networksecurity_projects_locations_client_tls_policies_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = networksecurity_projects_locations_client_tls_policies_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Networksecurity projects locations client tls policies get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ClientTlsPolicy result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn networksecurity_projects_locations_client_tls_policies_get(
        &self,
        args: &NetworksecurityProjectsLocationsClientTlsPoliciesGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ClientTlsPolicy, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = networksecurity_projects_locations_client_tls_policies_get_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = networksecurity_projects_locations_client_tls_policies_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Networksecurity projects locations client tls policies get iam policy.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleIamV1Policy result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn networksecurity_projects_locations_client_tls_policies_get_iam_policy(
        &self,
        args: &NetworksecurityProjectsLocationsClientTlsPoliciesGetIamPolicyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleIamV1Policy, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = networksecurity_projects_locations_client_tls_policies_get_iam_policy_builder(
            &self.http_client,
            &args.resource,
            &args.options.requestedPolicyVersion,
        )
        .map_err(ProviderError::Api)?;

        let task = networksecurity_projects_locations_client_tls_policies_get_iam_policy_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Networksecurity projects locations client tls policies list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ListClientTlsPoliciesResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn networksecurity_projects_locations_client_tls_policies_list(
        &self,
        args: &NetworksecurityProjectsLocationsClientTlsPoliciesListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ListClientTlsPoliciesResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = networksecurity_projects_locations_client_tls_policies_list_builder(
            &self.http_client,
            &args.parent,
            &args.pageSize,
            &args.pageToken,
        )
        .map_err(ProviderError::Api)?;

        let task = networksecurity_projects_locations_client_tls_policies_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Networksecurity projects locations client tls policies patch.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn networksecurity_projects_locations_client_tls_policies_patch(
        &self,
        args: &NetworksecurityProjectsLocationsClientTlsPoliciesPatchArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = networksecurity_projects_locations_client_tls_policies_patch_builder(
            &self.http_client,
            &args.name,
            &args.updateMask,
        )
        .map_err(ProviderError::Api)?;

        let task = networksecurity_projects_locations_client_tls_policies_patch_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Networksecurity projects locations client tls policies set iam policy.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleIamV1Policy result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn networksecurity_projects_locations_client_tls_policies_set_iam_policy(
        &self,
        args: &NetworksecurityProjectsLocationsClientTlsPoliciesSetIamPolicyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleIamV1Policy, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = networksecurity_projects_locations_client_tls_policies_set_iam_policy_builder(
            &self.http_client,
            &args.resource,
        )
        .map_err(ProviderError::Api)?;

        let task = networksecurity_projects_locations_client_tls_policies_set_iam_policy_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Networksecurity projects locations client tls policies test iam permissions.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleIamV1TestIamPermissionsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn networksecurity_projects_locations_client_tls_policies_test_iam_permissions(
        &self,
        args: &NetworksecurityProjectsLocationsClientTlsPoliciesTestIamPermissionsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleIamV1TestIamPermissionsResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = networksecurity_projects_locations_client_tls_policies_test_iam_permissions_builder(
            &self.http_client,
            &args.resource,
        )
        .map_err(ProviderError::Api)?;

        let task = networksecurity_projects_locations_client_tls_policies_test_iam_permissions_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Networksecurity projects locations dns threat detectors create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the DnsThreatDetector result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn networksecurity_projects_locations_dns_threat_detectors_create(
        &self,
        args: &NetworksecurityProjectsLocationsDnsThreatDetectorsCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<DnsThreatDetector, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = networksecurity_projects_locations_dns_threat_detectors_create_builder(
            &self.http_client,
            &args.parent,
            &args.dnsThreatDetectorId,
        )
        .map_err(ProviderError::Api)?;

        let task = networksecurity_projects_locations_dns_threat_detectors_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Networksecurity projects locations dns threat detectors delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Empty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn networksecurity_projects_locations_dns_threat_detectors_delete(
        &self,
        args: &NetworksecurityProjectsLocationsDnsThreatDetectorsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Empty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = networksecurity_projects_locations_dns_threat_detectors_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = networksecurity_projects_locations_dns_threat_detectors_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Networksecurity projects locations dns threat detectors get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the DnsThreatDetector result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn networksecurity_projects_locations_dns_threat_detectors_get(
        &self,
        args: &NetworksecurityProjectsLocationsDnsThreatDetectorsGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<DnsThreatDetector, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = networksecurity_projects_locations_dns_threat_detectors_get_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = networksecurity_projects_locations_dns_threat_detectors_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Networksecurity projects locations dns threat detectors list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ListDnsThreatDetectorsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn networksecurity_projects_locations_dns_threat_detectors_list(
        &self,
        args: &NetworksecurityProjectsLocationsDnsThreatDetectorsListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ListDnsThreatDetectorsResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = networksecurity_projects_locations_dns_threat_detectors_list_builder(
            &self.http_client,
            &args.parent,
            &args.pageSize,
            &args.pageToken,
        )
        .map_err(ProviderError::Api)?;

        let task = networksecurity_projects_locations_dns_threat_detectors_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Networksecurity projects locations dns threat detectors patch.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the DnsThreatDetector result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn networksecurity_projects_locations_dns_threat_detectors_patch(
        &self,
        args: &NetworksecurityProjectsLocationsDnsThreatDetectorsPatchArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<DnsThreatDetector, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = networksecurity_projects_locations_dns_threat_detectors_patch_builder(
            &self.http_client,
            &args.name,
            &args.updateMask,
        )
        .map_err(ProviderError::Api)?;

        let task = networksecurity_projects_locations_dns_threat_detectors_patch_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Networksecurity projects locations firewall endpoint associations create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn networksecurity_projects_locations_firewall_endpoint_associations_create(
        &self,
        args: &NetworksecurityProjectsLocationsFirewallEndpointAssociationsCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = networksecurity_projects_locations_firewall_endpoint_associations_create_builder(
            &self.http_client,
            &args.parent,
            &args.firewallEndpointAssociationId,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = networksecurity_projects_locations_firewall_endpoint_associations_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Networksecurity projects locations firewall endpoint associations delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn networksecurity_projects_locations_firewall_endpoint_associations_delete(
        &self,
        args: &NetworksecurityProjectsLocationsFirewallEndpointAssociationsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = networksecurity_projects_locations_firewall_endpoint_associations_delete_builder(
            &self.http_client,
            &args.name,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = networksecurity_projects_locations_firewall_endpoint_associations_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Networksecurity projects locations firewall endpoint associations get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the FirewallEndpointAssociation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn networksecurity_projects_locations_firewall_endpoint_associations_get(
        &self,
        args: &NetworksecurityProjectsLocationsFirewallEndpointAssociationsGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<FirewallEndpointAssociation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = networksecurity_projects_locations_firewall_endpoint_associations_get_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = networksecurity_projects_locations_firewall_endpoint_associations_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Networksecurity projects locations firewall endpoint associations list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ListFirewallEndpointAssociationsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn networksecurity_projects_locations_firewall_endpoint_associations_list(
        &self,
        args: &NetworksecurityProjectsLocationsFirewallEndpointAssociationsListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ListFirewallEndpointAssociationsResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = networksecurity_projects_locations_firewall_endpoint_associations_list_builder(
            &self.http_client,
            &args.parent,
            &args.filter,
            &args.orderBy,
            &args.pageSize,
            &args.pageToken,
        )
        .map_err(ProviderError::Api)?;

        let task = networksecurity_projects_locations_firewall_endpoint_associations_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Networksecurity projects locations firewall endpoint associations patch.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn networksecurity_projects_locations_firewall_endpoint_associations_patch(
        &self,
        args: &NetworksecurityProjectsLocationsFirewallEndpointAssociationsPatchArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = networksecurity_projects_locations_firewall_endpoint_associations_patch_builder(
            &self.http_client,
            &args.name,
            &args.requestId,
            &args.updateMask,
        )
        .map_err(ProviderError::Api)?;

        let task = networksecurity_projects_locations_firewall_endpoint_associations_patch_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Networksecurity projects locations gateway security policies create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn networksecurity_projects_locations_gateway_security_policies_create(
        &self,
        args: &NetworksecurityProjectsLocationsGatewaySecurityPoliciesCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = networksecurity_projects_locations_gateway_security_policies_create_builder(
            &self.http_client,
            &args.parent,
            &args.gatewaySecurityPolicyId,
        )
        .map_err(ProviderError::Api)?;

        let task = networksecurity_projects_locations_gateway_security_policies_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Networksecurity projects locations gateway security policies delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn networksecurity_projects_locations_gateway_security_policies_delete(
        &self,
        args: &NetworksecurityProjectsLocationsGatewaySecurityPoliciesDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = networksecurity_projects_locations_gateway_security_policies_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = networksecurity_projects_locations_gateway_security_policies_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Networksecurity projects locations gateway security policies get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GatewaySecurityPolicy result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn networksecurity_projects_locations_gateway_security_policies_get(
        &self,
        args: &NetworksecurityProjectsLocationsGatewaySecurityPoliciesGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GatewaySecurityPolicy, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = networksecurity_projects_locations_gateway_security_policies_get_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = networksecurity_projects_locations_gateway_security_policies_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Networksecurity projects locations gateway security policies list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ListGatewaySecurityPoliciesResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn networksecurity_projects_locations_gateway_security_policies_list(
        &self,
        args: &NetworksecurityProjectsLocationsGatewaySecurityPoliciesListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ListGatewaySecurityPoliciesResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = networksecurity_projects_locations_gateway_security_policies_list_builder(
            &self.http_client,
            &args.parent,
            &args.pageSize,
            &args.pageToken,
        )
        .map_err(ProviderError::Api)?;

        let task = networksecurity_projects_locations_gateway_security_policies_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Networksecurity projects locations gateway security policies patch.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn networksecurity_projects_locations_gateway_security_policies_patch(
        &self,
        args: &NetworksecurityProjectsLocationsGatewaySecurityPoliciesPatchArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = networksecurity_projects_locations_gateway_security_policies_patch_builder(
            &self.http_client,
            &args.name,
            &args.updateMask,
        )
        .map_err(ProviderError::Api)?;

        let task = networksecurity_projects_locations_gateway_security_policies_patch_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Networksecurity projects locations gateway security policies rules create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn networksecurity_projects_locations_gateway_security_policies_rules_create(
        &self,
        args: &NetworksecurityProjectsLocationsGatewaySecurityPoliciesRulesCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = networksecurity_projects_locations_gateway_security_policies_rules_create_builder(
            &self.http_client,
            &args.parent,
            &args.gatewaySecurityPolicyRuleId,
        )
        .map_err(ProviderError::Api)?;

        let task = networksecurity_projects_locations_gateway_security_policies_rules_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Networksecurity projects locations gateway security policies rules delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn networksecurity_projects_locations_gateway_security_policies_rules_delete(
        &self,
        args: &NetworksecurityProjectsLocationsGatewaySecurityPoliciesRulesDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = networksecurity_projects_locations_gateway_security_policies_rules_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = networksecurity_projects_locations_gateway_security_policies_rules_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Networksecurity projects locations gateway security policies rules get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GatewaySecurityPolicyRule result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn networksecurity_projects_locations_gateway_security_policies_rules_get(
        &self,
        args: &NetworksecurityProjectsLocationsGatewaySecurityPoliciesRulesGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GatewaySecurityPolicyRule, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = networksecurity_projects_locations_gateway_security_policies_rules_get_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = networksecurity_projects_locations_gateway_security_policies_rules_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Networksecurity projects locations gateway security policies rules list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ListGatewaySecurityPolicyRulesResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn networksecurity_projects_locations_gateway_security_policies_rules_list(
        &self,
        args: &NetworksecurityProjectsLocationsGatewaySecurityPoliciesRulesListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ListGatewaySecurityPolicyRulesResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = networksecurity_projects_locations_gateway_security_policies_rules_list_builder(
            &self.http_client,
            &args.parent,
            &args.pageSize,
            &args.pageToken,
        )
        .map_err(ProviderError::Api)?;

        let task = networksecurity_projects_locations_gateway_security_policies_rules_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Networksecurity projects locations gateway security policies rules patch.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn networksecurity_projects_locations_gateway_security_policies_rules_patch(
        &self,
        args: &NetworksecurityProjectsLocationsGatewaySecurityPoliciesRulesPatchArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = networksecurity_projects_locations_gateway_security_policies_rules_patch_builder(
            &self.http_client,
            &args.name,
            &args.updateMask,
        )
        .map_err(ProviderError::Api)?;

        let task = networksecurity_projects_locations_gateway_security_policies_rules_patch_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Networksecurity projects locations intercept deployment groups create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn networksecurity_projects_locations_intercept_deployment_groups_create(
        &self,
        args: &NetworksecurityProjectsLocationsInterceptDeploymentGroupsCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = networksecurity_projects_locations_intercept_deployment_groups_create_builder(
            &self.http_client,
            &args.parent,
            &args.interceptDeploymentGroupId,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = networksecurity_projects_locations_intercept_deployment_groups_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Networksecurity projects locations intercept deployment groups delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn networksecurity_projects_locations_intercept_deployment_groups_delete(
        &self,
        args: &NetworksecurityProjectsLocationsInterceptDeploymentGroupsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = networksecurity_projects_locations_intercept_deployment_groups_delete_builder(
            &self.http_client,
            &args.name,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = networksecurity_projects_locations_intercept_deployment_groups_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Networksecurity projects locations intercept deployment groups get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the InterceptDeploymentGroup result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn networksecurity_projects_locations_intercept_deployment_groups_get(
        &self,
        args: &NetworksecurityProjectsLocationsInterceptDeploymentGroupsGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<InterceptDeploymentGroup, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = networksecurity_projects_locations_intercept_deployment_groups_get_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = networksecurity_projects_locations_intercept_deployment_groups_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Networksecurity projects locations intercept deployment groups list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ListInterceptDeploymentGroupsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn networksecurity_projects_locations_intercept_deployment_groups_list(
        &self,
        args: &NetworksecurityProjectsLocationsInterceptDeploymentGroupsListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ListInterceptDeploymentGroupsResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = networksecurity_projects_locations_intercept_deployment_groups_list_builder(
            &self.http_client,
            &args.parent,
            &args.filter,
            &args.orderBy,
            &args.pageSize,
            &args.pageToken,
        )
        .map_err(ProviderError::Api)?;

        let task = networksecurity_projects_locations_intercept_deployment_groups_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Networksecurity projects locations intercept deployment groups patch.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn networksecurity_projects_locations_intercept_deployment_groups_patch(
        &self,
        args: &NetworksecurityProjectsLocationsInterceptDeploymentGroupsPatchArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = networksecurity_projects_locations_intercept_deployment_groups_patch_builder(
            &self.http_client,
            &args.name,
            &args.requestId,
            &args.updateMask,
        )
        .map_err(ProviderError::Api)?;

        let task = networksecurity_projects_locations_intercept_deployment_groups_patch_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Networksecurity projects locations intercept deployments create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn networksecurity_projects_locations_intercept_deployments_create(
        &self,
        args: &NetworksecurityProjectsLocationsInterceptDeploymentsCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = networksecurity_projects_locations_intercept_deployments_create_builder(
            &self.http_client,
            &args.parent,
            &args.interceptDeploymentId,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = networksecurity_projects_locations_intercept_deployments_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Networksecurity projects locations intercept deployments delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn networksecurity_projects_locations_intercept_deployments_delete(
        &self,
        args: &NetworksecurityProjectsLocationsInterceptDeploymentsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = networksecurity_projects_locations_intercept_deployments_delete_builder(
            &self.http_client,
            &args.name,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = networksecurity_projects_locations_intercept_deployments_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Networksecurity projects locations intercept deployments get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the InterceptDeployment result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn networksecurity_projects_locations_intercept_deployments_get(
        &self,
        args: &NetworksecurityProjectsLocationsInterceptDeploymentsGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<InterceptDeployment, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = networksecurity_projects_locations_intercept_deployments_get_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = networksecurity_projects_locations_intercept_deployments_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Networksecurity projects locations intercept deployments list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ListInterceptDeploymentsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn networksecurity_projects_locations_intercept_deployments_list(
        &self,
        args: &NetworksecurityProjectsLocationsInterceptDeploymentsListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ListInterceptDeploymentsResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = networksecurity_projects_locations_intercept_deployments_list_builder(
            &self.http_client,
            &args.parent,
            &args.filter,
            &args.orderBy,
            &args.pageSize,
            &args.pageToken,
        )
        .map_err(ProviderError::Api)?;

        let task = networksecurity_projects_locations_intercept_deployments_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Networksecurity projects locations intercept deployments patch.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn networksecurity_projects_locations_intercept_deployments_patch(
        &self,
        args: &NetworksecurityProjectsLocationsInterceptDeploymentsPatchArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = networksecurity_projects_locations_intercept_deployments_patch_builder(
            &self.http_client,
            &args.name,
            &args.requestId,
            &args.updateMask,
        )
        .map_err(ProviderError::Api)?;

        let task = networksecurity_projects_locations_intercept_deployments_patch_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Networksecurity projects locations intercept endpoint group associations create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn networksecurity_projects_locations_intercept_endpoint_group_associations_create(
        &self,
        args: &NetworksecurityProjectsLocationsInterceptEndpointGroupAssociationsCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = networksecurity_projects_locations_intercept_endpoint_group_associations_create_builder(
            &self.http_client,
            &args.parent,
            &args.interceptEndpointGroupAssociationId,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = networksecurity_projects_locations_intercept_endpoint_group_associations_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Networksecurity projects locations intercept endpoint group associations delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn networksecurity_projects_locations_intercept_endpoint_group_associations_delete(
        &self,
        args: &NetworksecurityProjectsLocationsInterceptEndpointGroupAssociationsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = networksecurity_projects_locations_intercept_endpoint_group_associations_delete_builder(
            &self.http_client,
            &args.name,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = networksecurity_projects_locations_intercept_endpoint_group_associations_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Networksecurity projects locations intercept endpoint group associations get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the InterceptEndpointGroupAssociation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn networksecurity_projects_locations_intercept_endpoint_group_associations_get(
        &self,
        args: &NetworksecurityProjectsLocationsInterceptEndpointGroupAssociationsGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<InterceptEndpointGroupAssociation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = networksecurity_projects_locations_intercept_endpoint_group_associations_get_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = networksecurity_projects_locations_intercept_endpoint_group_associations_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Networksecurity projects locations intercept endpoint group associations list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ListInterceptEndpointGroupAssociationsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn networksecurity_projects_locations_intercept_endpoint_group_associations_list(
        &self,
        args: &NetworksecurityProjectsLocationsInterceptEndpointGroupAssociationsListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ListInterceptEndpointGroupAssociationsResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = networksecurity_projects_locations_intercept_endpoint_group_associations_list_builder(
            &self.http_client,
            &args.parent,
            &args.filter,
            &args.orderBy,
            &args.pageSize,
            &args.pageToken,
        )
        .map_err(ProviderError::Api)?;

        let task = networksecurity_projects_locations_intercept_endpoint_group_associations_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Networksecurity projects locations intercept endpoint group associations patch.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn networksecurity_projects_locations_intercept_endpoint_group_associations_patch(
        &self,
        args: &NetworksecurityProjectsLocationsInterceptEndpointGroupAssociationsPatchArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = networksecurity_projects_locations_intercept_endpoint_group_associations_patch_builder(
            &self.http_client,
            &args.name,
            &args.requestId,
            &args.updateMask,
        )
        .map_err(ProviderError::Api)?;

        let task = networksecurity_projects_locations_intercept_endpoint_group_associations_patch_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Networksecurity projects locations intercept endpoint groups create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn networksecurity_projects_locations_intercept_endpoint_groups_create(
        &self,
        args: &NetworksecurityProjectsLocationsInterceptEndpointGroupsCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = networksecurity_projects_locations_intercept_endpoint_groups_create_builder(
            &self.http_client,
            &args.parent,
            &args.interceptEndpointGroupId,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = networksecurity_projects_locations_intercept_endpoint_groups_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Networksecurity projects locations intercept endpoint groups delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn networksecurity_projects_locations_intercept_endpoint_groups_delete(
        &self,
        args: &NetworksecurityProjectsLocationsInterceptEndpointGroupsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = networksecurity_projects_locations_intercept_endpoint_groups_delete_builder(
            &self.http_client,
            &args.name,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = networksecurity_projects_locations_intercept_endpoint_groups_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Networksecurity projects locations intercept endpoint groups get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the InterceptEndpointGroup result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn networksecurity_projects_locations_intercept_endpoint_groups_get(
        &self,
        args: &NetworksecurityProjectsLocationsInterceptEndpointGroupsGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<InterceptEndpointGroup, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = networksecurity_projects_locations_intercept_endpoint_groups_get_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = networksecurity_projects_locations_intercept_endpoint_groups_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Networksecurity projects locations intercept endpoint groups list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ListInterceptEndpointGroupsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn networksecurity_projects_locations_intercept_endpoint_groups_list(
        &self,
        args: &NetworksecurityProjectsLocationsInterceptEndpointGroupsListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ListInterceptEndpointGroupsResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = networksecurity_projects_locations_intercept_endpoint_groups_list_builder(
            &self.http_client,
            &args.parent,
            &args.filter,
            &args.orderBy,
            &args.pageSize,
            &args.pageToken,
        )
        .map_err(ProviderError::Api)?;

        let task = networksecurity_projects_locations_intercept_endpoint_groups_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Networksecurity projects locations intercept endpoint groups patch.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn networksecurity_projects_locations_intercept_endpoint_groups_patch(
        &self,
        args: &NetworksecurityProjectsLocationsInterceptEndpointGroupsPatchArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = networksecurity_projects_locations_intercept_endpoint_groups_patch_builder(
            &self.http_client,
            &args.name,
            &args.requestId,
            &args.updateMask,
        )
        .map_err(ProviderError::Api)?;

        let task = networksecurity_projects_locations_intercept_endpoint_groups_patch_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Networksecurity projects locations mirroring deployment groups create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn networksecurity_projects_locations_mirroring_deployment_groups_create(
        &self,
        args: &NetworksecurityProjectsLocationsMirroringDeploymentGroupsCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = networksecurity_projects_locations_mirroring_deployment_groups_create_builder(
            &self.http_client,
            &args.parent,
            &args.mirroringDeploymentGroupId,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = networksecurity_projects_locations_mirroring_deployment_groups_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Networksecurity projects locations mirroring deployment groups delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn networksecurity_projects_locations_mirroring_deployment_groups_delete(
        &self,
        args: &NetworksecurityProjectsLocationsMirroringDeploymentGroupsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = networksecurity_projects_locations_mirroring_deployment_groups_delete_builder(
            &self.http_client,
            &args.name,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = networksecurity_projects_locations_mirroring_deployment_groups_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Networksecurity projects locations mirroring deployment groups get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the MirroringDeploymentGroup result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn networksecurity_projects_locations_mirroring_deployment_groups_get(
        &self,
        args: &NetworksecurityProjectsLocationsMirroringDeploymentGroupsGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<MirroringDeploymentGroup, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = networksecurity_projects_locations_mirroring_deployment_groups_get_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = networksecurity_projects_locations_mirroring_deployment_groups_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Networksecurity projects locations mirroring deployment groups list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ListMirroringDeploymentGroupsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn networksecurity_projects_locations_mirroring_deployment_groups_list(
        &self,
        args: &NetworksecurityProjectsLocationsMirroringDeploymentGroupsListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ListMirroringDeploymentGroupsResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = networksecurity_projects_locations_mirroring_deployment_groups_list_builder(
            &self.http_client,
            &args.parent,
            &args.filter,
            &args.orderBy,
            &args.pageSize,
            &args.pageToken,
        )
        .map_err(ProviderError::Api)?;

        let task = networksecurity_projects_locations_mirroring_deployment_groups_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Networksecurity projects locations mirroring deployment groups patch.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn networksecurity_projects_locations_mirroring_deployment_groups_patch(
        &self,
        args: &NetworksecurityProjectsLocationsMirroringDeploymentGroupsPatchArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = networksecurity_projects_locations_mirroring_deployment_groups_patch_builder(
            &self.http_client,
            &args.name,
            &args.requestId,
            &args.updateMask,
        )
        .map_err(ProviderError::Api)?;

        let task = networksecurity_projects_locations_mirroring_deployment_groups_patch_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Networksecurity projects locations mirroring deployments create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn networksecurity_projects_locations_mirroring_deployments_create(
        &self,
        args: &NetworksecurityProjectsLocationsMirroringDeploymentsCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = networksecurity_projects_locations_mirroring_deployments_create_builder(
            &self.http_client,
            &args.parent,
            &args.mirroringDeploymentId,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = networksecurity_projects_locations_mirroring_deployments_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Networksecurity projects locations mirroring deployments delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn networksecurity_projects_locations_mirroring_deployments_delete(
        &self,
        args: &NetworksecurityProjectsLocationsMirroringDeploymentsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = networksecurity_projects_locations_mirroring_deployments_delete_builder(
            &self.http_client,
            &args.name,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = networksecurity_projects_locations_mirroring_deployments_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Networksecurity projects locations mirroring deployments get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the MirroringDeployment result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn networksecurity_projects_locations_mirroring_deployments_get(
        &self,
        args: &NetworksecurityProjectsLocationsMirroringDeploymentsGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<MirroringDeployment, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = networksecurity_projects_locations_mirroring_deployments_get_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = networksecurity_projects_locations_mirroring_deployments_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Networksecurity projects locations mirroring deployments list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ListMirroringDeploymentsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn networksecurity_projects_locations_mirroring_deployments_list(
        &self,
        args: &NetworksecurityProjectsLocationsMirroringDeploymentsListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ListMirroringDeploymentsResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = networksecurity_projects_locations_mirroring_deployments_list_builder(
            &self.http_client,
            &args.parent,
            &args.filter,
            &args.orderBy,
            &args.pageSize,
            &args.pageToken,
        )
        .map_err(ProviderError::Api)?;

        let task = networksecurity_projects_locations_mirroring_deployments_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Networksecurity projects locations mirroring deployments patch.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn networksecurity_projects_locations_mirroring_deployments_patch(
        &self,
        args: &NetworksecurityProjectsLocationsMirroringDeploymentsPatchArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = networksecurity_projects_locations_mirroring_deployments_patch_builder(
            &self.http_client,
            &args.name,
            &args.requestId,
            &args.updateMask,
        )
        .map_err(ProviderError::Api)?;

        let task = networksecurity_projects_locations_mirroring_deployments_patch_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Networksecurity projects locations mirroring endpoint group associations create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn networksecurity_projects_locations_mirroring_endpoint_group_associations_create(
        &self,
        args: &NetworksecurityProjectsLocationsMirroringEndpointGroupAssociationsCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = networksecurity_projects_locations_mirroring_endpoint_group_associations_create_builder(
            &self.http_client,
            &args.parent,
            &args.mirroringEndpointGroupAssociationId,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = networksecurity_projects_locations_mirroring_endpoint_group_associations_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Networksecurity projects locations mirroring endpoint group associations delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn networksecurity_projects_locations_mirroring_endpoint_group_associations_delete(
        &self,
        args: &NetworksecurityProjectsLocationsMirroringEndpointGroupAssociationsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = networksecurity_projects_locations_mirroring_endpoint_group_associations_delete_builder(
            &self.http_client,
            &args.name,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = networksecurity_projects_locations_mirroring_endpoint_group_associations_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Networksecurity projects locations mirroring endpoint group associations get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the MirroringEndpointGroupAssociation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn networksecurity_projects_locations_mirroring_endpoint_group_associations_get(
        &self,
        args: &NetworksecurityProjectsLocationsMirroringEndpointGroupAssociationsGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<MirroringEndpointGroupAssociation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = networksecurity_projects_locations_mirroring_endpoint_group_associations_get_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = networksecurity_projects_locations_mirroring_endpoint_group_associations_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Networksecurity projects locations mirroring endpoint group associations list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ListMirroringEndpointGroupAssociationsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn networksecurity_projects_locations_mirroring_endpoint_group_associations_list(
        &self,
        args: &NetworksecurityProjectsLocationsMirroringEndpointGroupAssociationsListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ListMirroringEndpointGroupAssociationsResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = networksecurity_projects_locations_mirroring_endpoint_group_associations_list_builder(
            &self.http_client,
            &args.parent,
            &args.filter,
            &args.orderBy,
            &args.pageSize,
            &args.pageToken,
        )
        .map_err(ProviderError::Api)?;

        let task = networksecurity_projects_locations_mirroring_endpoint_group_associations_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Networksecurity projects locations mirroring endpoint group associations patch.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn networksecurity_projects_locations_mirroring_endpoint_group_associations_patch(
        &self,
        args: &NetworksecurityProjectsLocationsMirroringEndpointGroupAssociationsPatchArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = networksecurity_projects_locations_mirroring_endpoint_group_associations_patch_builder(
            &self.http_client,
            &args.name,
            &args.requestId,
            &args.updateMask,
        )
        .map_err(ProviderError::Api)?;

        let task = networksecurity_projects_locations_mirroring_endpoint_group_associations_patch_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Networksecurity projects locations mirroring endpoint groups create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn networksecurity_projects_locations_mirroring_endpoint_groups_create(
        &self,
        args: &NetworksecurityProjectsLocationsMirroringEndpointGroupsCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = networksecurity_projects_locations_mirroring_endpoint_groups_create_builder(
            &self.http_client,
            &args.parent,
            &args.mirroringEndpointGroupId,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = networksecurity_projects_locations_mirroring_endpoint_groups_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Networksecurity projects locations mirroring endpoint groups delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn networksecurity_projects_locations_mirroring_endpoint_groups_delete(
        &self,
        args: &NetworksecurityProjectsLocationsMirroringEndpointGroupsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = networksecurity_projects_locations_mirroring_endpoint_groups_delete_builder(
            &self.http_client,
            &args.name,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = networksecurity_projects_locations_mirroring_endpoint_groups_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Networksecurity projects locations mirroring endpoint groups get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the MirroringEndpointGroup result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn networksecurity_projects_locations_mirroring_endpoint_groups_get(
        &self,
        args: &NetworksecurityProjectsLocationsMirroringEndpointGroupsGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<MirroringEndpointGroup, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = networksecurity_projects_locations_mirroring_endpoint_groups_get_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = networksecurity_projects_locations_mirroring_endpoint_groups_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Networksecurity projects locations mirroring endpoint groups list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ListMirroringEndpointGroupsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn networksecurity_projects_locations_mirroring_endpoint_groups_list(
        &self,
        args: &NetworksecurityProjectsLocationsMirroringEndpointGroupsListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ListMirroringEndpointGroupsResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = networksecurity_projects_locations_mirroring_endpoint_groups_list_builder(
            &self.http_client,
            &args.parent,
            &args.filter,
            &args.orderBy,
            &args.pageSize,
            &args.pageToken,
        )
        .map_err(ProviderError::Api)?;

        let task = networksecurity_projects_locations_mirroring_endpoint_groups_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Networksecurity projects locations mirroring endpoint groups patch.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn networksecurity_projects_locations_mirroring_endpoint_groups_patch(
        &self,
        args: &NetworksecurityProjectsLocationsMirroringEndpointGroupsPatchArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = networksecurity_projects_locations_mirroring_endpoint_groups_patch_builder(
            &self.http_client,
            &args.name,
            &args.requestId,
            &args.updateMask,
        )
        .map_err(ProviderError::Api)?;

        let task = networksecurity_projects_locations_mirroring_endpoint_groups_patch_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Networksecurity projects locations operations cancel.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Empty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn networksecurity_projects_locations_operations_cancel(
        &self,
        args: &NetworksecurityProjectsLocationsOperationsCancelArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Empty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = networksecurity_projects_locations_operations_cancel_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = networksecurity_projects_locations_operations_cancel_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Networksecurity projects locations operations delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Empty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn networksecurity_projects_locations_operations_delete(
        &self,
        args: &NetworksecurityProjectsLocationsOperationsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Empty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = networksecurity_projects_locations_operations_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = networksecurity_projects_locations_operations_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Networksecurity projects locations operations get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn networksecurity_projects_locations_operations_get(
        &self,
        args: &NetworksecurityProjectsLocationsOperationsGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = networksecurity_projects_locations_operations_get_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = networksecurity_projects_locations_operations_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Networksecurity projects locations operations list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ListOperationsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn networksecurity_projects_locations_operations_list(
        &self,
        args: &NetworksecurityProjectsLocationsOperationsListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ListOperationsResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = networksecurity_projects_locations_operations_list_builder(
            &self.http_client,
            &args.name,
            &args.filter,
            &args.pageSize,
            &args.pageToken,
            &args.returnPartialSuccess,
        )
        .map_err(ProviderError::Api)?;

        let task = networksecurity_projects_locations_operations_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Networksecurity projects locations server tls policies create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn networksecurity_projects_locations_server_tls_policies_create(
        &self,
        args: &NetworksecurityProjectsLocationsServerTlsPoliciesCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = networksecurity_projects_locations_server_tls_policies_create_builder(
            &self.http_client,
            &args.parent,
            &args.serverTlsPolicyId,
        )
        .map_err(ProviderError::Api)?;

        let task = networksecurity_projects_locations_server_tls_policies_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Networksecurity projects locations server tls policies delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn networksecurity_projects_locations_server_tls_policies_delete(
        &self,
        args: &NetworksecurityProjectsLocationsServerTlsPoliciesDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = networksecurity_projects_locations_server_tls_policies_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = networksecurity_projects_locations_server_tls_policies_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Networksecurity projects locations server tls policies get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ServerTlsPolicy result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn networksecurity_projects_locations_server_tls_policies_get(
        &self,
        args: &NetworksecurityProjectsLocationsServerTlsPoliciesGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ServerTlsPolicy, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = networksecurity_projects_locations_server_tls_policies_get_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = networksecurity_projects_locations_server_tls_policies_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Networksecurity projects locations server tls policies get iam policy.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleIamV1Policy result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn networksecurity_projects_locations_server_tls_policies_get_iam_policy(
        &self,
        args: &NetworksecurityProjectsLocationsServerTlsPoliciesGetIamPolicyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleIamV1Policy, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = networksecurity_projects_locations_server_tls_policies_get_iam_policy_builder(
            &self.http_client,
            &args.resource,
            &args.options.requestedPolicyVersion,
        )
        .map_err(ProviderError::Api)?;

        let task = networksecurity_projects_locations_server_tls_policies_get_iam_policy_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Networksecurity projects locations server tls policies list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ListServerTlsPoliciesResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn networksecurity_projects_locations_server_tls_policies_list(
        &self,
        args: &NetworksecurityProjectsLocationsServerTlsPoliciesListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ListServerTlsPoliciesResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = networksecurity_projects_locations_server_tls_policies_list_builder(
            &self.http_client,
            &args.parent,
            &args.pageSize,
            &args.pageToken,
            &args.returnPartialSuccess,
        )
        .map_err(ProviderError::Api)?;

        let task = networksecurity_projects_locations_server_tls_policies_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Networksecurity projects locations server tls policies patch.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn networksecurity_projects_locations_server_tls_policies_patch(
        &self,
        args: &NetworksecurityProjectsLocationsServerTlsPoliciesPatchArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = networksecurity_projects_locations_server_tls_policies_patch_builder(
            &self.http_client,
            &args.name,
            &args.updateMask,
        )
        .map_err(ProviderError::Api)?;

        let task = networksecurity_projects_locations_server_tls_policies_patch_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Networksecurity projects locations server tls policies set iam policy.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleIamV1Policy result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn networksecurity_projects_locations_server_tls_policies_set_iam_policy(
        &self,
        args: &NetworksecurityProjectsLocationsServerTlsPoliciesSetIamPolicyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleIamV1Policy, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = networksecurity_projects_locations_server_tls_policies_set_iam_policy_builder(
            &self.http_client,
            &args.resource,
        )
        .map_err(ProviderError::Api)?;

        let task = networksecurity_projects_locations_server_tls_policies_set_iam_policy_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Networksecurity projects locations server tls policies test iam permissions.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleIamV1TestIamPermissionsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn networksecurity_projects_locations_server_tls_policies_test_iam_permissions(
        &self,
        args: &NetworksecurityProjectsLocationsServerTlsPoliciesTestIamPermissionsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleIamV1TestIamPermissionsResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = networksecurity_projects_locations_server_tls_policies_test_iam_permissions_builder(
            &self.http_client,
            &args.resource,
        )
        .map_err(ProviderError::Api)?;

        let task = networksecurity_projects_locations_server_tls_policies_test_iam_permissions_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Networksecurity projects locations tls inspection policies create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn networksecurity_projects_locations_tls_inspection_policies_create(
        &self,
        args: &NetworksecurityProjectsLocationsTlsInspectionPoliciesCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = networksecurity_projects_locations_tls_inspection_policies_create_builder(
            &self.http_client,
            &args.parent,
            &args.tlsInspectionPolicyId,
        )
        .map_err(ProviderError::Api)?;

        let task = networksecurity_projects_locations_tls_inspection_policies_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Networksecurity projects locations tls inspection policies delete.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn networksecurity_projects_locations_tls_inspection_policies_delete(
        &self,
        args: &NetworksecurityProjectsLocationsTlsInspectionPoliciesDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = networksecurity_projects_locations_tls_inspection_policies_delete_builder(
            &self.http_client,
            &args.name,
            &args.force,
        )
        .map_err(ProviderError::Api)?;

        let task = networksecurity_projects_locations_tls_inspection_policies_delete_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Networksecurity projects locations tls inspection policies get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TlsInspectionPolicy result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn networksecurity_projects_locations_tls_inspection_policies_get(
        &self,
        args: &NetworksecurityProjectsLocationsTlsInspectionPoliciesGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TlsInspectionPolicy, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = networksecurity_projects_locations_tls_inspection_policies_get_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = networksecurity_projects_locations_tls_inspection_policies_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Networksecurity projects locations tls inspection policies list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ListTlsInspectionPoliciesResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn networksecurity_projects_locations_tls_inspection_policies_list(
        &self,
        args: &NetworksecurityProjectsLocationsTlsInspectionPoliciesListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ListTlsInspectionPoliciesResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = networksecurity_projects_locations_tls_inspection_policies_list_builder(
            &self.http_client,
            &args.parent,
            &args.pageSize,
            &args.pageToken,
        )
        .map_err(ProviderError::Api)?;

        let task = networksecurity_projects_locations_tls_inspection_policies_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Networksecurity projects locations tls inspection policies patch.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn networksecurity_projects_locations_tls_inspection_policies_patch(
        &self,
        args: &NetworksecurityProjectsLocationsTlsInspectionPoliciesPatchArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = networksecurity_projects_locations_tls_inspection_policies_patch_builder(
            &self.http_client,
            &args.name,
            &args.updateMask,
        )
        .map_err(ProviderError::Api)?;

        let task = networksecurity_projects_locations_tls_inspection_policies_patch_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Networksecurity projects locations url lists create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn networksecurity_projects_locations_url_lists_create(
        &self,
        args: &NetworksecurityProjectsLocationsUrlListsCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = networksecurity_projects_locations_url_lists_create_builder(
            &self.http_client,
            &args.parent,
            &args.urlListId,
        )
        .map_err(ProviderError::Api)?;

        let task = networksecurity_projects_locations_url_lists_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Networksecurity projects locations url lists delete.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn networksecurity_projects_locations_url_lists_delete(
        &self,
        args: &NetworksecurityProjectsLocationsUrlListsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = networksecurity_projects_locations_url_lists_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = networksecurity_projects_locations_url_lists_delete_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Networksecurity projects locations url lists get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the UrlList result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn networksecurity_projects_locations_url_lists_get(
        &self,
        args: &NetworksecurityProjectsLocationsUrlListsGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<UrlList, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = networksecurity_projects_locations_url_lists_get_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = networksecurity_projects_locations_url_lists_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Networksecurity projects locations url lists list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ListUrlListsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn networksecurity_projects_locations_url_lists_list(
        &self,
        args: &NetworksecurityProjectsLocationsUrlListsListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ListUrlListsResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = networksecurity_projects_locations_url_lists_list_builder(
            &self.http_client,
            &args.parent,
            &args.pageSize,
            &args.pageToken,
        )
        .map_err(ProviderError::Api)?;

        let task = networksecurity_projects_locations_url_lists_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Networksecurity projects locations url lists patch.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn networksecurity_projects_locations_url_lists_patch(
        &self,
        args: &NetworksecurityProjectsLocationsUrlListsPatchArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = networksecurity_projects_locations_url_lists_patch_builder(
            &self.http_client,
            &args.name,
            &args.updateMask,
        )
        .map_err(ProviderError::Api)?;

        let task = networksecurity_projects_locations_url_lists_patch_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

}
