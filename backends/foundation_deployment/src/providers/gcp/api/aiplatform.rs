//! AiplatformProvider - State-aware aiplatform API client.
//!
//! WHY: Users need state-aware API clients that automatically track
//!      resource changes in the state store.
//!
//! WHAT: Provider wrapping ProviderClient<S> with methods for
//!       aiplatform API endpoints that auto-store results.
//!
//! HOW: Each method wraps the task with StoreStateIdentifierTask
//!      for automatic state persistence on success.

#![cfg(feature = "gcp")]

use crate::providers::gcp::clients::aiplatform::{
    aiplatform_batch_prediction_jobs_create_builder, aiplatform_batch_prediction_jobs_create_task,
    aiplatform_custom_jobs_operations_cancel_builder, aiplatform_custom_jobs_operations_cancel_task,
    aiplatform_custom_jobs_operations_delete_builder, aiplatform_custom_jobs_operations_delete_task,
    aiplatform_custom_jobs_operations_wait_builder, aiplatform_custom_jobs_operations_wait_task,
    aiplatform_data_labeling_jobs_operations_cancel_builder, aiplatform_data_labeling_jobs_operations_cancel_task,
    aiplatform_data_labeling_jobs_operations_delete_builder, aiplatform_data_labeling_jobs_operations_delete_task,
    aiplatform_data_labeling_jobs_operations_wait_builder, aiplatform_data_labeling_jobs_operations_wait_task,
    aiplatform_datasets_create_builder, aiplatform_datasets_create_task,
    aiplatform_datasets_delete_builder, aiplatform_datasets_delete_task,
    aiplatform_datasets_patch_builder, aiplatform_datasets_patch_task,
    aiplatform_datasets_annotation_specs_operations_cancel_builder, aiplatform_datasets_annotation_specs_operations_cancel_task,
    aiplatform_datasets_annotation_specs_operations_delete_builder, aiplatform_datasets_annotation_specs_operations_delete_task,
    aiplatform_datasets_annotation_specs_operations_wait_builder, aiplatform_datasets_annotation_specs_operations_wait_task,
    aiplatform_datasets_data_items_annotations_operations_cancel_builder, aiplatform_datasets_data_items_annotations_operations_cancel_task,
    aiplatform_datasets_data_items_annotations_operations_delete_builder, aiplatform_datasets_data_items_annotations_operations_delete_task,
    aiplatform_datasets_data_items_annotations_operations_wait_builder, aiplatform_datasets_data_items_annotations_operations_wait_task,
    aiplatform_datasets_data_items_operations_cancel_builder, aiplatform_datasets_data_items_operations_cancel_task,
    aiplatform_datasets_data_items_operations_delete_builder, aiplatform_datasets_data_items_operations_delete_task,
    aiplatform_datasets_data_items_operations_wait_builder, aiplatform_datasets_data_items_operations_wait_task,
    aiplatform_datasets_dataset_versions_create_builder, aiplatform_datasets_dataset_versions_create_task,
    aiplatform_datasets_dataset_versions_delete_builder, aiplatform_datasets_dataset_versions_delete_task,
    aiplatform_datasets_dataset_versions_patch_builder, aiplatform_datasets_dataset_versions_patch_task,
    aiplatform_datasets_operations_cancel_builder, aiplatform_datasets_operations_cancel_task,
    aiplatform_datasets_operations_delete_builder, aiplatform_datasets_operations_delete_task,
    aiplatform_datasets_operations_wait_builder, aiplatform_datasets_operations_wait_task,
    aiplatform_datasets_saved_queries_operations_cancel_builder, aiplatform_datasets_saved_queries_operations_cancel_task,
    aiplatform_datasets_saved_queries_operations_delete_builder, aiplatform_datasets_saved_queries_operations_delete_task,
    aiplatform_datasets_saved_queries_operations_wait_builder, aiplatform_datasets_saved_queries_operations_wait_task,
    aiplatform_deployment_resource_pools_operations_cancel_builder, aiplatform_deployment_resource_pools_operations_cancel_task,
    aiplatform_deployment_resource_pools_operations_delete_builder, aiplatform_deployment_resource_pools_operations_delete_task,
    aiplatform_deployment_resource_pools_operations_wait_builder, aiplatform_deployment_resource_pools_operations_wait_task,
    aiplatform_endpoints_compute_tokens_builder, aiplatform_endpoints_compute_tokens_task,
    aiplatform_endpoints_count_tokens_builder, aiplatform_endpoints_count_tokens_task,
    aiplatform_endpoints_fetch_predict_operation_builder, aiplatform_endpoints_fetch_predict_operation_task,
    aiplatform_endpoints_generate_content_builder, aiplatform_endpoints_generate_content_task,
    aiplatform_endpoints_predict_builder, aiplatform_endpoints_predict_task,
    aiplatform_endpoints_predict_long_running_builder, aiplatform_endpoints_predict_long_running_task,
    aiplatform_endpoints_stream_generate_content_builder, aiplatform_endpoints_stream_generate_content_task,
    aiplatform_endpoints_chat_completions_builder, aiplatform_endpoints_chat_completions_task,
    aiplatform_endpoints_operations_cancel_builder, aiplatform_endpoints_operations_cancel_task,
    aiplatform_endpoints_operations_delete_builder, aiplatform_endpoints_operations_delete_task,
    aiplatform_endpoints_operations_wait_builder, aiplatform_endpoints_operations_wait_task,
    aiplatform_feature_groups_features_operations_delete_builder, aiplatform_feature_groups_features_operations_delete_task,
    aiplatform_feature_groups_features_operations_wait_builder, aiplatform_feature_groups_features_operations_wait_task,
    aiplatform_feature_groups_operations_delete_builder, aiplatform_feature_groups_operations_delete_task,
    aiplatform_feature_groups_operations_wait_builder, aiplatform_feature_groups_operations_wait_task,
    aiplatform_feature_online_stores_feature_views_operations_delete_builder, aiplatform_feature_online_stores_feature_views_operations_delete_task,
    aiplatform_feature_online_stores_feature_views_operations_wait_builder, aiplatform_feature_online_stores_feature_views_operations_wait_task,
    aiplatform_feature_online_stores_operations_delete_builder, aiplatform_feature_online_stores_operations_delete_task,
    aiplatform_feature_online_stores_operations_wait_builder, aiplatform_feature_online_stores_operations_wait_task,
    aiplatform_featurestores_entity_types_features_operations_cancel_builder, aiplatform_featurestores_entity_types_features_operations_cancel_task,
    aiplatform_featurestores_entity_types_features_operations_delete_builder, aiplatform_featurestores_entity_types_features_operations_delete_task,
    aiplatform_featurestores_entity_types_features_operations_wait_builder, aiplatform_featurestores_entity_types_features_operations_wait_task,
    aiplatform_featurestores_entity_types_operations_cancel_builder, aiplatform_featurestores_entity_types_operations_cancel_task,
    aiplatform_featurestores_entity_types_operations_delete_builder, aiplatform_featurestores_entity_types_operations_delete_task,
    aiplatform_featurestores_entity_types_operations_wait_builder, aiplatform_featurestores_entity_types_operations_wait_task,
    aiplatform_featurestores_operations_cancel_builder, aiplatform_featurestores_operations_cancel_task,
    aiplatform_featurestores_operations_delete_builder, aiplatform_featurestores_operations_delete_task,
    aiplatform_featurestores_operations_wait_builder, aiplatform_featurestores_operations_wait_task,
    aiplatform_hyperparameter_tuning_jobs_operations_cancel_builder, aiplatform_hyperparameter_tuning_jobs_operations_cancel_task,
    aiplatform_hyperparameter_tuning_jobs_operations_delete_builder, aiplatform_hyperparameter_tuning_jobs_operations_delete_task,
    aiplatform_hyperparameter_tuning_jobs_operations_wait_builder, aiplatform_hyperparameter_tuning_jobs_operations_wait_task,
    aiplatform_index_endpoints_operations_cancel_builder, aiplatform_index_endpoints_operations_cancel_task,
    aiplatform_index_endpoints_operations_delete_builder, aiplatform_index_endpoints_operations_delete_task,
    aiplatform_index_endpoints_operations_wait_builder, aiplatform_index_endpoints_operations_wait_task,
    aiplatform_indexes_operations_cancel_builder, aiplatform_indexes_operations_cancel_task,
    aiplatform_indexes_operations_delete_builder, aiplatform_indexes_operations_delete_task,
    aiplatform_indexes_operations_wait_builder, aiplatform_indexes_operations_wait_task,
    aiplatform_media_upload_builder, aiplatform_media_upload_task,
    aiplatform_metadata_stores_artifacts_operations_cancel_builder, aiplatform_metadata_stores_artifacts_operations_cancel_task,
    aiplatform_metadata_stores_artifacts_operations_delete_builder, aiplatform_metadata_stores_artifacts_operations_delete_task,
    aiplatform_metadata_stores_artifacts_operations_wait_builder, aiplatform_metadata_stores_artifacts_operations_wait_task,
    aiplatform_metadata_stores_contexts_operations_cancel_builder, aiplatform_metadata_stores_contexts_operations_cancel_task,
    aiplatform_metadata_stores_contexts_operations_delete_builder, aiplatform_metadata_stores_contexts_operations_delete_task,
    aiplatform_metadata_stores_contexts_operations_wait_builder, aiplatform_metadata_stores_contexts_operations_wait_task,
    aiplatform_metadata_stores_executions_operations_cancel_builder, aiplatform_metadata_stores_executions_operations_cancel_task,
    aiplatform_metadata_stores_executions_operations_delete_builder, aiplatform_metadata_stores_executions_operations_delete_task,
    aiplatform_metadata_stores_executions_operations_wait_builder, aiplatform_metadata_stores_executions_operations_wait_task,
    aiplatform_metadata_stores_operations_cancel_builder, aiplatform_metadata_stores_operations_cancel_task,
    aiplatform_metadata_stores_operations_delete_builder, aiplatform_metadata_stores_operations_delete_task,
    aiplatform_metadata_stores_operations_wait_builder, aiplatform_metadata_stores_operations_wait_task,
    aiplatform_migratable_resources_operations_cancel_builder, aiplatform_migratable_resources_operations_cancel_task,
    aiplatform_migratable_resources_operations_delete_builder, aiplatform_migratable_resources_operations_delete_task,
    aiplatform_migratable_resources_operations_wait_builder, aiplatform_migratable_resources_operations_wait_task,
    aiplatform_model_deployment_monitoring_jobs_operations_cancel_builder, aiplatform_model_deployment_monitoring_jobs_operations_cancel_task,
    aiplatform_model_deployment_monitoring_jobs_operations_delete_builder, aiplatform_model_deployment_monitoring_jobs_operations_delete_task,
    aiplatform_model_deployment_monitoring_jobs_operations_wait_builder, aiplatform_model_deployment_monitoring_jobs_operations_wait_task,
    aiplatform_models_evaluations_operations_cancel_builder, aiplatform_models_evaluations_operations_cancel_task,
    aiplatform_models_evaluations_operations_delete_builder, aiplatform_models_evaluations_operations_delete_task,
    aiplatform_models_evaluations_operations_wait_builder, aiplatform_models_evaluations_operations_wait_task,
    aiplatform_models_operations_cancel_builder, aiplatform_models_operations_cancel_task,
    aiplatform_models_operations_delete_builder, aiplatform_models_operations_delete_task,
    aiplatform_models_operations_wait_builder, aiplatform_models_operations_wait_task,
    aiplatform_notebook_execution_jobs_operations_cancel_builder, aiplatform_notebook_execution_jobs_operations_cancel_task,
    aiplatform_notebook_execution_jobs_operations_delete_builder, aiplatform_notebook_execution_jobs_operations_delete_task,
    aiplatform_notebook_execution_jobs_operations_wait_builder, aiplatform_notebook_execution_jobs_operations_wait_task,
    aiplatform_notebook_runtime_templates_operations_cancel_builder, aiplatform_notebook_runtime_templates_operations_cancel_task,
    aiplatform_notebook_runtime_templates_operations_delete_builder, aiplatform_notebook_runtime_templates_operations_delete_task,
    aiplatform_notebook_runtime_templates_operations_wait_builder, aiplatform_notebook_runtime_templates_operations_wait_task,
    aiplatform_notebook_runtimes_operations_cancel_builder, aiplatform_notebook_runtimes_operations_cancel_task,
    aiplatform_notebook_runtimes_operations_delete_builder, aiplatform_notebook_runtimes_operations_delete_task,
    aiplatform_notebook_runtimes_operations_wait_builder, aiplatform_notebook_runtimes_operations_wait_task,
    aiplatform_operations_cancel_builder, aiplatform_operations_cancel_task,
    aiplatform_operations_delete_builder, aiplatform_operations_delete_task,
    aiplatform_operations_wait_builder, aiplatform_operations_wait_task,
    aiplatform_persistent_resources_operations_cancel_builder, aiplatform_persistent_resources_operations_cancel_task,
    aiplatform_persistent_resources_operations_delete_builder, aiplatform_persistent_resources_operations_delete_task,
    aiplatform_persistent_resources_operations_wait_builder, aiplatform_persistent_resources_operations_wait_task,
    aiplatform_pipeline_jobs_operations_cancel_builder, aiplatform_pipeline_jobs_operations_cancel_task,
    aiplatform_pipeline_jobs_operations_delete_builder, aiplatform_pipeline_jobs_operations_delete_task,
    aiplatform_pipeline_jobs_operations_wait_builder, aiplatform_pipeline_jobs_operations_wait_task,
    aiplatform_projects_update_cache_config_builder, aiplatform_projects_update_cache_config_task,
    aiplatform_projects_locations_ask_contexts_builder, aiplatform_projects_locations_ask_contexts_task,
    aiplatform_projects_locations_async_retrieve_contexts_builder, aiplatform_projects_locations_async_retrieve_contexts_task,
    aiplatform_projects_locations_augment_prompt_builder, aiplatform_projects_locations_augment_prompt_task,
    aiplatform_projects_locations_corroborate_content_builder, aiplatform_projects_locations_corroborate_content_task,
    aiplatform_projects_locations_deploy_builder, aiplatform_projects_locations_deploy_task,
    aiplatform_projects_locations_evaluate_dataset_builder, aiplatform_projects_locations_evaluate_dataset_task,
    aiplatform_projects_locations_evaluate_instances_builder, aiplatform_projects_locations_evaluate_instances_task,
    aiplatform_projects_locations_generate_instance_rubrics_builder, aiplatform_projects_locations_generate_instance_rubrics_task,
    aiplatform_projects_locations_generate_synthetic_data_builder, aiplatform_projects_locations_generate_synthetic_data_task,
    aiplatform_projects_locations_retrieve_contexts_builder, aiplatform_projects_locations_retrieve_contexts_task,
    aiplatform_projects_locations_update_rag_engine_config_builder, aiplatform_projects_locations_update_rag_engine_config_task,
    aiplatform_projects_locations_batch_prediction_jobs_cancel_builder, aiplatform_projects_locations_batch_prediction_jobs_cancel_task,
    aiplatform_projects_locations_batch_prediction_jobs_create_builder, aiplatform_projects_locations_batch_prediction_jobs_create_task,
    aiplatform_projects_locations_batch_prediction_jobs_delete_builder, aiplatform_projects_locations_batch_prediction_jobs_delete_task,
    aiplatform_projects_locations_cached_contents_create_builder, aiplatform_projects_locations_cached_contents_create_task,
    aiplatform_projects_locations_cached_contents_delete_builder, aiplatform_projects_locations_cached_contents_delete_task,
    aiplatform_projects_locations_cached_contents_patch_builder, aiplatform_projects_locations_cached_contents_patch_task,
    aiplatform_projects_locations_custom_jobs_cancel_builder, aiplatform_projects_locations_custom_jobs_cancel_task,
    aiplatform_projects_locations_custom_jobs_create_builder, aiplatform_projects_locations_custom_jobs_create_task,
    aiplatform_projects_locations_custom_jobs_delete_builder, aiplatform_projects_locations_custom_jobs_delete_task,
    aiplatform_projects_locations_custom_jobs_operations_cancel_builder, aiplatform_projects_locations_custom_jobs_operations_cancel_task,
    aiplatform_projects_locations_custom_jobs_operations_delete_builder, aiplatform_projects_locations_custom_jobs_operations_delete_task,
    aiplatform_projects_locations_custom_jobs_operations_wait_builder, aiplatform_projects_locations_custom_jobs_operations_wait_task,
    aiplatform_projects_locations_data_labeling_jobs_cancel_builder, aiplatform_projects_locations_data_labeling_jobs_cancel_task,
    aiplatform_projects_locations_data_labeling_jobs_create_builder, aiplatform_projects_locations_data_labeling_jobs_create_task,
    aiplatform_projects_locations_data_labeling_jobs_delete_builder, aiplatform_projects_locations_data_labeling_jobs_delete_task,
    aiplatform_projects_locations_data_labeling_jobs_operations_cancel_builder, aiplatform_projects_locations_data_labeling_jobs_operations_cancel_task,
    aiplatform_projects_locations_data_labeling_jobs_operations_delete_builder, aiplatform_projects_locations_data_labeling_jobs_operations_delete_task,
    aiplatform_projects_locations_data_labeling_jobs_operations_wait_builder, aiplatform_projects_locations_data_labeling_jobs_operations_wait_task,
    aiplatform_projects_locations_datasets_create_builder, aiplatform_projects_locations_datasets_create_task,
    aiplatform_projects_locations_datasets_delete_builder, aiplatform_projects_locations_datasets_delete_task,
    aiplatform_projects_locations_datasets_export_builder, aiplatform_projects_locations_datasets_export_task,
    aiplatform_projects_locations_datasets_get_iam_policy_builder, aiplatform_projects_locations_datasets_get_iam_policy_task,
    aiplatform_projects_locations_datasets_import_builder, aiplatform_projects_locations_datasets_import_task,
    aiplatform_projects_locations_datasets_patch_builder, aiplatform_projects_locations_datasets_patch_task,
    aiplatform_projects_locations_datasets_set_iam_policy_builder, aiplatform_projects_locations_datasets_set_iam_policy_task,
    aiplatform_projects_locations_datasets_test_iam_permissions_builder, aiplatform_projects_locations_datasets_test_iam_permissions_task,
    aiplatform_projects_locations_datasets_annotation_specs_operations_cancel_builder, aiplatform_projects_locations_datasets_annotation_specs_operations_cancel_task,
    aiplatform_projects_locations_datasets_annotation_specs_operations_delete_builder, aiplatform_projects_locations_datasets_annotation_specs_operations_delete_task,
    aiplatform_projects_locations_datasets_annotation_specs_operations_wait_builder, aiplatform_projects_locations_datasets_annotation_specs_operations_wait_task,
    aiplatform_projects_locations_datasets_data_items_annotations_operations_cancel_builder, aiplatform_projects_locations_datasets_data_items_annotations_operations_cancel_task,
    aiplatform_projects_locations_datasets_data_items_annotations_operations_delete_builder, aiplatform_projects_locations_datasets_data_items_annotations_operations_delete_task,
    aiplatform_projects_locations_datasets_data_items_annotations_operations_wait_builder, aiplatform_projects_locations_datasets_data_items_annotations_operations_wait_task,
    aiplatform_projects_locations_datasets_data_items_operations_cancel_builder, aiplatform_projects_locations_datasets_data_items_operations_cancel_task,
    aiplatform_projects_locations_datasets_data_items_operations_delete_builder, aiplatform_projects_locations_datasets_data_items_operations_delete_task,
    aiplatform_projects_locations_datasets_data_items_operations_wait_builder, aiplatform_projects_locations_datasets_data_items_operations_wait_task,
    aiplatform_projects_locations_datasets_dataset_versions_create_builder, aiplatform_projects_locations_datasets_dataset_versions_create_task,
    aiplatform_projects_locations_datasets_dataset_versions_delete_builder, aiplatform_projects_locations_datasets_dataset_versions_delete_task,
    aiplatform_projects_locations_datasets_dataset_versions_patch_builder, aiplatform_projects_locations_datasets_dataset_versions_patch_task,
    aiplatform_projects_locations_datasets_operations_cancel_builder, aiplatform_projects_locations_datasets_operations_cancel_task,
    aiplatform_projects_locations_datasets_operations_delete_builder, aiplatform_projects_locations_datasets_operations_delete_task,
    aiplatform_projects_locations_datasets_operations_wait_builder, aiplatform_projects_locations_datasets_operations_wait_task,
    aiplatform_projects_locations_datasets_saved_queries_delete_builder, aiplatform_projects_locations_datasets_saved_queries_delete_task,
    aiplatform_projects_locations_datasets_saved_queries_operations_cancel_builder, aiplatform_projects_locations_datasets_saved_queries_operations_cancel_task,
    aiplatform_projects_locations_datasets_saved_queries_operations_delete_builder, aiplatform_projects_locations_datasets_saved_queries_operations_delete_task,
    aiplatform_projects_locations_datasets_saved_queries_operations_wait_builder, aiplatform_projects_locations_datasets_saved_queries_operations_wait_task,
    aiplatform_projects_locations_deployment_resource_pools_create_builder, aiplatform_projects_locations_deployment_resource_pools_create_task,
    aiplatform_projects_locations_deployment_resource_pools_delete_builder, aiplatform_projects_locations_deployment_resource_pools_delete_task,
    aiplatform_projects_locations_deployment_resource_pools_patch_builder, aiplatform_projects_locations_deployment_resource_pools_patch_task,
    aiplatform_projects_locations_deployment_resource_pools_operations_cancel_builder, aiplatform_projects_locations_deployment_resource_pools_operations_cancel_task,
    aiplatform_projects_locations_deployment_resource_pools_operations_delete_builder, aiplatform_projects_locations_deployment_resource_pools_operations_delete_task,
    aiplatform_projects_locations_deployment_resource_pools_operations_wait_builder, aiplatform_projects_locations_deployment_resource_pools_operations_wait_task,
    aiplatform_projects_locations_endpoints_compute_tokens_builder, aiplatform_projects_locations_endpoints_compute_tokens_task,
    aiplatform_projects_locations_endpoints_count_tokens_builder, aiplatform_projects_locations_endpoints_count_tokens_task,
    aiplatform_projects_locations_endpoints_create_builder, aiplatform_projects_locations_endpoints_create_task,
    aiplatform_projects_locations_endpoints_delete_builder, aiplatform_projects_locations_endpoints_delete_task,
    aiplatform_projects_locations_endpoints_deploy_model_builder, aiplatform_projects_locations_endpoints_deploy_model_task,
    aiplatform_projects_locations_endpoints_direct_predict_builder, aiplatform_projects_locations_endpoints_direct_predict_task,
    aiplatform_projects_locations_endpoints_direct_raw_predict_builder, aiplatform_projects_locations_endpoints_direct_raw_predict_task,
    aiplatform_projects_locations_endpoints_explain_builder, aiplatform_projects_locations_endpoints_explain_task,
    aiplatform_projects_locations_endpoints_fetch_predict_operation_builder, aiplatform_projects_locations_endpoints_fetch_predict_operation_task,
    aiplatform_projects_locations_endpoints_generate_content_builder, aiplatform_projects_locations_endpoints_generate_content_task,
    aiplatform_projects_locations_endpoints_mutate_deployed_model_builder, aiplatform_projects_locations_endpoints_mutate_deployed_model_task,
    aiplatform_projects_locations_endpoints_patch_builder, aiplatform_projects_locations_endpoints_patch_task,
    aiplatform_projects_locations_endpoints_predict_builder, aiplatform_projects_locations_endpoints_predict_task,
    aiplatform_projects_locations_endpoints_predict_long_running_builder, aiplatform_projects_locations_endpoints_predict_long_running_task,
    aiplatform_projects_locations_endpoints_raw_predict_builder, aiplatform_projects_locations_endpoints_raw_predict_task,
    aiplatform_projects_locations_endpoints_server_streaming_predict_builder, aiplatform_projects_locations_endpoints_server_streaming_predict_task,
    aiplatform_projects_locations_endpoints_stream_generate_content_builder, aiplatform_projects_locations_endpoints_stream_generate_content_task,
    aiplatform_projects_locations_endpoints_stream_raw_predict_builder, aiplatform_projects_locations_endpoints_stream_raw_predict_task,
    aiplatform_projects_locations_endpoints_undeploy_model_builder, aiplatform_projects_locations_endpoints_undeploy_model_task,
    aiplatform_projects_locations_endpoints_update_builder, aiplatform_projects_locations_endpoints_update_task,
    aiplatform_projects_locations_endpoints_chat_completions_builder, aiplatform_projects_locations_endpoints_chat_completions_task,
    aiplatform_projects_locations_endpoints_deployed_models_invoke_invoke_builder, aiplatform_projects_locations_endpoints_deployed_models_invoke_invoke_task,
    aiplatform_projects_locations_endpoints_google_science_inference_builder, aiplatform_projects_locations_endpoints_google_science_inference_task,
    aiplatform_projects_locations_endpoints_invoke_invoke_builder, aiplatform_projects_locations_endpoints_invoke_invoke_task,
    aiplatform_projects_locations_endpoints_openapi_completions_builder, aiplatform_projects_locations_endpoints_openapi_completions_task,
    aiplatform_projects_locations_endpoints_openapi_embeddings_builder, aiplatform_projects_locations_endpoints_openapi_embeddings_task,
    aiplatform_projects_locations_endpoints_openapi_responses_builder, aiplatform_projects_locations_endpoints_openapi_responses_task,
    aiplatform_projects_locations_endpoints_operations_cancel_builder, aiplatform_projects_locations_endpoints_operations_cancel_task,
    aiplatform_projects_locations_endpoints_operations_delete_builder, aiplatform_projects_locations_endpoints_operations_delete_task,
    aiplatform_projects_locations_endpoints_operations_wait_builder, aiplatform_projects_locations_endpoints_operations_wait_task,
    aiplatform_projects_locations_evaluation_items_create_builder, aiplatform_projects_locations_evaluation_items_create_task,
    aiplatform_projects_locations_evaluation_items_delete_builder, aiplatform_projects_locations_evaluation_items_delete_task,
    aiplatform_projects_locations_evaluation_runs_cancel_builder, aiplatform_projects_locations_evaluation_runs_cancel_task,
    aiplatform_projects_locations_evaluation_runs_create_builder, aiplatform_projects_locations_evaluation_runs_create_task,
    aiplatform_projects_locations_evaluation_runs_delete_builder, aiplatform_projects_locations_evaluation_runs_delete_task,
    aiplatform_projects_locations_evaluation_sets_create_builder, aiplatform_projects_locations_evaluation_sets_create_task,
    aiplatform_projects_locations_evaluation_sets_delete_builder, aiplatform_projects_locations_evaluation_sets_delete_task,
    aiplatform_projects_locations_evaluation_sets_patch_builder, aiplatform_projects_locations_evaluation_sets_patch_task,
    aiplatform_projects_locations_feature_groups_create_builder, aiplatform_projects_locations_feature_groups_create_task,
    aiplatform_projects_locations_feature_groups_delete_builder, aiplatform_projects_locations_feature_groups_delete_task,
    aiplatform_projects_locations_feature_groups_get_iam_policy_builder, aiplatform_projects_locations_feature_groups_get_iam_policy_task,
    aiplatform_projects_locations_feature_groups_patch_builder, aiplatform_projects_locations_feature_groups_patch_task,
    aiplatform_projects_locations_feature_groups_set_iam_policy_builder, aiplatform_projects_locations_feature_groups_set_iam_policy_task,
    aiplatform_projects_locations_feature_groups_test_iam_permissions_builder, aiplatform_projects_locations_feature_groups_test_iam_permissions_task,
    aiplatform_projects_locations_feature_groups_features_batch_create_builder, aiplatform_projects_locations_feature_groups_features_batch_create_task,
    aiplatform_projects_locations_feature_groups_features_create_builder, aiplatform_projects_locations_feature_groups_features_create_task,
    aiplatform_projects_locations_feature_groups_features_delete_builder, aiplatform_projects_locations_feature_groups_features_delete_task,
    aiplatform_projects_locations_feature_groups_features_patch_builder, aiplatform_projects_locations_feature_groups_features_patch_task,
    aiplatform_projects_locations_feature_groups_features_operations_delete_builder, aiplatform_projects_locations_feature_groups_features_operations_delete_task,
    aiplatform_projects_locations_feature_groups_features_operations_wait_builder, aiplatform_projects_locations_feature_groups_features_operations_wait_task,
    aiplatform_projects_locations_feature_groups_operations_delete_builder, aiplatform_projects_locations_feature_groups_operations_delete_task,
    aiplatform_projects_locations_feature_groups_operations_wait_builder, aiplatform_projects_locations_feature_groups_operations_wait_task,
    aiplatform_projects_locations_feature_online_stores_create_builder, aiplatform_projects_locations_feature_online_stores_create_task,
    aiplatform_projects_locations_feature_online_stores_delete_builder, aiplatform_projects_locations_feature_online_stores_delete_task,
    aiplatform_projects_locations_feature_online_stores_get_iam_policy_builder, aiplatform_projects_locations_feature_online_stores_get_iam_policy_task,
    aiplatform_projects_locations_feature_online_stores_patch_builder, aiplatform_projects_locations_feature_online_stores_patch_task,
    aiplatform_projects_locations_feature_online_stores_set_iam_policy_builder, aiplatform_projects_locations_feature_online_stores_set_iam_policy_task,
    aiplatform_projects_locations_feature_online_stores_test_iam_permissions_builder, aiplatform_projects_locations_feature_online_stores_test_iam_permissions_task,
    aiplatform_projects_locations_feature_online_stores_feature_views_create_builder, aiplatform_projects_locations_feature_online_stores_feature_views_create_task,
    aiplatform_projects_locations_feature_online_stores_feature_views_delete_builder, aiplatform_projects_locations_feature_online_stores_feature_views_delete_task,
    aiplatform_projects_locations_feature_online_stores_feature_views_direct_write_builder, aiplatform_projects_locations_feature_online_stores_feature_views_direct_write_task,
    aiplatform_projects_locations_feature_online_stores_feature_views_fetch_feature_values_builder, aiplatform_projects_locations_feature_online_stores_feature_views_fetch_feature_values_task,
    aiplatform_projects_locations_feature_online_stores_feature_views_generate_fetch_access_token_builder, aiplatform_projects_locations_feature_online_stores_feature_views_generate_fetch_access_token_task,
    aiplatform_projects_locations_feature_online_stores_feature_views_get_iam_policy_builder, aiplatform_projects_locations_feature_online_stores_feature_views_get_iam_policy_task,
    aiplatform_projects_locations_feature_online_stores_feature_views_patch_builder, aiplatform_projects_locations_feature_online_stores_feature_views_patch_task,
    aiplatform_projects_locations_feature_online_stores_feature_views_search_nearest_entities_builder, aiplatform_projects_locations_feature_online_stores_feature_views_search_nearest_entities_task,
    aiplatform_projects_locations_feature_online_stores_feature_views_set_iam_policy_builder, aiplatform_projects_locations_feature_online_stores_feature_views_set_iam_policy_task,
    aiplatform_projects_locations_feature_online_stores_feature_views_sync_builder, aiplatform_projects_locations_feature_online_stores_feature_views_sync_task,
    aiplatform_projects_locations_feature_online_stores_feature_views_test_iam_permissions_builder, aiplatform_projects_locations_feature_online_stores_feature_views_test_iam_permissions_task,
    aiplatform_projects_locations_feature_online_stores_feature_views_operations_delete_builder, aiplatform_projects_locations_feature_online_stores_feature_views_operations_delete_task,
    aiplatform_projects_locations_feature_online_stores_feature_views_operations_wait_builder, aiplatform_projects_locations_feature_online_stores_feature_views_operations_wait_task,
    aiplatform_projects_locations_feature_online_stores_operations_delete_builder, aiplatform_projects_locations_feature_online_stores_operations_delete_task,
    aiplatform_projects_locations_feature_online_stores_operations_wait_builder, aiplatform_projects_locations_feature_online_stores_operations_wait_task,
    aiplatform_projects_locations_featurestores_batch_read_feature_values_builder, aiplatform_projects_locations_featurestores_batch_read_feature_values_task,
    aiplatform_projects_locations_featurestores_create_builder, aiplatform_projects_locations_featurestores_create_task,
    aiplatform_projects_locations_featurestores_delete_builder, aiplatform_projects_locations_featurestores_delete_task,
    aiplatform_projects_locations_featurestores_get_iam_policy_builder, aiplatform_projects_locations_featurestores_get_iam_policy_task,
    aiplatform_projects_locations_featurestores_patch_builder, aiplatform_projects_locations_featurestores_patch_task,
    aiplatform_projects_locations_featurestores_set_iam_policy_builder, aiplatform_projects_locations_featurestores_set_iam_policy_task,
    aiplatform_projects_locations_featurestores_test_iam_permissions_builder, aiplatform_projects_locations_featurestores_test_iam_permissions_task,
    aiplatform_projects_locations_featurestores_entity_types_create_builder, aiplatform_projects_locations_featurestores_entity_types_create_task,
    aiplatform_projects_locations_featurestores_entity_types_delete_builder, aiplatform_projects_locations_featurestores_entity_types_delete_task,
    aiplatform_projects_locations_featurestores_entity_types_delete_feature_values_builder, aiplatform_projects_locations_featurestores_entity_types_delete_feature_values_task,
    aiplatform_projects_locations_featurestores_entity_types_export_feature_values_builder, aiplatform_projects_locations_featurestores_entity_types_export_feature_values_task,
    aiplatform_projects_locations_featurestores_entity_types_get_iam_policy_builder, aiplatform_projects_locations_featurestores_entity_types_get_iam_policy_task,
    aiplatform_projects_locations_featurestores_entity_types_import_feature_values_builder, aiplatform_projects_locations_featurestores_entity_types_import_feature_values_task,
    aiplatform_projects_locations_featurestores_entity_types_patch_builder, aiplatform_projects_locations_featurestores_entity_types_patch_task,
    aiplatform_projects_locations_featurestores_entity_types_read_feature_values_builder, aiplatform_projects_locations_featurestores_entity_types_read_feature_values_task,
    aiplatform_projects_locations_featurestores_entity_types_set_iam_policy_builder, aiplatform_projects_locations_featurestores_entity_types_set_iam_policy_task,
    aiplatform_projects_locations_featurestores_entity_types_streaming_read_feature_values_builder, aiplatform_projects_locations_featurestores_entity_types_streaming_read_feature_values_task,
    aiplatform_projects_locations_featurestores_entity_types_test_iam_permissions_builder, aiplatform_projects_locations_featurestores_entity_types_test_iam_permissions_task,
    aiplatform_projects_locations_featurestores_entity_types_write_feature_values_builder, aiplatform_projects_locations_featurestores_entity_types_write_feature_values_task,
    aiplatform_projects_locations_featurestores_entity_types_features_batch_create_builder, aiplatform_projects_locations_featurestores_entity_types_features_batch_create_task,
    aiplatform_projects_locations_featurestores_entity_types_features_create_builder, aiplatform_projects_locations_featurestores_entity_types_features_create_task,
    aiplatform_projects_locations_featurestores_entity_types_features_delete_builder, aiplatform_projects_locations_featurestores_entity_types_features_delete_task,
    aiplatform_projects_locations_featurestores_entity_types_features_patch_builder, aiplatform_projects_locations_featurestores_entity_types_features_patch_task,
    aiplatform_projects_locations_featurestores_entity_types_features_operations_cancel_builder, aiplatform_projects_locations_featurestores_entity_types_features_operations_cancel_task,
    aiplatform_projects_locations_featurestores_entity_types_features_operations_delete_builder, aiplatform_projects_locations_featurestores_entity_types_features_operations_delete_task,
    aiplatform_projects_locations_featurestores_entity_types_features_operations_wait_builder, aiplatform_projects_locations_featurestores_entity_types_features_operations_wait_task,
    aiplatform_projects_locations_featurestores_entity_types_operations_cancel_builder, aiplatform_projects_locations_featurestores_entity_types_operations_cancel_task,
    aiplatform_projects_locations_featurestores_entity_types_operations_delete_builder, aiplatform_projects_locations_featurestores_entity_types_operations_delete_task,
    aiplatform_projects_locations_featurestores_entity_types_operations_wait_builder, aiplatform_projects_locations_featurestores_entity_types_operations_wait_task,
    aiplatform_projects_locations_featurestores_operations_cancel_builder, aiplatform_projects_locations_featurestores_operations_cancel_task,
    aiplatform_projects_locations_featurestores_operations_delete_builder, aiplatform_projects_locations_featurestores_operations_delete_task,
    aiplatform_projects_locations_featurestores_operations_wait_builder, aiplatform_projects_locations_featurestores_operations_wait_task,
    aiplatform_projects_locations_hyperparameter_tuning_jobs_cancel_builder, aiplatform_projects_locations_hyperparameter_tuning_jobs_cancel_task,
    aiplatform_projects_locations_hyperparameter_tuning_jobs_create_builder, aiplatform_projects_locations_hyperparameter_tuning_jobs_create_task,
    aiplatform_projects_locations_hyperparameter_tuning_jobs_delete_builder, aiplatform_projects_locations_hyperparameter_tuning_jobs_delete_task,
    aiplatform_projects_locations_hyperparameter_tuning_jobs_operations_cancel_builder, aiplatform_projects_locations_hyperparameter_tuning_jobs_operations_cancel_task,
    aiplatform_projects_locations_hyperparameter_tuning_jobs_operations_delete_builder, aiplatform_projects_locations_hyperparameter_tuning_jobs_operations_delete_task,
    aiplatform_projects_locations_hyperparameter_tuning_jobs_operations_wait_builder, aiplatform_projects_locations_hyperparameter_tuning_jobs_operations_wait_task,
    aiplatform_projects_locations_index_endpoints_create_builder, aiplatform_projects_locations_index_endpoints_create_task,
    aiplatform_projects_locations_index_endpoints_delete_builder, aiplatform_projects_locations_index_endpoints_delete_task,
    aiplatform_projects_locations_index_endpoints_deploy_index_builder, aiplatform_projects_locations_index_endpoints_deploy_index_task,
    aiplatform_projects_locations_index_endpoints_find_neighbors_builder, aiplatform_projects_locations_index_endpoints_find_neighbors_task,
    aiplatform_projects_locations_index_endpoints_mutate_deployed_index_builder, aiplatform_projects_locations_index_endpoints_mutate_deployed_index_task,
    aiplatform_projects_locations_index_endpoints_patch_builder, aiplatform_projects_locations_index_endpoints_patch_task,
    aiplatform_projects_locations_index_endpoints_read_index_datapoints_builder, aiplatform_projects_locations_index_endpoints_read_index_datapoints_task,
    aiplatform_projects_locations_index_endpoints_undeploy_index_builder, aiplatform_projects_locations_index_endpoints_undeploy_index_task,
    aiplatform_projects_locations_index_endpoints_operations_cancel_builder, aiplatform_projects_locations_index_endpoints_operations_cancel_task,
    aiplatform_projects_locations_index_endpoints_operations_delete_builder, aiplatform_projects_locations_index_endpoints_operations_delete_task,
    aiplatform_projects_locations_index_endpoints_operations_wait_builder, aiplatform_projects_locations_index_endpoints_operations_wait_task,
    aiplatform_projects_locations_indexes_create_builder, aiplatform_projects_locations_indexes_create_task,
    aiplatform_projects_locations_indexes_delete_builder, aiplatform_projects_locations_indexes_delete_task,
    aiplatform_projects_locations_indexes_patch_builder, aiplatform_projects_locations_indexes_patch_task,
    aiplatform_projects_locations_indexes_remove_datapoints_builder, aiplatform_projects_locations_indexes_remove_datapoints_task,
    aiplatform_projects_locations_indexes_upsert_datapoints_builder, aiplatform_projects_locations_indexes_upsert_datapoints_task,
    aiplatform_projects_locations_indexes_operations_cancel_builder, aiplatform_projects_locations_indexes_operations_cancel_task,
    aiplatform_projects_locations_indexes_operations_delete_builder, aiplatform_projects_locations_indexes_operations_delete_task,
    aiplatform_projects_locations_indexes_operations_wait_builder, aiplatform_projects_locations_indexes_operations_wait_task,
    aiplatform_projects_locations_metadata_stores_create_builder, aiplatform_projects_locations_metadata_stores_create_task,
    aiplatform_projects_locations_metadata_stores_delete_builder, aiplatform_projects_locations_metadata_stores_delete_task,
    aiplatform_projects_locations_metadata_stores_artifacts_create_builder, aiplatform_projects_locations_metadata_stores_artifacts_create_task,
    aiplatform_projects_locations_metadata_stores_artifacts_delete_builder, aiplatform_projects_locations_metadata_stores_artifacts_delete_task,
    aiplatform_projects_locations_metadata_stores_artifacts_patch_builder, aiplatform_projects_locations_metadata_stores_artifacts_patch_task,
    aiplatform_projects_locations_metadata_stores_artifacts_purge_builder, aiplatform_projects_locations_metadata_stores_artifacts_purge_task,
    aiplatform_projects_locations_metadata_stores_artifacts_operations_cancel_builder, aiplatform_projects_locations_metadata_stores_artifacts_operations_cancel_task,
    aiplatform_projects_locations_metadata_stores_artifacts_operations_delete_builder, aiplatform_projects_locations_metadata_stores_artifacts_operations_delete_task,
    aiplatform_projects_locations_metadata_stores_artifacts_operations_wait_builder, aiplatform_projects_locations_metadata_stores_artifacts_operations_wait_task,
    aiplatform_projects_locations_metadata_stores_contexts_add_context_artifacts_and_executions_builder, aiplatform_projects_locations_metadata_stores_contexts_add_context_artifacts_and_executions_task,
    aiplatform_projects_locations_metadata_stores_contexts_add_context_children_builder, aiplatform_projects_locations_metadata_stores_contexts_add_context_children_task,
    aiplatform_projects_locations_metadata_stores_contexts_create_builder, aiplatform_projects_locations_metadata_stores_contexts_create_task,
    aiplatform_projects_locations_metadata_stores_contexts_delete_builder, aiplatform_projects_locations_metadata_stores_contexts_delete_task,
    aiplatform_projects_locations_metadata_stores_contexts_patch_builder, aiplatform_projects_locations_metadata_stores_contexts_patch_task,
    aiplatform_projects_locations_metadata_stores_contexts_purge_builder, aiplatform_projects_locations_metadata_stores_contexts_purge_task,
    aiplatform_projects_locations_metadata_stores_contexts_remove_context_children_builder, aiplatform_projects_locations_metadata_stores_contexts_remove_context_children_task,
    aiplatform_projects_locations_metadata_stores_contexts_operations_cancel_builder, aiplatform_projects_locations_metadata_stores_contexts_operations_cancel_task,
    aiplatform_projects_locations_metadata_stores_contexts_operations_delete_builder, aiplatform_projects_locations_metadata_stores_contexts_operations_delete_task,
    aiplatform_projects_locations_metadata_stores_contexts_operations_wait_builder, aiplatform_projects_locations_metadata_stores_contexts_operations_wait_task,
    aiplatform_projects_locations_metadata_stores_executions_add_execution_events_builder, aiplatform_projects_locations_metadata_stores_executions_add_execution_events_task,
    aiplatform_projects_locations_metadata_stores_executions_create_builder, aiplatform_projects_locations_metadata_stores_executions_create_task,
    aiplatform_projects_locations_metadata_stores_executions_delete_builder, aiplatform_projects_locations_metadata_stores_executions_delete_task,
    aiplatform_projects_locations_metadata_stores_executions_patch_builder, aiplatform_projects_locations_metadata_stores_executions_patch_task,
    aiplatform_projects_locations_metadata_stores_executions_purge_builder, aiplatform_projects_locations_metadata_stores_executions_purge_task,
    aiplatform_projects_locations_metadata_stores_executions_operations_cancel_builder, aiplatform_projects_locations_metadata_stores_executions_operations_cancel_task,
    aiplatform_projects_locations_metadata_stores_executions_operations_delete_builder, aiplatform_projects_locations_metadata_stores_executions_operations_delete_task,
    aiplatform_projects_locations_metadata_stores_executions_operations_wait_builder, aiplatform_projects_locations_metadata_stores_executions_operations_wait_task,
    aiplatform_projects_locations_metadata_stores_metadata_schemas_create_builder, aiplatform_projects_locations_metadata_stores_metadata_schemas_create_task,
    aiplatform_projects_locations_metadata_stores_operations_cancel_builder, aiplatform_projects_locations_metadata_stores_operations_cancel_task,
    aiplatform_projects_locations_metadata_stores_operations_delete_builder, aiplatform_projects_locations_metadata_stores_operations_delete_task,
    aiplatform_projects_locations_metadata_stores_operations_wait_builder, aiplatform_projects_locations_metadata_stores_operations_wait_task,
    aiplatform_projects_locations_migratable_resources_batch_migrate_builder, aiplatform_projects_locations_migratable_resources_batch_migrate_task,
    aiplatform_projects_locations_migratable_resources_search_builder, aiplatform_projects_locations_migratable_resources_search_task,
    aiplatform_projects_locations_migratable_resources_operations_cancel_builder, aiplatform_projects_locations_migratable_resources_operations_cancel_task,
    aiplatform_projects_locations_migratable_resources_operations_delete_builder, aiplatform_projects_locations_migratable_resources_operations_delete_task,
    aiplatform_projects_locations_migratable_resources_operations_wait_builder, aiplatform_projects_locations_migratable_resources_operations_wait_task,
    aiplatform_projects_locations_model_deployment_monitoring_jobs_create_builder, aiplatform_projects_locations_model_deployment_monitoring_jobs_create_task,
    aiplatform_projects_locations_model_deployment_monitoring_jobs_delete_builder, aiplatform_projects_locations_model_deployment_monitoring_jobs_delete_task,
    aiplatform_projects_locations_model_deployment_monitoring_jobs_patch_builder, aiplatform_projects_locations_model_deployment_monitoring_jobs_patch_task,
    aiplatform_projects_locations_model_deployment_monitoring_jobs_pause_builder, aiplatform_projects_locations_model_deployment_monitoring_jobs_pause_task,
    aiplatform_projects_locations_model_deployment_monitoring_jobs_resume_builder, aiplatform_projects_locations_model_deployment_monitoring_jobs_resume_task,
    aiplatform_projects_locations_model_deployment_monitoring_jobs_search_model_deployment_monitoring_stats_anomalies_builder, aiplatform_projects_locations_model_deployment_monitoring_jobs_search_model_deployment_monitoring_stats_anomalies_task,
    aiplatform_projects_locations_model_deployment_monitoring_jobs_operations_cancel_builder, aiplatform_projects_locations_model_deployment_monitoring_jobs_operations_cancel_task,
    aiplatform_projects_locations_model_deployment_monitoring_jobs_operations_delete_builder, aiplatform_projects_locations_model_deployment_monitoring_jobs_operations_delete_task,
    aiplatform_projects_locations_model_deployment_monitoring_jobs_operations_wait_builder, aiplatform_projects_locations_model_deployment_monitoring_jobs_operations_wait_task,
    aiplatform_projects_locations_models_copy_builder, aiplatform_projects_locations_models_copy_task,
    aiplatform_projects_locations_models_delete_builder, aiplatform_projects_locations_models_delete_task,
    aiplatform_projects_locations_models_delete_version_builder, aiplatform_projects_locations_models_delete_version_task,
    aiplatform_projects_locations_models_export_builder, aiplatform_projects_locations_models_export_task,
    aiplatform_projects_locations_models_get_iam_policy_builder, aiplatform_projects_locations_models_get_iam_policy_task,
    aiplatform_projects_locations_models_merge_version_aliases_builder, aiplatform_projects_locations_models_merge_version_aliases_task,
    aiplatform_projects_locations_models_patch_builder, aiplatform_projects_locations_models_patch_task,
    aiplatform_projects_locations_models_set_iam_policy_builder, aiplatform_projects_locations_models_set_iam_policy_task,
    aiplatform_projects_locations_models_test_iam_permissions_builder, aiplatform_projects_locations_models_test_iam_permissions_task,
    aiplatform_projects_locations_models_update_explanation_dataset_builder, aiplatform_projects_locations_models_update_explanation_dataset_task,
    aiplatform_projects_locations_models_upload_builder, aiplatform_projects_locations_models_upload_task,
    aiplatform_projects_locations_models_evaluations_import_builder, aiplatform_projects_locations_models_evaluations_import_task,
    aiplatform_projects_locations_models_evaluations_operations_cancel_builder, aiplatform_projects_locations_models_evaluations_operations_cancel_task,
    aiplatform_projects_locations_models_evaluations_operations_delete_builder, aiplatform_projects_locations_models_evaluations_operations_delete_task,
    aiplatform_projects_locations_models_evaluations_operations_wait_builder, aiplatform_projects_locations_models_evaluations_operations_wait_task,
    aiplatform_projects_locations_models_evaluations_slices_batch_import_builder, aiplatform_projects_locations_models_evaluations_slices_batch_import_task,
    aiplatform_projects_locations_models_operations_cancel_builder, aiplatform_projects_locations_models_operations_cancel_task,
    aiplatform_projects_locations_models_operations_delete_builder, aiplatform_projects_locations_models_operations_delete_task,
    aiplatform_projects_locations_models_operations_wait_builder, aiplatform_projects_locations_models_operations_wait_task,
    aiplatform_projects_locations_nas_jobs_cancel_builder, aiplatform_projects_locations_nas_jobs_cancel_task,
    aiplatform_projects_locations_nas_jobs_create_builder, aiplatform_projects_locations_nas_jobs_create_task,
    aiplatform_projects_locations_nas_jobs_delete_builder, aiplatform_projects_locations_nas_jobs_delete_task,
    aiplatform_projects_locations_notebook_execution_jobs_create_builder, aiplatform_projects_locations_notebook_execution_jobs_create_task,
    aiplatform_projects_locations_notebook_execution_jobs_delete_builder, aiplatform_projects_locations_notebook_execution_jobs_delete_task,
    aiplatform_projects_locations_notebook_execution_jobs_operations_cancel_builder, aiplatform_projects_locations_notebook_execution_jobs_operations_cancel_task,
    aiplatform_projects_locations_notebook_execution_jobs_operations_delete_builder, aiplatform_projects_locations_notebook_execution_jobs_operations_delete_task,
    aiplatform_projects_locations_notebook_execution_jobs_operations_wait_builder, aiplatform_projects_locations_notebook_execution_jobs_operations_wait_task,
    aiplatform_projects_locations_notebook_runtime_templates_create_builder, aiplatform_projects_locations_notebook_runtime_templates_create_task,
    aiplatform_projects_locations_notebook_runtime_templates_delete_builder, aiplatform_projects_locations_notebook_runtime_templates_delete_task,
    aiplatform_projects_locations_notebook_runtime_templates_get_iam_policy_builder, aiplatform_projects_locations_notebook_runtime_templates_get_iam_policy_task,
    aiplatform_projects_locations_notebook_runtime_templates_patch_builder, aiplatform_projects_locations_notebook_runtime_templates_patch_task,
    aiplatform_projects_locations_notebook_runtime_templates_set_iam_policy_builder, aiplatform_projects_locations_notebook_runtime_templates_set_iam_policy_task,
    aiplatform_projects_locations_notebook_runtime_templates_test_iam_permissions_builder, aiplatform_projects_locations_notebook_runtime_templates_test_iam_permissions_task,
    aiplatform_projects_locations_notebook_runtime_templates_operations_cancel_builder, aiplatform_projects_locations_notebook_runtime_templates_operations_cancel_task,
    aiplatform_projects_locations_notebook_runtime_templates_operations_delete_builder, aiplatform_projects_locations_notebook_runtime_templates_operations_delete_task,
    aiplatform_projects_locations_notebook_runtime_templates_operations_wait_builder, aiplatform_projects_locations_notebook_runtime_templates_operations_wait_task,
    aiplatform_projects_locations_notebook_runtimes_assign_builder, aiplatform_projects_locations_notebook_runtimes_assign_task,
    aiplatform_projects_locations_notebook_runtimes_delete_builder, aiplatform_projects_locations_notebook_runtimes_delete_task,
    aiplatform_projects_locations_notebook_runtimes_start_builder, aiplatform_projects_locations_notebook_runtimes_start_task,
    aiplatform_projects_locations_notebook_runtimes_stop_builder, aiplatform_projects_locations_notebook_runtimes_stop_task,
    aiplatform_projects_locations_notebook_runtimes_upgrade_builder, aiplatform_projects_locations_notebook_runtimes_upgrade_task,
    aiplatform_projects_locations_notebook_runtimes_operations_cancel_builder, aiplatform_projects_locations_notebook_runtimes_operations_cancel_task,
    aiplatform_projects_locations_notebook_runtimes_operations_delete_builder, aiplatform_projects_locations_notebook_runtimes_operations_delete_task,
    aiplatform_projects_locations_notebook_runtimes_operations_wait_builder, aiplatform_projects_locations_notebook_runtimes_operations_wait_task,
    aiplatform_projects_locations_operations_cancel_builder, aiplatform_projects_locations_operations_cancel_task,
    aiplatform_projects_locations_operations_delete_builder, aiplatform_projects_locations_operations_delete_task,
    aiplatform_projects_locations_operations_wait_builder, aiplatform_projects_locations_operations_wait_task,
    aiplatform_projects_locations_persistent_resources_create_builder, aiplatform_projects_locations_persistent_resources_create_task,
    aiplatform_projects_locations_persistent_resources_delete_builder, aiplatform_projects_locations_persistent_resources_delete_task,
    aiplatform_projects_locations_persistent_resources_patch_builder, aiplatform_projects_locations_persistent_resources_patch_task,
    aiplatform_projects_locations_persistent_resources_reboot_builder, aiplatform_projects_locations_persistent_resources_reboot_task,
    aiplatform_projects_locations_persistent_resources_operations_cancel_builder, aiplatform_projects_locations_persistent_resources_operations_cancel_task,
    aiplatform_projects_locations_persistent_resources_operations_delete_builder, aiplatform_projects_locations_persistent_resources_operations_delete_task,
    aiplatform_projects_locations_persistent_resources_operations_wait_builder, aiplatform_projects_locations_persistent_resources_operations_wait_task,
    aiplatform_projects_locations_pipeline_jobs_batch_cancel_builder, aiplatform_projects_locations_pipeline_jobs_batch_cancel_task,
    aiplatform_projects_locations_pipeline_jobs_batch_delete_builder, aiplatform_projects_locations_pipeline_jobs_batch_delete_task,
    aiplatform_projects_locations_pipeline_jobs_cancel_builder, aiplatform_projects_locations_pipeline_jobs_cancel_task,
    aiplatform_projects_locations_pipeline_jobs_create_builder, aiplatform_projects_locations_pipeline_jobs_create_task,
    aiplatform_projects_locations_pipeline_jobs_delete_builder, aiplatform_projects_locations_pipeline_jobs_delete_task,
    aiplatform_projects_locations_pipeline_jobs_operations_cancel_builder, aiplatform_projects_locations_pipeline_jobs_operations_cancel_task,
    aiplatform_projects_locations_pipeline_jobs_operations_delete_builder, aiplatform_projects_locations_pipeline_jobs_operations_delete_task,
    aiplatform_projects_locations_pipeline_jobs_operations_wait_builder, aiplatform_projects_locations_pipeline_jobs_operations_wait_task,
    aiplatform_projects_locations_publishers_models_compute_tokens_builder, aiplatform_projects_locations_publishers_models_compute_tokens_task,
    aiplatform_projects_locations_publishers_models_count_tokens_builder, aiplatform_projects_locations_publishers_models_count_tokens_task,
    aiplatform_projects_locations_publishers_models_embed_content_builder, aiplatform_projects_locations_publishers_models_embed_content_task,
    aiplatform_projects_locations_publishers_models_fetch_predict_operation_builder, aiplatform_projects_locations_publishers_models_fetch_predict_operation_task,
    aiplatform_projects_locations_publishers_models_generate_content_builder, aiplatform_projects_locations_publishers_models_generate_content_task,
    aiplatform_projects_locations_publishers_models_predict_builder, aiplatform_projects_locations_publishers_models_predict_task,
    aiplatform_projects_locations_publishers_models_predict_long_running_builder, aiplatform_projects_locations_publishers_models_predict_long_running_task,
    aiplatform_projects_locations_publishers_models_raw_predict_builder, aiplatform_projects_locations_publishers_models_raw_predict_task,
    aiplatform_projects_locations_publishers_models_server_streaming_predict_builder, aiplatform_projects_locations_publishers_models_server_streaming_predict_task,
    aiplatform_projects_locations_publishers_models_stream_generate_content_builder, aiplatform_projects_locations_publishers_models_stream_generate_content_task,
    aiplatform_projects_locations_publishers_models_stream_raw_predict_builder, aiplatform_projects_locations_publishers_models_stream_raw_predict_task,
    aiplatform_projects_locations_publishers_models_invoke_invoke_builder, aiplatform_projects_locations_publishers_models_invoke_invoke_task,
    aiplatform_projects_locations_rag_corpora_create_builder, aiplatform_projects_locations_rag_corpora_create_task,
    aiplatform_projects_locations_rag_corpora_delete_builder, aiplatform_projects_locations_rag_corpora_delete_task,
    aiplatform_projects_locations_rag_corpora_patch_builder, aiplatform_projects_locations_rag_corpora_patch_task,
    aiplatform_projects_locations_rag_corpora_operations_cancel_builder, aiplatform_projects_locations_rag_corpora_operations_cancel_task,
    aiplatform_projects_locations_rag_corpora_operations_delete_builder, aiplatform_projects_locations_rag_corpora_operations_delete_task,
    aiplatform_projects_locations_rag_corpora_operations_wait_builder, aiplatform_projects_locations_rag_corpora_operations_wait_task,
    aiplatform_projects_locations_rag_corpora_rag_files_delete_builder, aiplatform_projects_locations_rag_corpora_rag_files_delete_task,
    aiplatform_projects_locations_rag_corpora_rag_files_import_builder, aiplatform_projects_locations_rag_corpora_rag_files_import_task,
    aiplatform_projects_locations_rag_corpora_rag_files_operations_cancel_builder, aiplatform_projects_locations_rag_corpora_rag_files_operations_cancel_task,
    aiplatform_projects_locations_rag_corpora_rag_files_operations_delete_builder, aiplatform_projects_locations_rag_corpora_rag_files_operations_delete_task,
    aiplatform_projects_locations_rag_corpora_rag_files_operations_wait_builder, aiplatform_projects_locations_rag_corpora_rag_files_operations_wait_task,
    aiplatform_projects_locations_rag_engine_config_operations_cancel_builder, aiplatform_projects_locations_rag_engine_config_operations_cancel_task,
    aiplatform_projects_locations_rag_engine_config_operations_delete_builder, aiplatform_projects_locations_rag_engine_config_operations_delete_task,
    aiplatform_projects_locations_rag_engine_config_operations_wait_builder, aiplatform_projects_locations_rag_engine_config_operations_wait_task,
    aiplatform_projects_locations_reasoning_engines_create_builder, aiplatform_projects_locations_reasoning_engines_create_task,
    aiplatform_projects_locations_reasoning_engines_delete_builder, aiplatform_projects_locations_reasoning_engines_delete_task,
    aiplatform_projects_locations_reasoning_engines_execute_code_builder, aiplatform_projects_locations_reasoning_engines_execute_code_task,
    aiplatform_projects_locations_reasoning_engines_get_iam_policy_builder, aiplatform_projects_locations_reasoning_engines_get_iam_policy_task,
    aiplatform_projects_locations_reasoning_engines_patch_builder, aiplatform_projects_locations_reasoning_engines_patch_task,
    aiplatform_projects_locations_reasoning_engines_query_builder, aiplatform_projects_locations_reasoning_engines_query_task,
    aiplatform_projects_locations_reasoning_engines_set_iam_policy_builder, aiplatform_projects_locations_reasoning_engines_set_iam_policy_task,
    aiplatform_projects_locations_reasoning_engines_stream_query_builder, aiplatform_projects_locations_reasoning_engines_stream_query_task,
    aiplatform_projects_locations_reasoning_engines_test_iam_permissions_builder, aiplatform_projects_locations_reasoning_engines_test_iam_permissions_task,
    aiplatform_projects_locations_reasoning_engines_memories_create_builder, aiplatform_projects_locations_reasoning_engines_memories_create_task,
    aiplatform_projects_locations_reasoning_engines_memories_delete_builder, aiplatform_projects_locations_reasoning_engines_memories_delete_task,
    aiplatform_projects_locations_reasoning_engines_memories_generate_builder, aiplatform_projects_locations_reasoning_engines_memories_generate_task,
    aiplatform_projects_locations_reasoning_engines_memories_patch_builder, aiplatform_projects_locations_reasoning_engines_memories_patch_task,
    aiplatform_projects_locations_reasoning_engines_memories_purge_builder, aiplatform_projects_locations_reasoning_engines_memories_purge_task,
    aiplatform_projects_locations_reasoning_engines_memories_retrieve_builder, aiplatform_projects_locations_reasoning_engines_memories_retrieve_task,
    aiplatform_projects_locations_reasoning_engines_memories_rollback_builder, aiplatform_projects_locations_reasoning_engines_memories_rollback_task,
    aiplatform_projects_locations_reasoning_engines_memories_operations_cancel_builder, aiplatform_projects_locations_reasoning_engines_memories_operations_cancel_task,
    aiplatform_projects_locations_reasoning_engines_memories_operations_delete_builder, aiplatform_projects_locations_reasoning_engines_memories_operations_delete_task,
    aiplatform_projects_locations_reasoning_engines_memories_operations_wait_builder, aiplatform_projects_locations_reasoning_engines_memories_operations_wait_task,
    aiplatform_projects_locations_reasoning_engines_operations_cancel_builder, aiplatform_projects_locations_reasoning_engines_operations_cancel_task,
    aiplatform_projects_locations_reasoning_engines_operations_delete_builder, aiplatform_projects_locations_reasoning_engines_operations_delete_task,
    aiplatform_projects_locations_reasoning_engines_operations_wait_builder, aiplatform_projects_locations_reasoning_engines_operations_wait_task,
    aiplatform_projects_locations_reasoning_engines_sandbox_environments_create_builder, aiplatform_projects_locations_reasoning_engines_sandbox_environments_create_task,
    aiplatform_projects_locations_reasoning_engines_sandbox_environments_delete_builder, aiplatform_projects_locations_reasoning_engines_sandbox_environments_delete_task,
    aiplatform_projects_locations_reasoning_engines_sandbox_environments_execute_builder, aiplatform_projects_locations_reasoning_engines_sandbox_environments_execute_task,
    aiplatform_projects_locations_reasoning_engines_sandbox_environments_operations_cancel_builder, aiplatform_projects_locations_reasoning_engines_sandbox_environments_operations_cancel_task,
    aiplatform_projects_locations_reasoning_engines_sandbox_environments_operations_delete_builder, aiplatform_projects_locations_reasoning_engines_sandbox_environments_operations_delete_task,
    aiplatform_projects_locations_reasoning_engines_sandbox_environments_operations_wait_builder, aiplatform_projects_locations_reasoning_engines_sandbox_environments_operations_wait_task,
    aiplatform_projects_locations_reasoning_engines_sessions_append_event_builder, aiplatform_projects_locations_reasoning_engines_sessions_append_event_task,
    aiplatform_projects_locations_reasoning_engines_sessions_create_builder, aiplatform_projects_locations_reasoning_engines_sessions_create_task,
    aiplatform_projects_locations_reasoning_engines_sessions_delete_builder, aiplatform_projects_locations_reasoning_engines_sessions_delete_task,
    aiplatform_projects_locations_reasoning_engines_sessions_patch_builder, aiplatform_projects_locations_reasoning_engines_sessions_patch_task,
    aiplatform_projects_locations_reasoning_engines_sessions_operations_cancel_builder, aiplatform_projects_locations_reasoning_engines_sessions_operations_cancel_task,
    aiplatform_projects_locations_reasoning_engines_sessions_operations_delete_builder, aiplatform_projects_locations_reasoning_engines_sessions_operations_delete_task,
    aiplatform_projects_locations_reasoning_engines_sessions_operations_wait_builder, aiplatform_projects_locations_reasoning_engines_sessions_operations_wait_task,
    aiplatform_projects_locations_schedules_create_builder, aiplatform_projects_locations_schedules_create_task,
    aiplatform_projects_locations_schedules_delete_builder, aiplatform_projects_locations_schedules_delete_task,
    aiplatform_projects_locations_schedules_patch_builder, aiplatform_projects_locations_schedules_patch_task,
    aiplatform_projects_locations_schedules_pause_builder, aiplatform_projects_locations_schedules_pause_task,
    aiplatform_projects_locations_schedules_resume_builder, aiplatform_projects_locations_schedules_resume_task,
    aiplatform_projects_locations_schedules_operations_cancel_builder, aiplatform_projects_locations_schedules_operations_cancel_task,
    aiplatform_projects_locations_schedules_operations_delete_builder, aiplatform_projects_locations_schedules_operations_delete_task,
    aiplatform_projects_locations_schedules_operations_wait_builder, aiplatform_projects_locations_schedules_operations_wait_task,
    aiplatform_projects_locations_specialist_pools_create_builder, aiplatform_projects_locations_specialist_pools_create_task,
    aiplatform_projects_locations_specialist_pools_delete_builder, aiplatform_projects_locations_specialist_pools_delete_task,
    aiplatform_projects_locations_specialist_pools_patch_builder, aiplatform_projects_locations_specialist_pools_patch_task,
    aiplatform_projects_locations_specialist_pools_operations_cancel_builder, aiplatform_projects_locations_specialist_pools_operations_cancel_task,
    aiplatform_projects_locations_specialist_pools_operations_delete_builder, aiplatform_projects_locations_specialist_pools_operations_delete_task,
    aiplatform_projects_locations_specialist_pools_operations_wait_builder, aiplatform_projects_locations_specialist_pools_operations_wait_task,
    aiplatform_projects_locations_studies_create_builder, aiplatform_projects_locations_studies_create_task,
    aiplatform_projects_locations_studies_delete_builder, aiplatform_projects_locations_studies_delete_task,
    aiplatform_projects_locations_studies_lookup_builder, aiplatform_projects_locations_studies_lookup_task,
    aiplatform_projects_locations_studies_operations_cancel_builder, aiplatform_projects_locations_studies_operations_cancel_task,
    aiplatform_projects_locations_studies_operations_delete_builder, aiplatform_projects_locations_studies_operations_delete_task,
    aiplatform_projects_locations_studies_operations_wait_builder, aiplatform_projects_locations_studies_operations_wait_task,
    aiplatform_projects_locations_studies_trials_add_trial_measurement_builder, aiplatform_projects_locations_studies_trials_add_trial_measurement_task,
    aiplatform_projects_locations_studies_trials_check_trial_early_stopping_state_builder, aiplatform_projects_locations_studies_trials_check_trial_early_stopping_state_task,
    aiplatform_projects_locations_studies_trials_complete_builder, aiplatform_projects_locations_studies_trials_complete_task,
    aiplatform_projects_locations_studies_trials_create_builder, aiplatform_projects_locations_studies_trials_create_task,
    aiplatform_projects_locations_studies_trials_delete_builder, aiplatform_projects_locations_studies_trials_delete_task,
    aiplatform_projects_locations_studies_trials_list_optimal_trials_builder, aiplatform_projects_locations_studies_trials_list_optimal_trials_task,
    aiplatform_projects_locations_studies_trials_stop_builder, aiplatform_projects_locations_studies_trials_stop_task,
    aiplatform_projects_locations_studies_trials_suggest_builder, aiplatform_projects_locations_studies_trials_suggest_task,
    aiplatform_projects_locations_studies_trials_operations_cancel_builder, aiplatform_projects_locations_studies_trials_operations_cancel_task,
    aiplatform_projects_locations_studies_trials_operations_delete_builder, aiplatform_projects_locations_studies_trials_operations_delete_task,
    aiplatform_projects_locations_studies_trials_operations_wait_builder, aiplatform_projects_locations_studies_trials_operations_wait_task,
    aiplatform_projects_locations_tensorboards_create_builder, aiplatform_projects_locations_tensorboards_create_task,
    aiplatform_projects_locations_tensorboards_delete_builder, aiplatform_projects_locations_tensorboards_delete_task,
    aiplatform_projects_locations_tensorboards_patch_builder, aiplatform_projects_locations_tensorboards_patch_task,
    aiplatform_projects_locations_tensorboards_experiments_batch_create_builder, aiplatform_projects_locations_tensorboards_experiments_batch_create_task,
    aiplatform_projects_locations_tensorboards_experiments_create_builder, aiplatform_projects_locations_tensorboards_experiments_create_task,
    aiplatform_projects_locations_tensorboards_experiments_delete_builder, aiplatform_projects_locations_tensorboards_experiments_delete_task,
    aiplatform_projects_locations_tensorboards_experiments_patch_builder, aiplatform_projects_locations_tensorboards_experiments_patch_task,
    aiplatform_projects_locations_tensorboards_experiments_write_builder, aiplatform_projects_locations_tensorboards_experiments_write_task,
    aiplatform_projects_locations_tensorboards_experiments_operations_cancel_builder, aiplatform_projects_locations_tensorboards_experiments_operations_cancel_task,
    aiplatform_projects_locations_tensorboards_experiments_operations_delete_builder, aiplatform_projects_locations_tensorboards_experiments_operations_delete_task,
    aiplatform_projects_locations_tensorboards_experiments_operations_wait_builder, aiplatform_projects_locations_tensorboards_experiments_operations_wait_task,
    aiplatform_projects_locations_tensorboards_experiments_runs_batch_create_builder, aiplatform_projects_locations_tensorboards_experiments_runs_batch_create_task,
    aiplatform_projects_locations_tensorboards_experiments_runs_create_builder, aiplatform_projects_locations_tensorboards_experiments_runs_create_task,
    aiplatform_projects_locations_tensorboards_experiments_runs_delete_builder, aiplatform_projects_locations_tensorboards_experiments_runs_delete_task,
    aiplatform_projects_locations_tensorboards_experiments_runs_patch_builder, aiplatform_projects_locations_tensorboards_experiments_runs_patch_task,
    aiplatform_projects_locations_tensorboards_experiments_runs_write_builder, aiplatform_projects_locations_tensorboards_experiments_runs_write_task,
    aiplatform_projects_locations_tensorboards_experiments_runs_operations_cancel_builder, aiplatform_projects_locations_tensorboards_experiments_runs_operations_cancel_task,
    aiplatform_projects_locations_tensorboards_experiments_runs_operations_delete_builder, aiplatform_projects_locations_tensorboards_experiments_runs_operations_delete_task,
    aiplatform_projects_locations_tensorboards_experiments_runs_operations_wait_builder, aiplatform_projects_locations_tensorboards_experiments_runs_operations_wait_task,
    aiplatform_projects_locations_tensorboards_experiments_runs_time_series_create_builder, aiplatform_projects_locations_tensorboards_experiments_runs_time_series_create_task,
    aiplatform_projects_locations_tensorboards_experiments_runs_time_series_delete_builder, aiplatform_projects_locations_tensorboards_experiments_runs_time_series_delete_task,
    aiplatform_projects_locations_tensorboards_experiments_runs_time_series_export_tensorboard_time_series_builder, aiplatform_projects_locations_tensorboards_experiments_runs_time_series_export_tensorboard_time_series_task,
    aiplatform_projects_locations_tensorboards_experiments_runs_time_series_patch_builder, aiplatform_projects_locations_tensorboards_experiments_runs_time_series_patch_task,
    aiplatform_projects_locations_tensorboards_experiments_runs_time_series_operations_cancel_builder, aiplatform_projects_locations_tensorboards_experiments_runs_time_series_operations_cancel_task,
    aiplatform_projects_locations_tensorboards_experiments_runs_time_series_operations_delete_builder, aiplatform_projects_locations_tensorboards_experiments_runs_time_series_operations_delete_task,
    aiplatform_projects_locations_tensorboards_experiments_runs_time_series_operations_wait_builder, aiplatform_projects_locations_tensorboards_experiments_runs_time_series_operations_wait_task,
    aiplatform_projects_locations_tensorboards_operations_cancel_builder, aiplatform_projects_locations_tensorboards_operations_cancel_task,
    aiplatform_projects_locations_tensorboards_operations_delete_builder, aiplatform_projects_locations_tensorboards_operations_delete_task,
    aiplatform_projects_locations_tensorboards_operations_wait_builder, aiplatform_projects_locations_tensorboards_operations_wait_task,
    aiplatform_projects_locations_training_pipelines_cancel_builder, aiplatform_projects_locations_training_pipelines_cancel_task,
    aiplatform_projects_locations_training_pipelines_create_builder, aiplatform_projects_locations_training_pipelines_create_task,
    aiplatform_projects_locations_training_pipelines_delete_builder, aiplatform_projects_locations_training_pipelines_delete_task,
    aiplatform_projects_locations_training_pipelines_operations_cancel_builder, aiplatform_projects_locations_training_pipelines_operations_cancel_task,
    aiplatform_projects_locations_training_pipelines_operations_delete_builder, aiplatform_projects_locations_training_pipelines_operations_delete_task,
    aiplatform_projects_locations_training_pipelines_operations_wait_builder, aiplatform_projects_locations_training_pipelines_operations_wait_task,
    aiplatform_projects_locations_tuning_jobs_cancel_builder, aiplatform_projects_locations_tuning_jobs_cancel_task,
    aiplatform_projects_locations_tuning_jobs_create_builder, aiplatform_projects_locations_tuning_jobs_create_task,
    aiplatform_projects_locations_tuning_jobs_rebase_tuned_model_builder, aiplatform_projects_locations_tuning_jobs_rebase_tuned_model_task,
    aiplatform_projects_locations_tuning_jobs_operations_cancel_builder, aiplatform_projects_locations_tuning_jobs_operations_cancel_task,
    aiplatform_projects_locations_tuning_jobs_operations_delete_builder, aiplatform_projects_locations_tuning_jobs_operations_delete_task,
    aiplatform_publishers_models_compute_tokens_builder, aiplatform_publishers_models_compute_tokens_task,
    aiplatform_publishers_models_count_tokens_builder, aiplatform_publishers_models_count_tokens_task,
    aiplatform_publishers_models_fetch_predict_operation_builder, aiplatform_publishers_models_fetch_predict_operation_task,
    aiplatform_publishers_models_generate_content_builder, aiplatform_publishers_models_generate_content_task,
    aiplatform_publishers_models_predict_builder, aiplatform_publishers_models_predict_task,
    aiplatform_publishers_models_predict_long_running_builder, aiplatform_publishers_models_predict_long_running_task,
    aiplatform_publishers_models_stream_generate_content_builder, aiplatform_publishers_models_stream_generate_content_task,
    aiplatform_rag_corpora_operations_cancel_builder, aiplatform_rag_corpora_operations_cancel_task,
    aiplatform_rag_corpora_operations_delete_builder, aiplatform_rag_corpora_operations_delete_task,
    aiplatform_rag_corpora_operations_wait_builder, aiplatform_rag_corpora_operations_wait_task,
    aiplatform_rag_corpora_rag_files_operations_cancel_builder, aiplatform_rag_corpora_rag_files_operations_cancel_task,
    aiplatform_rag_corpora_rag_files_operations_delete_builder, aiplatform_rag_corpora_rag_files_operations_delete_task,
    aiplatform_rag_corpora_rag_files_operations_wait_builder, aiplatform_rag_corpora_rag_files_operations_wait_task,
    aiplatform_rag_engine_config_operations_cancel_builder, aiplatform_rag_engine_config_operations_cancel_task,
    aiplatform_rag_engine_config_operations_delete_builder, aiplatform_rag_engine_config_operations_delete_task,
    aiplatform_rag_engine_config_operations_wait_builder, aiplatform_rag_engine_config_operations_wait_task,
    aiplatform_reasoning_engines_create_builder, aiplatform_reasoning_engines_create_task,
    aiplatform_reasoning_engines_delete_builder, aiplatform_reasoning_engines_delete_task,
    aiplatform_reasoning_engines_execute_code_builder, aiplatform_reasoning_engines_execute_code_task,
    aiplatform_reasoning_engines_patch_builder, aiplatform_reasoning_engines_patch_task,
    aiplatform_reasoning_engines_query_builder, aiplatform_reasoning_engines_query_task,
    aiplatform_reasoning_engines_stream_query_builder, aiplatform_reasoning_engines_stream_query_task,
    aiplatform_reasoning_engines_memories_operations_cancel_builder, aiplatform_reasoning_engines_memories_operations_cancel_task,
    aiplatform_reasoning_engines_memories_operations_delete_builder, aiplatform_reasoning_engines_memories_operations_delete_task,
    aiplatform_reasoning_engines_memories_operations_wait_builder, aiplatform_reasoning_engines_memories_operations_wait_task,
    aiplatform_reasoning_engines_operations_cancel_builder, aiplatform_reasoning_engines_operations_cancel_task,
    aiplatform_reasoning_engines_operations_delete_builder, aiplatform_reasoning_engines_operations_delete_task,
    aiplatform_reasoning_engines_operations_wait_builder, aiplatform_reasoning_engines_operations_wait_task,
    aiplatform_reasoning_engines_sandbox_environments_create_builder, aiplatform_reasoning_engines_sandbox_environments_create_task,
    aiplatform_reasoning_engines_sandbox_environments_delete_builder, aiplatform_reasoning_engines_sandbox_environments_delete_task,
    aiplatform_reasoning_engines_sandbox_environments_execute_builder, aiplatform_reasoning_engines_sandbox_environments_execute_task,
    aiplatform_reasoning_engines_sandbox_environments_operations_cancel_builder, aiplatform_reasoning_engines_sandbox_environments_operations_cancel_task,
    aiplatform_reasoning_engines_sandbox_environments_operations_delete_builder, aiplatform_reasoning_engines_sandbox_environments_operations_delete_task,
    aiplatform_reasoning_engines_sandbox_environments_operations_wait_builder, aiplatform_reasoning_engines_sandbox_environments_operations_wait_task,
    aiplatform_reasoning_engines_sessions_operations_cancel_builder, aiplatform_reasoning_engines_sessions_operations_cancel_task,
    aiplatform_reasoning_engines_sessions_operations_delete_builder, aiplatform_reasoning_engines_sessions_operations_delete_task,
    aiplatform_reasoning_engines_sessions_operations_wait_builder, aiplatform_reasoning_engines_sessions_operations_wait_task,
    aiplatform_schedules_operations_cancel_builder, aiplatform_schedules_operations_cancel_task,
    aiplatform_schedules_operations_delete_builder, aiplatform_schedules_operations_delete_task,
    aiplatform_schedules_operations_wait_builder, aiplatform_schedules_operations_wait_task,
    aiplatform_specialist_pools_operations_cancel_builder, aiplatform_specialist_pools_operations_cancel_task,
    aiplatform_specialist_pools_operations_delete_builder, aiplatform_specialist_pools_operations_delete_task,
    aiplatform_specialist_pools_operations_wait_builder, aiplatform_specialist_pools_operations_wait_task,
    aiplatform_studies_operations_cancel_builder, aiplatform_studies_operations_cancel_task,
    aiplatform_studies_operations_delete_builder, aiplatform_studies_operations_delete_task,
    aiplatform_studies_operations_wait_builder, aiplatform_studies_operations_wait_task,
    aiplatform_studies_trials_operations_cancel_builder, aiplatform_studies_trials_operations_cancel_task,
    aiplatform_studies_trials_operations_delete_builder, aiplatform_studies_trials_operations_delete_task,
    aiplatform_studies_trials_operations_wait_builder, aiplatform_studies_trials_operations_wait_task,
    aiplatform_tensorboards_experiments_operations_cancel_builder, aiplatform_tensorboards_experiments_operations_cancel_task,
    aiplatform_tensorboards_experiments_operations_delete_builder, aiplatform_tensorboards_experiments_operations_delete_task,
    aiplatform_tensorboards_experiments_operations_wait_builder, aiplatform_tensorboards_experiments_operations_wait_task,
    aiplatform_tensorboards_experiments_runs_operations_cancel_builder, aiplatform_tensorboards_experiments_runs_operations_cancel_task,
    aiplatform_tensorboards_experiments_runs_operations_delete_builder, aiplatform_tensorboards_experiments_runs_operations_delete_task,
    aiplatform_tensorboards_experiments_runs_operations_wait_builder, aiplatform_tensorboards_experiments_runs_operations_wait_task,
    aiplatform_tensorboards_experiments_runs_time_series_operations_cancel_builder, aiplatform_tensorboards_experiments_runs_time_series_operations_cancel_task,
    aiplatform_tensorboards_experiments_runs_time_series_operations_delete_builder, aiplatform_tensorboards_experiments_runs_time_series_operations_delete_task,
    aiplatform_tensorboards_experiments_runs_time_series_operations_wait_builder, aiplatform_tensorboards_experiments_runs_time_series_operations_wait_task,
    aiplatform_tensorboards_operations_cancel_builder, aiplatform_tensorboards_operations_cancel_task,
    aiplatform_tensorboards_operations_delete_builder, aiplatform_tensorboards_operations_delete_task,
    aiplatform_tensorboards_operations_wait_builder, aiplatform_tensorboards_operations_wait_task,
    aiplatform_training_pipelines_operations_cancel_builder, aiplatform_training_pipelines_operations_cancel_task,
    aiplatform_training_pipelines_operations_delete_builder, aiplatform_training_pipelines_operations_delete_task,
    aiplatform_training_pipelines_operations_wait_builder, aiplatform_training_pipelines_operations_wait_task,
    aiplatform_tuning_jobs_operations_cancel_builder, aiplatform_tuning_jobs_operations_cancel_task,
    aiplatform_tuning_jobs_operations_delete_builder, aiplatform_tuning_jobs_operations_delete_task,
    aiplatform_evaluate_dataset_builder, aiplatform_evaluate_dataset_task,
    aiplatform_evaluate_instances_builder, aiplatform_evaluate_instances_task,
    aiplatform_generate_instance_rubrics_builder, aiplatform_generate_instance_rubrics_task,
};
use crate::providers::gcp::clients::types::{ApiError, ApiPending};
use crate::providers::gcp::clients::aiplatform::GoogleApiHttpBody;
use crate::providers::gcp::clients::aiplatform::GoogleCloudAiplatformV1AddContextArtifactsAndExecutionsResponse;
use crate::providers::gcp::clients::aiplatform::GoogleCloudAiplatformV1AddContextChildrenResponse;
use crate::providers::gcp::clients::aiplatform::GoogleCloudAiplatformV1AddExecutionEventsResponse;
use crate::providers::gcp::clients::aiplatform::GoogleCloudAiplatformV1AppendEventResponse;
use crate::providers::gcp::clients::aiplatform::GoogleCloudAiplatformV1Artifact;
use crate::providers::gcp::clients::aiplatform::GoogleCloudAiplatformV1AskContextsResponse;
use crate::providers::gcp::clients::aiplatform::GoogleCloudAiplatformV1AugmentPromptResponse;
use crate::providers::gcp::clients::aiplatform::GoogleCloudAiplatformV1BatchCreateTensorboardRunsResponse;
use crate::providers::gcp::clients::aiplatform::GoogleCloudAiplatformV1BatchCreateTensorboardTimeSeriesResponse;
use crate::providers::gcp::clients::aiplatform::GoogleCloudAiplatformV1BatchImportEvaluatedAnnotationsResponse;
use crate::providers::gcp::clients::aiplatform::GoogleCloudAiplatformV1BatchPredictionJob;
use crate::providers::gcp::clients::aiplatform::GoogleCloudAiplatformV1CachedContent;
use crate::providers::gcp::clients::aiplatform::GoogleCloudAiplatformV1ComputeTokensResponse;
use crate::providers::gcp::clients::aiplatform::GoogleCloudAiplatformV1Context;
use crate::providers::gcp::clients::aiplatform::GoogleCloudAiplatformV1CorroborateContentResponse;
use crate::providers::gcp::clients::aiplatform::GoogleCloudAiplatformV1CountTokensResponse;
use crate::providers::gcp::clients::aiplatform::GoogleCloudAiplatformV1CustomJob;
use crate::providers::gcp::clients::aiplatform::GoogleCloudAiplatformV1DataLabelingJob;
use crate::providers::gcp::clients::aiplatform::GoogleCloudAiplatformV1Dataset;
use crate::providers::gcp::clients::aiplatform::GoogleCloudAiplatformV1DatasetVersion;
use crate::providers::gcp::clients::aiplatform::GoogleCloudAiplatformV1DirectPredictResponse;
use crate::providers::gcp::clients::aiplatform::GoogleCloudAiplatformV1DirectRawPredictResponse;
use crate::providers::gcp::clients::aiplatform::GoogleCloudAiplatformV1EmbedContentResponse;
use crate::providers::gcp::clients::aiplatform::GoogleCloudAiplatformV1Endpoint;
use crate::providers::gcp::clients::aiplatform::GoogleCloudAiplatformV1EntityType;
use crate::providers::gcp::clients::aiplatform::GoogleCloudAiplatformV1EvaluateInstancesResponse;
use crate::providers::gcp::clients::aiplatform::GoogleCloudAiplatformV1EvaluationItem;
use crate::providers::gcp::clients::aiplatform::GoogleCloudAiplatformV1EvaluationRun;
use crate::providers::gcp::clients::aiplatform::GoogleCloudAiplatformV1EvaluationSet;
use crate::providers::gcp::clients::aiplatform::GoogleCloudAiplatformV1ExecuteCodeResponse;
use crate::providers::gcp::clients::aiplatform::GoogleCloudAiplatformV1ExecuteSandboxEnvironmentResponse;
use crate::providers::gcp::clients::aiplatform::GoogleCloudAiplatformV1Execution;
use crate::providers::gcp::clients::aiplatform::GoogleCloudAiplatformV1ExplainResponse;
use crate::providers::gcp::clients::aiplatform::GoogleCloudAiplatformV1ExportTensorboardTimeSeriesDataResponse;
use crate::providers::gcp::clients::aiplatform::GoogleCloudAiplatformV1Feature;
use crate::providers::gcp::clients::aiplatform::GoogleCloudAiplatformV1FeatureViewDirectWriteResponse;
use crate::providers::gcp::clients::aiplatform::GoogleCloudAiplatformV1FetchFeatureValuesResponse;
use crate::providers::gcp::clients::aiplatform::GoogleCloudAiplatformV1FindNeighborsResponse;
use crate::providers::gcp::clients::aiplatform::GoogleCloudAiplatformV1GenerateContentResponse;
use crate::providers::gcp::clients::aiplatform::GoogleCloudAiplatformV1GenerateFetchAccessTokenResponse;
use crate::providers::gcp::clients::aiplatform::GoogleCloudAiplatformV1GenerateInstanceRubricsResponse;
use crate::providers::gcp::clients::aiplatform::GoogleCloudAiplatformV1GenerateSyntheticDataResponse;
use crate::providers::gcp::clients::aiplatform::GoogleCloudAiplatformV1HyperparameterTuningJob;
use crate::providers::gcp::clients::aiplatform::GoogleCloudAiplatformV1IndexEndpoint;
use crate::providers::gcp::clients::aiplatform::GoogleCloudAiplatformV1ListOptimalTrialsResponse;
use crate::providers::gcp::clients::aiplatform::GoogleCloudAiplatformV1MetadataSchema;
use crate::providers::gcp::clients::aiplatform::GoogleCloudAiplatformV1Model;
use crate::providers::gcp::clients::aiplatform::GoogleCloudAiplatformV1ModelDeploymentMonitoringJob;
use crate::providers::gcp::clients::aiplatform::GoogleCloudAiplatformV1ModelEvaluation;
use crate::providers::gcp::clients::aiplatform::GoogleCloudAiplatformV1NasJob;
use crate::providers::gcp::clients::aiplatform::GoogleCloudAiplatformV1NotebookRuntimeTemplate;
use crate::providers::gcp::clients::aiplatform::GoogleCloudAiplatformV1PipelineJob;
use crate::providers::gcp::clients::aiplatform::GoogleCloudAiplatformV1PredictResponse;
use crate::providers::gcp::clients::aiplatform::GoogleCloudAiplatformV1QueryReasoningEngineResponse;
use crate::providers::gcp::clients::aiplatform::GoogleCloudAiplatformV1ReadFeatureValuesResponse;
use crate::providers::gcp::clients::aiplatform::GoogleCloudAiplatformV1ReadIndexDatapointsResponse;
use crate::providers::gcp::clients::aiplatform::GoogleCloudAiplatformV1RemoveContextChildrenResponse;
use crate::providers::gcp::clients::aiplatform::GoogleCloudAiplatformV1RemoveDatapointsResponse;
use crate::providers::gcp::clients::aiplatform::GoogleCloudAiplatformV1RetrieveContextsResponse;
use crate::providers::gcp::clients::aiplatform::GoogleCloudAiplatformV1RetrieveMemoriesResponse;
use crate::providers::gcp::clients::aiplatform::GoogleCloudAiplatformV1Schedule;
use crate::providers::gcp::clients::aiplatform::GoogleCloudAiplatformV1SearchMigratableResourcesResponse;
use crate::providers::gcp::clients::aiplatform::GoogleCloudAiplatformV1SearchModelDeploymentMonitoringStatsAnomaliesResponse;
use crate::providers::gcp::clients::aiplatform::GoogleCloudAiplatformV1SearchNearestEntitiesResponse;
use crate::providers::gcp::clients::aiplatform::GoogleCloudAiplatformV1Session;
use crate::providers::gcp::clients::aiplatform::GoogleCloudAiplatformV1StreamingPredictResponse;
use crate::providers::gcp::clients::aiplatform::GoogleCloudAiplatformV1Study;
use crate::providers::gcp::clients::aiplatform::GoogleCloudAiplatformV1SyncFeatureViewResponse;
use crate::providers::gcp::clients::aiplatform::GoogleCloudAiplatformV1TensorboardExperiment;
use crate::providers::gcp::clients::aiplatform::GoogleCloudAiplatformV1TensorboardRun;
use crate::providers::gcp::clients::aiplatform::GoogleCloudAiplatformV1TensorboardTimeSeries;
use crate::providers::gcp::clients::aiplatform::GoogleCloudAiplatformV1TrainingPipeline;
use crate::providers::gcp::clients::aiplatform::GoogleCloudAiplatformV1Trial;
use crate::providers::gcp::clients::aiplatform::GoogleCloudAiplatformV1TuningJob;
use crate::providers::gcp::clients::aiplatform::GoogleCloudAiplatformV1UploadRagFileResponse;
use crate::providers::gcp::clients::aiplatform::GoogleCloudAiplatformV1UpsertDatapointsResponse;
use crate::providers::gcp::clients::aiplatform::GoogleCloudAiplatformV1WriteFeatureValuesResponse;
use crate::providers::gcp::clients::aiplatform::GoogleCloudAiplatformV1WriteTensorboardExperimentDataResponse;
use crate::providers::gcp::clients::aiplatform::GoogleCloudAiplatformV1WriteTensorboardRunDataResponse;
use crate::providers::gcp::clients::aiplatform::GoogleIamV1Policy;
use crate::providers::gcp::clients::aiplatform::GoogleIamV1TestIamPermissionsResponse;
use crate::providers::gcp::clients::aiplatform::GoogleLongrunningOperation;
use crate::providers::gcp::clients::aiplatform::GoogleProtobufEmpty;
use crate::providers::gcp::clients::aiplatform::AiplatformBatchPredictionJobsCreateArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformCustomJobsOperationsCancelArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformCustomJobsOperationsDeleteArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformCustomJobsOperationsWaitArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformDataLabelingJobsOperationsCancelArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformDataLabelingJobsOperationsDeleteArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformDataLabelingJobsOperationsWaitArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformDatasetsAnnotationSpecsOperationsCancelArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformDatasetsAnnotationSpecsOperationsDeleteArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformDatasetsAnnotationSpecsOperationsWaitArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformDatasetsCreateArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformDatasetsDataItemsAnnotationsOperationsCancelArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformDatasetsDataItemsAnnotationsOperationsDeleteArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformDatasetsDataItemsAnnotationsOperationsWaitArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformDatasetsDataItemsOperationsCancelArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformDatasetsDataItemsOperationsDeleteArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformDatasetsDataItemsOperationsWaitArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformDatasetsDatasetVersionsCreateArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformDatasetsDatasetVersionsDeleteArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformDatasetsDatasetVersionsPatchArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformDatasetsDeleteArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformDatasetsOperationsCancelArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformDatasetsOperationsDeleteArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformDatasetsOperationsWaitArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformDatasetsPatchArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformDatasetsSavedQueriesOperationsCancelArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformDatasetsSavedQueriesOperationsDeleteArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformDatasetsSavedQueriesOperationsWaitArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformDeploymentResourcePoolsOperationsCancelArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformDeploymentResourcePoolsOperationsDeleteArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformDeploymentResourcePoolsOperationsWaitArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformEndpointsChatCompletionsArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformEndpointsComputeTokensArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformEndpointsCountTokensArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformEndpointsFetchPredictOperationArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformEndpointsGenerateContentArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformEndpointsOperationsCancelArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformEndpointsOperationsDeleteArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformEndpointsOperationsWaitArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformEndpointsPredictArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformEndpointsPredictLongRunningArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformEndpointsStreamGenerateContentArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformEvaluateDatasetArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformEvaluateInstancesArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformFeatureGroupsFeaturesOperationsDeleteArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformFeatureGroupsFeaturesOperationsWaitArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformFeatureGroupsOperationsDeleteArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformFeatureGroupsOperationsWaitArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformFeatureOnlineStoresFeatureViewsOperationsDeleteArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformFeatureOnlineStoresFeatureViewsOperationsWaitArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformFeatureOnlineStoresOperationsDeleteArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformFeatureOnlineStoresOperationsWaitArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformFeaturestoresEntityTypesFeaturesOperationsCancelArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformFeaturestoresEntityTypesFeaturesOperationsDeleteArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformFeaturestoresEntityTypesFeaturesOperationsWaitArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformFeaturestoresEntityTypesOperationsCancelArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformFeaturestoresEntityTypesOperationsDeleteArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformFeaturestoresEntityTypesOperationsWaitArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformFeaturestoresOperationsCancelArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformFeaturestoresOperationsDeleteArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformFeaturestoresOperationsWaitArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformGenerateInstanceRubricsArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformHyperparameterTuningJobsOperationsCancelArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformHyperparameterTuningJobsOperationsDeleteArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformHyperparameterTuningJobsOperationsWaitArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformIndexEndpointsOperationsCancelArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformIndexEndpointsOperationsDeleteArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformIndexEndpointsOperationsWaitArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformIndexesOperationsCancelArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformIndexesOperationsDeleteArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformIndexesOperationsWaitArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformMediaUploadArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformMetadataStoresArtifactsOperationsCancelArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformMetadataStoresArtifactsOperationsDeleteArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformMetadataStoresArtifactsOperationsWaitArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformMetadataStoresContextsOperationsCancelArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformMetadataStoresContextsOperationsDeleteArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformMetadataStoresContextsOperationsWaitArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformMetadataStoresExecutionsOperationsCancelArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformMetadataStoresExecutionsOperationsDeleteArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformMetadataStoresExecutionsOperationsWaitArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformMetadataStoresOperationsCancelArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformMetadataStoresOperationsDeleteArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformMetadataStoresOperationsWaitArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformMigratableResourcesOperationsCancelArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformMigratableResourcesOperationsDeleteArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformMigratableResourcesOperationsWaitArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformModelDeploymentMonitoringJobsOperationsCancelArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformModelDeploymentMonitoringJobsOperationsDeleteArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformModelDeploymentMonitoringJobsOperationsWaitArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformModelsEvaluationsOperationsCancelArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformModelsEvaluationsOperationsDeleteArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformModelsEvaluationsOperationsWaitArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformModelsOperationsCancelArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformModelsOperationsDeleteArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformModelsOperationsWaitArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformNotebookExecutionJobsOperationsCancelArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformNotebookExecutionJobsOperationsDeleteArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformNotebookExecutionJobsOperationsWaitArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformNotebookRuntimeTemplatesOperationsCancelArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformNotebookRuntimeTemplatesOperationsDeleteArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformNotebookRuntimeTemplatesOperationsWaitArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformNotebookRuntimesOperationsCancelArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformNotebookRuntimesOperationsDeleteArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformNotebookRuntimesOperationsWaitArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformOperationsCancelArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformOperationsDeleteArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformOperationsWaitArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformPersistentResourcesOperationsCancelArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformPersistentResourcesOperationsDeleteArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformPersistentResourcesOperationsWaitArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformPipelineJobsOperationsCancelArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformPipelineJobsOperationsDeleteArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformPipelineJobsOperationsWaitArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsAskContextsArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsAsyncRetrieveContextsArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsAugmentPromptArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsBatchPredictionJobsCancelArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsBatchPredictionJobsCreateArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsBatchPredictionJobsDeleteArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsCachedContentsCreateArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsCachedContentsDeleteArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsCachedContentsPatchArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsCorroborateContentArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsCustomJobsCancelArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsCustomJobsCreateArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsCustomJobsDeleteArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsCustomJobsOperationsCancelArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsCustomJobsOperationsDeleteArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsCustomJobsOperationsWaitArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsDataLabelingJobsCancelArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsDataLabelingJobsCreateArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsDataLabelingJobsDeleteArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsDataLabelingJobsOperationsCancelArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsDataLabelingJobsOperationsDeleteArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsDataLabelingJobsOperationsWaitArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsDatasetsAnnotationSpecsOperationsCancelArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsDatasetsAnnotationSpecsOperationsDeleteArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsDatasetsAnnotationSpecsOperationsWaitArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsDatasetsCreateArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsDatasetsDataItemsAnnotationsOperationsCancelArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsDatasetsDataItemsAnnotationsOperationsDeleteArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsDatasetsDataItemsAnnotationsOperationsWaitArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsDatasetsDataItemsOperationsCancelArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsDatasetsDataItemsOperationsDeleteArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsDatasetsDataItemsOperationsWaitArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsDatasetsDatasetVersionsCreateArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsDatasetsDatasetVersionsDeleteArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsDatasetsDatasetVersionsPatchArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsDatasetsDeleteArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsDatasetsExportArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsDatasetsGetIamPolicyArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsDatasetsImportArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsDatasetsOperationsCancelArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsDatasetsOperationsDeleteArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsDatasetsOperationsWaitArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsDatasetsPatchArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsDatasetsSavedQueriesDeleteArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsDatasetsSavedQueriesOperationsCancelArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsDatasetsSavedQueriesOperationsDeleteArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsDatasetsSavedQueriesOperationsWaitArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsDatasetsSetIamPolicyArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsDatasetsTestIamPermissionsArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsDeployArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsDeploymentResourcePoolsCreateArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsDeploymentResourcePoolsDeleteArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsDeploymentResourcePoolsOperationsCancelArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsDeploymentResourcePoolsOperationsDeleteArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsDeploymentResourcePoolsOperationsWaitArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsDeploymentResourcePoolsPatchArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsEndpointsChatCompletionsArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsEndpointsComputeTokensArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsEndpointsCountTokensArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsEndpointsCreateArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsEndpointsDeleteArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsEndpointsDeployModelArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsEndpointsDeployedModelsInvokeInvokeArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsEndpointsDirectPredictArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsEndpointsDirectRawPredictArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsEndpointsExplainArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsEndpointsFetchPredictOperationArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsEndpointsGenerateContentArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsEndpointsGoogleScienceInferenceArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsEndpointsInvokeInvokeArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsEndpointsMutateDeployedModelArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsEndpointsOpenapiCompletionsArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsEndpointsOpenapiEmbeddingsArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsEndpointsOpenapiResponsesArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsEndpointsOperationsCancelArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsEndpointsOperationsDeleteArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsEndpointsOperationsWaitArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsEndpointsPatchArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsEndpointsPredictArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsEndpointsPredictLongRunningArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsEndpointsRawPredictArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsEndpointsServerStreamingPredictArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsEndpointsStreamGenerateContentArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsEndpointsStreamRawPredictArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsEndpointsUndeployModelArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsEndpointsUpdateArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsEvaluateDatasetArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsEvaluateInstancesArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsEvaluationItemsCreateArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsEvaluationItemsDeleteArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsEvaluationRunsCancelArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsEvaluationRunsCreateArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsEvaluationRunsDeleteArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsEvaluationSetsCreateArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsEvaluationSetsDeleteArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsEvaluationSetsPatchArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsFeatureGroupsCreateArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsFeatureGroupsDeleteArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsFeatureGroupsFeaturesBatchCreateArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsFeatureGroupsFeaturesCreateArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsFeatureGroupsFeaturesDeleteArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsFeatureGroupsFeaturesOperationsDeleteArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsFeatureGroupsFeaturesOperationsWaitArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsFeatureGroupsFeaturesPatchArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsFeatureGroupsGetIamPolicyArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsFeatureGroupsOperationsDeleteArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsFeatureGroupsOperationsWaitArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsFeatureGroupsPatchArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsFeatureGroupsSetIamPolicyArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsFeatureGroupsTestIamPermissionsArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsFeatureOnlineStoresCreateArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsFeatureOnlineStoresDeleteArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsFeatureOnlineStoresFeatureViewsCreateArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsFeatureOnlineStoresFeatureViewsDeleteArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsFeatureOnlineStoresFeatureViewsDirectWriteArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsFeatureOnlineStoresFeatureViewsFetchFeatureValuesArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsFeatureOnlineStoresFeatureViewsGenerateFetchAccessTokenArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsFeatureOnlineStoresFeatureViewsGetIamPolicyArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsFeatureOnlineStoresFeatureViewsOperationsDeleteArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsFeatureOnlineStoresFeatureViewsOperationsWaitArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsFeatureOnlineStoresFeatureViewsPatchArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsFeatureOnlineStoresFeatureViewsSearchNearestEntitiesArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsFeatureOnlineStoresFeatureViewsSetIamPolicyArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsFeatureOnlineStoresFeatureViewsSyncArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsFeatureOnlineStoresFeatureViewsTestIamPermissionsArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsFeatureOnlineStoresGetIamPolicyArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsFeatureOnlineStoresOperationsDeleteArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsFeatureOnlineStoresOperationsWaitArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsFeatureOnlineStoresPatchArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsFeatureOnlineStoresSetIamPolicyArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsFeatureOnlineStoresTestIamPermissionsArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsFeaturestoresBatchReadFeatureValuesArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsFeaturestoresCreateArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsFeaturestoresDeleteArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsFeaturestoresEntityTypesCreateArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsFeaturestoresEntityTypesDeleteArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsFeaturestoresEntityTypesDeleteFeatureValuesArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsFeaturestoresEntityTypesExportFeatureValuesArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsFeaturestoresEntityTypesFeaturesBatchCreateArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsFeaturestoresEntityTypesFeaturesCreateArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsFeaturestoresEntityTypesFeaturesDeleteArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsFeaturestoresEntityTypesFeaturesOperationsCancelArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsFeaturestoresEntityTypesFeaturesOperationsDeleteArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsFeaturestoresEntityTypesFeaturesOperationsWaitArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsFeaturestoresEntityTypesFeaturesPatchArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsFeaturestoresEntityTypesGetIamPolicyArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsFeaturestoresEntityTypesImportFeatureValuesArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsFeaturestoresEntityTypesOperationsCancelArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsFeaturestoresEntityTypesOperationsDeleteArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsFeaturestoresEntityTypesOperationsWaitArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsFeaturestoresEntityTypesPatchArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsFeaturestoresEntityTypesReadFeatureValuesArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsFeaturestoresEntityTypesSetIamPolicyArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsFeaturestoresEntityTypesStreamingReadFeatureValuesArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsFeaturestoresEntityTypesTestIamPermissionsArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsFeaturestoresEntityTypesWriteFeatureValuesArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsFeaturestoresGetIamPolicyArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsFeaturestoresOperationsCancelArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsFeaturestoresOperationsDeleteArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsFeaturestoresOperationsWaitArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsFeaturestoresPatchArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsFeaturestoresSetIamPolicyArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsFeaturestoresTestIamPermissionsArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsGenerateInstanceRubricsArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsGenerateSyntheticDataArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsHyperparameterTuningJobsCancelArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsHyperparameterTuningJobsCreateArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsHyperparameterTuningJobsDeleteArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsHyperparameterTuningJobsOperationsCancelArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsHyperparameterTuningJobsOperationsDeleteArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsHyperparameterTuningJobsOperationsWaitArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsIndexEndpointsCreateArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsIndexEndpointsDeleteArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsIndexEndpointsDeployIndexArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsIndexEndpointsFindNeighborsArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsIndexEndpointsMutateDeployedIndexArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsIndexEndpointsOperationsCancelArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsIndexEndpointsOperationsDeleteArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsIndexEndpointsOperationsWaitArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsIndexEndpointsPatchArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsIndexEndpointsReadIndexDatapointsArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsIndexEndpointsUndeployIndexArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsIndexesCreateArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsIndexesDeleteArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsIndexesOperationsCancelArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsIndexesOperationsDeleteArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsIndexesOperationsWaitArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsIndexesPatchArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsIndexesRemoveDatapointsArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsIndexesUpsertDatapointsArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsMetadataStoresArtifactsCreateArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsMetadataStoresArtifactsDeleteArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsMetadataStoresArtifactsOperationsCancelArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsMetadataStoresArtifactsOperationsDeleteArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsMetadataStoresArtifactsOperationsWaitArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsMetadataStoresArtifactsPatchArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsMetadataStoresArtifactsPurgeArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsMetadataStoresContextsAddContextArtifactsAndExecutionsArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsMetadataStoresContextsAddContextChildrenArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsMetadataStoresContextsCreateArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsMetadataStoresContextsDeleteArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsMetadataStoresContextsOperationsCancelArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsMetadataStoresContextsOperationsDeleteArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsMetadataStoresContextsOperationsWaitArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsMetadataStoresContextsPatchArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsMetadataStoresContextsPurgeArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsMetadataStoresContextsRemoveContextChildrenArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsMetadataStoresCreateArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsMetadataStoresDeleteArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsMetadataStoresExecutionsAddExecutionEventsArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsMetadataStoresExecutionsCreateArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsMetadataStoresExecutionsDeleteArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsMetadataStoresExecutionsOperationsCancelArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsMetadataStoresExecutionsOperationsDeleteArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsMetadataStoresExecutionsOperationsWaitArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsMetadataStoresExecutionsPatchArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsMetadataStoresExecutionsPurgeArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsMetadataStoresMetadataSchemasCreateArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsMetadataStoresOperationsCancelArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsMetadataStoresOperationsDeleteArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsMetadataStoresOperationsWaitArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsMigratableResourcesBatchMigrateArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsMigratableResourcesOperationsCancelArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsMigratableResourcesOperationsDeleteArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsMigratableResourcesOperationsWaitArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsMigratableResourcesSearchArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsModelDeploymentMonitoringJobsCreateArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsModelDeploymentMonitoringJobsDeleteArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsModelDeploymentMonitoringJobsOperationsCancelArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsModelDeploymentMonitoringJobsOperationsDeleteArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsModelDeploymentMonitoringJobsOperationsWaitArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsModelDeploymentMonitoringJobsPatchArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsModelDeploymentMonitoringJobsPauseArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsModelDeploymentMonitoringJobsResumeArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsModelDeploymentMonitoringJobsSearchModelDeploymentMonitoringStatsAnomaliesArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsModelsCopyArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsModelsDeleteArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsModelsDeleteVersionArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsModelsEvaluationsImportArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsModelsEvaluationsOperationsCancelArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsModelsEvaluationsOperationsDeleteArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsModelsEvaluationsOperationsWaitArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsModelsEvaluationsSlicesBatchImportArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsModelsExportArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsModelsGetIamPolicyArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsModelsMergeVersionAliasesArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsModelsOperationsCancelArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsModelsOperationsDeleteArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsModelsOperationsWaitArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsModelsPatchArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsModelsSetIamPolicyArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsModelsTestIamPermissionsArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsModelsUpdateExplanationDatasetArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsModelsUploadArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsNasJobsCancelArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsNasJobsCreateArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsNasJobsDeleteArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsNotebookExecutionJobsCreateArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsNotebookExecutionJobsDeleteArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsNotebookExecutionJobsOperationsCancelArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsNotebookExecutionJobsOperationsDeleteArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsNotebookExecutionJobsOperationsWaitArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsNotebookRuntimeTemplatesCreateArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsNotebookRuntimeTemplatesDeleteArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsNotebookRuntimeTemplatesGetIamPolicyArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsNotebookRuntimeTemplatesOperationsCancelArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsNotebookRuntimeTemplatesOperationsDeleteArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsNotebookRuntimeTemplatesOperationsWaitArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsNotebookRuntimeTemplatesPatchArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsNotebookRuntimeTemplatesSetIamPolicyArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsNotebookRuntimeTemplatesTestIamPermissionsArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsNotebookRuntimesAssignArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsNotebookRuntimesDeleteArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsNotebookRuntimesOperationsCancelArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsNotebookRuntimesOperationsDeleteArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsNotebookRuntimesOperationsWaitArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsNotebookRuntimesStartArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsNotebookRuntimesStopArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsNotebookRuntimesUpgradeArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsOperationsCancelArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsOperationsDeleteArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsOperationsWaitArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsPersistentResourcesCreateArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsPersistentResourcesDeleteArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsPersistentResourcesOperationsCancelArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsPersistentResourcesOperationsDeleteArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsPersistentResourcesOperationsWaitArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsPersistentResourcesPatchArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsPersistentResourcesRebootArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsPipelineJobsBatchCancelArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsPipelineJobsBatchDeleteArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsPipelineJobsCancelArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsPipelineJobsCreateArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsPipelineJobsDeleteArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsPipelineJobsOperationsCancelArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsPipelineJobsOperationsDeleteArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsPipelineJobsOperationsWaitArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsPublishersModelsComputeTokensArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsPublishersModelsCountTokensArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsPublishersModelsEmbedContentArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsPublishersModelsFetchPredictOperationArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsPublishersModelsGenerateContentArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsPublishersModelsInvokeInvokeArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsPublishersModelsPredictArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsPublishersModelsPredictLongRunningArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsPublishersModelsRawPredictArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsPublishersModelsServerStreamingPredictArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsPublishersModelsStreamGenerateContentArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsPublishersModelsStreamRawPredictArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsRagCorporaCreateArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsRagCorporaDeleteArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsRagCorporaOperationsCancelArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsRagCorporaOperationsDeleteArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsRagCorporaOperationsWaitArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsRagCorporaPatchArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsRagCorporaRagFilesDeleteArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsRagCorporaRagFilesImportArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsRagCorporaRagFilesOperationsCancelArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsRagCorporaRagFilesOperationsDeleteArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsRagCorporaRagFilesOperationsWaitArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsRagEngineConfigOperationsCancelArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsRagEngineConfigOperationsDeleteArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsRagEngineConfigOperationsWaitArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsReasoningEnginesCreateArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsReasoningEnginesDeleteArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsReasoningEnginesExecuteCodeArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsReasoningEnginesGetIamPolicyArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsReasoningEnginesMemoriesCreateArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsReasoningEnginesMemoriesDeleteArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsReasoningEnginesMemoriesGenerateArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsReasoningEnginesMemoriesOperationsCancelArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsReasoningEnginesMemoriesOperationsDeleteArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsReasoningEnginesMemoriesOperationsWaitArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsReasoningEnginesMemoriesPatchArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsReasoningEnginesMemoriesPurgeArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsReasoningEnginesMemoriesRetrieveArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsReasoningEnginesMemoriesRollbackArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsReasoningEnginesOperationsCancelArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsReasoningEnginesOperationsDeleteArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsReasoningEnginesOperationsWaitArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsReasoningEnginesPatchArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsReasoningEnginesQueryArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsReasoningEnginesSandboxEnvironmentsCreateArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsReasoningEnginesSandboxEnvironmentsDeleteArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsReasoningEnginesSandboxEnvironmentsExecuteArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsReasoningEnginesSandboxEnvironmentsOperationsCancelArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsReasoningEnginesSandboxEnvironmentsOperationsDeleteArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsReasoningEnginesSandboxEnvironmentsOperationsWaitArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsReasoningEnginesSessionsAppendEventArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsReasoningEnginesSessionsCreateArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsReasoningEnginesSessionsDeleteArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsReasoningEnginesSessionsOperationsCancelArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsReasoningEnginesSessionsOperationsDeleteArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsReasoningEnginesSessionsOperationsWaitArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsReasoningEnginesSessionsPatchArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsReasoningEnginesSetIamPolicyArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsReasoningEnginesStreamQueryArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsReasoningEnginesTestIamPermissionsArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsRetrieveContextsArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsSchedulesCreateArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsSchedulesDeleteArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsSchedulesOperationsCancelArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsSchedulesOperationsDeleteArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsSchedulesOperationsWaitArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsSchedulesPatchArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsSchedulesPauseArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsSchedulesResumeArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsSpecialistPoolsCreateArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsSpecialistPoolsDeleteArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsSpecialistPoolsOperationsCancelArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsSpecialistPoolsOperationsDeleteArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsSpecialistPoolsOperationsWaitArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsSpecialistPoolsPatchArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsStudiesCreateArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsStudiesDeleteArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsStudiesLookupArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsStudiesOperationsCancelArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsStudiesOperationsDeleteArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsStudiesOperationsWaitArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsStudiesTrialsAddTrialMeasurementArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsStudiesTrialsCheckTrialEarlyStoppingStateArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsStudiesTrialsCompleteArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsStudiesTrialsCreateArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsStudiesTrialsDeleteArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsStudiesTrialsListOptimalTrialsArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsStudiesTrialsOperationsCancelArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsStudiesTrialsOperationsDeleteArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsStudiesTrialsOperationsWaitArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsStudiesTrialsStopArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsStudiesTrialsSuggestArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsTensorboardsCreateArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsTensorboardsDeleteArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsTensorboardsExperimentsBatchCreateArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsTensorboardsExperimentsCreateArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsTensorboardsExperimentsDeleteArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsTensorboardsExperimentsOperationsCancelArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsTensorboardsExperimentsOperationsDeleteArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsTensorboardsExperimentsOperationsWaitArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsTensorboardsExperimentsPatchArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsTensorboardsExperimentsRunsBatchCreateArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsTensorboardsExperimentsRunsCreateArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsTensorboardsExperimentsRunsDeleteArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsTensorboardsExperimentsRunsOperationsCancelArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsTensorboardsExperimentsRunsOperationsDeleteArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsTensorboardsExperimentsRunsOperationsWaitArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsTensorboardsExperimentsRunsPatchArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsTensorboardsExperimentsRunsTimeSeriesCreateArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsTensorboardsExperimentsRunsTimeSeriesDeleteArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsTensorboardsExperimentsRunsTimeSeriesExportTensorboardTimeSeriesArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsTensorboardsExperimentsRunsTimeSeriesOperationsCancelArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsTensorboardsExperimentsRunsTimeSeriesOperationsDeleteArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsTensorboardsExperimentsRunsTimeSeriesOperationsWaitArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsTensorboardsExperimentsRunsTimeSeriesPatchArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsTensorboardsExperimentsRunsWriteArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsTensorboardsExperimentsWriteArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsTensorboardsOperationsCancelArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsTensorboardsOperationsDeleteArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsTensorboardsOperationsWaitArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsTensorboardsPatchArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsTrainingPipelinesCancelArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsTrainingPipelinesCreateArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsTrainingPipelinesDeleteArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsTrainingPipelinesOperationsCancelArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsTrainingPipelinesOperationsDeleteArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsTrainingPipelinesOperationsWaitArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsTuningJobsCancelArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsTuningJobsCreateArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsTuningJobsOperationsCancelArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsTuningJobsOperationsDeleteArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsTuningJobsRebaseTunedModelArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsLocationsUpdateRagEngineConfigArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformProjectsUpdateCacheConfigArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformPublishersModelsComputeTokensArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformPublishersModelsCountTokensArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformPublishersModelsFetchPredictOperationArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformPublishersModelsGenerateContentArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformPublishersModelsPredictArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformPublishersModelsPredictLongRunningArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformPublishersModelsStreamGenerateContentArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformRagCorporaOperationsCancelArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformRagCorporaOperationsDeleteArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformRagCorporaOperationsWaitArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformRagCorporaRagFilesOperationsCancelArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformRagCorporaRagFilesOperationsDeleteArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformRagCorporaRagFilesOperationsWaitArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformRagEngineConfigOperationsCancelArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformRagEngineConfigOperationsDeleteArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformRagEngineConfigOperationsWaitArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformReasoningEnginesCreateArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformReasoningEnginesDeleteArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformReasoningEnginesExecuteCodeArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformReasoningEnginesMemoriesOperationsCancelArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformReasoningEnginesMemoriesOperationsDeleteArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformReasoningEnginesMemoriesOperationsWaitArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformReasoningEnginesOperationsCancelArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformReasoningEnginesOperationsDeleteArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformReasoningEnginesOperationsWaitArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformReasoningEnginesPatchArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformReasoningEnginesQueryArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformReasoningEnginesSandboxEnvironmentsCreateArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformReasoningEnginesSandboxEnvironmentsDeleteArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformReasoningEnginesSandboxEnvironmentsExecuteArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformReasoningEnginesSandboxEnvironmentsOperationsCancelArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformReasoningEnginesSandboxEnvironmentsOperationsDeleteArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformReasoningEnginesSandboxEnvironmentsOperationsWaitArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformReasoningEnginesSessionsOperationsCancelArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformReasoningEnginesSessionsOperationsDeleteArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformReasoningEnginesSessionsOperationsWaitArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformReasoningEnginesStreamQueryArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformSchedulesOperationsCancelArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformSchedulesOperationsDeleteArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformSchedulesOperationsWaitArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformSpecialistPoolsOperationsCancelArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformSpecialistPoolsOperationsDeleteArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformSpecialistPoolsOperationsWaitArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformStudiesOperationsCancelArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformStudiesOperationsDeleteArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformStudiesOperationsWaitArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformStudiesTrialsOperationsCancelArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformStudiesTrialsOperationsDeleteArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformStudiesTrialsOperationsWaitArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformTensorboardsExperimentsOperationsCancelArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformTensorboardsExperimentsOperationsDeleteArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformTensorboardsExperimentsOperationsWaitArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformTensorboardsExperimentsRunsOperationsCancelArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformTensorboardsExperimentsRunsOperationsDeleteArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformTensorboardsExperimentsRunsOperationsWaitArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformTensorboardsExperimentsRunsTimeSeriesOperationsCancelArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformTensorboardsExperimentsRunsTimeSeriesOperationsDeleteArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformTensorboardsExperimentsRunsTimeSeriesOperationsWaitArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformTensorboardsOperationsCancelArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformTensorboardsOperationsDeleteArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformTensorboardsOperationsWaitArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformTrainingPipelinesOperationsCancelArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformTrainingPipelinesOperationsDeleteArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformTrainingPipelinesOperationsWaitArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformTuningJobsOperationsCancelArgs;
use crate::providers::gcp::clients::aiplatform::AiplatformTuningJobsOperationsDeleteArgs;
use crate::provider_client::{ProviderClient, ProviderError};
use foundation_core::valtron::{execute, StreamIterator};
use foundation_core::wire::simple_http::client::SimpleHttpClient;
use foundation_db::state::store_state_task::StoreStateIdentifierTask;
use std::sync::Arc;

/// AiplatformProvider with automatic state tracking.
///
/// # Type Parameters
///
/// * `S` - StateStore implementation (FileStateStore, SqliteStateStore, etc.)
///
/// # Example
///
/// ```rust
/// let state_store = FileStateStore::new("/path", "my-project", "dev");
/// let client = ProviderClient::new("my-project", "dev", state_store);
/// let http_client = SimpleHttpClient::new(...);
/// let provider = AiplatformProvider::new(client, http_client);
/// ```
#[derive(Clone)]
pub struct AiplatformProvider<S>
where
    S: foundation_db::state::traits::StateStore + Send + Sync + 'static,
{
    client: ProviderClient<S>,
    http_client: Arc<SimpleHttpClient>,
}

impl<S> AiplatformProvider<S>
where
    S: foundation_db::state::traits::StateStore + Send + Sync + 'static,
{
    /// Create new AiplatformProvider.
    pub fn new(client: ProviderClient<S>, http_client: SimpleHttpClient) -> Self {
        Self {
            client,
            http_client: Arc::new(http_client),
        }
    }

    /// Aiplatform batch prediction jobs create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudAiplatformV1BatchPredictionJob result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_batch_prediction_jobs_create(
        &self,
        args: &AiplatformBatchPredictionJobsCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudAiplatformV1BatchPredictionJob, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_batch_prediction_jobs_create_builder(
            &self.http_client,
            &args.parent,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_batch_prediction_jobs_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform custom jobs operations cancel.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_custom_jobs_operations_cancel(
        &self,
        args: &AiplatformCustomJobsOperationsCancelArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_custom_jobs_operations_cancel_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_custom_jobs_operations_cancel_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform custom jobs operations delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_custom_jobs_operations_delete(
        &self,
        args: &AiplatformCustomJobsOperationsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_custom_jobs_operations_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_custom_jobs_operations_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform custom jobs operations wait.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_custom_jobs_operations_wait(
        &self,
        args: &AiplatformCustomJobsOperationsWaitArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_custom_jobs_operations_wait_builder(
            &self.http_client,
            &args.name,
            &args.timeout,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_custom_jobs_operations_wait_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform data labeling jobs operations cancel.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_data_labeling_jobs_operations_cancel(
        &self,
        args: &AiplatformDataLabelingJobsOperationsCancelArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_data_labeling_jobs_operations_cancel_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_data_labeling_jobs_operations_cancel_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform data labeling jobs operations delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_data_labeling_jobs_operations_delete(
        &self,
        args: &AiplatformDataLabelingJobsOperationsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_data_labeling_jobs_operations_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_data_labeling_jobs_operations_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform data labeling jobs operations wait.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_data_labeling_jobs_operations_wait(
        &self,
        args: &AiplatformDataLabelingJobsOperationsWaitArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_data_labeling_jobs_operations_wait_builder(
            &self.http_client,
            &args.name,
            &args.timeout,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_data_labeling_jobs_operations_wait_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform datasets create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_datasets_create(
        &self,
        args: &AiplatformDatasetsCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_datasets_create_builder(
            &self.http_client,
            &args.parent,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_datasets_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform datasets delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_datasets_delete(
        &self,
        args: &AiplatformDatasetsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_datasets_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_datasets_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform datasets patch.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudAiplatformV1Dataset result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_datasets_patch(
        &self,
        args: &AiplatformDatasetsPatchArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudAiplatformV1Dataset, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_datasets_patch_builder(
            &self.http_client,
            &args.name,
            &args.updateMask,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_datasets_patch_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform datasets annotation specs operations cancel.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_datasets_annotation_specs_operations_cancel(
        &self,
        args: &AiplatformDatasetsAnnotationSpecsOperationsCancelArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_datasets_annotation_specs_operations_cancel_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_datasets_annotation_specs_operations_cancel_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform datasets annotation specs operations delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_datasets_annotation_specs_operations_delete(
        &self,
        args: &AiplatformDatasetsAnnotationSpecsOperationsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_datasets_annotation_specs_operations_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_datasets_annotation_specs_operations_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform datasets annotation specs operations wait.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_datasets_annotation_specs_operations_wait(
        &self,
        args: &AiplatformDatasetsAnnotationSpecsOperationsWaitArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_datasets_annotation_specs_operations_wait_builder(
            &self.http_client,
            &args.name,
            &args.timeout,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_datasets_annotation_specs_operations_wait_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform datasets data items annotations operations cancel.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_datasets_data_items_annotations_operations_cancel(
        &self,
        args: &AiplatformDatasetsDataItemsAnnotationsOperationsCancelArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_datasets_data_items_annotations_operations_cancel_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_datasets_data_items_annotations_operations_cancel_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform datasets data items annotations operations delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_datasets_data_items_annotations_operations_delete(
        &self,
        args: &AiplatformDatasetsDataItemsAnnotationsOperationsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_datasets_data_items_annotations_operations_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_datasets_data_items_annotations_operations_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform datasets data items annotations operations wait.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_datasets_data_items_annotations_operations_wait(
        &self,
        args: &AiplatformDatasetsDataItemsAnnotationsOperationsWaitArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_datasets_data_items_annotations_operations_wait_builder(
            &self.http_client,
            &args.name,
            &args.timeout,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_datasets_data_items_annotations_operations_wait_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform datasets data items operations cancel.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_datasets_data_items_operations_cancel(
        &self,
        args: &AiplatformDatasetsDataItemsOperationsCancelArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_datasets_data_items_operations_cancel_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_datasets_data_items_operations_cancel_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform datasets data items operations delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_datasets_data_items_operations_delete(
        &self,
        args: &AiplatformDatasetsDataItemsOperationsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_datasets_data_items_operations_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_datasets_data_items_operations_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform datasets data items operations wait.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_datasets_data_items_operations_wait(
        &self,
        args: &AiplatformDatasetsDataItemsOperationsWaitArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_datasets_data_items_operations_wait_builder(
            &self.http_client,
            &args.name,
            &args.timeout,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_datasets_data_items_operations_wait_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform datasets dataset versions create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_datasets_dataset_versions_create(
        &self,
        args: &AiplatformDatasetsDatasetVersionsCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_datasets_dataset_versions_create_builder(
            &self.http_client,
            &args.parent,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_datasets_dataset_versions_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform datasets dataset versions delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_datasets_dataset_versions_delete(
        &self,
        args: &AiplatformDatasetsDatasetVersionsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_datasets_dataset_versions_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_datasets_dataset_versions_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform datasets dataset versions patch.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudAiplatformV1DatasetVersion result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_datasets_dataset_versions_patch(
        &self,
        args: &AiplatformDatasetsDatasetVersionsPatchArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudAiplatformV1DatasetVersion, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_datasets_dataset_versions_patch_builder(
            &self.http_client,
            &args.name,
            &args.updateMask,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_datasets_dataset_versions_patch_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform datasets operations cancel.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_datasets_operations_cancel(
        &self,
        args: &AiplatformDatasetsOperationsCancelArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_datasets_operations_cancel_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_datasets_operations_cancel_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform datasets operations delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_datasets_operations_delete(
        &self,
        args: &AiplatformDatasetsOperationsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_datasets_operations_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_datasets_operations_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform datasets operations wait.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_datasets_operations_wait(
        &self,
        args: &AiplatformDatasetsOperationsWaitArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_datasets_operations_wait_builder(
            &self.http_client,
            &args.name,
            &args.timeout,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_datasets_operations_wait_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform datasets saved queries operations cancel.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_datasets_saved_queries_operations_cancel(
        &self,
        args: &AiplatformDatasetsSavedQueriesOperationsCancelArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_datasets_saved_queries_operations_cancel_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_datasets_saved_queries_operations_cancel_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform datasets saved queries operations delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_datasets_saved_queries_operations_delete(
        &self,
        args: &AiplatformDatasetsSavedQueriesOperationsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_datasets_saved_queries_operations_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_datasets_saved_queries_operations_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform datasets saved queries operations wait.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_datasets_saved_queries_operations_wait(
        &self,
        args: &AiplatformDatasetsSavedQueriesOperationsWaitArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_datasets_saved_queries_operations_wait_builder(
            &self.http_client,
            &args.name,
            &args.timeout,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_datasets_saved_queries_operations_wait_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform deployment resource pools operations cancel.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_deployment_resource_pools_operations_cancel(
        &self,
        args: &AiplatformDeploymentResourcePoolsOperationsCancelArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_deployment_resource_pools_operations_cancel_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_deployment_resource_pools_operations_cancel_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform deployment resource pools operations delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_deployment_resource_pools_operations_delete(
        &self,
        args: &AiplatformDeploymentResourcePoolsOperationsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_deployment_resource_pools_operations_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_deployment_resource_pools_operations_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform deployment resource pools operations wait.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_deployment_resource_pools_operations_wait(
        &self,
        args: &AiplatformDeploymentResourcePoolsOperationsWaitArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_deployment_resource_pools_operations_wait_builder(
            &self.http_client,
            &args.name,
            &args.timeout,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_deployment_resource_pools_operations_wait_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform endpoints compute tokens.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudAiplatformV1ComputeTokensResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_endpoints_compute_tokens(
        &self,
        args: &AiplatformEndpointsComputeTokensArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudAiplatformV1ComputeTokensResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_endpoints_compute_tokens_builder(
            &self.http_client,
            &args.endpoint,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_endpoints_compute_tokens_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform endpoints count tokens.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudAiplatformV1CountTokensResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_endpoints_count_tokens(
        &self,
        args: &AiplatformEndpointsCountTokensArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudAiplatformV1CountTokensResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_endpoints_count_tokens_builder(
            &self.http_client,
            &args.endpoint,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_endpoints_count_tokens_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform endpoints fetch predict operation.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_endpoints_fetch_predict_operation(
        &self,
        args: &AiplatformEndpointsFetchPredictOperationArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_endpoints_fetch_predict_operation_builder(
            &self.http_client,
            &args.endpoint,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_endpoints_fetch_predict_operation_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform endpoints generate content.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudAiplatformV1GenerateContentResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_endpoints_generate_content(
        &self,
        args: &AiplatformEndpointsGenerateContentArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudAiplatformV1GenerateContentResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_endpoints_generate_content_builder(
            &self.http_client,
            &args.model,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_endpoints_generate_content_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform endpoints predict.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudAiplatformV1PredictResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_endpoints_predict(
        &self,
        args: &AiplatformEndpointsPredictArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudAiplatformV1PredictResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_endpoints_predict_builder(
            &self.http_client,
            &args.endpoint,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_endpoints_predict_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform endpoints predict long running.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_endpoints_predict_long_running(
        &self,
        args: &AiplatformEndpointsPredictLongRunningArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_endpoints_predict_long_running_builder(
            &self.http_client,
            &args.endpoint,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_endpoints_predict_long_running_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform endpoints stream generate content.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudAiplatformV1GenerateContentResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_endpoints_stream_generate_content(
        &self,
        args: &AiplatformEndpointsStreamGenerateContentArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudAiplatformV1GenerateContentResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_endpoints_stream_generate_content_builder(
            &self.http_client,
            &args.model,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_endpoints_stream_generate_content_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform endpoints chat completions.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleApiHttpBody result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_endpoints_chat_completions(
        &self,
        args: &AiplatformEndpointsChatCompletionsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleApiHttpBody, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_endpoints_chat_completions_builder(
            &self.http_client,
            &args.endpoint,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_endpoints_chat_completions_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform endpoints operations cancel.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_endpoints_operations_cancel(
        &self,
        args: &AiplatformEndpointsOperationsCancelArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_endpoints_operations_cancel_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_endpoints_operations_cancel_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform endpoints operations delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_endpoints_operations_delete(
        &self,
        args: &AiplatformEndpointsOperationsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_endpoints_operations_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_endpoints_operations_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform endpoints operations wait.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_endpoints_operations_wait(
        &self,
        args: &AiplatformEndpointsOperationsWaitArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_endpoints_operations_wait_builder(
            &self.http_client,
            &args.name,
            &args.timeout,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_endpoints_operations_wait_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform feature groups features operations delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_feature_groups_features_operations_delete(
        &self,
        args: &AiplatformFeatureGroupsFeaturesOperationsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_feature_groups_features_operations_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_feature_groups_features_operations_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform feature groups features operations wait.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_feature_groups_features_operations_wait(
        &self,
        args: &AiplatformFeatureGroupsFeaturesOperationsWaitArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_feature_groups_features_operations_wait_builder(
            &self.http_client,
            &args.name,
            &args.timeout,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_feature_groups_features_operations_wait_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform feature groups operations delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_feature_groups_operations_delete(
        &self,
        args: &AiplatformFeatureGroupsOperationsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_feature_groups_operations_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_feature_groups_operations_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform feature groups operations wait.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_feature_groups_operations_wait(
        &self,
        args: &AiplatformFeatureGroupsOperationsWaitArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_feature_groups_operations_wait_builder(
            &self.http_client,
            &args.name,
            &args.timeout,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_feature_groups_operations_wait_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform feature online stores feature views operations delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_feature_online_stores_feature_views_operations_delete(
        &self,
        args: &AiplatformFeatureOnlineStoresFeatureViewsOperationsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_feature_online_stores_feature_views_operations_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_feature_online_stores_feature_views_operations_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform feature online stores feature views operations wait.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_feature_online_stores_feature_views_operations_wait(
        &self,
        args: &AiplatformFeatureOnlineStoresFeatureViewsOperationsWaitArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_feature_online_stores_feature_views_operations_wait_builder(
            &self.http_client,
            &args.name,
            &args.timeout,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_feature_online_stores_feature_views_operations_wait_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform feature online stores operations delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_feature_online_stores_operations_delete(
        &self,
        args: &AiplatformFeatureOnlineStoresOperationsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_feature_online_stores_operations_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_feature_online_stores_operations_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform feature online stores operations wait.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_feature_online_stores_operations_wait(
        &self,
        args: &AiplatformFeatureOnlineStoresOperationsWaitArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_feature_online_stores_operations_wait_builder(
            &self.http_client,
            &args.name,
            &args.timeout,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_feature_online_stores_operations_wait_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform featurestores entity types features operations cancel.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_featurestores_entity_types_features_operations_cancel(
        &self,
        args: &AiplatformFeaturestoresEntityTypesFeaturesOperationsCancelArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_featurestores_entity_types_features_operations_cancel_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_featurestores_entity_types_features_operations_cancel_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform featurestores entity types features operations delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_featurestores_entity_types_features_operations_delete(
        &self,
        args: &AiplatformFeaturestoresEntityTypesFeaturesOperationsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_featurestores_entity_types_features_operations_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_featurestores_entity_types_features_operations_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform featurestores entity types features operations wait.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_featurestores_entity_types_features_operations_wait(
        &self,
        args: &AiplatformFeaturestoresEntityTypesFeaturesOperationsWaitArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_featurestores_entity_types_features_operations_wait_builder(
            &self.http_client,
            &args.name,
            &args.timeout,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_featurestores_entity_types_features_operations_wait_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform featurestores entity types operations cancel.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_featurestores_entity_types_operations_cancel(
        &self,
        args: &AiplatformFeaturestoresEntityTypesOperationsCancelArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_featurestores_entity_types_operations_cancel_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_featurestores_entity_types_operations_cancel_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform featurestores entity types operations delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_featurestores_entity_types_operations_delete(
        &self,
        args: &AiplatformFeaturestoresEntityTypesOperationsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_featurestores_entity_types_operations_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_featurestores_entity_types_operations_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform featurestores entity types operations wait.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_featurestores_entity_types_operations_wait(
        &self,
        args: &AiplatformFeaturestoresEntityTypesOperationsWaitArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_featurestores_entity_types_operations_wait_builder(
            &self.http_client,
            &args.name,
            &args.timeout,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_featurestores_entity_types_operations_wait_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform featurestores operations cancel.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_featurestores_operations_cancel(
        &self,
        args: &AiplatformFeaturestoresOperationsCancelArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_featurestores_operations_cancel_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_featurestores_operations_cancel_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform featurestores operations delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_featurestores_operations_delete(
        &self,
        args: &AiplatformFeaturestoresOperationsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_featurestores_operations_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_featurestores_operations_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform featurestores operations wait.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_featurestores_operations_wait(
        &self,
        args: &AiplatformFeaturestoresOperationsWaitArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_featurestores_operations_wait_builder(
            &self.http_client,
            &args.name,
            &args.timeout,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_featurestores_operations_wait_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform hyperparameter tuning jobs operations cancel.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_hyperparameter_tuning_jobs_operations_cancel(
        &self,
        args: &AiplatformHyperparameterTuningJobsOperationsCancelArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_hyperparameter_tuning_jobs_operations_cancel_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_hyperparameter_tuning_jobs_operations_cancel_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform hyperparameter tuning jobs operations delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_hyperparameter_tuning_jobs_operations_delete(
        &self,
        args: &AiplatformHyperparameterTuningJobsOperationsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_hyperparameter_tuning_jobs_operations_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_hyperparameter_tuning_jobs_operations_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform hyperparameter tuning jobs operations wait.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_hyperparameter_tuning_jobs_operations_wait(
        &self,
        args: &AiplatformHyperparameterTuningJobsOperationsWaitArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_hyperparameter_tuning_jobs_operations_wait_builder(
            &self.http_client,
            &args.name,
            &args.timeout,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_hyperparameter_tuning_jobs_operations_wait_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform index endpoints operations cancel.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_index_endpoints_operations_cancel(
        &self,
        args: &AiplatformIndexEndpointsOperationsCancelArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_index_endpoints_operations_cancel_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_index_endpoints_operations_cancel_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform index endpoints operations delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_index_endpoints_operations_delete(
        &self,
        args: &AiplatformIndexEndpointsOperationsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_index_endpoints_operations_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_index_endpoints_operations_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform index endpoints operations wait.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_index_endpoints_operations_wait(
        &self,
        args: &AiplatformIndexEndpointsOperationsWaitArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_index_endpoints_operations_wait_builder(
            &self.http_client,
            &args.name,
            &args.timeout,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_index_endpoints_operations_wait_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform indexes operations cancel.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_indexes_operations_cancel(
        &self,
        args: &AiplatformIndexesOperationsCancelArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_indexes_operations_cancel_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_indexes_operations_cancel_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform indexes operations delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_indexes_operations_delete(
        &self,
        args: &AiplatformIndexesOperationsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_indexes_operations_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_indexes_operations_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform indexes operations wait.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_indexes_operations_wait(
        &self,
        args: &AiplatformIndexesOperationsWaitArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_indexes_operations_wait_builder(
            &self.http_client,
            &args.name,
            &args.timeout,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_indexes_operations_wait_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform media upload.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudAiplatformV1UploadRagFileResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_media_upload(
        &self,
        args: &AiplatformMediaUploadArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudAiplatformV1UploadRagFileResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_media_upload_builder(
            &self.http_client,
            &args.parent,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_media_upload_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform metadata stores artifacts operations cancel.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_metadata_stores_artifacts_operations_cancel(
        &self,
        args: &AiplatformMetadataStoresArtifactsOperationsCancelArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_metadata_stores_artifacts_operations_cancel_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_metadata_stores_artifacts_operations_cancel_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform metadata stores artifacts operations delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_metadata_stores_artifacts_operations_delete(
        &self,
        args: &AiplatformMetadataStoresArtifactsOperationsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_metadata_stores_artifacts_operations_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_metadata_stores_artifacts_operations_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform metadata stores artifacts operations wait.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_metadata_stores_artifacts_operations_wait(
        &self,
        args: &AiplatformMetadataStoresArtifactsOperationsWaitArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_metadata_stores_artifacts_operations_wait_builder(
            &self.http_client,
            &args.name,
            &args.timeout,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_metadata_stores_artifacts_operations_wait_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform metadata stores contexts operations cancel.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_metadata_stores_contexts_operations_cancel(
        &self,
        args: &AiplatformMetadataStoresContextsOperationsCancelArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_metadata_stores_contexts_operations_cancel_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_metadata_stores_contexts_operations_cancel_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform metadata stores contexts operations delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_metadata_stores_contexts_operations_delete(
        &self,
        args: &AiplatformMetadataStoresContextsOperationsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_metadata_stores_contexts_operations_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_metadata_stores_contexts_operations_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform metadata stores contexts operations wait.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_metadata_stores_contexts_operations_wait(
        &self,
        args: &AiplatformMetadataStoresContextsOperationsWaitArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_metadata_stores_contexts_operations_wait_builder(
            &self.http_client,
            &args.name,
            &args.timeout,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_metadata_stores_contexts_operations_wait_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform metadata stores executions operations cancel.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_metadata_stores_executions_operations_cancel(
        &self,
        args: &AiplatformMetadataStoresExecutionsOperationsCancelArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_metadata_stores_executions_operations_cancel_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_metadata_stores_executions_operations_cancel_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform metadata stores executions operations delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_metadata_stores_executions_operations_delete(
        &self,
        args: &AiplatformMetadataStoresExecutionsOperationsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_metadata_stores_executions_operations_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_metadata_stores_executions_operations_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform metadata stores executions operations wait.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_metadata_stores_executions_operations_wait(
        &self,
        args: &AiplatformMetadataStoresExecutionsOperationsWaitArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_metadata_stores_executions_operations_wait_builder(
            &self.http_client,
            &args.name,
            &args.timeout,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_metadata_stores_executions_operations_wait_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform metadata stores operations cancel.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_metadata_stores_operations_cancel(
        &self,
        args: &AiplatformMetadataStoresOperationsCancelArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_metadata_stores_operations_cancel_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_metadata_stores_operations_cancel_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform metadata stores operations delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_metadata_stores_operations_delete(
        &self,
        args: &AiplatformMetadataStoresOperationsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_metadata_stores_operations_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_metadata_stores_operations_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform metadata stores operations wait.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_metadata_stores_operations_wait(
        &self,
        args: &AiplatformMetadataStoresOperationsWaitArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_metadata_stores_operations_wait_builder(
            &self.http_client,
            &args.name,
            &args.timeout,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_metadata_stores_operations_wait_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform migratable resources operations cancel.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_migratable_resources_operations_cancel(
        &self,
        args: &AiplatformMigratableResourcesOperationsCancelArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_migratable_resources_operations_cancel_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_migratable_resources_operations_cancel_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform migratable resources operations delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_migratable_resources_operations_delete(
        &self,
        args: &AiplatformMigratableResourcesOperationsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_migratable_resources_operations_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_migratable_resources_operations_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform migratable resources operations wait.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_migratable_resources_operations_wait(
        &self,
        args: &AiplatformMigratableResourcesOperationsWaitArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_migratable_resources_operations_wait_builder(
            &self.http_client,
            &args.name,
            &args.timeout,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_migratable_resources_operations_wait_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform model deployment monitoring jobs operations cancel.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_model_deployment_monitoring_jobs_operations_cancel(
        &self,
        args: &AiplatformModelDeploymentMonitoringJobsOperationsCancelArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_model_deployment_monitoring_jobs_operations_cancel_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_model_deployment_monitoring_jobs_operations_cancel_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform model deployment monitoring jobs operations delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_model_deployment_monitoring_jobs_operations_delete(
        &self,
        args: &AiplatformModelDeploymentMonitoringJobsOperationsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_model_deployment_monitoring_jobs_operations_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_model_deployment_monitoring_jobs_operations_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform model deployment monitoring jobs operations wait.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_model_deployment_monitoring_jobs_operations_wait(
        &self,
        args: &AiplatformModelDeploymentMonitoringJobsOperationsWaitArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_model_deployment_monitoring_jobs_operations_wait_builder(
            &self.http_client,
            &args.name,
            &args.timeout,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_model_deployment_monitoring_jobs_operations_wait_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform models evaluations operations cancel.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_models_evaluations_operations_cancel(
        &self,
        args: &AiplatformModelsEvaluationsOperationsCancelArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_models_evaluations_operations_cancel_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_models_evaluations_operations_cancel_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform models evaluations operations delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_models_evaluations_operations_delete(
        &self,
        args: &AiplatformModelsEvaluationsOperationsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_models_evaluations_operations_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_models_evaluations_operations_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform models evaluations operations wait.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_models_evaluations_operations_wait(
        &self,
        args: &AiplatformModelsEvaluationsOperationsWaitArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_models_evaluations_operations_wait_builder(
            &self.http_client,
            &args.name,
            &args.timeout,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_models_evaluations_operations_wait_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform models operations cancel.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_models_operations_cancel(
        &self,
        args: &AiplatformModelsOperationsCancelArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_models_operations_cancel_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_models_operations_cancel_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform models operations delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_models_operations_delete(
        &self,
        args: &AiplatformModelsOperationsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_models_operations_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_models_operations_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform models operations wait.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_models_operations_wait(
        &self,
        args: &AiplatformModelsOperationsWaitArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_models_operations_wait_builder(
            &self.http_client,
            &args.name,
            &args.timeout,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_models_operations_wait_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform notebook execution jobs operations cancel.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_notebook_execution_jobs_operations_cancel(
        &self,
        args: &AiplatformNotebookExecutionJobsOperationsCancelArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_notebook_execution_jobs_operations_cancel_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_notebook_execution_jobs_operations_cancel_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform notebook execution jobs operations delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_notebook_execution_jobs_operations_delete(
        &self,
        args: &AiplatformNotebookExecutionJobsOperationsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_notebook_execution_jobs_operations_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_notebook_execution_jobs_operations_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform notebook execution jobs operations wait.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_notebook_execution_jobs_operations_wait(
        &self,
        args: &AiplatformNotebookExecutionJobsOperationsWaitArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_notebook_execution_jobs_operations_wait_builder(
            &self.http_client,
            &args.name,
            &args.timeout,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_notebook_execution_jobs_operations_wait_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform notebook runtime templates operations cancel.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_notebook_runtime_templates_operations_cancel(
        &self,
        args: &AiplatformNotebookRuntimeTemplatesOperationsCancelArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_notebook_runtime_templates_operations_cancel_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_notebook_runtime_templates_operations_cancel_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform notebook runtime templates operations delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_notebook_runtime_templates_operations_delete(
        &self,
        args: &AiplatformNotebookRuntimeTemplatesOperationsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_notebook_runtime_templates_operations_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_notebook_runtime_templates_operations_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform notebook runtime templates operations wait.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_notebook_runtime_templates_operations_wait(
        &self,
        args: &AiplatformNotebookRuntimeTemplatesOperationsWaitArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_notebook_runtime_templates_operations_wait_builder(
            &self.http_client,
            &args.name,
            &args.timeout,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_notebook_runtime_templates_operations_wait_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform notebook runtimes operations cancel.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_notebook_runtimes_operations_cancel(
        &self,
        args: &AiplatformNotebookRuntimesOperationsCancelArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_notebook_runtimes_operations_cancel_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_notebook_runtimes_operations_cancel_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform notebook runtimes operations delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_notebook_runtimes_operations_delete(
        &self,
        args: &AiplatformNotebookRuntimesOperationsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_notebook_runtimes_operations_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_notebook_runtimes_operations_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform notebook runtimes operations wait.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_notebook_runtimes_operations_wait(
        &self,
        args: &AiplatformNotebookRuntimesOperationsWaitArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_notebook_runtimes_operations_wait_builder(
            &self.http_client,
            &args.name,
            &args.timeout,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_notebook_runtimes_operations_wait_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform operations cancel.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_operations_cancel(
        &self,
        args: &AiplatformOperationsCancelArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_operations_cancel_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_operations_cancel_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform operations delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_operations_delete(
        &self,
        args: &AiplatformOperationsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_operations_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_operations_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform operations wait.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_operations_wait(
        &self,
        args: &AiplatformOperationsWaitArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_operations_wait_builder(
            &self.http_client,
            &args.name,
            &args.timeout,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_operations_wait_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform persistent resources operations cancel.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_persistent_resources_operations_cancel(
        &self,
        args: &AiplatformPersistentResourcesOperationsCancelArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_persistent_resources_operations_cancel_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_persistent_resources_operations_cancel_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform persistent resources operations delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_persistent_resources_operations_delete(
        &self,
        args: &AiplatformPersistentResourcesOperationsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_persistent_resources_operations_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_persistent_resources_operations_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform persistent resources operations wait.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_persistent_resources_operations_wait(
        &self,
        args: &AiplatformPersistentResourcesOperationsWaitArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_persistent_resources_operations_wait_builder(
            &self.http_client,
            &args.name,
            &args.timeout,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_persistent_resources_operations_wait_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform pipeline jobs operations cancel.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_pipeline_jobs_operations_cancel(
        &self,
        args: &AiplatformPipelineJobsOperationsCancelArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_pipeline_jobs_operations_cancel_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_pipeline_jobs_operations_cancel_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform pipeline jobs operations delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_pipeline_jobs_operations_delete(
        &self,
        args: &AiplatformPipelineJobsOperationsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_pipeline_jobs_operations_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_pipeline_jobs_operations_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform pipeline jobs operations wait.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_pipeline_jobs_operations_wait(
        &self,
        args: &AiplatformPipelineJobsOperationsWaitArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_pipeline_jobs_operations_wait_builder(
            &self.http_client,
            &args.name,
            &args.timeout,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_pipeline_jobs_operations_wait_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects update cache config.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_update_cache_config(
        &self,
        args: &AiplatformProjectsUpdateCacheConfigArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_update_cache_config_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_update_cache_config_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations ask contexts.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudAiplatformV1AskContextsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_ask_contexts(
        &self,
        args: &AiplatformProjectsLocationsAskContextsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudAiplatformV1AskContextsResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_ask_contexts_builder(
            &self.http_client,
            &args.parent,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_ask_contexts_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations async retrieve contexts.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_async_retrieve_contexts(
        &self,
        args: &AiplatformProjectsLocationsAsyncRetrieveContextsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_async_retrieve_contexts_builder(
            &self.http_client,
            &args.parent,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_async_retrieve_contexts_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations augment prompt.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudAiplatformV1AugmentPromptResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_augment_prompt(
        &self,
        args: &AiplatformProjectsLocationsAugmentPromptArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudAiplatformV1AugmentPromptResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_augment_prompt_builder(
            &self.http_client,
            &args.parent,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_augment_prompt_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations corroborate content.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudAiplatformV1CorroborateContentResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_corroborate_content(
        &self,
        args: &AiplatformProjectsLocationsCorroborateContentArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudAiplatformV1CorroborateContentResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_corroborate_content_builder(
            &self.http_client,
            &args.parent,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_corroborate_content_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations deploy.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_deploy(
        &self,
        args: &AiplatformProjectsLocationsDeployArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_deploy_builder(
            &self.http_client,
            &args.destination,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_deploy_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations evaluate dataset.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_evaluate_dataset(
        &self,
        args: &AiplatformProjectsLocationsEvaluateDatasetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_evaluate_dataset_builder(
            &self.http_client,
            &args.location,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_evaluate_dataset_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations evaluate instances.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudAiplatformV1EvaluateInstancesResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_evaluate_instances(
        &self,
        args: &AiplatformProjectsLocationsEvaluateInstancesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudAiplatformV1EvaluateInstancesResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_evaluate_instances_builder(
            &self.http_client,
            &args.location,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_evaluate_instances_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations generate instance rubrics.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudAiplatformV1GenerateInstanceRubricsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_generate_instance_rubrics(
        &self,
        args: &AiplatformProjectsLocationsGenerateInstanceRubricsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudAiplatformV1GenerateInstanceRubricsResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_generate_instance_rubrics_builder(
            &self.http_client,
            &args.location,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_generate_instance_rubrics_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations generate synthetic data.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudAiplatformV1GenerateSyntheticDataResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_generate_synthetic_data(
        &self,
        args: &AiplatformProjectsLocationsGenerateSyntheticDataArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudAiplatformV1GenerateSyntheticDataResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_generate_synthetic_data_builder(
            &self.http_client,
            &args.location,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_generate_synthetic_data_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations retrieve contexts.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudAiplatformV1RetrieveContextsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_retrieve_contexts(
        &self,
        args: &AiplatformProjectsLocationsRetrieveContextsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudAiplatformV1RetrieveContextsResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_retrieve_contexts_builder(
            &self.http_client,
            &args.parent,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_retrieve_contexts_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations update rag engine config.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_update_rag_engine_config(
        &self,
        args: &AiplatformProjectsLocationsUpdateRagEngineConfigArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_update_rag_engine_config_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_update_rag_engine_config_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations batch prediction jobs cancel.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_batch_prediction_jobs_cancel(
        &self,
        args: &AiplatformProjectsLocationsBatchPredictionJobsCancelArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_batch_prediction_jobs_cancel_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_batch_prediction_jobs_cancel_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations batch prediction jobs create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudAiplatformV1BatchPredictionJob result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_batch_prediction_jobs_create(
        &self,
        args: &AiplatformProjectsLocationsBatchPredictionJobsCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudAiplatformV1BatchPredictionJob, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_batch_prediction_jobs_create_builder(
            &self.http_client,
            &args.parent,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_batch_prediction_jobs_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations batch prediction jobs delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_batch_prediction_jobs_delete(
        &self,
        args: &AiplatformProjectsLocationsBatchPredictionJobsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_batch_prediction_jobs_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_batch_prediction_jobs_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations cached contents create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudAiplatformV1CachedContent result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_cached_contents_create(
        &self,
        args: &AiplatformProjectsLocationsCachedContentsCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudAiplatformV1CachedContent, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_cached_contents_create_builder(
            &self.http_client,
            &args.parent,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_cached_contents_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations cached contents delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_cached_contents_delete(
        &self,
        args: &AiplatformProjectsLocationsCachedContentsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_cached_contents_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_cached_contents_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations cached contents patch.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudAiplatformV1CachedContent result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_cached_contents_patch(
        &self,
        args: &AiplatformProjectsLocationsCachedContentsPatchArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudAiplatformV1CachedContent, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_cached_contents_patch_builder(
            &self.http_client,
            &args.name,
            &args.updateMask,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_cached_contents_patch_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations custom jobs cancel.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_custom_jobs_cancel(
        &self,
        args: &AiplatformProjectsLocationsCustomJobsCancelArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_custom_jobs_cancel_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_custom_jobs_cancel_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations custom jobs create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudAiplatformV1CustomJob result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_custom_jobs_create(
        &self,
        args: &AiplatformProjectsLocationsCustomJobsCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudAiplatformV1CustomJob, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_custom_jobs_create_builder(
            &self.http_client,
            &args.parent,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_custom_jobs_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations custom jobs delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_custom_jobs_delete(
        &self,
        args: &AiplatformProjectsLocationsCustomJobsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_custom_jobs_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_custom_jobs_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations custom jobs operations cancel.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_custom_jobs_operations_cancel(
        &self,
        args: &AiplatformProjectsLocationsCustomJobsOperationsCancelArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_custom_jobs_operations_cancel_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_custom_jobs_operations_cancel_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations custom jobs operations delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_custom_jobs_operations_delete(
        &self,
        args: &AiplatformProjectsLocationsCustomJobsOperationsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_custom_jobs_operations_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_custom_jobs_operations_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations custom jobs operations wait.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_custom_jobs_operations_wait(
        &self,
        args: &AiplatformProjectsLocationsCustomJobsOperationsWaitArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_custom_jobs_operations_wait_builder(
            &self.http_client,
            &args.name,
            &args.timeout,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_custom_jobs_operations_wait_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations data labeling jobs cancel.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_data_labeling_jobs_cancel(
        &self,
        args: &AiplatformProjectsLocationsDataLabelingJobsCancelArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_data_labeling_jobs_cancel_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_data_labeling_jobs_cancel_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations data labeling jobs create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudAiplatformV1DataLabelingJob result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_data_labeling_jobs_create(
        &self,
        args: &AiplatformProjectsLocationsDataLabelingJobsCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudAiplatformV1DataLabelingJob, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_data_labeling_jobs_create_builder(
            &self.http_client,
            &args.parent,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_data_labeling_jobs_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations data labeling jobs delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_data_labeling_jobs_delete(
        &self,
        args: &AiplatformProjectsLocationsDataLabelingJobsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_data_labeling_jobs_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_data_labeling_jobs_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations data labeling jobs operations cancel.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_data_labeling_jobs_operations_cancel(
        &self,
        args: &AiplatformProjectsLocationsDataLabelingJobsOperationsCancelArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_data_labeling_jobs_operations_cancel_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_data_labeling_jobs_operations_cancel_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations data labeling jobs operations delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_data_labeling_jobs_operations_delete(
        &self,
        args: &AiplatformProjectsLocationsDataLabelingJobsOperationsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_data_labeling_jobs_operations_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_data_labeling_jobs_operations_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations data labeling jobs operations wait.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_data_labeling_jobs_operations_wait(
        &self,
        args: &AiplatformProjectsLocationsDataLabelingJobsOperationsWaitArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_data_labeling_jobs_operations_wait_builder(
            &self.http_client,
            &args.name,
            &args.timeout,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_data_labeling_jobs_operations_wait_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations datasets create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_datasets_create(
        &self,
        args: &AiplatformProjectsLocationsDatasetsCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_datasets_create_builder(
            &self.http_client,
            &args.parent,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_datasets_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations datasets delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_datasets_delete(
        &self,
        args: &AiplatformProjectsLocationsDatasetsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_datasets_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_datasets_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations datasets export.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_datasets_export(
        &self,
        args: &AiplatformProjectsLocationsDatasetsExportArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_datasets_export_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_datasets_export_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations datasets get iam policy.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleIamV1Policy result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_datasets_get_iam_policy(
        &self,
        args: &AiplatformProjectsLocationsDatasetsGetIamPolicyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleIamV1Policy, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_datasets_get_iam_policy_builder(
            &self.http_client,
            &args.resource,
            &args.options.requestedPolicyVersion,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_datasets_get_iam_policy_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations datasets import.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_datasets_import(
        &self,
        args: &AiplatformProjectsLocationsDatasetsImportArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_datasets_import_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_datasets_import_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations datasets patch.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudAiplatformV1Dataset result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_datasets_patch(
        &self,
        args: &AiplatformProjectsLocationsDatasetsPatchArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudAiplatformV1Dataset, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_datasets_patch_builder(
            &self.http_client,
            &args.name,
            &args.updateMask,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_datasets_patch_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations datasets set iam policy.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleIamV1Policy result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_datasets_set_iam_policy(
        &self,
        args: &AiplatformProjectsLocationsDatasetsSetIamPolicyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleIamV1Policy, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_datasets_set_iam_policy_builder(
            &self.http_client,
            &args.resource,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_datasets_set_iam_policy_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations datasets test iam permissions.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleIamV1TestIamPermissionsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_datasets_test_iam_permissions(
        &self,
        args: &AiplatformProjectsLocationsDatasetsTestIamPermissionsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleIamV1TestIamPermissionsResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_datasets_test_iam_permissions_builder(
            &self.http_client,
            &args.resource,
            &args.permissions,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_datasets_test_iam_permissions_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations datasets annotation specs operations cancel.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_datasets_annotation_specs_operations_cancel(
        &self,
        args: &AiplatformProjectsLocationsDatasetsAnnotationSpecsOperationsCancelArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_datasets_annotation_specs_operations_cancel_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_datasets_annotation_specs_operations_cancel_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations datasets annotation specs operations delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_datasets_annotation_specs_operations_delete(
        &self,
        args: &AiplatformProjectsLocationsDatasetsAnnotationSpecsOperationsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_datasets_annotation_specs_operations_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_datasets_annotation_specs_operations_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations datasets annotation specs operations wait.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_datasets_annotation_specs_operations_wait(
        &self,
        args: &AiplatformProjectsLocationsDatasetsAnnotationSpecsOperationsWaitArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_datasets_annotation_specs_operations_wait_builder(
            &self.http_client,
            &args.name,
            &args.timeout,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_datasets_annotation_specs_operations_wait_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations datasets data items annotations operations cancel.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_datasets_data_items_annotations_operations_cancel(
        &self,
        args: &AiplatformProjectsLocationsDatasetsDataItemsAnnotationsOperationsCancelArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_datasets_data_items_annotations_operations_cancel_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_datasets_data_items_annotations_operations_cancel_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations datasets data items annotations operations delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_datasets_data_items_annotations_operations_delete(
        &self,
        args: &AiplatformProjectsLocationsDatasetsDataItemsAnnotationsOperationsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_datasets_data_items_annotations_operations_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_datasets_data_items_annotations_operations_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations datasets data items annotations operations wait.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_datasets_data_items_annotations_operations_wait(
        &self,
        args: &AiplatformProjectsLocationsDatasetsDataItemsAnnotationsOperationsWaitArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_datasets_data_items_annotations_operations_wait_builder(
            &self.http_client,
            &args.name,
            &args.timeout,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_datasets_data_items_annotations_operations_wait_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations datasets data items operations cancel.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_datasets_data_items_operations_cancel(
        &self,
        args: &AiplatformProjectsLocationsDatasetsDataItemsOperationsCancelArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_datasets_data_items_operations_cancel_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_datasets_data_items_operations_cancel_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations datasets data items operations delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_datasets_data_items_operations_delete(
        &self,
        args: &AiplatformProjectsLocationsDatasetsDataItemsOperationsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_datasets_data_items_operations_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_datasets_data_items_operations_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations datasets data items operations wait.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_datasets_data_items_operations_wait(
        &self,
        args: &AiplatformProjectsLocationsDatasetsDataItemsOperationsWaitArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_datasets_data_items_operations_wait_builder(
            &self.http_client,
            &args.name,
            &args.timeout,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_datasets_data_items_operations_wait_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations datasets dataset versions create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_datasets_dataset_versions_create(
        &self,
        args: &AiplatformProjectsLocationsDatasetsDatasetVersionsCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_datasets_dataset_versions_create_builder(
            &self.http_client,
            &args.parent,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_datasets_dataset_versions_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations datasets dataset versions delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_datasets_dataset_versions_delete(
        &self,
        args: &AiplatformProjectsLocationsDatasetsDatasetVersionsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_datasets_dataset_versions_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_datasets_dataset_versions_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations datasets dataset versions patch.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudAiplatformV1DatasetVersion result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_datasets_dataset_versions_patch(
        &self,
        args: &AiplatformProjectsLocationsDatasetsDatasetVersionsPatchArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudAiplatformV1DatasetVersion, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_datasets_dataset_versions_patch_builder(
            &self.http_client,
            &args.name,
            &args.updateMask,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_datasets_dataset_versions_patch_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations datasets operations cancel.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_datasets_operations_cancel(
        &self,
        args: &AiplatformProjectsLocationsDatasetsOperationsCancelArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_datasets_operations_cancel_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_datasets_operations_cancel_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations datasets operations delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_datasets_operations_delete(
        &self,
        args: &AiplatformProjectsLocationsDatasetsOperationsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_datasets_operations_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_datasets_operations_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations datasets operations wait.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_datasets_operations_wait(
        &self,
        args: &AiplatformProjectsLocationsDatasetsOperationsWaitArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_datasets_operations_wait_builder(
            &self.http_client,
            &args.name,
            &args.timeout,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_datasets_operations_wait_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations datasets saved queries delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_datasets_saved_queries_delete(
        &self,
        args: &AiplatformProjectsLocationsDatasetsSavedQueriesDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_datasets_saved_queries_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_datasets_saved_queries_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations datasets saved queries operations cancel.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_datasets_saved_queries_operations_cancel(
        &self,
        args: &AiplatformProjectsLocationsDatasetsSavedQueriesOperationsCancelArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_datasets_saved_queries_operations_cancel_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_datasets_saved_queries_operations_cancel_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations datasets saved queries operations delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_datasets_saved_queries_operations_delete(
        &self,
        args: &AiplatformProjectsLocationsDatasetsSavedQueriesOperationsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_datasets_saved_queries_operations_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_datasets_saved_queries_operations_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations datasets saved queries operations wait.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_datasets_saved_queries_operations_wait(
        &self,
        args: &AiplatformProjectsLocationsDatasetsSavedQueriesOperationsWaitArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_datasets_saved_queries_operations_wait_builder(
            &self.http_client,
            &args.name,
            &args.timeout,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_datasets_saved_queries_operations_wait_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations deployment resource pools create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_deployment_resource_pools_create(
        &self,
        args: &AiplatformProjectsLocationsDeploymentResourcePoolsCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_deployment_resource_pools_create_builder(
            &self.http_client,
            &args.parent,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_deployment_resource_pools_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations deployment resource pools delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_deployment_resource_pools_delete(
        &self,
        args: &AiplatformProjectsLocationsDeploymentResourcePoolsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_deployment_resource_pools_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_deployment_resource_pools_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations deployment resource pools patch.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_deployment_resource_pools_patch(
        &self,
        args: &AiplatformProjectsLocationsDeploymentResourcePoolsPatchArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_deployment_resource_pools_patch_builder(
            &self.http_client,
            &args.name,
            &args.updateMask,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_deployment_resource_pools_patch_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations deployment resource pools operations cancel.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_deployment_resource_pools_operations_cancel(
        &self,
        args: &AiplatformProjectsLocationsDeploymentResourcePoolsOperationsCancelArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_deployment_resource_pools_operations_cancel_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_deployment_resource_pools_operations_cancel_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations deployment resource pools operations delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_deployment_resource_pools_operations_delete(
        &self,
        args: &AiplatformProjectsLocationsDeploymentResourcePoolsOperationsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_deployment_resource_pools_operations_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_deployment_resource_pools_operations_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations deployment resource pools operations wait.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_deployment_resource_pools_operations_wait(
        &self,
        args: &AiplatformProjectsLocationsDeploymentResourcePoolsOperationsWaitArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_deployment_resource_pools_operations_wait_builder(
            &self.http_client,
            &args.name,
            &args.timeout,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_deployment_resource_pools_operations_wait_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations endpoints compute tokens.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudAiplatformV1ComputeTokensResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_endpoints_compute_tokens(
        &self,
        args: &AiplatformProjectsLocationsEndpointsComputeTokensArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudAiplatformV1ComputeTokensResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_endpoints_compute_tokens_builder(
            &self.http_client,
            &args.endpoint,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_endpoints_compute_tokens_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations endpoints count tokens.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudAiplatformV1CountTokensResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_endpoints_count_tokens(
        &self,
        args: &AiplatformProjectsLocationsEndpointsCountTokensArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudAiplatformV1CountTokensResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_endpoints_count_tokens_builder(
            &self.http_client,
            &args.endpoint,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_endpoints_count_tokens_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations endpoints create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_endpoints_create(
        &self,
        args: &AiplatformProjectsLocationsEndpointsCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_endpoints_create_builder(
            &self.http_client,
            &args.parent,
            &args.endpointId,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_endpoints_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations endpoints delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_endpoints_delete(
        &self,
        args: &AiplatformProjectsLocationsEndpointsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_endpoints_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_endpoints_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations endpoints deploy model.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_endpoints_deploy_model(
        &self,
        args: &AiplatformProjectsLocationsEndpointsDeployModelArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_endpoints_deploy_model_builder(
            &self.http_client,
            &args.endpoint,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_endpoints_deploy_model_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations endpoints direct predict.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudAiplatformV1DirectPredictResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_endpoints_direct_predict(
        &self,
        args: &AiplatformProjectsLocationsEndpointsDirectPredictArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudAiplatformV1DirectPredictResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_endpoints_direct_predict_builder(
            &self.http_client,
            &args.endpoint,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_endpoints_direct_predict_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations endpoints direct raw predict.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudAiplatformV1DirectRawPredictResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_endpoints_direct_raw_predict(
        &self,
        args: &AiplatformProjectsLocationsEndpointsDirectRawPredictArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudAiplatformV1DirectRawPredictResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_endpoints_direct_raw_predict_builder(
            &self.http_client,
            &args.endpoint,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_endpoints_direct_raw_predict_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations endpoints explain.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudAiplatformV1ExplainResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_endpoints_explain(
        &self,
        args: &AiplatformProjectsLocationsEndpointsExplainArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudAiplatformV1ExplainResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_endpoints_explain_builder(
            &self.http_client,
            &args.endpoint,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_endpoints_explain_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations endpoints fetch predict operation.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_endpoints_fetch_predict_operation(
        &self,
        args: &AiplatformProjectsLocationsEndpointsFetchPredictOperationArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_endpoints_fetch_predict_operation_builder(
            &self.http_client,
            &args.endpoint,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_endpoints_fetch_predict_operation_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations endpoints generate content.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudAiplatformV1GenerateContentResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_endpoints_generate_content(
        &self,
        args: &AiplatformProjectsLocationsEndpointsGenerateContentArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudAiplatformV1GenerateContentResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_endpoints_generate_content_builder(
            &self.http_client,
            &args.model,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_endpoints_generate_content_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations endpoints mutate deployed model.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_endpoints_mutate_deployed_model(
        &self,
        args: &AiplatformProjectsLocationsEndpointsMutateDeployedModelArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_endpoints_mutate_deployed_model_builder(
            &self.http_client,
            &args.endpoint,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_endpoints_mutate_deployed_model_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations endpoints patch.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudAiplatformV1Endpoint result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_endpoints_patch(
        &self,
        args: &AiplatformProjectsLocationsEndpointsPatchArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudAiplatformV1Endpoint, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_endpoints_patch_builder(
            &self.http_client,
            &args.name,
            &args.updateMask,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_endpoints_patch_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations endpoints predict.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudAiplatformV1PredictResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_endpoints_predict(
        &self,
        args: &AiplatformProjectsLocationsEndpointsPredictArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudAiplatformV1PredictResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_endpoints_predict_builder(
            &self.http_client,
            &args.endpoint,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_endpoints_predict_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations endpoints predict long running.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_endpoints_predict_long_running(
        &self,
        args: &AiplatformProjectsLocationsEndpointsPredictLongRunningArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_endpoints_predict_long_running_builder(
            &self.http_client,
            &args.endpoint,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_endpoints_predict_long_running_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations endpoints raw predict.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleApiHttpBody result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_endpoints_raw_predict(
        &self,
        args: &AiplatformProjectsLocationsEndpointsRawPredictArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleApiHttpBody, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_endpoints_raw_predict_builder(
            &self.http_client,
            &args.endpoint,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_endpoints_raw_predict_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations endpoints server streaming predict.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudAiplatformV1StreamingPredictResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_endpoints_server_streaming_predict(
        &self,
        args: &AiplatformProjectsLocationsEndpointsServerStreamingPredictArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudAiplatformV1StreamingPredictResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_endpoints_server_streaming_predict_builder(
            &self.http_client,
            &args.endpoint,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_endpoints_server_streaming_predict_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations endpoints stream generate content.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudAiplatformV1GenerateContentResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_endpoints_stream_generate_content(
        &self,
        args: &AiplatformProjectsLocationsEndpointsStreamGenerateContentArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudAiplatformV1GenerateContentResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_endpoints_stream_generate_content_builder(
            &self.http_client,
            &args.model,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_endpoints_stream_generate_content_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations endpoints stream raw predict.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleApiHttpBody result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_endpoints_stream_raw_predict(
        &self,
        args: &AiplatformProjectsLocationsEndpointsStreamRawPredictArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleApiHttpBody, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_endpoints_stream_raw_predict_builder(
            &self.http_client,
            &args.endpoint,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_endpoints_stream_raw_predict_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations endpoints undeploy model.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_endpoints_undeploy_model(
        &self,
        args: &AiplatformProjectsLocationsEndpointsUndeployModelArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_endpoints_undeploy_model_builder(
            &self.http_client,
            &args.endpoint,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_endpoints_undeploy_model_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations endpoints update.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_endpoints_update(
        &self,
        args: &AiplatformProjectsLocationsEndpointsUpdateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_endpoints_update_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_endpoints_update_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations endpoints chat completions.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleApiHttpBody result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_endpoints_chat_completions(
        &self,
        args: &AiplatformProjectsLocationsEndpointsChatCompletionsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleApiHttpBody, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_endpoints_chat_completions_builder(
            &self.http_client,
            &args.endpoint,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_endpoints_chat_completions_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations endpoints deployed models invoke invoke.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleApiHttpBody result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_endpoints_deployed_models_invoke_invoke(
        &self,
        args: &AiplatformProjectsLocationsEndpointsDeployedModelsInvokeInvokeArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleApiHttpBody, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_endpoints_deployed_models_invoke_invoke_builder(
            &self.http_client,
            &args.endpoint,
            &args.deployedModelId,
            &args.invokeId,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_endpoints_deployed_models_invoke_invoke_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations endpoints google science inference.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleApiHttpBody result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_endpoints_google_science_inference(
        &self,
        args: &AiplatformProjectsLocationsEndpointsGoogleScienceInferenceArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleApiHttpBody, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_endpoints_google_science_inference_builder(
            &self.http_client,
            &args.endpoint,
            &args.deployedModelId,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_endpoints_google_science_inference_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations endpoints invoke invoke.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleApiHttpBody result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_endpoints_invoke_invoke(
        &self,
        args: &AiplatformProjectsLocationsEndpointsInvokeInvokeArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleApiHttpBody, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_endpoints_invoke_invoke_builder(
            &self.http_client,
            &args.endpoint,
            &args.invokeId,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_endpoints_invoke_invoke_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations endpoints openapi completions.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleApiHttpBody result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_endpoints_openapi_completions(
        &self,
        args: &AiplatformProjectsLocationsEndpointsOpenapiCompletionsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleApiHttpBody, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_endpoints_openapi_completions_builder(
            &self.http_client,
            &args.endpoint,
            &args.deployedModelId,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_endpoints_openapi_completions_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations endpoints openapi embeddings.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleApiHttpBody result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_endpoints_openapi_embeddings(
        &self,
        args: &AiplatformProjectsLocationsEndpointsOpenapiEmbeddingsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleApiHttpBody, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_endpoints_openapi_embeddings_builder(
            &self.http_client,
            &args.endpoint,
            &args.deployedModelId,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_endpoints_openapi_embeddings_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations endpoints openapi responses.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleApiHttpBody result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_endpoints_openapi_responses(
        &self,
        args: &AiplatformProjectsLocationsEndpointsOpenapiResponsesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleApiHttpBody, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_endpoints_openapi_responses_builder(
            &self.http_client,
            &args.endpoint,
            &args.deployedModelId,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_endpoints_openapi_responses_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations endpoints operations cancel.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_endpoints_operations_cancel(
        &self,
        args: &AiplatformProjectsLocationsEndpointsOperationsCancelArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_endpoints_operations_cancel_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_endpoints_operations_cancel_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations endpoints operations delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_endpoints_operations_delete(
        &self,
        args: &AiplatformProjectsLocationsEndpointsOperationsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_endpoints_operations_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_endpoints_operations_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations endpoints operations wait.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_endpoints_operations_wait(
        &self,
        args: &AiplatformProjectsLocationsEndpointsOperationsWaitArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_endpoints_operations_wait_builder(
            &self.http_client,
            &args.name,
            &args.timeout,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_endpoints_operations_wait_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations evaluation items create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudAiplatformV1EvaluationItem result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_evaluation_items_create(
        &self,
        args: &AiplatformProjectsLocationsEvaluationItemsCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudAiplatformV1EvaluationItem, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_evaluation_items_create_builder(
            &self.http_client,
            &args.parent,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_evaluation_items_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations evaluation items delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_evaluation_items_delete(
        &self,
        args: &AiplatformProjectsLocationsEvaluationItemsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_evaluation_items_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_evaluation_items_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations evaluation runs cancel.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_evaluation_runs_cancel(
        &self,
        args: &AiplatformProjectsLocationsEvaluationRunsCancelArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_evaluation_runs_cancel_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_evaluation_runs_cancel_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations evaluation runs create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudAiplatformV1EvaluationRun result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_evaluation_runs_create(
        &self,
        args: &AiplatformProjectsLocationsEvaluationRunsCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudAiplatformV1EvaluationRun, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_evaluation_runs_create_builder(
            &self.http_client,
            &args.parent,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_evaluation_runs_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations evaluation runs delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_evaluation_runs_delete(
        &self,
        args: &AiplatformProjectsLocationsEvaluationRunsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_evaluation_runs_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_evaluation_runs_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations evaluation sets create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudAiplatformV1EvaluationSet result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_evaluation_sets_create(
        &self,
        args: &AiplatformProjectsLocationsEvaluationSetsCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudAiplatformV1EvaluationSet, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_evaluation_sets_create_builder(
            &self.http_client,
            &args.parent,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_evaluation_sets_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations evaluation sets delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_evaluation_sets_delete(
        &self,
        args: &AiplatformProjectsLocationsEvaluationSetsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_evaluation_sets_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_evaluation_sets_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations evaluation sets patch.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudAiplatformV1EvaluationSet result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_evaluation_sets_patch(
        &self,
        args: &AiplatformProjectsLocationsEvaluationSetsPatchArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudAiplatformV1EvaluationSet, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_evaluation_sets_patch_builder(
            &self.http_client,
            &args.name,
            &args.updateMask,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_evaluation_sets_patch_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations feature groups create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_feature_groups_create(
        &self,
        args: &AiplatformProjectsLocationsFeatureGroupsCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_feature_groups_create_builder(
            &self.http_client,
            &args.parent,
            &args.featureGroupId,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_feature_groups_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations feature groups delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_feature_groups_delete(
        &self,
        args: &AiplatformProjectsLocationsFeatureGroupsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_feature_groups_delete_builder(
            &self.http_client,
            &args.name,
            &args.force,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_feature_groups_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations feature groups get iam policy.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleIamV1Policy result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_feature_groups_get_iam_policy(
        &self,
        args: &AiplatformProjectsLocationsFeatureGroupsGetIamPolicyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleIamV1Policy, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_feature_groups_get_iam_policy_builder(
            &self.http_client,
            &args.resource,
            &args.options.requestedPolicyVersion,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_feature_groups_get_iam_policy_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations feature groups patch.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_feature_groups_patch(
        &self,
        args: &AiplatformProjectsLocationsFeatureGroupsPatchArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_feature_groups_patch_builder(
            &self.http_client,
            &args.name,
            &args.updateMask,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_feature_groups_patch_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations feature groups set iam policy.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleIamV1Policy result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_feature_groups_set_iam_policy(
        &self,
        args: &AiplatformProjectsLocationsFeatureGroupsSetIamPolicyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleIamV1Policy, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_feature_groups_set_iam_policy_builder(
            &self.http_client,
            &args.resource,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_feature_groups_set_iam_policy_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations feature groups test iam permissions.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleIamV1TestIamPermissionsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_feature_groups_test_iam_permissions(
        &self,
        args: &AiplatformProjectsLocationsFeatureGroupsTestIamPermissionsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleIamV1TestIamPermissionsResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_feature_groups_test_iam_permissions_builder(
            &self.http_client,
            &args.resource,
            &args.permissions,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_feature_groups_test_iam_permissions_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations feature groups features batch create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_feature_groups_features_batch_create(
        &self,
        args: &AiplatformProjectsLocationsFeatureGroupsFeaturesBatchCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_feature_groups_features_batch_create_builder(
            &self.http_client,
            &args.parent,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_feature_groups_features_batch_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations feature groups features create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_feature_groups_features_create(
        &self,
        args: &AiplatformProjectsLocationsFeatureGroupsFeaturesCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_feature_groups_features_create_builder(
            &self.http_client,
            &args.parent,
            &args.featureId,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_feature_groups_features_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations feature groups features delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_feature_groups_features_delete(
        &self,
        args: &AiplatformProjectsLocationsFeatureGroupsFeaturesDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_feature_groups_features_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_feature_groups_features_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations feature groups features patch.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_feature_groups_features_patch(
        &self,
        args: &AiplatformProjectsLocationsFeatureGroupsFeaturesPatchArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_feature_groups_features_patch_builder(
            &self.http_client,
            &args.name,
            &args.updateMask,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_feature_groups_features_patch_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations feature groups features operations delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_feature_groups_features_operations_delete(
        &self,
        args: &AiplatformProjectsLocationsFeatureGroupsFeaturesOperationsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_feature_groups_features_operations_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_feature_groups_features_operations_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations feature groups features operations wait.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_feature_groups_features_operations_wait(
        &self,
        args: &AiplatformProjectsLocationsFeatureGroupsFeaturesOperationsWaitArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_feature_groups_features_operations_wait_builder(
            &self.http_client,
            &args.name,
            &args.timeout,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_feature_groups_features_operations_wait_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations feature groups operations delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_feature_groups_operations_delete(
        &self,
        args: &AiplatformProjectsLocationsFeatureGroupsOperationsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_feature_groups_operations_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_feature_groups_operations_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations feature groups operations wait.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_feature_groups_operations_wait(
        &self,
        args: &AiplatformProjectsLocationsFeatureGroupsOperationsWaitArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_feature_groups_operations_wait_builder(
            &self.http_client,
            &args.name,
            &args.timeout,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_feature_groups_operations_wait_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations feature online stores create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_feature_online_stores_create(
        &self,
        args: &AiplatformProjectsLocationsFeatureOnlineStoresCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_feature_online_stores_create_builder(
            &self.http_client,
            &args.parent,
            &args.featureOnlineStoreId,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_feature_online_stores_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations feature online stores delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_feature_online_stores_delete(
        &self,
        args: &AiplatformProjectsLocationsFeatureOnlineStoresDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_feature_online_stores_delete_builder(
            &self.http_client,
            &args.name,
            &args.force,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_feature_online_stores_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations feature online stores get iam policy.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleIamV1Policy result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_feature_online_stores_get_iam_policy(
        &self,
        args: &AiplatformProjectsLocationsFeatureOnlineStoresGetIamPolicyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleIamV1Policy, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_feature_online_stores_get_iam_policy_builder(
            &self.http_client,
            &args.resource,
            &args.options.requestedPolicyVersion,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_feature_online_stores_get_iam_policy_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations feature online stores patch.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_feature_online_stores_patch(
        &self,
        args: &AiplatformProjectsLocationsFeatureOnlineStoresPatchArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_feature_online_stores_patch_builder(
            &self.http_client,
            &args.name,
            &args.updateMask,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_feature_online_stores_patch_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations feature online stores set iam policy.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleIamV1Policy result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_feature_online_stores_set_iam_policy(
        &self,
        args: &AiplatformProjectsLocationsFeatureOnlineStoresSetIamPolicyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleIamV1Policy, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_feature_online_stores_set_iam_policy_builder(
            &self.http_client,
            &args.resource,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_feature_online_stores_set_iam_policy_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations feature online stores test iam permissions.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleIamV1TestIamPermissionsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_feature_online_stores_test_iam_permissions(
        &self,
        args: &AiplatformProjectsLocationsFeatureOnlineStoresTestIamPermissionsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleIamV1TestIamPermissionsResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_feature_online_stores_test_iam_permissions_builder(
            &self.http_client,
            &args.resource,
            &args.permissions,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_feature_online_stores_test_iam_permissions_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations feature online stores feature views create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_feature_online_stores_feature_views_create(
        &self,
        args: &AiplatformProjectsLocationsFeatureOnlineStoresFeatureViewsCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_feature_online_stores_feature_views_create_builder(
            &self.http_client,
            &args.parent,
            &args.featureViewId,
            &args.runSyncImmediately,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_feature_online_stores_feature_views_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations feature online stores feature views delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_feature_online_stores_feature_views_delete(
        &self,
        args: &AiplatformProjectsLocationsFeatureOnlineStoresFeatureViewsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_feature_online_stores_feature_views_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_feature_online_stores_feature_views_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations feature online stores feature views direct write.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudAiplatformV1FeatureViewDirectWriteResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_feature_online_stores_feature_views_direct_write(
        &self,
        args: &AiplatformProjectsLocationsFeatureOnlineStoresFeatureViewsDirectWriteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudAiplatformV1FeatureViewDirectWriteResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_feature_online_stores_feature_views_direct_write_builder(
            &self.http_client,
            &args.featureView,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_feature_online_stores_feature_views_direct_write_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations feature online stores feature views fetch feature values.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudAiplatformV1FetchFeatureValuesResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_feature_online_stores_feature_views_fetch_feature_values(
        &self,
        args: &AiplatformProjectsLocationsFeatureOnlineStoresFeatureViewsFetchFeatureValuesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudAiplatformV1FetchFeatureValuesResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_feature_online_stores_feature_views_fetch_feature_values_builder(
            &self.http_client,
            &args.featureView,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_feature_online_stores_feature_views_fetch_feature_values_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations feature online stores feature views generate fetch access token.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudAiplatformV1GenerateFetchAccessTokenResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_feature_online_stores_feature_views_generate_fetch_access_token(
        &self,
        args: &AiplatformProjectsLocationsFeatureOnlineStoresFeatureViewsGenerateFetchAccessTokenArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudAiplatformV1GenerateFetchAccessTokenResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_feature_online_stores_feature_views_generate_fetch_access_token_builder(
            &self.http_client,
            &args.featureView,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_feature_online_stores_feature_views_generate_fetch_access_token_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations feature online stores feature views get iam policy.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleIamV1Policy result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_feature_online_stores_feature_views_get_iam_policy(
        &self,
        args: &AiplatformProjectsLocationsFeatureOnlineStoresFeatureViewsGetIamPolicyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleIamV1Policy, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_feature_online_stores_feature_views_get_iam_policy_builder(
            &self.http_client,
            &args.resource,
            &args.options.requestedPolicyVersion,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_feature_online_stores_feature_views_get_iam_policy_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations feature online stores feature views patch.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_feature_online_stores_feature_views_patch(
        &self,
        args: &AiplatformProjectsLocationsFeatureOnlineStoresFeatureViewsPatchArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_feature_online_stores_feature_views_patch_builder(
            &self.http_client,
            &args.name,
            &args.updateMask,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_feature_online_stores_feature_views_patch_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations feature online stores feature views search nearest entities.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudAiplatformV1SearchNearestEntitiesResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_feature_online_stores_feature_views_search_nearest_entities(
        &self,
        args: &AiplatformProjectsLocationsFeatureOnlineStoresFeatureViewsSearchNearestEntitiesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudAiplatformV1SearchNearestEntitiesResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_feature_online_stores_feature_views_search_nearest_entities_builder(
            &self.http_client,
            &args.featureView,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_feature_online_stores_feature_views_search_nearest_entities_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations feature online stores feature views set iam policy.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleIamV1Policy result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_feature_online_stores_feature_views_set_iam_policy(
        &self,
        args: &AiplatformProjectsLocationsFeatureOnlineStoresFeatureViewsSetIamPolicyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleIamV1Policy, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_feature_online_stores_feature_views_set_iam_policy_builder(
            &self.http_client,
            &args.resource,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_feature_online_stores_feature_views_set_iam_policy_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations feature online stores feature views sync.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudAiplatformV1SyncFeatureViewResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_feature_online_stores_feature_views_sync(
        &self,
        args: &AiplatformProjectsLocationsFeatureOnlineStoresFeatureViewsSyncArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudAiplatformV1SyncFeatureViewResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_feature_online_stores_feature_views_sync_builder(
            &self.http_client,
            &args.featureView,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_feature_online_stores_feature_views_sync_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations feature online stores feature views test iam permissions.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleIamV1TestIamPermissionsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_feature_online_stores_feature_views_test_iam_permissions(
        &self,
        args: &AiplatformProjectsLocationsFeatureOnlineStoresFeatureViewsTestIamPermissionsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleIamV1TestIamPermissionsResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_feature_online_stores_feature_views_test_iam_permissions_builder(
            &self.http_client,
            &args.resource,
            &args.permissions,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_feature_online_stores_feature_views_test_iam_permissions_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations feature online stores feature views operations delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_feature_online_stores_feature_views_operations_delete(
        &self,
        args: &AiplatformProjectsLocationsFeatureOnlineStoresFeatureViewsOperationsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_feature_online_stores_feature_views_operations_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_feature_online_stores_feature_views_operations_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations feature online stores feature views operations wait.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_feature_online_stores_feature_views_operations_wait(
        &self,
        args: &AiplatformProjectsLocationsFeatureOnlineStoresFeatureViewsOperationsWaitArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_feature_online_stores_feature_views_operations_wait_builder(
            &self.http_client,
            &args.name,
            &args.timeout,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_feature_online_stores_feature_views_operations_wait_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations feature online stores operations delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_feature_online_stores_operations_delete(
        &self,
        args: &AiplatformProjectsLocationsFeatureOnlineStoresOperationsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_feature_online_stores_operations_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_feature_online_stores_operations_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations feature online stores operations wait.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_feature_online_stores_operations_wait(
        &self,
        args: &AiplatformProjectsLocationsFeatureOnlineStoresOperationsWaitArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_feature_online_stores_operations_wait_builder(
            &self.http_client,
            &args.name,
            &args.timeout,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_feature_online_stores_operations_wait_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations featurestores batch read feature values.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_featurestores_batch_read_feature_values(
        &self,
        args: &AiplatformProjectsLocationsFeaturestoresBatchReadFeatureValuesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_featurestores_batch_read_feature_values_builder(
            &self.http_client,
            &args.featurestore,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_featurestores_batch_read_feature_values_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations featurestores create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_featurestores_create(
        &self,
        args: &AiplatformProjectsLocationsFeaturestoresCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_featurestores_create_builder(
            &self.http_client,
            &args.parent,
            &args.featurestoreId,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_featurestores_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations featurestores delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_featurestores_delete(
        &self,
        args: &AiplatformProjectsLocationsFeaturestoresDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_featurestores_delete_builder(
            &self.http_client,
            &args.name,
            &args.force,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_featurestores_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations featurestores get iam policy.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleIamV1Policy result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_featurestores_get_iam_policy(
        &self,
        args: &AiplatformProjectsLocationsFeaturestoresGetIamPolicyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleIamV1Policy, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_featurestores_get_iam_policy_builder(
            &self.http_client,
            &args.resource,
            &args.options.requestedPolicyVersion,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_featurestores_get_iam_policy_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations featurestores patch.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_featurestores_patch(
        &self,
        args: &AiplatformProjectsLocationsFeaturestoresPatchArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_featurestores_patch_builder(
            &self.http_client,
            &args.name,
            &args.updateMask,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_featurestores_patch_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations featurestores set iam policy.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleIamV1Policy result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_featurestores_set_iam_policy(
        &self,
        args: &AiplatformProjectsLocationsFeaturestoresSetIamPolicyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleIamV1Policy, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_featurestores_set_iam_policy_builder(
            &self.http_client,
            &args.resource,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_featurestores_set_iam_policy_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations featurestores test iam permissions.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleIamV1TestIamPermissionsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_featurestores_test_iam_permissions(
        &self,
        args: &AiplatformProjectsLocationsFeaturestoresTestIamPermissionsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleIamV1TestIamPermissionsResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_featurestores_test_iam_permissions_builder(
            &self.http_client,
            &args.resource,
            &args.permissions,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_featurestores_test_iam_permissions_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations featurestores entity types create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_featurestores_entity_types_create(
        &self,
        args: &AiplatformProjectsLocationsFeaturestoresEntityTypesCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_featurestores_entity_types_create_builder(
            &self.http_client,
            &args.parent,
            &args.entityTypeId,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_featurestores_entity_types_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations featurestores entity types delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_featurestores_entity_types_delete(
        &self,
        args: &AiplatformProjectsLocationsFeaturestoresEntityTypesDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_featurestores_entity_types_delete_builder(
            &self.http_client,
            &args.name,
            &args.force,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_featurestores_entity_types_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations featurestores entity types delete feature values.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_featurestores_entity_types_delete_feature_values(
        &self,
        args: &AiplatformProjectsLocationsFeaturestoresEntityTypesDeleteFeatureValuesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_featurestores_entity_types_delete_feature_values_builder(
            &self.http_client,
            &args.entityType,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_featurestores_entity_types_delete_feature_values_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations featurestores entity types export feature values.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_featurestores_entity_types_export_feature_values(
        &self,
        args: &AiplatformProjectsLocationsFeaturestoresEntityTypesExportFeatureValuesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_featurestores_entity_types_export_feature_values_builder(
            &self.http_client,
            &args.entityType,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_featurestores_entity_types_export_feature_values_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations featurestores entity types get iam policy.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleIamV1Policy result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_featurestores_entity_types_get_iam_policy(
        &self,
        args: &AiplatformProjectsLocationsFeaturestoresEntityTypesGetIamPolicyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleIamV1Policy, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_featurestores_entity_types_get_iam_policy_builder(
            &self.http_client,
            &args.resource,
            &args.options.requestedPolicyVersion,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_featurestores_entity_types_get_iam_policy_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations featurestores entity types import feature values.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_featurestores_entity_types_import_feature_values(
        &self,
        args: &AiplatformProjectsLocationsFeaturestoresEntityTypesImportFeatureValuesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_featurestores_entity_types_import_feature_values_builder(
            &self.http_client,
            &args.entityType,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_featurestores_entity_types_import_feature_values_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations featurestores entity types patch.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudAiplatformV1EntityType result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_featurestores_entity_types_patch(
        &self,
        args: &AiplatformProjectsLocationsFeaturestoresEntityTypesPatchArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudAiplatformV1EntityType, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_featurestores_entity_types_patch_builder(
            &self.http_client,
            &args.name,
            &args.updateMask,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_featurestores_entity_types_patch_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations featurestores entity types read feature values.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudAiplatformV1ReadFeatureValuesResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_featurestores_entity_types_read_feature_values(
        &self,
        args: &AiplatformProjectsLocationsFeaturestoresEntityTypesReadFeatureValuesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudAiplatformV1ReadFeatureValuesResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_featurestores_entity_types_read_feature_values_builder(
            &self.http_client,
            &args.entityType,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_featurestores_entity_types_read_feature_values_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations featurestores entity types set iam policy.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleIamV1Policy result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_featurestores_entity_types_set_iam_policy(
        &self,
        args: &AiplatformProjectsLocationsFeaturestoresEntityTypesSetIamPolicyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleIamV1Policy, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_featurestores_entity_types_set_iam_policy_builder(
            &self.http_client,
            &args.resource,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_featurestores_entity_types_set_iam_policy_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations featurestores entity types streaming read feature values.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudAiplatformV1ReadFeatureValuesResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_featurestores_entity_types_streaming_read_feature_values(
        &self,
        args: &AiplatformProjectsLocationsFeaturestoresEntityTypesStreamingReadFeatureValuesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudAiplatformV1ReadFeatureValuesResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_featurestores_entity_types_streaming_read_feature_values_builder(
            &self.http_client,
            &args.entityType,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_featurestores_entity_types_streaming_read_feature_values_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations featurestores entity types test iam permissions.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleIamV1TestIamPermissionsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_featurestores_entity_types_test_iam_permissions(
        &self,
        args: &AiplatformProjectsLocationsFeaturestoresEntityTypesTestIamPermissionsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleIamV1TestIamPermissionsResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_featurestores_entity_types_test_iam_permissions_builder(
            &self.http_client,
            &args.resource,
            &args.permissions,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_featurestores_entity_types_test_iam_permissions_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations featurestores entity types write feature values.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudAiplatformV1WriteFeatureValuesResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_featurestores_entity_types_write_feature_values(
        &self,
        args: &AiplatformProjectsLocationsFeaturestoresEntityTypesWriteFeatureValuesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudAiplatformV1WriteFeatureValuesResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_featurestores_entity_types_write_feature_values_builder(
            &self.http_client,
            &args.entityType,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_featurestores_entity_types_write_feature_values_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations featurestores entity types features batch create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_featurestores_entity_types_features_batch_create(
        &self,
        args: &AiplatformProjectsLocationsFeaturestoresEntityTypesFeaturesBatchCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_featurestores_entity_types_features_batch_create_builder(
            &self.http_client,
            &args.parent,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_featurestores_entity_types_features_batch_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations featurestores entity types features create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_featurestores_entity_types_features_create(
        &self,
        args: &AiplatformProjectsLocationsFeaturestoresEntityTypesFeaturesCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_featurestores_entity_types_features_create_builder(
            &self.http_client,
            &args.parent,
            &args.featureId,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_featurestores_entity_types_features_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations featurestores entity types features delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_featurestores_entity_types_features_delete(
        &self,
        args: &AiplatformProjectsLocationsFeaturestoresEntityTypesFeaturesDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_featurestores_entity_types_features_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_featurestores_entity_types_features_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations featurestores entity types features patch.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudAiplatformV1Feature result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_featurestores_entity_types_features_patch(
        &self,
        args: &AiplatformProjectsLocationsFeaturestoresEntityTypesFeaturesPatchArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudAiplatformV1Feature, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_featurestores_entity_types_features_patch_builder(
            &self.http_client,
            &args.name,
            &args.updateMask,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_featurestores_entity_types_features_patch_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations featurestores entity types features operations cancel.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_featurestores_entity_types_features_operations_cancel(
        &self,
        args: &AiplatformProjectsLocationsFeaturestoresEntityTypesFeaturesOperationsCancelArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_featurestores_entity_types_features_operations_cancel_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_featurestores_entity_types_features_operations_cancel_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations featurestores entity types features operations delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_featurestores_entity_types_features_operations_delete(
        &self,
        args: &AiplatformProjectsLocationsFeaturestoresEntityTypesFeaturesOperationsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_featurestores_entity_types_features_operations_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_featurestores_entity_types_features_operations_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations featurestores entity types features operations wait.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_featurestores_entity_types_features_operations_wait(
        &self,
        args: &AiplatformProjectsLocationsFeaturestoresEntityTypesFeaturesOperationsWaitArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_featurestores_entity_types_features_operations_wait_builder(
            &self.http_client,
            &args.name,
            &args.timeout,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_featurestores_entity_types_features_operations_wait_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations featurestores entity types operations cancel.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_featurestores_entity_types_operations_cancel(
        &self,
        args: &AiplatformProjectsLocationsFeaturestoresEntityTypesOperationsCancelArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_featurestores_entity_types_operations_cancel_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_featurestores_entity_types_operations_cancel_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations featurestores entity types operations delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_featurestores_entity_types_operations_delete(
        &self,
        args: &AiplatformProjectsLocationsFeaturestoresEntityTypesOperationsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_featurestores_entity_types_operations_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_featurestores_entity_types_operations_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations featurestores entity types operations wait.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_featurestores_entity_types_operations_wait(
        &self,
        args: &AiplatformProjectsLocationsFeaturestoresEntityTypesOperationsWaitArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_featurestores_entity_types_operations_wait_builder(
            &self.http_client,
            &args.name,
            &args.timeout,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_featurestores_entity_types_operations_wait_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations featurestores operations cancel.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_featurestores_operations_cancel(
        &self,
        args: &AiplatformProjectsLocationsFeaturestoresOperationsCancelArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_featurestores_operations_cancel_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_featurestores_operations_cancel_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations featurestores operations delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_featurestores_operations_delete(
        &self,
        args: &AiplatformProjectsLocationsFeaturestoresOperationsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_featurestores_operations_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_featurestores_operations_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations featurestores operations wait.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_featurestores_operations_wait(
        &self,
        args: &AiplatformProjectsLocationsFeaturestoresOperationsWaitArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_featurestores_operations_wait_builder(
            &self.http_client,
            &args.name,
            &args.timeout,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_featurestores_operations_wait_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations hyperparameter tuning jobs cancel.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_hyperparameter_tuning_jobs_cancel(
        &self,
        args: &AiplatformProjectsLocationsHyperparameterTuningJobsCancelArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_hyperparameter_tuning_jobs_cancel_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_hyperparameter_tuning_jobs_cancel_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations hyperparameter tuning jobs create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudAiplatformV1HyperparameterTuningJob result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_hyperparameter_tuning_jobs_create(
        &self,
        args: &AiplatformProjectsLocationsHyperparameterTuningJobsCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudAiplatformV1HyperparameterTuningJob, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_hyperparameter_tuning_jobs_create_builder(
            &self.http_client,
            &args.parent,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_hyperparameter_tuning_jobs_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations hyperparameter tuning jobs delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_hyperparameter_tuning_jobs_delete(
        &self,
        args: &AiplatformProjectsLocationsHyperparameterTuningJobsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_hyperparameter_tuning_jobs_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_hyperparameter_tuning_jobs_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations hyperparameter tuning jobs operations cancel.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_hyperparameter_tuning_jobs_operations_cancel(
        &self,
        args: &AiplatformProjectsLocationsHyperparameterTuningJobsOperationsCancelArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_hyperparameter_tuning_jobs_operations_cancel_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_hyperparameter_tuning_jobs_operations_cancel_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations hyperparameter tuning jobs operations delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_hyperparameter_tuning_jobs_operations_delete(
        &self,
        args: &AiplatformProjectsLocationsHyperparameterTuningJobsOperationsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_hyperparameter_tuning_jobs_operations_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_hyperparameter_tuning_jobs_operations_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations hyperparameter tuning jobs operations wait.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_hyperparameter_tuning_jobs_operations_wait(
        &self,
        args: &AiplatformProjectsLocationsHyperparameterTuningJobsOperationsWaitArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_hyperparameter_tuning_jobs_operations_wait_builder(
            &self.http_client,
            &args.name,
            &args.timeout,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_hyperparameter_tuning_jobs_operations_wait_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations index endpoints create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_index_endpoints_create(
        &self,
        args: &AiplatformProjectsLocationsIndexEndpointsCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_index_endpoints_create_builder(
            &self.http_client,
            &args.parent,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_index_endpoints_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations index endpoints delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_index_endpoints_delete(
        &self,
        args: &AiplatformProjectsLocationsIndexEndpointsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_index_endpoints_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_index_endpoints_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations index endpoints deploy index.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_index_endpoints_deploy_index(
        &self,
        args: &AiplatformProjectsLocationsIndexEndpointsDeployIndexArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_index_endpoints_deploy_index_builder(
            &self.http_client,
            &args.indexEndpoint,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_index_endpoints_deploy_index_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations index endpoints find neighbors.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudAiplatformV1FindNeighborsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_index_endpoints_find_neighbors(
        &self,
        args: &AiplatformProjectsLocationsIndexEndpointsFindNeighborsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudAiplatformV1FindNeighborsResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_index_endpoints_find_neighbors_builder(
            &self.http_client,
            &args.indexEndpoint,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_index_endpoints_find_neighbors_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations index endpoints mutate deployed index.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_index_endpoints_mutate_deployed_index(
        &self,
        args: &AiplatformProjectsLocationsIndexEndpointsMutateDeployedIndexArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_index_endpoints_mutate_deployed_index_builder(
            &self.http_client,
            &args.indexEndpoint,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_index_endpoints_mutate_deployed_index_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations index endpoints patch.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudAiplatformV1IndexEndpoint result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_index_endpoints_patch(
        &self,
        args: &AiplatformProjectsLocationsIndexEndpointsPatchArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudAiplatformV1IndexEndpoint, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_index_endpoints_patch_builder(
            &self.http_client,
            &args.name,
            &args.updateMask,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_index_endpoints_patch_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations index endpoints read index datapoints.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudAiplatformV1ReadIndexDatapointsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_index_endpoints_read_index_datapoints(
        &self,
        args: &AiplatformProjectsLocationsIndexEndpointsReadIndexDatapointsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudAiplatformV1ReadIndexDatapointsResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_index_endpoints_read_index_datapoints_builder(
            &self.http_client,
            &args.indexEndpoint,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_index_endpoints_read_index_datapoints_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations index endpoints undeploy index.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_index_endpoints_undeploy_index(
        &self,
        args: &AiplatformProjectsLocationsIndexEndpointsUndeployIndexArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_index_endpoints_undeploy_index_builder(
            &self.http_client,
            &args.indexEndpoint,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_index_endpoints_undeploy_index_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations index endpoints operations cancel.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_index_endpoints_operations_cancel(
        &self,
        args: &AiplatformProjectsLocationsIndexEndpointsOperationsCancelArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_index_endpoints_operations_cancel_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_index_endpoints_operations_cancel_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations index endpoints operations delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_index_endpoints_operations_delete(
        &self,
        args: &AiplatformProjectsLocationsIndexEndpointsOperationsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_index_endpoints_operations_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_index_endpoints_operations_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations index endpoints operations wait.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_index_endpoints_operations_wait(
        &self,
        args: &AiplatformProjectsLocationsIndexEndpointsOperationsWaitArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_index_endpoints_operations_wait_builder(
            &self.http_client,
            &args.name,
            &args.timeout,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_index_endpoints_operations_wait_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations indexes create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_indexes_create(
        &self,
        args: &AiplatformProjectsLocationsIndexesCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_indexes_create_builder(
            &self.http_client,
            &args.parent,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_indexes_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations indexes delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_indexes_delete(
        &self,
        args: &AiplatformProjectsLocationsIndexesDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_indexes_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_indexes_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations indexes patch.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_indexes_patch(
        &self,
        args: &AiplatformProjectsLocationsIndexesPatchArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_indexes_patch_builder(
            &self.http_client,
            &args.name,
            &args.updateMask,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_indexes_patch_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations indexes remove datapoints.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudAiplatformV1RemoveDatapointsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_indexes_remove_datapoints(
        &self,
        args: &AiplatformProjectsLocationsIndexesRemoveDatapointsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudAiplatformV1RemoveDatapointsResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_indexes_remove_datapoints_builder(
            &self.http_client,
            &args.index,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_indexes_remove_datapoints_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations indexes upsert datapoints.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudAiplatformV1UpsertDatapointsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_indexes_upsert_datapoints(
        &self,
        args: &AiplatformProjectsLocationsIndexesUpsertDatapointsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudAiplatformV1UpsertDatapointsResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_indexes_upsert_datapoints_builder(
            &self.http_client,
            &args.index,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_indexes_upsert_datapoints_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations indexes operations cancel.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_indexes_operations_cancel(
        &self,
        args: &AiplatformProjectsLocationsIndexesOperationsCancelArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_indexes_operations_cancel_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_indexes_operations_cancel_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations indexes operations delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_indexes_operations_delete(
        &self,
        args: &AiplatformProjectsLocationsIndexesOperationsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_indexes_operations_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_indexes_operations_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations indexes operations wait.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_indexes_operations_wait(
        &self,
        args: &AiplatformProjectsLocationsIndexesOperationsWaitArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_indexes_operations_wait_builder(
            &self.http_client,
            &args.name,
            &args.timeout,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_indexes_operations_wait_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations metadata stores create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_metadata_stores_create(
        &self,
        args: &AiplatformProjectsLocationsMetadataStoresCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_metadata_stores_create_builder(
            &self.http_client,
            &args.parent,
            &args.metadataStoreId,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_metadata_stores_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations metadata stores delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_metadata_stores_delete(
        &self,
        args: &AiplatformProjectsLocationsMetadataStoresDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_metadata_stores_delete_builder(
            &self.http_client,
            &args.name,
            &args.force,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_metadata_stores_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations metadata stores artifacts create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudAiplatformV1Artifact result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_metadata_stores_artifacts_create(
        &self,
        args: &AiplatformProjectsLocationsMetadataStoresArtifactsCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudAiplatformV1Artifact, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_metadata_stores_artifacts_create_builder(
            &self.http_client,
            &args.parent,
            &args.artifactId,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_metadata_stores_artifacts_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations metadata stores artifacts delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_metadata_stores_artifacts_delete(
        &self,
        args: &AiplatformProjectsLocationsMetadataStoresArtifactsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_metadata_stores_artifacts_delete_builder(
            &self.http_client,
            &args.name,
            &args.etag,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_metadata_stores_artifacts_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations metadata stores artifacts patch.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudAiplatformV1Artifact result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_metadata_stores_artifacts_patch(
        &self,
        args: &AiplatformProjectsLocationsMetadataStoresArtifactsPatchArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudAiplatformV1Artifact, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_metadata_stores_artifacts_patch_builder(
            &self.http_client,
            &args.name,
            &args.allowMissing,
            &args.updateMask,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_metadata_stores_artifacts_patch_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations metadata stores artifacts purge.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_metadata_stores_artifacts_purge(
        &self,
        args: &AiplatformProjectsLocationsMetadataStoresArtifactsPurgeArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_metadata_stores_artifacts_purge_builder(
            &self.http_client,
            &args.parent,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_metadata_stores_artifacts_purge_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations metadata stores artifacts operations cancel.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_metadata_stores_artifacts_operations_cancel(
        &self,
        args: &AiplatformProjectsLocationsMetadataStoresArtifactsOperationsCancelArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_metadata_stores_artifacts_operations_cancel_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_metadata_stores_artifacts_operations_cancel_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations metadata stores artifacts operations delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_metadata_stores_artifacts_operations_delete(
        &self,
        args: &AiplatformProjectsLocationsMetadataStoresArtifactsOperationsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_metadata_stores_artifacts_operations_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_metadata_stores_artifacts_operations_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations metadata stores artifacts operations wait.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_metadata_stores_artifacts_operations_wait(
        &self,
        args: &AiplatformProjectsLocationsMetadataStoresArtifactsOperationsWaitArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_metadata_stores_artifacts_operations_wait_builder(
            &self.http_client,
            &args.name,
            &args.timeout,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_metadata_stores_artifacts_operations_wait_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations metadata stores contexts add context artifacts and executions.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudAiplatformV1AddContextArtifactsAndExecutionsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_metadata_stores_contexts_add_context_artifacts_and_executions(
        &self,
        args: &AiplatformProjectsLocationsMetadataStoresContextsAddContextArtifactsAndExecutionsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudAiplatformV1AddContextArtifactsAndExecutionsResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_metadata_stores_contexts_add_context_artifacts_and_executions_builder(
            &self.http_client,
            &args.context,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_metadata_stores_contexts_add_context_artifacts_and_executions_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations metadata stores contexts add context children.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudAiplatformV1AddContextChildrenResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_metadata_stores_contexts_add_context_children(
        &self,
        args: &AiplatformProjectsLocationsMetadataStoresContextsAddContextChildrenArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudAiplatformV1AddContextChildrenResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_metadata_stores_contexts_add_context_children_builder(
            &self.http_client,
            &args.context,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_metadata_stores_contexts_add_context_children_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations metadata stores contexts create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudAiplatformV1Context result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_metadata_stores_contexts_create(
        &self,
        args: &AiplatformProjectsLocationsMetadataStoresContextsCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudAiplatformV1Context, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_metadata_stores_contexts_create_builder(
            &self.http_client,
            &args.parent,
            &args.contextId,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_metadata_stores_contexts_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations metadata stores contexts delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_metadata_stores_contexts_delete(
        &self,
        args: &AiplatformProjectsLocationsMetadataStoresContextsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_metadata_stores_contexts_delete_builder(
            &self.http_client,
            &args.name,
            &args.etag,
            &args.force,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_metadata_stores_contexts_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations metadata stores contexts patch.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudAiplatformV1Context result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_metadata_stores_contexts_patch(
        &self,
        args: &AiplatformProjectsLocationsMetadataStoresContextsPatchArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudAiplatformV1Context, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_metadata_stores_contexts_patch_builder(
            &self.http_client,
            &args.name,
            &args.allowMissing,
            &args.updateMask,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_metadata_stores_contexts_patch_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations metadata stores contexts purge.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_metadata_stores_contexts_purge(
        &self,
        args: &AiplatformProjectsLocationsMetadataStoresContextsPurgeArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_metadata_stores_contexts_purge_builder(
            &self.http_client,
            &args.parent,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_metadata_stores_contexts_purge_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations metadata stores contexts remove context children.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudAiplatformV1RemoveContextChildrenResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_metadata_stores_contexts_remove_context_children(
        &self,
        args: &AiplatformProjectsLocationsMetadataStoresContextsRemoveContextChildrenArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudAiplatformV1RemoveContextChildrenResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_metadata_stores_contexts_remove_context_children_builder(
            &self.http_client,
            &args.context,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_metadata_stores_contexts_remove_context_children_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations metadata stores contexts operations cancel.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_metadata_stores_contexts_operations_cancel(
        &self,
        args: &AiplatformProjectsLocationsMetadataStoresContextsOperationsCancelArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_metadata_stores_contexts_operations_cancel_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_metadata_stores_contexts_operations_cancel_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations metadata stores contexts operations delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_metadata_stores_contexts_operations_delete(
        &self,
        args: &AiplatformProjectsLocationsMetadataStoresContextsOperationsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_metadata_stores_contexts_operations_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_metadata_stores_contexts_operations_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations metadata stores contexts operations wait.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_metadata_stores_contexts_operations_wait(
        &self,
        args: &AiplatformProjectsLocationsMetadataStoresContextsOperationsWaitArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_metadata_stores_contexts_operations_wait_builder(
            &self.http_client,
            &args.name,
            &args.timeout,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_metadata_stores_contexts_operations_wait_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations metadata stores executions add execution events.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudAiplatformV1AddExecutionEventsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_metadata_stores_executions_add_execution_events(
        &self,
        args: &AiplatformProjectsLocationsMetadataStoresExecutionsAddExecutionEventsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudAiplatformV1AddExecutionEventsResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_metadata_stores_executions_add_execution_events_builder(
            &self.http_client,
            &args.execution,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_metadata_stores_executions_add_execution_events_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations metadata stores executions create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudAiplatformV1Execution result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_metadata_stores_executions_create(
        &self,
        args: &AiplatformProjectsLocationsMetadataStoresExecutionsCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudAiplatformV1Execution, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_metadata_stores_executions_create_builder(
            &self.http_client,
            &args.parent,
            &args.executionId,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_metadata_stores_executions_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations metadata stores executions delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_metadata_stores_executions_delete(
        &self,
        args: &AiplatformProjectsLocationsMetadataStoresExecutionsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_metadata_stores_executions_delete_builder(
            &self.http_client,
            &args.name,
            &args.etag,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_metadata_stores_executions_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations metadata stores executions patch.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudAiplatformV1Execution result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_metadata_stores_executions_patch(
        &self,
        args: &AiplatformProjectsLocationsMetadataStoresExecutionsPatchArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudAiplatformV1Execution, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_metadata_stores_executions_patch_builder(
            &self.http_client,
            &args.name,
            &args.allowMissing,
            &args.updateMask,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_metadata_stores_executions_patch_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations metadata stores executions purge.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_metadata_stores_executions_purge(
        &self,
        args: &AiplatformProjectsLocationsMetadataStoresExecutionsPurgeArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_metadata_stores_executions_purge_builder(
            &self.http_client,
            &args.parent,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_metadata_stores_executions_purge_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations metadata stores executions operations cancel.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_metadata_stores_executions_operations_cancel(
        &self,
        args: &AiplatformProjectsLocationsMetadataStoresExecutionsOperationsCancelArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_metadata_stores_executions_operations_cancel_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_metadata_stores_executions_operations_cancel_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations metadata stores executions operations delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_metadata_stores_executions_operations_delete(
        &self,
        args: &AiplatformProjectsLocationsMetadataStoresExecutionsOperationsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_metadata_stores_executions_operations_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_metadata_stores_executions_operations_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations metadata stores executions operations wait.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_metadata_stores_executions_operations_wait(
        &self,
        args: &AiplatformProjectsLocationsMetadataStoresExecutionsOperationsWaitArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_metadata_stores_executions_operations_wait_builder(
            &self.http_client,
            &args.name,
            &args.timeout,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_metadata_stores_executions_operations_wait_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations metadata stores metadata schemas create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudAiplatformV1MetadataSchema result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_metadata_stores_metadata_schemas_create(
        &self,
        args: &AiplatformProjectsLocationsMetadataStoresMetadataSchemasCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudAiplatformV1MetadataSchema, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_metadata_stores_metadata_schemas_create_builder(
            &self.http_client,
            &args.parent,
            &args.metadataSchemaId,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_metadata_stores_metadata_schemas_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations metadata stores operations cancel.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_metadata_stores_operations_cancel(
        &self,
        args: &AiplatformProjectsLocationsMetadataStoresOperationsCancelArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_metadata_stores_operations_cancel_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_metadata_stores_operations_cancel_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations metadata stores operations delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_metadata_stores_operations_delete(
        &self,
        args: &AiplatformProjectsLocationsMetadataStoresOperationsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_metadata_stores_operations_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_metadata_stores_operations_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations metadata stores operations wait.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_metadata_stores_operations_wait(
        &self,
        args: &AiplatformProjectsLocationsMetadataStoresOperationsWaitArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_metadata_stores_operations_wait_builder(
            &self.http_client,
            &args.name,
            &args.timeout,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_metadata_stores_operations_wait_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations migratable resources batch migrate.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_migratable_resources_batch_migrate(
        &self,
        args: &AiplatformProjectsLocationsMigratableResourcesBatchMigrateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_migratable_resources_batch_migrate_builder(
            &self.http_client,
            &args.parent,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_migratable_resources_batch_migrate_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations migratable resources search.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudAiplatformV1SearchMigratableResourcesResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_migratable_resources_search(
        &self,
        args: &AiplatformProjectsLocationsMigratableResourcesSearchArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudAiplatformV1SearchMigratableResourcesResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_migratable_resources_search_builder(
            &self.http_client,
            &args.parent,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_migratable_resources_search_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations migratable resources operations cancel.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_migratable_resources_operations_cancel(
        &self,
        args: &AiplatformProjectsLocationsMigratableResourcesOperationsCancelArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_migratable_resources_operations_cancel_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_migratable_resources_operations_cancel_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations migratable resources operations delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_migratable_resources_operations_delete(
        &self,
        args: &AiplatformProjectsLocationsMigratableResourcesOperationsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_migratable_resources_operations_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_migratable_resources_operations_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations migratable resources operations wait.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_migratable_resources_operations_wait(
        &self,
        args: &AiplatformProjectsLocationsMigratableResourcesOperationsWaitArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_migratable_resources_operations_wait_builder(
            &self.http_client,
            &args.name,
            &args.timeout,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_migratable_resources_operations_wait_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations model deployment monitoring jobs create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudAiplatformV1ModelDeploymentMonitoringJob result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_model_deployment_monitoring_jobs_create(
        &self,
        args: &AiplatformProjectsLocationsModelDeploymentMonitoringJobsCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudAiplatformV1ModelDeploymentMonitoringJob, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_model_deployment_monitoring_jobs_create_builder(
            &self.http_client,
            &args.parent,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_model_deployment_monitoring_jobs_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations model deployment monitoring jobs delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_model_deployment_monitoring_jobs_delete(
        &self,
        args: &AiplatformProjectsLocationsModelDeploymentMonitoringJobsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_model_deployment_monitoring_jobs_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_model_deployment_monitoring_jobs_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations model deployment monitoring jobs patch.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_model_deployment_monitoring_jobs_patch(
        &self,
        args: &AiplatformProjectsLocationsModelDeploymentMonitoringJobsPatchArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_model_deployment_monitoring_jobs_patch_builder(
            &self.http_client,
            &args.name,
            &args.updateMask,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_model_deployment_monitoring_jobs_patch_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations model deployment monitoring jobs pause.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_model_deployment_monitoring_jobs_pause(
        &self,
        args: &AiplatformProjectsLocationsModelDeploymentMonitoringJobsPauseArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_model_deployment_monitoring_jobs_pause_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_model_deployment_monitoring_jobs_pause_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations model deployment monitoring jobs resume.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_model_deployment_monitoring_jobs_resume(
        &self,
        args: &AiplatformProjectsLocationsModelDeploymentMonitoringJobsResumeArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_model_deployment_monitoring_jobs_resume_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_model_deployment_monitoring_jobs_resume_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations model deployment monitoring jobs search model deployment monitoring stats anomalies.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudAiplatformV1SearchModelDeploymentMonitoringStatsAnomaliesResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_model_deployment_monitoring_jobs_search_model_deployment_monitoring_stats_anomalies(
        &self,
        args: &AiplatformProjectsLocationsModelDeploymentMonitoringJobsSearchModelDeploymentMonitoringStatsAnomaliesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudAiplatformV1SearchModelDeploymentMonitoringStatsAnomaliesResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_model_deployment_monitoring_jobs_search_model_deployment_monitoring_stats_anomalies_builder(
            &self.http_client,
            &args.modelDeploymentMonitoringJob,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_model_deployment_monitoring_jobs_search_model_deployment_monitoring_stats_anomalies_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations model deployment monitoring jobs operations cancel.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_model_deployment_monitoring_jobs_operations_cancel(
        &self,
        args: &AiplatformProjectsLocationsModelDeploymentMonitoringJobsOperationsCancelArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_model_deployment_monitoring_jobs_operations_cancel_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_model_deployment_monitoring_jobs_operations_cancel_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations model deployment monitoring jobs operations delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_model_deployment_monitoring_jobs_operations_delete(
        &self,
        args: &AiplatformProjectsLocationsModelDeploymentMonitoringJobsOperationsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_model_deployment_monitoring_jobs_operations_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_model_deployment_monitoring_jobs_operations_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations model deployment monitoring jobs operations wait.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_model_deployment_monitoring_jobs_operations_wait(
        &self,
        args: &AiplatformProjectsLocationsModelDeploymentMonitoringJobsOperationsWaitArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_model_deployment_monitoring_jobs_operations_wait_builder(
            &self.http_client,
            &args.name,
            &args.timeout,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_model_deployment_monitoring_jobs_operations_wait_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations models copy.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_models_copy(
        &self,
        args: &AiplatformProjectsLocationsModelsCopyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_models_copy_builder(
            &self.http_client,
            &args.parent,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_models_copy_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations models delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_models_delete(
        &self,
        args: &AiplatformProjectsLocationsModelsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_models_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_models_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations models delete version.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_models_delete_version(
        &self,
        args: &AiplatformProjectsLocationsModelsDeleteVersionArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_models_delete_version_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_models_delete_version_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations models export.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_models_export(
        &self,
        args: &AiplatformProjectsLocationsModelsExportArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_models_export_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_models_export_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations models get iam policy.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleIamV1Policy result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_models_get_iam_policy(
        &self,
        args: &AiplatformProjectsLocationsModelsGetIamPolicyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleIamV1Policy, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_models_get_iam_policy_builder(
            &self.http_client,
            &args.resource,
            &args.options.requestedPolicyVersion,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_models_get_iam_policy_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations models merge version aliases.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudAiplatformV1Model result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_models_merge_version_aliases(
        &self,
        args: &AiplatformProjectsLocationsModelsMergeVersionAliasesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudAiplatformV1Model, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_models_merge_version_aliases_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_models_merge_version_aliases_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations models patch.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudAiplatformV1Model result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_models_patch(
        &self,
        args: &AiplatformProjectsLocationsModelsPatchArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudAiplatformV1Model, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_models_patch_builder(
            &self.http_client,
            &args.name,
            &args.updateMask,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_models_patch_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations models set iam policy.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleIamV1Policy result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_models_set_iam_policy(
        &self,
        args: &AiplatformProjectsLocationsModelsSetIamPolicyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleIamV1Policy, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_models_set_iam_policy_builder(
            &self.http_client,
            &args.resource,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_models_set_iam_policy_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations models test iam permissions.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleIamV1TestIamPermissionsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_models_test_iam_permissions(
        &self,
        args: &AiplatformProjectsLocationsModelsTestIamPermissionsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleIamV1TestIamPermissionsResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_models_test_iam_permissions_builder(
            &self.http_client,
            &args.resource,
            &args.permissions,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_models_test_iam_permissions_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations models update explanation dataset.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_models_update_explanation_dataset(
        &self,
        args: &AiplatformProjectsLocationsModelsUpdateExplanationDatasetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_models_update_explanation_dataset_builder(
            &self.http_client,
            &args.model,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_models_update_explanation_dataset_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations models upload.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_models_upload(
        &self,
        args: &AiplatformProjectsLocationsModelsUploadArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_models_upload_builder(
            &self.http_client,
            &args.parent,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_models_upload_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations models evaluations import.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudAiplatformV1ModelEvaluation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_models_evaluations_import(
        &self,
        args: &AiplatformProjectsLocationsModelsEvaluationsImportArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudAiplatformV1ModelEvaluation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_models_evaluations_import_builder(
            &self.http_client,
            &args.parent,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_models_evaluations_import_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations models evaluations operations cancel.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_models_evaluations_operations_cancel(
        &self,
        args: &AiplatformProjectsLocationsModelsEvaluationsOperationsCancelArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_models_evaluations_operations_cancel_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_models_evaluations_operations_cancel_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations models evaluations operations delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_models_evaluations_operations_delete(
        &self,
        args: &AiplatformProjectsLocationsModelsEvaluationsOperationsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_models_evaluations_operations_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_models_evaluations_operations_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations models evaluations operations wait.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_models_evaluations_operations_wait(
        &self,
        args: &AiplatformProjectsLocationsModelsEvaluationsOperationsWaitArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_models_evaluations_operations_wait_builder(
            &self.http_client,
            &args.name,
            &args.timeout,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_models_evaluations_operations_wait_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations models evaluations slices batch import.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudAiplatformV1BatchImportEvaluatedAnnotationsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_models_evaluations_slices_batch_import(
        &self,
        args: &AiplatformProjectsLocationsModelsEvaluationsSlicesBatchImportArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudAiplatformV1BatchImportEvaluatedAnnotationsResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_models_evaluations_slices_batch_import_builder(
            &self.http_client,
            &args.parent,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_models_evaluations_slices_batch_import_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations models operations cancel.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_models_operations_cancel(
        &self,
        args: &AiplatformProjectsLocationsModelsOperationsCancelArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_models_operations_cancel_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_models_operations_cancel_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations models operations delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_models_operations_delete(
        &self,
        args: &AiplatformProjectsLocationsModelsOperationsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_models_operations_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_models_operations_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations models operations wait.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_models_operations_wait(
        &self,
        args: &AiplatformProjectsLocationsModelsOperationsWaitArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_models_operations_wait_builder(
            &self.http_client,
            &args.name,
            &args.timeout,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_models_operations_wait_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations nas jobs cancel.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_nas_jobs_cancel(
        &self,
        args: &AiplatformProjectsLocationsNasJobsCancelArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_nas_jobs_cancel_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_nas_jobs_cancel_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations nas jobs create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudAiplatformV1NasJob result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_nas_jobs_create(
        &self,
        args: &AiplatformProjectsLocationsNasJobsCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudAiplatformV1NasJob, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_nas_jobs_create_builder(
            &self.http_client,
            &args.parent,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_nas_jobs_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations nas jobs delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_nas_jobs_delete(
        &self,
        args: &AiplatformProjectsLocationsNasJobsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_nas_jobs_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_nas_jobs_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations notebook execution jobs create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_notebook_execution_jobs_create(
        &self,
        args: &AiplatformProjectsLocationsNotebookExecutionJobsCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_notebook_execution_jobs_create_builder(
            &self.http_client,
            &args.parent,
            &args.notebookExecutionJobId,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_notebook_execution_jobs_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations notebook execution jobs delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_notebook_execution_jobs_delete(
        &self,
        args: &AiplatformProjectsLocationsNotebookExecutionJobsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_notebook_execution_jobs_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_notebook_execution_jobs_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations notebook execution jobs operations cancel.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_notebook_execution_jobs_operations_cancel(
        &self,
        args: &AiplatformProjectsLocationsNotebookExecutionJobsOperationsCancelArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_notebook_execution_jobs_operations_cancel_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_notebook_execution_jobs_operations_cancel_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations notebook execution jobs operations delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_notebook_execution_jobs_operations_delete(
        &self,
        args: &AiplatformProjectsLocationsNotebookExecutionJobsOperationsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_notebook_execution_jobs_operations_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_notebook_execution_jobs_operations_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations notebook execution jobs operations wait.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_notebook_execution_jobs_operations_wait(
        &self,
        args: &AiplatformProjectsLocationsNotebookExecutionJobsOperationsWaitArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_notebook_execution_jobs_operations_wait_builder(
            &self.http_client,
            &args.name,
            &args.timeout,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_notebook_execution_jobs_operations_wait_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations notebook runtime templates create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_notebook_runtime_templates_create(
        &self,
        args: &AiplatformProjectsLocationsNotebookRuntimeTemplatesCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_notebook_runtime_templates_create_builder(
            &self.http_client,
            &args.parent,
            &args.notebookRuntimeTemplateId,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_notebook_runtime_templates_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations notebook runtime templates delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_notebook_runtime_templates_delete(
        &self,
        args: &AiplatformProjectsLocationsNotebookRuntimeTemplatesDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_notebook_runtime_templates_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_notebook_runtime_templates_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations notebook runtime templates get iam policy.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleIamV1Policy result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_notebook_runtime_templates_get_iam_policy(
        &self,
        args: &AiplatformProjectsLocationsNotebookRuntimeTemplatesGetIamPolicyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleIamV1Policy, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_notebook_runtime_templates_get_iam_policy_builder(
            &self.http_client,
            &args.resource,
            &args.options.requestedPolicyVersion,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_notebook_runtime_templates_get_iam_policy_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations notebook runtime templates patch.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudAiplatformV1NotebookRuntimeTemplate result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_notebook_runtime_templates_patch(
        &self,
        args: &AiplatformProjectsLocationsNotebookRuntimeTemplatesPatchArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudAiplatformV1NotebookRuntimeTemplate, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_notebook_runtime_templates_patch_builder(
            &self.http_client,
            &args.name,
            &args.updateMask,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_notebook_runtime_templates_patch_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations notebook runtime templates set iam policy.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleIamV1Policy result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_notebook_runtime_templates_set_iam_policy(
        &self,
        args: &AiplatformProjectsLocationsNotebookRuntimeTemplatesSetIamPolicyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleIamV1Policy, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_notebook_runtime_templates_set_iam_policy_builder(
            &self.http_client,
            &args.resource,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_notebook_runtime_templates_set_iam_policy_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations notebook runtime templates test iam permissions.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleIamV1TestIamPermissionsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_notebook_runtime_templates_test_iam_permissions(
        &self,
        args: &AiplatformProjectsLocationsNotebookRuntimeTemplatesTestIamPermissionsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleIamV1TestIamPermissionsResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_notebook_runtime_templates_test_iam_permissions_builder(
            &self.http_client,
            &args.resource,
            &args.permissions,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_notebook_runtime_templates_test_iam_permissions_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations notebook runtime templates operations cancel.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_notebook_runtime_templates_operations_cancel(
        &self,
        args: &AiplatformProjectsLocationsNotebookRuntimeTemplatesOperationsCancelArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_notebook_runtime_templates_operations_cancel_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_notebook_runtime_templates_operations_cancel_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations notebook runtime templates operations delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_notebook_runtime_templates_operations_delete(
        &self,
        args: &AiplatformProjectsLocationsNotebookRuntimeTemplatesOperationsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_notebook_runtime_templates_operations_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_notebook_runtime_templates_operations_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations notebook runtime templates operations wait.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_notebook_runtime_templates_operations_wait(
        &self,
        args: &AiplatformProjectsLocationsNotebookRuntimeTemplatesOperationsWaitArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_notebook_runtime_templates_operations_wait_builder(
            &self.http_client,
            &args.name,
            &args.timeout,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_notebook_runtime_templates_operations_wait_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations notebook runtimes assign.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_notebook_runtimes_assign(
        &self,
        args: &AiplatformProjectsLocationsNotebookRuntimesAssignArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_notebook_runtimes_assign_builder(
            &self.http_client,
            &args.parent,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_notebook_runtimes_assign_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations notebook runtimes delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_notebook_runtimes_delete(
        &self,
        args: &AiplatformProjectsLocationsNotebookRuntimesDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_notebook_runtimes_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_notebook_runtimes_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations notebook runtimes start.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_notebook_runtimes_start(
        &self,
        args: &AiplatformProjectsLocationsNotebookRuntimesStartArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_notebook_runtimes_start_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_notebook_runtimes_start_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations notebook runtimes stop.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_notebook_runtimes_stop(
        &self,
        args: &AiplatformProjectsLocationsNotebookRuntimesStopArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_notebook_runtimes_stop_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_notebook_runtimes_stop_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations notebook runtimes upgrade.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_notebook_runtimes_upgrade(
        &self,
        args: &AiplatformProjectsLocationsNotebookRuntimesUpgradeArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_notebook_runtimes_upgrade_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_notebook_runtimes_upgrade_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations notebook runtimes operations cancel.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_notebook_runtimes_operations_cancel(
        &self,
        args: &AiplatformProjectsLocationsNotebookRuntimesOperationsCancelArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_notebook_runtimes_operations_cancel_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_notebook_runtimes_operations_cancel_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations notebook runtimes operations delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_notebook_runtimes_operations_delete(
        &self,
        args: &AiplatformProjectsLocationsNotebookRuntimesOperationsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_notebook_runtimes_operations_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_notebook_runtimes_operations_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations notebook runtimes operations wait.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_notebook_runtimes_operations_wait(
        &self,
        args: &AiplatformProjectsLocationsNotebookRuntimesOperationsWaitArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_notebook_runtimes_operations_wait_builder(
            &self.http_client,
            &args.name,
            &args.timeout,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_notebook_runtimes_operations_wait_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations operations cancel.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_operations_cancel(
        &self,
        args: &AiplatformProjectsLocationsOperationsCancelArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_operations_cancel_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_operations_cancel_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations operations delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_operations_delete(
        &self,
        args: &AiplatformProjectsLocationsOperationsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_operations_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_operations_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations operations wait.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_operations_wait(
        &self,
        args: &AiplatformProjectsLocationsOperationsWaitArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_operations_wait_builder(
            &self.http_client,
            &args.name,
            &args.timeout,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_operations_wait_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations persistent resources create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_persistent_resources_create(
        &self,
        args: &AiplatformProjectsLocationsPersistentResourcesCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_persistent_resources_create_builder(
            &self.http_client,
            &args.parent,
            &args.persistentResourceId,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_persistent_resources_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations persistent resources delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_persistent_resources_delete(
        &self,
        args: &AiplatformProjectsLocationsPersistentResourcesDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_persistent_resources_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_persistent_resources_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations persistent resources patch.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_persistent_resources_patch(
        &self,
        args: &AiplatformProjectsLocationsPersistentResourcesPatchArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_persistent_resources_patch_builder(
            &self.http_client,
            &args.name,
            &args.updateMask,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_persistent_resources_patch_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations persistent resources reboot.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_persistent_resources_reboot(
        &self,
        args: &AiplatformProjectsLocationsPersistentResourcesRebootArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_persistent_resources_reboot_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_persistent_resources_reboot_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations persistent resources operations cancel.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_persistent_resources_operations_cancel(
        &self,
        args: &AiplatformProjectsLocationsPersistentResourcesOperationsCancelArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_persistent_resources_operations_cancel_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_persistent_resources_operations_cancel_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations persistent resources operations delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_persistent_resources_operations_delete(
        &self,
        args: &AiplatformProjectsLocationsPersistentResourcesOperationsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_persistent_resources_operations_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_persistent_resources_operations_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations persistent resources operations wait.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_persistent_resources_operations_wait(
        &self,
        args: &AiplatformProjectsLocationsPersistentResourcesOperationsWaitArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_persistent_resources_operations_wait_builder(
            &self.http_client,
            &args.name,
            &args.timeout,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_persistent_resources_operations_wait_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations pipeline jobs batch cancel.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_pipeline_jobs_batch_cancel(
        &self,
        args: &AiplatformProjectsLocationsPipelineJobsBatchCancelArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_pipeline_jobs_batch_cancel_builder(
            &self.http_client,
            &args.parent,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_pipeline_jobs_batch_cancel_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations pipeline jobs batch delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_pipeline_jobs_batch_delete(
        &self,
        args: &AiplatformProjectsLocationsPipelineJobsBatchDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_pipeline_jobs_batch_delete_builder(
            &self.http_client,
            &args.parent,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_pipeline_jobs_batch_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations pipeline jobs cancel.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_pipeline_jobs_cancel(
        &self,
        args: &AiplatformProjectsLocationsPipelineJobsCancelArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_pipeline_jobs_cancel_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_pipeline_jobs_cancel_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations pipeline jobs create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudAiplatformV1PipelineJob result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_pipeline_jobs_create(
        &self,
        args: &AiplatformProjectsLocationsPipelineJobsCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudAiplatformV1PipelineJob, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_pipeline_jobs_create_builder(
            &self.http_client,
            &args.parent,
            &args.pipelineJobId,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_pipeline_jobs_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations pipeline jobs delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_pipeline_jobs_delete(
        &self,
        args: &AiplatformProjectsLocationsPipelineJobsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_pipeline_jobs_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_pipeline_jobs_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations pipeline jobs operations cancel.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_pipeline_jobs_operations_cancel(
        &self,
        args: &AiplatformProjectsLocationsPipelineJobsOperationsCancelArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_pipeline_jobs_operations_cancel_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_pipeline_jobs_operations_cancel_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations pipeline jobs operations delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_pipeline_jobs_operations_delete(
        &self,
        args: &AiplatformProjectsLocationsPipelineJobsOperationsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_pipeline_jobs_operations_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_pipeline_jobs_operations_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations pipeline jobs operations wait.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_pipeline_jobs_operations_wait(
        &self,
        args: &AiplatformProjectsLocationsPipelineJobsOperationsWaitArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_pipeline_jobs_operations_wait_builder(
            &self.http_client,
            &args.name,
            &args.timeout,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_pipeline_jobs_operations_wait_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations publishers models compute tokens.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudAiplatformV1ComputeTokensResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_publishers_models_compute_tokens(
        &self,
        args: &AiplatformProjectsLocationsPublishersModelsComputeTokensArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudAiplatformV1ComputeTokensResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_publishers_models_compute_tokens_builder(
            &self.http_client,
            &args.endpoint,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_publishers_models_compute_tokens_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations publishers models count tokens.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudAiplatformV1CountTokensResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_publishers_models_count_tokens(
        &self,
        args: &AiplatformProjectsLocationsPublishersModelsCountTokensArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudAiplatformV1CountTokensResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_publishers_models_count_tokens_builder(
            &self.http_client,
            &args.endpoint,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_publishers_models_count_tokens_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations publishers models embed content.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudAiplatformV1EmbedContentResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_publishers_models_embed_content(
        &self,
        args: &AiplatformProjectsLocationsPublishersModelsEmbedContentArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudAiplatformV1EmbedContentResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_publishers_models_embed_content_builder(
            &self.http_client,
            &args.model,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_publishers_models_embed_content_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations publishers models fetch predict operation.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_publishers_models_fetch_predict_operation(
        &self,
        args: &AiplatformProjectsLocationsPublishersModelsFetchPredictOperationArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_publishers_models_fetch_predict_operation_builder(
            &self.http_client,
            &args.endpoint,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_publishers_models_fetch_predict_operation_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations publishers models generate content.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudAiplatformV1GenerateContentResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_publishers_models_generate_content(
        &self,
        args: &AiplatformProjectsLocationsPublishersModelsGenerateContentArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudAiplatformV1GenerateContentResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_publishers_models_generate_content_builder(
            &self.http_client,
            &args.model,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_publishers_models_generate_content_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations publishers models predict.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudAiplatformV1PredictResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_publishers_models_predict(
        &self,
        args: &AiplatformProjectsLocationsPublishersModelsPredictArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudAiplatformV1PredictResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_publishers_models_predict_builder(
            &self.http_client,
            &args.endpoint,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_publishers_models_predict_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations publishers models predict long running.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_publishers_models_predict_long_running(
        &self,
        args: &AiplatformProjectsLocationsPublishersModelsPredictLongRunningArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_publishers_models_predict_long_running_builder(
            &self.http_client,
            &args.endpoint,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_publishers_models_predict_long_running_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations publishers models raw predict.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleApiHttpBody result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_publishers_models_raw_predict(
        &self,
        args: &AiplatformProjectsLocationsPublishersModelsRawPredictArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleApiHttpBody, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_publishers_models_raw_predict_builder(
            &self.http_client,
            &args.endpoint,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_publishers_models_raw_predict_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations publishers models server streaming predict.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudAiplatformV1StreamingPredictResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_publishers_models_server_streaming_predict(
        &self,
        args: &AiplatformProjectsLocationsPublishersModelsServerStreamingPredictArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudAiplatformV1StreamingPredictResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_publishers_models_server_streaming_predict_builder(
            &self.http_client,
            &args.endpoint,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_publishers_models_server_streaming_predict_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations publishers models stream generate content.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudAiplatformV1GenerateContentResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_publishers_models_stream_generate_content(
        &self,
        args: &AiplatformProjectsLocationsPublishersModelsStreamGenerateContentArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudAiplatformV1GenerateContentResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_publishers_models_stream_generate_content_builder(
            &self.http_client,
            &args.model,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_publishers_models_stream_generate_content_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations publishers models stream raw predict.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleApiHttpBody result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_publishers_models_stream_raw_predict(
        &self,
        args: &AiplatformProjectsLocationsPublishersModelsStreamRawPredictArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleApiHttpBody, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_publishers_models_stream_raw_predict_builder(
            &self.http_client,
            &args.endpoint,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_publishers_models_stream_raw_predict_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations publishers models invoke invoke.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleApiHttpBody result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_publishers_models_invoke_invoke(
        &self,
        args: &AiplatformProjectsLocationsPublishersModelsInvokeInvokeArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleApiHttpBody, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_publishers_models_invoke_invoke_builder(
            &self.http_client,
            &args.endpoint,
            &args.invokeId,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_publishers_models_invoke_invoke_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations rag corpora create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_rag_corpora_create(
        &self,
        args: &AiplatformProjectsLocationsRagCorporaCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_rag_corpora_create_builder(
            &self.http_client,
            &args.parent,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_rag_corpora_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations rag corpora delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_rag_corpora_delete(
        &self,
        args: &AiplatformProjectsLocationsRagCorporaDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_rag_corpora_delete_builder(
            &self.http_client,
            &args.name,
            &args.force,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_rag_corpora_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations rag corpora patch.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_rag_corpora_patch(
        &self,
        args: &AiplatformProjectsLocationsRagCorporaPatchArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_rag_corpora_patch_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_rag_corpora_patch_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations rag corpora operations cancel.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_rag_corpora_operations_cancel(
        &self,
        args: &AiplatformProjectsLocationsRagCorporaOperationsCancelArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_rag_corpora_operations_cancel_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_rag_corpora_operations_cancel_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations rag corpora operations delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_rag_corpora_operations_delete(
        &self,
        args: &AiplatformProjectsLocationsRagCorporaOperationsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_rag_corpora_operations_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_rag_corpora_operations_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations rag corpora operations wait.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_rag_corpora_operations_wait(
        &self,
        args: &AiplatformProjectsLocationsRagCorporaOperationsWaitArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_rag_corpora_operations_wait_builder(
            &self.http_client,
            &args.name,
            &args.timeout,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_rag_corpora_operations_wait_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations rag corpora rag files delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_rag_corpora_rag_files_delete(
        &self,
        args: &AiplatformProjectsLocationsRagCorporaRagFilesDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_rag_corpora_rag_files_delete_builder(
            &self.http_client,
            &args.name,
            &args.forceDelete,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_rag_corpora_rag_files_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations rag corpora rag files import.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_rag_corpora_rag_files_import(
        &self,
        args: &AiplatformProjectsLocationsRagCorporaRagFilesImportArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_rag_corpora_rag_files_import_builder(
            &self.http_client,
            &args.parent,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_rag_corpora_rag_files_import_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations rag corpora rag files operations cancel.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_rag_corpora_rag_files_operations_cancel(
        &self,
        args: &AiplatformProjectsLocationsRagCorporaRagFilesOperationsCancelArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_rag_corpora_rag_files_operations_cancel_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_rag_corpora_rag_files_operations_cancel_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations rag corpora rag files operations delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_rag_corpora_rag_files_operations_delete(
        &self,
        args: &AiplatformProjectsLocationsRagCorporaRagFilesOperationsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_rag_corpora_rag_files_operations_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_rag_corpora_rag_files_operations_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations rag corpora rag files operations wait.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_rag_corpora_rag_files_operations_wait(
        &self,
        args: &AiplatformProjectsLocationsRagCorporaRagFilesOperationsWaitArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_rag_corpora_rag_files_operations_wait_builder(
            &self.http_client,
            &args.name,
            &args.timeout,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_rag_corpora_rag_files_operations_wait_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations rag engine config operations cancel.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_rag_engine_config_operations_cancel(
        &self,
        args: &AiplatformProjectsLocationsRagEngineConfigOperationsCancelArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_rag_engine_config_operations_cancel_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_rag_engine_config_operations_cancel_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations rag engine config operations delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_rag_engine_config_operations_delete(
        &self,
        args: &AiplatformProjectsLocationsRagEngineConfigOperationsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_rag_engine_config_operations_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_rag_engine_config_operations_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations rag engine config operations wait.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_rag_engine_config_operations_wait(
        &self,
        args: &AiplatformProjectsLocationsRagEngineConfigOperationsWaitArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_rag_engine_config_operations_wait_builder(
            &self.http_client,
            &args.name,
            &args.timeout,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_rag_engine_config_operations_wait_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations reasoning engines create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_reasoning_engines_create(
        &self,
        args: &AiplatformProjectsLocationsReasoningEnginesCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_reasoning_engines_create_builder(
            &self.http_client,
            &args.parent,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_reasoning_engines_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations reasoning engines delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_reasoning_engines_delete(
        &self,
        args: &AiplatformProjectsLocationsReasoningEnginesDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_reasoning_engines_delete_builder(
            &self.http_client,
            &args.name,
            &args.force,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_reasoning_engines_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations reasoning engines execute code.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudAiplatformV1ExecuteCodeResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_reasoning_engines_execute_code(
        &self,
        args: &AiplatformProjectsLocationsReasoningEnginesExecuteCodeArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudAiplatformV1ExecuteCodeResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_reasoning_engines_execute_code_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_reasoning_engines_execute_code_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations reasoning engines get iam policy.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleIamV1Policy result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_reasoning_engines_get_iam_policy(
        &self,
        args: &AiplatformProjectsLocationsReasoningEnginesGetIamPolicyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleIamV1Policy, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_reasoning_engines_get_iam_policy_builder(
            &self.http_client,
            &args.resource,
            &args.options.requestedPolicyVersion,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_reasoning_engines_get_iam_policy_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations reasoning engines patch.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_reasoning_engines_patch(
        &self,
        args: &AiplatformProjectsLocationsReasoningEnginesPatchArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_reasoning_engines_patch_builder(
            &self.http_client,
            &args.name,
            &args.updateMask,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_reasoning_engines_patch_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations reasoning engines query.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudAiplatformV1QueryReasoningEngineResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_reasoning_engines_query(
        &self,
        args: &AiplatformProjectsLocationsReasoningEnginesQueryArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudAiplatformV1QueryReasoningEngineResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_reasoning_engines_query_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_reasoning_engines_query_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations reasoning engines set iam policy.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleIamV1Policy result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_reasoning_engines_set_iam_policy(
        &self,
        args: &AiplatformProjectsLocationsReasoningEnginesSetIamPolicyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleIamV1Policy, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_reasoning_engines_set_iam_policy_builder(
            &self.http_client,
            &args.resource,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_reasoning_engines_set_iam_policy_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations reasoning engines stream query.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleApiHttpBody result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_reasoning_engines_stream_query(
        &self,
        args: &AiplatformProjectsLocationsReasoningEnginesStreamQueryArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleApiHttpBody, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_reasoning_engines_stream_query_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_reasoning_engines_stream_query_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations reasoning engines test iam permissions.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleIamV1TestIamPermissionsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_reasoning_engines_test_iam_permissions(
        &self,
        args: &AiplatformProjectsLocationsReasoningEnginesTestIamPermissionsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleIamV1TestIamPermissionsResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_reasoning_engines_test_iam_permissions_builder(
            &self.http_client,
            &args.resource,
            &args.permissions,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_reasoning_engines_test_iam_permissions_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations reasoning engines memories create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_reasoning_engines_memories_create(
        &self,
        args: &AiplatformProjectsLocationsReasoningEnginesMemoriesCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_reasoning_engines_memories_create_builder(
            &self.http_client,
            &args.parent,
            &args.memoryId,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_reasoning_engines_memories_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations reasoning engines memories delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_reasoning_engines_memories_delete(
        &self,
        args: &AiplatformProjectsLocationsReasoningEnginesMemoriesDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_reasoning_engines_memories_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_reasoning_engines_memories_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations reasoning engines memories generate.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_reasoning_engines_memories_generate(
        &self,
        args: &AiplatformProjectsLocationsReasoningEnginesMemoriesGenerateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_reasoning_engines_memories_generate_builder(
            &self.http_client,
            &args.parent,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_reasoning_engines_memories_generate_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations reasoning engines memories patch.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_reasoning_engines_memories_patch(
        &self,
        args: &AiplatformProjectsLocationsReasoningEnginesMemoriesPatchArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_reasoning_engines_memories_patch_builder(
            &self.http_client,
            &args.name,
            &args.updateMask,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_reasoning_engines_memories_patch_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations reasoning engines memories purge.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_reasoning_engines_memories_purge(
        &self,
        args: &AiplatformProjectsLocationsReasoningEnginesMemoriesPurgeArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_reasoning_engines_memories_purge_builder(
            &self.http_client,
            &args.parent,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_reasoning_engines_memories_purge_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations reasoning engines memories retrieve.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudAiplatformV1RetrieveMemoriesResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_reasoning_engines_memories_retrieve(
        &self,
        args: &AiplatformProjectsLocationsReasoningEnginesMemoriesRetrieveArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudAiplatformV1RetrieveMemoriesResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_reasoning_engines_memories_retrieve_builder(
            &self.http_client,
            &args.parent,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_reasoning_engines_memories_retrieve_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations reasoning engines memories rollback.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_reasoning_engines_memories_rollback(
        &self,
        args: &AiplatformProjectsLocationsReasoningEnginesMemoriesRollbackArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_reasoning_engines_memories_rollback_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_reasoning_engines_memories_rollback_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations reasoning engines memories operations cancel.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_reasoning_engines_memories_operations_cancel(
        &self,
        args: &AiplatformProjectsLocationsReasoningEnginesMemoriesOperationsCancelArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_reasoning_engines_memories_operations_cancel_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_reasoning_engines_memories_operations_cancel_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations reasoning engines memories operations delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_reasoning_engines_memories_operations_delete(
        &self,
        args: &AiplatformProjectsLocationsReasoningEnginesMemoriesOperationsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_reasoning_engines_memories_operations_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_reasoning_engines_memories_operations_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations reasoning engines memories operations wait.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_reasoning_engines_memories_operations_wait(
        &self,
        args: &AiplatformProjectsLocationsReasoningEnginesMemoriesOperationsWaitArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_reasoning_engines_memories_operations_wait_builder(
            &self.http_client,
            &args.name,
            &args.timeout,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_reasoning_engines_memories_operations_wait_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations reasoning engines operations cancel.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_reasoning_engines_operations_cancel(
        &self,
        args: &AiplatformProjectsLocationsReasoningEnginesOperationsCancelArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_reasoning_engines_operations_cancel_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_reasoning_engines_operations_cancel_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations reasoning engines operations delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_reasoning_engines_operations_delete(
        &self,
        args: &AiplatformProjectsLocationsReasoningEnginesOperationsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_reasoning_engines_operations_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_reasoning_engines_operations_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations reasoning engines operations wait.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_reasoning_engines_operations_wait(
        &self,
        args: &AiplatformProjectsLocationsReasoningEnginesOperationsWaitArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_reasoning_engines_operations_wait_builder(
            &self.http_client,
            &args.name,
            &args.timeout,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_reasoning_engines_operations_wait_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations reasoning engines sandbox environments create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_reasoning_engines_sandbox_environments_create(
        &self,
        args: &AiplatformProjectsLocationsReasoningEnginesSandboxEnvironmentsCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_reasoning_engines_sandbox_environments_create_builder(
            &self.http_client,
            &args.parent,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_reasoning_engines_sandbox_environments_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations reasoning engines sandbox environments delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_reasoning_engines_sandbox_environments_delete(
        &self,
        args: &AiplatformProjectsLocationsReasoningEnginesSandboxEnvironmentsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_reasoning_engines_sandbox_environments_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_reasoning_engines_sandbox_environments_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations reasoning engines sandbox environments execute.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudAiplatformV1ExecuteSandboxEnvironmentResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_reasoning_engines_sandbox_environments_execute(
        &self,
        args: &AiplatformProjectsLocationsReasoningEnginesSandboxEnvironmentsExecuteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudAiplatformV1ExecuteSandboxEnvironmentResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_reasoning_engines_sandbox_environments_execute_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_reasoning_engines_sandbox_environments_execute_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations reasoning engines sandbox environments operations cancel.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_reasoning_engines_sandbox_environments_operations_cancel(
        &self,
        args: &AiplatformProjectsLocationsReasoningEnginesSandboxEnvironmentsOperationsCancelArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_reasoning_engines_sandbox_environments_operations_cancel_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_reasoning_engines_sandbox_environments_operations_cancel_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations reasoning engines sandbox environments operations delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_reasoning_engines_sandbox_environments_operations_delete(
        &self,
        args: &AiplatformProjectsLocationsReasoningEnginesSandboxEnvironmentsOperationsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_reasoning_engines_sandbox_environments_operations_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_reasoning_engines_sandbox_environments_operations_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations reasoning engines sandbox environments operations wait.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_reasoning_engines_sandbox_environments_operations_wait(
        &self,
        args: &AiplatformProjectsLocationsReasoningEnginesSandboxEnvironmentsOperationsWaitArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_reasoning_engines_sandbox_environments_operations_wait_builder(
            &self.http_client,
            &args.name,
            &args.timeout,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_reasoning_engines_sandbox_environments_operations_wait_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations reasoning engines sessions append event.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudAiplatformV1AppendEventResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_reasoning_engines_sessions_append_event(
        &self,
        args: &AiplatformProjectsLocationsReasoningEnginesSessionsAppendEventArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudAiplatformV1AppendEventResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_reasoning_engines_sessions_append_event_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_reasoning_engines_sessions_append_event_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations reasoning engines sessions create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_reasoning_engines_sessions_create(
        &self,
        args: &AiplatformProjectsLocationsReasoningEnginesSessionsCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_reasoning_engines_sessions_create_builder(
            &self.http_client,
            &args.parent,
            &args.sessionId,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_reasoning_engines_sessions_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations reasoning engines sessions delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_reasoning_engines_sessions_delete(
        &self,
        args: &AiplatformProjectsLocationsReasoningEnginesSessionsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_reasoning_engines_sessions_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_reasoning_engines_sessions_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations reasoning engines sessions patch.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudAiplatformV1Session result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_reasoning_engines_sessions_patch(
        &self,
        args: &AiplatformProjectsLocationsReasoningEnginesSessionsPatchArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudAiplatformV1Session, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_reasoning_engines_sessions_patch_builder(
            &self.http_client,
            &args.name,
            &args.updateMask,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_reasoning_engines_sessions_patch_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations reasoning engines sessions operations cancel.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_reasoning_engines_sessions_operations_cancel(
        &self,
        args: &AiplatformProjectsLocationsReasoningEnginesSessionsOperationsCancelArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_reasoning_engines_sessions_operations_cancel_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_reasoning_engines_sessions_operations_cancel_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations reasoning engines sessions operations delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_reasoning_engines_sessions_operations_delete(
        &self,
        args: &AiplatformProjectsLocationsReasoningEnginesSessionsOperationsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_reasoning_engines_sessions_operations_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_reasoning_engines_sessions_operations_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations reasoning engines sessions operations wait.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_reasoning_engines_sessions_operations_wait(
        &self,
        args: &AiplatformProjectsLocationsReasoningEnginesSessionsOperationsWaitArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_reasoning_engines_sessions_operations_wait_builder(
            &self.http_client,
            &args.name,
            &args.timeout,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_reasoning_engines_sessions_operations_wait_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations schedules create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudAiplatformV1Schedule result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_schedules_create(
        &self,
        args: &AiplatformProjectsLocationsSchedulesCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudAiplatformV1Schedule, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_schedules_create_builder(
            &self.http_client,
            &args.parent,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_schedules_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations schedules delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_schedules_delete(
        &self,
        args: &AiplatformProjectsLocationsSchedulesDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_schedules_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_schedules_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations schedules patch.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudAiplatformV1Schedule result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_schedules_patch(
        &self,
        args: &AiplatformProjectsLocationsSchedulesPatchArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudAiplatformV1Schedule, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_schedules_patch_builder(
            &self.http_client,
            &args.name,
            &args.updateMask,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_schedules_patch_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations schedules pause.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_schedules_pause(
        &self,
        args: &AiplatformProjectsLocationsSchedulesPauseArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_schedules_pause_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_schedules_pause_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations schedules resume.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_schedules_resume(
        &self,
        args: &AiplatformProjectsLocationsSchedulesResumeArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_schedules_resume_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_schedules_resume_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations schedules operations cancel.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_schedules_operations_cancel(
        &self,
        args: &AiplatformProjectsLocationsSchedulesOperationsCancelArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_schedules_operations_cancel_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_schedules_operations_cancel_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations schedules operations delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_schedules_operations_delete(
        &self,
        args: &AiplatformProjectsLocationsSchedulesOperationsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_schedules_operations_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_schedules_operations_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations schedules operations wait.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_schedules_operations_wait(
        &self,
        args: &AiplatformProjectsLocationsSchedulesOperationsWaitArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_schedules_operations_wait_builder(
            &self.http_client,
            &args.name,
            &args.timeout,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_schedules_operations_wait_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations specialist pools create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_specialist_pools_create(
        &self,
        args: &AiplatformProjectsLocationsSpecialistPoolsCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_specialist_pools_create_builder(
            &self.http_client,
            &args.parent,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_specialist_pools_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations specialist pools delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_specialist_pools_delete(
        &self,
        args: &AiplatformProjectsLocationsSpecialistPoolsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_specialist_pools_delete_builder(
            &self.http_client,
            &args.name,
            &args.force,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_specialist_pools_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations specialist pools patch.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_specialist_pools_patch(
        &self,
        args: &AiplatformProjectsLocationsSpecialistPoolsPatchArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_specialist_pools_patch_builder(
            &self.http_client,
            &args.name,
            &args.updateMask,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_specialist_pools_patch_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations specialist pools operations cancel.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_specialist_pools_operations_cancel(
        &self,
        args: &AiplatformProjectsLocationsSpecialistPoolsOperationsCancelArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_specialist_pools_operations_cancel_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_specialist_pools_operations_cancel_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations specialist pools operations delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_specialist_pools_operations_delete(
        &self,
        args: &AiplatformProjectsLocationsSpecialistPoolsOperationsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_specialist_pools_operations_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_specialist_pools_operations_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations specialist pools operations wait.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_specialist_pools_operations_wait(
        &self,
        args: &AiplatformProjectsLocationsSpecialistPoolsOperationsWaitArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_specialist_pools_operations_wait_builder(
            &self.http_client,
            &args.name,
            &args.timeout,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_specialist_pools_operations_wait_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations studies create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudAiplatformV1Study result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_studies_create(
        &self,
        args: &AiplatformProjectsLocationsStudiesCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudAiplatformV1Study, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_studies_create_builder(
            &self.http_client,
            &args.parent,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_studies_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations studies delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_studies_delete(
        &self,
        args: &AiplatformProjectsLocationsStudiesDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_studies_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_studies_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations studies lookup.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudAiplatformV1Study result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_studies_lookup(
        &self,
        args: &AiplatformProjectsLocationsStudiesLookupArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudAiplatformV1Study, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_studies_lookup_builder(
            &self.http_client,
            &args.parent,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_studies_lookup_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations studies operations cancel.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_studies_operations_cancel(
        &self,
        args: &AiplatformProjectsLocationsStudiesOperationsCancelArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_studies_operations_cancel_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_studies_operations_cancel_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations studies operations delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_studies_operations_delete(
        &self,
        args: &AiplatformProjectsLocationsStudiesOperationsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_studies_operations_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_studies_operations_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations studies operations wait.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_studies_operations_wait(
        &self,
        args: &AiplatformProjectsLocationsStudiesOperationsWaitArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_studies_operations_wait_builder(
            &self.http_client,
            &args.name,
            &args.timeout,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_studies_operations_wait_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations studies trials add trial measurement.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudAiplatformV1Trial result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_studies_trials_add_trial_measurement(
        &self,
        args: &AiplatformProjectsLocationsStudiesTrialsAddTrialMeasurementArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudAiplatformV1Trial, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_studies_trials_add_trial_measurement_builder(
            &self.http_client,
            &args.trialName,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_studies_trials_add_trial_measurement_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations studies trials check trial early stopping state.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_studies_trials_check_trial_early_stopping_state(
        &self,
        args: &AiplatformProjectsLocationsStudiesTrialsCheckTrialEarlyStoppingStateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_studies_trials_check_trial_early_stopping_state_builder(
            &self.http_client,
            &args.trialName,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_studies_trials_check_trial_early_stopping_state_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations studies trials complete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudAiplatformV1Trial result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_studies_trials_complete(
        &self,
        args: &AiplatformProjectsLocationsStudiesTrialsCompleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudAiplatformV1Trial, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_studies_trials_complete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_studies_trials_complete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations studies trials create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudAiplatformV1Trial result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_studies_trials_create(
        &self,
        args: &AiplatformProjectsLocationsStudiesTrialsCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudAiplatformV1Trial, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_studies_trials_create_builder(
            &self.http_client,
            &args.parent,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_studies_trials_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations studies trials delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_studies_trials_delete(
        &self,
        args: &AiplatformProjectsLocationsStudiesTrialsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_studies_trials_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_studies_trials_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations studies trials list optimal trials.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudAiplatformV1ListOptimalTrialsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_studies_trials_list_optimal_trials(
        &self,
        args: &AiplatformProjectsLocationsStudiesTrialsListOptimalTrialsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudAiplatformV1ListOptimalTrialsResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_studies_trials_list_optimal_trials_builder(
            &self.http_client,
            &args.parent,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_studies_trials_list_optimal_trials_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations studies trials stop.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudAiplatformV1Trial result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_studies_trials_stop(
        &self,
        args: &AiplatformProjectsLocationsStudiesTrialsStopArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudAiplatformV1Trial, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_studies_trials_stop_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_studies_trials_stop_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations studies trials suggest.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_studies_trials_suggest(
        &self,
        args: &AiplatformProjectsLocationsStudiesTrialsSuggestArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_studies_trials_suggest_builder(
            &self.http_client,
            &args.parent,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_studies_trials_suggest_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations studies trials operations cancel.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_studies_trials_operations_cancel(
        &self,
        args: &AiplatformProjectsLocationsStudiesTrialsOperationsCancelArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_studies_trials_operations_cancel_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_studies_trials_operations_cancel_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations studies trials operations delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_studies_trials_operations_delete(
        &self,
        args: &AiplatformProjectsLocationsStudiesTrialsOperationsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_studies_trials_operations_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_studies_trials_operations_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations studies trials operations wait.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_studies_trials_operations_wait(
        &self,
        args: &AiplatformProjectsLocationsStudiesTrialsOperationsWaitArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_studies_trials_operations_wait_builder(
            &self.http_client,
            &args.name,
            &args.timeout,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_studies_trials_operations_wait_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations tensorboards create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_tensorboards_create(
        &self,
        args: &AiplatformProjectsLocationsTensorboardsCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_tensorboards_create_builder(
            &self.http_client,
            &args.parent,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_tensorboards_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations tensorboards delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_tensorboards_delete(
        &self,
        args: &AiplatformProjectsLocationsTensorboardsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_tensorboards_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_tensorboards_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations tensorboards patch.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_tensorboards_patch(
        &self,
        args: &AiplatformProjectsLocationsTensorboardsPatchArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_tensorboards_patch_builder(
            &self.http_client,
            &args.name,
            &args.updateMask,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_tensorboards_patch_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations tensorboards experiments batch create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudAiplatformV1BatchCreateTensorboardTimeSeriesResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_tensorboards_experiments_batch_create(
        &self,
        args: &AiplatformProjectsLocationsTensorboardsExperimentsBatchCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudAiplatformV1BatchCreateTensorboardTimeSeriesResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_tensorboards_experiments_batch_create_builder(
            &self.http_client,
            &args.parent,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_tensorboards_experiments_batch_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations tensorboards experiments create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudAiplatformV1TensorboardExperiment result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_tensorboards_experiments_create(
        &self,
        args: &AiplatformProjectsLocationsTensorboardsExperimentsCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudAiplatformV1TensorboardExperiment, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_tensorboards_experiments_create_builder(
            &self.http_client,
            &args.parent,
            &args.tensorboardExperimentId,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_tensorboards_experiments_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations tensorboards experiments delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_tensorboards_experiments_delete(
        &self,
        args: &AiplatformProjectsLocationsTensorboardsExperimentsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_tensorboards_experiments_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_tensorboards_experiments_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations tensorboards experiments patch.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudAiplatformV1TensorboardExperiment result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_tensorboards_experiments_patch(
        &self,
        args: &AiplatformProjectsLocationsTensorboardsExperimentsPatchArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudAiplatformV1TensorboardExperiment, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_tensorboards_experiments_patch_builder(
            &self.http_client,
            &args.name,
            &args.updateMask,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_tensorboards_experiments_patch_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations tensorboards experiments write.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudAiplatformV1WriteTensorboardExperimentDataResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_tensorboards_experiments_write(
        &self,
        args: &AiplatformProjectsLocationsTensorboardsExperimentsWriteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudAiplatformV1WriteTensorboardExperimentDataResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_tensorboards_experiments_write_builder(
            &self.http_client,
            &args.tensorboardExperiment,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_tensorboards_experiments_write_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations tensorboards experiments operations cancel.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_tensorboards_experiments_operations_cancel(
        &self,
        args: &AiplatformProjectsLocationsTensorboardsExperimentsOperationsCancelArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_tensorboards_experiments_operations_cancel_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_tensorboards_experiments_operations_cancel_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations tensorboards experiments operations delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_tensorboards_experiments_operations_delete(
        &self,
        args: &AiplatformProjectsLocationsTensorboardsExperimentsOperationsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_tensorboards_experiments_operations_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_tensorboards_experiments_operations_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations tensorboards experiments operations wait.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_tensorboards_experiments_operations_wait(
        &self,
        args: &AiplatformProjectsLocationsTensorboardsExperimentsOperationsWaitArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_tensorboards_experiments_operations_wait_builder(
            &self.http_client,
            &args.name,
            &args.timeout,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_tensorboards_experiments_operations_wait_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations tensorboards experiments runs batch create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudAiplatformV1BatchCreateTensorboardRunsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_tensorboards_experiments_runs_batch_create(
        &self,
        args: &AiplatformProjectsLocationsTensorboardsExperimentsRunsBatchCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudAiplatformV1BatchCreateTensorboardRunsResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_tensorboards_experiments_runs_batch_create_builder(
            &self.http_client,
            &args.parent,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_tensorboards_experiments_runs_batch_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations tensorboards experiments runs create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudAiplatformV1TensorboardRun result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_tensorboards_experiments_runs_create(
        &self,
        args: &AiplatformProjectsLocationsTensorboardsExperimentsRunsCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudAiplatformV1TensorboardRun, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_tensorboards_experiments_runs_create_builder(
            &self.http_client,
            &args.parent,
            &args.tensorboardRunId,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_tensorboards_experiments_runs_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations tensorboards experiments runs delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_tensorboards_experiments_runs_delete(
        &self,
        args: &AiplatformProjectsLocationsTensorboardsExperimentsRunsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_tensorboards_experiments_runs_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_tensorboards_experiments_runs_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations tensorboards experiments runs patch.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudAiplatformV1TensorboardRun result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_tensorboards_experiments_runs_patch(
        &self,
        args: &AiplatformProjectsLocationsTensorboardsExperimentsRunsPatchArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudAiplatformV1TensorboardRun, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_tensorboards_experiments_runs_patch_builder(
            &self.http_client,
            &args.name,
            &args.updateMask,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_tensorboards_experiments_runs_patch_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations tensorboards experiments runs write.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudAiplatformV1WriteTensorboardRunDataResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_tensorboards_experiments_runs_write(
        &self,
        args: &AiplatformProjectsLocationsTensorboardsExperimentsRunsWriteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudAiplatformV1WriteTensorboardRunDataResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_tensorboards_experiments_runs_write_builder(
            &self.http_client,
            &args.tensorboardRun,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_tensorboards_experiments_runs_write_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations tensorboards experiments runs operations cancel.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_tensorboards_experiments_runs_operations_cancel(
        &self,
        args: &AiplatformProjectsLocationsTensorboardsExperimentsRunsOperationsCancelArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_tensorboards_experiments_runs_operations_cancel_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_tensorboards_experiments_runs_operations_cancel_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations tensorboards experiments runs operations delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_tensorboards_experiments_runs_operations_delete(
        &self,
        args: &AiplatformProjectsLocationsTensorboardsExperimentsRunsOperationsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_tensorboards_experiments_runs_operations_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_tensorboards_experiments_runs_operations_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations tensorboards experiments runs operations wait.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_tensorboards_experiments_runs_operations_wait(
        &self,
        args: &AiplatformProjectsLocationsTensorboardsExperimentsRunsOperationsWaitArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_tensorboards_experiments_runs_operations_wait_builder(
            &self.http_client,
            &args.name,
            &args.timeout,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_tensorboards_experiments_runs_operations_wait_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations tensorboards experiments runs time series create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudAiplatformV1TensorboardTimeSeries result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_tensorboards_experiments_runs_time_series_create(
        &self,
        args: &AiplatformProjectsLocationsTensorboardsExperimentsRunsTimeSeriesCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudAiplatformV1TensorboardTimeSeries, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_tensorboards_experiments_runs_time_series_create_builder(
            &self.http_client,
            &args.parent,
            &args.tensorboardTimeSeriesId,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_tensorboards_experiments_runs_time_series_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations tensorboards experiments runs time series delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_tensorboards_experiments_runs_time_series_delete(
        &self,
        args: &AiplatformProjectsLocationsTensorboardsExperimentsRunsTimeSeriesDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_tensorboards_experiments_runs_time_series_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_tensorboards_experiments_runs_time_series_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations tensorboards experiments runs time series export tensorboard time series.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudAiplatformV1ExportTensorboardTimeSeriesDataResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_tensorboards_experiments_runs_time_series_export_tensorboard_time_series(
        &self,
        args: &AiplatformProjectsLocationsTensorboardsExperimentsRunsTimeSeriesExportTensorboardTimeSeriesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudAiplatformV1ExportTensorboardTimeSeriesDataResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_tensorboards_experiments_runs_time_series_export_tensorboard_time_series_builder(
            &self.http_client,
            &args.tensorboardTimeSeries,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_tensorboards_experiments_runs_time_series_export_tensorboard_time_series_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations tensorboards experiments runs time series patch.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudAiplatformV1TensorboardTimeSeries result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_tensorboards_experiments_runs_time_series_patch(
        &self,
        args: &AiplatformProjectsLocationsTensorboardsExperimentsRunsTimeSeriesPatchArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudAiplatformV1TensorboardTimeSeries, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_tensorboards_experiments_runs_time_series_patch_builder(
            &self.http_client,
            &args.name,
            &args.updateMask,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_tensorboards_experiments_runs_time_series_patch_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations tensorboards experiments runs time series operations cancel.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_tensorboards_experiments_runs_time_series_operations_cancel(
        &self,
        args: &AiplatformProjectsLocationsTensorboardsExperimentsRunsTimeSeriesOperationsCancelArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_tensorboards_experiments_runs_time_series_operations_cancel_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_tensorboards_experiments_runs_time_series_operations_cancel_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations tensorboards experiments runs time series operations delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_tensorboards_experiments_runs_time_series_operations_delete(
        &self,
        args: &AiplatformProjectsLocationsTensorboardsExperimentsRunsTimeSeriesOperationsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_tensorboards_experiments_runs_time_series_operations_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_tensorboards_experiments_runs_time_series_operations_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations tensorboards experiments runs time series operations wait.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_tensorboards_experiments_runs_time_series_operations_wait(
        &self,
        args: &AiplatformProjectsLocationsTensorboardsExperimentsRunsTimeSeriesOperationsWaitArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_tensorboards_experiments_runs_time_series_operations_wait_builder(
            &self.http_client,
            &args.name,
            &args.timeout,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_tensorboards_experiments_runs_time_series_operations_wait_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations tensorboards operations cancel.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_tensorboards_operations_cancel(
        &self,
        args: &AiplatformProjectsLocationsTensorboardsOperationsCancelArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_tensorboards_operations_cancel_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_tensorboards_operations_cancel_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations tensorboards operations delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_tensorboards_operations_delete(
        &self,
        args: &AiplatformProjectsLocationsTensorboardsOperationsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_tensorboards_operations_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_tensorboards_operations_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations tensorboards operations wait.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_tensorboards_operations_wait(
        &self,
        args: &AiplatformProjectsLocationsTensorboardsOperationsWaitArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_tensorboards_operations_wait_builder(
            &self.http_client,
            &args.name,
            &args.timeout,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_tensorboards_operations_wait_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations training pipelines cancel.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_training_pipelines_cancel(
        &self,
        args: &AiplatformProjectsLocationsTrainingPipelinesCancelArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_training_pipelines_cancel_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_training_pipelines_cancel_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations training pipelines create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudAiplatformV1TrainingPipeline result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_training_pipelines_create(
        &self,
        args: &AiplatformProjectsLocationsTrainingPipelinesCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudAiplatformV1TrainingPipeline, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_training_pipelines_create_builder(
            &self.http_client,
            &args.parent,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_training_pipelines_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations training pipelines delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_training_pipelines_delete(
        &self,
        args: &AiplatformProjectsLocationsTrainingPipelinesDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_training_pipelines_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_training_pipelines_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations training pipelines operations cancel.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_training_pipelines_operations_cancel(
        &self,
        args: &AiplatformProjectsLocationsTrainingPipelinesOperationsCancelArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_training_pipelines_operations_cancel_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_training_pipelines_operations_cancel_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations training pipelines operations delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_training_pipelines_operations_delete(
        &self,
        args: &AiplatformProjectsLocationsTrainingPipelinesOperationsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_training_pipelines_operations_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_training_pipelines_operations_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations training pipelines operations wait.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_training_pipelines_operations_wait(
        &self,
        args: &AiplatformProjectsLocationsTrainingPipelinesOperationsWaitArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_training_pipelines_operations_wait_builder(
            &self.http_client,
            &args.name,
            &args.timeout,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_training_pipelines_operations_wait_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations tuning jobs cancel.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_tuning_jobs_cancel(
        &self,
        args: &AiplatformProjectsLocationsTuningJobsCancelArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_tuning_jobs_cancel_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_tuning_jobs_cancel_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations tuning jobs create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudAiplatformV1TuningJob result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_tuning_jobs_create(
        &self,
        args: &AiplatformProjectsLocationsTuningJobsCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudAiplatformV1TuningJob, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_tuning_jobs_create_builder(
            &self.http_client,
            &args.parent,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_tuning_jobs_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations tuning jobs rebase tuned model.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_tuning_jobs_rebase_tuned_model(
        &self,
        args: &AiplatformProjectsLocationsTuningJobsRebaseTunedModelArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_tuning_jobs_rebase_tuned_model_builder(
            &self.http_client,
            &args.parent,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_tuning_jobs_rebase_tuned_model_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations tuning jobs operations cancel.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_tuning_jobs_operations_cancel(
        &self,
        args: &AiplatformProjectsLocationsTuningJobsOperationsCancelArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_tuning_jobs_operations_cancel_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_tuning_jobs_operations_cancel_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform projects locations tuning jobs operations delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_projects_locations_tuning_jobs_operations_delete(
        &self,
        args: &AiplatformProjectsLocationsTuningJobsOperationsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_projects_locations_tuning_jobs_operations_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_projects_locations_tuning_jobs_operations_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform publishers models compute tokens.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudAiplatformV1ComputeTokensResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_publishers_models_compute_tokens(
        &self,
        args: &AiplatformPublishersModelsComputeTokensArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudAiplatformV1ComputeTokensResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_publishers_models_compute_tokens_builder(
            &self.http_client,
            &args.endpoint,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_publishers_models_compute_tokens_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform publishers models count tokens.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudAiplatformV1CountTokensResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_publishers_models_count_tokens(
        &self,
        args: &AiplatformPublishersModelsCountTokensArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudAiplatformV1CountTokensResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_publishers_models_count_tokens_builder(
            &self.http_client,
            &args.endpoint,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_publishers_models_count_tokens_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform publishers models fetch predict operation.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_publishers_models_fetch_predict_operation(
        &self,
        args: &AiplatformPublishersModelsFetchPredictOperationArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_publishers_models_fetch_predict_operation_builder(
            &self.http_client,
            &args.endpoint,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_publishers_models_fetch_predict_operation_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform publishers models generate content.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudAiplatformV1GenerateContentResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_publishers_models_generate_content(
        &self,
        args: &AiplatformPublishersModelsGenerateContentArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudAiplatformV1GenerateContentResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_publishers_models_generate_content_builder(
            &self.http_client,
            &args.model,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_publishers_models_generate_content_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform publishers models predict.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudAiplatformV1PredictResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_publishers_models_predict(
        &self,
        args: &AiplatformPublishersModelsPredictArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudAiplatformV1PredictResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_publishers_models_predict_builder(
            &self.http_client,
            &args.endpoint,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_publishers_models_predict_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform publishers models predict long running.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_publishers_models_predict_long_running(
        &self,
        args: &AiplatformPublishersModelsPredictLongRunningArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_publishers_models_predict_long_running_builder(
            &self.http_client,
            &args.endpoint,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_publishers_models_predict_long_running_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform publishers models stream generate content.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudAiplatformV1GenerateContentResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_publishers_models_stream_generate_content(
        &self,
        args: &AiplatformPublishersModelsStreamGenerateContentArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudAiplatformV1GenerateContentResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_publishers_models_stream_generate_content_builder(
            &self.http_client,
            &args.model,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_publishers_models_stream_generate_content_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform rag corpora operations cancel.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_rag_corpora_operations_cancel(
        &self,
        args: &AiplatformRagCorporaOperationsCancelArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_rag_corpora_operations_cancel_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_rag_corpora_operations_cancel_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform rag corpora operations delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_rag_corpora_operations_delete(
        &self,
        args: &AiplatformRagCorporaOperationsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_rag_corpora_operations_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_rag_corpora_operations_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform rag corpora operations wait.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_rag_corpora_operations_wait(
        &self,
        args: &AiplatformRagCorporaOperationsWaitArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_rag_corpora_operations_wait_builder(
            &self.http_client,
            &args.name,
            &args.timeout,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_rag_corpora_operations_wait_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform rag corpora rag files operations cancel.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_rag_corpora_rag_files_operations_cancel(
        &self,
        args: &AiplatformRagCorporaRagFilesOperationsCancelArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_rag_corpora_rag_files_operations_cancel_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_rag_corpora_rag_files_operations_cancel_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform rag corpora rag files operations delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_rag_corpora_rag_files_operations_delete(
        &self,
        args: &AiplatformRagCorporaRagFilesOperationsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_rag_corpora_rag_files_operations_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_rag_corpora_rag_files_operations_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform rag corpora rag files operations wait.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_rag_corpora_rag_files_operations_wait(
        &self,
        args: &AiplatformRagCorporaRagFilesOperationsWaitArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_rag_corpora_rag_files_operations_wait_builder(
            &self.http_client,
            &args.name,
            &args.timeout,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_rag_corpora_rag_files_operations_wait_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform rag engine config operations cancel.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_rag_engine_config_operations_cancel(
        &self,
        args: &AiplatformRagEngineConfigOperationsCancelArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_rag_engine_config_operations_cancel_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_rag_engine_config_operations_cancel_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform rag engine config operations delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_rag_engine_config_operations_delete(
        &self,
        args: &AiplatformRagEngineConfigOperationsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_rag_engine_config_operations_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_rag_engine_config_operations_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform rag engine config operations wait.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_rag_engine_config_operations_wait(
        &self,
        args: &AiplatformRagEngineConfigOperationsWaitArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_rag_engine_config_operations_wait_builder(
            &self.http_client,
            &args.name,
            &args.timeout,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_rag_engine_config_operations_wait_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform reasoning engines create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_reasoning_engines_create(
        &self,
        args: &AiplatformReasoningEnginesCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_reasoning_engines_create_builder(
            &self.http_client,
            &args.parent,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_reasoning_engines_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform reasoning engines delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_reasoning_engines_delete(
        &self,
        args: &AiplatformReasoningEnginesDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_reasoning_engines_delete_builder(
            &self.http_client,
            &args.name,
            &args.force,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_reasoning_engines_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform reasoning engines execute code.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudAiplatformV1ExecuteCodeResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_reasoning_engines_execute_code(
        &self,
        args: &AiplatformReasoningEnginesExecuteCodeArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudAiplatformV1ExecuteCodeResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_reasoning_engines_execute_code_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_reasoning_engines_execute_code_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform reasoning engines patch.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_reasoning_engines_patch(
        &self,
        args: &AiplatformReasoningEnginesPatchArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_reasoning_engines_patch_builder(
            &self.http_client,
            &args.name,
            &args.updateMask,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_reasoning_engines_patch_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform reasoning engines query.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudAiplatformV1QueryReasoningEngineResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_reasoning_engines_query(
        &self,
        args: &AiplatformReasoningEnginesQueryArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudAiplatformV1QueryReasoningEngineResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_reasoning_engines_query_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_reasoning_engines_query_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform reasoning engines stream query.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleApiHttpBody result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_reasoning_engines_stream_query(
        &self,
        args: &AiplatformReasoningEnginesStreamQueryArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleApiHttpBody, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_reasoning_engines_stream_query_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_reasoning_engines_stream_query_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform reasoning engines memories operations cancel.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_reasoning_engines_memories_operations_cancel(
        &self,
        args: &AiplatformReasoningEnginesMemoriesOperationsCancelArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_reasoning_engines_memories_operations_cancel_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_reasoning_engines_memories_operations_cancel_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform reasoning engines memories operations delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_reasoning_engines_memories_operations_delete(
        &self,
        args: &AiplatformReasoningEnginesMemoriesOperationsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_reasoning_engines_memories_operations_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_reasoning_engines_memories_operations_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform reasoning engines memories operations wait.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_reasoning_engines_memories_operations_wait(
        &self,
        args: &AiplatformReasoningEnginesMemoriesOperationsWaitArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_reasoning_engines_memories_operations_wait_builder(
            &self.http_client,
            &args.name,
            &args.timeout,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_reasoning_engines_memories_operations_wait_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform reasoning engines operations cancel.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_reasoning_engines_operations_cancel(
        &self,
        args: &AiplatformReasoningEnginesOperationsCancelArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_reasoning_engines_operations_cancel_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_reasoning_engines_operations_cancel_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform reasoning engines operations delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_reasoning_engines_operations_delete(
        &self,
        args: &AiplatformReasoningEnginesOperationsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_reasoning_engines_operations_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_reasoning_engines_operations_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform reasoning engines operations wait.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_reasoning_engines_operations_wait(
        &self,
        args: &AiplatformReasoningEnginesOperationsWaitArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_reasoning_engines_operations_wait_builder(
            &self.http_client,
            &args.name,
            &args.timeout,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_reasoning_engines_operations_wait_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform reasoning engines sandbox environments create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_reasoning_engines_sandbox_environments_create(
        &self,
        args: &AiplatformReasoningEnginesSandboxEnvironmentsCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_reasoning_engines_sandbox_environments_create_builder(
            &self.http_client,
            &args.parent,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_reasoning_engines_sandbox_environments_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform reasoning engines sandbox environments delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_reasoning_engines_sandbox_environments_delete(
        &self,
        args: &AiplatformReasoningEnginesSandboxEnvironmentsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_reasoning_engines_sandbox_environments_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_reasoning_engines_sandbox_environments_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform reasoning engines sandbox environments execute.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudAiplatformV1ExecuteSandboxEnvironmentResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_reasoning_engines_sandbox_environments_execute(
        &self,
        args: &AiplatformReasoningEnginesSandboxEnvironmentsExecuteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudAiplatformV1ExecuteSandboxEnvironmentResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_reasoning_engines_sandbox_environments_execute_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_reasoning_engines_sandbox_environments_execute_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform reasoning engines sandbox environments operations cancel.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_reasoning_engines_sandbox_environments_operations_cancel(
        &self,
        args: &AiplatformReasoningEnginesSandboxEnvironmentsOperationsCancelArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_reasoning_engines_sandbox_environments_operations_cancel_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_reasoning_engines_sandbox_environments_operations_cancel_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform reasoning engines sandbox environments operations delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_reasoning_engines_sandbox_environments_operations_delete(
        &self,
        args: &AiplatformReasoningEnginesSandboxEnvironmentsOperationsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_reasoning_engines_sandbox_environments_operations_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_reasoning_engines_sandbox_environments_operations_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform reasoning engines sandbox environments operations wait.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_reasoning_engines_sandbox_environments_operations_wait(
        &self,
        args: &AiplatformReasoningEnginesSandboxEnvironmentsOperationsWaitArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_reasoning_engines_sandbox_environments_operations_wait_builder(
            &self.http_client,
            &args.name,
            &args.timeout,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_reasoning_engines_sandbox_environments_operations_wait_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform reasoning engines sessions operations cancel.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_reasoning_engines_sessions_operations_cancel(
        &self,
        args: &AiplatformReasoningEnginesSessionsOperationsCancelArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_reasoning_engines_sessions_operations_cancel_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_reasoning_engines_sessions_operations_cancel_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform reasoning engines sessions operations delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_reasoning_engines_sessions_operations_delete(
        &self,
        args: &AiplatformReasoningEnginesSessionsOperationsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_reasoning_engines_sessions_operations_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_reasoning_engines_sessions_operations_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform reasoning engines sessions operations wait.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_reasoning_engines_sessions_operations_wait(
        &self,
        args: &AiplatformReasoningEnginesSessionsOperationsWaitArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_reasoning_engines_sessions_operations_wait_builder(
            &self.http_client,
            &args.name,
            &args.timeout,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_reasoning_engines_sessions_operations_wait_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform schedules operations cancel.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_schedules_operations_cancel(
        &self,
        args: &AiplatformSchedulesOperationsCancelArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_schedules_operations_cancel_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_schedules_operations_cancel_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform schedules operations delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_schedules_operations_delete(
        &self,
        args: &AiplatformSchedulesOperationsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_schedules_operations_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_schedules_operations_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform schedules operations wait.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_schedules_operations_wait(
        &self,
        args: &AiplatformSchedulesOperationsWaitArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_schedules_operations_wait_builder(
            &self.http_client,
            &args.name,
            &args.timeout,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_schedules_operations_wait_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform specialist pools operations cancel.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_specialist_pools_operations_cancel(
        &self,
        args: &AiplatformSpecialistPoolsOperationsCancelArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_specialist_pools_operations_cancel_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_specialist_pools_operations_cancel_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform specialist pools operations delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_specialist_pools_operations_delete(
        &self,
        args: &AiplatformSpecialistPoolsOperationsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_specialist_pools_operations_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_specialist_pools_operations_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform specialist pools operations wait.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_specialist_pools_operations_wait(
        &self,
        args: &AiplatformSpecialistPoolsOperationsWaitArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_specialist_pools_operations_wait_builder(
            &self.http_client,
            &args.name,
            &args.timeout,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_specialist_pools_operations_wait_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform studies operations cancel.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_studies_operations_cancel(
        &self,
        args: &AiplatformStudiesOperationsCancelArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_studies_operations_cancel_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_studies_operations_cancel_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform studies operations delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_studies_operations_delete(
        &self,
        args: &AiplatformStudiesOperationsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_studies_operations_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_studies_operations_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform studies operations wait.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_studies_operations_wait(
        &self,
        args: &AiplatformStudiesOperationsWaitArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_studies_operations_wait_builder(
            &self.http_client,
            &args.name,
            &args.timeout,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_studies_operations_wait_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform studies trials operations cancel.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_studies_trials_operations_cancel(
        &self,
        args: &AiplatformStudiesTrialsOperationsCancelArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_studies_trials_operations_cancel_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_studies_trials_operations_cancel_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform studies trials operations delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_studies_trials_operations_delete(
        &self,
        args: &AiplatformStudiesTrialsOperationsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_studies_trials_operations_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_studies_trials_operations_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform studies trials operations wait.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_studies_trials_operations_wait(
        &self,
        args: &AiplatformStudiesTrialsOperationsWaitArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_studies_trials_operations_wait_builder(
            &self.http_client,
            &args.name,
            &args.timeout,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_studies_trials_operations_wait_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform tensorboards experiments operations cancel.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_tensorboards_experiments_operations_cancel(
        &self,
        args: &AiplatformTensorboardsExperimentsOperationsCancelArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_tensorboards_experiments_operations_cancel_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_tensorboards_experiments_operations_cancel_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform tensorboards experiments operations delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_tensorboards_experiments_operations_delete(
        &self,
        args: &AiplatformTensorboardsExperimentsOperationsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_tensorboards_experiments_operations_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_tensorboards_experiments_operations_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform tensorboards experiments operations wait.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_tensorboards_experiments_operations_wait(
        &self,
        args: &AiplatformTensorboardsExperimentsOperationsWaitArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_tensorboards_experiments_operations_wait_builder(
            &self.http_client,
            &args.name,
            &args.timeout,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_tensorboards_experiments_operations_wait_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform tensorboards experiments runs operations cancel.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_tensorboards_experiments_runs_operations_cancel(
        &self,
        args: &AiplatformTensorboardsExperimentsRunsOperationsCancelArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_tensorboards_experiments_runs_operations_cancel_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_tensorboards_experiments_runs_operations_cancel_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform tensorboards experiments runs operations delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_tensorboards_experiments_runs_operations_delete(
        &self,
        args: &AiplatformTensorboardsExperimentsRunsOperationsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_tensorboards_experiments_runs_operations_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_tensorboards_experiments_runs_operations_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform tensorboards experiments runs operations wait.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_tensorboards_experiments_runs_operations_wait(
        &self,
        args: &AiplatformTensorboardsExperimentsRunsOperationsWaitArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_tensorboards_experiments_runs_operations_wait_builder(
            &self.http_client,
            &args.name,
            &args.timeout,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_tensorboards_experiments_runs_operations_wait_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform tensorboards experiments runs time series operations cancel.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_tensorboards_experiments_runs_time_series_operations_cancel(
        &self,
        args: &AiplatformTensorboardsExperimentsRunsTimeSeriesOperationsCancelArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_tensorboards_experiments_runs_time_series_operations_cancel_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_tensorboards_experiments_runs_time_series_operations_cancel_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform tensorboards experiments runs time series operations delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_tensorboards_experiments_runs_time_series_operations_delete(
        &self,
        args: &AiplatformTensorboardsExperimentsRunsTimeSeriesOperationsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_tensorboards_experiments_runs_time_series_operations_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_tensorboards_experiments_runs_time_series_operations_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform tensorboards experiments runs time series operations wait.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_tensorboards_experiments_runs_time_series_operations_wait(
        &self,
        args: &AiplatformTensorboardsExperimentsRunsTimeSeriesOperationsWaitArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_tensorboards_experiments_runs_time_series_operations_wait_builder(
            &self.http_client,
            &args.name,
            &args.timeout,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_tensorboards_experiments_runs_time_series_operations_wait_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform tensorboards operations cancel.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_tensorboards_operations_cancel(
        &self,
        args: &AiplatformTensorboardsOperationsCancelArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_tensorboards_operations_cancel_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_tensorboards_operations_cancel_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform tensorboards operations delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_tensorboards_operations_delete(
        &self,
        args: &AiplatformTensorboardsOperationsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_tensorboards_operations_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_tensorboards_operations_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform tensorboards operations wait.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_tensorboards_operations_wait(
        &self,
        args: &AiplatformTensorboardsOperationsWaitArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_tensorboards_operations_wait_builder(
            &self.http_client,
            &args.name,
            &args.timeout,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_tensorboards_operations_wait_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform training pipelines operations cancel.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_training_pipelines_operations_cancel(
        &self,
        args: &AiplatformTrainingPipelinesOperationsCancelArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_training_pipelines_operations_cancel_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_training_pipelines_operations_cancel_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform training pipelines operations delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_training_pipelines_operations_delete(
        &self,
        args: &AiplatformTrainingPipelinesOperationsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_training_pipelines_operations_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_training_pipelines_operations_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform training pipelines operations wait.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_training_pipelines_operations_wait(
        &self,
        args: &AiplatformTrainingPipelinesOperationsWaitArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_training_pipelines_operations_wait_builder(
            &self.http_client,
            &args.name,
            &args.timeout,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_training_pipelines_operations_wait_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform tuning jobs operations cancel.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_tuning_jobs_operations_cancel(
        &self,
        args: &AiplatformTuningJobsOperationsCancelArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_tuning_jobs_operations_cancel_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_tuning_jobs_operations_cancel_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform tuning jobs operations delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleProtobufEmpty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_tuning_jobs_operations_delete(
        &self,
        args: &AiplatformTuningJobsOperationsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleProtobufEmpty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_tuning_jobs_operations_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_tuning_jobs_operations_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform evaluate dataset.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleLongrunningOperation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_evaluate_dataset(
        &self,
        args: &AiplatformEvaluateDatasetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleLongrunningOperation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_evaluate_dataset_builder(
            &self.http_client,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_evaluate_dataset_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform evaluate instances.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudAiplatformV1EvaluateInstancesResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_evaluate_instances(
        &self,
        args: &AiplatformEvaluateInstancesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudAiplatformV1EvaluateInstancesResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_evaluate_instances_builder(
            &self.http_client,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_evaluate_instances_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Aiplatform generate instance rubrics.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GoogleCloudAiplatformV1GenerateInstanceRubricsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn aiplatform_generate_instance_rubrics(
        &self,
        args: &AiplatformGenerateInstanceRubricsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GoogleCloudAiplatformV1GenerateInstanceRubricsResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = aiplatform_generate_instance_rubrics_builder(
            &self.http_client,
        )
        .map_err(ProviderError::Api)?;

        let task = aiplatform_generate_instance_rubrics_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

}
