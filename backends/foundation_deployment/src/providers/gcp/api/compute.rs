//! ComputeProvider - State-aware compute API client.
//!
//! WHY: Users need state-aware API clients that automatically track
//!      resource changes in the state store.
//!
//! WHAT: Provider wrapping ProviderClient<S> with methods for
//!       compute API endpoints that auto-store results.
//!
//! HOW: Each method wraps the task with StoreStateIdentifierTask
//!      for automatic state persistence on success.

#![cfg(feature = "gcp")]

use crate::providers::gcp::clients::compute::{
    compute_accelerator_types_aggregated_list_builder, compute_accelerator_types_aggregated_list_task,
    compute_accelerator_types_get_builder, compute_accelerator_types_get_task,
    compute_accelerator_types_list_builder, compute_accelerator_types_list_task,
    compute_addresses_aggregated_list_builder, compute_addresses_aggregated_list_task,
    compute_addresses_delete_builder, compute_addresses_delete_task,
    compute_addresses_get_builder, compute_addresses_get_task,
    compute_addresses_insert_builder, compute_addresses_insert_task,
    compute_addresses_list_builder, compute_addresses_list_task,
    compute_addresses_move_builder, compute_addresses_move_task,
    compute_addresses_set_labels_builder, compute_addresses_set_labels_task,
    compute_addresses_test_iam_permissions_builder, compute_addresses_test_iam_permissions_task,
    compute_advice_calendar_mode_builder, compute_advice_calendar_mode_task,
    compute_autoscalers_aggregated_list_builder, compute_autoscalers_aggregated_list_task,
    compute_autoscalers_delete_builder, compute_autoscalers_delete_task,
    compute_autoscalers_get_builder, compute_autoscalers_get_task,
    compute_autoscalers_insert_builder, compute_autoscalers_insert_task,
    compute_autoscalers_list_builder, compute_autoscalers_list_task,
    compute_autoscalers_patch_builder, compute_autoscalers_patch_task,
    compute_autoscalers_test_iam_permissions_builder, compute_autoscalers_test_iam_permissions_task,
    compute_autoscalers_update_builder, compute_autoscalers_update_task,
    compute_backend_buckets_add_signed_url_key_builder, compute_backend_buckets_add_signed_url_key_task,
    compute_backend_buckets_aggregated_list_builder, compute_backend_buckets_aggregated_list_task,
    compute_backend_buckets_delete_builder, compute_backend_buckets_delete_task,
    compute_backend_buckets_delete_signed_url_key_builder, compute_backend_buckets_delete_signed_url_key_task,
    compute_backend_buckets_get_builder, compute_backend_buckets_get_task,
    compute_backend_buckets_get_iam_policy_builder, compute_backend_buckets_get_iam_policy_task,
    compute_backend_buckets_insert_builder, compute_backend_buckets_insert_task,
    compute_backend_buckets_list_builder, compute_backend_buckets_list_task,
    compute_backend_buckets_list_usable_builder, compute_backend_buckets_list_usable_task,
    compute_backend_buckets_patch_builder, compute_backend_buckets_patch_task,
    compute_backend_buckets_set_edge_security_policy_builder, compute_backend_buckets_set_edge_security_policy_task,
    compute_backend_buckets_set_iam_policy_builder, compute_backend_buckets_set_iam_policy_task,
    compute_backend_buckets_test_iam_permissions_builder, compute_backend_buckets_test_iam_permissions_task,
    compute_backend_buckets_update_builder, compute_backend_buckets_update_task,
    compute_backend_services_add_signed_url_key_builder, compute_backend_services_add_signed_url_key_task,
    compute_backend_services_aggregated_list_builder, compute_backend_services_aggregated_list_task,
    compute_backend_services_delete_builder, compute_backend_services_delete_task,
    compute_backend_services_delete_signed_url_key_builder, compute_backend_services_delete_signed_url_key_task,
    compute_backend_services_get_builder, compute_backend_services_get_task,
    compute_backend_services_get_effective_security_policies_builder, compute_backend_services_get_effective_security_policies_task,
    compute_backend_services_get_health_builder, compute_backend_services_get_health_task,
    compute_backend_services_get_iam_policy_builder, compute_backend_services_get_iam_policy_task,
    compute_backend_services_insert_builder, compute_backend_services_insert_task,
    compute_backend_services_list_builder, compute_backend_services_list_task,
    compute_backend_services_list_usable_builder, compute_backend_services_list_usable_task,
    compute_backend_services_patch_builder, compute_backend_services_patch_task,
    compute_backend_services_set_edge_security_policy_builder, compute_backend_services_set_edge_security_policy_task,
    compute_backend_services_set_iam_policy_builder, compute_backend_services_set_iam_policy_task,
    compute_backend_services_set_security_policy_builder, compute_backend_services_set_security_policy_task,
    compute_backend_services_test_iam_permissions_builder, compute_backend_services_test_iam_permissions_task,
    compute_backend_services_update_builder, compute_backend_services_update_task,
    compute_cross_site_networks_delete_builder, compute_cross_site_networks_delete_task,
    compute_cross_site_networks_get_builder, compute_cross_site_networks_get_task,
    compute_cross_site_networks_insert_builder, compute_cross_site_networks_insert_task,
    compute_cross_site_networks_list_builder, compute_cross_site_networks_list_task,
    compute_cross_site_networks_patch_builder, compute_cross_site_networks_patch_task,
    compute_disk_types_aggregated_list_builder, compute_disk_types_aggregated_list_task,
    compute_disk_types_get_builder, compute_disk_types_get_task,
    compute_disk_types_list_builder, compute_disk_types_list_task,
    compute_disks_add_resource_policies_builder, compute_disks_add_resource_policies_task,
    compute_disks_aggregated_list_builder, compute_disks_aggregated_list_task,
    compute_disks_bulk_insert_builder, compute_disks_bulk_insert_task,
    compute_disks_bulk_set_labels_builder, compute_disks_bulk_set_labels_task,
    compute_disks_create_snapshot_builder, compute_disks_create_snapshot_task,
    compute_disks_delete_builder, compute_disks_delete_task,
    compute_disks_get_builder, compute_disks_get_task,
    compute_disks_get_iam_policy_builder, compute_disks_get_iam_policy_task,
    compute_disks_insert_builder, compute_disks_insert_task,
    compute_disks_list_builder, compute_disks_list_task,
    compute_disks_remove_resource_policies_builder, compute_disks_remove_resource_policies_task,
    compute_disks_resize_builder, compute_disks_resize_task,
    compute_disks_set_iam_policy_builder, compute_disks_set_iam_policy_task,
    compute_disks_set_labels_builder, compute_disks_set_labels_task,
    compute_disks_start_async_replication_builder, compute_disks_start_async_replication_task,
    compute_disks_stop_async_replication_builder, compute_disks_stop_async_replication_task,
    compute_disks_stop_group_async_replication_builder, compute_disks_stop_group_async_replication_task,
    compute_disks_test_iam_permissions_builder, compute_disks_test_iam_permissions_task,
    compute_disks_update_builder, compute_disks_update_task,
    compute_disks_update_kms_key_builder, compute_disks_update_kms_key_task,
    compute_external_vpn_gateways_delete_builder, compute_external_vpn_gateways_delete_task,
    compute_external_vpn_gateways_get_builder, compute_external_vpn_gateways_get_task,
    compute_external_vpn_gateways_insert_builder, compute_external_vpn_gateways_insert_task,
    compute_external_vpn_gateways_list_builder, compute_external_vpn_gateways_list_task,
    compute_external_vpn_gateways_set_labels_builder, compute_external_vpn_gateways_set_labels_task,
    compute_external_vpn_gateways_test_iam_permissions_builder, compute_external_vpn_gateways_test_iam_permissions_task,
    compute_firewall_policies_add_association_builder, compute_firewall_policies_add_association_task,
    compute_firewall_policies_add_rule_builder, compute_firewall_policies_add_rule_task,
    compute_firewall_policies_clone_rules_builder, compute_firewall_policies_clone_rules_task,
    compute_firewall_policies_delete_builder, compute_firewall_policies_delete_task,
    compute_firewall_policies_get_builder, compute_firewall_policies_get_task,
    compute_firewall_policies_get_association_builder, compute_firewall_policies_get_association_task,
    compute_firewall_policies_get_iam_policy_builder, compute_firewall_policies_get_iam_policy_task,
    compute_firewall_policies_get_rule_builder, compute_firewall_policies_get_rule_task,
    compute_firewall_policies_insert_builder, compute_firewall_policies_insert_task,
    compute_firewall_policies_list_builder, compute_firewall_policies_list_task,
    compute_firewall_policies_list_associations_builder, compute_firewall_policies_list_associations_task,
    compute_firewall_policies_move_builder, compute_firewall_policies_move_task,
    compute_firewall_policies_patch_builder, compute_firewall_policies_patch_task,
    compute_firewall_policies_patch_rule_builder, compute_firewall_policies_patch_rule_task,
    compute_firewall_policies_remove_association_builder, compute_firewall_policies_remove_association_task,
    compute_firewall_policies_remove_rule_builder, compute_firewall_policies_remove_rule_task,
    compute_firewall_policies_set_iam_policy_builder, compute_firewall_policies_set_iam_policy_task,
    compute_firewall_policies_test_iam_permissions_builder, compute_firewall_policies_test_iam_permissions_task,
    compute_firewalls_delete_builder, compute_firewalls_delete_task,
    compute_firewalls_get_builder, compute_firewalls_get_task,
    compute_firewalls_insert_builder, compute_firewalls_insert_task,
    compute_firewalls_list_builder, compute_firewalls_list_task,
    compute_firewalls_patch_builder, compute_firewalls_patch_task,
    compute_firewalls_test_iam_permissions_builder, compute_firewalls_test_iam_permissions_task,
    compute_firewalls_update_builder, compute_firewalls_update_task,
    compute_forwarding_rules_aggregated_list_builder, compute_forwarding_rules_aggregated_list_task,
    compute_forwarding_rules_delete_builder, compute_forwarding_rules_delete_task,
    compute_forwarding_rules_get_builder, compute_forwarding_rules_get_task,
    compute_forwarding_rules_insert_builder, compute_forwarding_rules_insert_task,
    compute_forwarding_rules_list_builder, compute_forwarding_rules_list_task,
    compute_forwarding_rules_patch_builder, compute_forwarding_rules_patch_task,
    compute_forwarding_rules_set_labels_builder, compute_forwarding_rules_set_labels_task,
    compute_forwarding_rules_set_target_builder, compute_forwarding_rules_set_target_task,
    compute_future_reservations_aggregated_list_builder, compute_future_reservations_aggregated_list_task,
    compute_future_reservations_cancel_builder, compute_future_reservations_cancel_task,
    compute_future_reservations_delete_builder, compute_future_reservations_delete_task,
    compute_future_reservations_get_builder, compute_future_reservations_get_task,
    compute_future_reservations_insert_builder, compute_future_reservations_insert_task,
    compute_future_reservations_list_builder, compute_future_reservations_list_task,
    compute_future_reservations_update_builder, compute_future_reservations_update_task,
    compute_global_addresses_delete_builder, compute_global_addresses_delete_task,
    compute_global_addresses_get_builder, compute_global_addresses_get_task,
    compute_global_addresses_insert_builder, compute_global_addresses_insert_task,
    compute_global_addresses_list_builder, compute_global_addresses_list_task,
    compute_global_addresses_move_builder, compute_global_addresses_move_task,
    compute_global_addresses_set_labels_builder, compute_global_addresses_set_labels_task,
    compute_global_addresses_test_iam_permissions_builder, compute_global_addresses_test_iam_permissions_task,
    compute_global_forwarding_rules_delete_builder, compute_global_forwarding_rules_delete_task,
    compute_global_forwarding_rules_get_builder, compute_global_forwarding_rules_get_task,
    compute_global_forwarding_rules_insert_builder, compute_global_forwarding_rules_insert_task,
    compute_global_forwarding_rules_list_builder, compute_global_forwarding_rules_list_task,
    compute_global_forwarding_rules_patch_builder, compute_global_forwarding_rules_patch_task,
    compute_global_forwarding_rules_set_labels_builder, compute_global_forwarding_rules_set_labels_task,
    compute_global_forwarding_rules_set_target_builder, compute_global_forwarding_rules_set_target_task,
    compute_global_network_endpoint_groups_attach_network_endpoints_builder, compute_global_network_endpoint_groups_attach_network_endpoints_task,
    compute_global_network_endpoint_groups_delete_builder, compute_global_network_endpoint_groups_delete_task,
    compute_global_network_endpoint_groups_detach_network_endpoints_builder, compute_global_network_endpoint_groups_detach_network_endpoints_task,
    compute_global_network_endpoint_groups_get_builder, compute_global_network_endpoint_groups_get_task,
    compute_global_network_endpoint_groups_insert_builder, compute_global_network_endpoint_groups_insert_task,
    compute_global_network_endpoint_groups_list_builder, compute_global_network_endpoint_groups_list_task,
    compute_global_network_endpoint_groups_list_network_endpoints_builder, compute_global_network_endpoint_groups_list_network_endpoints_task,
    compute_global_operations_aggregated_list_builder, compute_global_operations_aggregated_list_task,
    compute_global_operations_delete_builder, compute_global_operations_delete_task,
    compute_global_operations_get_builder, compute_global_operations_get_task,
    compute_global_operations_list_builder, compute_global_operations_list_task,
    compute_global_operations_wait_builder, compute_global_operations_wait_task,
    compute_global_organization_operations_delete_builder, compute_global_organization_operations_delete_task,
    compute_global_organization_operations_get_builder, compute_global_organization_operations_get_task,
    compute_global_organization_operations_list_builder, compute_global_organization_operations_list_task,
    compute_global_public_delegated_prefixes_delete_builder, compute_global_public_delegated_prefixes_delete_task,
    compute_global_public_delegated_prefixes_get_builder, compute_global_public_delegated_prefixes_get_task,
    compute_global_public_delegated_prefixes_insert_builder, compute_global_public_delegated_prefixes_insert_task,
    compute_global_public_delegated_prefixes_list_builder, compute_global_public_delegated_prefixes_list_task,
    compute_global_public_delegated_prefixes_patch_builder, compute_global_public_delegated_prefixes_patch_task,
    compute_health_checks_aggregated_list_builder, compute_health_checks_aggregated_list_task,
    compute_health_checks_delete_builder, compute_health_checks_delete_task,
    compute_health_checks_get_builder, compute_health_checks_get_task,
    compute_health_checks_insert_builder, compute_health_checks_insert_task,
    compute_health_checks_list_builder, compute_health_checks_list_task,
    compute_health_checks_patch_builder, compute_health_checks_patch_task,
    compute_health_checks_test_iam_permissions_builder, compute_health_checks_test_iam_permissions_task,
    compute_health_checks_update_builder, compute_health_checks_update_task,
    compute_http_health_checks_delete_builder, compute_http_health_checks_delete_task,
    compute_http_health_checks_get_builder, compute_http_health_checks_get_task,
    compute_http_health_checks_insert_builder, compute_http_health_checks_insert_task,
    compute_http_health_checks_list_builder, compute_http_health_checks_list_task,
    compute_http_health_checks_patch_builder, compute_http_health_checks_patch_task,
    compute_http_health_checks_test_iam_permissions_builder, compute_http_health_checks_test_iam_permissions_task,
    compute_http_health_checks_update_builder, compute_http_health_checks_update_task,
    compute_https_health_checks_delete_builder, compute_https_health_checks_delete_task,
    compute_https_health_checks_get_builder, compute_https_health_checks_get_task,
    compute_https_health_checks_insert_builder, compute_https_health_checks_insert_task,
    compute_https_health_checks_list_builder, compute_https_health_checks_list_task,
    compute_https_health_checks_patch_builder, compute_https_health_checks_patch_task,
    compute_https_health_checks_test_iam_permissions_builder, compute_https_health_checks_test_iam_permissions_task,
    compute_https_health_checks_update_builder, compute_https_health_checks_update_task,
    compute_image_family_views_get_builder, compute_image_family_views_get_task,
    compute_images_delete_builder, compute_images_delete_task,
    compute_images_deprecate_builder, compute_images_deprecate_task,
    compute_images_get_builder, compute_images_get_task,
    compute_images_get_from_family_builder, compute_images_get_from_family_task,
    compute_images_get_iam_policy_builder, compute_images_get_iam_policy_task,
    compute_images_insert_builder, compute_images_insert_task,
    compute_images_list_builder, compute_images_list_task,
    compute_images_patch_builder, compute_images_patch_task,
    compute_images_set_iam_policy_builder, compute_images_set_iam_policy_task,
    compute_images_set_labels_builder, compute_images_set_labels_task,
    compute_images_test_iam_permissions_builder, compute_images_test_iam_permissions_task,
    compute_instance_group_manager_resize_requests_cancel_builder, compute_instance_group_manager_resize_requests_cancel_task,
    compute_instance_group_manager_resize_requests_delete_builder, compute_instance_group_manager_resize_requests_delete_task,
    compute_instance_group_manager_resize_requests_get_builder, compute_instance_group_manager_resize_requests_get_task,
    compute_instance_group_manager_resize_requests_insert_builder, compute_instance_group_manager_resize_requests_insert_task,
    compute_instance_group_manager_resize_requests_list_builder, compute_instance_group_manager_resize_requests_list_task,
    compute_instance_group_managers_abandon_instances_builder, compute_instance_group_managers_abandon_instances_task,
    compute_instance_group_managers_aggregated_list_builder, compute_instance_group_managers_aggregated_list_task,
    compute_instance_group_managers_apply_updates_to_instances_builder, compute_instance_group_managers_apply_updates_to_instances_task,
    compute_instance_group_managers_create_instances_builder, compute_instance_group_managers_create_instances_task,
    compute_instance_group_managers_delete_builder, compute_instance_group_managers_delete_task,
    compute_instance_group_managers_delete_instances_builder, compute_instance_group_managers_delete_instances_task,
    compute_instance_group_managers_delete_per_instance_configs_builder, compute_instance_group_managers_delete_per_instance_configs_task,
    compute_instance_group_managers_get_builder, compute_instance_group_managers_get_task,
    compute_instance_group_managers_insert_builder, compute_instance_group_managers_insert_task,
    compute_instance_group_managers_list_builder, compute_instance_group_managers_list_task,
    compute_instance_group_managers_list_errors_builder, compute_instance_group_managers_list_errors_task,
    compute_instance_group_managers_list_managed_instances_builder, compute_instance_group_managers_list_managed_instances_task,
    compute_instance_group_managers_list_per_instance_configs_builder, compute_instance_group_managers_list_per_instance_configs_task,
    compute_instance_group_managers_patch_builder, compute_instance_group_managers_patch_task,
    compute_instance_group_managers_patch_per_instance_configs_builder, compute_instance_group_managers_patch_per_instance_configs_task,
    compute_instance_group_managers_recreate_instances_builder, compute_instance_group_managers_recreate_instances_task,
    compute_instance_group_managers_resize_builder, compute_instance_group_managers_resize_task,
    compute_instance_group_managers_resume_instances_builder, compute_instance_group_managers_resume_instances_task,
    compute_instance_group_managers_set_instance_template_builder, compute_instance_group_managers_set_instance_template_task,
    compute_instance_group_managers_set_target_pools_builder, compute_instance_group_managers_set_target_pools_task,
    compute_instance_group_managers_start_instances_builder, compute_instance_group_managers_start_instances_task,
    compute_instance_group_managers_stop_instances_builder, compute_instance_group_managers_stop_instances_task,
    compute_instance_group_managers_suspend_instances_builder, compute_instance_group_managers_suspend_instances_task,
    compute_instance_group_managers_update_per_instance_configs_builder, compute_instance_group_managers_update_per_instance_configs_task,
    compute_instance_groups_add_instances_builder, compute_instance_groups_add_instances_task,
    compute_instance_groups_aggregated_list_builder, compute_instance_groups_aggregated_list_task,
    compute_instance_groups_delete_builder, compute_instance_groups_delete_task,
    compute_instance_groups_get_builder, compute_instance_groups_get_task,
    compute_instance_groups_insert_builder, compute_instance_groups_insert_task,
    compute_instance_groups_list_builder, compute_instance_groups_list_task,
    compute_instance_groups_list_instances_builder, compute_instance_groups_list_instances_task,
    compute_instance_groups_remove_instances_builder, compute_instance_groups_remove_instances_task,
    compute_instance_groups_set_named_ports_builder, compute_instance_groups_set_named_ports_task,
    compute_instance_groups_test_iam_permissions_builder, compute_instance_groups_test_iam_permissions_task,
    compute_instance_settings_get_builder, compute_instance_settings_get_task,
    compute_instance_settings_patch_builder, compute_instance_settings_patch_task,
    compute_instance_templates_aggregated_list_builder, compute_instance_templates_aggregated_list_task,
    compute_instance_templates_delete_builder, compute_instance_templates_delete_task,
    compute_instance_templates_get_builder, compute_instance_templates_get_task,
    compute_instance_templates_get_iam_policy_builder, compute_instance_templates_get_iam_policy_task,
    compute_instance_templates_insert_builder, compute_instance_templates_insert_task,
    compute_instance_templates_list_builder, compute_instance_templates_list_task,
    compute_instance_templates_set_iam_policy_builder, compute_instance_templates_set_iam_policy_task,
    compute_instance_templates_test_iam_permissions_builder, compute_instance_templates_test_iam_permissions_task,
    compute_instances_add_access_config_builder, compute_instances_add_access_config_task,
    compute_instances_add_network_interface_builder, compute_instances_add_network_interface_task,
    compute_instances_add_resource_policies_builder, compute_instances_add_resource_policies_task,
    compute_instances_aggregated_list_builder, compute_instances_aggregated_list_task,
    compute_instances_attach_disk_builder, compute_instances_attach_disk_task,
    compute_instances_bulk_insert_builder, compute_instances_bulk_insert_task,
    compute_instances_delete_builder, compute_instances_delete_task,
    compute_instances_delete_access_config_builder, compute_instances_delete_access_config_task,
    compute_instances_delete_network_interface_builder, compute_instances_delete_network_interface_task,
    compute_instances_detach_disk_builder, compute_instances_detach_disk_task,
    compute_instances_get_builder, compute_instances_get_task,
    compute_instances_get_effective_firewalls_builder, compute_instances_get_effective_firewalls_task,
    compute_instances_get_guest_attributes_builder, compute_instances_get_guest_attributes_task,
    compute_instances_get_iam_policy_builder, compute_instances_get_iam_policy_task,
    compute_instances_get_screenshot_builder, compute_instances_get_screenshot_task,
    compute_instances_get_serial_port_output_builder, compute_instances_get_serial_port_output_task,
    compute_instances_get_shielded_instance_identity_builder, compute_instances_get_shielded_instance_identity_task,
    compute_instances_insert_builder, compute_instances_insert_task,
    compute_instances_list_builder, compute_instances_list_task,
    compute_instances_list_referrers_builder, compute_instances_list_referrers_task,
    compute_instances_perform_maintenance_builder, compute_instances_perform_maintenance_task,
    compute_instances_remove_resource_policies_builder, compute_instances_remove_resource_policies_task,
    compute_instances_report_host_as_faulty_builder, compute_instances_report_host_as_faulty_task,
    compute_instances_reset_builder, compute_instances_reset_task,
    compute_instances_resume_builder, compute_instances_resume_task,
    compute_instances_send_diagnostic_interrupt_builder, compute_instances_send_diagnostic_interrupt_task,
    compute_instances_set_deletion_protection_builder, compute_instances_set_deletion_protection_task,
    compute_instances_set_disk_auto_delete_builder, compute_instances_set_disk_auto_delete_task,
    compute_instances_set_iam_policy_builder, compute_instances_set_iam_policy_task,
    compute_instances_set_labels_builder, compute_instances_set_labels_task,
    compute_instances_set_machine_resources_builder, compute_instances_set_machine_resources_task,
    compute_instances_set_machine_type_builder, compute_instances_set_machine_type_task,
    compute_instances_set_metadata_builder, compute_instances_set_metadata_task,
    compute_instances_set_min_cpu_platform_builder, compute_instances_set_min_cpu_platform_task,
    compute_instances_set_name_builder, compute_instances_set_name_task,
    compute_instances_set_scheduling_builder, compute_instances_set_scheduling_task,
    compute_instances_set_security_policy_builder, compute_instances_set_security_policy_task,
    compute_instances_set_service_account_builder, compute_instances_set_service_account_task,
    compute_instances_set_shielded_instance_integrity_policy_builder, compute_instances_set_shielded_instance_integrity_policy_task,
    compute_instances_set_tags_builder, compute_instances_set_tags_task,
    compute_instances_simulate_maintenance_event_builder, compute_instances_simulate_maintenance_event_task,
    compute_instances_start_builder, compute_instances_start_task,
    compute_instances_start_with_encryption_key_builder, compute_instances_start_with_encryption_key_task,
    compute_instances_stop_builder, compute_instances_stop_task,
    compute_instances_suspend_builder, compute_instances_suspend_task,
    compute_instances_test_iam_permissions_builder, compute_instances_test_iam_permissions_task,
    compute_instances_update_builder, compute_instances_update_task,
    compute_instances_update_access_config_builder, compute_instances_update_access_config_task,
    compute_instances_update_display_device_builder, compute_instances_update_display_device_task,
    compute_instances_update_network_interface_builder, compute_instances_update_network_interface_task,
    compute_instances_update_shielded_instance_config_builder, compute_instances_update_shielded_instance_config_task,
    compute_instant_snapshot_groups_delete_builder, compute_instant_snapshot_groups_delete_task,
    compute_instant_snapshot_groups_get_builder, compute_instant_snapshot_groups_get_task,
    compute_instant_snapshot_groups_get_iam_policy_builder, compute_instant_snapshot_groups_get_iam_policy_task,
    compute_instant_snapshot_groups_insert_builder, compute_instant_snapshot_groups_insert_task,
    compute_instant_snapshot_groups_list_builder, compute_instant_snapshot_groups_list_task,
    compute_instant_snapshot_groups_set_iam_policy_builder, compute_instant_snapshot_groups_set_iam_policy_task,
    compute_instant_snapshot_groups_test_iam_permissions_builder, compute_instant_snapshot_groups_test_iam_permissions_task,
    compute_instant_snapshots_aggregated_list_builder, compute_instant_snapshots_aggregated_list_task,
    compute_instant_snapshots_delete_builder, compute_instant_snapshots_delete_task,
    compute_instant_snapshots_get_builder, compute_instant_snapshots_get_task,
    compute_instant_snapshots_get_iam_policy_builder, compute_instant_snapshots_get_iam_policy_task,
    compute_instant_snapshots_insert_builder, compute_instant_snapshots_insert_task,
    compute_instant_snapshots_list_builder, compute_instant_snapshots_list_task,
    compute_instant_snapshots_set_iam_policy_builder, compute_instant_snapshots_set_iam_policy_task,
    compute_instant_snapshots_set_labels_builder, compute_instant_snapshots_set_labels_task,
    compute_instant_snapshots_test_iam_permissions_builder, compute_instant_snapshots_test_iam_permissions_task,
    compute_interconnect_attachment_groups_delete_builder, compute_interconnect_attachment_groups_delete_task,
    compute_interconnect_attachment_groups_get_builder, compute_interconnect_attachment_groups_get_task,
    compute_interconnect_attachment_groups_get_iam_policy_builder, compute_interconnect_attachment_groups_get_iam_policy_task,
    compute_interconnect_attachment_groups_get_operational_status_builder, compute_interconnect_attachment_groups_get_operational_status_task,
    compute_interconnect_attachment_groups_insert_builder, compute_interconnect_attachment_groups_insert_task,
    compute_interconnect_attachment_groups_list_builder, compute_interconnect_attachment_groups_list_task,
    compute_interconnect_attachment_groups_patch_builder, compute_interconnect_attachment_groups_patch_task,
    compute_interconnect_attachment_groups_set_iam_policy_builder, compute_interconnect_attachment_groups_set_iam_policy_task,
    compute_interconnect_attachment_groups_test_iam_permissions_builder, compute_interconnect_attachment_groups_test_iam_permissions_task,
    compute_interconnect_attachments_aggregated_list_builder, compute_interconnect_attachments_aggregated_list_task,
    compute_interconnect_attachments_delete_builder, compute_interconnect_attachments_delete_task,
    compute_interconnect_attachments_get_builder, compute_interconnect_attachments_get_task,
    compute_interconnect_attachments_insert_builder, compute_interconnect_attachments_insert_task,
    compute_interconnect_attachments_list_builder, compute_interconnect_attachments_list_task,
    compute_interconnect_attachments_patch_builder, compute_interconnect_attachments_patch_task,
    compute_interconnect_attachments_set_labels_builder, compute_interconnect_attachments_set_labels_task,
    compute_interconnect_groups_create_members_builder, compute_interconnect_groups_create_members_task,
    compute_interconnect_groups_delete_builder, compute_interconnect_groups_delete_task,
    compute_interconnect_groups_get_builder, compute_interconnect_groups_get_task,
    compute_interconnect_groups_get_iam_policy_builder, compute_interconnect_groups_get_iam_policy_task,
    compute_interconnect_groups_get_operational_status_builder, compute_interconnect_groups_get_operational_status_task,
    compute_interconnect_groups_insert_builder, compute_interconnect_groups_insert_task,
    compute_interconnect_groups_list_builder, compute_interconnect_groups_list_task,
    compute_interconnect_groups_patch_builder, compute_interconnect_groups_patch_task,
    compute_interconnect_groups_set_iam_policy_builder, compute_interconnect_groups_set_iam_policy_task,
    compute_interconnect_groups_test_iam_permissions_builder, compute_interconnect_groups_test_iam_permissions_task,
    compute_interconnect_locations_get_builder, compute_interconnect_locations_get_task,
    compute_interconnect_locations_list_builder, compute_interconnect_locations_list_task,
    compute_interconnect_remote_locations_get_builder, compute_interconnect_remote_locations_get_task,
    compute_interconnect_remote_locations_list_builder, compute_interconnect_remote_locations_list_task,
    compute_interconnects_delete_builder, compute_interconnects_delete_task,
    compute_interconnects_get_builder, compute_interconnects_get_task,
    compute_interconnects_get_diagnostics_builder, compute_interconnects_get_diagnostics_task,
    compute_interconnects_get_macsec_config_builder, compute_interconnects_get_macsec_config_task,
    compute_interconnects_insert_builder, compute_interconnects_insert_task,
    compute_interconnects_list_builder, compute_interconnects_list_task,
    compute_interconnects_patch_builder, compute_interconnects_patch_task,
    compute_interconnects_set_labels_builder, compute_interconnects_set_labels_task,
    compute_license_codes_get_builder, compute_license_codes_get_task,
    compute_license_codes_test_iam_permissions_builder, compute_license_codes_test_iam_permissions_task,
    compute_licenses_delete_builder, compute_licenses_delete_task,
    compute_licenses_get_builder, compute_licenses_get_task,
    compute_licenses_get_iam_policy_builder, compute_licenses_get_iam_policy_task,
    compute_licenses_insert_builder, compute_licenses_insert_task,
    compute_licenses_list_builder, compute_licenses_list_task,
    compute_licenses_set_iam_policy_builder, compute_licenses_set_iam_policy_task,
    compute_licenses_test_iam_permissions_builder, compute_licenses_test_iam_permissions_task,
    compute_licenses_update_builder, compute_licenses_update_task,
    compute_machine_images_delete_builder, compute_machine_images_delete_task,
    compute_machine_images_get_builder, compute_machine_images_get_task,
    compute_machine_images_get_iam_policy_builder, compute_machine_images_get_iam_policy_task,
    compute_machine_images_insert_builder, compute_machine_images_insert_task,
    compute_machine_images_list_builder, compute_machine_images_list_task,
    compute_machine_images_set_iam_policy_builder, compute_machine_images_set_iam_policy_task,
    compute_machine_images_set_labels_builder, compute_machine_images_set_labels_task,
    compute_machine_images_test_iam_permissions_builder, compute_machine_images_test_iam_permissions_task,
    compute_machine_types_aggregated_list_builder, compute_machine_types_aggregated_list_task,
    compute_machine_types_get_builder, compute_machine_types_get_task,
    compute_machine_types_list_builder, compute_machine_types_list_task,
    compute_network_attachments_aggregated_list_builder, compute_network_attachments_aggregated_list_task,
    compute_network_attachments_delete_builder, compute_network_attachments_delete_task,
    compute_network_attachments_get_builder, compute_network_attachments_get_task,
    compute_network_attachments_get_iam_policy_builder, compute_network_attachments_get_iam_policy_task,
    compute_network_attachments_insert_builder, compute_network_attachments_insert_task,
    compute_network_attachments_list_builder, compute_network_attachments_list_task,
    compute_network_attachments_patch_builder, compute_network_attachments_patch_task,
    compute_network_attachments_set_iam_policy_builder, compute_network_attachments_set_iam_policy_task,
    compute_network_attachments_test_iam_permissions_builder, compute_network_attachments_test_iam_permissions_task,
    compute_network_edge_security_services_aggregated_list_builder, compute_network_edge_security_services_aggregated_list_task,
    compute_network_edge_security_services_delete_builder, compute_network_edge_security_services_delete_task,
    compute_network_edge_security_services_get_builder, compute_network_edge_security_services_get_task,
    compute_network_edge_security_services_insert_builder, compute_network_edge_security_services_insert_task,
    compute_network_edge_security_services_patch_builder, compute_network_edge_security_services_patch_task,
    compute_network_endpoint_groups_aggregated_list_builder, compute_network_endpoint_groups_aggregated_list_task,
    compute_network_endpoint_groups_attach_network_endpoints_builder, compute_network_endpoint_groups_attach_network_endpoints_task,
    compute_network_endpoint_groups_delete_builder, compute_network_endpoint_groups_delete_task,
    compute_network_endpoint_groups_detach_network_endpoints_builder, compute_network_endpoint_groups_detach_network_endpoints_task,
    compute_network_endpoint_groups_get_builder, compute_network_endpoint_groups_get_task,
    compute_network_endpoint_groups_insert_builder, compute_network_endpoint_groups_insert_task,
    compute_network_endpoint_groups_list_builder, compute_network_endpoint_groups_list_task,
    compute_network_endpoint_groups_list_network_endpoints_builder, compute_network_endpoint_groups_list_network_endpoints_task,
    compute_network_endpoint_groups_test_iam_permissions_builder, compute_network_endpoint_groups_test_iam_permissions_task,
    compute_network_firewall_policies_add_association_builder, compute_network_firewall_policies_add_association_task,
    compute_network_firewall_policies_add_packet_mirroring_rule_builder, compute_network_firewall_policies_add_packet_mirroring_rule_task,
    compute_network_firewall_policies_add_rule_builder, compute_network_firewall_policies_add_rule_task,
    compute_network_firewall_policies_aggregated_list_builder, compute_network_firewall_policies_aggregated_list_task,
    compute_network_firewall_policies_clone_rules_builder, compute_network_firewall_policies_clone_rules_task,
    compute_network_firewall_policies_delete_builder, compute_network_firewall_policies_delete_task,
    compute_network_firewall_policies_get_builder, compute_network_firewall_policies_get_task,
    compute_network_firewall_policies_get_association_builder, compute_network_firewall_policies_get_association_task,
    compute_network_firewall_policies_get_iam_policy_builder, compute_network_firewall_policies_get_iam_policy_task,
    compute_network_firewall_policies_get_packet_mirroring_rule_builder, compute_network_firewall_policies_get_packet_mirroring_rule_task,
    compute_network_firewall_policies_get_rule_builder, compute_network_firewall_policies_get_rule_task,
    compute_network_firewall_policies_insert_builder, compute_network_firewall_policies_insert_task,
    compute_network_firewall_policies_list_builder, compute_network_firewall_policies_list_task,
    compute_network_firewall_policies_patch_builder, compute_network_firewall_policies_patch_task,
    compute_network_firewall_policies_patch_packet_mirroring_rule_builder, compute_network_firewall_policies_patch_packet_mirroring_rule_task,
    compute_network_firewall_policies_patch_rule_builder, compute_network_firewall_policies_patch_rule_task,
    compute_network_firewall_policies_remove_association_builder, compute_network_firewall_policies_remove_association_task,
    compute_network_firewall_policies_remove_packet_mirroring_rule_builder, compute_network_firewall_policies_remove_packet_mirroring_rule_task,
    compute_network_firewall_policies_remove_rule_builder, compute_network_firewall_policies_remove_rule_task,
    compute_network_firewall_policies_set_iam_policy_builder, compute_network_firewall_policies_set_iam_policy_task,
    compute_network_firewall_policies_test_iam_permissions_builder, compute_network_firewall_policies_test_iam_permissions_task,
    compute_network_profiles_get_builder, compute_network_profiles_get_task,
    compute_network_profiles_list_builder, compute_network_profiles_list_task,
    compute_networks_add_peering_builder, compute_networks_add_peering_task,
    compute_networks_delete_builder, compute_networks_delete_task,
    compute_networks_get_builder, compute_networks_get_task,
    compute_networks_get_effective_firewalls_builder, compute_networks_get_effective_firewalls_task,
    compute_networks_insert_builder, compute_networks_insert_task,
    compute_networks_list_builder, compute_networks_list_task,
    compute_networks_list_peering_routes_builder, compute_networks_list_peering_routes_task,
    compute_networks_patch_builder, compute_networks_patch_task,
    compute_networks_remove_peering_builder, compute_networks_remove_peering_task,
    compute_networks_request_remove_peering_builder, compute_networks_request_remove_peering_task,
    compute_networks_switch_to_custom_mode_builder, compute_networks_switch_to_custom_mode_task,
    compute_networks_update_peering_builder, compute_networks_update_peering_task,
    compute_node_groups_add_nodes_builder, compute_node_groups_add_nodes_task,
    compute_node_groups_aggregated_list_builder, compute_node_groups_aggregated_list_task,
    compute_node_groups_delete_builder, compute_node_groups_delete_task,
    compute_node_groups_delete_nodes_builder, compute_node_groups_delete_nodes_task,
    compute_node_groups_get_builder, compute_node_groups_get_task,
    compute_node_groups_get_iam_policy_builder, compute_node_groups_get_iam_policy_task,
    compute_node_groups_insert_builder, compute_node_groups_insert_task,
    compute_node_groups_list_builder, compute_node_groups_list_task,
    compute_node_groups_list_nodes_builder, compute_node_groups_list_nodes_task,
    compute_node_groups_patch_builder, compute_node_groups_patch_task,
    compute_node_groups_perform_maintenance_builder, compute_node_groups_perform_maintenance_task,
    compute_node_groups_set_iam_policy_builder, compute_node_groups_set_iam_policy_task,
    compute_node_groups_set_node_template_builder, compute_node_groups_set_node_template_task,
    compute_node_groups_simulate_maintenance_event_builder, compute_node_groups_simulate_maintenance_event_task,
    compute_node_groups_test_iam_permissions_builder, compute_node_groups_test_iam_permissions_task,
    compute_node_templates_aggregated_list_builder, compute_node_templates_aggregated_list_task,
    compute_node_templates_delete_builder, compute_node_templates_delete_task,
    compute_node_templates_get_builder, compute_node_templates_get_task,
    compute_node_templates_get_iam_policy_builder, compute_node_templates_get_iam_policy_task,
    compute_node_templates_insert_builder, compute_node_templates_insert_task,
    compute_node_templates_list_builder, compute_node_templates_list_task,
    compute_node_templates_set_iam_policy_builder, compute_node_templates_set_iam_policy_task,
    compute_node_templates_test_iam_permissions_builder, compute_node_templates_test_iam_permissions_task,
    compute_node_types_aggregated_list_builder, compute_node_types_aggregated_list_task,
    compute_node_types_get_builder, compute_node_types_get_task,
    compute_node_types_list_builder, compute_node_types_list_task,
    compute_organization_security_policies_add_association_builder, compute_organization_security_policies_add_association_task,
    compute_organization_security_policies_add_rule_builder, compute_organization_security_policies_add_rule_task,
    compute_organization_security_policies_copy_rules_builder, compute_organization_security_policies_copy_rules_task,
    compute_organization_security_policies_delete_builder, compute_organization_security_policies_delete_task,
    compute_organization_security_policies_get_builder, compute_organization_security_policies_get_task,
    compute_organization_security_policies_get_association_builder, compute_organization_security_policies_get_association_task,
    compute_organization_security_policies_get_rule_builder, compute_organization_security_policies_get_rule_task,
    compute_organization_security_policies_insert_builder, compute_organization_security_policies_insert_task,
    compute_organization_security_policies_list_builder, compute_organization_security_policies_list_task,
    compute_organization_security_policies_list_associations_builder, compute_organization_security_policies_list_associations_task,
    compute_organization_security_policies_list_preconfigured_expression_sets_builder, compute_organization_security_policies_list_preconfigured_expression_sets_task,
    compute_organization_security_policies_move_builder, compute_organization_security_policies_move_task,
    compute_organization_security_policies_patch_builder, compute_organization_security_policies_patch_task,
    compute_organization_security_policies_patch_rule_builder, compute_organization_security_policies_patch_rule_task,
    compute_organization_security_policies_remove_association_builder, compute_organization_security_policies_remove_association_task,
    compute_organization_security_policies_remove_rule_builder, compute_organization_security_policies_remove_rule_task,
    compute_packet_mirrorings_aggregated_list_builder, compute_packet_mirrorings_aggregated_list_task,
    compute_packet_mirrorings_delete_builder, compute_packet_mirrorings_delete_task,
    compute_packet_mirrorings_get_builder, compute_packet_mirrorings_get_task,
    compute_packet_mirrorings_insert_builder, compute_packet_mirrorings_insert_task,
    compute_packet_mirrorings_list_builder, compute_packet_mirrorings_list_task,
    compute_packet_mirrorings_patch_builder, compute_packet_mirrorings_patch_task,
    compute_packet_mirrorings_test_iam_permissions_builder, compute_packet_mirrorings_test_iam_permissions_task,
    compute_preview_features_get_builder, compute_preview_features_get_task,
    compute_preview_features_list_builder, compute_preview_features_list_task,
    compute_preview_features_update_builder, compute_preview_features_update_task,
    compute_projects_disable_xpn_host_builder, compute_projects_disable_xpn_host_task,
    compute_projects_disable_xpn_resource_builder, compute_projects_disable_xpn_resource_task,
    compute_projects_enable_xpn_host_builder, compute_projects_enable_xpn_host_task,
    compute_projects_enable_xpn_resource_builder, compute_projects_enable_xpn_resource_task,
    compute_projects_get_builder, compute_projects_get_task,
    compute_projects_get_xpn_host_builder, compute_projects_get_xpn_host_task,
    compute_projects_get_xpn_resources_builder, compute_projects_get_xpn_resources_task,
    compute_projects_list_xpn_hosts_builder, compute_projects_list_xpn_hosts_task,
    compute_projects_move_disk_builder, compute_projects_move_disk_task,
    compute_projects_move_instance_builder, compute_projects_move_instance_task,
    compute_projects_set_cloud_armor_tier_builder, compute_projects_set_cloud_armor_tier_task,
    compute_projects_set_common_instance_metadata_builder, compute_projects_set_common_instance_metadata_task,
    compute_projects_set_default_network_tier_builder, compute_projects_set_default_network_tier_task,
    compute_projects_set_usage_export_bucket_builder, compute_projects_set_usage_export_bucket_task,
    compute_public_advertised_prefixes_announce_builder, compute_public_advertised_prefixes_announce_task,
    compute_public_advertised_prefixes_delete_builder, compute_public_advertised_prefixes_delete_task,
    compute_public_advertised_prefixes_get_builder, compute_public_advertised_prefixes_get_task,
    compute_public_advertised_prefixes_insert_builder, compute_public_advertised_prefixes_insert_task,
    compute_public_advertised_prefixes_list_builder, compute_public_advertised_prefixes_list_task,
    compute_public_advertised_prefixes_patch_builder, compute_public_advertised_prefixes_patch_task,
    compute_public_advertised_prefixes_withdraw_builder, compute_public_advertised_prefixes_withdraw_task,
    compute_public_delegated_prefixes_aggregated_list_builder, compute_public_delegated_prefixes_aggregated_list_task,
    compute_public_delegated_prefixes_announce_builder, compute_public_delegated_prefixes_announce_task,
    compute_public_delegated_prefixes_delete_builder, compute_public_delegated_prefixes_delete_task,
    compute_public_delegated_prefixes_get_builder, compute_public_delegated_prefixes_get_task,
    compute_public_delegated_prefixes_insert_builder, compute_public_delegated_prefixes_insert_task,
    compute_public_delegated_prefixes_list_builder, compute_public_delegated_prefixes_list_task,
    compute_public_delegated_prefixes_patch_builder, compute_public_delegated_prefixes_patch_task,
    compute_public_delegated_prefixes_withdraw_builder, compute_public_delegated_prefixes_withdraw_task,
    compute_region_autoscalers_delete_builder, compute_region_autoscalers_delete_task,
    compute_region_autoscalers_get_builder, compute_region_autoscalers_get_task,
    compute_region_autoscalers_insert_builder, compute_region_autoscalers_insert_task,
    compute_region_autoscalers_list_builder, compute_region_autoscalers_list_task,
    compute_region_autoscalers_patch_builder, compute_region_autoscalers_patch_task,
    compute_region_autoscalers_test_iam_permissions_builder, compute_region_autoscalers_test_iam_permissions_task,
    compute_region_autoscalers_update_builder, compute_region_autoscalers_update_task,
    compute_region_backend_buckets_delete_builder, compute_region_backend_buckets_delete_task,
    compute_region_backend_buckets_get_builder, compute_region_backend_buckets_get_task,
    compute_region_backend_buckets_get_iam_policy_builder, compute_region_backend_buckets_get_iam_policy_task,
    compute_region_backend_buckets_insert_builder, compute_region_backend_buckets_insert_task,
    compute_region_backend_buckets_list_builder, compute_region_backend_buckets_list_task,
    compute_region_backend_buckets_list_usable_builder, compute_region_backend_buckets_list_usable_task,
    compute_region_backend_buckets_patch_builder, compute_region_backend_buckets_patch_task,
    compute_region_backend_buckets_set_iam_policy_builder, compute_region_backend_buckets_set_iam_policy_task,
    compute_region_backend_buckets_test_iam_permissions_builder, compute_region_backend_buckets_test_iam_permissions_task,
    compute_region_backend_services_delete_builder, compute_region_backend_services_delete_task,
    compute_region_backend_services_get_builder, compute_region_backend_services_get_task,
    compute_region_backend_services_get_health_builder, compute_region_backend_services_get_health_task,
    compute_region_backend_services_get_iam_policy_builder, compute_region_backend_services_get_iam_policy_task,
    compute_region_backend_services_insert_builder, compute_region_backend_services_insert_task,
    compute_region_backend_services_list_builder, compute_region_backend_services_list_task,
    compute_region_backend_services_list_usable_builder, compute_region_backend_services_list_usable_task,
    compute_region_backend_services_patch_builder, compute_region_backend_services_patch_task,
    compute_region_backend_services_set_iam_policy_builder, compute_region_backend_services_set_iam_policy_task,
    compute_region_backend_services_set_security_policy_builder, compute_region_backend_services_set_security_policy_task,
    compute_region_backend_services_test_iam_permissions_builder, compute_region_backend_services_test_iam_permissions_task,
    compute_region_backend_services_update_builder, compute_region_backend_services_update_task,
    compute_region_commitments_aggregated_list_builder, compute_region_commitments_aggregated_list_task,
    compute_region_commitments_get_builder, compute_region_commitments_get_task,
    compute_region_commitments_insert_builder, compute_region_commitments_insert_task,
    compute_region_commitments_list_builder, compute_region_commitments_list_task,
    compute_region_commitments_update_builder, compute_region_commitments_update_task,
    compute_region_composite_health_checks_aggregated_list_builder, compute_region_composite_health_checks_aggregated_list_task,
    compute_region_composite_health_checks_delete_builder, compute_region_composite_health_checks_delete_task,
    compute_region_composite_health_checks_get_builder, compute_region_composite_health_checks_get_task,
    compute_region_composite_health_checks_get_health_builder, compute_region_composite_health_checks_get_health_task,
    compute_region_composite_health_checks_insert_builder, compute_region_composite_health_checks_insert_task,
    compute_region_composite_health_checks_list_builder, compute_region_composite_health_checks_list_task,
    compute_region_composite_health_checks_patch_builder, compute_region_composite_health_checks_patch_task,
    compute_region_composite_health_checks_test_iam_permissions_builder, compute_region_composite_health_checks_test_iam_permissions_task,
    compute_region_disk_types_get_builder, compute_region_disk_types_get_task,
    compute_region_disk_types_list_builder, compute_region_disk_types_list_task,
    compute_region_disks_add_resource_policies_builder, compute_region_disks_add_resource_policies_task,
    compute_region_disks_bulk_insert_builder, compute_region_disks_bulk_insert_task,
    compute_region_disks_create_snapshot_builder, compute_region_disks_create_snapshot_task,
    compute_region_disks_delete_builder, compute_region_disks_delete_task,
    compute_region_disks_get_builder, compute_region_disks_get_task,
    compute_region_disks_get_iam_policy_builder, compute_region_disks_get_iam_policy_task,
    compute_region_disks_insert_builder, compute_region_disks_insert_task,
    compute_region_disks_list_builder, compute_region_disks_list_task,
    compute_region_disks_remove_resource_policies_builder, compute_region_disks_remove_resource_policies_task,
    compute_region_disks_resize_builder, compute_region_disks_resize_task,
    compute_region_disks_set_iam_policy_builder, compute_region_disks_set_iam_policy_task,
    compute_region_disks_set_labels_builder, compute_region_disks_set_labels_task,
    compute_region_disks_start_async_replication_builder, compute_region_disks_start_async_replication_task,
    compute_region_disks_stop_async_replication_builder, compute_region_disks_stop_async_replication_task,
    compute_region_disks_stop_group_async_replication_builder, compute_region_disks_stop_group_async_replication_task,
    compute_region_disks_test_iam_permissions_builder, compute_region_disks_test_iam_permissions_task,
    compute_region_disks_update_builder, compute_region_disks_update_task,
    compute_region_disks_update_kms_key_builder, compute_region_disks_update_kms_key_task,
    compute_region_health_aggregation_policies_aggregated_list_builder, compute_region_health_aggregation_policies_aggregated_list_task,
    compute_region_health_aggregation_policies_delete_builder, compute_region_health_aggregation_policies_delete_task,
    compute_region_health_aggregation_policies_get_builder, compute_region_health_aggregation_policies_get_task,
    compute_region_health_aggregation_policies_insert_builder, compute_region_health_aggregation_policies_insert_task,
    compute_region_health_aggregation_policies_list_builder, compute_region_health_aggregation_policies_list_task,
    compute_region_health_aggregation_policies_patch_builder, compute_region_health_aggregation_policies_patch_task,
    compute_region_health_aggregation_policies_test_iam_permissions_builder, compute_region_health_aggregation_policies_test_iam_permissions_task,
    compute_region_health_check_services_aggregated_list_builder, compute_region_health_check_services_aggregated_list_task,
    compute_region_health_check_services_delete_builder, compute_region_health_check_services_delete_task,
    compute_region_health_check_services_get_builder, compute_region_health_check_services_get_task,
    compute_region_health_check_services_insert_builder, compute_region_health_check_services_insert_task,
    compute_region_health_check_services_list_builder, compute_region_health_check_services_list_task,
    compute_region_health_check_services_patch_builder, compute_region_health_check_services_patch_task,
    compute_region_health_check_services_test_iam_permissions_builder, compute_region_health_check_services_test_iam_permissions_task,
    compute_region_health_checks_delete_builder, compute_region_health_checks_delete_task,
    compute_region_health_checks_get_builder, compute_region_health_checks_get_task,
    compute_region_health_checks_insert_builder, compute_region_health_checks_insert_task,
    compute_region_health_checks_list_builder, compute_region_health_checks_list_task,
    compute_region_health_checks_patch_builder, compute_region_health_checks_patch_task,
    compute_region_health_checks_test_iam_permissions_builder, compute_region_health_checks_test_iam_permissions_task,
    compute_region_health_checks_update_builder, compute_region_health_checks_update_task,
    compute_region_health_sources_aggregated_list_builder, compute_region_health_sources_aggregated_list_task,
    compute_region_health_sources_delete_builder, compute_region_health_sources_delete_task,
    compute_region_health_sources_get_builder, compute_region_health_sources_get_task,
    compute_region_health_sources_get_health_builder, compute_region_health_sources_get_health_task,
    compute_region_health_sources_insert_builder, compute_region_health_sources_insert_task,
    compute_region_health_sources_list_builder, compute_region_health_sources_list_task,
    compute_region_health_sources_patch_builder, compute_region_health_sources_patch_task,
    compute_region_health_sources_test_iam_permissions_builder, compute_region_health_sources_test_iam_permissions_task,
    compute_region_instance_group_manager_resize_requests_cancel_builder, compute_region_instance_group_manager_resize_requests_cancel_task,
    compute_region_instance_group_manager_resize_requests_delete_builder, compute_region_instance_group_manager_resize_requests_delete_task,
    compute_region_instance_group_manager_resize_requests_get_builder, compute_region_instance_group_manager_resize_requests_get_task,
    compute_region_instance_group_manager_resize_requests_insert_builder, compute_region_instance_group_manager_resize_requests_insert_task,
    compute_region_instance_group_manager_resize_requests_list_builder, compute_region_instance_group_manager_resize_requests_list_task,
    compute_region_instance_group_managers_abandon_instances_builder, compute_region_instance_group_managers_abandon_instances_task,
    compute_region_instance_group_managers_apply_updates_to_instances_builder, compute_region_instance_group_managers_apply_updates_to_instances_task,
    compute_region_instance_group_managers_create_instances_builder, compute_region_instance_group_managers_create_instances_task,
    compute_region_instance_group_managers_delete_builder, compute_region_instance_group_managers_delete_task,
    compute_region_instance_group_managers_delete_instances_builder, compute_region_instance_group_managers_delete_instances_task,
    compute_region_instance_group_managers_delete_per_instance_configs_builder, compute_region_instance_group_managers_delete_per_instance_configs_task,
    compute_region_instance_group_managers_get_builder, compute_region_instance_group_managers_get_task,
    compute_region_instance_group_managers_insert_builder, compute_region_instance_group_managers_insert_task,
    compute_region_instance_group_managers_list_builder, compute_region_instance_group_managers_list_task,
    compute_region_instance_group_managers_list_errors_builder, compute_region_instance_group_managers_list_errors_task,
    compute_region_instance_group_managers_list_managed_instances_builder, compute_region_instance_group_managers_list_managed_instances_task,
    compute_region_instance_group_managers_list_per_instance_configs_builder, compute_region_instance_group_managers_list_per_instance_configs_task,
    compute_region_instance_group_managers_patch_builder, compute_region_instance_group_managers_patch_task,
    compute_region_instance_group_managers_patch_per_instance_configs_builder, compute_region_instance_group_managers_patch_per_instance_configs_task,
    compute_region_instance_group_managers_recreate_instances_builder, compute_region_instance_group_managers_recreate_instances_task,
    compute_region_instance_group_managers_resize_builder, compute_region_instance_group_managers_resize_task,
    compute_region_instance_group_managers_resume_instances_builder, compute_region_instance_group_managers_resume_instances_task,
    compute_region_instance_group_managers_set_instance_template_builder, compute_region_instance_group_managers_set_instance_template_task,
    compute_region_instance_group_managers_set_target_pools_builder, compute_region_instance_group_managers_set_target_pools_task,
    compute_region_instance_group_managers_start_instances_builder, compute_region_instance_group_managers_start_instances_task,
    compute_region_instance_group_managers_stop_instances_builder, compute_region_instance_group_managers_stop_instances_task,
    compute_region_instance_group_managers_suspend_instances_builder, compute_region_instance_group_managers_suspend_instances_task,
    compute_region_instance_group_managers_update_per_instance_configs_builder, compute_region_instance_group_managers_update_per_instance_configs_task,
    compute_region_instance_groups_get_builder, compute_region_instance_groups_get_task,
    compute_region_instance_groups_list_builder, compute_region_instance_groups_list_task,
    compute_region_instance_groups_list_instances_builder, compute_region_instance_groups_list_instances_task,
    compute_region_instance_groups_set_named_ports_builder, compute_region_instance_groups_set_named_ports_task,
    compute_region_instance_groups_test_iam_permissions_builder, compute_region_instance_groups_test_iam_permissions_task,
    compute_region_instance_templates_delete_builder, compute_region_instance_templates_delete_task,
    compute_region_instance_templates_get_builder, compute_region_instance_templates_get_task,
    compute_region_instance_templates_insert_builder, compute_region_instance_templates_insert_task,
    compute_region_instance_templates_list_builder, compute_region_instance_templates_list_task,
    compute_region_instances_bulk_insert_builder, compute_region_instances_bulk_insert_task,
    compute_region_instant_snapshot_groups_delete_builder, compute_region_instant_snapshot_groups_delete_task,
    compute_region_instant_snapshot_groups_get_builder, compute_region_instant_snapshot_groups_get_task,
    compute_region_instant_snapshot_groups_get_iam_policy_builder, compute_region_instant_snapshot_groups_get_iam_policy_task,
    compute_region_instant_snapshot_groups_insert_builder, compute_region_instant_snapshot_groups_insert_task,
    compute_region_instant_snapshot_groups_list_builder, compute_region_instant_snapshot_groups_list_task,
    compute_region_instant_snapshot_groups_set_iam_policy_builder, compute_region_instant_snapshot_groups_set_iam_policy_task,
    compute_region_instant_snapshot_groups_test_iam_permissions_builder, compute_region_instant_snapshot_groups_test_iam_permissions_task,
    compute_region_instant_snapshots_delete_builder, compute_region_instant_snapshots_delete_task,
    compute_region_instant_snapshots_get_builder, compute_region_instant_snapshots_get_task,
    compute_region_instant_snapshots_get_iam_policy_builder, compute_region_instant_snapshots_get_iam_policy_task,
    compute_region_instant_snapshots_insert_builder, compute_region_instant_snapshots_insert_task,
    compute_region_instant_snapshots_list_builder, compute_region_instant_snapshots_list_task,
    compute_region_instant_snapshots_set_iam_policy_builder, compute_region_instant_snapshots_set_iam_policy_task,
    compute_region_instant_snapshots_set_labels_builder, compute_region_instant_snapshots_set_labels_task,
    compute_region_instant_snapshots_test_iam_permissions_builder, compute_region_instant_snapshots_test_iam_permissions_task,
    compute_region_network_endpoint_groups_attach_network_endpoints_builder, compute_region_network_endpoint_groups_attach_network_endpoints_task,
    compute_region_network_endpoint_groups_delete_builder, compute_region_network_endpoint_groups_delete_task,
    compute_region_network_endpoint_groups_detach_network_endpoints_builder, compute_region_network_endpoint_groups_detach_network_endpoints_task,
    compute_region_network_endpoint_groups_get_builder, compute_region_network_endpoint_groups_get_task,
    compute_region_network_endpoint_groups_insert_builder, compute_region_network_endpoint_groups_insert_task,
    compute_region_network_endpoint_groups_list_builder, compute_region_network_endpoint_groups_list_task,
    compute_region_network_endpoint_groups_list_network_endpoints_builder, compute_region_network_endpoint_groups_list_network_endpoints_task,
    compute_region_network_firewall_policies_add_association_builder, compute_region_network_firewall_policies_add_association_task,
    compute_region_network_firewall_policies_add_rule_builder, compute_region_network_firewall_policies_add_rule_task,
    compute_region_network_firewall_policies_clone_rules_builder, compute_region_network_firewall_policies_clone_rules_task,
    compute_region_network_firewall_policies_delete_builder, compute_region_network_firewall_policies_delete_task,
    compute_region_network_firewall_policies_get_builder, compute_region_network_firewall_policies_get_task,
    compute_region_network_firewall_policies_get_association_builder, compute_region_network_firewall_policies_get_association_task,
    compute_region_network_firewall_policies_get_effective_firewalls_builder, compute_region_network_firewall_policies_get_effective_firewalls_task,
    compute_region_network_firewall_policies_get_iam_policy_builder, compute_region_network_firewall_policies_get_iam_policy_task,
    compute_region_network_firewall_policies_get_rule_builder, compute_region_network_firewall_policies_get_rule_task,
    compute_region_network_firewall_policies_insert_builder, compute_region_network_firewall_policies_insert_task,
    compute_region_network_firewall_policies_list_builder, compute_region_network_firewall_policies_list_task,
    compute_region_network_firewall_policies_patch_builder, compute_region_network_firewall_policies_patch_task,
    compute_region_network_firewall_policies_patch_rule_builder, compute_region_network_firewall_policies_patch_rule_task,
    compute_region_network_firewall_policies_remove_association_builder, compute_region_network_firewall_policies_remove_association_task,
    compute_region_network_firewall_policies_remove_rule_builder, compute_region_network_firewall_policies_remove_rule_task,
    compute_region_network_firewall_policies_set_iam_policy_builder, compute_region_network_firewall_policies_set_iam_policy_task,
    compute_region_network_firewall_policies_test_iam_permissions_builder, compute_region_network_firewall_policies_test_iam_permissions_task,
    compute_region_notification_endpoints_aggregated_list_builder, compute_region_notification_endpoints_aggregated_list_task,
    compute_region_notification_endpoints_delete_builder, compute_region_notification_endpoints_delete_task,
    compute_region_notification_endpoints_get_builder, compute_region_notification_endpoints_get_task,
    compute_region_notification_endpoints_insert_builder, compute_region_notification_endpoints_insert_task,
    compute_region_notification_endpoints_list_builder, compute_region_notification_endpoints_list_task,
    compute_region_notification_endpoints_test_iam_permissions_builder, compute_region_notification_endpoints_test_iam_permissions_task,
    compute_region_operations_delete_builder, compute_region_operations_delete_task,
    compute_region_operations_get_builder, compute_region_operations_get_task,
    compute_region_operations_list_builder, compute_region_operations_list_task,
    compute_region_operations_wait_builder, compute_region_operations_wait_task,
    compute_region_security_policies_add_rule_builder, compute_region_security_policies_add_rule_task,
    compute_region_security_policies_delete_builder, compute_region_security_policies_delete_task,
    compute_region_security_policies_get_builder, compute_region_security_policies_get_task,
    compute_region_security_policies_get_rule_builder, compute_region_security_policies_get_rule_task,
    compute_region_security_policies_insert_builder, compute_region_security_policies_insert_task,
    compute_region_security_policies_list_builder, compute_region_security_policies_list_task,
    compute_region_security_policies_patch_builder, compute_region_security_policies_patch_task,
    compute_region_security_policies_patch_rule_builder, compute_region_security_policies_patch_rule_task,
    compute_region_security_policies_remove_rule_builder, compute_region_security_policies_remove_rule_task,
    compute_region_security_policies_set_labels_builder, compute_region_security_policies_set_labels_task,
    compute_region_snapshot_settings_get_builder, compute_region_snapshot_settings_get_task,
    compute_region_snapshot_settings_patch_builder, compute_region_snapshot_settings_patch_task,
    compute_region_snapshots_delete_builder, compute_region_snapshots_delete_task,
    compute_region_snapshots_get_builder, compute_region_snapshots_get_task,
    compute_region_snapshots_get_iam_policy_builder, compute_region_snapshots_get_iam_policy_task,
    compute_region_snapshots_insert_builder, compute_region_snapshots_insert_task,
    compute_region_snapshots_list_builder, compute_region_snapshots_list_task,
    compute_region_snapshots_set_iam_policy_builder, compute_region_snapshots_set_iam_policy_task,
    compute_region_snapshots_set_labels_builder, compute_region_snapshots_set_labels_task,
    compute_region_snapshots_test_iam_permissions_builder, compute_region_snapshots_test_iam_permissions_task,
    compute_region_snapshots_update_kms_key_builder, compute_region_snapshots_update_kms_key_task,
    compute_region_ssl_certificates_delete_builder, compute_region_ssl_certificates_delete_task,
    compute_region_ssl_certificates_get_builder, compute_region_ssl_certificates_get_task,
    compute_region_ssl_certificates_insert_builder, compute_region_ssl_certificates_insert_task,
    compute_region_ssl_certificates_list_builder, compute_region_ssl_certificates_list_task,
    compute_region_ssl_policies_delete_builder, compute_region_ssl_policies_delete_task,
    compute_region_ssl_policies_get_builder, compute_region_ssl_policies_get_task,
    compute_region_ssl_policies_insert_builder, compute_region_ssl_policies_insert_task,
    compute_region_ssl_policies_list_builder, compute_region_ssl_policies_list_task,
    compute_region_ssl_policies_list_available_features_builder, compute_region_ssl_policies_list_available_features_task,
    compute_region_ssl_policies_patch_builder, compute_region_ssl_policies_patch_task,
    compute_region_target_http_proxies_delete_builder, compute_region_target_http_proxies_delete_task,
    compute_region_target_http_proxies_get_builder, compute_region_target_http_proxies_get_task,
    compute_region_target_http_proxies_insert_builder, compute_region_target_http_proxies_insert_task,
    compute_region_target_http_proxies_list_builder, compute_region_target_http_proxies_list_task,
    compute_region_target_http_proxies_set_url_map_builder, compute_region_target_http_proxies_set_url_map_task,
    compute_region_target_https_proxies_delete_builder, compute_region_target_https_proxies_delete_task,
    compute_region_target_https_proxies_get_builder, compute_region_target_https_proxies_get_task,
    compute_region_target_https_proxies_insert_builder, compute_region_target_https_proxies_insert_task,
    compute_region_target_https_proxies_list_builder, compute_region_target_https_proxies_list_task,
    compute_region_target_https_proxies_patch_builder, compute_region_target_https_proxies_patch_task,
    compute_region_target_https_proxies_set_ssl_certificates_builder, compute_region_target_https_proxies_set_ssl_certificates_task,
    compute_region_target_https_proxies_set_url_map_builder, compute_region_target_https_proxies_set_url_map_task,
    compute_region_target_tcp_proxies_delete_builder, compute_region_target_tcp_proxies_delete_task,
    compute_region_target_tcp_proxies_get_builder, compute_region_target_tcp_proxies_get_task,
    compute_region_target_tcp_proxies_insert_builder, compute_region_target_tcp_proxies_insert_task,
    compute_region_target_tcp_proxies_list_builder, compute_region_target_tcp_proxies_list_task,
    compute_region_url_maps_delete_builder, compute_region_url_maps_delete_task,
    compute_region_url_maps_get_builder, compute_region_url_maps_get_task,
    compute_region_url_maps_insert_builder, compute_region_url_maps_insert_task,
    compute_region_url_maps_list_builder, compute_region_url_maps_list_task,
    compute_region_url_maps_patch_builder, compute_region_url_maps_patch_task,
    compute_region_url_maps_update_builder, compute_region_url_maps_update_task,
    compute_region_url_maps_validate_builder, compute_region_url_maps_validate_task,
    compute_region_zones_list_builder, compute_region_zones_list_task,
    compute_regions_get_builder, compute_regions_get_task,
    compute_regions_list_builder, compute_regions_list_task,
    compute_reservation_blocks_get_builder, compute_reservation_blocks_get_task,
    compute_reservation_blocks_get_iam_policy_builder, compute_reservation_blocks_get_iam_policy_task,
    compute_reservation_blocks_list_builder, compute_reservation_blocks_list_task,
    compute_reservation_blocks_perform_maintenance_builder, compute_reservation_blocks_perform_maintenance_task,
    compute_reservation_blocks_set_iam_policy_builder, compute_reservation_blocks_set_iam_policy_task,
    compute_reservation_blocks_test_iam_permissions_builder, compute_reservation_blocks_test_iam_permissions_task,
    compute_reservation_slots_get_builder, compute_reservation_slots_get_task,
    compute_reservation_slots_get_version_builder, compute_reservation_slots_get_version_task,
    compute_reservation_slots_list_builder, compute_reservation_slots_list_task,
    compute_reservation_slots_update_builder, compute_reservation_slots_update_task,
    compute_reservation_sub_blocks_get_builder, compute_reservation_sub_blocks_get_task,
    compute_reservation_sub_blocks_get_iam_policy_builder, compute_reservation_sub_blocks_get_iam_policy_task,
    compute_reservation_sub_blocks_get_version_builder, compute_reservation_sub_blocks_get_version_task,
    compute_reservation_sub_blocks_list_builder, compute_reservation_sub_blocks_list_task,
    compute_reservation_sub_blocks_perform_maintenance_builder, compute_reservation_sub_blocks_perform_maintenance_task,
    compute_reservation_sub_blocks_report_faulty_builder, compute_reservation_sub_blocks_report_faulty_task,
    compute_reservation_sub_blocks_set_iam_policy_builder, compute_reservation_sub_blocks_set_iam_policy_task,
    compute_reservation_sub_blocks_test_iam_permissions_builder, compute_reservation_sub_blocks_test_iam_permissions_task,
    compute_reservations_aggregated_list_builder, compute_reservations_aggregated_list_task,
    compute_reservations_delete_builder, compute_reservations_delete_task,
    compute_reservations_get_builder, compute_reservations_get_task,
    compute_reservations_get_iam_policy_builder, compute_reservations_get_iam_policy_task,
    compute_reservations_insert_builder, compute_reservations_insert_task,
    compute_reservations_list_builder, compute_reservations_list_task,
    compute_reservations_perform_maintenance_builder, compute_reservations_perform_maintenance_task,
    compute_reservations_resize_builder, compute_reservations_resize_task,
    compute_reservations_set_iam_policy_builder, compute_reservations_set_iam_policy_task,
    compute_reservations_test_iam_permissions_builder, compute_reservations_test_iam_permissions_task,
    compute_reservations_update_builder, compute_reservations_update_task,
    compute_resource_policies_aggregated_list_builder, compute_resource_policies_aggregated_list_task,
    compute_resource_policies_delete_builder, compute_resource_policies_delete_task,
    compute_resource_policies_get_builder, compute_resource_policies_get_task,
    compute_resource_policies_get_iam_policy_builder, compute_resource_policies_get_iam_policy_task,
    compute_resource_policies_insert_builder, compute_resource_policies_insert_task,
    compute_resource_policies_list_builder, compute_resource_policies_list_task,
    compute_resource_policies_patch_builder, compute_resource_policies_patch_task,
    compute_resource_policies_set_iam_policy_builder, compute_resource_policies_set_iam_policy_task,
    compute_resource_policies_test_iam_permissions_builder, compute_resource_policies_test_iam_permissions_task,
    compute_routers_aggregated_list_builder, compute_routers_aggregated_list_task,
    compute_routers_delete_builder, compute_routers_delete_task,
    compute_routers_delete_route_policy_builder, compute_routers_delete_route_policy_task,
    compute_routers_get_builder, compute_routers_get_task,
    compute_routers_get_nat_ip_info_builder, compute_routers_get_nat_ip_info_task,
    compute_routers_get_nat_mapping_info_builder, compute_routers_get_nat_mapping_info_task,
    compute_routers_get_route_policy_builder, compute_routers_get_route_policy_task,
    compute_routers_get_router_status_builder, compute_routers_get_router_status_task,
    compute_routers_insert_builder, compute_routers_insert_task,
    compute_routers_list_builder, compute_routers_list_task,
    compute_routers_list_bgp_routes_builder, compute_routers_list_bgp_routes_task,
    compute_routers_list_route_policies_builder, compute_routers_list_route_policies_task,
    compute_routers_patch_builder, compute_routers_patch_task,
    compute_routers_patch_route_policy_builder, compute_routers_patch_route_policy_task,
    compute_routers_preview_builder, compute_routers_preview_task,
    compute_routers_update_builder, compute_routers_update_task,
    compute_routers_update_route_policy_builder, compute_routers_update_route_policy_task,
    compute_routes_delete_builder, compute_routes_delete_task,
    compute_routes_get_builder, compute_routes_get_task,
    compute_routes_insert_builder, compute_routes_insert_task,
    compute_routes_list_builder, compute_routes_list_task,
    compute_routes_test_iam_permissions_builder, compute_routes_test_iam_permissions_task,
    compute_security_policies_add_rule_builder, compute_security_policies_add_rule_task,
    compute_security_policies_aggregated_list_builder, compute_security_policies_aggregated_list_task,
    compute_security_policies_delete_builder, compute_security_policies_delete_task,
    compute_security_policies_get_builder, compute_security_policies_get_task,
    compute_security_policies_get_rule_builder, compute_security_policies_get_rule_task,
    compute_security_policies_insert_builder, compute_security_policies_insert_task,
    compute_security_policies_list_builder, compute_security_policies_list_task,
    compute_security_policies_list_preconfigured_expression_sets_builder, compute_security_policies_list_preconfigured_expression_sets_task,
    compute_security_policies_patch_builder, compute_security_policies_patch_task,
    compute_security_policies_patch_rule_builder, compute_security_policies_patch_rule_task,
    compute_security_policies_remove_rule_builder, compute_security_policies_remove_rule_task,
    compute_security_policies_set_labels_builder, compute_security_policies_set_labels_task,
    compute_service_attachments_aggregated_list_builder, compute_service_attachments_aggregated_list_task,
    compute_service_attachments_delete_builder, compute_service_attachments_delete_task,
    compute_service_attachments_get_builder, compute_service_attachments_get_task,
    compute_service_attachments_get_iam_policy_builder, compute_service_attachments_get_iam_policy_task,
    compute_service_attachments_insert_builder, compute_service_attachments_insert_task,
    compute_service_attachments_list_builder, compute_service_attachments_list_task,
    compute_service_attachments_patch_builder, compute_service_attachments_patch_task,
    compute_service_attachments_set_iam_policy_builder, compute_service_attachments_set_iam_policy_task,
    compute_service_attachments_test_iam_permissions_builder, compute_service_attachments_test_iam_permissions_task,
    compute_snapshot_settings_get_builder, compute_snapshot_settings_get_task,
    compute_snapshot_settings_patch_builder, compute_snapshot_settings_patch_task,
    compute_snapshots_delete_builder, compute_snapshots_delete_task,
    compute_snapshots_get_builder, compute_snapshots_get_task,
    compute_snapshots_get_iam_policy_builder, compute_snapshots_get_iam_policy_task,
    compute_snapshots_insert_builder, compute_snapshots_insert_task,
    compute_snapshots_list_builder, compute_snapshots_list_task,
    compute_snapshots_set_iam_policy_builder, compute_snapshots_set_iam_policy_task,
    compute_snapshots_set_labels_builder, compute_snapshots_set_labels_task,
    compute_snapshots_test_iam_permissions_builder, compute_snapshots_test_iam_permissions_task,
    compute_snapshots_update_kms_key_builder, compute_snapshots_update_kms_key_task,
    compute_ssl_certificates_aggregated_list_builder, compute_ssl_certificates_aggregated_list_task,
    compute_ssl_certificates_delete_builder, compute_ssl_certificates_delete_task,
    compute_ssl_certificates_get_builder, compute_ssl_certificates_get_task,
    compute_ssl_certificates_insert_builder, compute_ssl_certificates_insert_task,
    compute_ssl_certificates_list_builder, compute_ssl_certificates_list_task,
    compute_ssl_policies_aggregated_list_builder, compute_ssl_policies_aggregated_list_task,
    compute_ssl_policies_delete_builder, compute_ssl_policies_delete_task,
    compute_ssl_policies_get_builder, compute_ssl_policies_get_task,
    compute_ssl_policies_insert_builder, compute_ssl_policies_insert_task,
    compute_ssl_policies_list_builder, compute_ssl_policies_list_task,
    compute_ssl_policies_list_available_features_builder, compute_ssl_policies_list_available_features_task,
    compute_ssl_policies_patch_builder, compute_ssl_policies_patch_task,
    compute_storage_pool_types_aggregated_list_builder, compute_storage_pool_types_aggregated_list_task,
    compute_storage_pool_types_get_builder, compute_storage_pool_types_get_task,
    compute_storage_pool_types_list_builder, compute_storage_pool_types_list_task,
    compute_storage_pools_aggregated_list_builder, compute_storage_pools_aggregated_list_task,
    compute_storage_pools_delete_builder, compute_storage_pools_delete_task,
    compute_storage_pools_get_builder, compute_storage_pools_get_task,
    compute_storage_pools_get_iam_policy_builder, compute_storage_pools_get_iam_policy_task,
    compute_storage_pools_insert_builder, compute_storage_pools_insert_task,
    compute_storage_pools_list_builder, compute_storage_pools_list_task,
    compute_storage_pools_list_disks_builder, compute_storage_pools_list_disks_task,
    compute_storage_pools_set_iam_policy_builder, compute_storage_pools_set_iam_policy_task,
    compute_storage_pools_test_iam_permissions_builder, compute_storage_pools_test_iam_permissions_task,
    compute_storage_pools_update_builder, compute_storage_pools_update_task,
    compute_subnetworks_aggregated_list_builder, compute_subnetworks_aggregated_list_task,
    compute_subnetworks_delete_builder, compute_subnetworks_delete_task,
    compute_subnetworks_expand_ip_cidr_range_builder, compute_subnetworks_expand_ip_cidr_range_task,
    compute_subnetworks_get_builder, compute_subnetworks_get_task,
    compute_subnetworks_get_iam_policy_builder, compute_subnetworks_get_iam_policy_task,
    compute_subnetworks_insert_builder, compute_subnetworks_insert_task,
    compute_subnetworks_list_builder, compute_subnetworks_list_task,
    compute_subnetworks_list_usable_builder, compute_subnetworks_list_usable_task,
    compute_subnetworks_patch_builder, compute_subnetworks_patch_task,
    compute_subnetworks_set_iam_policy_builder, compute_subnetworks_set_iam_policy_task,
    compute_subnetworks_set_private_ip_google_access_builder, compute_subnetworks_set_private_ip_google_access_task,
    compute_subnetworks_test_iam_permissions_builder, compute_subnetworks_test_iam_permissions_task,
    compute_target_grpc_proxies_delete_builder, compute_target_grpc_proxies_delete_task,
    compute_target_grpc_proxies_get_builder, compute_target_grpc_proxies_get_task,
    compute_target_grpc_proxies_insert_builder, compute_target_grpc_proxies_insert_task,
    compute_target_grpc_proxies_list_builder, compute_target_grpc_proxies_list_task,
    compute_target_grpc_proxies_patch_builder, compute_target_grpc_proxies_patch_task,
    compute_target_http_proxies_aggregated_list_builder, compute_target_http_proxies_aggregated_list_task,
    compute_target_http_proxies_delete_builder, compute_target_http_proxies_delete_task,
    compute_target_http_proxies_get_builder, compute_target_http_proxies_get_task,
    compute_target_http_proxies_insert_builder, compute_target_http_proxies_insert_task,
    compute_target_http_proxies_list_builder, compute_target_http_proxies_list_task,
    compute_target_http_proxies_patch_builder, compute_target_http_proxies_patch_task,
    compute_target_http_proxies_set_url_map_builder, compute_target_http_proxies_set_url_map_task,
    compute_target_https_proxies_aggregated_list_builder, compute_target_https_proxies_aggregated_list_task,
    compute_target_https_proxies_delete_builder, compute_target_https_proxies_delete_task,
    compute_target_https_proxies_get_builder, compute_target_https_proxies_get_task,
    compute_target_https_proxies_insert_builder, compute_target_https_proxies_insert_task,
    compute_target_https_proxies_list_builder, compute_target_https_proxies_list_task,
    compute_target_https_proxies_patch_builder, compute_target_https_proxies_patch_task,
    compute_target_https_proxies_set_certificate_map_builder, compute_target_https_proxies_set_certificate_map_task,
    compute_target_https_proxies_set_quic_override_builder, compute_target_https_proxies_set_quic_override_task,
    compute_target_https_proxies_set_ssl_certificates_builder, compute_target_https_proxies_set_ssl_certificates_task,
    compute_target_https_proxies_set_ssl_policy_builder, compute_target_https_proxies_set_ssl_policy_task,
    compute_target_https_proxies_set_url_map_builder, compute_target_https_proxies_set_url_map_task,
    compute_target_instances_aggregated_list_builder, compute_target_instances_aggregated_list_task,
    compute_target_instances_delete_builder, compute_target_instances_delete_task,
    compute_target_instances_get_builder, compute_target_instances_get_task,
    compute_target_instances_insert_builder, compute_target_instances_insert_task,
    compute_target_instances_list_builder, compute_target_instances_list_task,
    compute_target_instances_set_security_policy_builder, compute_target_instances_set_security_policy_task,
    compute_target_instances_test_iam_permissions_builder, compute_target_instances_test_iam_permissions_task,
    compute_target_pools_add_health_check_builder, compute_target_pools_add_health_check_task,
    compute_target_pools_add_instance_builder, compute_target_pools_add_instance_task,
    compute_target_pools_aggregated_list_builder, compute_target_pools_aggregated_list_task,
    compute_target_pools_delete_builder, compute_target_pools_delete_task,
    compute_target_pools_get_builder, compute_target_pools_get_task,
    compute_target_pools_get_health_builder, compute_target_pools_get_health_task,
    compute_target_pools_insert_builder, compute_target_pools_insert_task,
    compute_target_pools_list_builder, compute_target_pools_list_task,
    compute_target_pools_remove_health_check_builder, compute_target_pools_remove_health_check_task,
    compute_target_pools_remove_instance_builder, compute_target_pools_remove_instance_task,
    compute_target_pools_set_backup_builder, compute_target_pools_set_backup_task,
    compute_target_pools_set_security_policy_builder, compute_target_pools_set_security_policy_task,
    compute_target_pools_test_iam_permissions_builder, compute_target_pools_test_iam_permissions_task,
    compute_target_ssl_proxies_delete_builder, compute_target_ssl_proxies_delete_task,
    compute_target_ssl_proxies_get_builder, compute_target_ssl_proxies_get_task,
    compute_target_ssl_proxies_insert_builder, compute_target_ssl_proxies_insert_task,
    compute_target_ssl_proxies_list_builder, compute_target_ssl_proxies_list_task,
    compute_target_ssl_proxies_set_backend_service_builder, compute_target_ssl_proxies_set_backend_service_task,
    compute_target_ssl_proxies_set_certificate_map_builder, compute_target_ssl_proxies_set_certificate_map_task,
    compute_target_ssl_proxies_set_proxy_header_builder, compute_target_ssl_proxies_set_proxy_header_task,
    compute_target_ssl_proxies_set_ssl_certificates_builder, compute_target_ssl_proxies_set_ssl_certificates_task,
    compute_target_ssl_proxies_set_ssl_policy_builder, compute_target_ssl_proxies_set_ssl_policy_task,
    compute_target_ssl_proxies_test_iam_permissions_builder, compute_target_ssl_proxies_test_iam_permissions_task,
    compute_target_tcp_proxies_aggregated_list_builder, compute_target_tcp_proxies_aggregated_list_task,
    compute_target_tcp_proxies_delete_builder, compute_target_tcp_proxies_delete_task,
    compute_target_tcp_proxies_get_builder, compute_target_tcp_proxies_get_task,
    compute_target_tcp_proxies_insert_builder, compute_target_tcp_proxies_insert_task,
    compute_target_tcp_proxies_list_builder, compute_target_tcp_proxies_list_task,
    compute_target_tcp_proxies_set_backend_service_builder, compute_target_tcp_proxies_set_backend_service_task,
    compute_target_tcp_proxies_set_proxy_header_builder, compute_target_tcp_proxies_set_proxy_header_task,
    compute_target_tcp_proxies_test_iam_permissions_builder, compute_target_tcp_proxies_test_iam_permissions_task,
    compute_target_vpn_gateways_aggregated_list_builder, compute_target_vpn_gateways_aggregated_list_task,
    compute_target_vpn_gateways_delete_builder, compute_target_vpn_gateways_delete_task,
    compute_target_vpn_gateways_get_builder, compute_target_vpn_gateways_get_task,
    compute_target_vpn_gateways_insert_builder, compute_target_vpn_gateways_insert_task,
    compute_target_vpn_gateways_list_builder, compute_target_vpn_gateways_list_task,
    compute_target_vpn_gateways_set_labels_builder, compute_target_vpn_gateways_set_labels_task,
    compute_url_maps_aggregated_list_builder, compute_url_maps_aggregated_list_task,
    compute_url_maps_delete_builder, compute_url_maps_delete_task,
    compute_url_maps_get_builder, compute_url_maps_get_task,
    compute_url_maps_insert_builder, compute_url_maps_insert_task,
    compute_url_maps_invalidate_cache_builder, compute_url_maps_invalidate_cache_task,
    compute_url_maps_list_builder, compute_url_maps_list_task,
    compute_url_maps_patch_builder, compute_url_maps_patch_task,
    compute_url_maps_test_iam_permissions_builder, compute_url_maps_test_iam_permissions_task,
    compute_url_maps_update_builder, compute_url_maps_update_task,
    compute_url_maps_validate_builder, compute_url_maps_validate_task,
    compute_vpn_gateways_aggregated_list_builder, compute_vpn_gateways_aggregated_list_task,
    compute_vpn_gateways_delete_builder, compute_vpn_gateways_delete_task,
    compute_vpn_gateways_get_builder, compute_vpn_gateways_get_task,
    compute_vpn_gateways_get_status_builder, compute_vpn_gateways_get_status_task,
    compute_vpn_gateways_insert_builder, compute_vpn_gateways_insert_task,
    compute_vpn_gateways_list_builder, compute_vpn_gateways_list_task,
    compute_vpn_gateways_set_labels_builder, compute_vpn_gateways_set_labels_task,
    compute_vpn_gateways_test_iam_permissions_builder, compute_vpn_gateways_test_iam_permissions_task,
    compute_vpn_tunnels_aggregated_list_builder, compute_vpn_tunnels_aggregated_list_task,
    compute_vpn_tunnels_delete_builder, compute_vpn_tunnels_delete_task,
    compute_vpn_tunnels_get_builder, compute_vpn_tunnels_get_task,
    compute_vpn_tunnels_insert_builder, compute_vpn_tunnels_insert_task,
    compute_vpn_tunnels_list_builder, compute_vpn_tunnels_list_task,
    compute_vpn_tunnels_set_labels_builder, compute_vpn_tunnels_set_labels_task,
    compute_wire_groups_delete_builder, compute_wire_groups_delete_task,
    compute_wire_groups_get_builder, compute_wire_groups_get_task,
    compute_wire_groups_insert_builder, compute_wire_groups_insert_task,
    compute_wire_groups_list_builder, compute_wire_groups_list_task,
    compute_wire_groups_patch_builder, compute_wire_groups_patch_task,
    compute_zone_operations_delete_builder, compute_zone_operations_delete_task,
    compute_zone_operations_get_builder, compute_zone_operations_get_task,
    compute_zone_operations_list_builder, compute_zone_operations_list_task,
    compute_zone_operations_wait_builder, compute_zone_operations_wait_task,
    compute_zone_vm_extension_policies_delete_builder, compute_zone_vm_extension_policies_delete_task,
    compute_zone_vm_extension_policies_get_builder, compute_zone_vm_extension_policies_get_task,
    compute_zone_vm_extension_policies_insert_builder, compute_zone_vm_extension_policies_insert_task,
    compute_zone_vm_extension_policies_list_builder, compute_zone_vm_extension_policies_list_task,
    compute_zone_vm_extension_policies_update_builder, compute_zone_vm_extension_policies_update_task,
    compute_zones_get_builder, compute_zones_get_task,
    compute_zones_list_builder, compute_zones_list_task,
};
use crate::providers::gcp::clients::types::{ApiError, ApiPending};
use crate::providers::gcp::clients::compute::AcceleratorType;
use crate::providers::gcp::clients::compute::AcceleratorTypeAggregatedList;
use crate::providers::gcp::clients::compute::AcceleratorTypeList;
use crate::providers::gcp::clients::compute::Address;
use crate::providers::gcp::clients::compute::AddressAggregatedList;
use crate::providers::gcp::clients::compute::AddressList;
use crate::providers::gcp::clients::compute::Autoscaler;
use crate::providers::gcp::clients::compute::AutoscalerAggregatedList;
use crate::providers::gcp::clients::compute::AutoscalerList;
use crate::providers::gcp::clients::compute::BackendBucket;
use crate::providers::gcp::clients::compute::BackendBucketAggregatedList;
use crate::providers::gcp::clients::compute::BackendBucketList;
use crate::providers::gcp::clients::compute::BackendBucketListUsable;
use crate::providers::gcp::clients::compute::BackendService;
use crate::providers::gcp::clients::compute::BackendServiceAggregatedList;
use crate::providers::gcp::clients::compute::BackendServiceGroupHealth;
use crate::providers::gcp::clients::compute::BackendServiceList;
use crate::providers::gcp::clients::compute::BackendServiceListUsable;
use crate::providers::gcp::clients::compute::CalendarModeAdviceResponse;
use crate::providers::gcp::clients::compute::Commitment;
use crate::providers::gcp::clients::compute::CommitmentAggregatedList;
use crate::providers::gcp::clients::compute::CommitmentList;
use crate::providers::gcp::clients::compute::CompositeHealthCheck;
use crate::providers::gcp::clients::compute::CompositeHealthCheckAggregatedList;
use crate::providers::gcp::clients::compute::CompositeHealthCheckHealth;
use crate::providers::gcp::clients::compute::CompositeHealthCheckList;
use crate::providers::gcp::clients::compute::CrossSiteNetwork;
use crate::providers::gcp::clients::compute::CrossSiteNetworkList;
use crate::providers::gcp::clients::compute::Disk;
use crate::providers::gcp::clients::compute::DiskAggregatedList;
use crate::providers::gcp::clients::compute::DiskList;
use crate::providers::gcp::clients::compute::DiskType;
use crate::providers::gcp::clients::compute::DiskTypeAggregatedList;
use crate::providers::gcp::clients::compute::DiskTypeList;
use crate::providers::gcp::clients::compute::ExchangedPeeringRoutesList;
use crate::providers::gcp::clients::compute::ExternalVpnGateway;
use crate::providers::gcp::clients::compute::ExternalVpnGatewayList;
use crate::providers::gcp::clients::compute::Firewall;
use crate::providers::gcp::clients::compute::FirewallList;
use crate::providers::gcp::clients::compute::FirewallPoliciesListAssociationsResponse;
use crate::providers::gcp::clients::compute::FirewallPolicy;
use crate::providers::gcp::clients::compute::FirewallPolicyAssociation;
use crate::providers::gcp::clients::compute::FirewallPolicyList;
use crate::providers::gcp::clients::compute::FirewallPolicyRule;
use crate::providers::gcp::clients::compute::ForwardingRule;
use crate::providers::gcp::clients::compute::ForwardingRuleAggregatedList;
use crate::providers::gcp::clients::compute::ForwardingRuleList;
use crate::providers::gcp::clients::compute::FutureReservation;
use crate::providers::gcp::clients::compute::FutureReservationsAggregatedListResponse;
use crate::providers::gcp::clients::compute::FutureReservationsListResponse;
use crate::providers::gcp::clients::compute::GuestAttributes;
use crate::providers::gcp::clients::compute::HealthAggregationPolicy;
use crate::providers::gcp::clients::compute::HealthAggregationPolicyAggregatedList;
use crate::providers::gcp::clients::compute::HealthAggregationPolicyList;
use crate::providers::gcp::clients::compute::HealthCheck;
use crate::providers::gcp::clients::compute::HealthCheckList;
use crate::providers::gcp::clients::compute::HealthCheckService;
use crate::providers::gcp::clients::compute::HealthCheckServiceAggregatedList;
use crate::providers::gcp::clients::compute::HealthCheckServicesList;
use crate::providers::gcp::clients::compute::HealthChecksAggregatedList;
use crate::providers::gcp::clients::compute::HealthSource;
use crate::providers::gcp::clients::compute::HealthSourceAggregatedList;
use crate::providers::gcp::clients::compute::HealthSourceHealth;
use crate::providers::gcp::clients::compute::HealthSourceList;
use crate::providers::gcp::clients::compute::HttpHealthCheck;
use crate::providers::gcp::clients::compute::HttpHealthCheckList;
use crate::providers::gcp::clients::compute::HttpsHealthCheck;
use crate::providers::gcp::clients::compute::HttpsHealthCheckList;
use crate::providers::gcp::clients::compute::Image;
use crate::providers::gcp::clients::compute::ImageFamilyView;
use crate::providers::gcp::clients::compute::ImageList;
use crate::providers::gcp::clients::compute::Instance;
use crate::providers::gcp::clients::compute::InstanceAggregatedList;
use crate::providers::gcp::clients::compute::InstanceGroup;
use crate::providers::gcp::clients::compute::InstanceGroupAggregatedList;
use crate::providers::gcp::clients::compute::InstanceGroupList;
use crate::providers::gcp::clients::compute::InstanceGroupManager;
use crate::providers::gcp::clients::compute::InstanceGroupManagerAggregatedList;
use crate::providers::gcp::clients::compute::InstanceGroupManagerList;
use crate::providers::gcp::clients::compute::InstanceGroupManagerResizeRequest;
use crate::providers::gcp::clients::compute::InstanceGroupManagerResizeRequestsListResponse;
use crate::providers::gcp::clients::compute::InstanceGroupManagersListErrorsResponse;
use crate::providers::gcp::clients::compute::InstanceGroupManagersListManagedInstancesResponse;
use crate::providers::gcp::clients::compute::InstanceGroupManagersListPerInstanceConfigsResp;
use crate::providers::gcp::clients::compute::InstanceGroupsListInstances;
use crate::providers::gcp::clients::compute::InstanceList;
use crate::providers::gcp::clients::compute::InstanceListReferrers;
use crate::providers::gcp::clients::compute::InstanceSettings;
use crate::providers::gcp::clients::compute::InstanceTemplate;
use crate::providers::gcp::clients::compute::InstanceTemplateAggregatedList;
use crate::providers::gcp::clients::compute::InstanceTemplateList;
use crate::providers::gcp::clients::compute::InstancesGetEffectiveFirewallsResponse;
use crate::providers::gcp::clients::compute::InstantSnapshot;
use crate::providers::gcp::clients::compute::InstantSnapshotAggregatedList;
use crate::providers::gcp::clients::compute::InstantSnapshotGroup;
use crate::providers::gcp::clients::compute::InstantSnapshotList;
use crate::providers::gcp::clients::compute::Interconnect;
use crate::providers::gcp::clients::compute::InterconnectAttachment;
use crate::providers::gcp::clients::compute::InterconnectAttachmentAggregatedList;
use crate::providers::gcp::clients::compute::InterconnectAttachmentGroup;
use crate::providers::gcp::clients::compute::InterconnectAttachmentGroupsGetOperationalStatusResponse;
use crate::providers::gcp::clients::compute::InterconnectAttachmentGroupsListResponse;
use crate::providers::gcp::clients::compute::InterconnectAttachmentList;
use crate::providers::gcp::clients::compute::InterconnectGroup;
use crate::providers::gcp::clients::compute::InterconnectGroupsGetOperationalStatusResponse;
use crate::providers::gcp::clients::compute::InterconnectGroupsListResponse;
use crate::providers::gcp::clients::compute::InterconnectList;
use crate::providers::gcp::clients::compute::InterconnectLocation;
use crate::providers::gcp::clients::compute::InterconnectLocationList;
use crate::providers::gcp::clients::compute::InterconnectRemoteLocation;
use crate::providers::gcp::clients::compute::InterconnectRemoteLocationList;
use crate::providers::gcp::clients::compute::InterconnectsGetDiagnosticsResponse;
use crate::providers::gcp::clients::compute::InterconnectsGetMacsecConfigResponse;
use crate::providers::gcp::clients::compute::License;
use crate::providers::gcp::clients::compute::LicenseCode;
use crate::providers::gcp::clients::compute::LicensesListResponse;
use crate::providers::gcp::clients::compute::ListInstantSnapshotGroups;
use crate::providers::gcp::clients::compute::MachineImage;
use crate::providers::gcp::clients::compute::MachineImageList;
use crate::providers::gcp::clients::compute::MachineType;
use crate::providers::gcp::clients::compute::MachineTypeAggregatedList;
use crate::providers::gcp::clients::compute::MachineTypeList;
use crate::providers::gcp::clients::compute::NatIpInfoResponse;
use crate::providers::gcp::clients::compute::Network;
use crate::providers::gcp::clients::compute::NetworkAttachment;
use crate::providers::gcp::clients::compute::NetworkAttachmentAggregatedList;
use crate::providers::gcp::clients::compute::NetworkAttachmentList;
use crate::providers::gcp::clients::compute::NetworkEdgeSecurityService;
use crate::providers::gcp::clients::compute::NetworkEdgeSecurityServiceAggregatedList;
use crate::providers::gcp::clients::compute::NetworkEndpointGroup;
use crate::providers::gcp::clients::compute::NetworkEndpointGroupAggregatedList;
use crate::providers::gcp::clients::compute::NetworkEndpointGroupList;
use crate::providers::gcp::clients::compute::NetworkEndpointGroupsListNetworkEndpoints;
use crate::providers::gcp::clients::compute::NetworkFirewallPolicyAggregatedList;
use crate::providers::gcp::clients::compute::NetworkList;
use crate::providers::gcp::clients::compute::NetworkProfile;
use crate::providers::gcp::clients::compute::NetworkProfilesListResponse;
use crate::providers::gcp::clients::compute::NetworksGetEffectiveFirewallsResponse;
use crate::providers::gcp::clients::compute::NodeGroup;
use crate::providers::gcp::clients::compute::NodeGroupAggregatedList;
use crate::providers::gcp::clients::compute::NodeGroupList;
use crate::providers::gcp::clients::compute::NodeGroupsListNodes;
use crate::providers::gcp::clients::compute::NodeTemplate;
use crate::providers::gcp::clients::compute::NodeTemplateAggregatedList;
use crate::providers::gcp::clients::compute::NodeTemplateList;
use crate::providers::gcp::clients::compute::NodeType;
use crate::providers::gcp::clients::compute::NodeTypeAggregatedList;
use crate::providers::gcp::clients::compute::NodeTypeList;
use crate::providers::gcp::clients::compute::NotificationEndpoint;
use crate::providers::gcp::clients::compute::NotificationEndpointAggregatedList;
use crate::providers::gcp::clients::compute::NotificationEndpointList;
use crate::providers::gcp::clients::compute::Operation;
use crate::providers::gcp::clients::compute::OperationAggregatedList;
use crate::providers::gcp::clients::compute::OperationList;
use crate::providers::gcp::clients::compute::OrganizationSecurityPoliciesListAssociationsResponse;
use crate::providers::gcp::clients::compute::PacketMirroring;
use crate::providers::gcp::clients::compute::PacketMirroringAggregatedList;
use crate::providers::gcp::clients::compute::PacketMirroringList;
use crate::providers::gcp::clients::compute::Policy;
use crate::providers::gcp::clients::compute::PreviewFeature;
use crate::providers::gcp::clients::compute::PreviewFeatureList;
use crate::providers::gcp::clients::compute::Project;
use crate::providers::gcp::clients::compute::ProjectsGetXpnResources;
use crate::providers::gcp::clients::compute::PublicAdvertisedPrefix;
use crate::providers::gcp::clients::compute::PublicAdvertisedPrefixList;
use crate::providers::gcp::clients::compute::PublicDelegatedPrefix;
use crate::providers::gcp::clients::compute::PublicDelegatedPrefixAggregatedList;
use crate::providers::gcp::clients::compute::PublicDelegatedPrefixList;
use crate::providers::gcp::clients::compute::Region;
use crate::providers::gcp::clients::compute::RegionAutoscalerList;
use crate::providers::gcp::clients::compute::RegionDiskTypeList;
use crate::providers::gcp::clients::compute::RegionInstanceGroupList;
use crate::providers::gcp::clients::compute::RegionInstanceGroupManagerList;
use crate::providers::gcp::clients::compute::RegionInstanceGroupManagerResizeRequestsListResponse;
use crate::providers::gcp::clients::compute::RegionInstanceGroupManagersListErrorsResponse;
use crate::providers::gcp::clients::compute::RegionInstanceGroupManagersListInstanceConfigsResp;
use crate::providers::gcp::clients::compute::RegionInstanceGroupManagersListInstancesResponse;
use crate::providers::gcp::clients::compute::RegionInstanceGroupsListInstances;
use crate::providers::gcp::clients::compute::RegionList;
use crate::providers::gcp::clients::compute::RegionNetworkFirewallPoliciesGetEffectiveFirewallsResponse;
use crate::providers::gcp::clients::compute::Reservation;
use crate::providers::gcp::clients::compute::ReservationAggregatedList;
use crate::providers::gcp::clients::compute::ReservationBlocksGetResponse;
use crate::providers::gcp::clients::compute::ReservationBlocksListResponse;
use crate::providers::gcp::clients::compute::ReservationList;
use crate::providers::gcp::clients::compute::ReservationSlotsGetResponse;
use crate::providers::gcp::clients::compute::ReservationSlotsListResponse;
use crate::providers::gcp::clients::compute::ReservationSubBlocksGetResponse;
use crate::providers::gcp::clients::compute::ReservationSubBlocksListResponse;
use crate::providers::gcp::clients::compute::ResourcePolicy;
use crate::providers::gcp::clients::compute::ResourcePolicyAggregatedList;
use crate::providers::gcp::clients::compute::ResourcePolicyList;
use crate::providers::gcp::clients::compute::Route;
use crate::providers::gcp::clients::compute::RouteList;
use crate::providers::gcp::clients::compute::Router;
use crate::providers::gcp::clients::compute::RouterAggregatedList;
use crate::providers::gcp::clients::compute::RouterList;
use crate::providers::gcp::clients::compute::RouterStatusResponse;
use crate::providers::gcp::clients::compute::RoutersGetRoutePolicyResponse;
use crate::providers::gcp::clients::compute::RoutersListBgpRoutes;
use crate::providers::gcp::clients::compute::RoutersListRoutePolicies;
use crate::providers::gcp::clients::compute::RoutersPreviewResponse;
use crate::providers::gcp::clients::compute::Screenshot;
use crate::providers::gcp::clients::compute::SecurityPoliciesAggregatedList;
use crate::providers::gcp::clients::compute::SecurityPoliciesListPreconfiguredExpressionSetsResponse;
use crate::providers::gcp::clients::compute::SecurityPolicy;
use crate::providers::gcp::clients::compute::SecurityPolicyAssociation;
use crate::providers::gcp::clients::compute::SecurityPolicyList;
use crate::providers::gcp::clients::compute::SecurityPolicyRule;
use crate::providers::gcp::clients::compute::SerialPortOutput;
use crate::providers::gcp::clients::compute::ServiceAttachment;
use crate::providers::gcp::clients::compute::ServiceAttachmentAggregatedList;
use crate::providers::gcp::clients::compute::ServiceAttachmentList;
use crate::providers::gcp::clients::compute::ShieldedInstanceIdentity;
use crate::providers::gcp::clients::compute::Snapshot;
use crate::providers::gcp::clients::compute::SnapshotList;
use crate::providers::gcp::clients::compute::SnapshotSettings;
use crate::providers::gcp::clients::compute::SslCertificate;
use crate::providers::gcp::clients::compute::SslCertificateAggregatedList;
use crate::providers::gcp::clients::compute::SslCertificateList;
use crate::providers::gcp::clients::compute::SslPoliciesAggregatedList;
use crate::providers::gcp::clients::compute::SslPoliciesList;
use crate::providers::gcp::clients::compute::SslPoliciesListAvailableFeaturesResponse;
use crate::providers::gcp::clients::compute::SslPolicy;
use crate::providers::gcp::clients::compute::StoragePool;
use crate::providers::gcp::clients::compute::StoragePoolAggregatedList;
use crate::providers::gcp::clients::compute::StoragePoolList;
use crate::providers::gcp::clients::compute::StoragePoolListDisks;
use crate::providers::gcp::clients::compute::StoragePoolType;
use crate::providers::gcp::clients::compute::StoragePoolTypeAggregatedList;
use crate::providers::gcp::clients::compute::StoragePoolTypeList;
use crate::providers::gcp::clients::compute::Subnetwork;
use crate::providers::gcp::clients::compute::SubnetworkAggregatedList;
use crate::providers::gcp::clients::compute::SubnetworkList;
use crate::providers::gcp::clients::compute::TargetGrpcProxy;
use crate::providers::gcp::clients::compute::TargetGrpcProxyList;
use crate::providers::gcp::clients::compute::TargetHttpProxy;
use crate::providers::gcp::clients::compute::TargetHttpProxyAggregatedList;
use crate::providers::gcp::clients::compute::TargetHttpProxyList;
use crate::providers::gcp::clients::compute::TargetHttpsProxy;
use crate::providers::gcp::clients::compute::TargetHttpsProxyAggregatedList;
use crate::providers::gcp::clients::compute::TargetHttpsProxyList;
use crate::providers::gcp::clients::compute::TargetInstance;
use crate::providers::gcp::clients::compute::TargetInstanceAggregatedList;
use crate::providers::gcp::clients::compute::TargetInstanceList;
use crate::providers::gcp::clients::compute::TargetPool;
use crate::providers::gcp::clients::compute::TargetPoolAggregatedList;
use crate::providers::gcp::clients::compute::TargetPoolInstanceHealth;
use crate::providers::gcp::clients::compute::TargetPoolList;
use crate::providers::gcp::clients::compute::TargetSslProxy;
use crate::providers::gcp::clients::compute::TargetSslProxyList;
use crate::providers::gcp::clients::compute::TargetTcpProxy;
use crate::providers::gcp::clients::compute::TargetTcpProxyAggregatedList;
use crate::providers::gcp::clients::compute::TargetTcpProxyList;
use crate::providers::gcp::clients::compute::TargetVpnGateway;
use crate::providers::gcp::clients::compute::TargetVpnGatewayAggregatedList;
use crate::providers::gcp::clients::compute::TargetVpnGatewayList;
use crate::providers::gcp::clients::compute::TestPermissionsResponse;
use crate::providers::gcp::clients::compute::UrlMap;
use crate::providers::gcp::clients::compute::UrlMapList;
use crate::providers::gcp::clients::compute::UrlMapsAggregatedList;
use crate::providers::gcp::clients::compute::UrlMapsValidateResponse;
use crate::providers::gcp::clients::compute::UsableSubnetworksAggregatedList;
use crate::providers::gcp::clients::compute::VmEndpointNatMappingsList;
use crate::providers::gcp::clients::compute::VmExtensionPolicy;
use crate::providers::gcp::clients::compute::VmExtensionPolicyList;
use crate::providers::gcp::clients::compute::VpnGateway;
use crate::providers::gcp::clients::compute::VpnGatewayAggregatedList;
use crate::providers::gcp::clients::compute::VpnGatewayList;
use crate::providers::gcp::clients::compute::VpnGatewaysGetStatusResponse;
use crate::providers::gcp::clients::compute::VpnTunnel;
use crate::providers::gcp::clients::compute::VpnTunnelAggregatedList;
use crate::providers::gcp::clients::compute::VpnTunnelList;
use crate::providers::gcp::clients::compute::WireGroup;
use crate::providers::gcp::clients::compute::WireGroupList;
use crate::providers::gcp::clients::compute::XpnHostList;
use crate::providers::gcp::clients::compute::Zone;
use crate::providers::gcp::clients::compute::ZoneList;
use crate::providers::gcp::clients::compute::ComputeAcceleratorTypesAggregatedListArgs;
use crate::providers::gcp::clients::compute::ComputeAcceleratorTypesGetArgs;
use crate::providers::gcp::clients::compute::ComputeAcceleratorTypesListArgs;
use crate::providers::gcp::clients::compute::ComputeAddressesAggregatedListArgs;
use crate::providers::gcp::clients::compute::ComputeAddressesDeleteArgs;
use crate::providers::gcp::clients::compute::ComputeAddressesGetArgs;
use crate::providers::gcp::clients::compute::ComputeAddressesInsertArgs;
use crate::providers::gcp::clients::compute::ComputeAddressesListArgs;
use crate::providers::gcp::clients::compute::ComputeAddressesMoveArgs;
use crate::providers::gcp::clients::compute::ComputeAddressesSetLabelsArgs;
use crate::providers::gcp::clients::compute::ComputeAddressesTestIamPermissionsArgs;
use crate::providers::gcp::clients::compute::ComputeAdviceCalendarModeArgs;
use crate::providers::gcp::clients::compute::ComputeAutoscalersAggregatedListArgs;
use crate::providers::gcp::clients::compute::ComputeAutoscalersDeleteArgs;
use crate::providers::gcp::clients::compute::ComputeAutoscalersGetArgs;
use crate::providers::gcp::clients::compute::ComputeAutoscalersInsertArgs;
use crate::providers::gcp::clients::compute::ComputeAutoscalersListArgs;
use crate::providers::gcp::clients::compute::ComputeAutoscalersPatchArgs;
use crate::providers::gcp::clients::compute::ComputeAutoscalersTestIamPermissionsArgs;
use crate::providers::gcp::clients::compute::ComputeAutoscalersUpdateArgs;
use crate::providers::gcp::clients::compute::ComputeBackendBucketsAddSignedUrlKeyArgs;
use crate::providers::gcp::clients::compute::ComputeBackendBucketsAggregatedListArgs;
use crate::providers::gcp::clients::compute::ComputeBackendBucketsDeleteArgs;
use crate::providers::gcp::clients::compute::ComputeBackendBucketsDeleteSignedUrlKeyArgs;
use crate::providers::gcp::clients::compute::ComputeBackendBucketsGetArgs;
use crate::providers::gcp::clients::compute::ComputeBackendBucketsGetIamPolicyArgs;
use crate::providers::gcp::clients::compute::ComputeBackendBucketsInsertArgs;
use crate::providers::gcp::clients::compute::ComputeBackendBucketsListArgs;
use crate::providers::gcp::clients::compute::ComputeBackendBucketsListUsableArgs;
use crate::providers::gcp::clients::compute::ComputeBackendBucketsPatchArgs;
use crate::providers::gcp::clients::compute::ComputeBackendBucketsSetEdgeSecurityPolicyArgs;
use crate::providers::gcp::clients::compute::ComputeBackendBucketsSetIamPolicyArgs;
use crate::providers::gcp::clients::compute::ComputeBackendBucketsTestIamPermissionsArgs;
use crate::providers::gcp::clients::compute::ComputeBackendBucketsUpdateArgs;
use crate::providers::gcp::clients::compute::ComputeBackendServicesAddSignedUrlKeyArgs;
use crate::providers::gcp::clients::compute::ComputeBackendServicesAggregatedListArgs;
use crate::providers::gcp::clients::compute::ComputeBackendServicesDeleteArgs;
use crate::providers::gcp::clients::compute::ComputeBackendServicesDeleteSignedUrlKeyArgs;
use crate::providers::gcp::clients::compute::ComputeBackendServicesGetArgs;
use crate::providers::gcp::clients::compute::ComputeBackendServicesGetEffectiveSecurityPoliciesArgs;
use crate::providers::gcp::clients::compute::ComputeBackendServicesGetHealthArgs;
use crate::providers::gcp::clients::compute::ComputeBackendServicesGetIamPolicyArgs;
use crate::providers::gcp::clients::compute::ComputeBackendServicesInsertArgs;
use crate::providers::gcp::clients::compute::ComputeBackendServicesListArgs;
use crate::providers::gcp::clients::compute::ComputeBackendServicesListUsableArgs;
use crate::providers::gcp::clients::compute::ComputeBackendServicesPatchArgs;
use crate::providers::gcp::clients::compute::ComputeBackendServicesSetEdgeSecurityPolicyArgs;
use crate::providers::gcp::clients::compute::ComputeBackendServicesSetIamPolicyArgs;
use crate::providers::gcp::clients::compute::ComputeBackendServicesSetSecurityPolicyArgs;
use crate::providers::gcp::clients::compute::ComputeBackendServicesTestIamPermissionsArgs;
use crate::providers::gcp::clients::compute::ComputeBackendServicesUpdateArgs;
use crate::providers::gcp::clients::compute::ComputeCrossSiteNetworksDeleteArgs;
use crate::providers::gcp::clients::compute::ComputeCrossSiteNetworksGetArgs;
use crate::providers::gcp::clients::compute::ComputeCrossSiteNetworksInsertArgs;
use crate::providers::gcp::clients::compute::ComputeCrossSiteNetworksListArgs;
use crate::providers::gcp::clients::compute::ComputeCrossSiteNetworksPatchArgs;
use crate::providers::gcp::clients::compute::ComputeDiskTypesAggregatedListArgs;
use crate::providers::gcp::clients::compute::ComputeDiskTypesGetArgs;
use crate::providers::gcp::clients::compute::ComputeDiskTypesListArgs;
use crate::providers::gcp::clients::compute::ComputeDisksAddResourcePoliciesArgs;
use crate::providers::gcp::clients::compute::ComputeDisksAggregatedListArgs;
use crate::providers::gcp::clients::compute::ComputeDisksBulkInsertArgs;
use crate::providers::gcp::clients::compute::ComputeDisksBulkSetLabelsArgs;
use crate::providers::gcp::clients::compute::ComputeDisksCreateSnapshotArgs;
use crate::providers::gcp::clients::compute::ComputeDisksDeleteArgs;
use crate::providers::gcp::clients::compute::ComputeDisksGetArgs;
use crate::providers::gcp::clients::compute::ComputeDisksGetIamPolicyArgs;
use crate::providers::gcp::clients::compute::ComputeDisksInsertArgs;
use crate::providers::gcp::clients::compute::ComputeDisksListArgs;
use crate::providers::gcp::clients::compute::ComputeDisksRemoveResourcePoliciesArgs;
use crate::providers::gcp::clients::compute::ComputeDisksResizeArgs;
use crate::providers::gcp::clients::compute::ComputeDisksSetIamPolicyArgs;
use crate::providers::gcp::clients::compute::ComputeDisksSetLabelsArgs;
use crate::providers::gcp::clients::compute::ComputeDisksStartAsyncReplicationArgs;
use crate::providers::gcp::clients::compute::ComputeDisksStopAsyncReplicationArgs;
use crate::providers::gcp::clients::compute::ComputeDisksStopGroupAsyncReplicationArgs;
use crate::providers::gcp::clients::compute::ComputeDisksTestIamPermissionsArgs;
use crate::providers::gcp::clients::compute::ComputeDisksUpdateArgs;
use crate::providers::gcp::clients::compute::ComputeDisksUpdateKmsKeyArgs;
use crate::providers::gcp::clients::compute::ComputeExternalVpnGatewaysDeleteArgs;
use crate::providers::gcp::clients::compute::ComputeExternalVpnGatewaysGetArgs;
use crate::providers::gcp::clients::compute::ComputeExternalVpnGatewaysInsertArgs;
use crate::providers::gcp::clients::compute::ComputeExternalVpnGatewaysListArgs;
use crate::providers::gcp::clients::compute::ComputeExternalVpnGatewaysSetLabelsArgs;
use crate::providers::gcp::clients::compute::ComputeExternalVpnGatewaysTestIamPermissionsArgs;
use crate::providers::gcp::clients::compute::ComputeFirewallPoliciesAddAssociationArgs;
use crate::providers::gcp::clients::compute::ComputeFirewallPoliciesAddRuleArgs;
use crate::providers::gcp::clients::compute::ComputeFirewallPoliciesCloneRulesArgs;
use crate::providers::gcp::clients::compute::ComputeFirewallPoliciesDeleteArgs;
use crate::providers::gcp::clients::compute::ComputeFirewallPoliciesGetArgs;
use crate::providers::gcp::clients::compute::ComputeFirewallPoliciesGetAssociationArgs;
use crate::providers::gcp::clients::compute::ComputeFirewallPoliciesGetIamPolicyArgs;
use crate::providers::gcp::clients::compute::ComputeFirewallPoliciesGetRuleArgs;
use crate::providers::gcp::clients::compute::ComputeFirewallPoliciesInsertArgs;
use crate::providers::gcp::clients::compute::ComputeFirewallPoliciesListArgs;
use crate::providers::gcp::clients::compute::ComputeFirewallPoliciesListAssociationsArgs;
use crate::providers::gcp::clients::compute::ComputeFirewallPoliciesMoveArgs;
use crate::providers::gcp::clients::compute::ComputeFirewallPoliciesPatchArgs;
use crate::providers::gcp::clients::compute::ComputeFirewallPoliciesPatchRuleArgs;
use crate::providers::gcp::clients::compute::ComputeFirewallPoliciesRemoveAssociationArgs;
use crate::providers::gcp::clients::compute::ComputeFirewallPoliciesRemoveRuleArgs;
use crate::providers::gcp::clients::compute::ComputeFirewallPoliciesSetIamPolicyArgs;
use crate::providers::gcp::clients::compute::ComputeFirewallPoliciesTestIamPermissionsArgs;
use crate::providers::gcp::clients::compute::ComputeFirewallsDeleteArgs;
use crate::providers::gcp::clients::compute::ComputeFirewallsGetArgs;
use crate::providers::gcp::clients::compute::ComputeFirewallsInsertArgs;
use crate::providers::gcp::clients::compute::ComputeFirewallsListArgs;
use crate::providers::gcp::clients::compute::ComputeFirewallsPatchArgs;
use crate::providers::gcp::clients::compute::ComputeFirewallsTestIamPermissionsArgs;
use crate::providers::gcp::clients::compute::ComputeFirewallsUpdateArgs;
use crate::providers::gcp::clients::compute::ComputeForwardingRulesAggregatedListArgs;
use crate::providers::gcp::clients::compute::ComputeForwardingRulesDeleteArgs;
use crate::providers::gcp::clients::compute::ComputeForwardingRulesGetArgs;
use crate::providers::gcp::clients::compute::ComputeForwardingRulesInsertArgs;
use crate::providers::gcp::clients::compute::ComputeForwardingRulesListArgs;
use crate::providers::gcp::clients::compute::ComputeForwardingRulesPatchArgs;
use crate::providers::gcp::clients::compute::ComputeForwardingRulesSetLabelsArgs;
use crate::providers::gcp::clients::compute::ComputeForwardingRulesSetTargetArgs;
use crate::providers::gcp::clients::compute::ComputeFutureReservationsAggregatedListArgs;
use crate::providers::gcp::clients::compute::ComputeFutureReservationsCancelArgs;
use crate::providers::gcp::clients::compute::ComputeFutureReservationsDeleteArgs;
use crate::providers::gcp::clients::compute::ComputeFutureReservationsGetArgs;
use crate::providers::gcp::clients::compute::ComputeFutureReservationsInsertArgs;
use crate::providers::gcp::clients::compute::ComputeFutureReservationsListArgs;
use crate::providers::gcp::clients::compute::ComputeFutureReservationsUpdateArgs;
use crate::providers::gcp::clients::compute::ComputeGlobalAddressesDeleteArgs;
use crate::providers::gcp::clients::compute::ComputeGlobalAddressesGetArgs;
use crate::providers::gcp::clients::compute::ComputeGlobalAddressesInsertArgs;
use crate::providers::gcp::clients::compute::ComputeGlobalAddressesListArgs;
use crate::providers::gcp::clients::compute::ComputeGlobalAddressesMoveArgs;
use crate::providers::gcp::clients::compute::ComputeGlobalAddressesSetLabelsArgs;
use crate::providers::gcp::clients::compute::ComputeGlobalAddressesTestIamPermissionsArgs;
use crate::providers::gcp::clients::compute::ComputeGlobalForwardingRulesDeleteArgs;
use crate::providers::gcp::clients::compute::ComputeGlobalForwardingRulesGetArgs;
use crate::providers::gcp::clients::compute::ComputeGlobalForwardingRulesInsertArgs;
use crate::providers::gcp::clients::compute::ComputeGlobalForwardingRulesListArgs;
use crate::providers::gcp::clients::compute::ComputeGlobalForwardingRulesPatchArgs;
use crate::providers::gcp::clients::compute::ComputeGlobalForwardingRulesSetLabelsArgs;
use crate::providers::gcp::clients::compute::ComputeGlobalForwardingRulesSetTargetArgs;
use crate::providers::gcp::clients::compute::ComputeGlobalNetworkEndpointGroupsAttachNetworkEndpointsArgs;
use crate::providers::gcp::clients::compute::ComputeGlobalNetworkEndpointGroupsDeleteArgs;
use crate::providers::gcp::clients::compute::ComputeGlobalNetworkEndpointGroupsDetachNetworkEndpointsArgs;
use crate::providers::gcp::clients::compute::ComputeGlobalNetworkEndpointGroupsGetArgs;
use crate::providers::gcp::clients::compute::ComputeGlobalNetworkEndpointGroupsInsertArgs;
use crate::providers::gcp::clients::compute::ComputeGlobalNetworkEndpointGroupsListArgs;
use crate::providers::gcp::clients::compute::ComputeGlobalNetworkEndpointGroupsListNetworkEndpointsArgs;
use crate::providers::gcp::clients::compute::ComputeGlobalOperationsAggregatedListArgs;
use crate::providers::gcp::clients::compute::ComputeGlobalOperationsDeleteArgs;
use crate::providers::gcp::clients::compute::ComputeGlobalOperationsGetArgs;
use crate::providers::gcp::clients::compute::ComputeGlobalOperationsListArgs;
use crate::providers::gcp::clients::compute::ComputeGlobalOperationsWaitArgs;
use crate::providers::gcp::clients::compute::ComputeGlobalOrganizationOperationsDeleteArgs;
use crate::providers::gcp::clients::compute::ComputeGlobalOrganizationOperationsGetArgs;
use crate::providers::gcp::clients::compute::ComputeGlobalOrganizationOperationsListArgs;
use crate::providers::gcp::clients::compute::ComputeGlobalPublicDelegatedPrefixesDeleteArgs;
use crate::providers::gcp::clients::compute::ComputeGlobalPublicDelegatedPrefixesGetArgs;
use crate::providers::gcp::clients::compute::ComputeGlobalPublicDelegatedPrefixesInsertArgs;
use crate::providers::gcp::clients::compute::ComputeGlobalPublicDelegatedPrefixesListArgs;
use crate::providers::gcp::clients::compute::ComputeGlobalPublicDelegatedPrefixesPatchArgs;
use crate::providers::gcp::clients::compute::ComputeHealthChecksAggregatedListArgs;
use crate::providers::gcp::clients::compute::ComputeHealthChecksDeleteArgs;
use crate::providers::gcp::clients::compute::ComputeHealthChecksGetArgs;
use crate::providers::gcp::clients::compute::ComputeHealthChecksInsertArgs;
use crate::providers::gcp::clients::compute::ComputeHealthChecksListArgs;
use crate::providers::gcp::clients::compute::ComputeHealthChecksPatchArgs;
use crate::providers::gcp::clients::compute::ComputeHealthChecksTestIamPermissionsArgs;
use crate::providers::gcp::clients::compute::ComputeHealthChecksUpdateArgs;
use crate::providers::gcp::clients::compute::ComputeHttpHealthChecksDeleteArgs;
use crate::providers::gcp::clients::compute::ComputeHttpHealthChecksGetArgs;
use crate::providers::gcp::clients::compute::ComputeHttpHealthChecksInsertArgs;
use crate::providers::gcp::clients::compute::ComputeHttpHealthChecksListArgs;
use crate::providers::gcp::clients::compute::ComputeHttpHealthChecksPatchArgs;
use crate::providers::gcp::clients::compute::ComputeHttpHealthChecksTestIamPermissionsArgs;
use crate::providers::gcp::clients::compute::ComputeHttpHealthChecksUpdateArgs;
use crate::providers::gcp::clients::compute::ComputeHttpsHealthChecksDeleteArgs;
use crate::providers::gcp::clients::compute::ComputeHttpsHealthChecksGetArgs;
use crate::providers::gcp::clients::compute::ComputeHttpsHealthChecksInsertArgs;
use crate::providers::gcp::clients::compute::ComputeHttpsHealthChecksListArgs;
use crate::providers::gcp::clients::compute::ComputeHttpsHealthChecksPatchArgs;
use crate::providers::gcp::clients::compute::ComputeHttpsHealthChecksTestIamPermissionsArgs;
use crate::providers::gcp::clients::compute::ComputeHttpsHealthChecksUpdateArgs;
use crate::providers::gcp::clients::compute::ComputeImageFamilyViewsGetArgs;
use crate::providers::gcp::clients::compute::ComputeImagesDeleteArgs;
use crate::providers::gcp::clients::compute::ComputeImagesDeprecateArgs;
use crate::providers::gcp::clients::compute::ComputeImagesGetArgs;
use crate::providers::gcp::clients::compute::ComputeImagesGetFromFamilyArgs;
use crate::providers::gcp::clients::compute::ComputeImagesGetIamPolicyArgs;
use crate::providers::gcp::clients::compute::ComputeImagesInsertArgs;
use crate::providers::gcp::clients::compute::ComputeImagesListArgs;
use crate::providers::gcp::clients::compute::ComputeImagesPatchArgs;
use crate::providers::gcp::clients::compute::ComputeImagesSetIamPolicyArgs;
use crate::providers::gcp::clients::compute::ComputeImagesSetLabelsArgs;
use crate::providers::gcp::clients::compute::ComputeImagesTestIamPermissionsArgs;
use crate::providers::gcp::clients::compute::ComputeInstanceGroupManagerResizeRequestsCancelArgs;
use crate::providers::gcp::clients::compute::ComputeInstanceGroupManagerResizeRequestsDeleteArgs;
use crate::providers::gcp::clients::compute::ComputeInstanceGroupManagerResizeRequestsGetArgs;
use crate::providers::gcp::clients::compute::ComputeInstanceGroupManagerResizeRequestsInsertArgs;
use crate::providers::gcp::clients::compute::ComputeInstanceGroupManagerResizeRequestsListArgs;
use crate::providers::gcp::clients::compute::ComputeInstanceGroupManagersAbandonInstancesArgs;
use crate::providers::gcp::clients::compute::ComputeInstanceGroupManagersAggregatedListArgs;
use crate::providers::gcp::clients::compute::ComputeInstanceGroupManagersApplyUpdatesToInstancesArgs;
use crate::providers::gcp::clients::compute::ComputeInstanceGroupManagersCreateInstancesArgs;
use crate::providers::gcp::clients::compute::ComputeInstanceGroupManagersDeleteArgs;
use crate::providers::gcp::clients::compute::ComputeInstanceGroupManagersDeleteInstancesArgs;
use crate::providers::gcp::clients::compute::ComputeInstanceGroupManagersDeletePerInstanceConfigsArgs;
use crate::providers::gcp::clients::compute::ComputeInstanceGroupManagersGetArgs;
use crate::providers::gcp::clients::compute::ComputeInstanceGroupManagersInsertArgs;
use crate::providers::gcp::clients::compute::ComputeInstanceGroupManagersListArgs;
use crate::providers::gcp::clients::compute::ComputeInstanceGroupManagersListErrorsArgs;
use crate::providers::gcp::clients::compute::ComputeInstanceGroupManagersListManagedInstancesArgs;
use crate::providers::gcp::clients::compute::ComputeInstanceGroupManagersListPerInstanceConfigsArgs;
use crate::providers::gcp::clients::compute::ComputeInstanceGroupManagersPatchArgs;
use crate::providers::gcp::clients::compute::ComputeInstanceGroupManagersPatchPerInstanceConfigsArgs;
use crate::providers::gcp::clients::compute::ComputeInstanceGroupManagersRecreateInstancesArgs;
use crate::providers::gcp::clients::compute::ComputeInstanceGroupManagersResizeArgs;
use crate::providers::gcp::clients::compute::ComputeInstanceGroupManagersResumeInstancesArgs;
use crate::providers::gcp::clients::compute::ComputeInstanceGroupManagersSetInstanceTemplateArgs;
use crate::providers::gcp::clients::compute::ComputeInstanceGroupManagersSetTargetPoolsArgs;
use crate::providers::gcp::clients::compute::ComputeInstanceGroupManagersStartInstancesArgs;
use crate::providers::gcp::clients::compute::ComputeInstanceGroupManagersStopInstancesArgs;
use crate::providers::gcp::clients::compute::ComputeInstanceGroupManagersSuspendInstancesArgs;
use crate::providers::gcp::clients::compute::ComputeInstanceGroupManagersUpdatePerInstanceConfigsArgs;
use crate::providers::gcp::clients::compute::ComputeInstanceGroupsAddInstancesArgs;
use crate::providers::gcp::clients::compute::ComputeInstanceGroupsAggregatedListArgs;
use crate::providers::gcp::clients::compute::ComputeInstanceGroupsDeleteArgs;
use crate::providers::gcp::clients::compute::ComputeInstanceGroupsGetArgs;
use crate::providers::gcp::clients::compute::ComputeInstanceGroupsInsertArgs;
use crate::providers::gcp::clients::compute::ComputeInstanceGroupsListArgs;
use crate::providers::gcp::clients::compute::ComputeInstanceGroupsListInstancesArgs;
use crate::providers::gcp::clients::compute::ComputeInstanceGroupsRemoveInstancesArgs;
use crate::providers::gcp::clients::compute::ComputeInstanceGroupsSetNamedPortsArgs;
use crate::providers::gcp::clients::compute::ComputeInstanceGroupsTestIamPermissionsArgs;
use crate::providers::gcp::clients::compute::ComputeInstanceSettingsGetArgs;
use crate::providers::gcp::clients::compute::ComputeInstanceSettingsPatchArgs;
use crate::providers::gcp::clients::compute::ComputeInstanceTemplatesAggregatedListArgs;
use crate::providers::gcp::clients::compute::ComputeInstanceTemplatesDeleteArgs;
use crate::providers::gcp::clients::compute::ComputeInstanceTemplatesGetArgs;
use crate::providers::gcp::clients::compute::ComputeInstanceTemplatesGetIamPolicyArgs;
use crate::providers::gcp::clients::compute::ComputeInstanceTemplatesInsertArgs;
use crate::providers::gcp::clients::compute::ComputeInstanceTemplatesListArgs;
use crate::providers::gcp::clients::compute::ComputeInstanceTemplatesSetIamPolicyArgs;
use crate::providers::gcp::clients::compute::ComputeInstanceTemplatesTestIamPermissionsArgs;
use crate::providers::gcp::clients::compute::ComputeInstancesAddAccessConfigArgs;
use crate::providers::gcp::clients::compute::ComputeInstancesAddNetworkInterfaceArgs;
use crate::providers::gcp::clients::compute::ComputeInstancesAddResourcePoliciesArgs;
use crate::providers::gcp::clients::compute::ComputeInstancesAggregatedListArgs;
use crate::providers::gcp::clients::compute::ComputeInstancesAttachDiskArgs;
use crate::providers::gcp::clients::compute::ComputeInstancesBulkInsertArgs;
use crate::providers::gcp::clients::compute::ComputeInstancesDeleteAccessConfigArgs;
use crate::providers::gcp::clients::compute::ComputeInstancesDeleteArgs;
use crate::providers::gcp::clients::compute::ComputeInstancesDeleteNetworkInterfaceArgs;
use crate::providers::gcp::clients::compute::ComputeInstancesDetachDiskArgs;
use crate::providers::gcp::clients::compute::ComputeInstancesGetArgs;
use crate::providers::gcp::clients::compute::ComputeInstancesGetEffectiveFirewallsArgs;
use crate::providers::gcp::clients::compute::ComputeInstancesGetGuestAttributesArgs;
use crate::providers::gcp::clients::compute::ComputeInstancesGetIamPolicyArgs;
use crate::providers::gcp::clients::compute::ComputeInstancesGetScreenshotArgs;
use crate::providers::gcp::clients::compute::ComputeInstancesGetSerialPortOutputArgs;
use crate::providers::gcp::clients::compute::ComputeInstancesGetShieldedInstanceIdentityArgs;
use crate::providers::gcp::clients::compute::ComputeInstancesInsertArgs;
use crate::providers::gcp::clients::compute::ComputeInstancesListArgs;
use crate::providers::gcp::clients::compute::ComputeInstancesListReferrersArgs;
use crate::providers::gcp::clients::compute::ComputeInstancesPerformMaintenanceArgs;
use crate::providers::gcp::clients::compute::ComputeInstancesRemoveResourcePoliciesArgs;
use crate::providers::gcp::clients::compute::ComputeInstancesReportHostAsFaultyArgs;
use crate::providers::gcp::clients::compute::ComputeInstancesResetArgs;
use crate::providers::gcp::clients::compute::ComputeInstancesResumeArgs;
use crate::providers::gcp::clients::compute::ComputeInstancesSendDiagnosticInterruptArgs;
use crate::providers::gcp::clients::compute::ComputeInstancesSetDeletionProtectionArgs;
use crate::providers::gcp::clients::compute::ComputeInstancesSetDiskAutoDeleteArgs;
use crate::providers::gcp::clients::compute::ComputeInstancesSetIamPolicyArgs;
use crate::providers::gcp::clients::compute::ComputeInstancesSetLabelsArgs;
use crate::providers::gcp::clients::compute::ComputeInstancesSetMachineResourcesArgs;
use crate::providers::gcp::clients::compute::ComputeInstancesSetMachineTypeArgs;
use crate::providers::gcp::clients::compute::ComputeInstancesSetMetadataArgs;
use crate::providers::gcp::clients::compute::ComputeInstancesSetMinCpuPlatformArgs;
use crate::providers::gcp::clients::compute::ComputeInstancesSetNameArgs;
use crate::providers::gcp::clients::compute::ComputeInstancesSetSchedulingArgs;
use crate::providers::gcp::clients::compute::ComputeInstancesSetSecurityPolicyArgs;
use crate::providers::gcp::clients::compute::ComputeInstancesSetServiceAccountArgs;
use crate::providers::gcp::clients::compute::ComputeInstancesSetShieldedInstanceIntegrityPolicyArgs;
use crate::providers::gcp::clients::compute::ComputeInstancesSetTagsArgs;
use crate::providers::gcp::clients::compute::ComputeInstancesSimulateMaintenanceEventArgs;
use crate::providers::gcp::clients::compute::ComputeInstancesStartArgs;
use crate::providers::gcp::clients::compute::ComputeInstancesStartWithEncryptionKeyArgs;
use crate::providers::gcp::clients::compute::ComputeInstancesStopArgs;
use crate::providers::gcp::clients::compute::ComputeInstancesSuspendArgs;
use crate::providers::gcp::clients::compute::ComputeInstancesTestIamPermissionsArgs;
use crate::providers::gcp::clients::compute::ComputeInstancesUpdateAccessConfigArgs;
use crate::providers::gcp::clients::compute::ComputeInstancesUpdateArgs;
use crate::providers::gcp::clients::compute::ComputeInstancesUpdateDisplayDeviceArgs;
use crate::providers::gcp::clients::compute::ComputeInstancesUpdateNetworkInterfaceArgs;
use crate::providers::gcp::clients::compute::ComputeInstancesUpdateShieldedInstanceConfigArgs;
use crate::providers::gcp::clients::compute::ComputeInstantSnapshotGroupsDeleteArgs;
use crate::providers::gcp::clients::compute::ComputeInstantSnapshotGroupsGetArgs;
use crate::providers::gcp::clients::compute::ComputeInstantSnapshotGroupsGetIamPolicyArgs;
use crate::providers::gcp::clients::compute::ComputeInstantSnapshotGroupsInsertArgs;
use crate::providers::gcp::clients::compute::ComputeInstantSnapshotGroupsListArgs;
use crate::providers::gcp::clients::compute::ComputeInstantSnapshotGroupsSetIamPolicyArgs;
use crate::providers::gcp::clients::compute::ComputeInstantSnapshotGroupsTestIamPermissionsArgs;
use crate::providers::gcp::clients::compute::ComputeInstantSnapshotsAggregatedListArgs;
use crate::providers::gcp::clients::compute::ComputeInstantSnapshotsDeleteArgs;
use crate::providers::gcp::clients::compute::ComputeInstantSnapshotsGetArgs;
use crate::providers::gcp::clients::compute::ComputeInstantSnapshotsGetIamPolicyArgs;
use crate::providers::gcp::clients::compute::ComputeInstantSnapshotsInsertArgs;
use crate::providers::gcp::clients::compute::ComputeInstantSnapshotsListArgs;
use crate::providers::gcp::clients::compute::ComputeInstantSnapshotsSetIamPolicyArgs;
use crate::providers::gcp::clients::compute::ComputeInstantSnapshotsSetLabelsArgs;
use crate::providers::gcp::clients::compute::ComputeInstantSnapshotsTestIamPermissionsArgs;
use crate::providers::gcp::clients::compute::ComputeInterconnectAttachmentGroupsDeleteArgs;
use crate::providers::gcp::clients::compute::ComputeInterconnectAttachmentGroupsGetArgs;
use crate::providers::gcp::clients::compute::ComputeInterconnectAttachmentGroupsGetIamPolicyArgs;
use crate::providers::gcp::clients::compute::ComputeInterconnectAttachmentGroupsGetOperationalStatusArgs;
use crate::providers::gcp::clients::compute::ComputeInterconnectAttachmentGroupsInsertArgs;
use crate::providers::gcp::clients::compute::ComputeInterconnectAttachmentGroupsListArgs;
use crate::providers::gcp::clients::compute::ComputeInterconnectAttachmentGroupsPatchArgs;
use crate::providers::gcp::clients::compute::ComputeInterconnectAttachmentGroupsSetIamPolicyArgs;
use crate::providers::gcp::clients::compute::ComputeInterconnectAttachmentGroupsTestIamPermissionsArgs;
use crate::providers::gcp::clients::compute::ComputeInterconnectAttachmentsAggregatedListArgs;
use crate::providers::gcp::clients::compute::ComputeInterconnectAttachmentsDeleteArgs;
use crate::providers::gcp::clients::compute::ComputeInterconnectAttachmentsGetArgs;
use crate::providers::gcp::clients::compute::ComputeInterconnectAttachmentsInsertArgs;
use crate::providers::gcp::clients::compute::ComputeInterconnectAttachmentsListArgs;
use crate::providers::gcp::clients::compute::ComputeInterconnectAttachmentsPatchArgs;
use crate::providers::gcp::clients::compute::ComputeInterconnectAttachmentsSetLabelsArgs;
use crate::providers::gcp::clients::compute::ComputeInterconnectGroupsCreateMembersArgs;
use crate::providers::gcp::clients::compute::ComputeInterconnectGroupsDeleteArgs;
use crate::providers::gcp::clients::compute::ComputeInterconnectGroupsGetArgs;
use crate::providers::gcp::clients::compute::ComputeInterconnectGroupsGetIamPolicyArgs;
use crate::providers::gcp::clients::compute::ComputeInterconnectGroupsGetOperationalStatusArgs;
use crate::providers::gcp::clients::compute::ComputeInterconnectGroupsInsertArgs;
use crate::providers::gcp::clients::compute::ComputeInterconnectGroupsListArgs;
use crate::providers::gcp::clients::compute::ComputeInterconnectGroupsPatchArgs;
use crate::providers::gcp::clients::compute::ComputeInterconnectGroupsSetIamPolicyArgs;
use crate::providers::gcp::clients::compute::ComputeInterconnectGroupsTestIamPermissionsArgs;
use crate::providers::gcp::clients::compute::ComputeInterconnectLocationsGetArgs;
use crate::providers::gcp::clients::compute::ComputeInterconnectLocationsListArgs;
use crate::providers::gcp::clients::compute::ComputeInterconnectRemoteLocationsGetArgs;
use crate::providers::gcp::clients::compute::ComputeInterconnectRemoteLocationsListArgs;
use crate::providers::gcp::clients::compute::ComputeInterconnectsDeleteArgs;
use crate::providers::gcp::clients::compute::ComputeInterconnectsGetArgs;
use crate::providers::gcp::clients::compute::ComputeInterconnectsGetDiagnosticsArgs;
use crate::providers::gcp::clients::compute::ComputeInterconnectsGetMacsecConfigArgs;
use crate::providers::gcp::clients::compute::ComputeInterconnectsInsertArgs;
use crate::providers::gcp::clients::compute::ComputeInterconnectsListArgs;
use crate::providers::gcp::clients::compute::ComputeInterconnectsPatchArgs;
use crate::providers::gcp::clients::compute::ComputeInterconnectsSetLabelsArgs;
use crate::providers::gcp::clients::compute::ComputeLicenseCodesGetArgs;
use crate::providers::gcp::clients::compute::ComputeLicenseCodesTestIamPermissionsArgs;
use crate::providers::gcp::clients::compute::ComputeLicensesDeleteArgs;
use crate::providers::gcp::clients::compute::ComputeLicensesGetArgs;
use crate::providers::gcp::clients::compute::ComputeLicensesGetIamPolicyArgs;
use crate::providers::gcp::clients::compute::ComputeLicensesInsertArgs;
use crate::providers::gcp::clients::compute::ComputeLicensesListArgs;
use crate::providers::gcp::clients::compute::ComputeLicensesSetIamPolicyArgs;
use crate::providers::gcp::clients::compute::ComputeLicensesTestIamPermissionsArgs;
use crate::providers::gcp::clients::compute::ComputeLicensesUpdateArgs;
use crate::providers::gcp::clients::compute::ComputeMachineImagesDeleteArgs;
use crate::providers::gcp::clients::compute::ComputeMachineImagesGetArgs;
use crate::providers::gcp::clients::compute::ComputeMachineImagesGetIamPolicyArgs;
use crate::providers::gcp::clients::compute::ComputeMachineImagesInsertArgs;
use crate::providers::gcp::clients::compute::ComputeMachineImagesListArgs;
use crate::providers::gcp::clients::compute::ComputeMachineImagesSetIamPolicyArgs;
use crate::providers::gcp::clients::compute::ComputeMachineImagesSetLabelsArgs;
use crate::providers::gcp::clients::compute::ComputeMachineImagesTestIamPermissionsArgs;
use crate::providers::gcp::clients::compute::ComputeMachineTypesAggregatedListArgs;
use crate::providers::gcp::clients::compute::ComputeMachineTypesGetArgs;
use crate::providers::gcp::clients::compute::ComputeMachineTypesListArgs;
use crate::providers::gcp::clients::compute::ComputeNetworkAttachmentsAggregatedListArgs;
use crate::providers::gcp::clients::compute::ComputeNetworkAttachmentsDeleteArgs;
use crate::providers::gcp::clients::compute::ComputeNetworkAttachmentsGetArgs;
use crate::providers::gcp::clients::compute::ComputeNetworkAttachmentsGetIamPolicyArgs;
use crate::providers::gcp::clients::compute::ComputeNetworkAttachmentsInsertArgs;
use crate::providers::gcp::clients::compute::ComputeNetworkAttachmentsListArgs;
use crate::providers::gcp::clients::compute::ComputeNetworkAttachmentsPatchArgs;
use crate::providers::gcp::clients::compute::ComputeNetworkAttachmentsSetIamPolicyArgs;
use crate::providers::gcp::clients::compute::ComputeNetworkAttachmentsTestIamPermissionsArgs;
use crate::providers::gcp::clients::compute::ComputeNetworkEdgeSecurityServicesAggregatedListArgs;
use crate::providers::gcp::clients::compute::ComputeNetworkEdgeSecurityServicesDeleteArgs;
use crate::providers::gcp::clients::compute::ComputeNetworkEdgeSecurityServicesGetArgs;
use crate::providers::gcp::clients::compute::ComputeNetworkEdgeSecurityServicesInsertArgs;
use crate::providers::gcp::clients::compute::ComputeNetworkEdgeSecurityServicesPatchArgs;
use crate::providers::gcp::clients::compute::ComputeNetworkEndpointGroupsAggregatedListArgs;
use crate::providers::gcp::clients::compute::ComputeNetworkEndpointGroupsAttachNetworkEndpointsArgs;
use crate::providers::gcp::clients::compute::ComputeNetworkEndpointGroupsDeleteArgs;
use crate::providers::gcp::clients::compute::ComputeNetworkEndpointGroupsDetachNetworkEndpointsArgs;
use crate::providers::gcp::clients::compute::ComputeNetworkEndpointGroupsGetArgs;
use crate::providers::gcp::clients::compute::ComputeNetworkEndpointGroupsInsertArgs;
use crate::providers::gcp::clients::compute::ComputeNetworkEndpointGroupsListArgs;
use crate::providers::gcp::clients::compute::ComputeNetworkEndpointGroupsListNetworkEndpointsArgs;
use crate::providers::gcp::clients::compute::ComputeNetworkEndpointGroupsTestIamPermissionsArgs;
use crate::providers::gcp::clients::compute::ComputeNetworkFirewallPoliciesAddAssociationArgs;
use crate::providers::gcp::clients::compute::ComputeNetworkFirewallPoliciesAddPacketMirroringRuleArgs;
use crate::providers::gcp::clients::compute::ComputeNetworkFirewallPoliciesAddRuleArgs;
use crate::providers::gcp::clients::compute::ComputeNetworkFirewallPoliciesAggregatedListArgs;
use crate::providers::gcp::clients::compute::ComputeNetworkFirewallPoliciesCloneRulesArgs;
use crate::providers::gcp::clients::compute::ComputeNetworkFirewallPoliciesDeleteArgs;
use crate::providers::gcp::clients::compute::ComputeNetworkFirewallPoliciesGetArgs;
use crate::providers::gcp::clients::compute::ComputeNetworkFirewallPoliciesGetAssociationArgs;
use crate::providers::gcp::clients::compute::ComputeNetworkFirewallPoliciesGetIamPolicyArgs;
use crate::providers::gcp::clients::compute::ComputeNetworkFirewallPoliciesGetPacketMirroringRuleArgs;
use crate::providers::gcp::clients::compute::ComputeNetworkFirewallPoliciesGetRuleArgs;
use crate::providers::gcp::clients::compute::ComputeNetworkFirewallPoliciesInsertArgs;
use crate::providers::gcp::clients::compute::ComputeNetworkFirewallPoliciesListArgs;
use crate::providers::gcp::clients::compute::ComputeNetworkFirewallPoliciesPatchArgs;
use crate::providers::gcp::clients::compute::ComputeNetworkFirewallPoliciesPatchPacketMirroringRuleArgs;
use crate::providers::gcp::clients::compute::ComputeNetworkFirewallPoliciesPatchRuleArgs;
use crate::providers::gcp::clients::compute::ComputeNetworkFirewallPoliciesRemoveAssociationArgs;
use crate::providers::gcp::clients::compute::ComputeNetworkFirewallPoliciesRemovePacketMirroringRuleArgs;
use crate::providers::gcp::clients::compute::ComputeNetworkFirewallPoliciesRemoveRuleArgs;
use crate::providers::gcp::clients::compute::ComputeNetworkFirewallPoliciesSetIamPolicyArgs;
use crate::providers::gcp::clients::compute::ComputeNetworkFirewallPoliciesTestIamPermissionsArgs;
use crate::providers::gcp::clients::compute::ComputeNetworkProfilesGetArgs;
use crate::providers::gcp::clients::compute::ComputeNetworkProfilesListArgs;
use crate::providers::gcp::clients::compute::ComputeNetworksAddPeeringArgs;
use crate::providers::gcp::clients::compute::ComputeNetworksDeleteArgs;
use crate::providers::gcp::clients::compute::ComputeNetworksGetArgs;
use crate::providers::gcp::clients::compute::ComputeNetworksGetEffectiveFirewallsArgs;
use crate::providers::gcp::clients::compute::ComputeNetworksInsertArgs;
use crate::providers::gcp::clients::compute::ComputeNetworksListArgs;
use crate::providers::gcp::clients::compute::ComputeNetworksListPeeringRoutesArgs;
use crate::providers::gcp::clients::compute::ComputeNetworksPatchArgs;
use crate::providers::gcp::clients::compute::ComputeNetworksRemovePeeringArgs;
use crate::providers::gcp::clients::compute::ComputeNetworksRequestRemovePeeringArgs;
use crate::providers::gcp::clients::compute::ComputeNetworksSwitchToCustomModeArgs;
use crate::providers::gcp::clients::compute::ComputeNetworksUpdatePeeringArgs;
use crate::providers::gcp::clients::compute::ComputeNodeGroupsAddNodesArgs;
use crate::providers::gcp::clients::compute::ComputeNodeGroupsAggregatedListArgs;
use crate::providers::gcp::clients::compute::ComputeNodeGroupsDeleteArgs;
use crate::providers::gcp::clients::compute::ComputeNodeGroupsDeleteNodesArgs;
use crate::providers::gcp::clients::compute::ComputeNodeGroupsGetArgs;
use crate::providers::gcp::clients::compute::ComputeNodeGroupsGetIamPolicyArgs;
use crate::providers::gcp::clients::compute::ComputeNodeGroupsInsertArgs;
use crate::providers::gcp::clients::compute::ComputeNodeGroupsListArgs;
use crate::providers::gcp::clients::compute::ComputeNodeGroupsListNodesArgs;
use crate::providers::gcp::clients::compute::ComputeNodeGroupsPatchArgs;
use crate::providers::gcp::clients::compute::ComputeNodeGroupsPerformMaintenanceArgs;
use crate::providers::gcp::clients::compute::ComputeNodeGroupsSetIamPolicyArgs;
use crate::providers::gcp::clients::compute::ComputeNodeGroupsSetNodeTemplateArgs;
use crate::providers::gcp::clients::compute::ComputeNodeGroupsSimulateMaintenanceEventArgs;
use crate::providers::gcp::clients::compute::ComputeNodeGroupsTestIamPermissionsArgs;
use crate::providers::gcp::clients::compute::ComputeNodeTemplatesAggregatedListArgs;
use crate::providers::gcp::clients::compute::ComputeNodeTemplatesDeleteArgs;
use crate::providers::gcp::clients::compute::ComputeNodeTemplatesGetArgs;
use crate::providers::gcp::clients::compute::ComputeNodeTemplatesGetIamPolicyArgs;
use crate::providers::gcp::clients::compute::ComputeNodeTemplatesInsertArgs;
use crate::providers::gcp::clients::compute::ComputeNodeTemplatesListArgs;
use crate::providers::gcp::clients::compute::ComputeNodeTemplatesSetIamPolicyArgs;
use crate::providers::gcp::clients::compute::ComputeNodeTemplatesTestIamPermissionsArgs;
use crate::providers::gcp::clients::compute::ComputeNodeTypesAggregatedListArgs;
use crate::providers::gcp::clients::compute::ComputeNodeTypesGetArgs;
use crate::providers::gcp::clients::compute::ComputeNodeTypesListArgs;
use crate::providers::gcp::clients::compute::ComputeOrganizationSecurityPoliciesAddAssociationArgs;
use crate::providers::gcp::clients::compute::ComputeOrganizationSecurityPoliciesAddRuleArgs;
use crate::providers::gcp::clients::compute::ComputeOrganizationSecurityPoliciesCopyRulesArgs;
use crate::providers::gcp::clients::compute::ComputeOrganizationSecurityPoliciesDeleteArgs;
use crate::providers::gcp::clients::compute::ComputeOrganizationSecurityPoliciesGetArgs;
use crate::providers::gcp::clients::compute::ComputeOrganizationSecurityPoliciesGetAssociationArgs;
use crate::providers::gcp::clients::compute::ComputeOrganizationSecurityPoliciesGetRuleArgs;
use crate::providers::gcp::clients::compute::ComputeOrganizationSecurityPoliciesInsertArgs;
use crate::providers::gcp::clients::compute::ComputeOrganizationSecurityPoliciesListArgs;
use crate::providers::gcp::clients::compute::ComputeOrganizationSecurityPoliciesListAssociationsArgs;
use crate::providers::gcp::clients::compute::ComputeOrganizationSecurityPoliciesListPreconfiguredExpressionSetsArgs;
use crate::providers::gcp::clients::compute::ComputeOrganizationSecurityPoliciesMoveArgs;
use crate::providers::gcp::clients::compute::ComputeOrganizationSecurityPoliciesPatchArgs;
use crate::providers::gcp::clients::compute::ComputeOrganizationSecurityPoliciesPatchRuleArgs;
use crate::providers::gcp::clients::compute::ComputeOrganizationSecurityPoliciesRemoveAssociationArgs;
use crate::providers::gcp::clients::compute::ComputeOrganizationSecurityPoliciesRemoveRuleArgs;
use crate::providers::gcp::clients::compute::ComputePacketMirroringsAggregatedListArgs;
use crate::providers::gcp::clients::compute::ComputePacketMirroringsDeleteArgs;
use crate::providers::gcp::clients::compute::ComputePacketMirroringsGetArgs;
use crate::providers::gcp::clients::compute::ComputePacketMirroringsInsertArgs;
use crate::providers::gcp::clients::compute::ComputePacketMirroringsListArgs;
use crate::providers::gcp::clients::compute::ComputePacketMirroringsPatchArgs;
use crate::providers::gcp::clients::compute::ComputePacketMirroringsTestIamPermissionsArgs;
use crate::providers::gcp::clients::compute::ComputePreviewFeaturesGetArgs;
use crate::providers::gcp::clients::compute::ComputePreviewFeaturesListArgs;
use crate::providers::gcp::clients::compute::ComputePreviewFeaturesUpdateArgs;
use crate::providers::gcp::clients::compute::ComputeProjectsDisableXpnHostArgs;
use crate::providers::gcp::clients::compute::ComputeProjectsDisableXpnResourceArgs;
use crate::providers::gcp::clients::compute::ComputeProjectsEnableXpnHostArgs;
use crate::providers::gcp::clients::compute::ComputeProjectsEnableXpnResourceArgs;
use crate::providers::gcp::clients::compute::ComputeProjectsGetArgs;
use crate::providers::gcp::clients::compute::ComputeProjectsGetXpnHostArgs;
use crate::providers::gcp::clients::compute::ComputeProjectsGetXpnResourcesArgs;
use crate::providers::gcp::clients::compute::ComputeProjectsListXpnHostsArgs;
use crate::providers::gcp::clients::compute::ComputeProjectsMoveDiskArgs;
use crate::providers::gcp::clients::compute::ComputeProjectsMoveInstanceArgs;
use crate::providers::gcp::clients::compute::ComputeProjectsSetCloudArmorTierArgs;
use crate::providers::gcp::clients::compute::ComputeProjectsSetCommonInstanceMetadataArgs;
use crate::providers::gcp::clients::compute::ComputeProjectsSetDefaultNetworkTierArgs;
use crate::providers::gcp::clients::compute::ComputeProjectsSetUsageExportBucketArgs;
use crate::providers::gcp::clients::compute::ComputePublicAdvertisedPrefixesAnnounceArgs;
use crate::providers::gcp::clients::compute::ComputePublicAdvertisedPrefixesDeleteArgs;
use crate::providers::gcp::clients::compute::ComputePublicAdvertisedPrefixesGetArgs;
use crate::providers::gcp::clients::compute::ComputePublicAdvertisedPrefixesInsertArgs;
use crate::providers::gcp::clients::compute::ComputePublicAdvertisedPrefixesListArgs;
use crate::providers::gcp::clients::compute::ComputePublicAdvertisedPrefixesPatchArgs;
use crate::providers::gcp::clients::compute::ComputePublicAdvertisedPrefixesWithdrawArgs;
use crate::providers::gcp::clients::compute::ComputePublicDelegatedPrefixesAggregatedListArgs;
use crate::providers::gcp::clients::compute::ComputePublicDelegatedPrefixesAnnounceArgs;
use crate::providers::gcp::clients::compute::ComputePublicDelegatedPrefixesDeleteArgs;
use crate::providers::gcp::clients::compute::ComputePublicDelegatedPrefixesGetArgs;
use crate::providers::gcp::clients::compute::ComputePublicDelegatedPrefixesInsertArgs;
use crate::providers::gcp::clients::compute::ComputePublicDelegatedPrefixesListArgs;
use crate::providers::gcp::clients::compute::ComputePublicDelegatedPrefixesPatchArgs;
use crate::providers::gcp::clients::compute::ComputePublicDelegatedPrefixesWithdrawArgs;
use crate::providers::gcp::clients::compute::ComputeRegionAutoscalersDeleteArgs;
use crate::providers::gcp::clients::compute::ComputeRegionAutoscalersGetArgs;
use crate::providers::gcp::clients::compute::ComputeRegionAutoscalersInsertArgs;
use crate::providers::gcp::clients::compute::ComputeRegionAutoscalersListArgs;
use crate::providers::gcp::clients::compute::ComputeRegionAutoscalersPatchArgs;
use crate::providers::gcp::clients::compute::ComputeRegionAutoscalersTestIamPermissionsArgs;
use crate::providers::gcp::clients::compute::ComputeRegionAutoscalersUpdateArgs;
use crate::providers::gcp::clients::compute::ComputeRegionBackendBucketsDeleteArgs;
use crate::providers::gcp::clients::compute::ComputeRegionBackendBucketsGetArgs;
use crate::providers::gcp::clients::compute::ComputeRegionBackendBucketsGetIamPolicyArgs;
use crate::providers::gcp::clients::compute::ComputeRegionBackendBucketsInsertArgs;
use crate::providers::gcp::clients::compute::ComputeRegionBackendBucketsListArgs;
use crate::providers::gcp::clients::compute::ComputeRegionBackendBucketsListUsableArgs;
use crate::providers::gcp::clients::compute::ComputeRegionBackendBucketsPatchArgs;
use crate::providers::gcp::clients::compute::ComputeRegionBackendBucketsSetIamPolicyArgs;
use crate::providers::gcp::clients::compute::ComputeRegionBackendBucketsTestIamPermissionsArgs;
use crate::providers::gcp::clients::compute::ComputeRegionBackendServicesDeleteArgs;
use crate::providers::gcp::clients::compute::ComputeRegionBackendServicesGetArgs;
use crate::providers::gcp::clients::compute::ComputeRegionBackendServicesGetHealthArgs;
use crate::providers::gcp::clients::compute::ComputeRegionBackendServicesGetIamPolicyArgs;
use crate::providers::gcp::clients::compute::ComputeRegionBackendServicesInsertArgs;
use crate::providers::gcp::clients::compute::ComputeRegionBackendServicesListArgs;
use crate::providers::gcp::clients::compute::ComputeRegionBackendServicesListUsableArgs;
use crate::providers::gcp::clients::compute::ComputeRegionBackendServicesPatchArgs;
use crate::providers::gcp::clients::compute::ComputeRegionBackendServicesSetIamPolicyArgs;
use crate::providers::gcp::clients::compute::ComputeRegionBackendServicesSetSecurityPolicyArgs;
use crate::providers::gcp::clients::compute::ComputeRegionBackendServicesTestIamPermissionsArgs;
use crate::providers::gcp::clients::compute::ComputeRegionBackendServicesUpdateArgs;
use crate::providers::gcp::clients::compute::ComputeRegionCommitmentsAggregatedListArgs;
use crate::providers::gcp::clients::compute::ComputeRegionCommitmentsGetArgs;
use crate::providers::gcp::clients::compute::ComputeRegionCommitmentsInsertArgs;
use crate::providers::gcp::clients::compute::ComputeRegionCommitmentsListArgs;
use crate::providers::gcp::clients::compute::ComputeRegionCommitmentsUpdateArgs;
use crate::providers::gcp::clients::compute::ComputeRegionCompositeHealthChecksAggregatedListArgs;
use crate::providers::gcp::clients::compute::ComputeRegionCompositeHealthChecksDeleteArgs;
use crate::providers::gcp::clients::compute::ComputeRegionCompositeHealthChecksGetArgs;
use crate::providers::gcp::clients::compute::ComputeRegionCompositeHealthChecksGetHealthArgs;
use crate::providers::gcp::clients::compute::ComputeRegionCompositeHealthChecksInsertArgs;
use crate::providers::gcp::clients::compute::ComputeRegionCompositeHealthChecksListArgs;
use crate::providers::gcp::clients::compute::ComputeRegionCompositeHealthChecksPatchArgs;
use crate::providers::gcp::clients::compute::ComputeRegionCompositeHealthChecksTestIamPermissionsArgs;
use crate::providers::gcp::clients::compute::ComputeRegionDiskTypesGetArgs;
use crate::providers::gcp::clients::compute::ComputeRegionDiskTypesListArgs;
use crate::providers::gcp::clients::compute::ComputeRegionDisksAddResourcePoliciesArgs;
use crate::providers::gcp::clients::compute::ComputeRegionDisksBulkInsertArgs;
use crate::providers::gcp::clients::compute::ComputeRegionDisksCreateSnapshotArgs;
use crate::providers::gcp::clients::compute::ComputeRegionDisksDeleteArgs;
use crate::providers::gcp::clients::compute::ComputeRegionDisksGetArgs;
use crate::providers::gcp::clients::compute::ComputeRegionDisksGetIamPolicyArgs;
use crate::providers::gcp::clients::compute::ComputeRegionDisksInsertArgs;
use crate::providers::gcp::clients::compute::ComputeRegionDisksListArgs;
use crate::providers::gcp::clients::compute::ComputeRegionDisksRemoveResourcePoliciesArgs;
use crate::providers::gcp::clients::compute::ComputeRegionDisksResizeArgs;
use crate::providers::gcp::clients::compute::ComputeRegionDisksSetIamPolicyArgs;
use crate::providers::gcp::clients::compute::ComputeRegionDisksSetLabelsArgs;
use crate::providers::gcp::clients::compute::ComputeRegionDisksStartAsyncReplicationArgs;
use crate::providers::gcp::clients::compute::ComputeRegionDisksStopAsyncReplicationArgs;
use crate::providers::gcp::clients::compute::ComputeRegionDisksStopGroupAsyncReplicationArgs;
use crate::providers::gcp::clients::compute::ComputeRegionDisksTestIamPermissionsArgs;
use crate::providers::gcp::clients::compute::ComputeRegionDisksUpdateArgs;
use crate::providers::gcp::clients::compute::ComputeRegionDisksUpdateKmsKeyArgs;
use crate::providers::gcp::clients::compute::ComputeRegionHealthAggregationPoliciesAggregatedListArgs;
use crate::providers::gcp::clients::compute::ComputeRegionHealthAggregationPoliciesDeleteArgs;
use crate::providers::gcp::clients::compute::ComputeRegionHealthAggregationPoliciesGetArgs;
use crate::providers::gcp::clients::compute::ComputeRegionHealthAggregationPoliciesInsertArgs;
use crate::providers::gcp::clients::compute::ComputeRegionHealthAggregationPoliciesListArgs;
use crate::providers::gcp::clients::compute::ComputeRegionHealthAggregationPoliciesPatchArgs;
use crate::providers::gcp::clients::compute::ComputeRegionHealthAggregationPoliciesTestIamPermissionsArgs;
use crate::providers::gcp::clients::compute::ComputeRegionHealthCheckServicesAggregatedListArgs;
use crate::providers::gcp::clients::compute::ComputeRegionHealthCheckServicesDeleteArgs;
use crate::providers::gcp::clients::compute::ComputeRegionHealthCheckServicesGetArgs;
use crate::providers::gcp::clients::compute::ComputeRegionHealthCheckServicesInsertArgs;
use crate::providers::gcp::clients::compute::ComputeRegionHealthCheckServicesListArgs;
use crate::providers::gcp::clients::compute::ComputeRegionHealthCheckServicesPatchArgs;
use crate::providers::gcp::clients::compute::ComputeRegionHealthCheckServicesTestIamPermissionsArgs;
use crate::providers::gcp::clients::compute::ComputeRegionHealthChecksDeleteArgs;
use crate::providers::gcp::clients::compute::ComputeRegionHealthChecksGetArgs;
use crate::providers::gcp::clients::compute::ComputeRegionHealthChecksInsertArgs;
use crate::providers::gcp::clients::compute::ComputeRegionHealthChecksListArgs;
use crate::providers::gcp::clients::compute::ComputeRegionHealthChecksPatchArgs;
use crate::providers::gcp::clients::compute::ComputeRegionHealthChecksTestIamPermissionsArgs;
use crate::providers::gcp::clients::compute::ComputeRegionHealthChecksUpdateArgs;
use crate::providers::gcp::clients::compute::ComputeRegionHealthSourcesAggregatedListArgs;
use crate::providers::gcp::clients::compute::ComputeRegionHealthSourcesDeleteArgs;
use crate::providers::gcp::clients::compute::ComputeRegionHealthSourcesGetArgs;
use crate::providers::gcp::clients::compute::ComputeRegionHealthSourcesGetHealthArgs;
use crate::providers::gcp::clients::compute::ComputeRegionHealthSourcesInsertArgs;
use crate::providers::gcp::clients::compute::ComputeRegionHealthSourcesListArgs;
use crate::providers::gcp::clients::compute::ComputeRegionHealthSourcesPatchArgs;
use crate::providers::gcp::clients::compute::ComputeRegionHealthSourcesTestIamPermissionsArgs;
use crate::providers::gcp::clients::compute::ComputeRegionInstanceGroupManagerResizeRequestsCancelArgs;
use crate::providers::gcp::clients::compute::ComputeRegionInstanceGroupManagerResizeRequestsDeleteArgs;
use crate::providers::gcp::clients::compute::ComputeRegionInstanceGroupManagerResizeRequestsGetArgs;
use crate::providers::gcp::clients::compute::ComputeRegionInstanceGroupManagerResizeRequestsInsertArgs;
use crate::providers::gcp::clients::compute::ComputeRegionInstanceGroupManagerResizeRequestsListArgs;
use crate::providers::gcp::clients::compute::ComputeRegionInstanceGroupManagersAbandonInstancesArgs;
use crate::providers::gcp::clients::compute::ComputeRegionInstanceGroupManagersApplyUpdatesToInstancesArgs;
use crate::providers::gcp::clients::compute::ComputeRegionInstanceGroupManagersCreateInstancesArgs;
use crate::providers::gcp::clients::compute::ComputeRegionInstanceGroupManagersDeleteArgs;
use crate::providers::gcp::clients::compute::ComputeRegionInstanceGroupManagersDeleteInstancesArgs;
use crate::providers::gcp::clients::compute::ComputeRegionInstanceGroupManagersDeletePerInstanceConfigsArgs;
use crate::providers::gcp::clients::compute::ComputeRegionInstanceGroupManagersGetArgs;
use crate::providers::gcp::clients::compute::ComputeRegionInstanceGroupManagersInsertArgs;
use crate::providers::gcp::clients::compute::ComputeRegionInstanceGroupManagersListArgs;
use crate::providers::gcp::clients::compute::ComputeRegionInstanceGroupManagersListErrorsArgs;
use crate::providers::gcp::clients::compute::ComputeRegionInstanceGroupManagersListManagedInstancesArgs;
use crate::providers::gcp::clients::compute::ComputeRegionInstanceGroupManagersListPerInstanceConfigsArgs;
use crate::providers::gcp::clients::compute::ComputeRegionInstanceGroupManagersPatchArgs;
use crate::providers::gcp::clients::compute::ComputeRegionInstanceGroupManagersPatchPerInstanceConfigsArgs;
use crate::providers::gcp::clients::compute::ComputeRegionInstanceGroupManagersRecreateInstancesArgs;
use crate::providers::gcp::clients::compute::ComputeRegionInstanceGroupManagersResizeArgs;
use crate::providers::gcp::clients::compute::ComputeRegionInstanceGroupManagersResumeInstancesArgs;
use crate::providers::gcp::clients::compute::ComputeRegionInstanceGroupManagersSetInstanceTemplateArgs;
use crate::providers::gcp::clients::compute::ComputeRegionInstanceGroupManagersSetTargetPoolsArgs;
use crate::providers::gcp::clients::compute::ComputeRegionInstanceGroupManagersStartInstancesArgs;
use crate::providers::gcp::clients::compute::ComputeRegionInstanceGroupManagersStopInstancesArgs;
use crate::providers::gcp::clients::compute::ComputeRegionInstanceGroupManagersSuspendInstancesArgs;
use crate::providers::gcp::clients::compute::ComputeRegionInstanceGroupManagersUpdatePerInstanceConfigsArgs;
use crate::providers::gcp::clients::compute::ComputeRegionInstanceGroupsGetArgs;
use crate::providers::gcp::clients::compute::ComputeRegionInstanceGroupsListArgs;
use crate::providers::gcp::clients::compute::ComputeRegionInstanceGroupsListInstancesArgs;
use crate::providers::gcp::clients::compute::ComputeRegionInstanceGroupsSetNamedPortsArgs;
use crate::providers::gcp::clients::compute::ComputeRegionInstanceGroupsTestIamPermissionsArgs;
use crate::providers::gcp::clients::compute::ComputeRegionInstanceTemplatesDeleteArgs;
use crate::providers::gcp::clients::compute::ComputeRegionInstanceTemplatesGetArgs;
use crate::providers::gcp::clients::compute::ComputeRegionInstanceTemplatesInsertArgs;
use crate::providers::gcp::clients::compute::ComputeRegionInstanceTemplatesListArgs;
use crate::providers::gcp::clients::compute::ComputeRegionInstancesBulkInsertArgs;
use crate::providers::gcp::clients::compute::ComputeRegionInstantSnapshotGroupsDeleteArgs;
use crate::providers::gcp::clients::compute::ComputeRegionInstantSnapshotGroupsGetArgs;
use crate::providers::gcp::clients::compute::ComputeRegionInstantSnapshotGroupsGetIamPolicyArgs;
use crate::providers::gcp::clients::compute::ComputeRegionInstantSnapshotGroupsInsertArgs;
use crate::providers::gcp::clients::compute::ComputeRegionInstantSnapshotGroupsListArgs;
use crate::providers::gcp::clients::compute::ComputeRegionInstantSnapshotGroupsSetIamPolicyArgs;
use crate::providers::gcp::clients::compute::ComputeRegionInstantSnapshotGroupsTestIamPermissionsArgs;
use crate::providers::gcp::clients::compute::ComputeRegionInstantSnapshotsDeleteArgs;
use crate::providers::gcp::clients::compute::ComputeRegionInstantSnapshotsGetArgs;
use crate::providers::gcp::clients::compute::ComputeRegionInstantSnapshotsGetIamPolicyArgs;
use crate::providers::gcp::clients::compute::ComputeRegionInstantSnapshotsInsertArgs;
use crate::providers::gcp::clients::compute::ComputeRegionInstantSnapshotsListArgs;
use crate::providers::gcp::clients::compute::ComputeRegionInstantSnapshotsSetIamPolicyArgs;
use crate::providers::gcp::clients::compute::ComputeRegionInstantSnapshotsSetLabelsArgs;
use crate::providers::gcp::clients::compute::ComputeRegionInstantSnapshotsTestIamPermissionsArgs;
use crate::providers::gcp::clients::compute::ComputeRegionNetworkEndpointGroupsAttachNetworkEndpointsArgs;
use crate::providers::gcp::clients::compute::ComputeRegionNetworkEndpointGroupsDeleteArgs;
use crate::providers::gcp::clients::compute::ComputeRegionNetworkEndpointGroupsDetachNetworkEndpointsArgs;
use crate::providers::gcp::clients::compute::ComputeRegionNetworkEndpointGroupsGetArgs;
use crate::providers::gcp::clients::compute::ComputeRegionNetworkEndpointGroupsInsertArgs;
use crate::providers::gcp::clients::compute::ComputeRegionNetworkEndpointGroupsListArgs;
use crate::providers::gcp::clients::compute::ComputeRegionNetworkEndpointGroupsListNetworkEndpointsArgs;
use crate::providers::gcp::clients::compute::ComputeRegionNetworkFirewallPoliciesAddAssociationArgs;
use crate::providers::gcp::clients::compute::ComputeRegionNetworkFirewallPoliciesAddRuleArgs;
use crate::providers::gcp::clients::compute::ComputeRegionNetworkFirewallPoliciesCloneRulesArgs;
use crate::providers::gcp::clients::compute::ComputeRegionNetworkFirewallPoliciesDeleteArgs;
use crate::providers::gcp::clients::compute::ComputeRegionNetworkFirewallPoliciesGetArgs;
use crate::providers::gcp::clients::compute::ComputeRegionNetworkFirewallPoliciesGetAssociationArgs;
use crate::providers::gcp::clients::compute::ComputeRegionNetworkFirewallPoliciesGetEffectiveFirewallsArgs;
use crate::providers::gcp::clients::compute::ComputeRegionNetworkFirewallPoliciesGetIamPolicyArgs;
use crate::providers::gcp::clients::compute::ComputeRegionNetworkFirewallPoliciesGetRuleArgs;
use crate::providers::gcp::clients::compute::ComputeRegionNetworkFirewallPoliciesInsertArgs;
use crate::providers::gcp::clients::compute::ComputeRegionNetworkFirewallPoliciesListArgs;
use crate::providers::gcp::clients::compute::ComputeRegionNetworkFirewallPoliciesPatchArgs;
use crate::providers::gcp::clients::compute::ComputeRegionNetworkFirewallPoliciesPatchRuleArgs;
use crate::providers::gcp::clients::compute::ComputeRegionNetworkFirewallPoliciesRemoveAssociationArgs;
use crate::providers::gcp::clients::compute::ComputeRegionNetworkFirewallPoliciesRemoveRuleArgs;
use crate::providers::gcp::clients::compute::ComputeRegionNetworkFirewallPoliciesSetIamPolicyArgs;
use crate::providers::gcp::clients::compute::ComputeRegionNetworkFirewallPoliciesTestIamPermissionsArgs;
use crate::providers::gcp::clients::compute::ComputeRegionNotificationEndpointsAggregatedListArgs;
use crate::providers::gcp::clients::compute::ComputeRegionNotificationEndpointsDeleteArgs;
use crate::providers::gcp::clients::compute::ComputeRegionNotificationEndpointsGetArgs;
use crate::providers::gcp::clients::compute::ComputeRegionNotificationEndpointsInsertArgs;
use crate::providers::gcp::clients::compute::ComputeRegionNotificationEndpointsListArgs;
use crate::providers::gcp::clients::compute::ComputeRegionNotificationEndpointsTestIamPermissionsArgs;
use crate::providers::gcp::clients::compute::ComputeRegionOperationsDeleteArgs;
use crate::providers::gcp::clients::compute::ComputeRegionOperationsGetArgs;
use crate::providers::gcp::clients::compute::ComputeRegionOperationsListArgs;
use crate::providers::gcp::clients::compute::ComputeRegionOperationsWaitArgs;
use crate::providers::gcp::clients::compute::ComputeRegionSecurityPoliciesAddRuleArgs;
use crate::providers::gcp::clients::compute::ComputeRegionSecurityPoliciesDeleteArgs;
use crate::providers::gcp::clients::compute::ComputeRegionSecurityPoliciesGetArgs;
use crate::providers::gcp::clients::compute::ComputeRegionSecurityPoliciesGetRuleArgs;
use crate::providers::gcp::clients::compute::ComputeRegionSecurityPoliciesInsertArgs;
use crate::providers::gcp::clients::compute::ComputeRegionSecurityPoliciesListArgs;
use crate::providers::gcp::clients::compute::ComputeRegionSecurityPoliciesPatchArgs;
use crate::providers::gcp::clients::compute::ComputeRegionSecurityPoliciesPatchRuleArgs;
use crate::providers::gcp::clients::compute::ComputeRegionSecurityPoliciesRemoveRuleArgs;
use crate::providers::gcp::clients::compute::ComputeRegionSecurityPoliciesSetLabelsArgs;
use crate::providers::gcp::clients::compute::ComputeRegionSnapshotSettingsGetArgs;
use crate::providers::gcp::clients::compute::ComputeRegionSnapshotSettingsPatchArgs;
use crate::providers::gcp::clients::compute::ComputeRegionSnapshotsDeleteArgs;
use crate::providers::gcp::clients::compute::ComputeRegionSnapshotsGetArgs;
use crate::providers::gcp::clients::compute::ComputeRegionSnapshotsGetIamPolicyArgs;
use crate::providers::gcp::clients::compute::ComputeRegionSnapshotsInsertArgs;
use crate::providers::gcp::clients::compute::ComputeRegionSnapshotsListArgs;
use crate::providers::gcp::clients::compute::ComputeRegionSnapshotsSetIamPolicyArgs;
use crate::providers::gcp::clients::compute::ComputeRegionSnapshotsSetLabelsArgs;
use crate::providers::gcp::clients::compute::ComputeRegionSnapshotsTestIamPermissionsArgs;
use crate::providers::gcp::clients::compute::ComputeRegionSnapshotsUpdateKmsKeyArgs;
use crate::providers::gcp::clients::compute::ComputeRegionSslCertificatesDeleteArgs;
use crate::providers::gcp::clients::compute::ComputeRegionSslCertificatesGetArgs;
use crate::providers::gcp::clients::compute::ComputeRegionSslCertificatesInsertArgs;
use crate::providers::gcp::clients::compute::ComputeRegionSslCertificatesListArgs;
use crate::providers::gcp::clients::compute::ComputeRegionSslPoliciesDeleteArgs;
use crate::providers::gcp::clients::compute::ComputeRegionSslPoliciesGetArgs;
use crate::providers::gcp::clients::compute::ComputeRegionSslPoliciesInsertArgs;
use crate::providers::gcp::clients::compute::ComputeRegionSslPoliciesListArgs;
use crate::providers::gcp::clients::compute::ComputeRegionSslPoliciesListAvailableFeaturesArgs;
use crate::providers::gcp::clients::compute::ComputeRegionSslPoliciesPatchArgs;
use crate::providers::gcp::clients::compute::ComputeRegionTargetHttpProxiesDeleteArgs;
use crate::providers::gcp::clients::compute::ComputeRegionTargetHttpProxiesGetArgs;
use crate::providers::gcp::clients::compute::ComputeRegionTargetHttpProxiesInsertArgs;
use crate::providers::gcp::clients::compute::ComputeRegionTargetHttpProxiesListArgs;
use crate::providers::gcp::clients::compute::ComputeRegionTargetHttpProxiesSetUrlMapArgs;
use crate::providers::gcp::clients::compute::ComputeRegionTargetHttpsProxiesDeleteArgs;
use crate::providers::gcp::clients::compute::ComputeRegionTargetHttpsProxiesGetArgs;
use crate::providers::gcp::clients::compute::ComputeRegionTargetHttpsProxiesInsertArgs;
use crate::providers::gcp::clients::compute::ComputeRegionTargetHttpsProxiesListArgs;
use crate::providers::gcp::clients::compute::ComputeRegionTargetHttpsProxiesPatchArgs;
use crate::providers::gcp::clients::compute::ComputeRegionTargetHttpsProxiesSetSslCertificatesArgs;
use crate::providers::gcp::clients::compute::ComputeRegionTargetHttpsProxiesSetUrlMapArgs;
use crate::providers::gcp::clients::compute::ComputeRegionTargetTcpProxiesDeleteArgs;
use crate::providers::gcp::clients::compute::ComputeRegionTargetTcpProxiesGetArgs;
use crate::providers::gcp::clients::compute::ComputeRegionTargetTcpProxiesInsertArgs;
use crate::providers::gcp::clients::compute::ComputeRegionTargetTcpProxiesListArgs;
use crate::providers::gcp::clients::compute::ComputeRegionUrlMapsDeleteArgs;
use crate::providers::gcp::clients::compute::ComputeRegionUrlMapsGetArgs;
use crate::providers::gcp::clients::compute::ComputeRegionUrlMapsInsertArgs;
use crate::providers::gcp::clients::compute::ComputeRegionUrlMapsListArgs;
use crate::providers::gcp::clients::compute::ComputeRegionUrlMapsPatchArgs;
use crate::providers::gcp::clients::compute::ComputeRegionUrlMapsUpdateArgs;
use crate::providers::gcp::clients::compute::ComputeRegionUrlMapsValidateArgs;
use crate::providers::gcp::clients::compute::ComputeRegionZonesListArgs;
use crate::providers::gcp::clients::compute::ComputeRegionsGetArgs;
use crate::providers::gcp::clients::compute::ComputeRegionsListArgs;
use crate::providers::gcp::clients::compute::ComputeReservationBlocksGetArgs;
use crate::providers::gcp::clients::compute::ComputeReservationBlocksGetIamPolicyArgs;
use crate::providers::gcp::clients::compute::ComputeReservationBlocksListArgs;
use crate::providers::gcp::clients::compute::ComputeReservationBlocksPerformMaintenanceArgs;
use crate::providers::gcp::clients::compute::ComputeReservationBlocksSetIamPolicyArgs;
use crate::providers::gcp::clients::compute::ComputeReservationBlocksTestIamPermissionsArgs;
use crate::providers::gcp::clients::compute::ComputeReservationSlotsGetArgs;
use crate::providers::gcp::clients::compute::ComputeReservationSlotsGetVersionArgs;
use crate::providers::gcp::clients::compute::ComputeReservationSlotsListArgs;
use crate::providers::gcp::clients::compute::ComputeReservationSlotsUpdateArgs;
use crate::providers::gcp::clients::compute::ComputeReservationSubBlocksGetArgs;
use crate::providers::gcp::clients::compute::ComputeReservationSubBlocksGetIamPolicyArgs;
use crate::providers::gcp::clients::compute::ComputeReservationSubBlocksGetVersionArgs;
use crate::providers::gcp::clients::compute::ComputeReservationSubBlocksListArgs;
use crate::providers::gcp::clients::compute::ComputeReservationSubBlocksPerformMaintenanceArgs;
use crate::providers::gcp::clients::compute::ComputeReservationSubBlocksReportFaultyArgs;
use crate::providers::gcp::clients::compute::ComputeReservationSubBlocksSetIamPolicyArgs;
use crate::providers::gcp::clients::compute::ComputeReservationSubBlocksTestIamPermissionsArgs;
use crate::providers::gcp::clients::compute::ComputeReservationsAggregatedListArgs;
use crate::providers::gcp::clients::compute::ComputeReservationsDeleteArgs;
use crate::providers::gcp::clients::compute::ComputeReservationsGetArgs;
use crate::providers::gcp::clients::compute::ComputeReservationsGetIamPolicyArgs;
use crate::providers::gcp::clients::compute::ComputeReservationsInsertArgs;
use crate::providers::gcp::clients::compute::ComputeReservationsListArgs;
use crate::providers::gcp::clients::compute::ComputeReservationsPerformMaintenanceArgs;
use crate::providers::gcp::clients::compute::ComputeReservationsResizeArgs;
use crate::providers::gcp::clients::compute::ComputeReservationsSetIamPolicyArgs;
use crate::providers::gcp::clients::compute::ComputeReservationsTestIamPermissionsArgs;
use crate::providers::gcp::clients::compute::ComputeReservationsUpdateArgs;
use crate::providers::gcp::clients::compute::ComputeResourcePoliciesAggregatedListArgs;
use crate::providers::gcp::clients::compute::ComputeResourcePoliciesDeleteArgs;
use crate::providers::gcp::clients::compute::ComputeResourcePoliciesGetArgs;
use crate::providers::gcp::clients::compute::ComputeResourcePoliciesGetIamPolicyArgs;
use crate::providers::gcp::clients::compute::ComputeResourcePoliciesInsertArgs;
use crate::providers::gcp::clients::compute::ComputeResourcePoliciesListArgs;
use crate::providers::gcp::clients::compute::ComputeResourcePoliciesPatchArgs;
use crate::providers::gcp::clients::compute::ComputeResourcePoliciesSetIamPolicyArgs;
use crate::providers::gcp::clients::compute::ComputeResourcePoliciesTestIamPermissionsArgs;
use crate::providers::gcp::clients::compute::ComputeRoutersAggregatedListArgs;
use crate::providers::gcp::clients::compute::ComputeRoutersDeleteArgs;
use crate::providers::gcp::clients::compute::ComputeRoutersDeleteRoutePolicyArgs;
use crate::providers::gcp::clients::compute::ComputeRoutersGetArgs;
use crate::providers::gcp::clients::compute::ComputeRoutersGetNatIpInfoArgs;
use crate::providers::gcp::clients::compute::ComputeRoutersGetNatMappingInfoArgs;
use crate::providers::gcp::clients::compute::ComputeRoutersGetRoutePolicyArgs;
use crate::providers::gcp::clients::compute::ComputeRoutersGetRouterStatusArgs;
use crate::providers::gcp::clients::compute::ComputeRoutersInsertArgs;
use crate::providers::gcp::clients::compute::ComputeRoutersListArgs;
use crate::providers::gcp::clients::compute::ComputeRoutersListBgpRoutesArgs;
use crate::providers::gcp::clients::compute::ComputeRoutersListRoutePoliciesArgs;
use crate::providers::gcp::clients::compute::ComputeRoutersPatchArgs;
use crate::providers::gcp::clients::compute::ComputeRoutersPatchRoutePolicyArgs;
use crate::providers::gcp::clients::compute::ComputeRoutersPreviewArgs;
use crate::providers::gcp::clients::compute::ComputeRoutersUpdateArgs;
use crate::providers::gcp::clients::compute::ComputeRoutersUpdateRoutePolicyArgs;
use crate::providers::gcp::clients::compute::ComputeRoutesDeleteArgs;
use crate::providers::gcp::clients::compute::ComputeRoutesGetArgs;
use crate::providers::gcp::clients::compute::ComputeRoutesInsertArgs;
use crate::providers::gcp::clients::compute::ComputeRoutesListArgs;
use crate::providers::gcp::clients::compute::ComputeRoutesTestIamPermissionsArgs;
use crate::providers::gcp::clients::compute::ComputeSecurityPoliciesAddRuleArgs;
use crate::providers::gcp::clients::compute::ComputeSecurityPoliciesAggregatedListArgs;
use crate::providers::gcp::clients::compute::ComputeSecurityPoliciesDeleteArgs;
use crate::providers::gcp::clients::compute::ComputeSecurityPoliciesGetArgs;
use crate::providers::gcp::clients::compute::ComputeSecurityPoliciesGetRuleArgs;
use crate::providers::gcp::clients::compute::ComputeSecurityPoliciesInsertArgs;
use crate::providers::gcp::clients::compute::ComputeSecurityPoliciesListArgs;
use crate::providers::gcp::clients::compute::ComputeSecurityPoliciesListPreconfiguredExpressionSetsArgs;
use crate::providers::gcp::clients::compute::ComputeSecurityPoliciesPatchArgs;
use crate::providers::gcp::clients::compute::ComputeSecurityPoliciesPatchRuleArgs;
use crate::providers::gcp::clients::compute::ComputeSecurityPoliciesRemoveRuleArgs;
use crate::providers::gcp::clients::compute::ComputeSecurityPoliciesSetLabelsArgs;
use crate::providers::gcp::clients::compute::ComputeServiceAttachmentsAggregatedListArgs;
use crate::providers::gcp::clients::compute::ComputeServiceAttachmentsDeleteArgs;
use crate::providers::gcp::clients::compute::ComputeServiceAttachmentsGetArgs;
use crate::providers::gcp::clients::compute::ComputeServiceAttachmentsGetIamPolicyArgs;
use crate::providers::gcp::clients::compute::ComputeServiceAttachmentsInsertArgs;
use crate::providers::gcp::clients::compute::ComputeServiceAttachmentsListArgs;
use crate::providers::gcp::clients::compute::ComputeServiceAttachmentsPatchArgs;
use crate::providers::gcp::clients::compute::ComputeServiceAttachmentsSetIamPolicyArgs;
use crate::providers::gcp::clients::compute::ComputeServiceAttachmentsTestIamPermissionsArgs;
use crate::providers::gcp::clients::compute::ComputeSnapshotSettingsGetArgs;
use crate::providers::gcp::clients::compute::ComputeSnapshotSettingsPatchArgs;
use crate::providers::gcp::clients::compute::ComputeSnapshotsDeleteArgs;
use crate::providers::gcp::clients::compute::ComputeSnapshotsGetArgs;
use crate::providers::gcp::clients::compute::ComputeSnapshotsGetIamPolicyArgs;
use crate::providers::gcp::clients::compute::ComputeSnapshotsInsertArgs;
use crate::providers::gcp::clients::compute::ComputeSnapshotsListArgs;
use crate::providers::gcp::clients::compute::ComputeSnapshotsSetIamPolicyArgs;
use crate::providers::gcp::clients::compute::ComputeSnapshotsSetLabelsArgs;
use crate::providers::gcp::clients::compute::ComputeSnapshotsTestIamPermissionsArgs;
use crate::providers::gcp::clients::compute::ComputeSnapshotsUpdateKmsKeyArgs;
use crate::providers::gcp::clients::compute::ComputeSslCertificatesAggregatedListArgs;
use crate::providers::gcp::clients::compute::ComputeSslCertificatesDeleteArgs;
use crate::providers::gcp::clients::compute::ComputeSslCertificatesGetArgs;
use crate::providers::gcp::clients::compute::ComputeSslCertificatesInsertArgs;
use crate::providers::gcp::clients::compute::ComputeSslCertificatesListArgs;
use crate::providers::gcp::clients::compute::ComputeSslPoliciesAggregatedListArgs;
use crate::providers::gcp::clients::compute::ComputeSslPoliciesDeleteArgs;
use crate::providers::gcp::clients::compute::ComputeSslPoliciesGetArgs;
use crate::providers::gcp::clients::compute::ComputeSslPoliciesInsertArgs;
use crate::providers::gcp::clients::compute::ComputeSslPoliciesListArgs;
use crate::providers::gcp::clients::compute::ComputeSslPoliciesListAvailableFeaturesArgs;
use crate::providers::gcp::clients::compute::ComputeSslPoliciesPatchArgs;
use crate::providers::gcp::clients::compute::ComputeStoragePoolTypesAggregatedListArgs;
use crate::providers::gcp::clients::compute::ComputeStoragePoolTypesGetArgs;
use crate::providers::gcp::clients::compute::ComputeStoragePoolTypesListArgs;
use crate::providers::gcp::clients::compute::ComputeStoragePoolsAggregatedListArgs;
use crate::providers::gcp::clients::compute::ComputeStoragePoolsDeleteArgs;
use crate::providers::gcp::clients::compute::ComputeStoragePoolsGetArgs;
use crate::providers::gcp::clients::compute::ComputeStoragePoolsGetIamPolicyArgs;
use crate::providers::gcp::clients::compute::ComputeStoragePoolsInsertArgs;
use crate::providers::gcp::clients::compute::ComputeStoragePoolsListArgs;
use crate::providers::gcp::clients::compute::ComputeStoragePoolsListDisksArgs;
use crate::providers::gcp::clients::compute::ComputeStoragePoolsSetIamPolicyArgs;
use crate::providers::gcp::clients::compute::ComputeStoragePoolsTestIamPermissionsArgs;
use crate::providers::gcp::clients::compute::ComputeStoragePoolsUpdateArgs;
use crate::providers::gcp::clients::compute::ComputeSubnetworksAggregatedListArgs;
use crate::providers::gcp::clients::compute::ComputeSubnetworksDeleteArgs;
use crate::providers::gcp::clients::compute::ComputeSubnetworksExpandIpCidrRangeArgs;
use crate::providers::gcp::clients::compute::ComputeSubnetworksGetArgs;
use crate::providers::gcp::clients::compute::ComputeSubnetworksGetIamPolicyArgs;
use crate::providers::gcp::clients::compute::ComputeSubnetworksInsertArgs;
use crate::providers::gcp::clients::compute::ComputeSubnetworksListArgs;
use crate::providers::gcp::clients::compute::ComputeSubnetworksListUsableArgs;
use crate::providers::gcp::clients::compute::ComputeSubnetworksPatchArgs;
use crate::providers::gcp::clients::compute::ComputeSubnetworksSetIamPolicyArgs;
use crate::providers::gcp::clients::compute::ComputeSubnetworksSetPrivateIpGoogleAccessArgs;
use crate::providers::gcp::clients::compute::ComputeSubnetworksTestIamPermissionsArgs;
use crate::providers::gcp::clients::compute::ComputeTargetGrpcProxiesDeleteArgs;
use crate::providers::gcp::clients::compute::ComputeTargetGrpcProxiesGetArgs;
use crate::providers::gcp::clients::compute::ComputeTargetGrpcProxiesInsertArgs;
use crate::providers::gcp::clients::compute::ComputeTargetGrpcProxiesListArgs;
use crate::providers::gcp::clients::compute::ComputeTargetGrpcProxiesPatchArgs;
use crate::providers::gcp::clients::compute::ComputeTargetHttpProxiesAggregatedListArgs;
use crate::providers::gcp::clients::compute::ComputeTargetHttpProxiesDeleteArgs;
use crate::providers::gcp::clients::compute::ComputeTargetHttpProxiesGetArgs;
use crate::providers::gcp::clients::compute::ComputeTargetHttpProxiesInsertArgs;
use crate::providers::gcp::clients::compute::ComputeTargetHttpProxiesListArgs;
use crate::providers::gcp::clients::compute::ComputeTargetHttpProxiesPatchArgs;
use crate::providers::gcp::clients::compute::ComputeTargetHttpProxiesSetUrlMapArgs;
use crate::providers::gcp::clients::compute::ComputeTargetHttpsProxiesAggregatedListArgs;
use crate::providers::gcp::clients::compute::ComputeTargetHttpsProxiesDeleteArgs;
use crate::providers::gcp::clients::compute::ComputeTargetHttpsProxiesGetArgs;
use crate::providers::gcp::clients::compute::ComputeTargetHttpsProxiesInsertArgs;
use crate::providers::gcp::clients::compute::ComputeTargetHttpsProxiesListArgs;
use crate::providers::gcp::clients::compute::ComputeTargetHttpsProxiesPatchArgs;
use crate::providers::gcp::clients::compute::ComputeTargetHttpsProxiesSetCertificateMapArgs;
use crate::providers::gcp::clients::compute::ComputeTargetHttpsProxiesSetQuicOverrideArgs;
use crate::providers::gcp::clients::compute::ComputeTargetHttpsProxiesSetSslCertificatesArgs;
use crate::providers::gcp::clients::compute::ComputeTargetHttpsProxiesSetSslPolicyArgs;
use crate::providers::gcp::clients::compute::ComputeTargetHttpsProxiesSetUrlMapArgs;
use crate::providers::gcp::clients::compute::ComputeTargetInstancesAggregatedListArgs;
use crate::providers::gcp::clients::compute::ComputeTargetInstancesDeleteArgs;
use crate::providers::gcp::clients::compute::ComputeTargetInstancesGetArgs;
use crate::providers::gcp::clients::compute::ComputeTargetInstancesInsertArgs;
use crate::providers::gcp::clients::compute::ComputeTargetInstancesListArgs;
use crate::providers::gcp::clients::compute::ComputeTargetInstancesSetSecurityPolicyArgs;
use crate::providers::gcp::clients::compute::ComputeTargetInstancesTestIamPermissionsArgs;
use crate::providers::gcp::clients::compute::ComputeTargetPoolsAddHealthCheckArgs;
use crate::providers::gcp::clients::compute::ComputeTargetPoolsAddInstanceArgs;
use crate::providers::gcp::clients::compute::ComputeTargetPoolsAggregatedListArgs;
use crate::providers::gcp::clients::compute::ComputeTargetPoolsDeleteArgs;
use crate::providers::gcp::clients::compute::ComputeTargetPoolsGetArgs;
use crate::providers::gcp::clients::compute::ComputeTargetPoolsGetHealthArgs;
use crate::providers::gcp::clients::compute::ComputeTargetPoolsInsertArgs;
use crate::providers::gcp::clients::compute::ComputeTargetPoolsListArgs;
use crate::providers::gcp::clients::compute::ComputeTargetPoolsRemoveHealthCheckArgs;
use crate::providers::gcp::clients::compute::ComputeTargetPoolsRemoveInstanceArgs;
use crate::providers::gcp::clients::compute::ComputeTargetPoolsSetBackupArgs;
use crate::providers::gcp::clients::compute::ComputeTargetPoolsSetSecurityPolicyArgs;
use crate::providers::gcp::clients::compute::ComputeTargetPoolsTestIamPermissionsArgs;
use crate::providers::gcp::clients::compute::ComputeTargetSslProxiesDeleteArgs;
use crate::providers::gcp::clients::compute::ComputeTargetSslProxiesGetArgs;
use crate::providers::gcp::clients::compute::ComputeTargetSslProxiesInsertArgs;
use crate::providers::gcp::clients::compute::ComputeTargetSslProxiesListArgs;
use crate::providers::gcp::clients::compute::ComputeTargetSslProxiesSetBackendServiceArgs;
use crate::providers::gcp::clients::compute::ComputeTargetSslProxiesSetCertificateMapArgs;
use crate::providers::gcp::clients::compute::ComputeTargetSslProxiesSetProxyHeaderArgs;
use crate::providers::gcp::clients::compute::ComputeTargetSslProxiesSetSslCertificatesArgs;
use crate::providers::gcp::clients::compute::ComputeTargetSslProxiesSetSslPolicyArgs;
use crate::providers::gcp::clients::compute::ComputeTargetSslProxiesTestIamPermissionsArgs;
use crate::providers::gcp::clients::compute::ComputeTargetTcpProxiesAggregatedListArgs;
use crate::providers::gcp::clients::compute::ComputeTargetTcpProxiesDeleteArgs;
use crate::providers::gcp::clients::compute::ComputeTargetTcpProxiesGetArgs;
use crate::providers::gcp::clients::compute::ComputeTargetTcpProxiesInsertArgs;
use crate::providers::gcp::clients::compute::ComputeTargetTcpProxiesListArgs;
use crate::providers::gcp::clients::compute::ComputeTargetTcpProxiesSetBackendServiceArgs;
use crate::providers::gcp::clients::compute::ComputeTargetTcpProxiesSetProxyHeaderArgs;
use crate::providers::gcp::clients::compute::ComputeTargetTcpProxiesTestIamPermissionsArgs;
use crate::providers::gcp::clients::compute::ComputeTargetVpnGatewaysAggregatedListArgs;
use crate::providers::gcp::clients::compute::ComputeTargetVpnGatewaysDeleteArgs;
use crate::providers::gcp::clients::compute::ComputeTargetVpnGatewaysGetArgs;
use crate::providers::gcp::clients::compute::ComputeTargetVpnGatewaysInsertArgs;
use crate::providers::gcp::clients::compute::ComputeTargetVpnGatewaysListArgs;
use crate::providers::gcp::clients::compute::ComputeTargetVpnGatewaysSetLabelsArgs;
use crate::providers::gcp::clients::compute::ComputeUrlMapsAggregatedListArgs;
use crate::providers::gcp::clients::compute::ComputeUrlMapsDeleteArgs;
use crate::providers::gcp::clients::compute::ComputeUrlMapsGetArgs;
use crate::providers::gcp::clients::compute::ComputeUrlMapsInsertArgs;
use crate::providers::gcp::clients::compute::ComputeUrlMapsInvalidateCacheArgs;
use crate::providers::gcp::clients::compute::ComputeUrlMapsListArgs;
use crate::providers::gcp::clients::compute::ComputeUrlMapsPatchArgs;
use crate::providers::gcp::clients::compute::ComputeUrlMapsTestIamPermissionsArgs;
use crate::providers::gcp::clients::compute::ComputeUrlMapsUpdateArgs;
use crate::providers::gcp::clients::compute::ComputeUrlMapsValidateArgs;
use crate::providers::gcp::clients::compute::ComputeVpnGatewaysAggregatedListArgs;
use crate::providers::gcp::clients::compute::ComputeVpnGatewaysDeleteArgs;
use crate::providers::gcp::clients::compute::ComputeVpnGatewaysGetArgs;
use crate::providers::gcp::clients::compute::ComputeVpnGatewaysGetStatusArgs;
use crate::providers::gcp::clients::compute::ComputeVpnGatewaysInsertArgs;
use crate::providers::gcp::clients::compute::ComputeVpnGatewaysListArgs;
use crate::providers::gcp::clients::compute::ComputeVpnGatewaysSetLabelsArgs;
use crate::providers::gcp::clients::compute::ComputeVpnGatewaysTestIamPermissionsArgs;
use crate::providers::gcp::clients::compute::ComputeVpnTunnelsAggregatedListArgs;
use crate::providers::gcp::clients::compute::ComputeVpnTunnelsDeleteArgs;
use crate::providers::gcp::clients::compute::ComputeVpnTunnelsGetArgs;
use crate::providers::gcp::clients::compute::ComputeVpnTunnelsInsertArgs;
use crate::providers::gcp::clients::compute::ComputeVpnTunnelsListArgs;
use crate::providers::gcp::clients::compute::ComputeVpnTunnelsSetLabelsArgs;
use crate::providers::gcp::clients::compute::ComputeWireGroupsDeleteArgs;
use crate::providers::gcp::clients::compute::ComputeWireGroupsGetArgs;
use crate::providers::gcp::clients::compute::ComputeWireGroupsInsertArgs;
use crate::providers::gcp::clients::compute::ComputeWireGroupsListArgs;
use crate::providers::gcp::clients::compute::ComputeWireGroupsPatchArgs;
use crate::providers::gcp::clients::compute::ComputeZoneOperationsDeleteArgs;
use crate::providers::gcp::clients::compute::ComputeZoneOperationsGetArgs;
use crate::providers::gcp::clients::compute::ComputeZoneOperationsListArgs;
use crate::providers::gcp::clients::compute::ComputeZoneOperationsWaitArgs;
use crate::providers::gcp::clients::compute::ComputeZoneVmExtensionPoliciesDeleteArgs;
use crate::providers::gcp::clients::compute::ComputeZoneVmExtensionPoliciesGetArgs;
use crate::providers::gcp::clients::compute::ComputeZoneVmExtensionPoliciesInsertArgs;
use crate::providers::gcp::clients::compute::ComputeZoneVmExtensionPoliciesListArgs;
use crate::providers::gcp::clients::compute::ComputeZoneVmExtensionPoliciesUpdateArgs;
use crate::providers::gcp::clients::compute::ComputeZonesGetArgs;
use crate::providers::gcp::clients::compute::ComputeZonesListArgs;
use crate::provider_client::{ProviderClient, ProviderError};
use foundation_core::valtron::{execute, StreamIterator};
use foundation_core::wire::simple_http::client::{SimpleHttpClient, DnsResolver};
use foundation_db::state::store_state_task::StoreStateIdentifierTask;
use std::sync::Arc;

/// ComputeProvider with automatic state tracking.
///
/// # Type Parameters
///
/// * `S` - StateStore implementation (FileStateStore, SqliteStateStore, etc.)
/// * `R` - DNS resolver type for HTTP client
///
/// # Example
///
/// ```rust
/// let state_store = FileStateStore::new("/path", "my-project", "dev");
/// let http_client = SimpleHttpClient::with_resolver(StaticSocketAddr::new(addr));
/// let client = ProviderClient::new("my-project", "dev", state_store, http_client);
/// let provider = ComputeProvider::from_provider_client(client);
/// ```
#[derive(Clone)]
pub struct ComputeProvider<S, R>
where
    S: foundation_db::state::traits::StateStore + Send + Sync + 'static,
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + 'static,
{
    client: ProviderClient<S, R>,
    http_client: Arc<SimpleHttpClient<R>>,
}

impl<S, R> ComputeProvider<S, R>
where
    S: foundation_db::state::traits::StateStore + Send + Sync + 'static,
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + 'static,
{
    /// Create new ComputeProvider.
    pub fn new(client: ProviderClient<S, R>, http_client: Arc<SimpleHttpClient<R>>) -> Self {
        Self {
            client,
            http_client,
        }
    }

    /// Create new ComputeProvider from ProviderClient, extracting the HTTP client.
    ///
    /// This is a convenience method that calls `Self::new()` with `client.http_client()`.
    pub fn from_provider_client(client: ProviderClient<S, R>) -> Self {
        Self::new(client, client.http_client.clone())
    }

    /// Compute accelerator types aggregated list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the AcceleratorTypeAggregatedList result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_accelerator_types_aggregated_list(
        &self,
        args: &ComputeAcceleratorTypesAggregatedListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<AcceleratorTypeAggregatedList, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_accelerator_types_aggregated_list_builder(
            &self.http_client,
            &args.project,
            &args.filter,
            &args.includeAllScopes,
            &args.maxResults,
            &args.orderBy,
            &args.pageToken,
            &args.returnPartialSuccess,
            &args.serviceProjectNumber,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_accelerator_types_aggregated_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute accelerator types get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the AcceleratorType result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_accelerator_types_get(
        &self,
        args: &ComputeAcceleratorTypesGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<AcceleratorType, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_accelerator_types_get_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.acceleratorType,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_accelerator_types_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute accelerator types list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the AcceleratorTypeList result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_accelerator_types_list(
        &self,
        args: &ComputeAcceleratorTypesListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<AcceleratorTypeList, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_accelerator_types_list_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.filter,
            &args.maxResults,
            &args.orderBy,
            &args.pageToken,
            &args.returnPartialSuccess,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_accelerator_types_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute addresses aggregated list.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the AddressAggregatedList result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_addresses_aggregated_list(
        &self,
        args: &ComputeAddressesAggregatedListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<AddressAggregatedList, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_addresses_aggregated_list_builder(
            &self.http_client,
            &args.project,
            &args.filter,
            &args.includeAllScopes,
            &args.maxResults,
            &args.orderBy,
            &args.pageToken,
            &args.returnPartialSuccess,
            &args.serviceProjectNumber,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_addresses_aggregated_list_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute addresses delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_addresses_delete(
        &self,
        args: &ComputeAddressesDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_addresses_delete_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.address,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_addresses_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute addresses get.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Address result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_addresses_get(
        &self,
        args: &ComputeAddressesGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Address, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_addresses_get_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.address,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_addresses_get_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute addresses insert.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_addresses_insert(
        &self,
        args: &ComputeAddressesInsertArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_addresses_insert_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_addresses_insert_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute addresses list.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the AddressList result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_addresses_list(
        &self,
        args: &ComputeAddressesListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<AddressList, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_addresses_list_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.filter,
            &args.maxResults,
            &args.orderBy,
            &args.pageToken,
            &args.returnPartialSuccess,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_addresses_list_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute addresses move.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_addresses_move(
        &self,
        args: &ComputeAddressesMoveArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_addresses_move_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.address,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_addresses_move_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute addresses set labels.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_addresses_set_labels(
        &self,
        args: &ComputeAddressesSetLabelsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_addresses_set_labels_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.resource,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_addresses_set_labels_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute addresses test iam permissions.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TestPermissionsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_addresses_test_iam_permissions(
        &self,
        args: &ComputeAddressesTestIamPermissionsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TestPermissionsResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_addresses_test_iam_permissions_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.resource,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_addresses_test_iam_permissions_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute advice calendar mode.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the CalendarModeAdviceResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_advice_calendar_mode(
        &self,
        args: &ComputeAdviceCalendarModeArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<CalendarModeAdviceResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_advice_calendar_mode_builder(
            &self.http_client,
            &args.project,
            &args.region,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_advice_calendar_mode_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute autoscalers aggregated list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the AutoscalerAggregatedList result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_autoscalers_aggregated_list(
        &self,
        args: &ComputeAutoscalersAggregatedListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<AutoscalerAggregatedList, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_autoscalers_aggregated_list_builder(
            &self.http_client,
            &args.project,
            &args.filter,
            &args.includeAllScopes,
            &args.maxResults,
            &args.orderBy,
            &args.pageToken,
            &args.returnPartialSuccess,
            &args.serviceProjectNumber,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_autoscalers_aggregated_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute autoscalers delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_autoscalers_delete(
        &self,
        args: &ComputeAutoscalersDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_autoscalers_delete_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.autoscaler,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_autoscalers_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute autoscalers get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Autoscaler result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_autoscalers_get(
        &self,
        args: &ComputeAutoscalersGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Autoscaler, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_autoscalers_get_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.autoscaler,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_autoscalers_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute autoscalers insert.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_autoscalers_insert(
        &self,
        args: &ComputeAutoscalersInsertArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_autoscalers_insert_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_autoscalers_insert_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute autoscalers list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the AutoscalerList result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_autoscalers_list(
        &self,
        args: &ComputeAutoscalersListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<AutoscalerList, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_autoscalers_list_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.filter,
            &args.maxResults,
            &args.orderBy,
            &args.pageToken,
            &args.returnPartialSuccess,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_autoscalers_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute autoscalers patch.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_autoscalers_patch(
        &self,
        args: &ComputeAutoscalersPatchArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_autoscalers_patch_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.autoscaler,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_autoscalers_patch_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute autoscalers test iam permissions.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TestPermissionsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_autoscalers_test_iam_permissions(
        &self,
        args: &ComputeAutoscalersTestIamPermissionsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TestPermissionsResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_autoscalers_test_iam_permissions_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.resource,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_autoscalers_test_iam_permissions_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute autoscalers update.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_autoscalers_update(
        &self,
        args: &ComputeAutoscalersUpdateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_autoscalers_update_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.autoscaler,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_autoscalers_update_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute backend buckets add signed url key.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_backend_buckets_add_signed_url_key(
        &self,
        args: &ComputeBackendBucketsAddSignedUrlKeyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_backend_buckets_add_signed_url_key_builder(
            &self.http_client,
            &args.project,
            &args.backendBucket,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_backend_buckets_add_signed_url_key_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute backend buckets aggregated list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the BackendBucketAggregatedList result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_backend_buckets_aggregated_list(
        &self,
        args: &ComputeBackendBucketsAggregatedListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<BackendBucketAggregatedList, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_backend_buckets_aggregated_list_builder(
            &self.http_client,
            &args.project,
            &args.filter,
            &args.includeAllScopes,
            &args.maxResults,
            &args.orderBy,
            &args.pageToken,
            &args.returnPartialSuccess,
            &args.serviceProjectNumber,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_backend_buckets_aggregated_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute backend buckets delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_backend_buckets_delete(
        &self,
        args: &ComputeBackendBucketsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_backend_buckets_delete_builder(
            &self.http_client,
            &args.project,
            &args.backendBucket,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_backend_buckets_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute backend buckets delete signed url key.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_backend_buckets_delete_signed_url_key(
        &self,
        args: &ComputeBackendBucketsDeleteSignedUrlKeyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_backend_buckets_delete_signed_url_key_builder(
            &self.http_client,
            &args.project,
            &args.backendBucket,
            &args.keyName,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_backend_buckets_delete_signed_url_key_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute backend buckets get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the BackendBucket result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_backend_buckets_get(
        &self,
        args: &ComputeBackendBucketsGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<BackendBucket, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_backend_buckets_get_builder(
            &self.http_client,
            &args.project,
            &args.backendBucket,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_backend_buckets_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute backend buckets get iam policy.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Policy result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_backend_buckets_get_iam_policy(
        &self,
        args: &ComputeBackendBucketsGetIamPolicyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Policy, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_backend_buckets_get_iam_policy_builder(
            &self.http_client,
            &args.project,
            &args.resource,
            &args.optionsRequestedPolicyVersion,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_backend_buckets_get_iam_policy_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute backend buckets insert.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_backend_buckets_insert(
        &self,
        args: &ComputeBackendBucketsInsertArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_backend_buckets_insert_builder(
            &self.http_client,
            &args.project,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_backend_buckets_insert_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute backend buckets list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the BackendBucketList result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_backend_buckets_list(
        &self,
        args: &ComputeBackendBucketsListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<BackendBucketList, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_backend_buckets_list_builder(
            &self.http_client,
            &args.project,
            &args.filter,
            &args.maxResults,
            &args.orderBy,
            &args.pageToken,
            &args.returnPartialSuccess,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_backend_buckets_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute backend buckets list usable.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the BackendBucketListUsable result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_backend_buckets_list_usable(
        &self,
        args: &ComputeBackendBucketsListUsableArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<BackendBucketListUsable, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_backend_buckets_list_usable_builder(
            &self.http_client,
            &args.project,
            &args.filter,
            &args.maxResults,
            &args.orderBy,
            &args.pageToken,
            &args.returnPartialSuccess,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_backend_buckets_list_usable_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute backend buckets patch.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_backend_buckets_patch(
        &self,
        args: &ComputeBackendBucketsPatchArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_backend_buckets_patch_builder(
            &self.http_client,
            &args.project,
            &args.backendBucket,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_backend_buckets_patch_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute backend buckets set edge security policy.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_backend_buckets_set_edge_security_policy(
        &self,
        args: &ComputeBackendBucketsSetEdgeSecurityPolicyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_backend_buckets_set_edge_security_policy_builder(
            &self.http_client,
            &args.project,
            &args.backendBucket,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_backend_buckets_set_edge_security_policy_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute backend buckets set iam policy.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Policy result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_backend_buckets_set_iam_policy(
        &self,
        args: &ComputeBackendBucketsSetIamPolicyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Policy, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_backend_buckets_set_iam_policy_builder(
            &self.http_client,
            &args.project,
            &args.resource,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_backend_buckets_set_iam_policy_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute backend buckets test iam permissions.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TestPermissionsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_backend_buckets_test_iam_permissions(
        &self,
        args: &ComputeBackendBucketsTestIamPermissionsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TestPermissionsResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_backend_buckets_test_iam_permissions_builder(
            &self.http_client,
            &args.project,
            &args.resource,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_backend_buckets_test_iam_permissions_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute backend buckets update.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_backend_buckets_update(
        &self,
        args: &ComputeBackendBucketsUpdateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_backend_buckets_update_builder(
            &self.http_client,
            &args.project,
            &args.backendBucket,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_backend_buckets_update_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute backend services add signed url key.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_backend_services_add_signed_url_key(
        &self,
        args: &ComputeBackendServicesAddSignedUrlKeyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_backend_services_add_signed_url_key_builder(
            &self.http_client,
            &args.project,
            &args.backendService,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_backend_services_add_signed_url_key_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute backend services aggregated list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the BackendServiceAggregatedList result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_backend_services_aggregated_list(
        &self,
        args: &ComputeBackendServicesAggregatedListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<BackendServiceAggregatedList, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_backend_services_aggregated_list_builder(
            &self.http_client,
            &args.project,
            &args.filter,
            &args.includeAllScopes,
            &args.maxResults,
            &args.orderBy,
            &args.pageToken,
            &args.returnPartialSuccess,
            &args.serviceProjectNumber,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_backend_services_aggregated_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute backend services delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_backend_services_delete(
        &self,
        args: &ComputeBackendServicesDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_backend_services_delete_builder(
            &self.http_client,
            &args.project,
            &args.backendService,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_backend_services_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute backend services delete signed url key.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_backend_services_delete_signed_url_key(
        &self,
        args: &ComputeBackendServicesDeleteSignedUrlKeyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_backend_services_delete_signed_url_key_builder(
            &self.http_client,
            &args.project,
            &args.backendService,
            &args.keyName,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_backend_services_delete_signed_url_key_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute backend services get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the BackendService result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_backend_services_get(
        &self,
        args: &ComputeBackendServicesGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<BackendService, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_backend_services_get_builder(
            &self.http_client,
            &args.project,
            &args.backendService,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_backend_services_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute backend services get effective security policies.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_backend_services_get_effective_security_policies(
        &self,
        args: &ComputeBackendServicesGetEffectiveSecurityPoliciesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_backend_services_get_effective_security_policies_builder(
            &self.http_client,
            &args.project,
            &args.backendService,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_backend_services_get_effective_security_policies_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute backend services get health.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the BackendServiceGroupHealth result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_backend_services_get_health(
        &self,
        args: &ComputeBackendServicesGetHealthArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<BackendServiceGroupHealth, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_backend_services_get_health_builder(
            &self.http_client,
            &args.project,
            &args.backendService,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_backend_services_get_health_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute backend services get iam policy.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Policy result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_backend_services_get_iam_policy(
        &self,
        args: &ComputeBackendServicesGetIamPolicyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Policy, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_backend_services_get_iam_policy_builder(
            &self.http_client,
            &args.project,
            &args.resource,
            &args.optionsRequestedPolicyVersion,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_backend_services_get_iam_policy_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute backend services insert.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_backend_services_insert(
        &self,
        args: &ComputeBackendServicesInsertArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_backend_services_insert_builder(
            &self.http_client,
            &args.project,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_backend_services_insert_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute backend services list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the BackendServiceList result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_backend_services_list(
        &self,
        args: &ComputeBackendServicesListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<BackendServiceList, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_backend_services_list_builder(
            &self.http_client,
            &args.project,
            &args.filter,
            &args.maxResults,
            &args.orderBy,
            &args.pageToken,
            &args.returnPartialSuccess,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_backend_services_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute backend services list usable.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the BackendServiceListUsable result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_backend_services_list_usable(
        &self,
        args: &ComputeBackendServicesListUsableArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<BackendServiceListUsable, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_backend_services_list_usable_builder(
            &self.http_client,
            &args.project,
            &args.filter,
            &args.maxResults,
            &args.orderBy,
            &args.pageToken,
            &args.returnPartialSuccess,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_backend_services_list_usable_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute backend services patch.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_backend_services_patch(
        &self,
        args: &ComputeBackendServicesPatchArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_backend_services_patch_builder(
            &self.http_client,
            &args.project,
            &args.backendService,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_backend_services_patch_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute backend services set edge security policy.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_backend_services_set_edge_security_policy(
        &self,
        args: &ComputeBackendServicesSetEdgeSecurityPolicyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_backend_services_set_edge_security_policy_builder(
            &self.http_client,
            &args.project,
            &args.backendService,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_backend_services_set_edge_security_policy_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute backend services set iam policy.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Policy result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_backend_services_set_iam_policy(
        &self,
        args: &ComputeBackendServicesSetIamPolicyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Policy, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_backend_services_set_iam_policy_builder(
            &self.http_client,
            &args.project,
            &args.resource,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_backend_services_set_iam_policy_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute backend services set security policy.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_backend_services_set_security_policy(
        &self,
        args: &ComputeBackendServicesSetSecurityPolicyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_backend_services_set_security_policy_builder(
            &self.http_client,
            &args.project,
            &args.backendService,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_backend_services_set_security_policy_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute backend services test iam permissions.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TestPermissionsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_backend_services_test_iam_permissions(
        &self,
        args: &ComputeBackendServicesTestIamPermissionsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TestPermissionsResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_backend_services_test_iam_permissions_builder(
            &self.http_client,
            &args.project,
            &args.resource,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_backend_services_test_iam_permissions_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute backend services update.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_backend_services_update(
        &self,
        args: &ComputeBackendServicesUpdateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_backend_services_update_builder(
            &self.http_client,
            &args.project,
            &args.backendService,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_backend_services_update_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute cross site networks delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_cross_site_networks_delete(
        &self,
        args: &ComputeCrossSiteNetworksDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_cross_site_networks_delete_builder(
            &self.http_client,
            &args.project,
            &args.crossSiteNetwork,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_cross_site_networks_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute cross site networks get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the CrossSiteNetwork result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_cross_site_networks_get(
        &self,
        args: &ComputeCrossSiteNetworksGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<CrossSiteNetwork, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_cross_site_networks_get_builder(
            &self.http_client,
            &args.project,
            &args.crossSiteNetwork,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_cross_site_networks_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute cross site networks insert.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_cross_site_networks_insert(
        &self,
        args: &ComputeCrossSiteNetworksInsertArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_cross_site_networks_insert_builder(
            &self.http_client,
            &args.project,
            &args.requestId,
            &args.validateOnly,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_cross_site_networks_insert_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute cross site networks list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the CrossSiteNetworkList result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_cross_site_networks_list(
        &self,
        args: &ComputeCrossSiteNetworksListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<CrossSiteNetworkList, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_cross_site_networks_list_builder(
            &self.http_client,
            &args.project,
            &args.filter,
            &args.maxResults,
            &args.orderBy,
            &args.pageToken,
            &args.returnPartialSuccess,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_cross_site_networks_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute cross site networks patch.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_cross_site_networks_patch(
        &self,
        args: &ComputeCrossSiteNetworksPatchArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_cross_site_networks_patch_builder(
            &self.http_client,
            &args.project,
            &args.crossSiteNetwork,
            &args.requestId,
            &args.updateMask,
            &args.validateOnly,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_cross_site_networks_patch_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute disk types aggregated list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the DiskTypeAggregatedList result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_disk_types_aggregated_list(
        &self,
        args: &ComputeDiskTypesAggregatedListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<DiskTypeAggregatedList, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_disk_types_aggregated_list_builder(
            &self.http_client,
            &args.project,
            &args.filter,
            &args.includeAllScopes,
            &args.maxResults,
            &args.orderBy,
            &args.pageToken,
            &args.returnPartialSuccess,
            &args.serviceProjectNumber,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_disk_types_aggregated_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute disk types get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the DiskType result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_disk_types_get(
        &self,
        args: &ComputeDiskTypesGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<DiskType, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_disk_types_get_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.diskType,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_disk_types_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute disk types list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the DiskTypeList result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_disk_types_list(
        &self,
        args: &ComputeDiskTypesListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<DiskTypeList, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_disk_types_list_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.filter,
            &args.maxResults,
            &args.orderBy,
            &args.pageToken,
            &args.returnPartialSuccess,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_disk_types_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute disks add resource policies.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_disks_add_resource_policies(
        &self,
        args: &ComputeDisksAddResourcePoliciesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_disks_add_resource_policies_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.disk,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_disks_add_resource_policies_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute disks aggregated list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the DiskAggregatedList result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_disks_aggregated_list(
        &self,
        args: &ComputeDisksAggregatedListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<DiskAggregatedList, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_disks_aggregated_list_builder(
            &self.http_client,
            &args.project,
            &args.filter,
            &args.includeAllScopes,
            &args.maxResults,
            &args.orderBy,
            &args.pageToken,
            &args.returnPartialSuccess,
            &args.serviceProjectNumber,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_disks_aggregated_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute disks bulk insert.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_disks_bulk_insert(
        &self,
        args: &ComputeDisksBulkInsertArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_disks_bulk_insert_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_disks_bulk_insert_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute disks bulk set labels.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_disks_bulk_set_labels(
        &self,
        args: &ComputeDisksBulkSetLabelsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_disks_bulk_set_labels_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.requestId,
            &args.resource,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_disks_bulk_set_labels_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute disks create snapshot.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_disks_create_snapshot(
        &self,
        args: &ComputeDisksCreateSnapshotArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_disks_create_snapshot_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.disk,
            &args.guestFlush,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_disks_create_snapshot_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute disks delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_disks_delete(
        &self,
        args: &ComputeDisksDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_disks_delete_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.disk,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_disks_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute disks get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Disk result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_disks_get(
        &self,
        args: &ComputeDisksGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Disk, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_disks_get_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.disk,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_disks_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute disks get iam policy.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Policy result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_disks_get_iam_policy(
        &self,
        args: &ComputeDisksGetIamPolicyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Policy, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_disks_get_iam_policy_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.resource,
            &args.optionsRequestedPolicyVersion,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_disks_get_iam_policy_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute disks insert.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_disks_insert(
        &self,
        args: &ComputeDisksInsertArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_disks_insert_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.requestId,
            &args.sourceImage,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_disks_insert_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute disks list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the DiskList result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_disks_list(
        &self,
        args: &ComputeDisksListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<DiskList, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_disks_list_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.filter,
            &args.maxResults,
            &args.orderBy,
            &args.pageToken,
            &args.returnPartialSuccess,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_disks_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute disks remove resource policies.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_disks_remove_resource_policies(
        &self,
        args: &ComputeDisksRemoveResourcePoliciesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_disks_remove_resource_policies_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.disk,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_disks_remove_resource_policies_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute disks resize.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_disks_resize(
        &self,
        args: &ComputeDisksResizeArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_disks_resize_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.disk,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_disks_resize_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute disks set iam policy.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Policy result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_disks_set_iam_policy(
        &self,
        args: &ComputeDisksSetIamPolicyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Policy, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_disks_set_iam_policy_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.resource,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_disks_set_iam_policy_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute disks set labels.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_disks_set_labels(
        &self,
        args: &ComputeDisksSetLabelsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_disks_set_labels_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.resource,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_disks_set_labels_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute disks start async replication.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_disks_start_async_replication(
        &self,
        args: &ComputeDisksStartAsyncReplicationArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_disks_start_async_replication_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.disk,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_disks_start_async_replication_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute disks stop async replication.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_disks_stop_async_replication(
        &self,
        args: &ComputeDisksStopAsyncReplicationArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_disks_stop_async_replication_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.disk,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_disks_stop_async_replication_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute disks stop group async replication.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_disks_stop_group_async_replication(
        &self,
        args: &ComputeDisksStopGroupAsyncReplicationArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_disks_stop_group_async_replication_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_disks_stop_group_async_replication_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute disks test iam permissions.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TestPermissionsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_disks_test_iam_permissions(
        &self,
        args: &ComputeDisksTestIamPermissionsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TestPermissionsResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_disks_test_iam_permissions_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.resource,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_disks_test_iam_permissions_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute disks update.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_disks_update(
        &self,
        args: &ComputeDisksUpdateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_disks_update_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.disk,
            &args.paths,
            &args.requestId,
            &args.updateMask,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_disks_update_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute disks update kms key.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_disks_update_kms_key(
        &self,
        args: &ComputeDisksUpdateKmsKeyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_disks_update_kms_key_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.disk,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_disks_update_kms_key_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute external vpn gateways delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_external_vpn_gateways_delete(
        &self,
        args: &ComputeExternalVpnGatewaysDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_external_vpn_gateways_delete_builder(
            &self.http_client,
            &args.project,
            &args.externalVpnGateway,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_external_vpn_gateways_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute external vpn gateways get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ExternalVpnGateway result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_external_vpn_gateways_get(
        &self,
        args: &ComputeExternalVpnGatewaysGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ExternalVpnGateway, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_external_vpn_gateways_get_builder(
            &self.http_client,
            &args.project,
            &args.externalVpnGateway,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_external_vpn_gateways_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute external vpn gateways insert.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_external_vpn_gateways_insert(
        &self,
        args: &ComputeExternalVpnGatewaysInsertArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_external_vpn_gateways_insert_builder(
            &self.http_client,
            &args.project,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_external_vpn_gateways_insert_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute external vpn gateways list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ExternalVpnGatewayList result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_external_vpn_gateways_list(
        &self,
        args: &ComputeExternalVpnGatewaysListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ExternalVpnGatewayList, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_external_vpn_gateways_list_builder(
            &self.http_client,
            &args.project,
            &args.filter,
            &args.maxResults,
            &args.orderBy,
            &args.pageToken,
            &args.returnPartialSuccess,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_external_vpn_gateways_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute external vpn gateways set labels.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_external_vpn_gateways_set_labels(
        &self,
        args: &ComputeExternalVpnGatewaysSetLabelsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_external_vpn_gateways_set_labels_builder(
            &self.http_client,
            &args.project,
            &args.resource,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_external_vpn_gateways_set_labels_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute external vpn gateways test iam permissions.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TestPermissionsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_external_vpn_gateways_test_iam_permissions(
        &self,
        args: &ComputeExternalVpnGatewaysTestIamPermissionsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TestPermissionsResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_external_vpn_gateways_test_iam_permissions_builder(
            &self.http_client,
            &args.project,
            &args.resource,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_external_vpn_gateways_test_iam_permissions_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute firewall policies add association.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_firewall_policies_add_association(
        &self,
        args: &ComputeFirewallPoliciesAddAssociationArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_firewall_policies_add_association_builder(
            &self.http_client,
            &args.firewallPolicy,
            &args.replaceExistingAssociation,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_firewall_policies_add_association_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute firewall policies add rule.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_firewall_policies_add_rule(
        &self,
        args: &ComputeFirewallPoliciesAddRuleArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_firewall_policies_add_rule_builder(
            &self.http_client,
            &args.firewallPolicy,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_firewall_policies_add_rule_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute firewall policies clone rules.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_firewall_policies_clone_rules(
        &self,
        args: &ComputeFirewallPoliciesCloneRulesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_firewall_policies_clone_rules_builder(
            &self.http_client,
            &args.firewallPolicy,
            &args.requestId,
            &args.sourceFirewallPolicy,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_firewall_policies_clone_rules_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute firewall policies delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_firewall_policies_delete(
        &self,
        args: &ComputeFirewallPoliciesDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_firewall_policies_delete_builder(
            &self.http_client,
            &args.firewallPolicy,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_firewall_policies_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute firewall policies get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the FirewallPolicy result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_firewall_policies_get(
        &self,
        args: &ComputeFirewallPoliciesGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<FirewallPolicy, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_firewall_policies_get_builder(
            &self.http_client,
            &args.firewallPolicy,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_firewall_policies_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute firewall policies get association.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the FirewallPolicyAssociation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_firewall_policies_get_association(
        &self,
        args: &ComputeFirewallPoliciesGetAssociationArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<FirewallPolicyAssociation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_firewall_policies_get_association_builder(
            &self.http_client,
            &args.firewallPolicy,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_firewall_policies_get_association_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute firewall policies get iam policy.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Policy result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_firewall_policies_get_iam_policy(
        &self,
        args: &ComputeFirewallPoliciesGetIamPolicyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Policy, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_firewall_policies_get_iam_policy_builder(
            &self.http_client,
            &args.resource,
            &args.optionsRequestedPolicyVersion,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_firewall_policies_get_iam_policy_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute firewall policies get rule.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the FirewallPolicyRule result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_firewall_policies_get_rule(
        &self,
        args: &ComputeFirewallPoliciesGetRuleArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<FirewallPolicyRule, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_firewall_policies_get_rule_builder(
            &self.http_client,
            &args.firewallPolicy,
            &args.priority,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_firewall_policies_get_rule_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute firewall policies insert.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_firewall_policies_insert(
        &self,
        args: &ComputeFirewallPoliciesInsertArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_firewall_policies_insert_builder(
            &self.http_client,
            &args.parentId,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_firewall_policies_insert_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute firewall policies list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the FirewallPolicyList result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_firewall_policies_list(
        &self,
        args: &ComputeFirewallPoliciesListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<FirewallPolicyList, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_firewall_policies_list_builder(
            &self.http_client,
            &args.filter,
            &args.maxResults,
            &args.orderBy,
            &args.pageToken,
            &args.parentId,
            &args.returnPartialSuccess,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_firewall_policies_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute firewall policies list associations.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the FirewallPoliciesListAssociationsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_firewall_policies_list_associations(
        &self,
        args: &ComputeFirewallPoliciesListAssociationsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<FirewallPoliciesListAssociationsResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_firewall_policies_list_associations_builder(
            &self.http_client,
            &args.includeInheritedPolicies,
            &args.targetResource,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_firewall_policies_list_associations_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute firewall policies move.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_firewall_policies_move(
        &self,
        args: &ComputeFirewallPoliciesMoveArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_firewall_policies_move_builder(
            &self.http_client,
            &args.firewallPolicy,
            &args.parentId,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_firewall_policies_move_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute firewall policies patch.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_firewall_policies_patch(
        &self,
        args: &ComputeFirewallPoliciesPatchArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_firewall_policies_patch_builder(
            &self.http_client,
            &args.firewallPolicy,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_firewall_policies_patch_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute firewall policies patch rule.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_firewall_policies_patch_rule(
        &self,
        args: &ComputeFirewallPoliciesPatchRuleArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_firewall_policies_patch_rule_builder(
            &self.http_client,
            &args.firewallPolicy,
            &args.priority,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_firewall_policies_patch_rule_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute firewall policies remove association.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_firewall_policies_remove_association(
        &self,
        args: &ComputeFirewallPoliciesRemoveAssociationArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_firewall_policies_remove_association_builder(
            &self.http_client,
            &args.firewallPolicy,
            &args.name,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_firewall_policies_remove_association_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute firewall policies remove rule.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_firewall_policies_remove_rule(
        &self,
        args: &ComputeFirewallPoliciesRemoveRuleArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_firewall_policies_remove_rule_builder(
            &self.http_client,
            &args.firewallPolicy,
            &args.priority,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_firewall_policies_remove_rule_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute firewall policies set iam policy.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Policy result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_firewall_policies_set_iam_policy(
        &self,
        args: &ComputeFirewallPoliciesSetIamPolicyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Policy, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_firewall_policies_set_iam_policy_builder(
            &self.http_client,
            &args.resource,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_firewall_policies_set_iam_policy_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute firewall policies test iam permissions.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TestPermissionsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_firewall_policies_test_iam_permissions(
        &self,
        args: &ComputeFirewallPoliciesTestIamPermissionsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TestPermissionsResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_firewall_policies_test_iam_permissions_builder(
            &self.http_client,
            &args.resource,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_firewall_policies_test_iam_permissions_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute firewalls delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_firewalls_delete(
        &self,
        args: &ComputeFirewallsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_firewalls_delete_builder(
            &self.http_client,
            &args.project,
            &args.firewall,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_firewalls_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute firewalls get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Firewall result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_firewalls_get(
        &self,
        args: &ComputeFirewallsGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Firewall, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_firewalls_get_builder(
            &self.http_client,
            &args.project,
            &args.firewall,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_firewalls_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute firewalls insert.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_firewalls_insert(
        &self,
        args: &ComputeFirewallsInsertArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_firewalls_insert_builder(
            &self.http_client,
            &args.project,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_firewalls_insert_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute firewalls list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the FirewallList result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_firewalls_list(
        &self,
        args: &ComputeFirewallsListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<FirewallList, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_firewalls_list_builder(
            &self.http_client,
            &args.project,
            &args.filter,
            &args.maxResults,
            &args.orderBy,
            &args.pageToken,
            &args.returnPartialSuccess,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_firewalls_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute firewalls patch.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_firewalls_patch(
        &self,
        args: &ComputeFirewallsPatchArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_firewalls_patch_builder(
            &self.http_client,
            &args.project,
            &args.firewall,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_firewalls_patch_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute firewalls test iam permissions.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TestPermissionsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_firewalls_test_iam_permissions(
        &self,
        args: &ComputeFirewallsTestIamPermissionsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TestPermissionsResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_firewalls_test_iam_permissions_builder(
            &self.http_client,
            &args.project,
            &args.resource,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_firewalls_test_iam_permissions_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute firewalls update.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_firewalls_update(
        &self,
        args: &ComputeFirewallsUpdateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_firewalls_update_builder(
            &self.http_client,
            &args.project,
            &args.firewall,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_firewalls_update_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute forwarding rules aggregated list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ForwardingRuleAggregatedList result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_forwarding_rules_aggregated_list(
        &self,
        args: &ComputeForwardingRulesAggregatedListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ForwardingRuleAggregatedList, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_forwarding_rules_aggregated_list_builder(
            &self.http_client,
            &args.project,
            &args.filter,
            &args.includeAllScopes,
            &args.maxResults,
            &args.orderBy,
            &args.pageToken,
            &args.returnPartialSuccess,
            &args.serviceProjectNumber,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_forwarding_rules_aggregated_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute forwarding rules delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_forwarding_rules_delete(
        &self,
        args: &ComputeForwardingRulesDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_forwarding_rules_delete_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.forwardingRule,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_forwarding_rules_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute forwarding rules get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ForwardingRule result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_forwarding_rules_get(
        &self,
        args: &ComputeForwardingRulesGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ForwardingRule, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_forwarding_rules_get_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.forwardingRule,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_forwarding_rules_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute forwarding rules insert.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_forwarding_rules_insert(
        &self,
        args: &ComputeForwardingRulesInsertArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_forwarding_rules_insert_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_forwarding_rules_insert_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute forwarding rules list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ForwardingRuleList result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_forwarding_rules_list(
        &self,
        args: &ComputeForwardingRulesListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ForwardingRuleList, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_forwarding_rules_list_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.filter,
            &args.maxResults,
            &args.orderBy,
            &args.pageToken,
            &args.returnPartialSuccess,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_forwarding_rules_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute forwarding rules patch.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_forwarding_rules_patch(
        &self,
        args: &ComputeForwardingRulesPatchArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_forwarding_rules_patch_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.forwardingRule,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_forwarding_rules_patch_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute forwarding rules set labels.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_forwarding_rules_set_labels(
        &self,
        args: &ComputeForwardingRulesSetLabelsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_forwarding_rules_set_labels_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.resource,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_forwarding_rules_set_labels_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute forwarding rules set target.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_forwarding_rules_set_target(
        &self,
        args: &ComputeForwardingRulesSetTargetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_forwarding_rules_set_target_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.forwardingRule,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_forwarding_rules_set_target_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute future reservations aggregated list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the FutureReservationsAggregatedListResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_future_reservations_aggregated_list(
        &self,
        args: &ComputeFutureReservationsAggregatedListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<FutureReservationsAggregatedListResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_future_reservations_aggregated_list_builder(
            &self.http_client,
            &args.project,
            &args.filter,
            &args.includeAllScopes,
            &args.maxResults,
            &args.orderBy,
            &args.pageToken,
            &args.returnPartialSuccess,
            &args.serviceProjectNumber,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_future_reservations_aggregated_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute future reservations cancel.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_future_reservations_cancel(
        &self,
        args: &ComputeFutureReservationsCancelArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_future_reservations_cancel_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.futureReservation,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_future_reservations_cancel_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute future reservations delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_future_reservations_delete(
        &self,
        args: &ComputeFutureReservationsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_future_reservations_delete_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.futureReservation,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_future_reservations_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute future reservations get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the FutureReservation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_future_reservations_get(
        &self,
        args: &ComputeFutureReservationsGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<FutureReservation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_future_reservations_get_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.futureReservation,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_future_reservations_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute future reservations insert.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_future_reservations_insert(
        &self,
        args: &ComputeFutureReservationsInsertArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_future_reservations_insert_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_future_reservations_insert_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute future reservations list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the FutureReservationsListResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_future_reservations_list(
        &self,
        args: &ComputeFutureReservationsListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<FutureReservationsListResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_future_reservations_list_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.filter,
            &args.maxResults,
            &args.orderBy,
            &args.pageToken,
            &args.returnPartialSuccess,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_future_reservations_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute future reservations update.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_future_reservations_update(
        &self,
        args: &ComputeFutureReservationsUpdateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_future_reservations_update_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.futureReservation,
            &args.requestId,
            &args.updateMask,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_future_reservations_update_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute global addresses delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_global_addresses_delete(
        &self,
        args: &ComputeGlobalAddressesDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_global_addresses_delete_builder(
            &self.http_client,
            &args.project,
            &args.address,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_global_addresses_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute global addresses get.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Address result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_global_addresses_get(
        &self,
        args: &ComputeGlobalAddressesGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Address, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_global_addresses_get_builder(
            &self.http_client,
            &args.project,
            &args.address,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_global_addresses_get_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute global addresses insert.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_global_addresses_insert(
        &self,
        args: &ComputeGlobalAddressesInsertArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_global_addresses_insert_builder(
            &self.http_client,
            &args.project,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_global_addresses_insert_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute global addresses list.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the AddressList result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_global_addresses_list(
        &self,
        args: &ComputeGlobalAddressesListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<AddressList, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_global_addresses_list_builder(
            &self.http_client,
            &args.project,
            &args.filter,
            &args.maxResults,
            &args.orderBy,
            &args.pageToken,
            &args.returnPartialSuccess,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_global_addresses_list_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute global addresses move.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_global_addresses_move(
        &self,
        args: &ComputeGlobalAddressesMoveArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_global_addresses_move_builder(
            &self.http_client,
            &args.project,
            &args.address,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_global_addresses_move_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute global addresses set labels.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_global_addresses_set_labels(
        &self,
        args: &ComputeGlobalAddressesSetLabelsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_global_addresses_set_labels_builder(
            &self.http_client,
            &args.project,
            &args.resource,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_global_addresses_set_labels_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute global addresses test iam permissions.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TestPermissionsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_global_addresses_test_iam_permissions(
        &self,
        args: &ComputeGlobalAddressesTestIamPermissionsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TestPermissionsResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_global_addresses_test_iam_permissions_builder(
            &self.http_client,
            &args.project,
            &args.resource,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_global_addresses_test_iam_permissions_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute global forwarding rules delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_global_forwarding_rules_delete(
        &self,
        args: &ComputeGlobalForwardingRulesDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_global_forwarding_rules_delete_builder(
            &self.http_client,
            &args.project,
            &args.forwardingRule,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_global_forwarding_rules_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute global forwarding rules get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ForwardingRule result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_global_forwarding_rules_get(
        &self,
        args: &ComputeGlobalForwardingRulesGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ForwardingRule, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_global_forwarding_rules_get_builder(
            &self.http_client,
            &args.project,
            &args.forwardingRule,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_global_forwarding_rules_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute global forwarding rules insert.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_global_forwarding_rules_insert(
        &self,
        args: &ComputeGlobalForwardingRulesInsertArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_global_forwarding_rules_insert_builder(
            &self.http_client,
            &args.project,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_global_forwarding_rules_insert_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute global forwarding rules list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ForwardingRuleList result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_global_forwarding_rules_list(
        &self,
        args: &ComputeGlobalForwardingRulesListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ForwardingRuleList, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_global_forwarding_rules_list_builder(
            &self.http_client,
            &args.project,
            &args.filter,
            &args.maxResults,
            &args.orderBy,
            &args.pageToken,
            &args.returnPartialSuccess,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_global_forwarding_rules_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute global forwarding rules patch.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_global_forwarding_rules_patch(
        &self,
        args: &ComputeGlobalForwardingRulesPatchArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_global_forwarding_rules_patch_builder(
            &self.http_client,
            &args.project,
            &args.forwardingRule,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_global_forwarding_rules_patch_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute global forwarding rules set labels.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_global_forwarding_rules_set_labels(
        &self,
        args: &ComputeGlobalForwardingRulesSetLabelsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_global_forwarding_rules_set_labels_builder(
            &self.http_client,
            &args.project,
            &args.resource,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_global_forwarding_rules_set_labels_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute global forwarding rules set target.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_global_forwarding_rules_set_target(
        &self,
        args: &ComputeGlobalForwardingRulesSetTargetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_global_forwarding_rules_set_target_builder(
            &self.http_client,
            &args.project,
            &args.forwardingRule,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_global_forwarding_rules_set_target_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute global network endpoint groups attach network endpoints.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_global_network_endpoint_groups_attach_network_endpoints(
        &self,
        args: &ComputeGlobalNetworkEndpointGroupsAttachNetworkEndpointsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_global_network_endpoint_groups_attach_network_endpoints_builder(
            &self.http_client,
            &args.project,
            &args.networkEndpointGroup,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_global_network_endpoint_groups_attach_network_endpoints_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute global network endpoint groups delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_global_network_endpoint_groups_delete(
        &self,
        args: &ComputeGlobalNetworkEndpointGroupsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_global_network_endpoint_groups_delete_builder(
            &self.http_client,
            &args.project,
            &args.networkEndpointGroup,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_global_network_endpoint_groups_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute global network endpoint groups detach network endpoints.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_global_network_endpoint_groups_detach_network_endpoints(
        &self,
        args: &ComputeGlobalNetworkEndpointGroupsDetachNetworkEndpointsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_global_network_endpoint_groups_detach_network_endpoints_builder(
            &self.http_client,
            &args.project,
            &args.networkEndpointGroup,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_global_network_endpoint_groups_detach_network_endpoints_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute global network endpoint groups get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the NetworkEndpointGroup result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_global_network_endpoint_groups_get(
        &self,
        args: &ComputeGlobalNetworkEndpointGroupsGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<NetworkEndpointGroup, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_global_network_endpoint_groups_get_builder(
            &self.http_client,
            &args.project,
            &args.networkEndpointGroup,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_global_network_endpoint_groups_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute global network endpoint groups insert.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_global_network_endpoint_groups_insert(
        &self,
        args: &ComputeGlobalNetworkEndpointGroupsInsertArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_global_network_endpoint_groups_insert_builder(
            &self.http_client,
            &args.project,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_global_network_endpoint_groups_insert_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute global network endpoint groups list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the NetworkEndpointGroupList result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_global_network_endpoint_groups_list(
        &self,
        args: &ComputeGlobalNetworkEndpointGroupsListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<NetworkEndpointGroupList, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_global_network_endpoint_groups_list_builder(
            &self.http_client,
            &args.project,
            &args.filter,
            &args.maxResults,
            &args.orderBy,
            &args.pageToken,
            &args.returnPartialSuccess,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_global_network_endpoint_groups_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute global network endpoint groups list network endpoints.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the NetworkEndpointGroupsListNetworkEndpoints result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_global_network_endpoint_groups_list_network_endpoints(
        &self,
        args: &ComputeGlobalNetworkEndpointGroupsListNetworkEndpointsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<NetworkEndpointGroupsListNetworkEndpoints, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_global_network_endpoint_groups_list_network_endpoints_builder(
            &self.http_client,
            &args.project,
            &args.networkEndpointGroup,
            &args.filter,
            &args.maxResults,
            &args.orderBy,
            &args.pageToken,
            &args.returnPartialSuccess,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_global_network_endpoint_groups_list_network_endpoints_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute global operations aggregated list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the OperationAggregatedList result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_global_operations_aggregated_list(
        &self,
        args: &ComputeGlobalOperationsAggregatedListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<OperationAggregatedList, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_global_operations_aggregated_list_builder(
            &self.http_client,
            &args.project,
            &args.filter,
            &args.includeAllScopes,
            &args.maxResults,
            &args.orderBy,
            &args.pageToken,
            &args.returnPartialSuccess,
            &args.serviceProjectNumber,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_global_operations_aggregated_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute global operations delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_global_operations_delete(
        &self,
        args: &ComputeGlobalOperationsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_global_operations_delete_builder(
            &self.http_client,
            &args.project,
            &args.operation,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_global_operations_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute global operations get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_global_operations_get(
        &self,
        args: &ComputeGlobalOperationsGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_global_operations_get_builder(
            &self.http_client,
            &args.project,
            &args.operation,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_global_operations_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute global operations list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the OperationList result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_global_operations_list(
        &self,
        args: &ComputeGlobalOperationsListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<OperationList, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_global_operations_list_builder(
            &self.http_client,
            &args.project,
            &args.filter,
            &args.maxResults,
            &args.orderBy,
            &args.pageToken,
            &args.returnPartialSuccess,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_global_operations_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute global operations wait.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_global_operations_wait(
        &self,
        args: &ComputeGlobalOperationsWaitArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_global_operations_wait_builder(
            &self.http_client,
            &args.project,
            &args.operation,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_global_operations_wait_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute global organization operations delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_global_organization_operations_delete(
        &self,
        args: &ComputeGlobalOrganizationOperationsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_global_organization_operations_delete_builder(
            &self.http_client,
            &args.operation,
            &args.parentId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_global_organization_operations_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute global organization operations get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_global_organization_operations_get(
        &self,
        args: &ComputeGlobalOrganizationOperationsGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_global_organization_operations_get_builder(
            &self.http_client,
            &args.operation,
            &args.parentId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_global_organization_operations_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute global organization operations list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the OperationList result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_global_organization_operations_list(
        &self,
        args: &ComputeGlobalOrganizationOperationsListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<OperationList, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_global_organization_operations_list_builder(
            &self.http_client,
            &args.filter,
            &args.maxResults,
            &args.orderBy,
            &args.pageToken,
            &args.parentId,
            &args.returnPartialSuccess,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_global_organization_operations_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute global public delegated prefixes delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_global_public_delegated_prefixes_delete(
        &self,
        args: &ComputeGlobalPublicDelegatedPrefixesDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_global_public_delegated_prefixes_delete_builder(
            &self.http_client,
            &args.project,
            &args.publicDelegatedPrefix,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_global_public_delegated_prefixes_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute global public delegated prefixes get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the PublicDelegatedPrefix result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_global_public_delegated_prefixes_get(
        &self,
        args: &ComputeGlobalPublicDelegatedPrefixesGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<PublicDelegatedPrefix, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_global_public_delegated_prefixes_get_builder(
            &self.http_client,
            &args.project,
            &args.publicDelegatedPrefix,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_global_public_delegated_prefixes_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute global public delegated prefixes insert.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_global_public_delegated_prefixes_insert(
        &self,
        args: &ComputeGlobalPublicDelegatedPrefixesInsertArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_global_public_delegated_prefixes_insert_builder(
            &self.http_client,
            &args.project,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_global_public_delegated_prefixes_insert_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute global public delegated prefixes list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the PublicDelegatedPrefixList result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_global_public_delegated_prefixes_list(
        &self,
        args: &ComputeGlobalPublicDelegatedPrefixesListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<PublicDelegatedPrefixList, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_global_public_delegated_prefixes_list_builder(
            &self.http_client,
            &args.project,
            &args.filter,
            &args.maxResults,
            &args.orderBy,
            &args.pageToken,
            &args.returnPartialSuccess,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_global_public_delegated_prefixes_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute global public delegated prefixes patch.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_global_public_delegated_prefixes_patch(
        &self,
        args: &ComputeGlobalPublicDelegatedPrefixesPatchArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_global_public_delegated_prefixes_patch_builder(
            &self.http_client,
            &args.project,
            &args.publicDelegatedPrefix,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_global_public_delegated_prefixes_patch_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute health checks aggregated list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the HealthChecksAggregatedList result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_health_checks_aggregated_list(
        &self,
        args: &ComputeHealthChecksAggregatedListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<HealthChecksAggregatedList, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_health_checks_aggregated_list_builder(
            &self.http_client,
            &args.project,
            &args.filter,
            &args.includeAllScopes,
            &args.maxResults,
            &args.orderBy,
            &args.pageToken,
            &args.returnPartialSuccess,
            &args.serviceProjectNumber,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_health_checks_aggregated_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute health checks delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_health_checks_delete(
        &self,
        args: &ComputeHealthChecksDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_health_checks_delete_builder(
            &self.http_client,
            &args.project,
            &args.healthCheck,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_health_checks_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute health checks get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the HealthCheck result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_health_checks_get(
        &self,
        args: &ComputeHealthChecksGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<HealthCheck, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_health_checks_get_builder(
            &self.http_client,
            &args.project,
            &args.healthCheck,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_health_checks_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute health checks insert.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_health_checks_insert(
        &self,
        args: &ComputeHealthChecksInsertArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_health_checks_insert_builder(
            &self.http_client,
            &args.project,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_health_checks_insert_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute health checks list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the HealthCheckList result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_health_checks_list(
        &self,
        args: &ComputeHealthChecksListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<HealthCheckList, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_health_checks_list_builder(
            &self.http_client,
            &args.project,
            &args.filter,
            &args.maxResults,
            &args.orderBy,
            &args.pageToken,
            &args.returnPartialSuccess,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_health_checks_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute health checks patch.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_health_checks_patch(
        &self,
        args: &ComputeHealthChecksPatchArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_health_checks_patch_builder(
            &self.http_client,
            &args.project,
            &args.healthCheck,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_health_checks_patch_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute health checks test iam permissions.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TestPermissionsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_health_checks_test_iam_permissions(
        &self,
        args: &ComputeHealthChecksTestIamPermissionsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TestPermissionsResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_health_checks_test_iam_permissions_builder(
            &self.http_client,
            &args.project,
            &args.resource,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_health_checks_test_iam_permissions_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute health checks update.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_health_checks_update(
        &self,
        args: &ComputeHealthChecksUpdateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_health_checks_update_builder(
            &self.http_client,
            &args.project,
            &args.healthCheck,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_health_checks_update_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute http health checks delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_http_health_checks_delete(
        &self,
        args: &ComputeHttpHealthChecksDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_http_health_checks_delete_builder(
            &self.http_client,
            &args.project,
            &args.httpHealthCheck,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_http_health_checks_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute http health checks get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the HttpHealthCheck result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_http_health_checks_get(
        &self,
        args: &ComputeHttpHealthChecksGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<HttpHealthCheck, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_http_health_checks_get_builder(
            &self.http_client,
            &args.project,
            &args.httpHealthCheck,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_http_health_checks_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute http health checks insert.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_http_health_checks_insert(
        &self,
        args: &ComputeHttpHealthChecksInsertArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_http_health_checks_insert_builder(
            &self.http_client,
            &args.project,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_http_health_checks_insert_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute http health checks list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the HttpHealthCheckList result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_http_health_checks_list(
        &self,
        args: &ComputeHttpHealthChecksListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<HttpHealthCheckList, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_http_health_checks_list_builder(
            &self.http_client,
            &args.project,
            &args.filter,
            &args.maxResults,
            &args.orderBy,
            &args.pageToken,
            &args.returnPartialSuccess,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_http_health_checks_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute http health checks patch.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_http_health_checks_patch(
        &self,
        args: &ComputeHttpHealthChecksPatchArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_http_health_checks_patch_builder(
            &self.http_client,
            &args.project,
            &args.httpHealthCheck,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_http_health_checks_patch_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute http health checks test iam permissions.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TestPermissionsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_http_health_checks_test_iam_permissions(
        &self,
        args: &ComputeHttpHealthChecksTestIamPermissionsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TestPermissionsResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_http_health_checks_test_iam_permissions_builder(
            &self.http_client,
            &args.project,
            &args.resource,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_http_health_checks_test_iam_permissions_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute http health checks update.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_http_health_checks_update(
        &self,
        args: &ComputeHttpHealthChecksUpdateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_http_health_checks_update_builder(
            &self.http_client,
            &args.project,
            &args.httpHealthCheck,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_http_health_checks_update_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute https health checks delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_https_health_checks_delete(
        &self,
        args: &ComputeHttpsHealthChecksDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_https_health_checks_delete_builder(
            &self.http_client,
            &args.project,
            &args.httpsHealthCheck,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_https_health_checks_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute https health checks get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the HttpsHealthCheck result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_https_health_checks_get(
        &self,
        args: &ComputeHttpsHealthChecksGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<HttpsHealthCheck, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_https_health_checks_get_builder(
            &self.http_client,
            &args.project,
            &args.httpsHealthCheck,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_https_health_checks_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute https health checks insert.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_https_health_checks_insert(
        &self,
        args: &ComputeHttpsHealthChecksInsertArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_https_health_checks_insert_builder(
            &self.http_client,
            &args.project,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_https_health_checks_insert_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute https health checks list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the HttpsHealthCheckList result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_https_health_checks_list(
        &self,
        args: &ComputeHttpsHealthChecksListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<HttpsHealthCheckList, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_https_health_checks_list_builder(
            &self.http_client,
            &args.project,
            &args.filter,
            &args.maxResults,
            &args.orderBy,
            &args.pageToken,
            &args.returnPartialSuccess,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_https_health_checks_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute https health checks patch.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_https_health_checks_patch(
        &self,
        args: &ComputeHttpsHealthChecksPatchArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_https_health_checks_patch_builder(
            &self.http_client,
            &args.project,
            &args.httpsHealthCheck,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_https_health_checks_patch_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute https health checks test iam permissions.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TestPermissionsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_https_health_checks_test_iam_permissions(
        &self,
        args: &ComputeHttpsHealthChecksTestIamPermissionsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TestPermissionsResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_https_health_checks_test_iam_permissions_builder(
            &self.http_client,
            &args.project,
            &args.resource,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_https_health_checks_test_iam_permissions_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute https health checks update.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_https_health_checks_update(
        &self,
        args: &ComputeHttpsHealthChecksUpdateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_https_health_checks_update_builder(
            &self.http_client,
            &args.project,
            &args.httpsHealthCheck,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_https_health_checks_update_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute image family views get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ImageFamilyView result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_image_family_views_get(
        &self,
        args: &ComputeImageFamilyViewsGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ImageFamilyView, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_image_family_views_get_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.family,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_image_family_views_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute images delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_images_delete(
        &self,
        args: &ComputeImagesDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_images_delete_builder(
            &self.http_client,
            &args.project,
            &args.image,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_images_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute images deprecate.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_images_deprecate(
        &self,
        args: &ComputeImagesDeprecateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_images_deprecate_builder(
            &self.http_client,
            &args.project,
            &args.image,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_images_deprecate_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute images get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Image result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_images_get(
        &self,
        args: &ComputeImagesGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Image, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_images_get_builder(
            &self.http_client,
            &args.project,
            &args.image,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_images_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute images get from family.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Image result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_images_get_from_family(
        &self,
        args: &ComputeImagesGetFromFamilyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Image, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_images_get_from_family_builder(
            &self.http_client,
            &args.project,
            &args.family,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_images_get_from_family_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute images get iam policy.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Policy result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_images_get_iam_policy(
        &self,
        args: &ComputeImagesGetIamPolicyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Policy, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_images_get_iam_policy_builder(
            &self.http_client,
            &args.project,
            &args.resource,
            &args.optionsRequestedPolicyVersion,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_images_get_iam_policy_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute images insert.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_images_insert(
        &self,
        args: &ComputeImagesInsertArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_images_insert_builder(
            &self.http_client,
            &args.project,
            &args.forceCreate,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_images_insert_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute images list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ImageList result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_images_list(
        &self,
        args: &ComputeImagesListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ImageList, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_images_list_builder(
            &self.http_client,
            &args.project,
            &args.filter,
            &args.maxResults,
            &args.orderBy,
            &args.pageToken,
            &args.returnPartialSuccess,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_images_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute images patch.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_images_patch(
        &self,
        args: &ComputeImagesPatchArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_images_patch_builder(
            &self.http_client,
            &args.project,
            &args.image,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_images_patch_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute images set iam policy.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Policy result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_images_set_iam_policy(
        &self,
        args: &ComputeImagesSetIamPolicyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Policy, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_images_set_iam_policy_builder(
            &self.http_client,
            &args.project,
            &args.resource,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_images_set_iam_policy_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute images set labels.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_images_set_labels(
        &self,
        args: &ComputeImagesSetLabelsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_images_set_labels_builder(
            &self.http_client,
            &args.project,
            &args.resource,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_images_set_labels_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute images test iam permissions.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TestPermissionsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_images_test_iam_permissions(
        &self,
        args: &ComputeImagesTestIamPermissionsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TestPermissionsResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_images_test_iam_permissions_builder(
            &self.http_client,
            &args.project,
            &args.resource,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_images_test_iam_permissions_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute instance group manager resize requests cancel.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_instance_group_manager_resize_requests_cancel(
        &self,
        args: &ComputeInstanceGroupManagerResizeRequestsCancelArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_instance_group_manager_resize_requests_cancel_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.instanceGroupManager,
            &args.resizeRequest,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_instance_group_manager_resize_requests_cancel_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute instance group manager resize requests delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_instance_group_manager_resize_requests_delete(
        &self,
        args: &ComputeInstanceGroupManagerResizeRequestsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_instance_group_manager_resize_requests_delete_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.instanceGroupManager,
            &args.resizeRequest,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_instance_group_manager_resize_requests_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute instance group manager resize requests get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the InstanceGroupManagerResizeRequest result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_instance_group_manager_resize_requests_get(
        &self,
        args: &ComputeInstanceGroupManagerResizeRequestsGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<InstanceGroupManagerResizeRequest, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_instance_group_manager_resize_requests_get_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.instanceGroupManager,
            &args.resizeRequest,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_instance_group_manager_resize_requests_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute instance group manager resize requests insert.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_instance_group_manager_resize_requests_insert(
        &self,
        args: &ComputeInstanceGroupManagerResizeRequestsInsertArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_instance_group_manager_resize_requests_insert_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.instanceGroupManager,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_instance_group_manager_resize_requests_insert_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute instance group manager resize requests list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the InstanceGroupManagerResizeRequestsListResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_instance_group_manager_resize_requests_list(
        &self,
        args: &ComputeInstanceGroupManagerResizeRequestsListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<InstanceGroupManagerResizeRequestsListResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_instance_group_manager_resize_requests_list_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.instanceGroupManager,
            &args.filter,
            &args.maxResults,
            &args.orderBy,
            &args.pageToken,
            &args.returnPartialSuccess,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_instance_group_manager_resize_requests_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute instance group managers abandon instances.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_instance_group_managers_abandon_instances(
        &self,
        args: &ComputeInstanceGroupManagersAbandonInstancesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_instance_group_managers_abandon_instances_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.instanceGroupManager,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_instance_group_managers_abandon_instances_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute instance group managers aggregated list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the InstanceGroupManagerAggregatedList result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_instance_group_managers_aggregated_list(
        &self,
        args: &ComputeInstanceGroupManagersAggregatedListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<InstanceGroupManagerAggregatedList, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_instance_group_managers_aggregated_list_builder(
            &self.http_client,
            &args.project,
            &args.filter,
            &args.includeAllScopes,
            &args.maxResults,
            &args.orderBy,
            &args.pageToken,
            &args.returnPartialSuccess,
            &args.serviceProjectNumber,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_instance_group_managers_aggregated_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute instance group managers apply updates to instances.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_instance_group_managers_apply_updates_to_instances(
        &self,
        args: &ComputeInstanceGroupManagersApplyUpdatesToInstancesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_instance_group_managers_apply_updates_to_instances_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.instanceGroupManager,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_instance_group_managers_apply_updates_to_instances_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute instance group managers create instances.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_instance_group_managers_create_instances(
        &self,
        args: &ComputeInstanceGroupManagersCreateInstancesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_instance_group_managers_create_instances_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.instanceGroupManager,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_instance_group_managers_create_instances_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute instance group managers delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_instance_group_managers_delete(
        &self,
        args: &ComputeInstanceGroupManagersDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_instance_group_managers_delete_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.instanceGroupManager,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_instance_group_managers_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute instance group managers delete instances.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_instance_group_managers_delete_instances(
        &self,
        args: &ComputeInstanceGroupManagersDeleteInstancesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_instance_group_managers_delete_instances_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.instanceGroupManager,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_instance_group_managers_delete_instances_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute instance group managers delete per instance configs.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_instance_group_managers_delete_per_instance_configs(
        &self,
        args: &ComputeInstanceGroupManagersDeletePerInstanceConfigsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_instance_group_managers_delete_per_instance_configs_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.instanceGroupManager,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_instance_group_managers_delete_per_instance_configs_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute instance group managers get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the InstanceGroupManager result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_instance_group_managers_get(
        &self,
        args: &ComputeInstanceGroupManagersGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<InstanceGroupManager, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_instance_group_managers_get_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.instanceGroupManager,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_instance_group_managers_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute instance group managers insert.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_instance_group_managers_insert(
        &self,
        args: &ComputeInstanceGroupManagersInsertArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_instance_group_managers_insert_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_instance_group_managers_insert_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute instance group managers list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the InstanceGroupManagerList result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_instance_group_managers_list(
        &self,
        args: &ComputeInstanceGroupManagersListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<InstanceGroupManagerList, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_instance_group_managers_list_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.filter,
            &args.maxResults,
            &args.orderBy,
            &args.pageToken,
            &args.returnPartialSuccess,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_instance_group_managers_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute instance group managers list errors.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the InstanceGroupManagersListErrorsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_instance_group_managers_list_errors(
        &self,
        args: &ComputeInstanceGroupManagersListErrorsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<InstanceGroupManagersListErrorsResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_instance_group_managers_list_errors_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.instanceGroupManager,
            &args.filter,
            &args.maxResults,
            &args.orderBy,
            &args.pageToken,
            &args.returnPartialSuccess,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_instance_group_managers_list_errors_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute instance group managers list managed instances.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the InstanceGroupManagersListManagedInstancesResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_instance_group_managers_list_managed_instances(
        &self,
        args: &ComputeInstanceGroupManagersListManagedInstancesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<InstanceGroupManagersListManagedInstancesResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_instance_group_managers_list_managed_instances_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.instanceGroupManager,
            &args.filter,
            &args.maxResults,
            &args.orderBy,
            &args.pageToken,
            &args.returnPartialSuccess,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_instance_group_managers_list_managed_instances_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute instance group managers list per instance configs.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the InstanceGroupManagersListPerInstanceConfigsResp result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_instance_group_managers_list_per_instance_configs(
        &self,
        args: &ComputeInstanceGroupManagersListPerInstanceConfigsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<InstanceGroupManagersListPerInstanceConfigsResp, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_instance_group_managers_list_per_instance_configs_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.instanceGroupManager,
            &args.filter,
            &args.maxResults,
            &args.orderBy,
            &args.pageToken,
            &args.returnPartialSuccess,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_instance_group_managers_list_per_instance_configs_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute instance group managers patch.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_instance_group_managers_patch(
        &self,
        args: &ComputeInstanceGroupManagersPatchArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_instance_group_managers_patch_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.instanceGroupManager,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_instance_group_managers_patch_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute instance group managers patch per instance configs.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_instance_group_managers_patch_per_instance_configs(
        &self,
        args: &ComputeInstanceGroupManagersPatchPerInstanceConfigsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_instance_group_managers_patch_per_instance_configs_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.instanceGroupManager,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_instance_group_managers_patch_per_instance_configs_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute instance group managers recreate instances.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_instance_group_managers_recreate_instances(
        &self,
        args: &ComputeInstanceGroupManagersRecreateInstancesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_instance_group_managers_recreate_instances_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.instanceGroupManager,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_instance_group_managers_recreate_instances_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute instance group managers resize.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_instance_group_managers_resize(
        &self,
        args: &ComputeInstanceGroupManagersResizeArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_instance_group_managers_resize_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.instanceGroupManager,
            &args.requestId,
            &args.size,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_instance_group_managers_resize_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute instance group managers resume instances.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_instance_group_managers_resume_instances(
        &self,
        args: &ComputeInstanceGroupManagersResumeInstancesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_instance_group_managers_resume_instances_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.instanceGroupManager,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_instance_group_managers_resume_instances_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute instance group managers set instance template.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_instance_group_managers_set_instance_template(
        &self,
        args: &ComputeInstanceGroupManagersSetInstanceTemplateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_instance_group_managers_set_instance_template_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.instanceGroupManager,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_instance_group_managers_set_instance_template_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute instance group managers set target pools.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_instance_group_managers_set_target_pools(
        &self,
        args: &ComputeInstanceGroupManagersSetTargetPoolsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_instance_group_managers_set_target_pools_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.instanceGroupManager,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_instance_group_managers_set_target_pools_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute instance group managers start instances.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_instance_group_managers_start_instances(
        &self,
        args: &ComputeInstanceGroupManagersStartInstancesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_instance_group_managers_start_instances_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.instanceGroupManager,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_instance_group_managers_start_instances_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute instance group managers stop instances.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_instance_group_managers_stop_instances(
        &self,
        args: &ComputeInstanceGroupManagersStopInstancesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_instance_group_managers_stop_instances_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.instanceGroupManager,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_instance_group_managers_stop_instances_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute instance group managers suspend instances.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_instance_group_managers_suspend_instances(
        &self,
        args: &ComputeInstanceGroupManagersSuspendInstancesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_instance_group_managers_suspend_instances_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.instanceGroupManager,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_instance_group_managers_suspend_instances_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute instance group managers update per instance configs.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_instance_group_managers_update_per_instance_configs(
        &self,
        args: &ComputeInstanceGroupManagersUpdatePerInstanceConfigsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_instance_group_managers_update_per_instance_configs_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.instanceGroupManager,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_instance_group_managers_update_per_instance_configs_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute instance groups add instances.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_instance_groups_add_instances(
        &self,
        args: &ComputeInstanceGroupsAddInstancesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_instance_groups_add_instances_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.instanceGroup,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_instance_groups_add_instances_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute instance groups aggregated list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the InstanceGroupAggregatedList result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_instance_groups_aggregated_list(
        &self,
        args: &ComputeInstanceGroupsAggregatedListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<InstanceGroupAggregatedList, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_instance_groups_aggregated_list_builder(
            &self.http_client,
            &args.project,
            &args.filter,
            &args.includeAllScopes,
            &args.maxResults,
            &args.orderBy,
            &args.pageToken,
            &args.returnPartialSuccess,
            &args.serviceProjectNumber,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_instance_groups_aggregated_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute instance groups delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_instance_groups_delete(
        &self,
        args: &ComputeInstanceGroupsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_instance_groups_delete_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.instanceGroup,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_instance_groups_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute instance groups get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the InstanceGroup result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_instance_groups_get(
        &self,
        args: &ComputeInstanceGroupsGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<InstanceGroup, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_instance_groups_get_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.instanceGroup,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_instance_groups_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute instance groups insert.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_instance_groups_insert(
        &self,
        args: &ComputeInstanceGroupsInsertArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_instance_groups_insert_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_instance_groups_insert_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute instance groups list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the InstanceGroupList result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_instance_groups_list(
        &self,
        args: &ComputeInstanceGroupsListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<InstanceGroupList, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_instance_groups_list_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.filter,
            &args.maxResults,
            &args.orderBy,
            &args.pageToken,
            &args.returnPartialSuccess,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_instance_groups_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute instance groups list instances.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the InstanceGroupsListInstances result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_instance_groups_list_instances(
        &self,
        args: &ComputeInstanceGroupsListInstancesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<InstanceGroupsListInstances, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_instance_groups_list_instances_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.instanceGroup,
            &args.filter,
            &args.maxResults,
            &args.orderBy,
            &args.pageToken,
            &args.returnPartialSuccess,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_instance_groups_list_instances_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute instance groups remove instances.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_instance_groups_remove_instances(
        &self,
        args: &ComputeInstanceGroupsRemoveInstancesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_instance_groups_remove_instances_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.instanceGroup,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_instance_groups_remove_instances_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute instance groups set named ports.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_instance_groups_set_named_ports(
        &self,
        args: &ComputeInstanceGroupsSetNamedPortsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_instance_groups_set_named_ports_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.instanceGroup,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_instance_groups_set_named_ports_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute instance groups test iam permissions.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TestPermissionsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_instance_groups_test_iam_permissions(
        &self,
        args: &ComputeInstanceGroupsTestIamPermissionsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TestPermissionsResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_instance_groups_test_iam_permissions_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.resource,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_instance_groups_test_iam_permissions_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute instance settings get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the InstanceSettings result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_instance_settings_get(
        &self,
        args: &ComputeInstanceSettingsGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<InstanceSettings, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_instance_settings_get_builder(
            &self.http_client,
            &args.project,
            &args.zone,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_instance_settings_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute instance settings patch.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_instance_settings_patch(
        &self,
        args: &ComputeInstanceSettingsPatchArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_instance_settings_patch_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.requestId,
            &args.updateMask,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_instance_settings_patch_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute instance templates aggregated list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the InstanceTemplateAggregatedList result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_instance_templates_aggregated_list(
        &self,
        args: &ComputeInstanceTemplatesAggregatedListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<InstanceTemplateAggregatedList, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_instance_templates_aggregated_list_builder(
            &self.http_client,
            &args.project,
            &args.filter,
            &args.includeAllScopes,
            &args.maxResults,
            &args.orderBy,
            &args.pageToken,
            &args.returnPartialSuccess,
            &args.serviceProjectNumber,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_instance_templates_aggregated_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute instance templates delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_instance_templates_delete(
        &self,
        args: &ComputeInstanceTemplatesDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_instance_templates_delete_builder(
            &self.http_client,
            &args.project,
            &args.instanceTemplate,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_instance_templates_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute instance templates get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the InstanceTemplate result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_instance_templates_get(
        &self,
        args: &ComputeInstanceTemplatesGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<InstanceTemplate, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_instance_templates_get_builder(
            &self.http_client,
            &args.project,
            &args.instanceTemplate,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_instance_templates_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute instance templates get iam policy.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Policy result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_instance_templates_get_iam_policy(
        &self,
        args: &ComputeInstanceTemplatesGetIamPolicyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Policy, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_instance_templates_get_iam_policy_builder(
            &self.http_client,
            &args.project,
            &args.resource,
            &args.optionsRequestedPolicyVersion,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_instance_templates_get_iam_policy_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute instance templates insert.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_instance_templates_insert(
        &self,
        args: &ComputeInstanceTemplatesInsertArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_instance_templates_insert_builder(
            &self.http_client,
            &args.project,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_instance_templates_insert_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute instance templates list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the InstanceTemplateList result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_instance_templates_list(
        &self,
        args: &ComputeInstanceTemplatesListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<InstanceTemplateList, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_instance_templates_list_builder(
            &self.http_client,
            &args.project,
            &args.filter,
            &args.maxResults,
            &args.orderBy,
            &args.pageToken,
            &args.returnPartialSuccess,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_instance_templates_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute instance templates set iam policy.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Policy result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_instance_templates_set_iam_policy(
        &self,
        args: &ComputeInstanceTemplatesSetIamPolicyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Policy, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_instance_templates_set_iam_policy_builder(
            &self.http_client,
            &args.project,
            &args.resource,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_instance_templates_set_iam_policy_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute instance templates test iam permissions.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TestPermissionsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_instance_templates_test_iam_permissions(
        &self,
        args: &ComputeInstanceTemplatesTestIamPermissionsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TestPermissionsResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_instance_templates_test_iam_permissions_builder(
            &self.http_client,
            &args.project,
            &args.resource,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_instance_templates_test_iam_permissions_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute instances add access config.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_instances_add_access_config(
        &self,
        args: &ComputeInstancesAddAccessConfigArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_instances_add_access_config_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.instance,
            &args.networkInterface,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_instances_add_access_config_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute instances add network interface.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_instances_add_network_interface(
        &self,
        args: &ComputeInstancesAddNetworkInterfaceArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_instances_add_network_interface_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.instance,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_instances_add_network_interface_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute instances add resource policies.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_instances_add_resource_policies(
        &self,
        args: &ComputeInstancesAddResourcePoliciesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_instances_add_resource_policies_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.instance,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_instances_add_resource_policies_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute instances aggregated list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the InstanceAggregatedList result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_instances_aggregated_list(
        &self,
        args: &ComputeInstancesAggregatedListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<InstanceAggregatedList, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_instances_aggregated_list_builder(
            &self.http_client,
            &args.project,
            &args.filter,
            &args.includeAllScopes,
            &args.maxResults,
            &args.orderBy,
            &args.pageToken,
            &args.returnPartialSuccess,
            &args.serviceProjectNumber,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_instances_aggregated_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute instances attach disk.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_instances_attach_disk(
        &self,
        args: &ComputeInstancesAttachDiskArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_instances_attach_disk_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.instance,
            &args.forceAttach,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_instances_attach_disk_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute instances bulk insert.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_instances_bulk_insert(
        &self,
        args: &ComputeInstancesBulkInsertArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_instances_bulk_insert_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_instances_bulk_insert_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute instances delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_instances_delete(
        &self,
        args: &ComputeInstancesDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_instances_delete_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.instance,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_instances_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute instances delete access config.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_instances_delete_access_config(
        &self,
        args: &ComputeInstancesDeleteAccessConfigArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_instances_delete_access_config_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.instance,
            &args.accessConfig,
            &args.networkInterface,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_instances_delete_access_config_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute instances delete network interface.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_instances_delete_network_interface(
        &self,
        args: &ComputeInstancesDeleteNetworkInterfaceArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_instances_delete_network_interface_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.instance,
            &args.networkInterfaceName,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_instances_delete_network_interface_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute instances detach disk.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_instances_detach_disk(
        &self,
        args: &ComputeInstancesDetachDiskArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_instances_detach_disk_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.instance,
            &args.deviceName,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_instances_detach_disk_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute instances get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Instance result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_instances_get(
        &self,
        args: &ComputeInstancesGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Instance, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_instances_get_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.instance,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_instances_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute instances get effective firewalls.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the InstancesGetEffectiveFirewallsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_instances_get_effective_firewalls(
        &self,
        args: &ComputeInstancesGetEffectiveFirewallsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<InstancesGetEffectiveFirewallsResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_instances_get_effective_firewalls_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.instance,
            &args.networkInterface,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_instances_get_effective_firewalls_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute instances get guest attributes.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the GuestAttributes result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_instances_get_guest_attributes(
        &self,
        args: &ComputeInstancesGetGuestAttributesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<GuestAttributes, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_instances_get_guest_attributes_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.instance,
            &args.queryPath,
            &args.variableKey,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_instances_get_guest_attributes_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute instances get iam policy.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Policy result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_instances_get_iam_policy(
        &self,
        args: &ComputeInstancesGetIamPolicyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Policy, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_instances_get_iam_policy_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.resource,
            &args.optionsRequestedPolicyVersion,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_instances_get_iam_policy_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute instances get screenshot.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Screenshot result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_instances_get_screenshot(
        &self,
        args: &ComputeInstancesGetScreenshotArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Screenshot, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_instances_get_screenshot_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.instance,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_instances_get_screenshot_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute instances get serial port output.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the SerialPortOutput result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_instances_get_serial_port_output(
        &self,
        args: &ComputeInstancesGetSerialPortOutputArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<SerialPortOutput, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_instances_get_serial_port_output_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.instance,
            &args.port,
            &args.start,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_instances_get_serial_port_output_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute instances get shielded instance identity.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ShieldedInstanceIdentity result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_instances_get_shielded_instance_identity(
        &self,
        args: &ComputeInstancesGetShieldedInstanceIdentityArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ShieldedInstanceIdentity, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_instances_get_shielded_instance_identity_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.instance,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_instances_get_shielded_instance_identity_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute instances insert.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_instances_insert(
        &self,
        args: &ComputeInstancesInsertArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_instances_insert_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.requestId,
            &args.sourceInstanceTemplate,
            &args.sourceMachineImage,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_instances_insert_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute instances list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the InstanceList result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_instances_list(
        &self,
        args: &ComputeInstancesListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<InstanceList, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_instances_list_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.filter,
            &args.maxResults,
            &args.orderBy,
            &args.pageToken,
            &args.returnPartialSuccess,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_instances_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute instances list referrers.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the InstanceListReferrers result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_instances_list_referrers(
        &self,
        args: &ComputeInstancesListReferrersArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<InstanceListReferrers, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_instances_list_referrers_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.instance,
            &args.filter,
            &args.maxResults,
            &args.orderBy,
            &args.pageToken,
            &args.returnPartialSuccess,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_instances_list_referrers_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute instances perform maintenance.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_instances_perform_maintenance(
        &self,
        args: &ComputeInstancesPerformMaintenanceArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_instances_perform_maintenance_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.instance,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_instances_perform_maintenance_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute instances remove resource policies.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_instances_remove_resource_policies(
        &self,
        args: &ComputeInstancesRemoveResourcePoliciesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_instances_remove_resource_policies_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.instance,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_instances_remove_resource_policies_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute instances report host as faulty.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_instances_report_host_as_faulty(
        &self,
        args: &ComputeInstancesReportHostAsFaultyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_instances_report_host_as_faulty_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.instance,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_instances_report_host_as_faulty_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute instances reset.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_instances_reset(
        &self,
        args: &ComputeInstancesResetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_instances_reset_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.instance,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_instances_reset_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute instances resume.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_instances_resume(
        &self,
        args: &ComputeInstancesResumeArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_instances_resume_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.instance,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_instances_resume_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute instances send diagnostic interrupt.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_instances_send_diagnostic_interrupt(
        &self,
        args: &ComputeInstancesSendDiagnosticInterruptArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_instances_send_diagnostic_interrupt_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.instance,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_instances_send_diagnostic_interrupt_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute instances set deletion protection.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_instances_set_deletion_protection(
        &self,
        args: &ComputeInstancesSetDeletionProtectionArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_instances_set_deletion_protection_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.resource,
            &args.deletionProtection,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_instances_set_deletion_protection_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute instances set disk auto delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_instances_set_disk_auto_delete(
        &self,
        args: &ComputeInstancesSetDiskAutoDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_instances_set_disk_auto_delete_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.instance,
            &args.autoDelete,
            &args.deviceName,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_instances_set_disk_auto_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute instances set iam policy.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Policy result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_instances_set_iam_policy(
        &self,
        args: &ComputeInstancesSetIamPolicyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Policy, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_instances_set_iam_policy_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.resource,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_instances_set_iam_policy_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute instances set labels.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_instances_set_labels(
        &self,
        args: &ComputeInstancesSetLabelsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_instances_set_labels_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.instance,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_instances_set_labels_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute instances set machine resources.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_instances_set_machine_resources(
        &self,
        args: &ComputeInstancesSetMachineResourcesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_instances_set_machine_resources_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.instance,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_instances_set_machine_resources_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute instances set machine type.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_instances_set_machine_type(
        &self,
        args: &ComputeInstancesSetMachineTypeArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_instances_set_machine_type_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.instance,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_instances_set_machine_type_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute instances set metadata.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_instances_set_metadata(
        &self,
        args: &ComputeInstancesSetMetadataArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_instances_set_metadata_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.instance,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_instances_set_metadata_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute instances set min cpu platform.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_instances_set_min_cpu_platform(
        &self,
        args: &ComputeInstancesSetMinCpuPlatformArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_instances_set_min_cpu_platform_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.instance,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_instances_set_min_cpu_platform_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute instances set name.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_instances_set_name(
        &self,
        args: &ComputeInstancesSetNameArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_instances_set_name_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.instance,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_instances_set_name_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute instances set scheduling.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_instances_set_scheduling(
        &self,
        args: &ComputeInstancesSetSchedulingArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_instances_set_scheduling_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.instance,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_instances_set_scheduling_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute instances set security policy.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_instances_set_security_policy(
        &self,
        args: &ComputeInstancesSetSecurityPolicyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_instances_set_security_policy_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.instance,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_instances_set_security_policy_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute instances set service account.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_instances_set_service_account(
        &self,
        args: &ComputeInstancesSetServiceAccountArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_instances_set_service_account_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.instance,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_instances_set_service_account_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute instances set shielded instance integrity policy.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_instances_set_shielded_instance_integrity_policy(
        &self,
        args: &ComputeInstancesSetShieldedInstanceIntegrityPolicyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_instances_set_shielded_instance_integrity_policy_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.instance,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_instances_set_shielded_instance_integrity_policy_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute instances set tags.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_instances_set_tags(
        &self,
        args: &ComputeInstancesSetTagsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_instances_set_tags_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.instance,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_instances_set_tags_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute instances simulate maintenance event.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_instances_simulate_maintenance_event(
        &self,
        args: &ComputeInstancesSimulateMaintenanceEventArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_instances_simulate_maintenance_event_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.instance,
            &args.requestId,
            &args.withExtendedNotifications,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_instances_simulate_maintenance_event_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute instances start.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_instances_start(
        &self,
        args: &ComputeInstancesStartArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_instances_start_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.instance,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_instances_start_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute instances start with encryption key.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_instances_start_with_encryption_key(
        &self,
        args: &ComputeInstancesStartWithEncryptionKeyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_instances_start_with_encryption_key_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.instance,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_instances_start_with_encryption_key_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute instances stop.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_instances_stop(
        &self,
        args: &ComputeInstancesStopArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_instances_stop_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.instance,
            &args.discardLocalSsd,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_instances_stop_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute instances suspend.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_instances_suspend(
        &self,
        args: &ComputeInstancesSuspendArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_instances_suspend_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.instance,
            &args.discardLocalSsd,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_instances_suspend_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute instances test iam permissions.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TestPermissionsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_instances_test_iam_permissions(
        &self,
        args: &ComputeInstancesTestIamPermissionsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TestPermissionsResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_instances_test_iam_permissions_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.resource,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_instances_test_iam_permissions_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute instances update.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_instances_update(
        &self,
        args: &ComputeInstancesUpdateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_instances_update_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.instance,
            &args.discardLocalSsd,
            &args.minimalAction,
            &args.mostDisruptiveAllowedAction,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_instances_update_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute instances update access config.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_instances_update_access_config(
        &self,
        args: &ComputeInstancesUpdateAccessConfigArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_instances_update_access_config_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.instance,
            &args.networkInterface,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_instances_update_access_config_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute instances update display device.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_instances_update_display_device(
        &self,
        args: &ComputeInstancesUpdateDisplayDeviceArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_instances_update_display_device_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.instance,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_instances_update_display_device_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute instances update network interface.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_instances_update_network_interface(
        &self,
        args: &ComputeInstancesUpdateNetworkInterfaceArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_instances_update_network_interface_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.instance,
            &args.networkInterface,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_instances_update_network_interface_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute instances update shielded instance config.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_instances_update_shielded_instance_config(
        &self,
        args: &ComputeInstancesUpdateShieldedInstanceConfigArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_instances_update_shielded_instance_config_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.instance,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_instances_update_shielded_instance_config_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute instant snapshot groups delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_instant_snapshot_groups_delete(
        &self,
        args: &ComputeInstantSnapshotGroupsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_instant_snapshot_groups_delete_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.instantSnapshotGroup,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_instant_snapshot_groups_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute instant snapshot groups get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the InstantSnapshotGroup result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_instant_snapshot_groups_get(
        &self,
        args: &ComputeInstantSnapshotGroupsGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<InstantSnapshotGroup, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_instant_snapshot_groups_get_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.instantSnapshotGroup,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_instant_snapshot_groups_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute instant snapshot groups get iam policy.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Policy result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_instant_snapshot_groups_get_iam_policy(
        &self,
        args: &ComputeInstantSnapshotGroupsGetIamPolicyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Policy, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_instant_snapshot_groups_get_iam_policy_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.resource,
            &args.optionsRequestedPolicyVersion,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_instant_snapshot_groups_get_iam_policy_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute instant snapshot groups insert.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_instant_snapshot_groups_insert(
        &self,
        args: &ComputeInstantSnapshotGroupsInsertArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_instant_snapshot_groups_insert_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.requestId,
            &args.sourceConsistencyGroup,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_instant_snapshot_groups_insert_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute instant snapshot groups list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ListInstantSnapshotGroups result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_instant_snapshot_groups_list(
        &self,
        args: &ComputeInstantSnapshotGroupsListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ListInstantSnapshotGroups, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_instant_snapshot_groups_list_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.filter,
            &args.maxResults,
            &args.orderBy,
            &args.pageToken,
            &args.returnPartialSuccess,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_instant_snapshot_groups_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute instant snapshot groups set iam policy.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Policy result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_instant_snapshot_groups_set_iam_policy(
        &self,
        args: &ComputeInstantSnapshotGroupsSetIamPolicyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Policy, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_instant_snapshot_groups_set_iam_policy_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.resource,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_instant_snapshot_groups_set_iam_policy_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute instant snapshot groups test iam permissions.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TestPermissionsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_instant_snapshot_groups_test_iam_permissions(
        &self,
        args: &ComputeInstantSnapshotGroupsTestIamPermissionsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TestPermissionsResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_instant_snapshot_groups_test_iam_permissions_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.resource,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_instant_snapshot_groups_test_iam_permissions_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute instant snapshots aggregated list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the InstantSnapshotAggregatedList result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_instant_snapshots_aggregated_list(
        &self,
        args: &ComputeInstantSnapshotsAggregatedListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<InstantSnapshotAggregatedList, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_instant_snapshots_aggregated_list_builder(
            &self.http_client,
            &args.project,
            &args.filter,
            &args.includeAllScopes,
            &args.maxResults,
            &args.orderBy,
            &args.pageToken,
            &args.returnPartialSuccess,
            &args.serviceProjectNumber,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_instant_snapshots_aggregated_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute instant snapshots delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_instant_snapshots_delete(
        &self,
        args: &ComputeInstantSnapshotsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_instant_snapshots_delete_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.instantSnapshot,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_instant_snapshots_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute instant snapshots get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the InstantSnapshot result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_instant_snapshots_get(
        &self,
        args: &ComputeInstantSnapshotsGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<InstantSnapshot, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_instant_snapshots_get_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.instantSnapshot,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_instant_snapshots_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute instant snapshots get iam policy.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Policy result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_instant_snapshots_get_iam_policy(
        &self,
        args: &ComputeInstantSnapshotsGetIamPolicyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Policy, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_instant_snapshots_get_iam_policy_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.resource,
            &args.optionsRequestedPolicyVersion,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_instant_snapshots_get_iam_policy_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute instant snapshots insert.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_instant_snapshots_insert(
        &self,
        args: &ComputeInstantSnapshotsInsertArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_instant_snapshots_insert_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_instant_snapshots_insert_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute instant snapshots list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the InstantSnapshotList result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_instant_snapshots_list(
        &self,
        args: &ComputeInstantSnapshotsListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<InstantSnapshotList, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_instant_snapshots_list_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.filter,
            &args.maxResults,
            &args.orderBy,
            &args.pageToken,
            &args.returnPartialSuccess,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_instant_snapshots_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute instant snapshots set iam policy.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Policy result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_instant_snapshots_set_iam_policy(
        &self,
        args: &ComputeInstantSnapshotsSetIamPolicyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Policy, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_instant_snapshots_set_iam_policy_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.resource,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_instant_snapshots_set_iam_policy_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute instant snapshots set labels.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_instant_snapshots_set_labels(
        &self,
        args: &ComputeInstantSnapshotsSetLabelsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_instant_snapshots_set_labels_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.resource,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_instant_snapshots_set_labels_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute instant snapshots test iam permissions.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TestPermissionsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_instant_snapshots_test_iam_permissions(
        &self,
        args: &ComputeInstantSnapshotsTestIamPermissionsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TestPermissionsResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_instant_snapshots_test_iam_permissions_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.resource,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_instant_snapshots_test_iam_permissions_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute interconnect attachment groups delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_interconnect_attachment_groups_delete(
        &self,
        args: &ComputeInterconnectAttachmentGroupsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_interconnect_attachment_groups_delete_builder(
            &self.http_client,
            &args.project,
            &args.interconnectAttachmentGroup,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_interconnect_attachment_groups_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute interconnect attachment groups get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the InterconnectAttachmentGroup result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_interconnect_attachment_groups_get(
        &self,
        args: &ComputeInterconnectAttachmentGroupsGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<InterconnectAttachmentGroup, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_interconnect_attachment_groups_get_builder(
            &self.http_client,
            &args.project,
            &args.interconnectAttachmentGroup,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_interconnect_attachment_groups_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute interconnect attachment groups get iam policy.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Policy result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_interconnect_attachment_groups_get_iam_policy(
        &self,
        args: &ComputeInterconnectAttachmentGroupsGetIamPolicyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Policy, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_interconnect_attachment_groups_get_iam_policy_builder(
            &self.http_client,
            &args.project,
            &args.resource,
            &args.optionsRequestedPolicyVersion,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_interconnect_attachment_groups_get_iam_policy_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute interconnect attachment groups get operational status.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the InterconnectAttachmentGroupsGetOperationalStatusResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_interconnect_attachment_groups_get_operational_status(
        &self,
        args: &ComputeInterconnectAttachmentGroupsGetOperationalStatusArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<InterconnectAttachmentGroupsGetOperationalStatusResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_interconnect_attachment_groups_get_operational_status_builder(
            &self.http_client,
            &args.project,
            &args.interconnectAttachmentGroup,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_interconnect_attachment_groups_get_operational_status_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute interconnect attachment groups insert.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_interconnect_attachment_groups_insert(
        &self,
        args: &ComputeInterconnectAttachmentGroupsInsertArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_interconnect_attachment_groups_insert_builder(
            &self.http_client,
            &args.project,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_interconnect_attachment_groups_insert_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute interconnect attachment groups list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the InterconnectAttachmentGroupsListResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_interconnect_attachment_groups_list(
        &self,
        args: &ComputeInterconnectAttachmentGroupsListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<InterconnectAttachmentGroupsListResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_interconnect_attachment_groups_list_builder(
            &self.http_client,
            &args.project,
            &args.filter,
            &args.maxResults,
            &args.orderBy,
            &args.pageToken,
            &args.returnPartialSuccess,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_interconnect_attachment_groups_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute interconnect attachment groups patch.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_interconnect_attachment_groups_patch(
        &self,
        args: &ComputeInterconnectAttachmentGroupsPatchArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_interconnect_attachment_groups_patch_builder(
            &self.http_client,
            &args.project,
            &args.interconnectAttachmentGroup,
            &args.requestId,
            &args.updateMask,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_interconnect_attachment_groups_patch_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute interconnect attachment groups set iam policy.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Policy result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_interconnect_attachment_groups_set_iam_policy(
        &self,
        args: &ComputeInterconnectAttachmentGroupsSetIamPolicyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Policy, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_interconnect_attachment_groups_set_iam_policy_builder(
            &self.http_client,
            &args.project,
            &args.resource,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_interconnect_attachment_groups_set_iam_policy_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute interconnect attachment groups test iam permissions.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TestPermissionsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_interconnect_attachment_groups_test_iam_permissions(
        &self,
        args: &ComputeInterconnectAttachmentGroupsTestIamPermissionsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TestPermissionsResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_interconnect_attachment_groups_test_iam_permissions_builder(
            &self.http_client,
            &args.project,
            &args.resource,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_interconnect_attachment_groups_test_iam_permissions_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute interconnect attachments aggregated list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the InterconnectAttachmentAggregatedList result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_interconnect_attachments_aggregated_list(
        &self,
        args: &ComputeInterconnectAttachmentsAggregatedListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<InterconnectAttachmentAggregatedList, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_interconnect_attachments_aggregated_list_builder(
            &self.http_client,
            &args.project,
            &args.filter,
            &args.includeAllScopes,
            &args.maxResults,
            &args.orderBy,
            &args.pageToken,
            &args.returnPartialSuccess,
            &args.serviceProjectNumber,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_interconnect_attachments_aggregated_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute interconnect attachments delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_interconnect_attachments_delete(
        &self,
        args: &ComputeInterconnectAttachmentsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_interconnect_attachments_delete_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.interconnectAttachment,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_interconnect_attachments_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute interconnect attachments get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the InterconnectAttachment result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_interconnect_attachments_get(
        &self,
        args: &ComputeInterconnectAttachmentsGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<InterconnectAttachment, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_interconnect_attachments_get_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.interconnectAttachment,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_interconnect_attachments_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute interconnect attachments insert.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_interconnect_attachments_insert(
        &self,
        args: &ComputeInterconnectAttachmentsInsertArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_interconnect_attachments_insert_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.requestId,
            &args.validateOnly,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_interconnect_attachments_insert_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute interconnect attachments list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the InterconnectAttachmentList result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_interconnect_attachments_list(
        &self,
        args: &ComputeInterconnectAttachmentsListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<InterconnectAttachmentList, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_interconnect_attachments_list_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.filter,
            &args.maxResults,
            &args.orderBy,
            &args.pageToken,
            &args.returnPartialSuccess,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_interconnect_attachments_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute interconnect attachments patch.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_interconnect_attachments_patch(
        &self,
        args: &ComputeInterconnectAttachmentsPatchArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_interconnect_attachments_patch_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.interconnectAttachment,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_interconnect_attachments_patch_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute interconnect attachments set labels.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_interconnect_attachments_set_labels(
        &self,
        args: &ComputeInterconnectAttachmentsSetLabelsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_interconnect_attachments_set_labels_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.resource,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_interconnect_attachments_set_labels_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute interconnect groups create members.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_interconnect_groups_create_members(
        &self,
        args: &ComputeInterconnectGroupsCreateMembersArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_interconnect_groups_create_members_builder(
            &self.http_client,
            &args.project,
            &args.interconnectGroup,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_interconnect_groups_create_members_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute interconnect groups delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_interconnect_groups_delete(
        &self,
        args: &ComputeInterconnectGroupsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_interconnect_groups_delete_builder(
            &self.http_client,
            &args.project,
            &args.interconnectGroup,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_interconnect_groups_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute interconnect groups get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the InterconnectGroup result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_interconnect_groups_get(
        &self,
        args: &ComputeInterconnectGroupsGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<InterconnectGroup, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_interconnect_groups_get_builder(
            &self.http_client,
            &args.project,
            &args.interconnectGroup,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_interconnect_groups_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute interconnect groups get iam policy.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Policy result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_interconnect_groups_get_iam_policy(
        &self,
        args: &ComputeInterconnectGroupsGetIamPolicyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Policy, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_interconnect_groups_get_iam_policy_builder(
            &self.http_client,
            &args.project,
            &args.resource,
            &args.optionsRequestedPolicyVersion,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_interconnect_groups_get_iam_policy_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute interconnect groups get operational status.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the InterconnectGroupsGetOperationalStatusResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_interconnect_groups_get_operational_status(
        &self,
        args: &ComputeInterconnectGroupsGetOperationalStatusArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<InterconnectGroupsGetOperationalStatusResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_interconnect_groups_get_operational_status_builder(
            &self.http_client,
            &args.project,
            &args.interconnectGroup,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_interconnect_groups_get_operational_status_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute interconnect groups insert.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_interconnect_groups_insert(
        &self,
        args: &ComputeInterconnectGroupsInsertArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_interconnect_groups_insert_builder(
            &self.http_client,
            &args.project,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_interconnect_groups_insert_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute interconnect groups list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the InterconnectGroupsListResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_interconnect_groups_list(
        &self,
        args: &ComputeInterconnectGroupsListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<InterconnectGroupsListResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_interconnect_groups_list_builder(
            &self.http_client,
            &args.project,
            &args.filter,
            &args.maxResults,
            &args.orderBy,
            &args.pageToken,
            &args.returnPartialSuccess,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_interconnect_groups_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute interconnect groups patch.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_interconnect_groups_patch(
        &self,
        args: &ComputeInterconnectGroupsPatchArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_interconnect_groups_patch_builder(
            &self.http_client,
            &args.project,
            &args.interconnectGroup,
            &args.requestId,
            &args.updateMask,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_interconnect_groups_patch_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute interconnect groups set iam policy.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Policy result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_interconnect_groups_set_iam_policy(
        &self,
        args: &ComputeInterconnectGroupsSetIamPolicyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Policy, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_interconnect_groups_set_iam_policy_builder(
            &self.http_client,
            &args.project,
            &args.resource,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_interconnect_groups_set_iam_policy_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute interconnect groups test iam permissions.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TestPermissionsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_interconnect_groups_test_iam_permissions(
        &self,
        args: &ComputeInterconnectGroupsTestIamPermissionsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TestPermissionsResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_interconnect_groups_test_iam_permissions_builder(
            &self.http_client,
            &args.project,
            &args.resource,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_interconnect_groups_test_iam_permissions_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute interconnect locations get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the InterconnectLocation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_interconnect_locations_get(
        &self,
        args: &ComputeInterconnectLocationsGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<InterconnectLocation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_interconnect_locations_get_builder(
            &self.http_client,
            &args.project,
            &args.interconnectLocation,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_interconnect_locations_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute interconnect locations list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the InterconnectLocationList result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_interconnect_locations_list(
        &self,
        args: &ComputeInterconnectLocationsListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<InterconnectLocationList, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_interconnect_locations_list_builder(
            &self.http_client,
            &args.project,
            &args.filter,
            &args.maxResults,
            &args.orderBy,
            &args.pageToken,
            &args.returnPartialSuccess,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_interconnect_locations_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute interconnect remote locations get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the InterconnectRemoteLocation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_interconnect_remote_locations_get(
        &self,
        args: &ComputeInterconnectRemoteLocationsGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<InterconnectRemoteLocation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_interconnect_remote_locations_get_builder(
            &self.http_client,
            &args.project,
            &args.interconnectRemoteLocation,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_interconnect_remote_locations_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute interconnect remote locations list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the InterconnectRemoteLocationList result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_interconnect_remote_locations_list(
        &self,
        args: &ComputeInterconnectRemoteLocationsListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<InterconnectRemoteLocationList, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_interconnect_remote_locations_list_builder(
            &self.http_client,
            &args.project,
            &args.filter,
            &args.maxResults,
            &args.orderBy,
            &args.pageToken,
            &args.returnPartialSuccess,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_interconnect_remote_locations_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute interconnects delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_interconnects_delete(
        &self,
        args: &ComputeInterconnectsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_interconnects_delete_builder(
            &self.http_client,
            &args.project,
            &args.interconnect,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_interconnects_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute interconnects get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Interconnect result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_interconnects_get(
        &self,
        args: &ComputeInterconnectsGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Interconnect, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_interconnects_get_builder(
            &self.http_client,
            &args.project,
            &args.interconnect,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_interconnects_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute interconnects get diagnostics.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the InterconnectsGetDiagnosticsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_interconnects_get_diagnostics(
        &self,
        args: &ComputeInterconnectsGetDiagnosticsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<InterconnectsGetDiagnosticsResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_interconnects_get_diagnostics_builder(
            &self.http_client,
            &args.project,
            &args.interconnect,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_interconnects_get_diagnostics_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute interconnects get macsec config.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the InterconnectsGetMacsecConfigResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_interconnects_get_macsec_config(
        &self,
        args: &ComputeInterconnectsGetMacsecConfigArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<InterconnectsGetMacsecConfigResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_interconnects_get_macsec_config_builder(
            &self.http_client,
            &args.project,
            &args.interconnect,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_interconnects_get_macsec_config_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute interconnects insert.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_interconnects_insert(
        &self,
        args: &ComputeInterconnectsInsertArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_interconnects_insert_builder(
            &self.http_client,
            &args.project,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_interconnects_insert_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute interconnects list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the InterconnectList result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_interconnects_list(
        &self,
        args: &ComputeInterconnectsListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<InterconnectList, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_interconnects_list_builder(
            &self.http_client,
            &args.project,
            &args.filter,
            &args.maxResults,
            &args.orderBy,
            &args.pageToken,
            &args.returnPartialSuccess,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_interconnects_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute interconnects patch.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_interconnects_patch(
        &self,
        args: &ComputeInterconnectsPatchArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_interconnects_patch_builder(
            &self.http_client,
            &args.project,
            &args.interconnect,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_interconnects_patch_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute interconnects set labels.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_interconnects_set_labels(
        &self,
        args: &ComputeInterconnectsSetLabelsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_interconnects_set_labels_builder(
            &self.http_client,
            &args.project,
            &args.resource,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_interconnects_set_labels_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute license codes get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the LicenseCode result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_license_codes_get(
        &self,
        args: &ComputeLicenseCodesGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<LicenseCode, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_license_codes_get_builder(
            &self.http_client,
            &args.project,
            &args.licenseCode,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_license_codes_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute license codes test iam permissions.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TestPermissionsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_license_codes_test_iam_permissions(
        &self,
        args: &ComputeLicenseCodesTestIamPermissionsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TestPermissionsResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_license_codes_test_iam_permissions_builder(
            &self.http_client,
            &args.project,
            &args.resource,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_license_codes_test_iam_permissions_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute licenses delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_licenses_delete(
        &self,
        args: &ComputeLicensesDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_licenses_delete_builder(
            &self.http_client,
            &args.project,
            &args.license,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_licenses_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute licenses get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the License result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_licenses_get(
        &self,
        args: &ComputeLicensesGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<License, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_licenses_get_builder(
            &self.http_client,
            &args.project,
            &args.license,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_licenses_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute licenses get iam policy.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Policy result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_licenses_get_iam_policy(
        &self,
        args: &ComputeLicensesGetIamPolicyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Policy, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_licenses_get_iam_policy_builder(
            &self.http_client,
            &args.project,
            &args.resource,
            &args.optionsRequestedPolicyVersion,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_licenses_get_iam_policy_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute licenses insert.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_licenses_insert(
        &self,
        args: &ComputeLicensesInsertArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_licenses_insert_builder(
            &self.http_client,
            &args.project,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_licenses_insert_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute licenses list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the LicensesListResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_licenses_list(
        &self,
        args: &ComputeLicensesListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<LicensesListResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_licenses_list_builder(
            &self.http_client,
            &args.project,
            &args.filter,
            &args.maxResults,
            &args.orderBy,
            &args.pageToken,
            &args.returnPartialSuccess,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_licenses_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute licenses set iam policy.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Policy result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_licenses_set_iam_policy(
        &self,
        args: &ComputeLicensesSetIamPolicyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Policy, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_licenses_set_iam_policy_builder(
            &self.http_client,
            &args.project,
            &args.resource,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_licenses_set_iam_policy_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute licenses test iam permissions.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TestPermissionsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_licenses_test_iam_permissions(
        &self,
        args: &ComputeLicensesTestIamPermissionsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TestPermissionsResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_licenses_test_iam_permissions_builder(
            &self.http_client,
            &args.project,
            &args.resource,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_licenses_test_iam_permissions_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute licenses update.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_licenses_update(
        &self,
        args: &ComputeLicensesUpdateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_licenses_update_builder(
            &self.http_client,
            &args.project,
            &args.license,
            &args.requestId,
            &args.updateMask,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_licenses_update_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute machine images delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_machine_images_delete(
        &self,
        args: &ComputeMachineImagesDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_machine_images_delete_builder(
            &self.http_client,
            &args.project,
            &args.machineImage,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_machine_images_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute machine images get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the MachineImage result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_machine_images_get(
        &self,
        args: &ComputeMachineImagesGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<MachineImage, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_machine_images_get_builder(
            &self.http_client,
            &args.project,
            &args.machineImage,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_machine_images_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute machine images get iam policy.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Policy result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_machine_images_get_iam_policy(
        &self,
        args: &ComputeMachineImagesGetIamPolicyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Policy, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_machine_images_get_iam_policy_builder(
            &self.http_client,
            &args.project,
            &args.resource,
            &args.optionsRequestedPolicyVersion,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_machine_images_get_iam_policy_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute machine images insert.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_machine_images_insert(
        &self,
        args: &ComputeMachineImagesInsertArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_machine_images_insert_builder(
            &self.http_client,
            &args.project,
            &args.requestId,
            &args.sourceInstance,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_machine_images_insert_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute machine images list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the MachineImageList result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_machine_images_list(
        &self,
        args: &ComputeMachineImagesListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<MachineImageList, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_machine_images_list_builder(
            &self.http_client,
            &args.project,
            &args.filter,
            &args.maxResults,
            &args.orderBy,
            &args.pageToken,
            &args.returnPartialSuccess,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_machine_images_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute machine images set iam policy.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Policy result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_machine_images_set_iam_policy(
        &self,
        args: &ComputeMachineImagesSetIamPolicyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Policy, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_machine_images_set_iam_policy_builder(
            &self.http_client,
            &args.project,
            &args.resource,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_machine_images_set_iam_policy_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute machine images set labels.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_machine_images_set_labels(
        &self,
        args: &ComputeMachineImagesSetLabelsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_machine_images_set_labels_builder(
            &self.http_client,
            &args.project,
            &args.resource,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_machine_images_set_labels_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute machine images test iam permissions.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TestPermissionsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_machine_images_test_iam_permissions(
        &self,
        args: &ComputeMachineImagesTestIamPermissionsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TestPermissionsResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_machine_images_test_iam_permissions_builder(
            &self.http_client,
            &args.project,
            &args.resource,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_machine_images_test_iam_permissions_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute machine types aggregated list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the MachineTypeAggregatedList result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_machine_types_aggregated_list(
        &self,
        args: &ComputeMachineTypesAggregatedListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<MachineTypeAggregatedList, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_machine_types_aggregated_list_builder(
            &self.http_client,
            &args.project,
            &args.filter,
            &args.includeAllScopes,
            &args.maxResults,
            &args.orderBy,
            &args.pageToken,
            &args.returnPartialSuccess,
            &args.serviceProjectNumber,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_machine_types_aggregated_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute machine types get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the MachineType result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_machine_types_get(
        &self,
        args: &ComputeMachineTypesGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<MachineType, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_machine_types_get_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.machineType,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_machine_types_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute machine types list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the MachineTypeList result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_machine_types_list(
        &self,
        args: &ComputeMachineTypesListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<MachineTypeList, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_machine_types_list_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.filter,
            &args.maxResults,
            &args.orderBy,
            &args.pageToken,
            &args.returnPartialSuccess,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_machine_types_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute network attachments aggregated list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the NetworkAttachmentAggregatedList result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_network_attachments_aggregated_list(
        &self,
        args: &ComputeNetworkAttachmentsAggregatedListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<NetworkAttachmentAggregatedList, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_network_attachments_aggregated_list_builder(
            &self.http_client,
            &args.project,
            &args.filter,
            &args.includeAllScopes,
            &args.maxResults,
            &args.orderBy,
            &args.pageToken,
            &args.returnPartialSuccess,
            &args.serviceProjectNumber,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_network_attachments_aggregated_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute network attachments delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_network_attachments_delete(
        &self,
        args: &ComputeNetworkAttachmentsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_network_attachments_delete_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.networkAttachment,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_network_attachments_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute network attachments get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the NetworkAttachment result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_network_attachments_get(
        &self,
        args: &ComputeNetworkAttachmentsGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<NetworkAttachment, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_network_attachments_get_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.networkAttachment,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_network_attachments_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute network attachments get iam policy.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Policy result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_network_attachments_get_iam_policy(
        &self,
        args: &ComputeNetworkAttachmentsGetIamPolicyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Policy, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_network_attachments_get_iam_policy_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.resource,
            &args.optionsRequestedPolicyVersion,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_network_attachments_get_iam_policy_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute network attachments insert.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_network_attachments_insert(
        &self,
        args: &ComputeNetworkAttachmentsInsertArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_network_attachments_insert_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_network_attachments_insert_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute network attachments list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the NetworkAttachmentList result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_network_attachments_list(
        &self,
        args: &ComputeNetworkAttachmentsListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<NetworkAttachmentList, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_network_attachments_list_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.filter,
            &args.maxResults,
            &args.orderBy,
            &args.pageToken,
            &args.returnPartialSuccess,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_network_attachments_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute network attachments patch.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_network_attachments_patch(
        &self,
        args: &ComputeNetworkAttachmentsPatchArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_network_attachments_patch_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.networkAttachment,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_network_attachments_patch_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute network attachments set iam policy.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Policy result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_network_attachments_set_iam_policy(
        &self,
        args: &ComputeNetworkAttachmentsSetIamPolicyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Policy, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_network_attachments_set_iam_policy_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.resource,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_network_attachments_set_iam_policy_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute network attachments test iam permissions.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TestPermissionsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_network_attachments_test_iam_permissions(
        &self,
        args: &ComputeNetworkAttachmentsTestIamPermissionsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TestPermissionsResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_network_attachments_test_iam_permissions_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.resource,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_network_attachments_test_iam_permissions_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute network edge security services aggregated list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the NetworkEdgeSecurityServiceAggregatedList result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_network_edge_security_services_aggregated_list(
        &self,
        args: &ComputeNetworkEdgeSecurityServicesAggregatedListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<NetworkEdgeSecurityServiceAggregatedList, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_network_edge_security_services_aggregated_list_builder(
            &self.http_client,
            &args.project,
            &args.filter,
            &args.includeAllScopes,
            &args.maxResults,
            &args.orderBy,
            &args.pageToken,
            &args.returnPartialSuccess,
            &args.serviceProjectNumber,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_network_edge_security_services_aggregated_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute network edge security services delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_network_edge_security_services_delete(
        &self,
        args: &ComputeNetworkEdgeSecurityServicesDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_network_edge_security_services_delete_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.networkEdgeSecurityService,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_network_edge_security_services_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute network edge security services get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the NetworkEdgeSecurityService result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_network_edge_security_services_get(
        &self,
        args: &ComputeNetworkEdgeSecurityServicesGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<NetworkEdgeSecurityService, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_network_edge_security_services_get_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.networkEdgeSecurityService,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_network_edge_security_services_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute network edge security services insert.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_network_edge_security_services_insert(
        &self,
        args: &ComputeNetworkEdgeSecurityServicesInsertArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_network_edge_security_services_insert_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.requestId,
            &args.validateOnly,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_network_edge_security_services_insert_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute network edge security services patch.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_network_edge_security_services_patch(
        &self,
        args: &ComputeNetworkEdgeSecurityServicesPatchArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_network_edge_security_services_patch_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.networkEdgeSecurityService,
            &args.paths,
            &args.requestId,
            &args.updateMask,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_network_edge_security_services_patch_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute network endpoint groups aggregated list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the NetworkEndpointGroupAggregatedList result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_network_endpoint_groups_aggregated_list(
        &self,
        args: &ComputeNetworkEndpointGroupsAggregatedListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<NetworkEndpointGroupAggregatedList, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_network_endpoint_groups_aggregated_list_builder(
            &self.http_client,
            &args.project,
            &args.filter,
            &args.includeAllScopes,
            &args.maxResults,
            &args.orderBy,
            &args.pageToken,
            &args.returnPartialSuccess,
            &args.serviceProjectNumber,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_network_endpoint_groups_aggregated_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute network endpoint groups attach network endpoints.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_network_endpoint_groups_attach_network_endpoints(
        &self,
        args: &ComputeNetworkEndpointGroupsAttachNetworkEndpointsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_network_endpoint_groups_attach_network_endpoints_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.networkEndpointGroup,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_network_endpoint_groups_attach_network_endpoints_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute network endpoint groups delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_network_endpoint_groups_delete(
        &self,
        args: &ComputeNetworkEndpointGroupsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_network_endpoint_groups_delete_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.networkEndpointGroup,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_network_endpoint_groups_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute network endpoint groups detach network endpoints.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_network_endpoint_groups_detach_network_endpoints(
        &self,
        args: &ComputeNetworkEndpointGroupsDetachNetworkEndpointsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_network_endpoint_groups_detach_network_endpoints_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.networkEndpointGroup,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_network_endpoint_groups_detach_network_endpoints_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute network endpoint groups get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the NetworkEndpointGroup result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_network_endpoint_groups_get(
        &self,
        args: &ComputeNetworkEndpointGroupsGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<NetworkEndpointGroup, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_network_endpoint_groups_get_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.networkEndpointGroup,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_network_endpoint_groups_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute network endpoint groups insert.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_network_endpoint_groups_insert(
        &self,
        args: &ComputeNetworkEndpointGroupsInsertArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_network_endpoint_groups_insert_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_network_endpoint_groups_insert_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute network endpoint groups list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the NetworkEndpointGroupList result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_network_endpoint_groups_list(
        &self,
        args: &ComputeNetworkEndpointGroupsListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<NetworkEndpointGroupList, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_network_endpoint_groups_list_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.filter,
            &args.maxResults,
            &args.orderBy,
            &args.pageToken,
            &args.returnPartialSuccess,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_network_endpoint_groups_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute network endpoint groups list network endpoints.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the NetworkEndpointGroupsListNetworkEndpoints result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_network_endpoint_groups_list_network_endpoints(
        &self,
        args: &ComputeNetworkEndpointGroupsListNetworkEndpointsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<NetworkEndpointGroupsListNetworkEndpoints, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_network_endpoint_groups_list_network_endpoints_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.networkEndpointGroup,
            &args.filter,
            &args.maxResults,
            &args.orderBy,
            &args.pageToken,
            &args.returnPartialSuccess,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_network_endpoint_groups_list_network_endpoints_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute network endpoint groups test iam permissions.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TestPermissionsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_network_endpoint_groups_test_iam_permissions(
        &self,
        args: &ComputeNetworkEndpointGroupsTestIamPermissionsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TestPermissionsResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_network_endpoint_groups_test_iam_permissions_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.resource,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_network_endpoint_groups_test_iam_permissions_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute network firewall policies add association.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_network_firewall_policies_add_association(
        &self,
        args: &ComputeNetworkFirewallPoliciesAddAssociationArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_network_firewall_policies_add_association_builder(
            &self.http_client,
            &args.project,
            &args.firewallPolicy,
            &args.replaceExistingAssociation,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_network_firewall_policies_add_association_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute network firewall policies add packet mirroring rule.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_network_firewall_policies_add_packet_mirroring_rule(
        &self,
        args: &ComputeNetworkFirewallPoliciesAddPacketMirroringRuleArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_network_firewall_policies_add_packet_mirroring_rule_builder(
            &self.http_client,
            &args.project,
            &args.firewallPolicy,
            &args.maxPriority,
            &args.minPriority,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_network_firewall_policies_add_packet_mirroring_rule_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute network firewall policies add rule.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_network_firewall_policies_add_rule(
        &self,
        args: &ComputeNetworkFirewallPoliciesAddRuleArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_network_firewall_policies_add_rule_builder(
            &self.http_client,
            &args.project,
            &args.firewallPolicy,
            &args.maxPriority,
            &args.minPriority,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_network_firewall_policies_add_rule_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute network firewall policies aggregated list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the NetworkFirewallPolicyAggregatedList result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_network_firewall_policies_aggregated_list(
        &self,
        args: &ComputeNetworkFirewallPoliciesAggregatedListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<NetworkFirewallPolicyAggregatedList, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_network_firewall_policies_aggregated_list_builder(
            &self.http_client,
            &args.project,
            &args.filter,
            &args.includeAllScopes,
            &args.maxResults,
            &args.orderBy,
            &args.pageToken,
            &args.returnPartialSuccess,
            &args.serviceProjectNumber,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_network_firewall_policies_aggregated_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute network firewall policies clone rules.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_network_firewall_policies_clone_rules(
        &self,
        args: &ComputeNetworkFirewallPoliciesCloneRulesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_network_firewall_policies_clone_rules_builder(
            &self.http_client,
            &args.project,
            &args.firewallPolicy,
            &args.requestId,
            &args.sourceFirewallPolicy,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_network_firewall_policies_clone_rules_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute network firewall policies delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_network_firewall_policies_delete(
        &self,
        args: &ComputeNetworkFirewallPoliciesDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_network_firewall_policies_delete_builder(
            &self.http_client,
            &args.project,
            &args.firewallPolicy,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_network_firewall_policies_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute network firewall policies get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the FirewallPolicy result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_network_firewall_policies_get(
        &self,
        args: &ComputeNetworkFirewallPoliciesGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<FirewallPolicy, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_network_firewall_policies_get_builder(
            &self.http_client,
            &args.project,
            &args.firewallPolicy,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_network_firewall_policies_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute network firewall policies get association.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the FirewallPolicyAssociation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_network_firewall_policies_get_association(
        &self,
        args: &ComputeNetworkFirewallPoliciesGetAssociationArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<FirewallPolicyAssociation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_network_firewall_policies_get_association_builder(
            &self.http_client,
            &args.project,
            &args.firewallPolicy,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_network_firewall_policies_get_association_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute network firewall policies get iam policy.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Policy result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_network_firewall_policies_get_iam_policy(
        &self,
        args: &ComputeNetworkFirewallPoliciesGetIamPolicyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Policy, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_network_firewall_policies_get_iam_policy_builder(
            &self.http_client,
            &args.project,
            &args.resource,
            &args.optionsRequestedPolicyVersion,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_network_firewall_policies_get_iam_policy_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute network firewall policies get packet mirroring rule.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the FirewallPolicyRule result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_network_firewall_policies_get_packet_mirroring_rule(
        &self,
        args: &ComputeNetworkFirewallPoliciesGetPacketMirroringRuleArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<FirewallPolicyRule, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_network_firewall_policies_get_packet_mirroring_rule_builder(
            &self.http_client,
            &args.project,
            &args.firewallPolicy,
            &args.priority,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_network_firewall_policies_get_packet_mirroring_rule_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute network firewall policies get rule.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the FirewallPolicyRule result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_network_firewall_policies_get_rule(
        &self,
        args: &ComputeNetworkFirewallPoliciesGetRuleArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<FirewallPolicyRule, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_network_firewall_policies_get_rule_builder(
            &self.http_client,
            &args.project,
            &args.firewallPolicy,
            &args.priority,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_network_firewall_policies_get_rule_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute network firewall policies insert.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_network_firewall_policies_insert(
        &self,
        args: &ComputeNetworkFirewallPoliciesInsertArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_network_firewall_policies_insert_builder(
            &self.http_client,
            &args.project,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_network_firewall_policies_insert_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute network firewall policies list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the FirewallPolicyList result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_network_firewall_policies_list(
        &self,
        args: &ComputeNetworkFirewallPoliciesListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<FirewallPolicyList, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_network_firewall_policies_list_builder(
            &self.http_client,
            &args.project,
            &args.filter,
            &args.maxResults,
            &args.orderBy,
            &args.pageToken,
            &args.returnPartialSuccess,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_network_firewall_policies_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute network firewall policies patch.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_network_firewall_policies_patch(
        &self,
        args: &ComputeNetworkFirewallPoliciesPatchArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_network_firewall_policies_patch_builder(
            &self.http_client,
            &args.project,
            &args.firewallPolicy,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_network_firewall_policies_patch_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute network firewall policies patch packet mirroring rule.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_network_firewall_policies_patch_packet_mirroring_rule(
        &self,
        args: &ComputeNetworkFirewallPoliciesPatchPacketMirroringRuleArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_network_firewall_policies_patch_packet_mirroring_rule_builder(
            &self.http_client,
            &args.project,
            &args.firewallPolicy,
            &args.priority,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_network_firewall_policies_patch_packet_mirroring_rule_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute network firewall policies patch rule.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_network_firewall_policies_patch_rule(
        &self,
        args: &ComputeNetworkFirewallPoliciesPatchRuleArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_network_firewall_policies_patch_rule_builder(
            &self.http_client,
            &args.project,
            &args.firewallPolicy,
            &args.priority,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_network_firewall_policies_patch_rule_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute network firewall policies remove association.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_network_firewall_policies_remove_association(
        &self,
        args: &ComputeNetworkFirewallPoliciesRemoveAssociationArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_network_firewall_policies_remove_association_builder(
            &self.http_client,
            &args.project,
            &args.firewallPolicy,
            &args.name,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_network_firewall_policies_remove_association_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute network firewall policies remove packet mirroring rule.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_network_firewall_policies_remove_packet_mirroring_rule(
        &self,
        args: &ComputeNetworkFirewallPoliciesRemovePacketMirroringRuleArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_network_firewall_policies_remove_packet_mirroring_rule_builder(
            &self.http_client,
            &args.project,
            &args.firewallPolicy,
            &args.priority,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_network_firewall_policies_remove_packet_mirroring_rule_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute network firewall policies remove rule.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_network_firewall_policies_remove_rule(
        &self,
        args: &ComputeNetworkFirewallPoliciesRemoveRuleArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_network_firewall_policies_remove_rule_builder(
            &self.http_client,
            &args.project,
            &args.firewallPolicy,
            &args.priority,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_network_firewall_policies_remove_rule_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute network firewall policies set iam policy.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Policy result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_network_firewall_policies_set_iam_policy(
        &self,
        args: &ComputeNetworkFirewallPoliciesSetIamPolicyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Policy, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_network_firewall_policies_set_iam_policy_builder(
            &self.http_client,
            &args.project,
            &args.resource,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_network_firewall_policies_set_iam_policy_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute network firewall policies test iam permissions.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TestPermissionsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_network_firewall_policies_test_iam_permissions(
        &self,
        args: &ComputeNetworkFirewallPoliciesTestIamPermissionsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TestPermissionsResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_network_firewall_policies_test_iam_permissions_builder(
            &self.http_client,
            &args.project,
            &args.resource,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_network_firewall_policies_test_iam_permissions_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute network profiles get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the NetworkProfile result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_network_profiles_get(
        &self,
        args: &ComputeNetworkProfilesGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<NetworkProfile, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_network_profiles_get_builder(
            &self.http_client,
            &args.project,
            &args.networkProfile,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_network_profiles_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute network profiles list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the NetworkProfilesListResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_network_profiles_list(
        &self,
        args: &ComputeNetworkProfilesListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<NetworkProfilesListResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_network_profiles_list_builder(
            &self.http_client,
            &args.project,
            &args.filter,
            &args.maxResults,
            &args.orderBy,
            &args.pageToken,
            &args.returnPartialSuccess,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_network_profiles_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute networks add peering.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_networks_add_peering(
        &self,
        args: &ComputeNetworksAddPeeringArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_networks_add_peering_builder(
            &self.http_client,
            &args.project,
            &args.network,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_networks_add_peering_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute networks delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_networks_delete(
        &self,
        args: &ComputeNetworksDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_networks_delete_builder(
            &self.http_client,
            &args.project,
            &args.network,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_networks_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute networks get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Network result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_networks_get(
        &self,
        args: &ComputeNetworksGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Network, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_networks_get_builder(
            &self.http_client,
            &args.project,
            &args.network,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_networks_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute networks get effective firewalls.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the NetworksGetEffectiveFirewallsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_networks_get_effective_firewalls(
        &self,
        args: &ComputeNetworksGetEffectiveFirewallsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<NetworksGetEffectiveFirewallsResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_networks_get_effective_firewalls_builder(
            &self.http_client,
            &args.project,
            &args.network,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_networks_get_effective_firewalls_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute networks insert.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_networks_insert(
        &self,
        args: &ComputeNetworksInsertArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_networks_insert_builder(
            &self.http_client,
            &args.project,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_networks_insert_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute networks list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the NetworkList result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_networks_list(
        &self,
        args: &ComputeNetworksListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<NetworkList, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_networks_list_builder(
            &self.http_client,
            &args.project,
            &args.filter,
            &args.maxResults,
            &args.orderBy,
            &args.pageToken,
            &args.returnPartialSuccess,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_networks_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute networks list peering routes.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ExchangedPeeringRoutesList result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_networks_list_peering_routes(
        &self,
        args: &ComputeNetworksListPeeringRoutesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ExchangedPeeringRoutesList, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_networks_list_peering_routes_builder(
            &self.http_client,
            &args.project,
            &args.network,
            &args.direction,
            &args.filter,
            &args.maxResults,
            &args.orderBy,
            &args.pageToken,
            &args.peeringName,
            &args.region,
            &args.returnPartialSuccess,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_networks_list_peering_routes_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute networks patch.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_networks_patch(
        &self,
        args: &ComputeNetworksPatchArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_networks_patch_builder(
            &self.http_client,
            &args.project,
            &args.network,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_networks_patch_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute networks remove peering.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_networks_remove_peering(
        &self,
        args: &ComputeNetworksRemovePeeringArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_networks_remove_peering_builder(
            &self.http_client,
            &args.project,
            &args.network,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_networks_remove_peering_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute networks request remove peering.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_networks_request_remove_peering(
        &self,
        args: &ComputeNetworksRequestRemovePeeringArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_networks_request_remove_peering_builder(
            &self.http_client,
            &args.project,
            &args.network,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_networks_request_remove_peering_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute networks switch to custom mode.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_networks_switch_to_custom_mode(
        &self,
        args: &ComputeNetworksSwitchToCustomModeArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_networks_switch_to_custom_mode_builder(
            &self.http_client,
            &args.project,
            &args.network,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_networks_switch_to_custom_mode_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute networks update peering.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_networks_update_peering(
        &self,
        args: &ComputeNetworksUpdatePeeringArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_networks_update_peering_builder(
            &self.http_client,
            &args.project,
            &args.network,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_networks_update_peering_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute node groups add nodes.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_node_groups_add_nodes(
        &self,
        args: &ComputeNodeGroupsAddNodesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_node_groups_add_nodes_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.nodeGroup,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_node_groups_add_nodes_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute node groups aggregated list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the NodeGroupAggregatedList result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_node_groups_aggregated_list(
        &self,
        args: &ComputeNodeGroupsAggregatedListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<NodeGroupAggregatedList, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_node_groups_aggregated_list_builder(
            &self.http_client,
            &args.project,
            &args.filter,
            &args.includeAllScopes,
            &args.maxResults,
            &args.orderBy,
            &args.pageToken,
            &args.returnPartialSuccess,
            &args.serviceProjectNumber,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_node_groups_aggregated_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute node groups delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_node_groups_delete(
        &self,
        args: &ComputeNodeGroupsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_node_groups_delete_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.nodeGroup,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_node_groups_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute node groups delete nodes.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_node_groups_delete_nodes(
        &self,
        args: &ComputeNodeGroupsDeleteNodesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_node_groups_delete_nodes_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.nodeGroup,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_node_groups_delete_nodes_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute node groups get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the NodeGroup result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_node_groups_get(
        &self,
        args: &ComputeNodeGroupsGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<NodeGroup, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_node_groups_get_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.nodeGroup,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_node_groups_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute node groups get iam policy.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Policy result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_node_groups_get_iam_policy(
        &self,
        args: &ComputeNodeGroupsGetIamPolicyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Policy, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_node_groups_get_iam_policy_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.resource,
            &args.optionsRequestedPolicyVersion,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_node_groups_get_iam_policy_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute node groups insert.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_node_groups_insert(
        &self,
        args: &ComputeNodeGroupsInsertArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_node_groups_insert_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.initialNodeCount,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_node_groups_insert_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute node groups list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the NodeGroupList result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_node_groups_list(
        &self,
        args: &ComputeNodeGroupsListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<NodeGroupList, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_node_groups_list_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.filter,
            &args.maxResults,
            &args.orderBy,
            &args.pageToken,
            &args.returnPartialSuccess,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_node_groups_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute node groups list nodes.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the NodeGroupsListNodes result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_node_groups_list_nodes(
        &self,
        args: &ComputeNodeGroupsListNodesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<NodeGroupsListNodes, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_node_groups_list_nodes_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.nodeGroup,
            &args.filter,
            &args.maxResults,
            &args.orderBy,
            &args.pageToken,
            &args.returnPartialSuccess,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_node_groups_list_nodes_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute node groups patch.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_node_groups_patch(
        &self,
        args: &ComputeNodeGroupsPatchArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_node_groups_patch_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.nodeGroup,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_node_groups_patch_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute node groups perform maintenance.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_node_groups_perform_maintenance(
        &self,
        args: &ComputeNodeGroupsPerformMaintenanceArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_node_groups_perform_maintenance_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.nodeGroup,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_node_groups_perform_maintenance_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute node groups set iam policy.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Policy result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_node_groups_set_iam_policy(
        &self,
        args: &ComputeNodeGroupsSetIamPolicyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Policy, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_node_groups_set_iam_policy_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.resource,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_node_groups_set_iam_policy_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute node groups set node template.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_node_groups_set_node_template(
        &self,
        args: &ComputeNodeGroupsSetNodeTemplateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_node_groups_set_node_template_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.nodeGroup,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_node_groups_set_node_template_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute node groups simulate maintenance event.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_node_groups_simulate_maintenance_event(
        &self,
        args: &ComputeNodeGroupsSimulateMaintenanceEventArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_node_groups_simulate_maintenance_event_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.nodeGroup,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_node_groups_simulate_maintenance_event_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute node groups test iam permissions.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TestPermissionsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_node_groups_test_iam_permissions(
        &self,
        args: &ComputeNodeGroupsTestIamPermissionsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TestPermissionsResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_node_groups_test_iam_permissions_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.resource,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_node_groups_test_iam_permissions_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute node templates aggregated list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the NodeTemplateAggregatedList result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_node_templates_aggregated_list(
        &self,
        args: &ComputeNodeTemplatesAggregatedListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<NodeTemplateAggregatedList, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_node_templates_aggregated_list_builder(
            &self.http_client,
            &args.project,
            &args.filter,
            &args.includeAllScopes,
            &args.maxResults,
            &args.orderBy,
            &args.pageToken,
            &args.returnPartialSuccess,
            &args.serviceProjectNumber,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_node_templates_aggregated_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute node templates delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_node_templates_delete(
        &self,
        args: &ComputeNodeTemplatesDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_node_templates_delete_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.nodeTemplate,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_node_templates_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute node templates get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the NodeTemplate result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_node_templates_get(
        &self,
        args: &ComputeNodeTemplatesGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<NodeTemplate, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_node_templates_get_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.nodeTemplate,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_node_templates_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute node templates get iam policy.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Policy result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_node_templates_get_iam_policy(
        &self,
        args: &ComputeNodeTemplatesGetIamPolicyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Policy, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_node_templates_get_iam_policy_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.resource,
            &args.optionsRequestedPolicyVersion,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_node_templates_get_iam_policy_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute node templates insert.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_node_templates_insert(
        &self,
        args: &ComputeNodeTemplatesInsertArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_node_templates_insert_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_node_templates_insert_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute node templates list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the NodeTemplateList result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_node_templates_list(
        &self,
        args: &ComputeNodeTemplatesListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<NodeTemplateList, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_node_templates_list_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.filter,
            &args.maxResults,
            &args.orderBy,
            &args.pageToken,
            &args.returnPartialSuccess,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_node_templates_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute node templates set iam policy.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Policy result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_node_templates_set_iam_policy(
        &self,
        args: &ComputeNodeTemplatesSetIamPolicyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Policy, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_node_templates_set_iam_policy_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.resource,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_node_templates_set_iam_policy_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute node templates test iam permissions.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TestPermissionsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_node_templates_test_iam_permissions(
        &self,
        args: &ComputeNodeTemplatesTestIamPermissionsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TestPermissionsResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_node_templates_test_iam_permissions_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.resource,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_node_templates_test_iam_permissions_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute node types aggregated list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the NodeTypeAggregatedList result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_node_types_aggregated_list(
        &self,
        args: &ComputeNodeTypesAggregatedListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<NodeTypeAggregatedList, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_node_types_aggregated_list_builder(
            &self.http_client,
            &args.project,
            &args.filter,
            &args.includeAllScopes,
            &args.maxResults,
            &args.orderBy,
            &args.pageToken,
            &args.returnPartialSuccess,
            &args.serviceProjectNumber,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_node_types_aggregated_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute node types get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the NodeType result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_node_types_get(
        &self,
        args: &ComputeNodeTypesGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<NodeType, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_node_types_get_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.nodeType,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_node_types_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute node types list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the NodeTypeList result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_node_types_list(
        &self,
        args: &ComputeNodeTypesListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<NodeTypeList, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_node_types_list_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.filter,
            &args.maxResults,
            &args.orderBy,
            &args.pageToken,
            &args.returnPartialSuccess,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_node_types_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute organization security policies add association.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_organization_security_policies_add_association(
        &self,
        args: &ComputeOrganizationSecurityPoliciesAddAssociationArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_organization_security_policies_add_association_builder(
            &self.http_client,
            &args.securityPolicy,
            &args.replaceExistingAssociation,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_organization_security_policies_add_association_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute organization security policies add rule.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_organization_security_policies_add_rule(
        &self,
        args: &ComputeOrganizationSecurityPoliciesAddRuleArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_organization_security_policies_add_rule_builder(
            &self.http_client,
            &args.securityPolicy,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_organization_security_policies_add_rule_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute organization security policies copy rules.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_organization_security_policies_copy_rules(
        &self,
        args: &ComputeOrganizationSecurityPoliciesCopyRulesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_organization_security_policies_copy_rules_builder(
            &self.http_client,
            &args.securityPolicy,
            &args.requestId,
            &args.sourceSecurityPolicy,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_organization_security_policies_copy_rules_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute organization security policies delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_organization_security_policies_delete(
        &self,
        args: &ComputeOrganizationSecurityPoliciesDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_organization_security_policies_delete_builder(
            &self.http_client,
            &args.securityPolicy,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_organization_security_policies_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute organization security policies get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the SecurityPolicy result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_organization_security_policies_get(
        &self,
        args: &ComputeOrganizationSecurityPoliciesGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<SecurityPolicy, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_organization_security_policies_get_builder(
            &self.http_client,
            &args.securityPolicy,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_organization_security_policies_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute organization security policies get association.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the SecurityPolicyAssociation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_organization_security_policies_get_association(
        &self,
        args: &ComputeOrganizationSecurityPoliciesGetAssociationArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<SecurityPolicyAssociation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_organization_security_policies_get_association_builder(
            &self.http_client,
            &args.securityPolicy,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_organization_security_policies_get_association_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute organization security policies get rule.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the SecurityPolicyRule result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_organization_security_policies_get_rule(
        &self,
        args: &ComputeOrganizationSecurityPoliciesGetRuleArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<SecurityPolicyRule, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_organization_security_policies_get_rule_builder(
            &self.http_client,
            &args.securityPolicy,
            &args.priority,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_organization_security_policies_get_rule_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute organization security policies insert.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_organization_security_policies_insert(
        &self,
        args: &ComputeOrganizationSecurityPoliciesInsertArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_organization_security_policies_insert_builder(
            &self.http_client,
            &args.parentId,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_organization_security_policies_insert_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute organization security policies list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the SecurityPolicyList result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_organization_security_policies_list(
        &self,
        args: &ComputeOrganizationSecurityPoliciesListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<SecurityPolicyList, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_organization_security_policies_list_builder(
            &self.http_client,
            &args.filter,
            &args.maxResults,
            &args.orderBy,
            &args.pageToken,
            &args.parentId,
            &args.returnPartialSuccess,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_organization_security_policies_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute organization security policies list associations.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the OrganizationSecurityPoliciesListAssociationsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_organization_security_policies_list_associations(
        &self,
        args: &ComputeOrganizationSecurityPoliciesListAssociationsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<OrganizationSecurityPoliciesListAssociationsResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_organization_security_policies_list_associations_builder(
            &self.http_client,
            &args.targetResource,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_organization_security_policies_list_associations_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute organization security policies list preconfigured expression sets.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the SecurityPoliciesListPreconfiguredExpressionSetsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_organization_security_policies_list_preconfigured_expression_sets(
        &self,
        args: &ComputeOrganizationSecurityPoliciesListPreconfiguredExpressionSetsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<SecurityPoliciesListPreconfiguredExpressionSetsResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_organization_security_policies_list_preconfigured_expression_sets_builder(
            &self.http_client,
            &args.filter,
            &args.maxResults,
            &args.orderBy,
            &args.pageToken,
            &args.parentId,
            &args.returnPartialSuccess,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_organization_security_policies_list_preconfigured_expression_sets_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute organization security policies move.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_organization_security_policies_move(
        &self,
        args: &ComputeOrganizationSecurityPoliciesMoveArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_organization_security_policies_move_builder(
            &self.http_client,
            &args.securityPolicy,
            &args.parentId,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_organization_security_policies_move_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute organization security policies patch.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_organization_security_policies_patch(
        &self,
        args: &ComputeOrganizationSecurityPoliciesPatchArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_organization_security_policies_patch_builder(
            &self.http_client,
            &args.securityPolicy,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_organization_security_policies_patch_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute organization security policies patch rule.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_organization_security_policies_patch_rule(
        &self,
        args: &ComputeOrganizationSecurityPoliciesPatchRuleArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_organization_security_policies_patch_rule_builder(
            &self.http_client,
            &args.securityPolicy,
            &args.priority,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_organization_security_policies_patch_rule_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute organization security policies remove association.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_organization_security_policies_remove_association(
        &self,
        args: &ComputeOrganizationSecurityPoliciesRemoveAssociationArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_organization_security_policies_remove_association_builder(
            &self.http_client,
            &args.securityPolicy,
            &args.name,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_organization_security_policies_remove_association_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute organization security policies remove rule.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_organization_security_policies_remove_rule(
        &self,
        args: &ComputeOrganizationSecurityPoliciesRemoveRuleArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_organization_security_policies_remove_rule_builder(
            &self.http_client,
            &args.securityPolicy,
            &args.priority,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_organization_security_policies_remove_rule_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute packet mirrorings aggregated list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the PacketMirroringAggregatedList result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_packet_mirrorings_aggregated_list(
        &self,
        args: &ComputePacketMirroringsAggregatedListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<PacketMirroringAggregatedList, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_packet_mirrorings_aggregated_list_builder(
            &self.http_client,
            &args.project,
            &args.filter,
            &args.includeAllScopes,
            &args.maxResults,
            &args.orderBy,
            &args.pageToken,
            &args.returnPartialSuccess,
            &args.serviceProjectNumber,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_packet_mirrorings_aggregated_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute packet mirrorings delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_packet_mirrorings_delete(
        &self,
        args: &ComputePacketMirroringsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_packet_mirrorings_delete_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.packetMirroring,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_packet_mirrorings_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute packet mirrorings get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the PacketMirroring result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_packet_mirrorings_get(
        &self,
        args: &ComputePacketMirroringsGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<PacketMirroring, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_packet_mirrorings_get_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.packetMirroring,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_packet_mirrorings_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute packet mirrorings insert.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_packet_mirrorings_insert(
        &self,
        args: &ComputePacketMirroringsInsertArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_packet_mirrorings_insert_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_packet_mirrorings_insert_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute packet mirrorings list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the PacketMirroringList result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_packet_mirrorings_list(
        &self,
        args: &ComputePacketMirroringsListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<PacketMirroringList, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_packet_mirrorings_list_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.filter,
            &args.maxResults,
            &args.orderBy,
            &args.pageToken,
            &args.returnPartialSuccess,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_packet_mirrorings_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute packet mirrorings patch.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_packet_mirrorings_patch(
        &self,
        args: &ComputePacketMirroringsPatchArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_packet_mirrorings_patch_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.packetMirroring,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_packet_mirrorings_patch_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute packet mirrorings test iam permissions.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TestPermissionsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_packet_mirrorings_test_iam_permissions(
        &self,
        args: &ComputePacketMirroringsTestIamPermissionsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TestPermissionsResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_packet_mirrorings_test_iam_permissions_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.resource,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_packet_mirrorings_test_iam_permissions_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute preview features get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the PreviewFeature result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_preview_features_get(
        &self,
        args: &ComputePreviewFeaturesGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<PreviewFeature, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_preview_features_get_builder(
            &self.http_client,
            &args.project,
            &args.previewFeature,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_preview_features_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute preview features list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the PreviewFeatureList result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_preview_features_list(
        &self,
        args: &ComputePreviewFeaturesListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<PreviewFeatureList, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_preview_features_list_builder(
            &self.http_client,
            &args.project,
            &args.filter,
            &args.maxResults,
            &args.orderBy,
            &args.pageToken,
            &args.returnPartialSuccess,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_preview_features_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute preview features update.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_preview_features_update(
        &self,
        args: &ComputePreviewFeaturesUpdateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_preview_features_update_builder(
            &self.http_client,
            &args.project,
            &args.previewFeature,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_preview_features_update_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute projects disable xpn host.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_projects_disable_xpn_host(
        &self,
        args: &ComputeProjectsDisableXpnHostArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_projects_disable_xpn_host_builder(
            &self.http_client,
            &args.project,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_projects_disable_xpn_host_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute projects disable xpn resource.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_projects_disable_xpn_resource(
        &self,
        args: &ComputeProjectsDisableXpnResourceArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_projects_disable_xpn_resource_builder(
            &self.http_client,
            &args.project,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_projects_disable_xpn_resource_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute projects enable xpn host.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_projects_enable_xpn_host(
        &self,
        args: &ComputeProjectsEnableXpnHostArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_projects_enable_xpn_host_builder(
            &self.http_client,
            &args.project,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_projects_enable_xpn_host_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute projects enable xpn resource.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_projects_enable_xpn_resource(
        &self,
        args: &ComputeProjectsEnableXpnResourceArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_projects_enable_xpn_resource_builder(
            &self.http_client,
            &args.project,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_projects_enable_xpn_resource_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute projects get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Project result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_projects_get(
        &self,
        args: &ComputeProjectsGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Project, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_projects_get_builder(
            &self.http_client,
            &args.project,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_projects_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute projects get xpn host.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Project result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_projects_get_xpn_host(
        &self,
        args: &ComputeProjectsGetXpnHostArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Project, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_projects_get_xpn_host_builder(
            &self.http_client,
            &args.project,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_projects_get_xpn_host_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute projects get xpn resources.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ProjectsGetXpnResources result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_projects_get_xpn_resources(
        &self,
        args: &ComputeProjectsGetXpnResourcesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ProjectsGetXpnResources, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_projects_get_xpn_resources_builder(
            &self.http_client,
            &args.project,
            &args.filter,
            &args.maxResults,
            &args.orderBy,
            &args.pageToken,
            &args.returnPartialSuccess,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_projects_get_xpn_resources_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute projects list xpn hosts.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the XpnHostList result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_projects_list_xpn_hosts(
        &self,
        args: &ComputeProjectsListXpnHostsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<XpnHostList, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_projects_list_xpn_hosts_builder(
            &self.http_client,
            &args.project,
            &args.filter,
            &args.maxResults,
            &args.orderBy,
            &args.pageToken,
            &args.returnPartialSuccess,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_projects_list_xpn_hosts_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute projects move disk.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_projects_move_disk(
        &self,
        args: &ComputeProjectsMoveDiskArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_projects_move_disk_builder(
            &self.http_client,
            &args.project,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_projects_move_disk_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute projects move instance.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_projects_move_instance(
        &self,
        args: &ComputeProjectsMoveInstanceArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_projects_move_instance_builder(
            &self.http_client,
            &args.project,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_projects_move_instance_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute projects set cloud armor tier.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_projects_set_cloud_armor_tier(
        &self,
        args: &ComputeProjectsSetCloudArmorTierArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_projects_set_cloud_armor_tier_builder(
            &self.http_client,
            &args.project,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_projects_set_cloud_armor_tier_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute projects set common instance metadata.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_projects_set_common_instance_metadata(
        &self,
        args: &ComputeProjectsSetCommonInstanceMetadataArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_projects_set_common_instance_metadata_builder(
            &self.http_client,
            &args.project,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_projects_set_common_instance_metadata_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute projects set default network tier.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_projects_set_default_network_tier(
        &self,
        args: &ComputeProjectsSetDefaultNetworkTierArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_projects_set_default_network_tier_builder(
            &self.http_client,
            &args.project,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_projects_set_default_network_tier_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute projects set usage export bucket.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_projects_set_usage_export_bucket(
        &self,
        args: &ComputeProjectsSetUsageExportBucketArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_projects_set_usage_export_bucket_builder(
            &self.http_client,
            &args.project,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_projects_set_usage_export_bucket_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute public advertised prefixes announce.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_public_advertised_prefixes_announce(
        &self,
        args: &ComputePublicAdvertisedPrefixesAnnounceArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_public_advertised_prefixes_announce_builder(
            &self.http_client,
            &args.project,
            &args.publicAdvertisedPrefix,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_public_advertised_prefixes_announce_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute public advertised prefixes delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_public_advertised_prefixes_delete(
        &self,
        args: &ComputePublicAdvertisedPrefixesDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_public_advertised_prefixes_delete_builder(
            &self.http_client,
            &args.project,
            &args.publicAdvertisedPrefix,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_public_advertised_prefixes_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute public advertised prefixes get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the PublicAdvertisedPrefix result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_public_advertised_prefixes_get(
        &self,
        args: &ComputePublicAdvertisedPrefixesGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<PublicAdvertisedPrefix, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_public_advertised_prefixes_get_builder(
            &self.http_client,
            &args.project,
            &args.publicAdvertisedPrefix,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_public_advertised_prefixes_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute public advertised prefixes insert.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_public_advertised_prefixes_insert(
        &self,
        args: &ComputePublicAdvertisedPrefixesInsertArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_public_advertised_prefixes_insert_builder(
            &self.http_client,
            &args.project,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_public_advertised_prefixes_insert_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute public advertised prefixes list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the PublicAdvertisedPrefixList result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_public_advertised_prefixes_list(
        &self,
        args: &ComputePublicAdvertisedPrefixesListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<PublicAdvertisedPrefixList, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_public_advertised_prefixes_list_builder(
            &self.http_client,
            &args.project,
            &args.filter,
            &args.maxResults,
            &args.orderBy,
            &args.pageToken,
            &args.returnPartialSuccess,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_public_advertised_prefixes_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute public advertised prefixes patch.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_public_advertised_prefixes_patch(
        &self,
        args: &ComputePublicAdvertisedPrefixesPatchArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_public_advertised_prefixes_patch_builder(
            &self.http_client,
            &args.project,
            &args.publicAdvertisedPrefix,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_public_advertised_prefixes_patch_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute public advertised prefixes withdraw.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_public_advertised_prefixes_withdraw(
        &self,
        args: &ComputePublicAdvertisedPrefixesWithdrawArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_public_advertised_prefixes_withdraw_builder(
            &self.http_client,
            &args.project,
            &args.publicAdvertisedPrefix,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_public_advertised_prefixes_withdraw_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute public delegated prefixes aggregated list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the PublicDelegatedPrefixAggregatedList result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_public_delegated_prefixes_aggregated_list(
        &self,
        args: &ComputePublicDelegatedPrefixesAggregatedListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<PublicDelegatedPrefixAggregatedList, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_public_delegated_prefixes_aggregated_list_builder(
            &self.http_client,
            &args.project,
            &args.filter,
            &args.includeAllScopes,
            &args.maxResults,
            &args.orderBy,
            &args.pageToken,
            &args.returnPartialSuccess,
            &args.serviceProjectNumber,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_public_delegated_prefixes_aggregated_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute public delegated prefixes announce.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_public_delegated_prefixes_announce(
        &self,
        args: &ComputePublicDelegatedPrefixesAnnounceArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_public_delegated_prefixes_announce_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.publicDelegatedPrefix,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_public_delegated_prefixes_announce_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute public delegated prefixes delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_public_delegated_prefixes_delete(
        &self,
        args: &ComputePublicDelegatedPrefixesDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_public_delegated_prefixes_delete_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.publicDelegatedPrefix,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_public_delegated_prefixes_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute public delegated prefixes get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the PublicDelegatedPrefix result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_public_delegated_prefixes_get(
        &self,
        args: &ComputePublicDelegatedPrefixesGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<PublicDelegatedPrefix, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_public_delegated_prefixes_get_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.publicDelegatedPrefix,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_public_delegated_prefixes_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute public delegated prefixes insert.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_public_delegated_prefixes_insert(
        &self,
        args: &ComputePublicDelegatedPrefixesInsertArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_public_delegated_prefixes_insert_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_public_delegated_prefixes_insert_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute public delegated prefixes list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the PublicDelegatedPrefixList result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_public_delegated_prefixes_list(
        &self,
        args: &ComputePublicDelegatedPrefixesListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<PublicDelegatedPrefixList, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_public_delegated_prefixes_list_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.filter,
            &args.maxResults,
            &args.orderBy,
            &args.pageToken,
            &args.returnPartialSuccess,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_public_delegated_prefixes_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute public delegated prefixes patch.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_public_delegated_prefixes_patch(
        &self,
        args: &ComputePublicDelegatedPrefixesPatchArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_public_delegated_prefixes_patch_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.publicDelegatedPrefix,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_public_delegated_prefixes_patch_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute public delegated prefixes withdraw.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_public_delegated_prefixes_withdraw(
        &self,
        args: &ComputePublicDelegatedPrefixesWithdrawArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_public_delegated_prefixes_withdraw_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.publicDelegatedPrefix,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_public_delegated_prefixes_withdraw_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region autoscalers delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_region_autoscalers_delete(
        &self,
        args: &ComputeRegionAutoscalersDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_autoscalers_delete_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.autoscaler,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_autoscalers_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region autoscalers get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Autoscaler result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_region_autoscalers_get(
        &self,
        args: &ComputeRegionAutoscalersGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Autoscaler, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_autoscalers_get_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.autoscaler,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_autoscalers_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region autoscalers insert.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_region_autoscalers_insert(
        &self,
        args: &ComputeRegionAutoscalersInsertArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_autoscalers_insert_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_autoscalers_insert_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region autoscalers list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the RegionAutoscalerList result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_region_autoscalers_list(
        &self,
        args: &ComputeRegionAutoscalersListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<RegionAutoscalerList, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_autoscalers_list_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.filter,
            &args.maxResults,
            &args.orderBy,
            &args.pageToken,
            &args.returnPartialSuccess,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_autoscalers_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region autoscalers patch.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_region_autoscalers_patch(
        &self,
        args: &ComputeRegionAutoscalersPatchArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_autoscalers_patch_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.autoscaler,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_autoscalers_patch_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region autoscalers test iam permissions.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TestPermissionsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_region_autoscalers_test_iam_permissions(
        &self,
        args: &ComputeRegionAutoscalersTestIamPermissionsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TestPermissionsResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_autoscalers_test_iam_permissions_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.resource,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_autoscalers_test_iam_permissions_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region autoscalers update.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_region_autoscalers_update(
        &self,
        args: &ComputeRegionAutoscalersUpdateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_autoscalers_update_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.autoscaler,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_autoscalers_update_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region backend buckets delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_region_backend_buckets_delete(
        &self,
        args: &ComputeRegionBackendBucketsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_backend_buckets_delete_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.backendBucket,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_backend_buckets_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region backend buckets get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the BackendBucket result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_region_backend_buckets_get(
        &self,
        args: &ComputeRegionBackendBucketsGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<BackendBucket, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_backend_buckets_get_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.backendBucket,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_backend_buckets_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region backend buckets get iam policy.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Policy result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_region_backend_buckets_get_iam_policy(
        &self,
        args: &ComputeRegionBackendBucketsGetIamPolicyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Policy, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_backend_buckets_get_iam_policy_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.resource,
            &args.optionsRequestedPolicyVersion,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_backend_buckets_get_iam_policy_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region backend buckets insert.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_region_backend_buckets_insert(
        &self,
        args: &ComputeRegionBackendBucketsInsertArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_backend_buckets_insert_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_backend_buckets_insert_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region backend buckets list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the BackendBucketList result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_region_backend_buckets_list(
        &self,
        args: &ComputeRegionBackendBucketsListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<BackendBucketList, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_backend_buckets_list_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.filter,
            &args.maxResults,
            &args.orderBy,
            &args.pageToken,
            &args.returnPartialSuccess,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_backend_buckets_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region backend buckets list usable.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the BackendBucketListUsable result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_region_backend_buckets_list_usable(
        &self,
        args: &ComputeRegionBackendBucketsListUsableArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<BackendBucketListUsable, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_backend_buckets_list_usable_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.filter,
            &args.maxResults,
            &args.orderBy,
            &args.pageToken,
            &args.returnPartialSuccess,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_backend_buckets_list_usable_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region backend buckets patch.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_region_backend_buckets_patch(
        &self,
        args: &ComputeRegionBackendBucketsPatchArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_backend_buckets_patch_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.backendBucket,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_backend_buckets_patch_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region backend buckets set iam policy.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Policy result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_region_backend_buckets_set_iam_policy(
        &self,
        args: &ComputeRegionBackendBucketsSetIamPolicyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Policy, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_backend_buckets_set_iam_policy_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.resource,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_backend_buckets_set_iam_policy_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region backend buckets test iam permissions.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TestPermissionsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_region_backend_buckets_test_iam_permissions(
        &self,
        args: &ComputeRegionBackendBucketsTestIamPermissionsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TestPermissionsResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_backend_buckets_test_iam_permissions_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.resource,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_backend_buckets_test_iam_permissions_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region backend services delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_region_backend_services_delete(
        &self,
        args: &ComputeRegionBackendServicesDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_backend_services_delete_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.backendService,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_backend_services_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region backend services get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the BackendService result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_region_backend_services_get(
        &self,
        args: &ComputeRegionBackendServicesGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<BackendService, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_backend_services_get_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.backendService,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_backend_services_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region backend services get health.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the BackendServiceGroupHealth result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_region_backend_services_get_health(
        &self,
        args: &ComputeRegionBackendServicesGetHealthArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<BackendServiceGroupHealth, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_backend_services_get_health_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.backendService,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_backend_services_get_health_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region backend services get iam policy.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Policy result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_region_backend_services_get_iam_policy(
        &self,
        args: &ComputeRegionBackendServicesGetIamPolicyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Policy, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_backend_services_get_iam_policy_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.resource,
            &args.optionsRequestedPolicyVersion,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_backend_services_get_iam_policy_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region backend services insert.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_region_backend_services_insert(
        &self,
        args: &ComputeRegionBackendServicesInsertArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_backend_services_insert_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_backend_services_insert_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region backend services list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the BackendServiceList result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_region_backend_services_list(
        &self,
        args: &ComputeRegionBackendServicesListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<BackendServiceList, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_backend_services_list_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.filter,
            &args.maxResults,
            &args.orderBy,
            &args.pageToken,
            &args.returnPartialSuccess,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_backend_services_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region backend services list usable.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the BackendServiceListUsable result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_region_backend_services_list_usable(
        &self,
        args: &ComputeRegionBackendServicesListUsableArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<BackendServiceListUsable, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_backend_services_list_usable_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.filter,
            &args.maxResults,
            &args.orderBy,
            &args.pageToken,
            &args.returnPartialSuccess,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_backend_services_list_usable_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region backend services patch.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_region_backend_services_patch(
        &self,
        args: &ComputeRegionBackendServicesPatchArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_backend_services_patch_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.backendService,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_backend_services_patch_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region backend services set iam policy.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Policy result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_region_backend_services_set_iam_policy(
        &self,
        args: &ComputeRegionBackendServicesSetIamPolicyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Policy, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_backend_services_set_iam_policy_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.resource,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_backend_services_set_iam_policy_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region backend services set security policy.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_region_backend_services_set_security_policy(
        &self,
        args: &ComputeRegionBackendServicesSetSecurityPolicyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_backend_services_set_security_policy_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.backendService,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_backend_services_set_security_policy_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region backend services test iam permissions.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TestPermissionsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_region_backend_services_test_iam_permissions(
        &self,
        args: &ComputeRegionBackendServicesTestIamPermissionsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TestPermissionsResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_backend_services_test_iam_permissions_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.resource,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_backend_services_test_iam_permissions_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region backend services update.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_region_backend_services_update(
        &self,
        args: &ComputeRegionBackendServicesUpdateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_backend_services_update_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.backendService,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_backend_services_update_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region commitments aggregated list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the CommitmentAggregatedList result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_region_commitments_aggregated_list(
        &self,
        args: &ComputeRegionCommitmentsAggregatedListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<CommitmentAggregatedList, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_commitments_aggregated_list_builder(
            &self.http_client,
            &args.project,
            &args.filter,
            &args.includeAllScopes,
            &args.maxResults,
            &args.orderBy,
            &args.pageToken,
            &args.returnPartialSuccess,
            &args.serviceProjectNumber,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_commitments_aggregated_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region commitments get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Commitment result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_region_commitments_get(
        &self,
        args: &ComputeRegionCommitmentsGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Commitment, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_commitments_get_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.commitment,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_commitments_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region commitments insert.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_region_commitments_insert(
        &self,
        args: &ComputeRegionCommitmentsInsertArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_commitments_insert_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_commitments_insert_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region commitments list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the CommitmentList result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_region_commitments_list(
        &self,
        args: &ComputeRegionCommitmentsListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<CommitmentList, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_commitments_list_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.filter,
            &args.maxResults,
            &args.orderBy,
            &args.pageToken,
            &args.returnPartialSuccess,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_commitments_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region commitments update.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_region_commitments_update(
        &self,
        args: &ComputeRegionCommitmentsUpdateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_commitments_update_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.commitment,
            &args.paths,
            &args.requestId,
            &args.updateMask,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_commitments_update_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region composite health checks aggregated list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the CompositeHealthCheckAggregatedList result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_region_composite_health_checks_aggregated_list(
        &self,
        args: &ComputeRegionCompositeHealthChecksAggregatedListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<CompositeHealthCheckAggregatedList, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_composite_health_checks_aggregated_list_builder(
            &self.http_client,
            &args.project,
            &args.filter,
            &args.includeAllScopes,
            &args.maxResults,
            &args.orderBy,
            &args.pageToken,
            &args.returnPartialSuccess,
            &args.serviceProjectNumber,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_composite_health_checks_aggregated_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region composite health checks delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_region_composite_health_checks_delete(
        &self,
        args: &ComputeRegionCompositeHealthChecksDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_composite_health_checks_delete_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.compositeHealthCheck,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_composite_health_checks_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region composite health checks get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the CompositeHealthCheck result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_region_composite_health_checks_get(
        &self,
        args: &ComputeRegionCompositeHealthChecksGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<CompositeHealthCheck, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_composite_health_checks_get_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.compositeHealthCheck,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_composite_health_checks_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region composite health checks get health.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the CompositeHealthCheckHealth result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_region_composite_health_checks_get_health(
        &self,
        args: &ComputeRegionCompositeHealthChecksGetHealthArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<CompositeHealthCheckHealth, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_composite_health_checks_get_health_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.compositeHealthCheck,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_composite_health_checks_get_health_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region composite health checks insert.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_region_composite_health_checks_insert(
        &self,
        args: &ComputeRegionCompositeHealthChecksInsertArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_composite_health_checks_insert_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_composite_health_checks_insert_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region composite health checks list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the CompositeHealthCheckList result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_region_composite_health_checks_list(
        &self,
        args: &ComputeRegionCompositeHealthChecksListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<CompositeHealthCheckList, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_composite_health_checks_list_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.filter,
            &args.maxResults,
            &args.orderBy,
            &args.pageToken,
            &args.returnPartialSuccess,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_composite_health_checks_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region composite health checks patch.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_region_composite_health_checks_patch(
        &self,
        args: &ComputeRegionCompositeHealthChecksPatchArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_composite_health_checks_patch_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.compositeHealthCheck,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_composite_health_checks_patch_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region composite health checks test iam permissions.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TestPermissionsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_region_composite_health_checks_test_iam_permissions(
        &self,
        args: &ComputeRegionCompositeHealthChecksTestIamPermissionsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TestPermissionsResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_composite_health_checks_test_iam_permissions_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.resource,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_composite_health_checks_test_iam_permissions_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region disk types get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the DiskType result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_region_disk_types_get(
        &self,
        args: &ComputeRegionDiskTypesGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<DiskType, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_disk_types_get_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.diskType,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_disk_types_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region disk types list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the RegionDiskTypeList result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_region_disk_types_list(
        &self,
        args: &ComputeRegionDiskTypesListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<RegionDiskTypeList, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_disk_types_list_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.filter,
            &args.maxResults,
            &args.orderBy,
            &args.pageToken,
            &args.returnPartialSuccess,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_disk_types_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region disks add resource policies.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_region_disks_add_resource_policies(
        &self,
        args: &ComputeRegionDisksAddResourcePoliciesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_disks_add_resource_policies_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.disk,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_disks_add_resource_policies_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region disks bulk insert.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_region_disks_bulk_insert(
        &self,
        args: &ComputeRegionDisksBulkInsertArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_disks_bulk_insert_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_disks_bulk_insert_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region disks create snapshot.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_region_disks_create_snapshot(
        &self,
        args: &ComputeRegionDisksCreateSnapshotArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_disks_create_snapshot_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.disk,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_disks_create_snapshot_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region disks delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_region_disks_delete(
        &self,
        args: &ComputeRegionDisksDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_disks_delete_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.disk,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_disks_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region disks get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Disk result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_region_disks_get(
        &self,
        args: &ComputeRegionDisksGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Disk, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_disks_get_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.disk,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_disks_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region disks get iam policy.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Policy result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_region_disks_get_iam_policy(
        &self,
        args: &ComputeRegionDisksGetIamPolicyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Policy, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_disks_get_iam_policy_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.resource,
            &args.optionsRequestedPolicyVersion,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_disks_get_iam_policy_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region disks insert.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_region_disks_insert(
        &self,
        args: &ComputeRegionDisksInsertArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_disks_insert_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.requestId,
            &args.sourceImage,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_disks_insert_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region disks list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the DiskList result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_region_disks_list(
        &self,
        args: &ComputeRegionDisksListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<DiskList, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_disks_list_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.filter,
            &args.maxResults,
            &args.orderBy,
            &args.pageToken,
            &args.returnPartialSuccess,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_disks_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region disks remove resource policies.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_region_disks_remove_resource_policies(
        &self,
        args: &ComputeRegionDisksRemoveResourcePoliciesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_disks_remove_resource_policies_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.disk,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_disks_remove_resource_policies_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region disks resize.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_region_disks_resize(
        &self,
        args: &ComputeRegionDisksResizeArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_disks_resize_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.disk,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_disks_resize_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region disks set iam policy.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Policy result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_region_disks_set_iam_policy(
        &self,
        args: &ComputeRegionDisksSetIamPolicyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Policy, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_disks_set_iam_policy_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.resource,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_disks_set_iam_policy_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region disks set labels.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_region_disks_set_labels(
        &self,
        args: &ComputeRegionDisksSetLabelsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_disks_set_labels_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.resource,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_disks_set_labels_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region disks start async replication.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_region_disks_start_async_replication(
        &self,
        args: &ComputeRegionDisksStartAsyncReplicationArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_disks_start_async_replication_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.disk,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_disks_start_async_replication_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region disks stop async replication.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_region_disks_stop_async_replication(
        &self,
        args: &ComputeRegionDisksStopAsyncReplicationArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_disks_stop_async_replication_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.disk,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_disks_stop_async_replication_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region disks stop group async replication.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_region_disks_stop_group_async_replication(
        &self,
        args: &ComputeRegionDisksStopGroupAsyncReplicationArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_disks_stop_group_async_replication_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_disks_stop_group_async_replication_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region disks test iam permissions.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TestPermissionsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_region_disks_test_iam_permissions(
        &self,
        args: &ComputeRegionDisksTestIamPermissionsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TestPermissionsResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_disks_test_iam_permissions_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.resource,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_disks_test_iam_permissions_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region disks update.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_region_disks_update(
        &self,
        args: &ComputeRegionDisksUpdateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_disks_update_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.disk,
            &args.paths,
            &args.requestId,
            &args.updateMask,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_disks_update_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region disks update kms key.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_region_disks_update_kms_key(
        &self,
        args: &ComputeRegionDisksUpdateKmsKeyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_disks_update_kms_key_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.disk,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_disks_update_kms_key_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region health aggregation policies aggregated list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the HealthAggregationPolicyAggregatedList result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_region_health_aggregation_policies_aggregated_list(
        &self,
        args: &ComputeRegionHealthAggregationPoliciesAggregatedListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<HealthAggregationPolicyAggregatedList, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_health_aggregation_policies_aggregated_list_builder(
            &self.http_client,
            &args.project,
            &args.filter,
            &args.includeAllScopes,
            &args.maxResults,
            &args.orderBy,
            &args.pageToken,
            &args.returnPartialSuccess,
            &args.serviceProjectNumber,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_health_aggregation_policies_aggregated_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region health aggregation policies delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_region_health_aggregation_policies_delete(
        &self,
        args: &ComputeRegionHealthAggregationPoliciesDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_health_aggregation_policies_delete_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.healthAggregationPolicy,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_health_aggregation_policies_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region health aggregation policies get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the HealthAggregationPolicy result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_region_health_aggregation_policies_get(
        &self,
        args: &ComputeRegionHealthAggregationPoliciesGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<HealthAggregationPolicy, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_health_aggregation_policies_get_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.healthAggregationPolicy,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_health_aggregation_policies_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region health aggregation policies insert.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_region_health_aggregation_policies_insert(
        &self,
        args: &ComputeRegionHealthAggregationPoliciesInsertArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_health_aggregation_policies_insert_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_health_aggregation_policies_insert_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region health aggregation policies list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the HealthAggregationPolicyList result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_region_health_aggregation_policies_list(
        &self,
        args: &ComputeRegionHealthAggregationPoliciesListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<HealthAggregationPolicyList, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_health_aggregation_policies_list_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.filter,
            &args.maxResults,
            &args.orderBy,
            &args.pageToken,
            &args.returnPartialSuccess,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_health_aggregation_policies_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region health aggregation policies patch.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_region_health_aggregation_policies_patch(
        &self,
        args: &ComputeRegionHealthAggregationPoliciesPatchArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_health_aggregation_policies_patch_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.healthAggregationPolicy,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_health_aggregation_policies_patch_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region health aggregation policies test iam permissions.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TestPermissionsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_region_health_aggregation_policies_test_iam_permissions(
        &self,
        args: &ComputeRegionHealthAggregationPoliciesTestIamPermissionsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TestPermissionsResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_health_aggregation_policies_test_iam_permissions_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.resource,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_health_aggregation_policies_test_iam_permissions_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region health check services aggregated list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the HealthCheckServiceAggregatedList result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_region_health_check_services_aggregated_list(
        &self,
        args: &ComputeRegionHealthCheckServicesAggregatedListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<HealthCheckServiceAggregatedList, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_health_check_services_aggregated_list_builder(
            &self.http_client,
            &args.project,
            &args.filter,
            &args.includeAllScopes,
            &args.maxResults,
            &args.orderBy,
            &args.pageToken,
            &args.returnPartialSuccess,
            &args.serviceProjectNumber,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_health_check_services_aggregated_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region health check services delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_region_health_check_services_delete(
        &self,
        args: &ComputeRegionHealthCheckServicesDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_health_check_services_delete_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.healthCheckService,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_health_check_services_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region health check services get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the HealthCheckService result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_region_health_check_services_get(
        &self,
        args: &ComputeRegionHealthCheckServicesGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<HealthCheckService, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_health_check_services_get_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.healthCheckService,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_health_check_services_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region health check services insert.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_region_health_check_services_insert(
        &self,
        args: &ComputeRegionHealthCheckServicesInsertArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_health_check_services_insert_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_health_check_services_insert_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region health check services list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the HealthCheckServicesList result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_region_health_check_services_list(
        &self,
        args: &ComputeRegionHealthCheckServicesListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<HealthCheckServicesList, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_health_check_services_list_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.filter,
            &args.maxResults,
            &args.orderBy,
            &args.pageToken,
            &args.returnPartialSuccess,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_health_check_services_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region health check services patch.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_region_health_check_services_patch(
        &self,
        args: &ComputeRegionHealthCheckServicesPatchArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_health_check_services_patch_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.healthCheckService,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_health_check_services_patch_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region health check services test iam permissions.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TestPermissionsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_region_health_check_services_test_iam_permissions(
        &self,
        args: &ComputeRegionHealthCheckServicesTestIamPermissionsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TestPermissionsResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_health_check_services_test_iam_permissions_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.resource,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_health_check_services_test_iam_permissions_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region health checks delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_region_health_checks_delete(
        &self,
        args: &ComputeRegionHealthChecksDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_health_checks_delete_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.healthCheck,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_health_checks_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region health checks get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the HealthCheck result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_region_health_checks_get(
        &self,
        args: &ComputeRegionHealthChecksGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<HealthCheck, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_health_checks_get_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.healthCheck,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_health_checks_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region health checks insert.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_region_health_checks_insert(
        &self,
        args: &ComputeRegionHealthChecksInsertArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_health_checks_insert_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_health_checks_insert_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region health checks list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the HealthCheckList result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_region_health_checks_list(
        &self,
        args: &ComputeRegionHealthChecksListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<HealthCheckList, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_health_checks_list_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.filter,
            &args.maxResults,
            &args.orderBy,
            &args.pageToken,
            &args.returnPartialSuccess,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_health_checks_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region health checks patch.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_region_health_checks_patch(
        &self,
        args: &ComputeRegionHealthChecksPatchArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_health_checks_patch_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.healthCheck,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_health_checks_patch_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region health checks test iam permissions.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TestPermissionsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_region_health_checks_test_iam_permissions(
        &self,
        args: &ComputeRegionHealthChecksTestIamPermissionsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TestPermissionsResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_health_checks_test_iam_permissions_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.resource,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_health_checks_test_iam_permissions_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region health checks update.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_region_health_checks_update(
        &self,
        args: &ComputeRegionHealthChecksUpdateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_health_checks_update_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.healthCheck,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_health_checks_update_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region health sources aggregated list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the HealthSourceAggregatedList result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_region_health_sources_aggregated_list(
        &self,
        args: &ComputeRegionHealthSourcesAggregatedListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<HealthSourceAggregatedList, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_health_sources_aggregated_list_builder(
            &self.http_client,
            &args.project,
            &args.filter,
            &args.includeAllScopes,
            &args.maxResults,
            &args.orderBy,
            &args.pageToken,
            &args.returnPartialSuccess,
            &args.serviceProjectNumber,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_health_sources_aggregated_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region health sources delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_region_health_sources_delete(
        &self,
        args: &ComputeRegionHealthSourcesDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_health_sources_delete_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.healthSource,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_health_sources_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region health sources get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the HealthSource result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_region_health_sources_get(
        &self,
        args: &ComputeRegionHealthSourcesGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<HealthSource, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_health_sources_get_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.healthSource,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_health_sources_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region health sources get health.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the HealthSourceHealth result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_region_health_sources_get_health(
        &self,
        args: &ComputeRegionHealthSourcesGetHealthArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<HealthSourceHealth, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_health_sources_get_health_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.healthSource,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_health_sources_get_health_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region health sources insert.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_region_health_sources_insert(
        &self,
        args: &ComputeRegionHealthSourcesInsertArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_health_sources_insert_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_health_sources_insert_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region health sources list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the HealthSourceList result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_region_health_sources_list(
        &self,
        args: &ComputeRegionHealthSourcesListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<HealthSourceList, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_health_sources_list_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.filter,
            &args.maxResults,
            &args.orderBy,
            &args.pageToken,
            &args.returnPartialSuccess,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_health_sources_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region health sources patch.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_region_health_sources_patch(
        &self,
        args: &ComputeRegionHealthSourcesPatchArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_health_sources_patch_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.healthSource,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_health_sources_patch_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region health sources test iam permissions.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TestPermissionsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_region_health_sources_test_iam_permissions(
        &self,
        args: &ComputeRegionHealthSourcesTestIamPermissionsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TestPermissionsResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_health_sources_test_iam_permissions_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.resource,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_health_sources_test_iam_permissions_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region instance group manager resize requests cancel.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_region_instance_group_manager_resize_requests_cancel(
        &self,
        args: &ComputeRegionInstanceGroupManagerResizeRequestsCancelArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_instance_group_manager_resize_requests_cancel_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.instanceGroupManager,
            &args.resizeRequest,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_instance_group_manager_resize_requests_cancel_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region instance group manager resize requests delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_region_instance_group_manager_resize_requests_delete(
        &self,
        args: &ComputeRegionInstanceGroupManagerResizeRequestsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_instance_group_manager_resize_requests_delete_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.instanceGroupManager,
            &args.resizeRequest,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_instance_group_manager_resize_requests_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region instance group manager resize requests get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the InstanceGroupManagerResizeRequest result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_region_instance_group_manager_resize_requests_get(
        &self,
        args: &ComputeRegionInstanceGroupManagerResizeRequestsGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<InstanceGroupManagerResizeRequest, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_instance_group_manager_resize_requests_get_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.instanceGroupManager,
            &args.resizeRequest,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_instance_group_manager_resize_requests_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region instance group manager resize requests insert.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_region_instance_group_manager_resize_requests_insert(
        &self,
        args: &ComputeRegionInstanceGroupManagerResizeRequestsInsertArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_instance_group_manager_resize_requests_insert_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.instanceGroupManager,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_instance_group_manager_resize_requests_insert_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region instance group manager resize requests list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the RegionInstanceGroupManagerResizeRequestsListResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_region_instance_group_manager_resize_requests_list(
        &self,
        args: &ComputeRegionInstanceGroupManagerResizeRequestsListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<RegionInstanceGroupManagerResizeRequestsListResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_instance_group_manager_resize_requests_list_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.instanceGroupManager,
            &args.filter,
            &args.maxResults,
            &args.orderBy,
            &args.pageToken,
            &args.returnPartialSuccess,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_instance_group_manager_resize_requests_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region instance group managers abandon instances.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_region_instance_group_managers_abandon_instances(
        &self,
        args: &ComputeRegionInstanceGroupManagersAbandonInstancesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_instance_group_managers_abandon_instances_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.instanceGroupManager,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_instance_group_managers_abandon_instances_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region instance group managers apply updates to instances.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_region_instance_group_managers_apply_updates_to_instances(
        &self,
        args: &ComputeRegionInstanceGroupManagersApplyUpdatesToInstancesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_instance_group_managers_apply_updates_to_instances_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.instanceGroupManager,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_instance_group_managers_apply_updates_to_instances_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region instance group managers create instances.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_region_instance_group_managers_create_instances(
        &self,
        args: &ComputeRegionInstanceGroupManagersCreateInstancesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_instance_group_managers_create_instances_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.instanceGroupManager,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_instance_group_managers_create_instances_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region instance group managers delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_region_instance_group_managers_delete(
        &self,
        args: &ComputeRegionInstanceGroupManagersDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_instance_group_managers_delete_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.instanceGroupManager,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_instance_group_managers_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region instance group managers delete instances.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_region_instance_group_managers_delete_instances(
        &self,
        args: &ComputeRegionInstanceGroupManagersDeleteInstancesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_instance_group_managers_delete_instances_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.instanceGroupManager,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_instance_group_managers_delete_instances_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region instance group managers delete per instance configs.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_region_instance_group_managers_delete_per_instance_configs(
        &self,
        args: &ComputeRegionInstanceGroupManagersDeletePerInstanceConfigsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_instance_group_managers_delete_per_instance_configs_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.instanceGroupManager,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_instance_group_managers_delete_per_instance_configs_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region instance group managers get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the InstanceGroupManager result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_region_instance_group_managers_get(
        &self,
        args: &ComputeRegionInstanceGroupManagersGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<InstanceGroupManager, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_instance_group_managers_get_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.instanceGroupManager,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_instance_group_managers_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region instance group managers insert.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_region_instance_group_managers_insert(
        &self,
        args: &ComputeRegionInstanceGroupManagersInsertArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_instance_group_managers_insert_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_instance_group_managers_insert_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region instance group managers list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the RegionInstanceGroupManagerList result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_region_instance_group_managers_list(
        &self,
        args: &ComputeRegionInstanceGroupManagersListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<RegionInstanceGroupManagerList, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_instance_group_managers_list_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.filter,
            &args.maxResults,
            &args.orderBy,
            &args.pageToken,
            &args.returnPartialSuccess,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_instance_group_managers_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region instance group managers list errors.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the RegionInstanceGroupManagersListErrorsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_region_instance_group_managers_list_errors(
        &self,
        args: &ComputeRegionInstanceGroupManagersListErrorsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<RegionInstanceGroupManagersListErrorsResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_instance_group_managers_list_errors_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.instanceGroupManager,
            &args.filter,
            &args.maxResults,
            &args.orderBy,
            &args.pageToken,
            &args.returnPartialSuccess,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_instance_group_managers_list_errors_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region instance group managers list managed instances.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the RegionInstanceGroupManagersListInstancesResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_region_instance_group_managers_list_managed_instances(
        &self,
        args: &ComputeRegionInstanceGroupManagersListManagedInstancesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<RegionInstanceGroupManagersListInstancesResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_instance_group_managers_list_managed_instances_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.instanceGroupManager,
            &args.filter,
            &args.maxResults,
            &args.orderBy,
            &args.pageToken,
            &args.returnPartialSuccess,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_instance_group_managers_list_managed_instances_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region instance group managers list per instance configs.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the RegionInstanceGroupManagersListInstanceConfigsResp result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_region_instance_group_managers_list_per_instance_configs(
        &self,
        args: &ComputeRegionInstanceGroupManagersListPerInstanceConfigsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<RegionInstanceGroupManagersListInstanceConfigsResp, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_instance_group_managers_list_per_instance_configs_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.instanceGroupManager,
            &args.filter,
            &args.maxResults,
            &args.orderBy,
            &args.pageToken,
            &args.returnPartialSuccess,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_instance_group_managers_list_per_instance_configs_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region instance group managers patch.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_region_instance_group_managers_patch(
        &self,
        args: &ComputeRegionInstanceGroupManagersPatchArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_instance_group_managers_patch_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.instanceGroupManager,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_instance_group_managers_patch_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region instance group managers patch per instance configs.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_region_instance_group_managers_patch_per_instance_configs(
        &self,
        args: &ComputeRegionInstanceGroupManagersPatchPerInstanceConfigsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_instance_group_managers_patch_per_instance_configs_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.instanceGroupManager,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_instance_group_managers_patch_per_instance_configs_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region instance group managers recreate instances.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_region_instance_group_managers_recreate_instances(
        &self,
        args: &ComputeRegionInstanceGroupManagersRecreateInstancesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_instance_group_managers_recreate_instances_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.instanceGroupManager,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_instance_group_managers_recreate_instances_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region instance group managers resize.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_region_instance_group_managers_resize(
        &self,
        args: &ComputeRegionInstanceGroupManagersResizeArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_instance_group_managers_resize_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.instanceGroupManager,
            &args.requestId,
            &args.size,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_instance_group_managers_resize_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region instance group managers resume instances.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_region_instance_group_managers_resume_instances(
        &self,
        args: &ComputeRegionInstanceGroupManagersResumeInstancesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_instance_group_managers_resume_instances_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.instanceGroupManager,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_instance_group_managers_resume_instances_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region instance group managers set instance template.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_region_instance_group_managers_set_instance_template(
        &self,
        args: &ComputeRegionInstanceGroupManagersSetInstanceTemplateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_instance_group_managers_set_instance_template_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.instanceGroupManager,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_instance_group_managers_set_instance_template_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region instance group managers set target pools.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_region_instance_group_managers_set_target_pools(
        &self,
        args: &ComputeRegionInstanceGroupManagersSetTargetPoolsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_instance_group_managers_set_target_pools_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.instanceGroupManager,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_instance_group_managers_set_target_pools_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region instance group managers start instances.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_region_instance_group_managers_start_instances(
        &self,
        args: &ComputeRegionInstanceGroupManagersStartInstancesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_instance_group_managers_start_instances_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.instanceGroupManager,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_instance_group_managers_start_instances_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region instance group managers stop instances.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_region_instance_group_managers_stop_instances(
        &self,
        args: &ComputeRegionInstanceGroupManagersStopInstancesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_instance_group_managers_stop_instances_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.instanceGroupManager,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_instance_group_managers_stop_instances_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region instance group managers suspend instances.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_region_instance_group_managers_suspend_instances(
        &self,
        args: &ComputeRegionInstanceGroupManagersSuspendInstancesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_instance_group_managers_suspend_instances_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.instanceGroupManager,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_instance_group_managers_suspend_instances_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region instance group managers update per instance configs.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_region_instance_group_managers_update_per_instance_configs(
        &self,
        args: &ComputeRegionInstanceGroupManagersUpdatePerInstanceConfigsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_instance_group_managers_update_per_instance_configs_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.instanceGroupManager,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_instance_group_managers_update_per_instance_configs_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region instance groups get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the InstanceGroup result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_region_instance_groups_get(
        &self,
        args: &ComputeRegionInstanceGroupsGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<InstanceGroup, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_instance_groups_get_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.instanceGroup,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_instance_groups_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region instance groups list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the RegionInstanceGroupList result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_region_instance_groups_list(
        &self,
        args: &ComputeRegionInstanceGroupsListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<RegionInstanceGroupList, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_instance_groups_list_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.filter,
            &args.maxResults,
            &args.orderBy,
            &args.pageToken,
            &args.returnPartialSuccess,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_instance_groups_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region instance groups list instances.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the RegionInstanceGroupsListInstances result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_region_instance_groups_list_instances(
        &self,
        args: &ComputeRegionInstanceGroupsListInstancesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<RegionInstanceGroupsListInstances, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_instance_groups_list_instances_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.instanceGroup,
            &args.filter,
            &args.maxResults,
            &args.orderBy,
            &args.pageToken,
            &args.returnPartialSuccess,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_instance_groups_list_instances_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region instance groups set named ports.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_region_instance_groups_set_named_ports(
        &self,
        args: &ComputeRegionInstanceGroupsSetNamedPortsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_instance_groups_set_named_ports_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.instanceGroup,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_instance_groups_set_named_ports_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region instance groups test iam permissions.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TestPermissionsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_region_instance_groups_test_iam_permissions(
        &self,
        args: &ComputeRegionInstanceGroupsTestIamPermissionsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TestPermissionsResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_instance_groups_test_iam_permissions_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.resource,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_instance_groups_test_iam_permissions_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region instance templates delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_region_instance_templates_delete(
        &self,
        args: &ComputeRegionInstanceTemplatesDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_instance_templates_delete_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.instanceTemplate,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_instance_templates_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region instance templates get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the InstanceTemplate result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_region_instance_templates_get(
        &self,
        args: &ComputeRegionInstanceTemplatesGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<InstanceTemplate, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_instance_templates_get_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.instanceTemplate,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_instance_templates_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region instance templates insert.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_region_instance_templates_insert(
        &self,
        args: &ComputeRegionInstanceTemplatesInsertArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_instance_templates_insert_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_instance_templates_insert_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region instance templates list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the InstanceTemplateList result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_region_instance_templates_list(
        &self,
        args: &ComputeRegionInstanceTemplatesListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<InstanceTemplateList, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_instance_templates_list_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.filter,
            &args.maxResults,
            &args.orderBy,
            &args.pageToken,
            &args.returnPartialSuccess,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_instance_templates_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region instances bulk insert.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_region_instances_bulk_insert(
        &self,
        args: &ComputeRegionInstancesBulkInsertArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_instances_bulk_insert_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_instances_bulk_insert_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region instant snapshot groups delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_region_instant_snapshot_groups_delete(
        &self,
        args: &ComputeRegionInstantSnapshotGroupsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_instant_snapshot_groups_delete_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.instantSnapshotGroup,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_instant_snapshot_groups_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region instant snapshot groups get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the InstantSnapshotGroup result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_region_instant_snapshot_groups_get(
        &self,
        args: &ComputeRegionInstantSnapshotGroupsGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<InstantSnapshotGroup, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_instant_snapshot_groups_get_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.instantSnapshotGroup,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_instant_snapshot_groups_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region instant snapshot groups get iam policy.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Policy result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_region_instant_snapshot_groups_get_iam_policy(
        &self,
        args: &ComputeRegionInstantSnapshotGroupsGetIamPolicyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Policy, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_instant_snapshot_groups_get_iam_policy_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.resource,
            &args.optionsRequestedPolicyVersion,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_instant_snapshot_groups_get_iam_policy_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region instant snapshot groups insert.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_region_instant_snapshot_groups_insert(
        &self,
        args: &ComputeRegionInstantSnapshotGroupsInsertArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_instant_snapshot_groups_insert_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.requestId,
            &args.sourceConsistencyGroup,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_instant_snapshot_groups_insert_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region instant snapshot groups list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ListInstantSnapshotGroups result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_region_instant_snapshot_groups_list(
        &self,
        args: &ComputeRegionInstantSnapshotGroupsListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ListInstantSnapshotGroups, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_instant_snapshot_groups_list_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.filter,
            &args.maxResults,
            &args.orderBy,
            &args.pageToken,
            &args.returnPartialSuccess,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_instant_snapshot_groups_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region instant snapshot groups set iam policy.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Policy result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_region_instant_snapshot_groups_set_iam_policy(
        &self,
        args: &ComputeRegionInstantSnapshotGroupsSetIamPolicyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Policy, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_instant_snapshot_groups_set_iam_policy_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.resource,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_instant_snapshot_groups_set_iam_policy_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region instant snapshot groups test iam permissions.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TestPermissionsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_region_instant_snapshot_groups_test_iam_permissions(
        &self,
        args: &ComputeRegionInstantSnapshotGroupsTestIamPermissionsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TestPermissionsResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_instant_snapshot_groups_test_iam_permissions_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.resource,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_instant_snapshot_groups_test_iam_permissions_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region instant snapshots delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_region_instant_snapshots_delete(
        &self,
        args: &ComputeRegionInstantSnapshotsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_instant_snapshots_delete_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.instantSnapshot,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_instant_snapshots_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region instant snapshots get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the InstantSnapshot result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_region_instant_snapshots_get(
        &self,
        args: &ComputeRegionInstantSnapshotsGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<InstantSnapshot, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_instant_snapshots_get_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.instantSnapshot,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_instant_snapshots_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region instant snapshots get iam policy.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Policy result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_region_instant_snapshots_get_iam_policy(
        &self,
        args: &ComputeRegionInstantSnapshotsGetIamPolicyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Policy, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_instant_snapshots_get_iam_policy_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.resource,
            &args.optionsRequestedPolicyVersion,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_instant_snapshots_get_iam_policy_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region instant snapshots insert.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_region_instant_snapshots_insert(
        &self,
        args: &ComputeRegionInstantSnapshotsInsertArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_instant_snapshots_insert_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_instant_snapshots_insert_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region instant snapshots list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the InstantSnapshotList result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_region_instant_snapshots_list(
        &self,
        args: &ComputeRegionInstantSnapshotsListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<InstantSnapshotList, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_instant_snapshots_list_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.filter,
            &args.maxResults,
            &args.orderBy,
            &args.pageToken,
            &args.returnPartialSuccess,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_instant_snapshots_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region instant snapshots set iam policy.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Policy result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_region_instant_snapshots_set_iam_policy(
        &self,
        args: &ComputeRegionInstantSnapshotsSetIamPolicyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Policy, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_instant_snapshots_set_iam_policy_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.resource,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_instant_snapshots_set_iam_policy_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region instant snapshots set labels.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_region_instant_snapshots_set_labels(
        &self,
        args: &ComputeRegionInstantSnapshotsSetLabelsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_instant_snapshots_set_labels_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.resource,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_instant_snapshots_set_labels_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region instant snapshots test iam permissions.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TestPermissionsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_region_instant_snapshots_test_iam_permissions(
        &self,
        args: &ComputeRegionInstantSnapshotsTestIamPermissionsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TestPermissionsResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_instant_snapshots_test_iam_permissions_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.resource,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_instant_snapshots_test_iam_permissions_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region network endpoint groups attach network endpoints.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_region_network_endpoint_groups_attach_network_endpoints(
        &self,
        args: &ComputeRegionNetworkEndpointGroupsAttachNetworkEndpointsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_network_endpoint_groups_attach_network_endpoints_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.networkEndpointGroup,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_network_endpoint_groups_attach_network_endpoints_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region network endpoint groups delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_region_network_endpoint_groups_delete(
        &self,
        args: &ComputeRegionNetworkEndpointGroupsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_network_endpoint_groups_delete_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.networkEndpointGroup,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_network_endpoint_groups_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region network endpoint groups detach network endpoints.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_region_network_endpoint_groups_detach_network_endpoints(
        &self,
        args: &ComputeRegionNetworkEndpointGroupsDetachNetworkEndpointsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_network_endpoint_groups_detach_network_endpoints_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.networkEndpointGroup,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_network_endpoint_groups_detach_network_endpoints_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region network endpoint groups get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the NetworkEndpointGroup result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_region_network_endpoint_groups_get(
        &self,
        args: &ComputeRegionNetworkEndpointGroupsGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<NetworkEndpointGroup, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_network_endpoint_groups_get_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.networkEndpointGroup,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_network_endpoint_groups_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region network endpoint groups insert.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_region_network_endpoint_groups_insert(
        &self,
        args: &ComputeRegionNetworkEndpointGroupsInsertArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_network_endpoint_groups_insert_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_network_endpoint_groups_insert_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region network endpoint groups list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the NetworkEndpointGroupList result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_region_network_endpoint_groups_list(
        &self,
        args: &ComputeRegionNetworkEndpointGroupsListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<NetworkEndpointGroupList, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_network_endpoint_groups_list_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.filter,
            &args.maxResults,
            &args.orderBy,
            &args.pageToken,
            &args.returnPartialSuccess,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_network_endpoint_groups_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region network endpoint groups list network endpoints.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the NetworkEndpointGroupsListNetworkEndpoints result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_region_network_endpoint_groups_list_network_endpoints(
        &self,
        args: &ComputeRegionNetworkEndpointGroupsListNetworkEndpointsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<NetworkEndpointGroupsListNetworkEndpoints, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_network_endpoint_groups_list_network_endpoints_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.networkEndpointGroup,
            &args.filter,
            &args.maxResults,
            &args.orderBy,
            &args.pageToken,
            &args.returnPartialSuccess,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_network_endpoint_groups_list_network_endpoints_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region network firewall policies add association.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_region_network_firewall_policies_add_association(
        &self,
        args: &ComputeRegionNetworkFirewallPoliciesAddAssociationArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_network_firewall_policies_add_association_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.firewallPolicy,
            &args.replaceExistingAssociation,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_network_firewall_policies_add_association_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region network firewall policies add rule.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_region_network_firewall_policies_add_rule(
        &self,
        args: &ComputeRegionNetworkFirewallPoliciesAddRuleArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_network_firewall_policies_add_rule_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.firewallPolicy,
            &args.maxPriority,
            &args.minPriority,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_network_firewall_policies_add_rule_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region network firewall policies clone rules.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_region_network_firewall_policies_clone_rules(
        &self,
        args: &ComputeRegionNetworkFirewallPoliciesCloneRulesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_network_firewall_policies_clone_rules_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.firewallPolicy,
            &args.requestId,
            &args.sourceFirewallPolicy,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_network_firewall_policies_clone_rules_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region network firewall policies delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_region_network_firewall_policies_delete(
        &self,
        args: &ComputeRegionNetworkFirewallPoliciesDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_network_firewall_policies_delete_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.firewallPolicy,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_network_firewall_policies_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region network firewall policies get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the FirewallPolicy result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_region_network_firewall_policies_get(
        &self,
        args: &ComputeRegionNetworkFirewallPoliciesGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<FirewallPolicy, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_network_firewall_policies_get_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.firewallPolicy,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_network_firewall_policies_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region network firewall policies get association.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the FirewallPolicyAssociation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_region_network_firewall_policies_get_association(
        &self,
        args: &ComputeRegionNetworkFirewallPoliciesGetAssociationArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<FirewallPolicyAssociation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_network_firewall_policies_get_association_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.firewallPolicy,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_network_firewall_policies_get_association_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region network firewall policies get effective firewalls.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the RegionNetworkFirewallPoliciesGetEffectiveFirewallsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_region_network_firewall_policies_get_effective_firewalls(
        &self,
        args: &ComputeRegionNetworkFirewallPoliciesGetEffectiveFirewallsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<RegionNetworkFirewallPoliciesGetEffectiveFirewallsResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_network_firewall_policies_get_effective_firewalls_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.network,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_network_firewall_policies_get_effective_firewalls_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region network firewall policies get iam policy.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Policy result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_region_network_firewall_policies_get_iam_policy(
        &self,
        args: &ComputeRegionNetworkFirewallPoliciesGetIamPolicyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Policy, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_network_firewall_policies_get_iam_policy_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.resource,
            &args.optionsRequestedPolicyVersion,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_network_firewall_policies_get_iam_policy_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region network firewall policies get rule.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the FirewallPolicyRule result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_region_network_firewall_policies_get_rule(
        &self,
        args: &ComputeRegionNetworkFirewallPoliciesGetRuleArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<FirewallPolicyRule, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_network_firewall_policies_get_rule_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.firewallPolicy,
            &args.priority,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_network_firewall_policies_get_rule_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region network firewall policies insert.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_region_network_firewall_policies_insert(
        &self,
        args: &ComputeRegionNetworkFirewallPoliciesInsertArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_network_firewall_policies_insert_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_network_firewall_policies_insert_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region network firewall policies list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the FirewallPolicyList result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_region_network_firewall_policies_list(
        &self,
        args: &ComputeRegionNetworkFirewallPoliciesListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<FirewallPolicyList, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_network_firewall_policies_list_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.filter,
            &args.maxResults,
            &args.orderBy,
            &args.pageToken,
            &args.returnPartialSuccess,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_network_firewall_policies_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region network firewall policies patch.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_region_network_firewall_policies_patch(
        &self,
        args: &ComputeRegionNetworkFirewallPoliciesPatchArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_network_firewall_policies_patch_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.firewallPolicy,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_network_firewall_policies_patch_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region network firewall policies patch rule.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_region_network_firewall_policies_patch_rule(
        &self,
        args: &ComputeRegionNetworkFirewallPoliciesPatchRuleArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_network_firewall_policies_patch_rule_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.firewallPolicy,
            &args.priority,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_network_firewall_policies_patch_rule_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region network firewall policies remove association.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_region_network_firewall_policies_remove_association(
        &self,
        args: &ComputeRegionNetworkFirewallPoliciesRemoveAssociationArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_network_firewall_policies_remove_association_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.firewallPolicy,
            &args.name,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_network_firewall_policies_remove_association_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region network firewall policies remove rule.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_region_network_firewall_policies_remove_rule(
        &self,
        args: &ComputeRegionNetworkFirewallPoliciesRemoveRuleArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_network_firewall_policies_remove_rule_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.firewallPolicy,
            &args.priority,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_network_firewall_policies_remove_rule_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region network firewall policies set iam policy.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Policy result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_region_network_firewall_policies_set_iam_policy(
        &self,
        args: &ComputeRegionNetworkFirewallPoliciesSetIamPolicyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Policy, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_network_firewall_policies_set_iam_policy_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.resource,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_network_firewall_policies_set_iam_policy_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region network firewall policies test iam permissions.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TestPermissionsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_region_network_firewall_policies_test_iam_permissions(
        &self,
        args: &ComputeRegionNetworkFirewallPoliciesTestIamPermissionsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TestPermissionsResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_network_firewall_policies_test_iam_permissions_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.resource,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_network_firewall_policies_test_iam_permissions_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region notification endpoints aggregated list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the NotificationEndpointAggregatedList result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_region_notification_endpoints_aggregated_list(
        &self,
        args: &ComputeRegionNotificationEndpointsAggregatedListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<NotificationEndpointAggregatedList, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_notification_endpoints_aggregated_list_builder(
            &self.http_client,
            &args.project,
            &args.filter,
            &args.includeAllScopes,
            &args.maxResults,
            &args.orderBy,
            &args.pageToken,
            &args.returnPartialSuccess,
            &args.serviceProjectNumber,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_notification_endpoints_aggregated_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region notification endpoints delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_region_notification_endpoints_delete(
        &self,
        args: &ComputeRegionNotificationEndpointsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_notification_endpoints_delete_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.notificationEndpoint,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_notification_endpoints_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region notification endpoints get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the NotificationEndpoint result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_region_notification_endpoints_get(
        &self,
        args: &ComputeRegionNotificationEndpointsGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<NotificationEndpoint, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_notification_endpoints_get_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.notificationEndpoint,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_notification_endpoints_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region notification endpoints insert.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_region_notification_endpoints_insert(
        &self,
        args: &ComputeRegionNotificationEndpointsInsertArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_notification_endpoints_insert_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_notification_endpoints_insert_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region notification endpoints list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the NotificationEndpointList result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_region_notification_endpoints_list(
        &self,
        args: &ComputeRegionNotificationEndpointsListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<NotificationEndpointList, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_notification_endpoints_list_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.filter,
            &args.maxResults,
            &args.orderBy,
            &args.pageToken,
            &args.returnPartialSuccess,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_notification_endpoints_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region notification endpoints test iam permissions.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TestPermissionsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_region_notification_endpoints_test_iam_permissions(
        &self,
        args: &ComputeRegionNotificationEndpointsTestIamPermissionsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TestPermissionsResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_notification_endpoints_test_iam_permissions_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.resource,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_notification_endpoints_test_iam_permissions_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region operations delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_region_operations_delete(
        &self,
        args: &ComputeRegionOperationsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_operations_delete_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.operation,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_operations_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region operations get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_region_operations_get(
        &self,
        args: &ComputeRegionOperationsGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_operations_get_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.operation,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_operations_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region operations list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the OperationList result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_region_operations_list(
        &self,
        args: &ComputeRegionOperationsListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<OperationList, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_operations_list_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.filter,
            &args.maxResults,
            &args.orderBy,
            &args.pageToken,
            &args.returnPartialSuccess,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_operations_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region operations wait.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_region_operations_wait(
        &self,
        args: &ComputeRegionOperationsWaitArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_operations_wait_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.operation,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_operations_wait_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region security policies add rule.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_region_security_policies_add_rule(
        &self,
        args: &ComputeRegionSecurityPoliciesAddRuleArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_security_policies_add_rule_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.securityPolicy,
            &args.validateOnly,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_security_policies_add_rule_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region security policies delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_region_security_policies_delete(
        &self,
        args: &ComputeRegionSecurityPoliciesDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_security_policies_delete_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.securityPolicy,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_security_policies_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region security policies get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the SecurityPolicy result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_region_security_policies_get(
        &self,
        args: &ComputeRegionSecurityPoliciesGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<SecurityPolicy, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_security_policies_get_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.securityPolicy,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_security_policies_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region security policies get rule.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the SecurityPolicyRule result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_region_security_policies_get_rule(
        &self,
        args: &ComputeRegionSecurityPoliciesGetRuleArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<SecurityPolicyRule, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_security_policies_get_rule_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.securityPolicy,
            &args.priority,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_security_policies_get_rule_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region security policies insert.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_region_security_policies_insert(
        &self,
        args: &ComputeRegionSecurityPoliciesInsertArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_security_policies_insert_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.requestId,
            &args.validateOnly,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_security_policies_insert_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region security policies list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the SecurityPolicyList result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_region_security_policies_list(
        &self,
        args: &ComputeRegionSecurityPoliciesListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<SecurityPolicyList, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_security_policies_list_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.filter,
            &args.maxResults,
            &args.orderBy,
            &args.pageToken,
            &args.returnPartialSuccess,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_security_policies_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region security policies patch.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_region_security_policies_patch(
        &self,
        args: &ComputeRegionSecurityPoliciesPatchArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_security_policies_patch_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.securityPolicy,
            &args.requestId,
            &args.updateMask,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_security_policies_patch_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region security policies patch rule.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_region_security_policies_patch_rule(
        &self,
        args: &ComputeRegionSecurityPoliciesPatchRuleArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_security_policies_patch_rule_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.securityPolicy,
            &args.priority,
            &args.updateMask,
            &args.validateOnly,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_security_policies_patch_rule_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region security policies remove rule.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_region_security_policies_remove_rule(
        &self,
        args: &ComputeRegionSecurityPoliciesRemoveRuleArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_security_policies_remove_rule_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.securityPolicy,
            &args.priority,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_security_policies_remove_rule_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region security policies set labels.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_region_security_policies_set_labels(
        &self,
        args: &ComputeRegionSecurityPoliciesSetLabelsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_security_policies_set_labels_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.resource,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_security_policies_set_labels_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region snapshot settings get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the SnapshotSettings result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_region_snapshot_settings_get(
        &self,
        args: &ComputeRegionSnapshotSettingsGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<SnapshotSettings, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_snapshot_settings_get_builder(
            &self.http_client,
            &args.project,
            &args.region,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_snapshot_settings_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region snapshot settings patch.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_region_snapshot_settings_patch(
        &self,
        args: &ComputeRegionSnapshotSettingsPatchArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_snapshot_settings_patch_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.requestId,
            &args.updateMask,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_snapshot_settings_patch_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region snapshots delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_region_snapshots_delete(
        &self,
        args: &ComputeRegionSnapshotsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_snapshots_delete_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.snapshot,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_snapshots_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region snapshots get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Snapshot result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_region_snapshots_get(
        &self,
        args: &ComputeRegionSnapshotsGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Snapshot, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_snapshots_get_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.snapshot,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_snapshots_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region snapshots get iam policy.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Policy result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_region_snapshots_get_iam_policy(
        &self,
        args: &ComputeRegionSnapshotsGetIamPolicyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Policy, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_snapshots_get_iam_policy_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.resource,
            &args.optionsRequestedPolicyVersion,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_snapshots_get_iam_policy_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region snapshots insert.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_region_snapshots_insert(
        &self,
        args: &ComputeRegionSnapshotsInsertArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_snapshots_insert_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_snapshots_insert_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region snapshots list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the SnapshotList result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_region_snapshots_list(
        &self,
        args: &ComputeRegionSnapshotsListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<SnapshotList, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_snapshots_list_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.filter,
            &args.maxResults,
            &args.orderBy,
            &args.pageToken,
            &args.returnPartialSuccess,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_snapshots_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region snapshots set iam policy.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Policy result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_region_snapshots_set_iam_policy(
        &self,
        args: &ComputeRegionSnapshotsSetIamPolicyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Policy, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_snapshots_set_iam_policy_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.resource,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_snapshots_set_iam_policy_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region snapshots set labels.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_region_snapshots_set_labels(
        &self,
        args: &ComputeRegionSnapshotsSetLabelsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_snapshots_set_labels_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.resource,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_snapshots_set_labels_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region snapshots test iam permissions.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TestPermissionsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_region_snapshots_test_iam_permissions(
        &self,
        args: &ComputeRegionSnapshotsTestIamPermissionsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TestPermissionsResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_snapshots_test_iam_permissions_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.resource,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_snapshots_test_iam_permissions_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region snapshots update kms key.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_region_snapshots_update_kms_key(
        &self,
        args: &ComputeRegionSnapshotsUpdateKmsKeyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_snapshots_update_kms_key_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.snapshot,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_snapshots_update_kms_key_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region ssl certificates delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_region_ssl_certificates_delete(
        &self,
        args: &ComputeRegionSslCertificatesDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_ssl_certificates_delete_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.sslCertificate,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_ssl_certificates_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region ssl certificates get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the SslCertificate result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_region_ssl_certificates_get(
        &self,
        args: &ComputeRegionSslCertificatesGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<SslCertificate, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_ssl_certificates_get_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.sslCertificate,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_ssl_certificates_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region ssl certificates insert.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_region_ssl_certificates_insert(
        &self,
        args: &ComputeRegionSslCertificatesInsertArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_ssl_certificates_insert_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_ssl_certificates_insert_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region ssl certificates list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the SslCertificateList result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_region_ssl_certificates_list(
        &self,
        args: &ComputeRegionSslCertificatesListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<SslCertificateList, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_ssl_certificates_list_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.filter,
            &args.maxResults,
            &args.orderBy,
            &args.pageToken,
            &args.returnPartialSuccess,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_ssl_certificates_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region ssl policies delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_region_ssl_policies_delete(
        &self,
        args: &ComputeRegionSslPoliciesDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_ssl_policies_delete_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.sslPolicy,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_ssl_policies_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region ssl policies get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the SslPolicy result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_region_ssl_policies_get(
        &self,
        args: &ComputeRegionSslPoliciesGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<SslPolicy, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_ssl_policies_get_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.sslPolicy,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_ssl_policies_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region ssl policies insert.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_region_ssl_policies_insert(
        &self,
        args: &ComputeRegionSslPoliciesInsertArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_ssl_policies_insert_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_ssl_policies_insert_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region ssl policies list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the SslPoliciesList result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_region_ssl_policies_list(
        &self,
        args: &ComputeRegionSslPoliciesListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<SslPoliciesList, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_ssl_policies_list_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.filter,
            &args.maxResults,
            &args.orderBy,
            &args.pageToken,
            &args.returnPartialSuccess,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_ssl_policies_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region ssl policies list available features.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the SslPoliciesListAvailableFeaturesResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_region_ssl_policies_list_available_features(
        &self,
        args: &ComputeRegionSslPoliciesListAvailableFeaturesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<SslPoliciesListAvailableFeaturesResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_ssl_policies_list_available_features_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.filter,
            &args.maxResults,
            &args.orderBy,
            &args.pageToken,
            &args.returnPartialSuccess,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_ssl_policies_list_available_features_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region ssl policies patch.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_region_ssl_policies_patch(
        &self,
        args: &ComputeRegionSslPoliciesPatchArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_ssl_policies_patch_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.sslPolicy,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_ssl_policies_patch_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region target http proxies delete.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_region_target_http_proxies_delete(
        &self,
        args: &ComputeRegionTargetHttpProxiesDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_target_http_proxies_delete_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.targetHttpProxy,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_target_http_proxies_delete_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region target http proxies get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TargetHttpProxy result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_region_target_http_proxies_get(
        &self,
        args: &ComputeRegionTargetHttpProxiesGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TargetHttpProxy, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_target_http_proxies_get_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.targetHttpProxy,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_target_http_proxies_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region target http proxies insert.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_region_target_http_proxies_insert(
        &self,
        args: &ComputeRegionTargetHttpProxiesInsertArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_target_http_proxies_insert_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_target_http_proxies_insert_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region target http proxies list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TargetHttpProxyList result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_region_target_http_proxies_list(
        &self,
        args: &ComputeRegionTargetHttpProxiesListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TargetHttpProxyList, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_target_http_proxies_list_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.filter,
            &args.maxResults,
            &args.orderBy,
            &args.pageToken,
            &args.returnPartialSuccess,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_target_http_proxies_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region target http proxies set url map.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_region_target_http_proxies_set_url_map(
        &self,
        args: &ComputeRegionTargetHttpProxiesSetUrlMapArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_target_http_proxies_set_url_map_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.targetHttpProxy,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_target_http_proxies_set_url_map_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region target https proxies delete.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_region_target_https_proxies_delete(
        &self,
        args: &ComputeRegionTargetHttpsProxiesDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_target_https_proxies_delete_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.targetHttpsProxy,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_target_https_proxies_delete_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region target https proxies get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TargetHttpsProxy result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_region_target_https_proxies_get(
        &self,
        args: &ComputeRegionTargetHttpsProxiesGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TargetHttpsProxy, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_target_https_proxies_get_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.targetHttpsProxy,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_target_https_proxies_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region target https proxies insert.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_region_target_https_proxies_insert(
        &self,
        args: &ComputeRegionTargetHttpsProxiesInsertArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_target_https_proxies_insert_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_target_https_proxies_insert_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region target https proxies list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TargetHttpsProxyList result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_region_target_https_proxies_list(
        &self,
        args: &ComputeRegionTargetHttpsProxiesListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TargetHttpsProxyList, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_target_https_proxies_list_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.filter,
            &args.maxResults,
            &args.orderBy,
            &args.pageToken,
            &args.returnPartialSuccess,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_target_https_proxies_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region target https proxies patch.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_region_target_https_proxies_patch(
        &self,
        args: &ComputeRegionTargetHttpsProxiesPatchArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_target_https_proxies_patch_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.targetHttpsProxy,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_target_https_proxies_patch_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region target https proxies set ssl certificates.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_region_target_https_proxies_set_ssl_certificates(
        &self,
        args: &ComputeRegionTargetHttpsProxiesSetSslCertificatesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_target_https_proxies_set_ssl_certificates_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.targetHttpsProxy,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_target_https_proxies_set_ssl_certificates_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region target https proxies set url map.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_region_target_https_proxies_set_url_map(
        &self,
        args: &ComputeRegionTargetHttpsProxiesSetUrlMapArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_target_https_proxies_set_url_map_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.targetHttpsProxy,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_target_https_proxies_set_url_map_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region target tcp proxies delete.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_region_target_tcp_proxies_delete(
        &self,
        args: &ComputeRegionTargetTcpProxiesDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_target_tcp_proxies_delete_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.targetTcpProxy,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_target_tcp_proxies_delete_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region target tcp proxies get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TargetTcpProxy result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_region_target_tcp_proxies_get(
        &self,
        args: &ComputeRegionTargetTcpProxiesGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TargetTcpProxy, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_target_tcp_proxies_get_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.targetTcpProxy,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_target_tcp_proxies_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region target tcp proxies insert.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_region_target_tcp_proxies_insert(
        &self,
        args: &ComputeRegionTargetTcpProxiesInsertArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_target_tcp_proxies_insert_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_target_tcp_proxies_insert_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region target tcp proxies list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TargetTcpProxyList result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_region_target_tcp_proxies_list(
        &self,
        args: &ComputeRegionTargetTcpProxiesListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TargetTcpProxyList, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_target_tcp_proxies_list_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.filter,
            &args.maxResults,
            &args.orderBy,
            &args.pageToken,
            &args.returnPartialSuccess,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_target_tcp_proxies_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region url maps delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_region_url_maps_delete(
        &self,
        args: &ComputeRegionUrlMapsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_url_maps_delete_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.urlMap,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_url_maps_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region url maps get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the UrlMap result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_region_url_maps_get(
        &self,
        args: &ComputeRegionUrlMapsGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<UrlMap, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_url_maps_get_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.urlMap,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_url_maps_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region url maps insert.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_region_url_maps_insert(
        &self,
        args: &ComputeRegionUrlMapsInsertArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_url_maps_insert_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_url_maps_insert_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region url maps list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the UrlMapList result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_region_url_maps_list(
        &self,
        args: &ComputeRegionUrlMapsListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<UrlMapList, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_url_maps_list_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.filter,
            &args.maxResults,
            &args.orderBy,
            &args.pageToken,
            &args.returnPartialSuccess,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_url_maps_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region url maps patch.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_region_url_maps_patch(
        &self,
        args: &ComputeRegionUrlMapsPatchArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_url_maps_patch_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.urlMap,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_url_maps_patch_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region url maps update.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_region_url_maps_update(
        &self,
        args: &ComputeRegionUrlMapsUpdateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_url_maps_update_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.urlMap,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_url_maps_update_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region url maps validate.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the UrlMapsValidateResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_region_url_maps_validate(
        &self,
        args: &ComputeRegionUrlMapsValidateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<UrlMapsValidateResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_url_maps_validate_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.urlMap,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_url_maps_validate_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute region zones list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ZoneList result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_region_zones_list(
        &self,
        args: &ComputeRegionZonesListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ZoneList, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_region_zones_list_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.filter,
            &args.maxResults,
            &args.orderBy,
            &args.pageToken,
            &args.returnPartialSuccess,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_region_zones_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute regions get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Region result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_regions_get(
        &self,
        args: &ComputeRegionsGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Region, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_regions_get_builder(
            &self.http_client,
            &args.project,
            &args.region,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_regions_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute regions list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the RegionList result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_regions_list(
        &self,
        args: &ComputeRegionsListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<RegionList, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_regions_list_builder(
            &self.http_client,
            &args.project,
            &args.filter,
            &args.maxResults,
            &args.orderBy,
            &args.pageToken,
            &args.returnPartialSuccess,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_regions_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute reservation blocks get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ReservationBlocksGetResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_reservation_blocks_get(
        &self,
        args: &ComputeReservationBlocksGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ReservationBlocksGetResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_reservation_blocks_get_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.reservation,
            &args.reservationBlock,
            &args.view,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_reservation_blocks_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute reservation blocks get iam policy.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Policy result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_reservation_blocks_get_iam_policy(
        &self,
        args: &ComputeReservationBlocksGetIamPolicyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Policy, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_reservation_blocks_get_iam_policy_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.parentResource,
            &args.resource,
            &args.optionsRequestedPolicyVersion,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_reservation_blocks_get_iam_policy_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute reservation blocks list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ReservationBlocksListResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_reservation_blocks_list(
        &self,
        args: &ComputeReservationBlocksListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ReservationBlocksListResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_reservation_blocks_list_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.reservation,
            &args.filter,
            &args.maxResults,
            &args.orderBy,
            &args.pageToken,
            &args.returnPartialSuccess,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_reservation_blocks_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute reservation blocks perform maintenance.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_reservation_blocks_perform_maintenance(
        &self,
        args: &ComputeReservationBlocksPerformMaintenanceArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_reservation_blocks_perform_maintenance_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.reservation,
            &args.reservationBlock,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_reservation_blocks_perform_maintenance_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute reservation blocks set iam policy.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Policy result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_reservation_blocks_set_iam_policy(
        &self,
        args: &ComputeReservationBlocksSetIamPolicyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Policy, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_reservation_blocks_set_iam_policy_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.parentResource,
            &args.resource,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_reservation_blocks_set_iam_policy_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute reservation blocks test iam permissions.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TestPermissionsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_reservation_blocks_test_iam_permissions(
        &self,
        args: &ComputeReservationBlocksTestIamPermissionsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TestPermissionsResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_reservation_blocks_test_iam_permissions_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.parentResource,
            &args.resource,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_reservation_blocks_test_iam_permissions_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute reservation slots get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ReservationSlotsGetResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_reservation_slots_get(
        &self,
        args: &ComputeReservationSlotsGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ReservationSlotsGetResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_reservation_slots_get_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.parentName,
            &args.reservationSlot,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_reservation_slots_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute reservation slots get version.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_reservation_slots_get_version(
        &self,
        args: &ComputeReservationSlotsGetVersionArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_reservation_slots_get_version_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.parentName,
            &args.reservationSlot,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_reservation_slots_get_version_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute reservation slots list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ReservationSlotsListResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_reservation_slots_list(
        &self,
        args: &ComputeReservationSlotsListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ReservationSlotsListResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_reservation_slots_list_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.parentName,
            &args.filter,
            &args.maxResults,
            &args.orderBy,
            &args.pageToken,
            &args.returnPartialSuccess,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_reservation_slots_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute reservation slots update.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_reservation_slots_update(
        &self,
        args: &ComputeReservationSlotsUpdateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_reservation_slots_update_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.parentName,
            &args.reservationSlot,
            &args.updateMask,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_reservation_slots_update_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute reservation sub blocks get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ReservationSubBlocksGetResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_reservation_sub_blocks_get(
        &self,
        args: &ComputeReservationSubBlocksGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ReservationSubBlocksGetResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_reservation_sub_blocks_get_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.parentName,
            &args.reservationSubBlock,
            &args.view,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_reservation_sub_blocks_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute reservation sub blocks get iam policy.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Policy result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_reservation_sub_blocks_get_iam_policy(
        &self,
        args: &ComputeReservationSubBlocksGetIamPolicyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Policy, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_reservation_sub_blocks_get_iam_policy_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.parentResource,
            &args.resource,
            &args.optionsRequestedPolicyVersion,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_reservation_sub_blocks_get_iam_policy_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute reservation sub blocks get version.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_reservation_sub_blocks_get_version(
        &self,
        args: &ComputeReservationSubBlocksGetVersionArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_reservation_sub_blocks_get_version_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.parentName,
            &args.reservationSubBlock,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_reservation_sub_blocks_get_version_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute reservation sub blocks list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ReservationSubBlocksListResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_reservation_sub_blocks_list(
        &self,
        args: &ComputeReservationSubBlocksListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ReservationSubBlocksListResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_reservation_sub_blocks_list_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.parentName,
            &args.filter,
            &args.maxResults,
            &args.orderBy,
            &args.pageToken,
            &args.returnPartialSuccess,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_reservation_sub_blocks_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute reservation sub blocks perform maintenance.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_reservation_sub_blocks_perform_maintenance(
        &self,
        args: &ComputeReservationSubBlocksPerformMaintenanceArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_reservation_sub_blocks_perform_maintenance_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.parentName,
            &args.reservationSubBlock,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_reservation_sub_blocks_perform_maintenance_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute reservation sub blocks report faulty.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_reservation_sub_blocks_report_faulty(
        &self,
        args: &ComputeReservationSubBlocksReportFaultyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_reservation_sub_blocks_report_faulty_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.parentName,
            &args.reservationSubBlock,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_reservation_sub_blocks_report_faulty_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute reservation sub blocks set iam policy.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Policy result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_reservation_sub_blocks_set_iam_policy(
        &self,
        args: &ComputeReservationSubBlocksSetIamPolicyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Policy, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_reservation_sub_blocks_set_iam_policy_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.parentResource,
            &args.resource,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_reservation_sub_blocks_set_iam_policy_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute reservation sub blocks test iam permissions.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TestPermissionsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_reservation_sub_blocks_test_iam_permissions(
        &self,
        args: &ComputeReservationSubBlocksTestIamPermissionsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TestPermissionsResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_reservation_sub_blocks_test_iam_permissions_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.parentResource,
            &args.resource,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_reservation_sub_blocks_test_iam_permissions_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute reservations aggregated list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ReservationAggregatedList result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_reservations_aggregated_list(
        &self,
        args: &ComputeReservationsAggregatedListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ReservationAggregatedList, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_reservations_aggregated_list_builder(
            &self.http_client,
            &args.project,
            &args.filter,
            &args.includeAllScopes,
            &args.maxResults,
            &args.orderBy,
            &args.pageToken,
            &args.returnPartialSuccess,
            &args.serviceProjectNumber,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_reservations_aggregated_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute reservations delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_reservations_delete(
        &self,
        args: &ComputeReservationsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_reservations_delete_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.reservation,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_reservations_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute reservations get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Reservation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_reservations_get(
        &self,
        args: &ComputeReservationsGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Reservation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_reservations_get_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.reservation,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_reservations_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute reservations get iam policy.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Policy result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_reservations_get_iam_policy(
        &self,
        args: &ComputeReservationsGetIamPolicyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Policy, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_reservations_get_iam_policy_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.resource,
            &args.optionsRequestedPolicyVersion,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_reservations_get_iam_policy_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute reservations insert.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_reservations_insert(
        &self,
        args: &ComputeReservationsInsertArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_reservations_insert_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_reservations_insert_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute reservations list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ReservationList result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_reservations_list(
        &self,
        args: &ComputeReservationsListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ReservationList, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_reservations_list_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.filter,
            &args.maxResults,
            &args.orderBy,
            &args.pageToken,
            &args.returnPartialSuccess,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_reservations_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute reservations perform maintenance.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_reservations_perform_maintenance(
        &self,
        args: &ComputeReservationsPerformMaintenanceArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_reservations_perform_maintenance_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.reservation,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_reservations_perform_maintenance_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute reservations resize.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_reservations_resize(
        &self,
        args: &ComputeReservationsResizeArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_reservations_resize_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.reservation,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_reservations_resize_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute reservations set iam policy.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Policy result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_reservations_set_iam_policy(
        &self,
        args: &ComputeReservationsSetIamPolicyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Policy, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_reservations_set_iam_policy_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.resource,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_reservations_set_iam_policy_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute reservations test iam permissions.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TestPermissionsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_reservations_test_iam_permissions(
        &self,
        args: &ComputeReservationsTestIamPermissionsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TestPermissionsResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_reservations_test_iam_permissions_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.resource,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_reservations_test_iam_permissions_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute reservations update.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_reservations_update(
        &self,
        args: &ComputeReservationsUpdateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_reservations_update_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.reservation,
            &args.paths,
            &args.requestId,
            &args.updateMask,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_reservations_update_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute resource policies aggregated list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ResourcePolicyAggregatedList result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_resource_policies_aggregated_list(
        &self,
        args: &ComputeResourcePoliciesAggregatedListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ResourcePolicyAggregatedList, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_resource_policies_aggregated_list_builder(
            &self.http_client,
            &args.project,
            &args.filter,
            &args.includeAllScopes,
            &args.maxResults,
            &args.orderBy,
            &args.pageToken,
            &args.returnPartialSuccess,
            &args.serviceProjectNumber,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_resource_policies_aggregated_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute resource policies delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_resource_policies_delete(
        &self,
        args: &ComputeResourcePoliciesDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_resource_policies_delete_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.resourcePolicy,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_resource_policies_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute resource policies get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ResourcePolicy result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_resource_policies_get(
        &self,
        args: &ComputeResourcePoliciesGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ResourcePolicy, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_resource_policies_get_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.resourcePolicy,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_resource_policies_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute resource policies get iam policy.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Policy result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_resource_policies_get_iam_policy(
        &self,
        args: &ComputeResourcePoliciesGetIamPolicyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Policy, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_resource_policies_get_iam_policy_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.resource,
            &args.optionsRequestedPolicyVersion,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_resource_policies_get_iam_policy_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute resource policies insert.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_resource_policies_insert(
        &self,
        args: &ComputeResourcePoliciesInsertArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_resource_policies_insert_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_resource_policies_insert_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute resource policies list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ResourcePolicyList result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_resource_policies_list(
        &self,
        args: &ComputeResourcePoliciesListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ResourcePolicyList, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_resource_policies_list_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.filter,
            &args.maxResults,
            &args.orderBy,
            &args.pageToken,
            &args.returnPartialSuccess,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_resource_policies_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute resource policies patch.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_resource_policies_patch(
        &self,
        args: &ComputeResourcePoliciesPatchArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_resource_policies_patch_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.resourcePolicy,
            &args.requestId,
            &args.updateMask,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_resource_policies_patch_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute resource policies set iam policy.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Policy result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_resource_policies_set_iam_policy(
        &self,
        args: &ComputeResourcePoliciesSetIamPolicyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Policy, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_resource_policies_set_iam_policy_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.resource,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_resource_policies_set_iam_policy_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute resource policies test iam permissions.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TestPermissionsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_resource_policies_test_iam_permissions(
        &self,
        args: &ComputeResourcePoliciesTestIamPermissionsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TestPermissionsResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_resource_policies_test_iam_permissions_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.resource,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_resource_policies_test_iam_permissions_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute routers aggregated list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the RouterAggregatedList result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_routers_aggregated_list(
        &self,
        args: &ComputeRoutersAggregatedListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<RouterAggregatedList, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_routers_aggregated_list_builder(
            &self.http_client,
            &args.project,
            &args.filter,
            &args.includeAllScopes,
            &args.maxResults,
            &args.orderBy,
            &args.pageToken,
            &args.returnPartialSuccess,
            &args.serviceProjectNumber,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_routers_aggregated_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute routers delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_routers_delete(
        &self,
        args: &ComputeRoutersDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_routers_delete_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.router,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_routers_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute routers delete route policy.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_routers_delete_route_policy(
        &self,
        args: &ComputeRoutersDeleteRoutePolicyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_routers_delete_route_policy_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.router,
            &args.policy,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_routers_delete_route_policy_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute routers get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Router result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_routers_get(
        &self,
        args: &ComputeRoutersGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Router, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_routers_get_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.router,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_routers_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute routers get nat ip info.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the NatIpInfoResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_routers_get_nat_ip_info(
        &self,
        args: &ComputeRoutersGetNatIpInfoArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<NatIpInfoResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_routers_get_nat_ip_info_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.router,
            &args.natName,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_routers_get_nat_ip_info_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute routers get nat mapping info.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the VmEndpointNatMappingsList result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_routers_get_nat_mapping_info(
        &self,
        args: &ComputeRoutersGetNatMappingInfoArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<VmEndpointNatMappingsList, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_routers_get_nat_mapping_info_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.router,
            &args.filter,
            &args.maxResults,
            &args.natName,
            &args.orderBy,
            &args.pageToken,
            &args.returnPartialSuccess,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_routers_get_nat_mapping_info_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute routers get route policy.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the RoutersGetRoutePolicyResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_routers_get_route_policy(
        &self,
        args: &ComputeRoutersGetRoutePolicyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<RoutersGetRoutePolicyResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_routers_get_route_policy_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.router,
            &args.policy,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_routers_get_route_policy_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute routers get router status.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the RouterStatusResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_routers_get_router_status(
        &self,
        args: &ComputeRoutersGetRouterStatusArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<RouterStatusResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_routers_get_router_status_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.router,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_routers_get_router_status_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute routers insert.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_routers_insert(
        &self,
        args: &ComputeRoutersInsertArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_routers_insert_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_routers_insert_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute routers list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the RouterList result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_routers_list(
        &self,
        args: &ComputeRoutersListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<RouterList, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_routers_list_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.filter,
            &args.maxResults,
            &args.orderBy,
            &args.pageToken,
            &args.returnPartialSuccess,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_routers_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute routers list bgp routes.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the RoutersListBgpRoutes result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_routers_list_bgp_routes(
        &self,
        args: &ComputeRoutersListBgpRoutesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<RoutersListBgpRoutes, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_routers_list_bgp_routes_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.router,
            &args.addressFamily,
            &args.destinationPrefix,
            &args.filter,
            &args.maxResults,
            &args.orderBy,
            &args.pageToken,
            &args.peer,
            &args.policyApplied,
            &args.returnPartialSuccess,
            &args.routeType,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_routers_list_bgp_routes_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute routers list route policies.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the RoutersListRoutePolicies result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_routers_list_route_policies(
        &self,
        args: &ComputeRoutersListRoutePoliciesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<RoutersListRoutePolicies, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_routers_list_route_policies_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.router,
            &args.filter,
            &args.maxResults,
            &args.orderBy,
            &args.pageToken,
            &args.returnPartialSuccess,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_routers_list_route_policies_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute routers patch.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_routers_patch(
        &self,
        args: &ComputeRoutersPatchArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_routers_patch_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.router,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_routers_patch_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute routers patch route policy.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_routers_patch_route_policy(
        &self,
        args: &ComputeRoutersPatchRoutePolicyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_routers_patch_route_policy_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.router,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_routers_patch_route_policy_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute routers preview.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the RoutersPreviewResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_routers_preview(
        &self,
        args: &ComputeRoutersPreviewArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<RoutersPreviewResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_routers_preview_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.router,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_routers_preview_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute routers update.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_routers_update(
        &self,
        args: &ComputeRoutersUpdateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_routers_update_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.router,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_routers_update_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute routers update route policy.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_routers_update_route_policy(
        &self,
        args: &ComputeRoutersUpdateRoutePolicyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_routers_update_route_policy_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.router,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_routers_update_route_policy_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute routes delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_routes_delete(
        &self,
        args: &ComputeRoutesDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_routes_delete_builder(
            &self.http_client,
            &args.project,
            &args.route,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_routes_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute routes get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Route result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_routes_get(
        &self,
        args: &ComputeRoutesGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Route, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_routes_get_builder(
            &self.http_client,
            &args.project,
            &args.route,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_routes_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute routes insert.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_routes_insert(
        &self,
        args: &ComputeRoutesInsertArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_routes_insert_builder(
            &self.http_client,
            &args.project,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_routes_insert_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute routes list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the RouteList result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_routes_list(
        &self,
        args: &ComputeRoutesListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<RouteList, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_routes_list_builder(
            &self.http_client,
            &args.project,
            &args.filter,
            &args.maxResults,
            &args.orderBy,
            &args.pageToken,
            &args.returnPartialSuccess,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_routes_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute routes test iam permissions.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TestPermissionsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_routes_test_iam_permissions(
        &self,
        args: &ComputeRoutesTestIamPermissionsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TestPermissionsResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_routes_test_iam_permissions_builder(
            &self.http_client,
            &args.project,
            &args.resource,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_routes_test_iam_permissions_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute security policies add rule.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_security_policies_add_rule(
        &self,
        args: &ComputeSecurityPoliciesAddRuleArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_security_policies_add_rule_builder(
            &self.http_client,
            &args.project,
            &args.securityPolicy,
            &args.validateOnly,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_security_policies_add_rule_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute security policies aggregated list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the SecurityPoliciesAggregatedList result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_security_policies_aggregated_list(
        &self,
        args: &ComputeSecurityPoliciesAggregatedListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<SecurityPoliciesAggregatedList, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_security_policies_aggregated_list_builder(
            &self.http_client,
            &args.project,
            &args.filter,
            &args.includeAllScopes,
            &args.maxResults,
            &args.orderBy,
            &args.pageToken,
            &args.returnPartialSuccess,
            &args.serviceProjectNumber,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_security_policies_aggregated_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute security policies delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_security_policies_delete(
        &self,
        args: &ComputeSecurityPoliciesDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_security_policies_delete_builder(
            &self.http_client,
            &args.project,
            &args.securityPolicy,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_security_policies_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute security policies get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the SecurityPolicy result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_security_policies_get(
        &self,
        args: &ComputeSecurityPoliciesGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<SecurityPolicy, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_security_policies_get_builder(
            &self.http_client,
            &args.project,
            &args.securityPolicy,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_security_policies_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute security policies get rule.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the SecurityPolicyRule result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_security_policies_get_rule(
        &self,
        args: &ComputeSecurityPoliciesGetRuleArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<SecurityPolicyRule, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_security_policies_get_rule_builder(
            &self.http_client,
            &args.project,
            &args.securityPolicy,
            &args.priority,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_security_policies_get_rule_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute security policies insert.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_security_policies_insert(
        &self,
        args: &ComputeSecurityPoliciesInsertArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_security_policies_insert_builder(
            &self.http_client,
            &args.project,
            &args.requestId,
            &args.validateOnly,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_security_policies_insert_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute security policies list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the SecurityPolicyList result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_security_policies_list(
        &self,
        args: &ComputeSecurityPoliciesListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<SecurityPolicyList, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_security_policies_list_builder(
            &self.http_client,
            &args.project,
            &args.filter,
            &args.maxResults,
            &args.orderBy,
            &args.pageToken,
            &args.returnPartialSuccess,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_security_policies_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute security policies list preconfigured expression sets.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the SecurityPoliciesListPreconfiguredExpressionSetsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_security_policies_list_preconfigured_expression_sets(
        &self,
        args: &ComputeSecurityPoliciesListPreconfiguredExpressionSetsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<SecurityPoliciesListPreconfiguredExpressionSetsResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_security_policies_list_preconfigured_expression_sets_builder(
            &self.http_client,
            &args.project,
            &args.filter,
            &args.maxResults,
            &args.orderBy,
            &args.pageToken,
            &args.returnPartialSuccess,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_security_policies_list_preconfigured_expression_sets_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute security policies patch.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_security_policies_patch(
        &self,
        args: &ComputeSecurityPoliciesPatchArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_security_policies_patch_builder(
            &self.http_client,
            &args.project,
            &args.securityPolicy,
            &args.requestId,
            &args.updateMask,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_security_policies_patch_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute security policies patch rule.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_security_policies_patch_rule(
        &self,
        args: &ComputeSecurityPoliciesPatchRuleArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_security_policies_patch_rule_builder(
            &self.http_client,
            &args.project,
            &args.securityPolicy,
            &args.priority,
            &args.updateMask,
            &args.validateOnly,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_security_policies_patch_rule_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute security policies remove rule.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_security_policies_remove_rule(
        &self,
        args: &ComputeSecurityPoliciesRemoveRuleArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_security_policies_remove_rule_builder(
            &self.http_client,
            &args.project,
            &args.securityPolicy,
            &args.priority,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_security_policies_remove_rule_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute security policies set labels.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_security_policies_set_labels(
        &self,
        args: &ComputeSecurityPoliciesSetLabelsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_security_policies_set_labels_builder(
            &self.http_client,
            &args.project,
            &args.resource,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_security_policies_set_labels_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute service attachments aggregated list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ServiceAttachmentAggregatedList result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_service_attachments_aggregated_list(
        &self,
        args: &ComputeServiceAttachmentsAggregatedListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ServiceAttachmentAggregatedList, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_service_attachments_aggregated_list_builder(
            &self.http_client,
            &args.project,
            &args.filter,
            &args.includeAllScopes,
            &args.maxResults,
            &args.orderBy,
            &args.pageToken,
            &args.returnPartialSuccess,
            &args.serviceProjectNumber,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_service_attachments_aggregated_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute service attachments delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_service_attachments_delete(
        &self,
        args: &ComputeServiceAttachmentsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_service_attachments_delete_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.serviceAttachment,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_service_attachments_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute service attachments get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ServiceAttachment result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_service_attachments_get(
        &self,
        args: &ComputeServiceAttachmentsGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ServiceAttachment, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_service_attachments_get_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.serviceAttachment,
            &args.showNatIps,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_service_attachments_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute service attachments get iam policy.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Policy result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_service_attachments_get_iam_policy(
        &self,
        args: &ComputeServiceAttachmentsGetIamPolicyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Policy, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_service_attachments_get_iam_policy_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.resource,
            &args.optionsRequestedPolicyVersion,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_service_attachments_get_iam_policy_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute service attachments insert.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_service_attachments_insert(
        &self,
        args: &ComputeServiceAttachmentsInsertArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_service_attachments_insert_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_service_attachments_insert_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute service attachments list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ServiceAttachmentList result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_service_attachments_list(
        &self,
        args: &ComputeServiceAttachmentsListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ServiceAttachmentList, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_service_attachments_list_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.filter,
            &args.maxResults,
            &args.orderBy,
            &args.pageToken,
            &args.returnPartialSuccess,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_service_attachments_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute service attachments patch.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_service_attachments_patch(
        &self,
        args: &ComputeServiceAttachmentsPatchArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_service_attachments_patch_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.serviceAttachment,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_service_attachments_patch_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute service attachments set iam policy.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Policy result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_service_attachments_set_iam_policy(
        &self,
        args: &ComputeServiceAttachmentsSetIamPolicyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Policy, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_service_attachments_set_iam_policy_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.resource,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_service_attachments_set_iam_policy_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute service attachments test iam permissions.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TestPermissionsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_service_attachments_test_iam_permissions(
        &self,
        args: &ComputeServiceAttachmentsTestIamPermissionsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TestPermissionsResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_service_attachments_test_iam_permissions_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.resource,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_service_attachments_test_iam_permissions_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute snapshot settings get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the SnapshotSettings result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_snapshot_settings_get(
        &self,
        args: &ComputeSnapshotSettingsGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<SnapshotSettings, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_snapshot_settings_get_builder(
            &self.http_client,
            &args.project,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_snapshot_settings_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute snapshot settings patch.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_snapshot_settings_patch(
        &self,
        args: &ComputeSnapshotSettingsPatchArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_snapshot_settings_patch_builder(
            &self.http_client,
            &args.project,
            &args.requestId,
            &args.updateMask,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_snapshot_settings_patch_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute snapshots delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_snapshots_delete(
        &self,
        args: &ComputeSnapshotsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_snapshots_delete_builder(
            &self.http_client,
            &args.project,
            &args.snapshot,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_snapshots_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute snapshots get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Snapshot result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_snapshots_get(
        &self,
        args: &ComputeSnapshotsGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Snapshot, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_snapshots_get_builder(
            &self.http_client,
            &args.project,
            &args.snapshot,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_snapshots_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute snapshots get iam policy.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Policy result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_snapshots_get_iam_policy(
        &self,
        args: &ComputeSnapshotsGetIamPolicyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Policy, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_snapshots_get_iam_policy_builder(
            &self.http_client,
            &args.project,
            &args.resource,
            &args.optionsRequestedPolicyVersion,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_snapshots_get_iam_policy_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute snapshots insert.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_snapshots_insert(
        &self,
        args: &ComputeSnapshotsInsertArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_snapshots_insert_builder(
            &self.http_client,
            &args.project,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_snapshots_insert_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute snapshots list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the SnapshotList result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_snapshots_list(
        &self,
        args: &ComputeSnapshotsListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<SnapshotList, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_snapshots_list_builder(
            &self.http_client,
            &args.project,
            &args.filter,
            &args.maxResults,
            &args.orderBy,
            &args.pageToken,
            &args.returnPartialSuccess,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_snapshots_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute snapshots set iam policy.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Policy result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_snapshots_set_iam_policy(
        &self,
        args: &ComputeSnapshotsSetIamPolicyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Policy, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_snapshots_set_iam_policy_builder(
            &self.http_client,
            &args.project,
            &args.resource,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_snapshots_set_iam_policy_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute snapshots set labels.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_snapshots_set_labels(
        &self,
        args: &ComputeSnapshotsSetLabelsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_snapshots_set_labels_builder(
            &self.http_client,
            &args.project,
            &args.resource,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_snapshots_set_labels_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute snapshots test iam permissions.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TestPermissionsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_snapshots_test_iam_permissions(
        &self,
        args: &ComputeSnapshotsTestIamPermissionsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TestPermissionsResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_snapshots_test_iam_permissions_builder(
            &self.http_client,
            &args.project,
            &args.resource,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_snapshots_test_iam_permissions_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute snapshots update kms key.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_snapshots_update_kms_key(
        &self,
        args: &ComputeSnapshotsUpdateKmsKeyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_snapshots_update_kms_key_builder(
            &self.http_client,
            &args.project,
            &args.snapshot,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_snapshots_update_kms_key_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute ssl certificates aggregated list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the SslCertificateAggregatedList result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_ssl_certificates_aggregated_list(
        &self,
        args: &ComputeSslCertificatesAggregatedListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<SslCertificateAggregatedList, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_ssl_certificates_aggregated_list_builder(
            &self.http_client,
            &args.project,
            &args.filter,
            &args.includeAllScopes,
            &args.maxResults,
            &args.orderBy,
            &args.pageToken,
            &args.returnPartialSuccess,
            &args.serviceProjectNumber,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_ssl_certificates_aggregated_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute ssl certificates delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_ssl_certificates_delete(
        &self,
        args: &ComputeSslCertificatesDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_ssl_certificates_delete_builder(
            &self.http_client,
            &args.project,
            &args.sslCertificate,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_ssl_certificates_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute ssl certificates get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the SslCertificate result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_ssl_certificates_get(
        &self,
        args: &ComputeSslCertificatesGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<SslCertificate, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_ssl_certificates_get_builder(
            &self.http_client,
            &args.project,
            &args.sslCertificate,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_ssl_certificates_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute ssl certificates insert.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_ssl_certificates_insert(
        &self,
        args: &ComputeSslCertificatesInsertArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_ssl_certificates_insert_builder(
            &self.http_client,
            &args.project,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_ssl_certificates_insert_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute ssl certificates list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the SslCertificateList result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_ssl_certificates_list(
        &self,
        args: &ComputeSslCertificatesListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<SslCertificateList, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_ssl_certificates_list_builder(
            &self.http_client,
            &args.project,
            &args.filter,
            &args.maxResults,
            &args.orderBy,
            &args.pageToken,
            &args.returnPartialSuccess,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_ssl_certificates_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute ssl policies aggregated list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the SslPoliciesAggregatedList result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_ssl_policies_aggregated_list(
        &self,
        args: &ComputeSslPoliciesAggregatedListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<SslPoliciesAggregatedList, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_ssl_policies_aggregated_list_builder(
            &self.http_client,
            &args.project,
            &args.filter,
            &args.includeAllScopes,
            &args.maxResults,
            &args.orderBy,
            &args.pageToken,
            &args.returnPartialSuccess,
            &args.serviceProjectNumber,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_ssl_policies_aggregated_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute ssl policies delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_ssl_policies_delete(
        &self,
        args: &ComputeSslPoliciesDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_ssl_policies_delete_builder(
            &self.http_client,
            &args.project,
            &args.sslPolicy,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_ssl_policies_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute ssl policies get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the SslPolicy result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_ssl_policies_get(
        &self,
        args: &ComputeSslPoliciesGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<SslPolicy, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_ssl_policies_get_builder(
            &self.http_client,
            &args.project,
            &args.sslPolicy,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_ssl_policies_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute ssl policies insert.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_ssl_policies_insert(
        &self,
        args: &ComputeSslPoliciesInsertArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_ssl_policies_insert_builder(
            &self.http_client,
            &args.project,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_ssl_policies_insert_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute ssl policies list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the SslPoliciesList result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_ssl_policies_list(
        &self,
        args: &ComputeSslPoliciesListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<SslPoliciesList, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_ssl_policies_list_builder(
            &self.http_client,
            &args.project,
            &args.filter,
            &args.maxResults,
            &args.orderBy,
            &args.pageToken,
            &args.returnPartialSuccess,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_ssl_policies_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute ssl policies list available features.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the SslPoliciesListAvailableFeaturesResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_ssl_policies_list_available_features(
        &self,
        args: &ComputeSslPoliciesListAvailableFeaturesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<SslPoliciesListAvailableFeaturesResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_ssl_policies_list_available_features_builder(
            &self.http_client,
            &args.project,
            &args.filter,
            &args.maxResults,
            &args.orderBy,
            &args.pageToken,
            &args.returnPartialSuccess,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_ssl_policies_list_available_features_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute ssl policies patch.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_ssl_policies_patch(
        &self,
        args: &ComputeSslPoliciesPatchArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_ssl_policies_patch_builder(
            &self.http_client,
            &args.project,
            &args.sslPolicy,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_ssl_policies_patch_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute storage pool types aggregated list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the StoragePoolTypeAggregatedList result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_storage_pool_types_aggregated_list(
        &self,
        args: &ComputeStoragePoolTypesAggregatedListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<StoragePoolTypeAggregatedList, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_storage_pool_types_aggregated_list_builder(
            &self.http_client,
            &args.project,
            &args.filter,
            &args.includeAllScopes,
            &args.maxResults,
            &args.orderBy,
            &args.pageToken,
            &args.returnPartialSuccess,
            &args.serviceProjectNumber,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_storage_pool_types_aggregated_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute storage pool types get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the StoragePoolType result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_storage_pool_types_get(
        &self,
        args: &ComputeStoragePoolTypesGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<StoragePoolType, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_storage_pool_types_get_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.storagePoolType,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_storage_pool_types_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute storage pool types list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the StoragePoolTypeList result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_storage_pool_types_list(
        &self,
        args: &ComputeStoragePoolTypesListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<StoragePoolTypeList, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_storage_pool_types_list_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.filter,
            &args.maxResults,
            &args.orderBy,
            &args.pageToken,
            &args.returnPartialSuccess,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_storage_pool_types_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute storage pools aggregated list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the StoragePoolAggregatedList result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_storage_pools_aggregated_list(
        &self,
        args: &ComputeStoragePoolsAggregatedListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<StoragePoolAggregatedList, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_storage_pools_aggregated_list_builder(
            &self.http_client,
            &args.project,
            &args.filter,
            &args.includeAllScopes,
            &args.maxResults,
            &args.orderBy,
            &args.pageToken,
            &args.returnPartialSuccess,
            &args.serviceProjectNumber,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_storage_pools_aggregated_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute storage pools delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_storage_pools_delete(
        &self,
        args: &ComputeStoragePoolsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_storage_pools_delete_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.storagePool,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_storage_pools_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute storage pools get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the StoragePool result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_storage_pools_get(
        &self,
        args: &ComputeStoragePoolsGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<StoragePool, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_storage_pools_get_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.storagePool,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_storage_pools_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute storage pools get iam policy.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Policy result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_storage_pools_get_iam_policy(
        &self,
        args: &ComputeStoragePoolsGetIamPolicyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Policy, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_storage_pools_get_iam_policy_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.resource,
            &args.optionsRequestedPolicyVersion,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_storage_pools_get_iam_policy_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute storage pools insert.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_storage_pools_insert(
        &self,
        args: &ComputeStoragePoolsInsertArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_storage_pools_insert_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_storage_pools_insert_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute storage pools list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the StoragePoolList result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_storage_pools_list(
        &self,
        args: &ComputeStoragePoolsListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<StoragePoolList, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_storage_pools_list_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.filter,
            &args.maxResults,
            &args.orderBy,
            &args.pageToken,
            &args.returnPartialSuccess,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_storage_pools_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute storage pools list disks.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the StoragePoolListDisks result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_storage_pools_list_disks(
        &self,
        args: &ComputeStoragePoolsListDisksArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<StoragePoolListDisks, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_storage_pools_list_disks_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.storagePool,
            &args.filter,
            &args.maxResults,
            &args.orderBy,
            &args.pageToken,
            &args.returnPartialSuccess,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_storage_pools_list_disks_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute storage pools set iam policy.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Policy result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_storage_pools_set_iam_policy(
        &self,
        args: &ComputeStoragePoolsSetIamPolicyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Policy, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_storage_pools_set_iam_policy_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.resource,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_storage_pools_set_iam_policy_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute storage pools test iam permissions.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TestPermissionsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_storage_pools_test_iam_permissions(
        &self,
        args: &ComputeStoragePoolsTestIamPermissionsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TestPermissionsResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_storage_pools_test_iam_permissions_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.resource,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_storage_pools_test_iam_permissions_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute storage pools update.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_storage_pools_update(
        &self,
        args: &ComputeStoragePoolsUpdateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_storage_pools_update_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.storagePool,
            &args.requestId,
            &args.updateMask,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_storage_pools_update_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute subnetworks aggregated list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the SubnetworkAggregatedList result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_subnetworks_aggregated_list(
        &self,
        args: &ComputeSubnetworksAggregatedListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<SubnetworkAggregatedList, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_subnetworks_aggregated_list_builder(
            &self.http_client,
            &args.project,
            &args.filter,
            &args.includeAllScopes,
            &args.maxResults,
            &args.orderBy,
            &args.pageToken,
            &args.returnPartialSuccess,
            &args.serviceProjectNumber,
            &args.views,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_subnetworks_aggregated_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute subnetworks delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_subnetworks_delete(
        &self,
        args: &ComputeSubnetworksDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_subnetworks_delete_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.subnetwork,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_subnetworks_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute subnetworks expand ip cidr range.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_subnetworks_expand_ip_cidr_range(
        &self,
        args: &ComputeSubnetworksExpandIpCidrRangeArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_subnetworks_expand_ip_cidr_range_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.subnetwork,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_subnetworks_expand_ip_cidr_range_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute subnetworks get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Subnetwork result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_subnetworks_get(
        &self,
        args: &ComputeSubnetworksGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Subnetwork, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_subnetworks_get_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.subnetwork,
            &args.views,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_subnetworks_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute subnetworks get iam policy.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Policy result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_subnetworks_get_iam_policy(
        &self,
        args: &ComputeSubnetworksGetIamPolicyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Policy, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_subnetworks_get_iam_policy_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.resource,
            &args.optionsRequestedPolicyVersion,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_subnetworks_get_iam_policy_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute subnetworks insert.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_subnetworks_insert(
        &self,
        args: &ComputeSubnetworksInsertArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_subnetworks_insert_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_subnetworks_insert_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute subnetworks list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the SubnetworkList result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_subnetworks_list(
        &self,
        args: &ComputeSubnetworksListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<SubnetworkList, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_subnetworks_list_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.filter,
            &args.maxResults,
            &args.orderBy,
            &args.pageToken,
            &args.returnPartialSuccess,
            &args.views,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_subnetworks_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute subnetworks list usable.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the UsableSubnetworksAggregatedList result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_subnetworks_list_usable(
        &self,
        args: &ComputeSubnetworksListUsableArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<UsableSubnetworksAggregatedList, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_subnetworks_list_usable_builder(
            &self.http_client,
            &args.project,
            &args.filter,
            &args.maxResults,
            &args.orderBy,
            &args.pageToken,
            &args.returnPartialSuccess,
            &args.serviceProject,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_subnetworks_list_usable_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute subnetworks patch.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_subnetworks_patch(
        &self,
        args: &ComputeSubnetworksPatchArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_subnetworks_patch_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.subnetwork,
            &args.drainTimeoutSeconds,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_subnetworks_patch_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute subnetworks set iam policy.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Policy result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_subnetworks_set_iam_policy(
        &self,
        args: &ComputeSubnetworksSetIamPolicyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Policy, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_subnetworks_set_iam_policy_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.resource,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_subnetworks_set_iam_policy_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute subnetworks set private ip google access.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_subnetworks_set_private_ip_google_access(
        &self,
        args: &ComputeSubnetworksSetPrivateIpGoogleAccessArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_subnetworks_set_private_ip_google_access_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.subnetwork,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_subnetworks_set_private_ip_google_access_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute subnetworks test iam permissions.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TestPermissionsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_subnetworks_test_iam_permissions(
        &self,
        args: &ComputeSubnetworksTestIamPermissionsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TestPermissionsResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_subnetworks_test_iam_permissions_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.resource,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_subnetworks_test_iam_permissions_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute target grpc proxies delete.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_target_grpc_proxies_delete(
        &self,
        args: &ComputeTargetGrpcProxiesDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_target_grpc_proxies_delete_builder(
            &self.http_client,
            &args.project,
            &args.targetGrpcProxy,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_target_grpc_proxies_delete_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute target grpc proxies get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TargetGrpcProxy result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_target_grpc_proxies_get(
        &self,
        args: &ComputeTargetGrpcProxiesGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TargetGrpcProxy, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_target_grpc_proxies_get_builder(
            &self.http_client,
            &args.project,
            &args.targetGrpcProxy,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_target_grpc_proxies_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute target grpc proxies insert.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_target_grpc_proxies_insert(
        &self,
        args: &ComputeTargetGrpcProxiesInsertArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_target_grpc_proxies_insert_builder(
            &self.http_client,
            &args.project,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_target_grpc_proxies_insert_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute target grpc proxies list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TargetGrpcProxyList result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_target_grpc_proxies_list(
        &self,
        args: &ComputeTargetGrpcProxiesListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TargetGrpcProxyList, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_target_grpc_proxies_list_builder(
            &self.http_client,
            &args.project,
            &args.filter,
            &args.maxResults,
            &args.orderBy,
            &args.pageToken,
            &args.returnPartialSuccess,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_target_grpc_proxies_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute target grpc proxies patch.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_target_grpc_proxies_patch(
        &self,
        args: &ComputeTargetGrpcProxiesPatchArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_target_grpc_proxies_patch_builder(
            &self.http_client,
            &args.project,
            &args.targetGrpcProxy,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_target_grpc_proxies_patch_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute target http proxies aggregated list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TargetHttpProxyAggregatedList result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_target_http_proxies_aggregated_list(
        &self,
        args: &ComputeTargetHttpProxiesAggregatedListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TargetHttpProxyAggregatedList, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_target_http_proxies_aggregated_list_builder(
            &self.http_client,
            &args.project,
            &args.filter,
            &args.includeAllScopes,
            &args.maxResults,
            &args.orderBy,
            &args.pageToken,
            &args.returnPartialSuccess,
            &args.serviceProjectNumber,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_target_http_proxies_aggregated_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute target http proxies delete.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_target_http_proxies_delete(
        &self,
        args: &ComputeTargetHttpProxiesDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_target_http_proxies_delete_builder(
            &self.http_client,
            &args.project,
            &args.targetHttpProxy,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_target_http_proxies_delete_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute target http proxies get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TargetHttpProxy result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_target_http_proxies_get(
        &self,
        args: &ComputeTargetHttpProxiesGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TargetHttpProxy, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_target_http_proxies_get_builder(
            &self.http_client,
            &args.project,
            &args.targetHttpProxy,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_target_http_proxies_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute target http proxies insert.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_target_http_proxies_insert(
        &self,
        args: &ComputeTargetHttpProxiesInsertArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_target_http_proxies_insert_builder(
            &self.http_client,
            &args.project,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_target_http_proxies_insert_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute target http proxies list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TargetHttpProxyList result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_target_http_proxies_list(
        &self,
        args: &ComputeTargetHttpProxiesListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TargetHttpProxyList, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_target_http_proxies_list_builder(
            &self.http_client,
            &args.project,
            &args.filter,
            &args.maxResults,
            &args.orderBy,
            &args.pageToken,
            &args.returnPartialSuccess,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_target_http_proxies_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute target http proxies patch.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_target_http_proxies_patch(
        &self,
        args: &ComputeTargetHttpProxiesPatchArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_target_http_proxies_patch_builder(
            &self.http_client,
            &args.project,
            &args.targetHttpProxy,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_target_http_proxies_patch_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute target http proxies set url map.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_target_http_proxies_set_url_map(
        &self,
        args: &ComputeTargetHttpProxiesSetUrlMapArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_target_http_proxies_set_url_map_builder(
            &self.http_client,
            &args.project,
            &args.targetHttpProxy,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_target_http_proxies_set_url_map_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute target https proxies aggregated list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TargetHttpsProxyAggregatedList result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_target_https_proxies_aggregated_list(
        &self,
        args: &ComputeTargetHttpsProxiesAggregatedListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TargetHttpsProxyAggregatedList, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_target_https_proxies_aggregated_list_builder(
            &self.http_client,
            &args.project,
            &args.filter,
            &args.includeAllScopes,
            &args.maxResults,
            &args.orderBy,
            &args.pageToken,
            &args.returnPartialSuccess,
            &args.serviceProjectNumber,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_target_https_proxies_aggregated_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute target https proxies delete.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_target_https_proxies_delete(
        &self,
        args: &ComputeTargetHttpsProxiesDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_target_https_proxies_delete_builder(
            &self.http_client,
            &args.project,
            &args.targetHttpsProxy,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_target_https_proxies_delete_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute target https proxies get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TargetHttpsProxy result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_target_https_proxies_get(
        &self,
        args: &ComputeTargetHttpsProxiesGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TargetHttpsProxy, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_target_https_proxies_get_builder(
            &self.http_client,
            &args.project,
            &args.targetHttpsProxy,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_target_https_proxies_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute target https proxies insert.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_target_https_proxies_insert(
        &self,
        args: &ComputeTargetHttpsProxiesInsertArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_target_https_proxies_insert_builder(
            &self.http_client,
            &args.project,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_target_https_proxies_insert_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute target https proxies list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TargetHttpsProxyList result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_target_https_proxies_list(
        &self,
        args: &ComputeTargetHttpsProxiesListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TargetHttpsProxyList, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_target_https_proxies_list_builder(
            &self.http_client,
            &args.project,
            &args.filter,
            &args.maxResults,
            &args.orderBy,
            &args.pageToken,
            &args.returnPartialSuccess,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_target_https_proxies_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute target https proxies patch.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_target_https_proxies_patch(
        &self,
        args: &ComputeTargetHttpsProxiesPatchArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_target_https_proxies_patch_builder(
            &self.http_client,
            &args.project,
            &args.targetHttpsProxy,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_target_https_proxies_patch_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute target https proxies set certificate map.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_target_https_proxies_set_certificate_map(
        &self,
        args: &ComputeTargetHttpsProxiesSetCertificateMapArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_target_https_proxies_set_certificate_map_builder(
            &self.http_client,
            &args.project,
            &args.targetHttpsProxy,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_target_https_proxies_set_certificate_map_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute target https proxies set quic override.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_target_https_proxies_set_quic_override(
        &self,
        args: &ComputeTargetHttpsProxiesSetQuicOverrideArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_target_https_proxies_set_quic_override_builder(
            &self.http_client,
            &args.project,
            &args.targetHttpsProxy,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_target_https_proxies_set_quic_override_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute target https proxies set ssl certificates.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_target_https_proxies_set_ssl_certificates(
        &self,
        args: &ComputeTargetHttpsProxiesSetSslCertificatesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_target_https_proxies_set_ssl_certificates_builder(
            &self.http_client,
            &args.project,
            &args.targetHttpsProxy,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_target_https_proxies_set_ssl_certificates_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute target https proxies set ssl policy.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_target_https_proxies_set_ssl_policy(
        &self,
        args: &ComputeTargetHttpsProxiesSetSslPolicyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_target_https_proxies_set_ssl_policy_builder(
            &self.http_client,
            &args.project,
            &args.targetHttpsProxy,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_target_https_proxies_set_ssl_policy_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute target https proxies set url map.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_target_https_proxies_set_url_map(
        &self,
        args: &ComputeTargetHttpsProxiesSetUrlMapArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_target_https_proxies_set_url_map_builder(
            &self.http_client,
            &args.project,
            &args.targetHttpsProxy,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_target_https_proxies_set_url_map_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute target instances aggregated list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TargetInstanceAggregatedList result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_target_instances_aggregated_list(
        &self,
        args: &ComputeTargetInstancesAggregatedListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TargetInstanceAggregatedList, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_target_instances_aggregated_list_builder(
            &self.http_client,
            &args.project,
            &args.filter,
            &args.includeAllScopes,
            &args.maxResults,
            &args.orderBy,
            &args.pageToken,
            &args.returnPartialSuccess,
            &args.serviceProjectNumber,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_target_instances_aggregated_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute target instances delete.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_target_instances_delete(
        &self,
        args: &ComputeTargetInstancesDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_target_instances_delete_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.targetInstance,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_target_instances_delete_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute target instances get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TargetInstance result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_target_instances_get(
        &self,
        args: &ComputeTargetInstancesGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TargetInstance, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_target_instances_get_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.targetInstance,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_target_instances_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute target instances insert.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_target_instances_insert(
        &self,
        args: &ComputeTargetInstancesInsertArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_target_instances_insert_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_target_instances_insert_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute target instances list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TargetInstanceList result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_target_instances_list(
        &self,
        args: &ComputeTargetInstancesListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TargetInstanceList, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_target_instances_list_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.filter,
            &args.maxResults,
            &args.orderBy,
            &args.pageToken,
            &args.returnPartialSuccess,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_target_instances_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute target instances set security policy.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_target_instances_set_security_policy(
        &self,
        args: &ComputeTargetInstancesSetSecurityPolicyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_target_instances_set_security_policy_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.targetInstance,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_target_instances_set_security_policy_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute target instances test iam permissions.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TestPermissionsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_target_instances_test_iam_permissions(
        &self,
        args: &ComputeTargetInstancesTestIamPermissionsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TestPermissionsResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_target_instances_test_iam_permissions_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.resource,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_target_instances_test_iam_permissions_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute target pools add health check.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_target_pools_add_health_check(
        &self,
        args: &ComputeTargetPoolsAddHealthCheckArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_target_pools_add_health_check_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.targetPool,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_target_pools_add_health_check_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute target pools add instance.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_target_pools_add_instance(
        &self,
        args: &ComputeTargetPoolsAddInstanceArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_target_pools_add_instance_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.targetPool,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_target_pools_add_instance_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute target pools aggregated list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TargetPoolAggregatedList result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_target_pools_aggregated_list(
        &self,
        args: &ComputeTargetPoolsAggregatedListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TargetPoolAggregatedList, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_target_pools_aggregated_list_builder(
            &self.http_client,
            &args.project,
            &args.filter,
            &args.includeAllScopes,
            &args.maxResults,
            &args.orderBy,
            &args.pageToken,
            &args.returnPartialSuccess,
            &args.serviceProjectNumber,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_target_pools_aggregated_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute target pools delete.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_target_pools_delete(
        &self,
        args: &ComputeTargetPoolsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_target_pools_delete_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.targetPool,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_target_pools_delete_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute target pools get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TargetPool result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_target_pools_get(
        &self,
        args: &ComputeTargetPoolsGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TargetPool, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_target_pools_get_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.targetPool,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_target_pools_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute target pools get health.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TargetPoolInstanceHealth result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_target_pools_get_health(
        &self,
        args: &ComputeTargetPoolsGetHealthArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TargetPoolInstanceHealth, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_target_pools_get_health_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.targetPool,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_target_pools_get_health_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute target pools insert.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_target_pools_insert(
        &self,
        args: &ComputeTargetPoolsInsertArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_target_pools_insert_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_target_pools_insert_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute target pools list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TargetPoolList result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_target_pools_list(
        &self,
        args: &ComputeTargetPoolsListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TargetPoolList, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_target_pools_list_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.filter,
            &args.maxResults,
            &args.orderBy,
            &args.pageToken,
            &args.returnPartialSuccess,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_target_pools_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute target pools remove health check.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_target_pools_remove_health_check(
        &self,
        args: &ComputeTargetPoolsRemoveHealthCheckArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_target_pools_remove_health_check_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.targetPool,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_target_pools_remove_health_check_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute target pools remove instance.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_target_pools_remove_instance(
        &self,
        args: &ComputeTargetPoolsRemoveInstanceArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_target_pools_remove_instance_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.targetPool,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_target_pools_remove_instance_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute target pools set backup.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_target_pools_set_backup(
        &self,
        args: &ComputeTargetPoolsSetBackupArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_target_pools_set_backup_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.targetPool,
            &args.failoverRatio,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_target_pools_set_backup_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute target pools set security policy.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_target_pools_set_security_policy(
        &self,
        args: &ComputeTargetPoolsSetSecurityPolicyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_target_pools_set_security_policy_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.targetPool,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_target_pools_set_security_policy_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute target pools test iam permissions.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TestPermissionsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_target_pools_test_iam_permissions(
        &self,
        args: &ComputeTargetPoolsTestIamPermissionsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TestPermissionsResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_target_pools_test_iam_permissions_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.resource,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_target_pools_test_iam_permissions_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute target ssl proxies delete.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_target_ssl_proxies_delete(
        &self,
        args: &ComputeTargetSslProxiesDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_target_ssl_proxies_delete_builder(
            &self.http_client,
            &args.project,
            &args.targetSslProxy,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_target_ssl_proxies_delete_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute target ssl proxies get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TargetSslProxy result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_target_ssl_proxies_get(
        &self,
        args: &ComputeTargetSslProxiesGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TargetSslProxy, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_target_ssl_proxies_get_builder(
            &self.http_client,
            &args.project,
            &args.targetSslProxy,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_target_ssl_proxies_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute target ssl proxies insert.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_target_ssl_proxies_insert(
        &self,
        args: &ComputeTargetSslProxiesInsertArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_target_ssl_proxies_insert_builder(
            &self.http_client,
            &args.project,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_target_ssl_proxies_insert_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute target ssl proxies list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TargetSslProxyList result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_target_ssl_proxies_list(
        &self,
        args: &ComputeTargetSslProxiesListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TargetSslProxyList, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_target_ssl_proxies_list_builder(
            &self.http_client,
            &args.project,
            &args.filter,
            &args.maxResults,
            &args.orderBy,
            &args.pageToken,
            &args.returnPartialSuccess,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_target_ssl_proxies_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute target ssl proxies set backend service.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_target_ssl_proxies_set_backend_service(
        &self,
        args: &ComputeTargetSslProxiesSetBackendServiceArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_target_ssl_proxies_set_backend_service_builder(
            &self.http_client,
            &args.project,
            &args.targetSslProxy,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_target_ssl_proxies_set_backend_service_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute target ssl proxies set certificate map.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_target_ssl_proxies_set_certificate_map(
        &self,
        args: &ComputeTargetSslProxiesSetCertificateMapArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_target_ssl_proxies_set_certificate_map_builder(
            &self.http_client,
            &args.project,
            &args.targetSslProxy,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_target_ssl_proxies_set_certificate_map_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute target ssl proxies set proxy header.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_target_ssl_proxies_set_proxy_header(
        &self,
        args: &ComputeTargetSslProxiesSetProxyHeaderArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_target_ssl_proxies_set_proxy_header_builder(
            &self.http_client,
            &args.project,
            &args.targetSslProxy,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_target_ssl_proxies_set_proxy_header_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute target ssl proxies set ssl certificates.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_target_ssl_proxies_set_ssl_certificates(
        &self,
        args: &ComputeTargetSslProxiesSetSslCertificatesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_target_ssl_proxies_set_ssl_certificates_builder(
            &self.http_client,
            &args.project,
            &args.targetSslProxy,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_target_ssl_proxies_set_ssl_certificates_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute target ssl proxies set ssl policy.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_target_ssl_proxies_set_ssl_policy(
        &self,
        args: &ComputeTargetSslProxiesSetSslPolicyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_target_ssl_proxies_set_ssl_policy_builder(
            &self.http_client,
            &args.project,
            &args.targetSslProxy,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_target_ssl_proxies_set_ssl_policy_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute target ssl proxies test iam permissions.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TestPermissionsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_target_ssl_proxies_test_iam_permissions(
        &self,
        args: &ComputeTargetSslProxiesTestIamPermissionsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TestPermissionsResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_target_ssl_proxies_test_iam_permissions_builder(
            &self.http_client,
            &args.project,
            &args.resource,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_target_ssl_proxies_test_iam_permissions_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute target tcp proxies aggregated list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TargetTcpProxyAggregatedList result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_target_tcp_proxies_aggregated_list(
        &self,
        args: &ComputeTargetTcpProxiesAggregatedListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TargetTcpProxyAggregatedList, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_target_tcp_proxies_aggregated_list_builder(
            &self.http_client,
            &args.project,
            &args.filter,
            &args.includeAllScopes,
            &args.maxResults,
            &args.orderBy,
            &args.pageToken,
            &args.returnPartialSuccess,
            &args.serviceProjectNumber,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_target_tcp_proxies_aggregated_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute target tcp proxies delete.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_target_tcp_proxies_delete(
        &self,
        args: &ComputeTargetTcpProxiesDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_target_tcp_proxies_delete_builder(
            &self.http_client,
            &args.project,
            &args.targetTcpProxy,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_target_tcp_proxies_delete_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute target tcp proxies get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TargetTcpProxy result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_target_tcp_proxies_get(
        &self,
        args: &ComputeTargetTcpProxiesGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TargetTcpProxy, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_target_tcp_proxies_get_builder(
            &self.http_client,
            &args.project,
            &args.targetTcpProxy,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_target_tcp_proxies_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute target tcp proxies insert.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_target_tcp_proxies_insert(
        &self,
        args: &ComputeTargetTcpProxiesInsertArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_target_tcp_proxies_insert_builder(
            &self.http_client,
            &args.project,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_target_tcp_proxies_insert_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute target tcp proxies list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TargetTcpProxyList result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_target_tcp_proxies_list(
        &self,
        args: &ComputeTargetTcpProxiesListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TargetTcpProxyList, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_target_tcp_proxies_list_builder(
            &self.http_client,
            &args.project,
            &args.filter,
            &args.maxResults,
            &args.orderBy,
            &args.pageToken,
            &args.returnPartialSuccess,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_target_tcp_proxies_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute target tcp proxies set backend service.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_target_tcp_proxies_set_backend_service(
        &self,
        args: &ComputeTargetTcpProxiesSetBackendServiceArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_target_tcp_proxies_set_backend_service_builder(
            &self.http_client,
            &args.project,
            &args.targetTcpProxy,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_target_tcp_proxies_set_backend_service_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute target tcp proxies set proxy header.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_target_tcp_proxies_set_proxy_header(
        &self,
        args: &ComputeTargetTcpProxiesSetProxyHeaderArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_target_tcp_proxies_set_proxy_header_builder(
            &self.http_client,
            &args.project,
            &args.targetTcpProxy,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_target_tcp_proxies_set_proxy_header_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute target tcp proxies test iam permissions.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TestPermissionsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_target_tcp_proxies_test_iam_permissions(
        &self,
        args: &ComputeTargetTcpProxiesTestIamPermissionsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TestPermissionsResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_target_tcp_proxies_test_iam_permissions_builder(
            &self.http_client,
            &args.project,
            &args.resource,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_target_tcp_proxies_test_iam_permissions_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute target vpn gateways aggregated list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TargetVpnGatewayAggregatedList result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_target_vpn_gateways_aggregated_list(
        &self,
        args: &ComputeTargetVpnGatewaysAggregatedListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TargetVpnGatewayAggregatedList, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_target_vpn_gateways_aggregated_list_builder(
            &self.http_client,
            &args.project,
            &args.filter,
            &args.includeAllScopes,
            &args.maxResults,
            &args.orderBy,
            &args.pageToken,
            &args.returnPartialSuccess,
            &args.serviceProjectNumber,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_target_vpn_gateways_aggregated_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute target vpn gateways delete.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_target_vpn_gateways_delete(
        &self,
        args: &ComputeTargetVpnGatewaysDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_target_vpn_gateways_delete_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.targetVpnGateway,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_target_vpn_gateways_delete_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute target vpn gateways get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TargetVpnGateway result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_target_vpn_gateways_get(
        &self,
        args: &ComputeTargetVpnGatewaysGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TargetVpnGateway, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_target_vpn_gateways_get_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.targetVpnGateway,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_target_vpn_gateways_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute target vpn gateways insert.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_target_vpn_gateways_insert(
        &self,
        args: &ComputeTargetVpnGatewaysInsertArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_target_vpn_gateways_insert_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_target_vpn_gateways_insert_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute target vpn gateways list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TargetVpnGatewayList result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_target_vpn_gateways_list(
        &self,
        args: &ComputeTargetVpnGatewaysListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TargetVpnGatewayList, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_target_vpn_gateways_list_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.filter,
            &args.maxResults,
            &args.orderBy,
            &args.pageToken,
            &args.returnPartialSuccess,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_target_vpn_gateways_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute target vpn gateways set labels.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_target_vpn_gateways_set_labels(
        &self,
        args: &ComputeTargetVpnGatewaysSetLabelsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_target_vpn_gateways_set_labels_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.resource,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_target_vpn_gateways_set_labels_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute url maps aggregated list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the UrlMapsAggregatedList result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_url_maps_aggregated_list(
        &self,
        args: &ComputeUrlMapsAggregatedListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<UrlMapsAggregatedList, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_url_maps_aggregated_list_builder(
            &self.http_client,
            &args.project,
            &args.filter,
            &args.includeAllScopes,
            &args.maxResults,
            &args.orderBy,
            &args.pageToken,
            &args.returnPartialSuccess,
            &args.serviceProjectNumber,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_url_maps_aggregated_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute url maps delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_url_maps_delete(
        &self,
        args: &ComputeUrlMapsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_url_maps_delete_builder(
            &self.http_client,
            &args.project,
            &args.urlMap,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_url_maps_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute url maps get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the UrlMap result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_url_maps_get(
        &self,
        args: &ComputeUrlMapsGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<UrlMap, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_url_maps_get_builder(
            &self.http_client,
            &args.project,
            &args.urlMap,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_url_maps_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute url maps insert.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_url_maps_insert(
        &self,
        args: &ComputeUrlMapsInsertArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_url_maps_insert_builder(
            &self.http_client,
            &args.project,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_url_maps_insert_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute url maps invalidate cache.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_url_maps_invalidate_cache(
        &self,
        args: &ComputeUrlMapsInvalidateCacheArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_url_maps_invalidate_cache_builder(
            &self.http_client,
            &args.project,
            &args.urlMap,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_url_maps_invalidate_cache_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute url maps list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the UrlMapList result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_url_maps_list(
        &self,
        args: &ComputeUrlMapsListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<UrlMapList, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_url_maps_list_builder(
            &self.http_client,
            &args.project,
            &args.filter,
            &args.maxResults,
            &args.orderBy,
            &args.pageToken,
            &args.returnPartialSuccess,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_url_maps_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute url maps patch.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_url_maps_patch(
        &self,
        args: &ComputeUrlMapsPatchArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_url_maps_patch_builder(
            &self.http_client,
            &args.project,
            &args.urlMap,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_url_maps_patch_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute url maps test iam permissions.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TestPermissionsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_url_maps_test_iam_permissions(
        &self,
        args: &ComputeUrlMapsTestIamPermissionsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TestPermissionsResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_url_maps_test_iam_permissions_builder(
            &self.http_client,
            &args.project,
            &args.resource,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_url_maps_test_iam_permissions_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute url maps update.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_url_maps_update(
        &self,
        args: &ComputeUrlMapsUpdateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_url_maps_update_builder(
            &self.http_client,
            &args.project,
            &args.urlMap,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_url_maps_update_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute url maps validate.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the UrlMapsValidateResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_url_maps_validate(
        &self,
        args: &ComputeUrlMapsValidateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<UrlMapsValidateResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_url_maps_validate_builder(
            &self.http_client,
            &args.project,
            &args.urlMap,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_url_maps_validate_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute vpn gateways aggregated list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the VpnGatewayAggregatedList result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_vpn_gateways_aggregated_list(
        &self,
        args: &ComputeVpnGatewaysAggregatedListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<VpnGatewayAggregatedList, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_vpn_gateways_aggregated_list_builder(
            &self.http_client,
            &args.project,
            &args.filter,
            &args.includeAllScopes,
            &args.maxResults,
            &args.orderBy,
            &args.pageToken,
            &args.returnPartialSuccess,
            &args.serviceProjectNumber,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_vpn_gateways_aggregated_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute vpn gateways delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_vpn_gateways_delete(
        &self,
        args: &ComputeVpnGatewaysDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_vpn_gateways_delete_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.vpnGateway,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_vpn_gateways_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute vpn gateways get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the VpnGateway result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_vpn_gateways_get(
        &self,
        args: &ComputeVpnGatewaysGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<VpnGateway, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_vpn_gateways_get_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.vpnGateway,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_vpn_gateways_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute vpn gateways get status.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the VpnGatewaysGetStatusResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_vpn_gateways_get_status(
        &self,
        args: &ComputeVpnGatewaysGetStatusArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<VpnGatewaysGetStatusResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_vpn_gateways_get_status_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.vpnGateway,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_vpn_gateways_get_status_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute vpn gateways insert.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_vpn_gateways_insert(
        &self,
        args: &ComputeVpnGatewaysInsertArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_vpn_gateways_insert_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_vpn_gateways_insert_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute vpn gateways list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the VpnGatewayList result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_vpn_gateways_list(
        &self,
        args: &ComputeVpnGatewaysListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<VpnGatewayList, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_vpn_gateways_list_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.filter,
            &args.maxResults,
            &args.orderBy,
            &args.pageToken,
            &args.returnPartialSuccess,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_vpn_gateways_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute vpn gateways set labels.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_vpn_gateways_set_labels(
        &self,
        args: &ComputeVpnGatewaysSetLabelsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_vpn_gateways_set_labels_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.resource,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_vpn_gateways_set_labels_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute vpn gateways test iam permissions.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TestPermissionsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_vpn_gateways_test_iam_permissions(
        &self,
        args: &ComputeVpnGatewaysTestIamPermissionsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TestPermissionsResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_vpn_gateways_test_iam_permissions_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.resource,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_vpn_gateways_test_iam_permissions_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute vpn tunnels aggregated list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the VpnTunnelAggregatedList result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_vpn_tunnels_aggregated_list(
        &self,
        args: &ComputeVpnTunnelsAggregatedListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<VpnTunnelAggregatedList, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_vpn_tunnels_aggregated_list_builder(
            &self.http_client,
            &args.project,
            &args.filter,
            &args.includeAllScopes,
            &args.maxResults,
            &args.orderBy,
            &args.pageToken,
            &args.returnPartialSuccess,
            &args.serviceProjectNumber,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_vpn_tunnels_aggregated_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute vpn tunnels delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_vpn_tunnels_delete(
        &self,
        args: &ComputeVpnTunnelsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_vpn_tunnels_delete_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.vpnTunnel,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_vpn_tunnels_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute vpn tunnels get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the VpnTunnel result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_vpn_tunnels_get(
        &self,
        args: &ComputeVpnTunnelsGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<VpnTunnel, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_vpn_tunnels_get_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.vpnTunnel,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_vpn_tunnels_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute vpn tunnels insert.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_vpn_tunnels_insert(
        &self,
        args: &ComputeVpnTunnelsInsertArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_vpn_tunnels_insert_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_vpn_tunnels_insert_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute vpn tunnels list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the VpnTunnelList result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_vpn_tunnels_list(
        &self,
        args: &ComputeVpnTunnelsListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<VpnTunnelList, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_vpn_tunnels_list_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.filter,
            &args.maxResults,
            &args.orderBy,
            &args.pageToken,
            &args.returnPartialSuccess,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_vpn_tunnels_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute vpn tunnels set labels.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_vpn_tunnels_set_labels(
        &self,
        args: &ComputeVpnTunnelsSetLabelsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_vpn_tunnels_set_labels_builder(
            &self.http_client,
            &args.project,
            &args.region,
            &args.resource,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_vpn_tunnels_set_labels_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute wire groups delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_wire_groups_delete(
        &self,
        args: &ComputeWireGroupsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_wire_groups_delete_builder(
            &self.http_client,
            &args.project,
            &args.crossSiteNetwork,
            &args.wireGroup,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_wire_groups_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute wire groups get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the WireGroup result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_wire_groups_get(
        &self,
        args: &ComputeWireGroupsGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<WireGroup, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_wire_groups_get_builder(
            &self.http_client,
            &args.project,
            &args.crossSiteNetwork,
            &args.wireGroup,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_wire_groups_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute wire groups insert.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_wire_groups_insert(
        &self,
        args: &ComputeWireGroupsInsertArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_wire_groups_insert_builder(
            &self.http_client,
            &args.project,
            &args.crossSiteNetwork,
            &args.requestId,
            &args.validateOnly,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_wire_groups_insert_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute wire groups list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the WireGroupList result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_wire_groups_list(
        &self,
        args: &ComputeWireGroupsListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<WireGroupList, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_wire_groups_list_builder(
            &self.http_client,
            &args.project,
            &args.crossSiteNetwork,
            &args.filter,
            &args.maxResults,
            &args.orderBy,
            &args.pageToken,
            &args.returnPartialSuccess,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_wire_groups_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute wire groups patch.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_wire_groups_patch(
        &self,
        args: &ComputeWireGroupsPatchArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_wire_groups_patch_builder(
            &self.http_client,
            &args.project,
            &args.crossSiteNetwork,
            &args.wireGroup,
            &args.requestId,
            &args.updateMask,
            &args.validateOnly,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_wire_groups_patch_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute zone operations delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the serde_json::Value result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_zone_operations_delete(
        &self,
        args: &ComputeZoneOperationsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<serde_json::Value, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_zone_operations_delete_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.operation,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_zone_operations_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute zone operations get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_zone_operations_get(
        &self,
        args: &ComputeZoneOperationsGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_zone_operations_get_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.operation,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_zone_operations_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute zone operations list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the OperationList result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_zone_operations_list(
        &self,
        args: &ComputeZoneOperationsListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<OperationList, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_zone_operations_list_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.filter,
            &args.maxResults,
            &args.orderBy,
            &args.pageToken,
            &args.returnPartialSuccess,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_zone_operations_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute zone operations wait.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_zone_operations_wait(
        &self,
        args: &ComputeZoneOperationsWaitArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_zone_operations_wait_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.operation,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_zone_operations_wait_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute zone vm extension policies delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_zone_vm_extension_policies_delete(
        &self,
        args: &ComputeZoneVmExtensionPoliciesDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_zone_vm_extension_policies_delete_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.vmExtensionPolicy,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_zone_vm_extension_policies_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute zone vm extension policies get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the VmExtensionPolicy result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_zone_vm_extension_policies_get(
        &self,
        args: &ComputeZoneVmExtensionPoliciesGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<VmExtensionPolicy, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_zone_vm_extension_policies_get_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.vmExtensionPolicy,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_zone_vm_extension_policies_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute zone vm extension policies insert.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_zone_vm_extension_policies_insert(
        &self,
        args: &ComputeZoneVmExtensionPoliciesInsertArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_zone_vm_extension_policies_insert_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_zone_vm_extension_policies_insert_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute zone vm extension policies list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the VmExtensionPolicyList result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_zone_vm_extension_policies_list(
        &self,
        args: &ComputeZoneVmExtensionPoliciesListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<VmExtensionPolicyList, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_zone_vm_extension_policies_list_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.filter,
            &args.maxResults,
            &args.orderBy,
            &args.pageToken,
            &args.returnPartialSuccess,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_zone_vm_extension_policies_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute zone vm extension policies update.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn compute_zone_vm_extension_policies_update(
        &self,
        args: &ComputeZoneVmExtensionPoliciesUpdateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_zone_vm_extension_policies_update_builder(
            &self.http_client,
            &args.project,
            &args.zone,
            &args.vmExtensionPolicy,
            &args.requestId,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_zone_vm_extension_policies_update_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute zones get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Zone result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_zones_get(
        &self,
        args: &ComputeZonesGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Zone, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_zones_get_builder(
            &self.http_client,
            &args.project,
            &args.zone,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_zones_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Compute zones list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ZoneList result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn compute_zones_list(
        &self,
        args: &ComputeZonesListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ZoneList, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = compute_zones_list_builder(
            &self.http_client,
            &args.project,
            &args.filter,
            &args.maxResults,
            &args.orderBy,
            &args.pageToken,
            &args.returnPartialSuccess,
        )
        .map_err(ProviderError::Api)?;

        let task = compute_zones_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

}
