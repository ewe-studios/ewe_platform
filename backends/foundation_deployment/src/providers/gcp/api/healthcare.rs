//! HealthcareProvider - State-aware healthcare API client.
//!
//! WHY: Users need state-aware API clients that automatically track
//!      resource changes in the state store.
//!
//! WHAT: Provider wrapping ProviderClient<S> with methods for
//!       healthcare API endpoints that auto-store results.
//!
//! HOW: Each method wraps the task with StoreStateIdentifierTask
//!      for automatic state persistence on success.

#![cfg(feature = "gcp")]

use crate::providers::gcp::clients::healthcare::{
    healthcare_projects_locations_get_builder, healthcare_projects_locations_get_task,
    healthcare_projects_locations_list_builder, healthcare_projects_locations_list_task,
    healthcare_projects_locations_datasets_create_builder, healthcare_projects_locations_datasets_create_task,
    healthcare_projects_locations_datasets_deidentify_builder, healthcare_projects_locations_datasets_deidentify_task,
    healthcare_projects_locations_datasets_delete_builder, healthcare_projects_locations_datasets_delete_task,
    healthcare_projects_locations_datasets_get_builder, healthcare_projects_locations_datasets_get_task,
    healthcare_projects_locations_datasets_get_iam_policy_builder, healthcare_projects_locations_datasets_get_iam_policy_task,
    healthcare_projects_locations_datasets_list_builder, healthcare_projects_locations_datasets_list_task,
    healthcare_projects_locations_datasets_patch_builder, healthcare_projects_locations_datasets_patch_task,
    healthcare_projects_locations_datasets_set_iam_policy_builder, healthcare_projects_locations_datasets_set_iam_policy_task,
    healthcare_projects_locations_datasets_test_iam_permissions_builder, healthcare_projects_locations_datasets_test_iam_permissions_task,
    healthcare_projects_locations_datasets_consent_stores_check_data_access_builder, healthcare_projects_locations_datasets_consent_stores_check_data_access_task,
    healthcare_projects_locations_datasets_consent_stores_create_builder, healthcare_projects_locations_datasets_consent_stores_create_task,
    healthcare_projects_locations_datasets_consent_stores_delete_builder, healthcare_projects_locations_datasets_consent_stores_delete_task,
    healthcare_projects_locations_datasets_consent_stores_evaluate_user_consents_builder, healthcare_projects_locations_datasets_consent_stores_evaluate_user_consents_task,
    healthcare_projects_locations_datasets_consent_stores_get_builder, healthcare_projects_locations_datasets_consent_stores_get_task,
    healthcare_projects_locations_datasets_consent_stores_get_iam_policy_builder, healthcare_projects_locations_datasets_consent_stores_get_iam_policy_task,
    healthcare_projects_locations_datasets_consent_stores_list_builder, healthcare_projects_locations_datasets_consent_stores_list_task,
    healthcare_projects_locations_datasets_consent_stores_patch_builder, healthcare_projects_locations_datasets_consent_stores_patch_task,
    healthcare_projects_locations_datasets_consent_stores_query_accessible_data_builder, healthcare_projects_locations_datasets_consent_stores_query_accessible_data_task,
    healthcare_projects_locations_datasets_consent_stores_set_iam_policy_builder, healthcare_projects_locations_datasets_consent_stores_set_iam_policy_task,
    healthcare_projects_locations_datasets_consent_stores_test_iam_permissions_builder, healthcare_projects_locations_datasets_consent_stores_test_iam_permissions_task,
    healthcare_projects_locations_datasets_consent_stores_attribute_definitions_create_builder, healthcare_projects_locations_datasets_consent_stores_attribute_definitions_create_task,
    healthcare_projects_locations_datasets_consent_stores_attribute_definitions_delete_builder, healthcare_projects_locations_datasets_consent_stores_attribute_definitions_delete_task,
    healthcare_projects_locations_datasets_consent_stores_attribute_definitions_get_builder, healthcare_projects_locations_datasets_consent_stores_attribute_definitions_get_task,
    healthcare_projects_locations_datasets_consent_stores_attribute_definitions_list_builder, healthcare_projects_locations_datasets_consent_stores_attribute_definitions_list_task,
    healthcare_projects_locations_datasets_consent_stores_attribute_definitions_patch_builder, healthcare_projects_locations_datasets_consent_stores_attribute_definitions_patch_task,
    healthcare_projects_locations_datasets_consent_stores_consent_artifacts_create_builder, healthcare_projects_locations_datasets_consent_stores_consent_artifacts_create_task,
    healthcare_projects_locations_datasets_consent_stores_consent_artifacts_delete_builder, healthcare_projects_locations_datasets_consent_stores_consent_artifacts_delete_task,
    healthcare_projects_locations_datasets_consent_stores_consent_artifacts_get_builder, healthcare_projects_locations_datasets_consent_stores_consent_artifacts_get_task,
    healthcare_projects_locations_datasets_consent_stores_consent_artifacts_list_builder, healthcare_projects_locations_datasets_consent_stores_consent_artifacts_list_task,
    healthcare_projects_locations_datasets_consent_stores_consents_activate_builder, healthcare_projects_locations_datasets_consent_stores_consents_activate_task,
    healthcare_projects_locations_datasets_consent_stores_consents_create_builder, healthcare_projects_locations_datasets_consent_stores_consents_create_task,
    healthcare_projects_locations_datasets_consent_stores_consents_delete_builder, healthcare_projects_locations_datasets_consent_stores_consents_delete_task,
    healthcare_projects_locations_datasets_consent_stores_consents_delete_revision_builder, healthcare_projects_locations_datasets_consent_stores_consents_delete_revision_task,
    healthcare_projects_locations_datasets_consent_stores_consents_get_builder, healthcare_projects_locations_datasets_consent_stores_consents_get_task,
    healthcare_projects_locations_datasets_consent_stores_consents_list_builder, healthcare_projects_locations_datasets_consent_stores_consents_list_task,
    healthcare_projects_locations_datasets_consent_stores_consents_list_revisions_builder, healthcare_projects_locations_datasets_consent_stores_consents_list_revisions_task,
    healthcare_projects_locations_datasets_consent_stores_consents_patch_builder, healthcare_projects_locations_datasets_consent_stores_consents_patch_task,
    healthcare_projects_locations_datasets_consent_stores_consents_reject_builder, healthcare_projects_locations_datasets_consent_stores_consents_reject_task,
    healthcare_projects_locations_datasets_consent_stores_consents_revoke_builder, healthcare_projects_locations_datasets_consent_stores_consents_revoke_task,
    healthcare_projects_locations_datasets_consent_stores_user_data_mappings_archive_builder, healthcare_projects_locations_datasets_consent_stores_user_data_mappings_archive_task,
    healthcare_projects_locations_datasets_consent_stores_user_data_mappings_create_builder, healthcare_projects_locations_datasets_consent_stores_user_data_mappings_create_task,
    healthcare_projects_locations_datasets_consent_stores_user_data_mappings_delete_builder, healthcare_projects_locations_datasets_consent_stores_user_data_mappings_delete_task,
    healthcare_projects_locations_datasets_consent_stores_user_data_mappings_get_builder, healthcare_projects_locations_datasets_consent_stores_user_data_mappings_get_task,
    healthcare_projects_locations_datasets_consent_stores_user_data_mappings_list_builder, healthcare_projects_locations_datasets_consent_stores_user_data_mappings_list_task,
    healthcare_projects_locations_datasets_consent_stores_user_data_mappings_patch_builder, healthcare_projects_locations_datasets_consent_stores_user_data_mappings_patch_task,
    healthcare_projects_locations_datasets_data_mapper_workspaces_get_iam_policy_builder, healthcare_projects_locations_datasets_data_mapper_workspaces_get_iam_policy_task,
    healthcare_projects_locations_datasets_data_mapper_workspaces_set_iam_policy_builder, healthcare_projects_locations_datasets_data_mapper_workspaces_set_iam_policy_task,
    healthcare_projects_locations_datasets_data_mapper_workspaces_test_iam_permissions_builder, healthcare_projects_locations_datasets_data_mapper_workspaces_test_iam_permissions_task,
    healthcare_projects_locations_datasets_dicom_stores_create_builder, healthcare_projects_locations_datasets_dicom_stores_create_task,
    healthcare_projects_locations_datasets_dicom_stores_deidentify_builder, healthcare_projects_locations_datasets_dicom_stores_deidentify_task,
    healthcare_projects_locations_datasets_dicom_stores_delete_builder, healthcare_projects_locations_datasets_dicom_stores_delete_task,
    healthcare_projects_locations_datasets_dicom_stores_export_builder, healthcare_projects_locations_datasets_dicom_stores_export_task,
    healthcare_projects_locations_datasets_dicom_stores_get_builder, healthcare_projects_locations_datasets_dicom_stores_get_task,
    healthcare_projects_locations_datasets_dicom_stores_get_d_i_c_o_m_store_metrics_builder, healthcare_projects_locations_datasets_dicom_stores_get_d_i_c_o_m_store_metrics_task,
    healthcare_projects_locations_datasets_dicom_stores_get_iam_policy_builder, healthcare_projects_locations_datasets_dicom_stores_get_iam_policy_task,
    healthcare_projects_locations_datasets_dicom_stores_import_builder, healthcare_projects_locations_datasets_dicom_stores_import_task,
    healthcare_projects_locations_datasets_dicom_stores_list_builder, healthcare_projects_locations_datasets_dicom_stores_list_task,
    healthcare_projects_locations_datasets_dicom_stores_patch_builder, healthcare_projects_locations_datasets_dicom_stores_patch_task,
    healthcare_projects_locations_datasets_dicom_stores_search_for_instances_builder, healthcare_projects_locations_datasets_dicom_stores_search_for_instances_task,
    healthcare_projects_locations_datasets_dicom_stores_search_for_series_builder, healthcare_projects_locations_datasets_dicom_stores_search_for_series_task,
    healthcare_projects_locations_datasets_dicom_stores_search_for_studies_builder, healthcare_projects_locations_datasets_dicom_stores_search_for_studies_task,
    healthcare_projects_locations_datasets_dicom_stores_set_blob_storage_settings_builder, healthcare_projects_locations_datasets_dicom_stores_set_blob_storage_settings_task,
    healthcare_projects_locations_datasets_dicom_stores_set_iam_policy_builder, healthcare_projects_locations_datasets_dicom_stores_set_iam_policy_task,
    healthcare_projects_locations_datasets_dicom_stores_store_instances_builder, healthcare_projects_locations_datasets_dicom_stores_store_instances_task,
    healthcare_projects_locations_datasets_dicom_stores_test_iam_permissions_builder, healthcare_projects_locations_datasets_dicom_stores_test_iam_permissions_task,
    healthcare_projects_locations_datasets_dicom_stores_dicom_web_studies_get_study_metrics_builder, healthcare_projects_locations_datasets_dicom_stores_dicom_web_studies_get_study_metrics_task,
    healthcare_projects_locations_datasets_dicom_stores_dicom_web_studies_set_blob_storage_settings_builder, healthcare_projects_locations_datasets_dicom_stores_dicom_web_studies_set_blob_storage_settings_task,
    healthcare_projects_locations_datasets_dicom_stores_dicom_web_studies_series_get_series_metrics_builder, healthcare_projects_locations_datasets_dicom_stores_dicom_web_studies_series_get_series_metrics_task,
    healthcare_projects_locations_datasets_dicom_stores_dicom_web_studies_series_instances_get_storage_info_builder, healthcare_projects_locations_datasets_dicom_stores_dicom_web_studies_series_instances_get_storage_info_task,
    healthcare_projects_locations_datasets_dicom_stores_studies_delete_builder, healthcare_projects_locations_datasets_dicom_stores_studies_delete_task,
    healthcare_projects_locations_datasets_dicom_stores_studies_retrieve_metadata_builder, healthcare_projects_locations_datasets_dicom_stores_studies_retrieve_metadata_task,
    healthcare_projects_locations_datasets_dicom_stores_studies_retrieve_study_builder, healthcare_projects_locations_datasets_dicom_stores_studies_retrieve_study_task,
    healthcare_projects_locations_datasets_dicom_stores_studies_search_for_instances_builder, healthcare_projects_locations_datasets_dicom_stores_studies_search_for_instances_task,
    healthcare_projects_locations_datasets_dicom_stores_studies_search_for_series_builder, healthcare_projects_locations_datasets_dicom_stores_studies_search_for_series_task,
    healthcare_projects_locations_datasets_dicom_stores_studies_store_instances_builder, healthcare_projects_locations_datasets_dicom_stores_studies_store_instances_task,
    healthcare_projects_locations_datasets_dicom_stores_studies_series_delete_builder, healthcare_projects_locations_datasets_dicom_stores_studies_series_delete_task,
    healthcare_projects_locations_datasets_dicom_stores_studies_series_retrieve_metadata_builder, healthcare_projects_locations_datasets_dicom_stores_studies_series_retrieve_metadata_task,
    healthcare_projects_locations_datasets_dicom_stores_studies_series_retrieve_series_builder, healthcare_projects_locations_datasets_dicom_stores_studies_series_retrieve_series_task,
    healthcare_projects_locations_datasets_dicom_stores_studies_series_search_for_instances_builder, healthcare_projects_locations_datasets_dicom_stores_studies_series_search_for_instances_task,
    healthcare_projects_locations_datasets_dicom_stores_studies_series_instances_delete_builder, healthcare_projects_locations_datasets_dicom_stores_studies_series_instances_delete_task,
    healthcare_projects_locations_datasets_dicom_stores_studies_series_instances_retrieve_instance_builder, healthcare_projects_locations_datasets_dicom_stores_studies_series_instances_retrieve_instance_task,
    healthcare_projects_locations_datasets_dicom_stores_studies_series_instances_retrieve_metadata_builder, healthcare_projects_locations_datasets_dicom_stores_studies_series_instances_retrieve_metadata_task,
    healthcare_projects_locations_datasets_dicom_stores_studies_series_instances_retrieve_rendered_builder, healthcare_projects_locations_datasets_dicom_stores_studies_series_instances_retrieve_rendered_task,
    healthcare_projects_locations_datasets_dicom_stores_studies_series_instances_bulkdata_retrieve_bulkdata_builder, healthcare_projects_locations_datasets_dicom_stores_studies_series_instances_bulkdata_retrieve_bulkdata_task,
    healthcare_projects_locations_datasets_dicom_stores_studies_series_instances_frames_retrieve_frames_builder, healthcare_projects_locations_datasets_dicom_stores_studies_series_instances_frames_retrieve_frames_task,
    healthcare_projects_locations_datasets_dicom_stores_studies_series_instances_frames_retrieve_rendered_builder, healthcare_projects_locations_datasets_dicom_stores_studies_series_instances_frames_retrieve_rendered_task,
    healthcare_projects_locations_datasets_fhir_stores_apply_admin_consents_builder, healthcare_projects_locations_datasets_fhir_stores_apply_admin_consents_task,
    healthcare_projects_locations_datasets_fhir_stores_apply_consents_builder, healthcare_projects_locations_datasets_fhir_stores_apply_consents_task,
    healthcare_projects_locations_datasets_fhir_stores_bulk_export_group_builder, healthcare_projects_locations_datasets_fhir_stores_bulk_export_group_task,
    healthcare_projects_locations_datasets_fhir_stores_bulk_delete_builder, healthcare_projects_locations_datasets_fhir_stores_bulk_delete_task,
    healthcare_projects_locations_datasets_fhir_stores_create_builder, healthcare_projects_locations_datasets_fhir_stores_create_task,
    healthcare_projects_locations_datasets_fhir_stores_deidentify_builder, healthcare_projects_locations_datasets_fhir_stores_deidentify_task,
    healthcare_projects_locations_datasets_fhir_stores_delete_builder, healthcare_projects_locations_datasets_fhir_stores_delete_task,
    healthcare_projects_locations_datasets_fhir_stores_explain_data_access_builder, healthcare_projects_locations_datasets_fhir_stores_explain_data_access_task,
    healthcare_projects_locations_datasets_fhir_stores_export_builder, healthcare_projects_locations_datasets_fhir_stores_export_task,
    healthcare_projects_locations_datasets_fhir_stores_get_builder, healthcare_projects_locations_datasets_fhir_stores_get_task,
    healthcare_projects_locations_datasets_fhir_stores_get_f_h_i_r_store_metrics_builder, healthcare_projects_locations_datasets_fhir_stores_get_f_h_i_r_store_metrics_task,
    healthcare_projects_locations_datasets_fhir_stores_get_iam_policy_builder, healthcare_projects_locations_datasets_fhir_stores_get_iam_policy_task,
    healthcare_projects_locations_datasets_fhir_stores_import_builder, healthcare_projects_locations_datasets_fhir_stores_import_task,
    healthcare_projects_locations_datasets_fhir_stores_list_builder, healthcare_projects_locations_datasets_fhir_stores_list_task,
    healthcare_projects_locations_datasets_fhir_stores_patch_builder, healthcare_projects_locations_datasets_fhir_stores_patch_task,
    healthcare_projects_locations_datasets_fhir_stores_rollback_builder, healthcare_projects_locations_datasets_fhir_stores_rollback_task,
    healthcare_projects_locations_datasets_fhir_stores_set_iam_policy_builder, healthcare_projects_locations_datasets_fhir_stores_set_iam_policy_task,
    healthcare_projects_locations_datasets_fhir_stores_test_iam_permissions_builder, healthcare_projects_locations_datasets_fhir_stores_test_iam_permissions_task,
    healthcare_projects_locations_datasets_fhir_stores_fhir_binary_create_builder, healthcare_projects_locations_datasets_fhir_stores_fhir_binary_create_task,
    healthcare_projects_locations_datasets_fhir_stores_fhir_binary_read_builder, healthcare_projects_locations_datasets_fhir_stores_fhir_binary_read_task,
    healthcare_projects_locations_datasets_fhir_stores_fhir_binary_update_builder, healthcare_projects_locations_datasets_fhir_stores_fhir_binary_update_task,
    healthcare_projects_locations_datasets_fhir_stores_fhir_binary_vread_builder, healthcare_projects_locations_datasets_fhir_stores_fhir_binary_vread_task,
    healthcare_projects_locations_datasets_fhir_stores_fhir_consent_enforcement_status_builder, healthcare_projects_locations_datasets_fhir_stores_fhir_consent_enforcement_status_task,
    healthcare_projects_locations_datasets_fhir_stores_fhir_patient_consent_enforcement_status_builder, healthcare_projects_locations_datasets_fhir_stores_fhir_patient_consent_enforcement_status_task,
    healthcare_projects_locations_datasets_fhir_stores_fhir_patient_everything_builder, healthcare_projects_locations_datasets_fhir_stores_fhir_patient_everything_task,
    healthcare_projects_locations_datasets_fhir_stores_fhir_resource_purge_builder, healthcare_projects_locations_datasets_fhir_stores_fhir_resource_purge_task,
    healthcare_projects_locations_datasets_fhir_stores_fhir_resource_validate_builder, healthcare_projects_locations_datasets_fhir_stores_fhir_resource_validate_task,
    healthcare_projects_locations_datasets_fhir_stores_fhir_bulk_export_builder, healthcare_projects_locations_datasets_fhir_stores_fhir_bulk_export_task,
    healthcare_projects_locations_datasets_fhir_stores_fhir_capabilities_builder, healthcare_projects_locations_datasets_fhir_stores_fhir_capabilities_task,
    healthcare_projects_locations_datasets_fhir_stores_fhir_conditional_delete_builder, healthcare_projects_locations_datasets_fhir_stores_fhir_conditional_delete_task,
    healthcare_projects_locations_datasets_fhir_stores_fhir_conditional_patch_builder, healthcare_projects_locations_datasets_fhir_stores_fhir_conditional_patch_task,
    healthcare_projects_locations_datasets_fhir_stores_fhir_conditional_update_builder, healthcare_projects_locations_datasets_fhir_stores_fhir_conditional_update_task,
    healthcare_projects_locations_datasets_fhir_stores_fhir_create_builder, healthcare_projects_locations_datasets_fhir_stores_fhir_create_task,
    healthcare_projects_locations_datasets_fhir_stores_fhir_delete_builder, healthcare_projects_locations_datasets_fhir_stores_fhir_delete_task,
    healthcare_projects_locations_datasets_fhir_stores_fhir_execute_bundle_builder, healthcare_projects_locations_datasets_fhir_stores_fhir_execute_bundle_task,
    healthcare_projects_locations_datasets_fhir_stores_fhir_history_builder, healthcare_projects_locations_datasets_fhir_stores_fhir_history_task,
    healthcare_projects_locations_datasets_fhir_stores_fhir_patch_builder, healthcare_projects_locations_datasets_fhir_stores_fhir_patch_task,
    healthcare_projects_locations_datasets_fhir_stores_fhir_read_builder, healthcare_projects_locations_datasets_fhir_stores_fhir_read_task,
    healthcare_projects_locations_datasets_fhir_stores_fhir_search_builder, healthcare_projects_locations_datasets_fhir_stores_fhir_search_task,
    healthcare_projects_locations_datasets_fhir_stores_fhir_search_type_builder, healthcare_projects_locations_datasets_fhir_stores_fhir_search_type_task,
    healthcare_projects_locations_datasets_fhir_stores_fhir_update_builder, healthcare_projects_locations_datasets_fhir_stores_fhir_update_task,
    healthcare_projects_locations_datasets_fhir_stores_fhir_vread_builder, healthcare_projects_locations_datasets_fhir_stores_fhir_vread_task,
    healthcare_projects_locations_datasets_fhir_stores_operations_delete_fhir_operation_builder, healthcare_projects_locations_datasets_fhir_stores_operations_delete_fhir_operation_task,
    healthcare_projects_locations_datasets_fhir_stores_operations_get_fhir_operation_status_builder, healthcare_projects_locations_datasets_fhir_stores_operations_get_fhir_operation_status_task,
    healthcare_projects_locations_datasets_hl7_v2_stores_create_builder, healthcare_projects_locations_datasets_hl7_v2_stores_create_task,
    healthcare_projects_locations_datasets_hl7_v2_stores_delete_builder, healthcare_projects_locations_datasets_hl7_v2_stores_delete_task,
    healthcare_projects_locations_datasets_hl7_v2_stores_export_builder, healthcare_projects_locations_datasets_hl7_v2_stores_export_task,
    healthcare_projects_locations_datasets_hl7_v2_stores_get_builder, healthcare_projects_locations_datasets_hl7_v2_stores_get_task,
    healthcare_projects_locations_datasets_hl7_v2_stores_get_h_l7_v2_store_metrics_builder, healthcare_projects_locations_datasets_hl7_v2_stores_get_h_l7_v2_store_metrics_task,
    healthcare_projects_locations_datasets_hl7_v2_stores_get_iam_policy_builder, healthcare_projects_locations_datasets_hl7_v2_stores_get_iam_policy_task,
    healthcare_projects_locations_datasets_hl7_v2_stores_import_builder, healthcare_projects_locations_datasets_hl7_v2_stores_import_task,
    healthcare_projects_locations_datasets_hl7_v2_stores_list_builder, healthcare_projects_locations_datasets_hl7_v2_stores_list_task,
    healthcare_projects_locations_datasets_hl7_v2_stores_patch_builder, healthcare_projects_locations_datasets_hl7_v2_stores_patch_task,
    healthcare_projects_locations_datasets_hl7_v2_stores_rollback_builder, healthcare_projects_locations_datasets_hl7_v2_stores_rollback_task,
    healthcare_projects_locations_datasets_hl7_v2_stores_set_iam_policy_builder, healthcare_projects_locations_datasets_hl7_v2_stores_set_iam_policy_task,
    healthcare_projects_locations_datasets_hl7_v2_stores_test_iam_permissions_builder, healthcare_projects_locations_datasets_hl7_v2_stores_test_iam_permissions_task,
    healthcare_projects_locations_datasets_hl7_v2_stores_messages_create_builder, healthcare_projects_locations_datasets_hl7_v2_stores_messages_create_task,
    healthcare_projects_locations_datasets_hl7_v2_stores_messages_delete_builder, healthcare_projects_locations_datasets_hl7_v2_stores_messages_delete_task,
    healthcare_projects_locations_datasets_hl7_v2_stores_messages_get_builder, healthcare_projects_locations_datasets_hl7_v2_stores_messages_get_task,
    healthcare_projects_locations_datasets_hl7_v2_stores_messages_ingest_builder, healthcare_projects_locations_datasets_hl7_v2_stores_messages_ingest_task,
    healthcare_projects_locations_datasets_hl7_v2_stores_messages_list_builder, healthcare_projects_locations_datasets_hl7_v2_stores_messages_list_task,
    healthcare_projects_locations_datasets_hl7_v2_stores_messages_patch_builder, healthcare_projects_locations_datasets_hl7_v2_stores_messages_patch_task,
    healthcare_projects_locations_datasets_operations_cancel_builder, healthcare_projects_locations_datasets_operations_cancel_task,
    healthcare_projects_locations_datasets_operations_get_builder, healthcare_projects_locations_datasets_operations_get_task,
    healthcare_projects_locations_datasets_operations_list_builder, healthcare_projects_locations_datasets_operations_list_task,
    healthcare_projects_locations_services_nlp_analyze_entities_builder, healthcare_projects_locations_services_nlp_analyze_entities_task,
};
use crate::providers::gcp::clients::types::{ApiError, ApiPending};
use crate::providers::gcp::clients::healthcare::AnalyzeEntitiesResponse;
use crate::providers::gcp::clients::healthcare::ArchiveUserDataMappingResponse;
use crate::providers::gcp::clients::healthcare::AttributeDefinition;
use crate::providers::gcp::clients::healthcare::CheckDataAccessResponse;
use crate::providers::gcp::clients::healthcare::Consent;
use crate::providers::gcp::clients::healthcare::ConsentArtifact;
use crate::providers::gcp::clients::healthcare::ConsentStore;
use crate::providers::gcp::clients::healthcare::Dataset;
use crate::providers::gcp::clients::healthcare::DicomStore;
use crate::providers::gcp::clients::healthcare::DicomStoreMetrics;
use crate::providers::gcp::clients::healthcare::Empty;
use crate::providers::gcp::clients::healthcare::EvaluateUserConsentsResponse;
use crate::providers::gcp::clients::healthcare::ExplainDataAccessResponse;
use crate::providers::gcp::clients::healthcare::FhirStore;
use crate::providers::gcp::clients::healthcare::FhirStoreMetrics;
use crate::providers::gcp::clients::healthcare::Hl7V2Store;
use crate::providers::gcp::clients::healthcare::Hl7V2StoreMetrics;
use crate::providers::gcp::clients::healthcare::HttpBody;
use crate::providers::gcp::clients::healthcare::IngestMessageResponse;
use crate::providers::gcp::clients::healthcare::ListAttributeDefinitionsResponse;
use crate::providers::gcp::clients::healthcare::ListConsentArtifactsResponse;
use crate::providers::gcp::clients::healthcare::ListConsentRevisionsResponse;
use crate::providers::gcp::clients::healthcare::ListConsentStoresResponse;
use crate::providers::gcp::clients::healthcare::ListConsentsResponse;
use crate::providers::gcp::clients::healthcare::ListDatasetsResponse;
use crate::providers::gcp::clients::healthcare::ListDicomStoresResponse;
use crate::providers::gcp::clients::healthcare::ListFhirStoresResponse;
use crate::providers::gcp::clients::healthcare::ListHl7V2StoresResponse;
use crate::providers::gcp::clients::healthcare::ListLocationsResponse;
use crate::providers::gcp::clients::healthcare::ListMessagesResponse;
use crate::providers::gcp::clients::healthcare::ListOperationsResponse;
use crate::providers::gcp::clients::healthcare::ListUserDataMappingsResponse;
use crate::providers::gcp::clients::healthcare::Location;
use crate::providers::gcp::clients::healthcare::Message;
use crate::providers::gcp::clients::healthcare::Operation;
use crate::providers::gcp::clients::healthcare::Policy;
use crate::providers::gcp::clients::healthcare::SeriesMetrics;
use crate::providers::gcp::clients::healthcare::StorageInfo;
use crate::providers::gcp::clients::healthcare::StudyMetrics;
use crate::providers::gcp::clients::healthcare::TestIamPermissionsResponse;
use crate::providers::gcp::clients::healthcare::UserDataMapping;
use crate::providers::gcp::clients::healthcare::HealthcareProjectsLocationsDatasetsConsentStoresAttributeDefinitionsCreateArgs;
use crate::providers::gcp::clients::healthcare::HealthcareProjectsLocationsDatasetsConsentStoresAttributeDefinitionsDeleteArgs;
use crate::providers::gcp::clients::healthcare::HealthcareProjectsLocationsDatasetsConsentStoresAttributeDefinitionsGetArgs;
use crate::providers::gcp::clients::healthcare::HealthcareProjectsLocationsDatasetsConsentStoresAttributeDefinitionsListArgs;
use crate::providers::gcp::clients::healthcare::HealthcareProjectsLocationsDatasetsConsentStoresAttributeDefinitionsPatchArgs;
use crate::providers::gcp::clients::healthcare::HealthcareProjectsLocationsDatasetsConsentStoresCheckDataAccessArgs;
use crate::providers::gcp::clients::healthcare::HealthcareProjectsLocationsDatasetsConsentStoresConsentArtifactsCreateArgs;
use crate::providers::gcp::clients::healthcare::HealthcareProjectsLocationsDatasetsConsentStoresConsentArtifactsDeleteArgs;
use crate::providers::gcp::clients::healthcare::HealthcareProjectsLocationsDatasetsConsentStoresConsentArtifactsGetArgs;
use crate::providers::gcp::clients::healthcare::HealthcareProjectsLocationsDatasetsConsentStoresConsentArtifactsListArgs;
use crate::providers::gcp::clients::healthcare::HealthcareProjectsLocationsDatasetsConsentStoresConsentsActivateArgs;
use crate::providers::gcp::clients::healthcare::HealthcareProjectsLocationsDatasetsConsentStoresConsentsCreateArgs;
use crate::providers::gcp::clients::healthcare::HealthcareProjectsLocationsDatasetsConsentStoresConsentsDeleteArgs;
use crate::providers::gcp::clients::healthcare::HealthcareProjectsLocationsDatasetsConsentStoresConsentsDeleteRevisionArgs;
use crate::providers::gcp::clients::healthcare::HealthcareProjectsLocationsDatasetsConsentStoresConsentsGetArgs;
use crate::providers::gcp::clients::healthcare::HealthcareProjectsLocationsDatasetsConsentStoresConsentsListArgs;
use crate::providers::gcp::clients::healthcare::HealthcareProjectsLocationsDatasetsConsentStoresConsentsListRevisionsArgs;
use crate::providers::gcp::clients::healthcare::HealthcareProjectsLocationsDatasetsConsentStoresConsentsPatchArgs;
use crate::providers::gcp::clients::healthcare::HealthcareProjectsLocationsDatasetsConsentStoresConsentsRejectArgs;
use crate::providers::gcp::clients::healthcare::HealthcareProjectsLocationsDatasetsConsentStoresConsentsRevokeArgs;
use crate::providers::gcp::clients::healthcare::HealthcareProjectsLocationsDatasetsConsentStoresCreateArgs;
use crate::providers::gcp::clients::healthcare::HealthcareProjectsLocationsDatasetsConsentStoresDeleteArgs;
use crate::providers::gcp::clients::healthcare::HealthcareProjectsLocationsDatasetsConsentStoresEvaluateUserConsentsArgs;
use crate::providers::gcp::clients::healthcare::HealthcareProjectsLocationsDatasetsConsentStoresGetArgs;
use crate::providers::gcp::clients::healthcare::HealthcareProjectsLocationsDatasetsConsentStoresGetIamPolicyArgs;
use crate::providers::gcp::clients::healthcare::HealthcareProjectsLocationsDatasetsConsentStoresListArgs;
use crate::providers::gcp::clients::healthcare::HealthcareProjectsLocationsDatasetsConsentStoresPatchArgs;
use crate::providers::gcp::clients::healthcare::HealthcareProjectsLocationsDatasetsConsentStoresQueryAccessibleDataArgs;
use crate::providers::gcp::clients::healthcare::HealthcareProjectsLocationsDatasetsConsentStoresSetIamPolicyArgs;
use crate::providers::gcp::clients::healthcare::HealthcareProjectsLocationsDatasetsConsentStoresTestIamPermissionsArgs;
use crate::providers::gcp::clients::healthcare::HealthcareProjectsLocationsDatasetsConsentStoresUserDataMappingsArchiveArgs;
use crate::providers::gcp::clients::healthcare::HealthcareProjectsLocationsDatasetsConsentStoresUserDataMappingsCreateArgs;
use crate::providers::gcp::clients::healthcare::HealthcareProjectsLocationsDatasetsConsentStoresUserDataMappingsDeleteArgs;
use crate::providers::gcp::clients::healthcare::HealthcareProjectsLocationsDatasetsConsentStoresUserDataMappingsGetArgs;
use crate::providers::gcp::clients::healthcare::HealthcareProjectsLocationsDatasetsConsentStoresUserDataMappingsListArgs;
use crate::providers::gcp::clients::healthcare::HealthcareProjectsLocationsDatasetsConsentStoresUserDataMappingsPatchArgs;
use crate::providers::gcp::clients::healthcare::HealthcareProjectsLocationsDatasetsCreateArgs;
use crate::providers::gcp::clients::healthcare::HealthcareProjectsLocationsDatasetsDataMapperWorkspacesGetIamPolicyArgs;
use crate::providers::gcp::clients::healthcare::HealthcareProjectsLocationsDatasetsDataMapperWorkspacesSetIamPolicyArgs;
use crate::providers::gcp::clients::healthcare::HealthcareProjectsLocationsDatasetsDataMapperWorkspacesTestIamPermissionsArgs;
use crate::providers::gcp::clients::healthcare::HealthcareProjectsLocationsDatasetsDeidentifyArgs;
use crate::providers::gcp::clients::healthcare::HealthcareProjectsLocationsDatasetsDeleteArgs;
use crate::providers::gcp::clients::healthcare::HealthcareProjectsLocationsDatasetsDicomStoresCreateArgs;
use crate::providers::gcp::clients::healthcare::HealthcareProjectsLocationsDatasetsDicomStoresDeidentifyArgs;
use crate::providers::gcp::clients::healthcare::HealthcareProjectsLocationsDatasetsDicomStoresDeleteArgs;
use crate::providers::gcp::clients::healthcare::HealthcareProjectsLocationsDatasetsDicomStoresDicomWebStudiesGetStudyMetricsArgs;
use crate::providers::gcp::clients::healthcare::HealthcareProjectsLocationsDatasetsDicomStoresDicomWebStudiesSeriesGetSeriesMetricsArgs;
use crate::providers::gcp::clients::healthcare::HealthcareProjectsLocationsDatasetsDicomStoresDicomWebStudiesSeriesInstancesGetStorageInfoArgs;
use crate::providers::gcp::clients::healthcare::HealthcareProjectsLocationsDatasetsDicomStoresDicomWebStudiesSetBlobStorageSettingsArgs;
use crate::providers::gcp::clients::healthcare::HealthcareProjectsLocationsDatasetsDicomStoresExportArgs;
use crate::providers::gcp::clients::healthcare::HealthcareProjectsLocationsDatasetsDicomStoresGetArgs;
use crate::providers::gcp::clients::healthcare::HealthcareProjectsLocationsDatasetsDicomStoresGetDICOMStoreMetricsArgs;
use crate::providers::gcp::clients::healthcare::HealthcareProjectsLocationsDatasetsDicomStoresGetIamPolicyArgs;
use crate::providers::gcp::clients::healthcare::HealthcareProjectsLocationsDatasetsDicomStoresImportArgs;
use crate::providers::gcp::clients::healthcare::HealthcareProjectsLocationsDatasetsDicomStoresListArgs;
use crate::providers::gcp::clients::healthcare::HealthcareProjectsLocationsDatasetsDicomStoresPatchArgs;
use crate::providers::gcp::clients::healthcare::HealthcareProjectsLocationsDatasetsDicomStoresSearchForInstancesArgs;
use crate::providers::gcp::clients::healthcare::HealthcareProjectsLocationsDatasetsDicomStoresSearchForSeriesArgs;
use crate::providers::gcp::clients::healthcare::HealthcareProjectsLocationsDatasetsDicomStoresSearchForStudiesArgs;
use crate::providers::gcp::clients::healthcare::HealthcareProjectsLocationsDatasetsDicomStoresSetBlobStorageSettingsArgs;
use crate::providers::gcp::clients::healthcare::HealthcareProjectsLocationsDatasetsDicomStoresSetIamPolicyArgs;
use crate::providers::gcp::clients::healthcare::HealthcareProjectsLocationsDatasetsDicomStoresStoreInstancesArgs;
use crate::providers::gcp::clients::healthcare::HealthcareProjectsLocationsDatasetsDicomStoresStudiesDeleteArgs;
use crate::providers::gcp::clients::healthcare::HealthcareProjectsLocationsDatasetsDicomStoresStudiesRetrieveMetadataArgs;
use crate::providers::gcp::clients::healthcare::HealthcareProjectsLocationsDatasetsDicomStoresStudiesRetrieveStudyArgs;
use crate::providers::gcp::clients::healthcare::HealthcareProjectsLocationsDatasetsDicomStoresStudiesSearchForInstancesArgs;
use crate::providers::gcp::clients::healthcare::HealthcareProjectsLocationsDatasetsDicomStoresStudiesSearchForSeriesArgs;
use crate::providers::gcp::clients::healthcare::HealthcareProjectsLocationsDatasetsDicomStoresStudiesSeriesDeleteArgs;
use crate::providers::gcp::clients::healthcare::HealthcareProjectsLocationsDatasetsDicomStoresStudiesSeriesInstancesBulkdataRetrieveBulkdataArgs;
use crate::providers::gcp::clients::healthcare::HealthcareProjectsLocationsDatasetsDicomStoresStudiesSeriesInstancesDeleteArgs;
use crate::providers::gcp::clients::healthcare::HealthcareProjectsLocationsDatasetsDicomStoresStudiesSeriesInstancesFramesRetrieveFramesArgs;
use crate::providers::gcp::clients::healthcare::HealthcareProjectsLocationsDatasetsDicomStoresStudiesSeriesInstancesFramesRetrieveRenderedArgs;
use crate::providers::gcp::clients::healthcare::HealthcareProjectsLocationsDatasetsDicomStoresStudiesSeriesInstancesRetrieveInstanceArgs;
use crate::providers::gcp::clients::healthcare::HealthcareProjectsLocationsDatasetsDicomStoresStudiesSeriesInstancesRetrieveMetadataArgs;
use crate::providers::gcp::clients::healthcare::HealthcareProjectsLocationsDatasetsDicomStoresStudiesSeriesInstancesRetrieveRenderedArgs;
use crate::providers::gcp::clients::healthcare::HealthcareProjectsLocationsDatasetsDicomStoresStudiesSeriesRetrieveMetadataArgs;
use crate::providers::gcp::clients::healthcare::HealthcareProjectsLocationsDatasetsDicomStoresStudiesSeriesRetrieveSeriesArgs;
use crate::providers::gcp::clients::healthcare::HealthcareProjectsLocationsDatasetsDicomStoresStudiesSeriesSearchForInstancesArgs;
use crate::providers::gcp::clients::healthcare::HealthcareProjectsLocationsDatasetsDicomStoresStudiesStoreInstancesArgs;
use crate::providers::gcp::clients::healthcare::HealthcareProjectsLocationsDatasetsDicomStoresTestIamPermissionsArgs;
use crate::providers::gcp::clients::healthcare::HealthcareProjectsLocationsDatasetsFhirStoresApplyAdminConsentsArgs;
use crate::providers::gcp::clients::healthcare::HealthcareProjectsLocationsDatasetsFhirStoresApplyConsentsArgs;
use crate::providers::gcp::clients::healthcare::HealthcareProjectsLocationsDatasetsFhirStoresBulkDeleteArgs;
use crate::providers::gcp::clients::healthcare::HealthcareProjectsLocationsDatasetsFhirStoresBulkExportGroupArgs;
use crate::providers::gcp::clients::healthcare::HealthcareProjectsLocationsDatasetsFhirStoresCreateArgs;
use crate::providers::gcp::clients::healthcare::HealthcareProjectsLocationsDatasetsFhirStoresDeidentifyArgs;
use crate::providers::gcp::clients::healthcare::HealthcareProjectsLocationsDatasetsFhirStoresDeleteArgs;
use crate::providers::gcp::clients::healthcare::HealthcareProjectsLocationsDatasetsFhirStoresExplainDataAccessArgs;
use crate::providers::gcp::clients::healthcare::HealthcareProjectsLocationsDatasetsFhirStoresExportArgs;
use crate::providers::gcp::clients::healthcare::HealthcareProjectsLocationsDatasetsFhirStoresFhirBinaryCreateArgs;
use crate::providers::gcp::clients::healthcare::HealthcareProjectsLocationsDatasetsFhirStoresFhirBinaryReadArgs;
use crate::providers::gcp::clients::healthcare::HealthcareProjectsLocationsDatasetsFhirStoresFhirBinaryUpdateArgs;
use crate::providers::gcp::clients::healthcare::HealthcareProjectsLocationsDatasetsFhirStoresFhirBinaryVreadArgs;
use crate::providers::gcp::clients::healthcare::HealthcareProjectsLocationsDatasetsFhirStoresFhirBulkExportArgs;
use crate::providers::gcp::clients::healthcare::HealthcareProjectsLocationsDatasetsFhirStoresFhirCapabilitiesArgs;
use crate::providers::gcp::clients::healthcare::HealthcareProjectsLocationsDatasetsFhirStoresFhirConditionalDeleteArgs;
use crate::providers::gcp::clients::healthcare::HealthcareProjectsLocationsDatasetsFhirStoresFhirConditionalPatchArgs;
use crate::providers::gcp::clients::healthcare::HealthcareProjectsLocationsDatasetsFhirStoresFhirConditionalUpdateArgs;
use crate::providers::gcp::clients::healthcare::HealthcareProjectsLocationsDatasetsFhirStoresFhirConsentEnforcementStatusArgs;
use crate::providers::gcp::clients::healthcare::HealthcareProjectsLocationsDatasetsFhirStoresFhirCreateArgs;
use crate::providers::gcp::clients::healthcare::HealthcareProjectsLocationsDatasetsFhirStoresFhirDeleteArgs;
use crate::providers::gcp::clients::healthcare::HealthcareProjectsLocationsDatasetsFhirStoresFhirExecuteBundleArgs;
use crate::providers::gcp::clients::healthcare::HealthcareProjectsLocationsDatasetsFhirStoresFhirHistoryArgs;
use crate::providers::gcp::clients::healthcare::HealthcareProjectsLocationsDatasetsFhirStoresFhirPatchArgs;
use crate::providers::gcp::clients::healthcare::HealthcareProjectsLocationsDatasetsFhirStoresFhirPatientConsentEnforcementStatusArgs;
use crate::providers::gcp::clients::healthcare::HealthcareProjectsLocationsDatasetsFhirStoresFhirPatientEverythingArgs;
use crate::providers::gcp::clients::healthcare::HealthcareProjectsLocationsDatasetsFhirStoresFhirReadArgs;
use crate::providers::gcp::clients::healthcare::HealthcareProjectsLocationsDatasetsFhirStoresFhirResourcePurgeArgs;
use crate::providers::gcp::clients::healthcare::HealthcareProjectsLocationsDatasetsFhirStoresFhirResourceValidateArgs;
use crate::providers::gcp::clients::healthcare::HealthcareProjectsLocationsDatasetsFhirStoresFhirSearchArgs;
use crate::providers::gcp::clients::healthcare::HealthcareProjectsLocationsDatasetsFhirStoresFhirSearchTypeArgs;
use crate::providers::gcp::clients::healthcare::HealthcareProjectsLocationsDatasetsFhirStoresFhirUpdateArgs;
use crate::providers::gcp::clients::healthcare::HealthcareProjectsLocationsDatasetsFhirStoresFhirVreadArgs;
use crate::providers::gcp::clients::healthcare::HealthcareProjectsLocationsDatasetsFhirStoresGetArgs;
use crate::providers::gcp::clients::healthcare::HealthcareProjectsLocationsDatasetsFhirStoresGetFHIRStoreMetricsArgs;
use crate::providers::gcp::clients::healthcare::HealthcareProjectsLocationsDatasetsFhirStoresGetIamPolicyArgs;
use crate::providers::gcp::clients::healthcare::HealthcareProjectsLocationsDatasetsFhirStoresImportArgs;
use crate::providers::gcp::clients::healthcare::HealthcareProjectsLocationsDatasetsFhirStoresListArgs;
use crate::providers::gcp::clients::healthcare::HealthcareProjectsLocationsDatasetsFhirStoresOperationsDeleteFhirOperationArgs;
use crate::providers::gcp::clients::healthcare::HealthcareProjectsLocationsDatasetsFhirStoresOperationsGetFhirOperationStatusArgs;
use crate::providers::gcp::clients::healthcare::HealthcareProjectsLocationsDatasetsFhirStoresPatchArgs;
use crate::providers::gcp::clients::healthcare::HealthcareProjectsLocationsDatasetsFhirStoresRollbackArgs;
use crate::providers::gcp::clients::healthcare::HealthcareProjectsLocationsDatasetsFhirStoresSetIamPolicyArgs;
use crate::providers::gcp::clients::healthcare::HealthcareProjectsLocationsDatasetsFhirStoresTestIamPermissionsArgs;
use crate::providers::gcp::clients::healthcare::HealthcareProjectsLocationsDatasetsGetArgs;
use crate::providers::gcp::clients::healthcare::HealthcareProjectsLocationsDatasetsGetIamPolicyArgs;
use crate::providers::gcp::clients::healthcare::HealthcareProjectsLocationsDatasetsHl7V2StoresCreateArgs;
use crate::providers::gcp::clients::healthcare::HealthcareProjectsLocationsDatasetsHl7V2StoresDeleteArgs;
use crate::providers::gcp::clients::healthcare::HealthcareProjectsLocationsDatasetsHl7V2StoresExportArgs;
use crate::providers::gcp::clients::healthcare::HealthcareProjectsLocationsDatasetsHl7V2StoresGetArgs;
use crate::providers::gcp::clients::healthcare::HealthcareProjectsLocationsDatasetsHl7V2StoresGetHL7V2StoreMetricsArgs;
use crate::providers::gcp::clients::healthcare::HealthcareProjectsLocationsDatasetsHl7V2StoresGetIamPolicyArgs;
use crate::providers::gcp::clients::healthcare::HealthcareProjectsLocationsDatasetsHl7V2StoresImportArgs;
use crate::providers::gcp::clients::healthcare::HealthcareProjectsLocationsDatasetsHl7V2StoresListArgs;
use crate::providers::gcp::clients::healthcare::HealthcareProjectsLocationsDatasetsHl7V2StoresMessagesCreateArgs;
use crate::providers::gcp::clients::healthcare::HealthcareProjectsLocationsDatasetsHl7V2StoresMessagesDeleteArgs;
use crate::providers::gcp::clients::healthcare::HealthcareProjectsLocationsDatasetsHl7V2StoresMessagesGetArgs;
use crate::providers::gcp::clients::healthcare::HealthcareProjectsLocationsDatasetsHl7V2StoresMessagesIngestArgs;
use crate::providers::gcp::clients::healthcare::HealthcareProjectsLocationsDatasetsHl7V2StoresMessagesListArgs;
use crate::providers::gcp::clients::healthcare::HealthcareProjectsLocationsDatasetsHl7V2StoresMessagesPatchArgs;
use crate::providers::gcp::clients::healthcare::HealthcareProjectsLocationsDatasetsHl7V2StoresPatchArgs;
use crate::providers::gcp::clients::healthcare::HealthcareProjectsLocationsDatasetsHl7V2StoresRollbackArgs;
use crate::providers::gcp::clients::healthcare::HealthcareProjectsLocationsDatasetsHl7V2StoresSetIamPolicyArgs;
use crate::providers::gcp::clients::healthcare::HealthcareProjectsLocationsDatasetsHl7V2StoresTestIamPermissionsArgs;
use crate::providers::gcp::clients::healthcare::HealthcareProjectsLocationsDatasetsListArgs;
use crate::providers::gcp::clients::healthcare::HealthcareProjectsLocationsDatasetsOperationsCancelArgs;
use crate::providers::gcp::clients::healthcare::HealthcareProjectsLocationsDatasetsOperationsGetArgs;
use crate::providers::gcp::clients::healthcare::HealthcareProjectsLocationsDatasetsOperationsListArgs;
use crate::providers::gcp::clients::healthcare::HealthcareProjectsLocationsDatasetsPatchArgs;
use crate::providers::gcp::clients::healthcare::HealthcareProjectsLocationsDatasetsSetIamPolicyArgs;
use crate::providers::gcp::clients::healthcare::HealthcareProjectsLocationsDatasetsTestIamPermissionsArgs;
use crate::providers::gcp::clients::healthcare::HealthcareProjectsLocationsGetArgs;
use crate::providers::gcp::clients::healthcare::HealthcareProjectsLocationsListArgs;
use crate::providers::gcp::clients::healthcare::HealthcareProjectsLocationsServicesNlpAnalyzeEntitiesArgs;
use crate::provider_client::{ProviderClient, ProviderError};
use foundation_core::valtron::{execute, StreamIterator};
use foundation_core::wire::simple_http::client::SimpleHttpClient;
use foundation_db::state::store_state_task::StoreStateIdentifierTask;
use std::sync::Arc;

/// HealthcareProvider with automatic state tracking.
///
/// # Type Parameters
///
/// * `S` - StateStore implementation (FileStateStore, SqliteStateStore, etc.)
///
/// # Example
///
/// ```rust
/// let state_store = FileStateStore::new("/path", "my-project", "dev");
/// let client = ProviderClient::new("my-project", "dev", state_store);
/// let http_client = SimpleHttpClient::new(...);
/// let provider = HealthcareProvider::new(client, http_client);
/// ```
#[derive(Clone)]
pub struct HealthcareProvider<S>
where
    S: foundation_db::state::traits::StateStore + Send + Sync + 'static,
{
    client: ProviderClient<S>,
    http_client: Arc<SimpleHttpClient>,
}

impl<S> HealthcareProvider<S>
where
    S: foundation_db::state::traits::StateStore + Send + Sync + 'static,
{
    /// Create new HealthcareProvider.
    pub fn new(client: ProviderClient<S>, http_client: SimpleHttpClient) -> Self {
        Self {
            client,
            http_client: Arc::new(http_client),
        }
    }

    /// Healthcare projects locations get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Location result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn healthcare_projects_locations_get(
        &self,
        args: &HealthcareProjectsLocationsGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Location, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = healthcare_projects_locations_get_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = healthcare_projects_locations_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Healthcare projects locations list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ListLocationsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn healthcare_projects_locations_list(
        &self,
        args: &HealthcareProjectsLocationsListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ListLocationsResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = healthcare_projects_locations_list_builder(
            &self.http_client,
            &args.name,
            &args.extraLocationTypes,
            &args.filter,
            &args.pageSize,
            &args.pageToken,
        )
        .map_err(ProviderError::Api)?;

        let task = healthcare_projects_locations_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Healthcare projects locations datasets create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn healthcare_projects_locations_datasets_create(
        &self,
        args: &HealthcareProjectsLocationsDatasetsCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = healthcare_projects_locations_datasets_create_builder(
            &self.http_client,
            &args.parent,
            &args.datasetId,
        )
        .map_err(ProviderError::Api)?;

        let task = healthcare_projects_locations_datasets_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Healthcare projects locations datasets deidentify.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn healthcare_projects_locations_datasets_deidentify(
        &self,
        args: &HealthcareProjectsLocationsDatasetsDeidentifyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = healthcare_projects_locations_datasets_deidentify_builder(
            &self.http_client,
            &args.sourceDataset,
        )
        .map_err(ProviderError::Api)?;

        let task = healthcare_projects_locations_datasets_deidentify_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Healthcare projects locations datasets delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Empty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn healthcare_projects_locations_datasets_delete(
        &self,
        args: &HealthcareProjectsLocationsDatasetsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Empty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = healthcare_projects_locations_datasets_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = healthcare_projects_locations_datasets_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Healthcare projects locations datasets get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Dataset result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn healthcare_projects_locations_datasets_get(
        &self,
        args: &HealthcareProjectsLocationsDatasetsGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Dataset, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = healthcare_projects_locations_datasets_get_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = healthcare_projects_locations_datasets_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Healthcare projects locations datasets get iam policy.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Policy result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn healthcare_projects_locations_datasets_get_iam_policy(
        &self,
        args: &HealthcareProjectsLocationsDatasetsGetIamPolicyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Policy, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = healthcare_projects_locations_datasets_get_iam_policy_builder(
            &self.http_client,
            &args.resource,
            &args.options.requestedPolicyVersion,
        )
        .map_err(ProviderError::Api)?;

        let task = healthcare_projects_locations_datasets_get_iam_policy_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Healthcare projects locations datasets list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ListDatasetsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn healthcare_projects_locations_datasets_list(
        &self,
        args: &HealthcareProjectsLocationsDatasetsListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ListDatasetsResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = healthcare_projects_locations_datasets_list_builder(
            &self.http_client,
            &args.parent,
            &args.pageSize,
            &args.pageToken,
        )
        .map_err(ProviderError::Api)?;

        let task = healthcare_projects_locations_datasets_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Healthcare projects locations datasets patch.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Dataset result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn healthcare_projects_locations_datasets_patch(
        &self,
        args: &HealthcareProjectsLocationsDatasetsPatchArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Dataset, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = healthcare_projects_locations_datasets_patch_builder(
            &self.http_client,
            &args.name,
            &args.updateMask,
        )
        .map_err(ProviderError::Api)?;

        let task = healthcare_projects_locations_datasets_patch_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Healthcare projects locations datasets set iam policy.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Policy result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn healthcare_projects_locations_datasets_set_iam_policy(
        &self,
        args: &HealthcareProjectsLocationsDatasetsSetIamPolicyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Policy, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = healthcare_projects_locations_datasets_set_iam_policy_builder(
            &self.http_client,
            &args.resource,
        )
        .map_err(ProviderError::Api)?;

        let task = healthcare_projects_locations_datasets_set_iam_policy_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Healthcare projects locations datasets test iam permissions.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TestIamPermissionsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn healthcare_projects_locations_datasets_test_iam_permissions(
        &self,
        args: &HealthcareProjectsLocationsDatasetsTestIamPermissionsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TestIamPermissionsResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = healthcare_projects_locations_datasets_test_iam_permissions_builder(
            &self.http_client,
            &args.resource,
        )
        .map_err(ProviderError::Api)?;

        let task = healthcare_projects_locations_datasets_test_iam_permissions_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Healthcare projects locations datasets consent stores check data access.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the CheckDataAccessResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn healthcare_projects_locations_datasets_consent_stores_check_data_access(
        &self,
        args: &HealthcareProjectsLocationsDatasetsConsentStoresCheckDataAccessArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<CheckDataAccessResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = healthcare_projects_locations_datasets_consent_stores_check_data_access_builder(
            &self.http_client,
            &args.consentStore,
        )
        .map_err(ProviderError::Api)?;

        let task = healthcare_projects_locations_datasets_consent_stores_check_data_access_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Healthcare projects locations datasets consent stores create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ConsentStore result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn healthcare_projects_locations_datasets_consent_stores_create(
        &self,
        args: &HealthcareProjectsLocationsDatasetsConsentStoresCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ConsentStore, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = healthcare_projects_locations_datasets_consent_stores_create_builder(
            &self.http_client,
            &args.parent,
            &args.consentStoreId,
        )
        .map_err(ProviderError::Api)?;

        let task = healthcare_projects_locations_datasets_consent_stores_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Healthcare projects locations datasets consent stores delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Empty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn healthcare_projects_locations_datasets_consent_stores_delete(
        &self,
        args: &HealthcareProjectsLocationsDatasetsConsentStoresDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Empty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = healthcare_projects_locations_datasets_consent_stores_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = healthcare_projects_locations_datasets_consent_stores_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Healthcare projects locations datasets consent stores evaluate user consents.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the EvaluateUserConsentsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn healthcare_projects_locations_datasets_consent_stores_evaluate_user_consents(
        &self,
        args: &HealthcareProjectsLocationsDatasetsConsentStoresEvaluateUserConsentsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<EvaluateUserConsentsResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = healthcare_projects_locations_datasets_consent_stores_evaluate_user_consents_builder(
            &self.http_client,
            &args.consentStore,
        )
        .map_err(ProviderError::Api)?;

        let task = healthcare_projects_locations_datasets_consent_stores_evaluate_user_consents_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Healthcare projects locations datasets consent stores get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ConsentStore result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn healthcare_projects_locations_datasets_consent_stores_get(
        &self,
        args: &HealthcareProjectsLocationsDatasetsConsentStoresGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ConsentStore, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = healthcare_projects_locations_datasets_consent_stores_get_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = healthcare_projects_locations_datasets_consent_stores_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Healthcare projects locations datasets consent stores get iam policy.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Policy result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn healthcare_projects_locations_datasets_consent_stores_get_iam_policy(
        &self,
        args: &HealthcareProjectsLocationsDatasetsConsentStoresGetIamPolicyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Policy, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = healthcare_projects_locations_datasets_consent_stores_get_iam_policy_builder(
            &self.http_client,
            &args.resource,
            &args.options.requestedPolicyVersion,
        )
        .map_err(ProviderError::Api)?;

        let task = healthcare_projects_locations_datasets_consent_stores_get_iam_policy_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Healthcare projects locations datasets consent stores list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ListConsentStoresResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn healthcare_projects_locations_datasets_consent_stores_list(
        &self,
        args: &HealthcareProjectsLocationsDatasetsConsentStoresListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ListConsentStoresResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = healthcare_projects_locations_datasets_consent_stores_list_builder(
            &self.http_client,
            &args.parent,
            &args.filter,
            &args.pageSize,
            &args.pageToken,
        )
        .map_err(ProviderError::Api)?;

        let task = healthcare_projects_locations_datasets_consent_stores_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Healthcare projects locations datasets consent stores patch.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ConsentStore result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn healthcare_projects_locations_datasets_consent_stores_patch(
        &self,
        args: &HealthcareProjectsLocationsDatasetsConsentStoresPatchArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ConsentStore, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = healthcare_projects_locations_datasets_consent_stores_patch_builder(
            &self.http_client,
            &args.name,
            &args.updateMask,
        )
        .map_err(ProviderError::Api)?;

        let task = healthcare_projects_locations_datasets_consent_stores_patch_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Healthcare projects locations datasets consent stores query accessible data.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn healthcare_projects_locations_datasets_consent_stores_query_accessible_data(
        &self,
        args: &HealthcareProjectsLocationsDatasetsConsentStoresQueryAccessibleDataArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = healthcare_projects_locations_datasets_consent_stores_query_accessible_data_builder(
            &self.http_client,
            &args.consentStore,
        )
        .map_err(ProviderError::Api)?;

        let task = healthcare_projects_locations_datasets_consent_stores_query_accessible_data_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Healthcare projects locations datasets consent stores set iam policy.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Policy result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn healthcare_projects_locations_datasets_consent_stores_set_iam_policy(
        &self,
        args: &HealthcareProjectsLocationsDatasetsConsentStoresSetIamPolicyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Policy, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = healthcare_projects_locations_datasets_consent_stores_set_iam_policy_builder(
            &self.http_client,
            &args.resource,
        )
        .map_err(ProviderError::Api)?;

        let task = healthcare_projects_locations_datasets_consent_stores_set_iam_policy_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Healthcare projects locations datasets consent stores test iam permissions.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TestIamPermissionsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn healthcare_projects_locations_datasets_consent_stores_test_iam_permissions(
        &self,
        args: &HealthcareProjectsLocationsDatasetsConsentStoresTestIamPermissionsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TestIamPermissionsResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = healthcare_projects_locations_datasets_consent_stores_test_iam_permissions_builder(
            &self.http_client,
            &args.resource,
        )
        .map_err(ProviderError::Api)?;

        let task = healthcare_projects_locations_datasets_consent_stores_test_iam_permissions_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Healthcare projects locations datasets consent stores attribute definitions create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the AttributeDefinition result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn healthcare_projects_locations_datasets_consent_stores_attribute_definitions_create(
        &self,
        args: &HealthcareProjectsLocationsDatasetsConsentStoresAttributeDefinitionsCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<AttributeDefinition, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = healthcare_projects_locations_datasets_consent_stores_attribute_definitions_create_builder(
            &self.http_client,
            &args.parent,
            &args.attributeDefinitionId,
        )
        .map_err(ProviderError::Api)?;

        let task = healthcare_projects_locations_datasets_consent_stores_attribute_definitions_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Healthcare projects locations datasets consent stores attribute definitions delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Empty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn healthcare_projects_locations_datasets_consent_stores_attribute_definitions_delete(
        &self,
        args: &HealthcareProjectsLocationsDatasetsConsentStoresAttributeDefinitionsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Empty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = healthcare_projects_locations_datasets_consent_stores_attribute_definitions_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = healthcare_projects_locations_datasets_consent_stores_attribute_definitions_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Healthcare projects locations datasets consent stores attribute definitions get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the AttributeDefinition result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn healthcare_projects_locations_datasets_consent_stores_attribute_definitions_get(
        &self,
        args: &HealthcareProjectsLocationsDatasetsConsentStoresAttributeDefinitionsGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<AttributeDefinition, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = healthcare_projects_locations_datasets_consent_stores_attribute_definitions_get_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = healthcare_projects_locations_datasets_consent_stores_attribute_definitions_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Healthcare projects locations datasets consent stores attribute definitions list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ListAttributeDefinitionsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn healthcare_projects_locations_datasets_consent_stores_attribute_definitions_list(
        &self,
        args: &HealthcareProjectsLocationsDatasetsConsentStoresAttributeDefinitionsListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ListAttributeDefinitionsResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = healthcare_projects_locations_datasets_consent_stores_attribute_definitions_list_builder(
            &self.http_client,
            &args.parent,
            &args.filter,
            &args.pageSize,
            &args.pageToken,
        )
        .map_err(ProviderError::Api)?;

        let task = healthcare_projects_locations_datasets_consent_stores_attribute_definitions_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Healthcare projects locations datasets consent stores attribute definitions patch.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the AttributeDefinition result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn healthcare_projects_locations_datasets_consent_stores_attribute_definitions_patch(
        &self,
        args: &HealthcareProjectsLocationsDatasetsConsentStoresAttributeDefinitionsPatchArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<AttributeDefinition, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = healthcare_projects_locations_datasets_consent_stores_attribute_definitions_patch_builder(
            &self.http_client,
            &args.name,
            &args.updateMask,
        )
        .map_err(ProviderError::Api)?;

        let task = healthcare_projects_locations_datasets_consent_stores_attribute_definitions_patch_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Healthcare projects locations datasets consent stores consent artifacts create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ConsentArtifact result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn healthcare_projects_locations_datasets_consent_stores_consent_artifacts_create(
        &self,
        args: &HealthcareProjectsLocationsDatasetsConsentStoresConsentArtifactsCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ConsentArtifact, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = healthcare_projects_locations_datasets_consent_stores_consent_artifacts_create_builder(
            &self.http_client,
            &args.parent,
        )
        .map_err(ProviderError::Api)?;

        let task = healthcare_projects_locations_datasets_consent_stores_consent_artifacts_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Healthcare projects locations datasets consent stores consent artifacts delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Empty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn healthcare_projects_locations_datasets_consent_stores_consent_artifacts_delete(
        &self,
        args: &HealthcareProjectsLocationsDatasetsConsentStoresConsentArtifactsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Empty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = healthcare_projects_locations_datasets_consent_stores_consent_artifacts_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = healthcare_projects_locations_datasets_consent_stores_consent_artifacts_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Healthcare projects locations datasets consent stores consent artifacts get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ConsentArtifact result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn healthcare_projects_locations_datasets_consent_stores_consent_artifacts_get(
        &self,
        args: &HealthcareProjectsLocationsDatasetsConsentStoresConsentArtifactsGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ConsentArtifact, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = healthcare_projects_locations_datasets_consent_stores_consent_artifacts_get_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = healthcare_projects_locations_datasets_consent_stores_consent_artifacts_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Healthcare projects locations datasets consent stores consent artifacts list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ListConsentArtifactsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn healthcare_projects_locations_datasets_consent_stores_consent_artifacts_list(
        &self,
        args: &HealthcareProjectsLocationsDatasetsConsentStoresConsentArtifactsListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ListConsentArtifactsResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = healthcare_projects_locations_datasets_consent_stores_consent_artifacts_list_builder(
            &self.http_client,
            &args.parent,
            &args.filter,
            &args.pageSize,
            &args.pageToken,
        )
        .map_err(ProviderError::Api)?;

        let task = healthcare_projects_locations_datasets_consent_stores_consent_artifacts_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Healthcare projects locations datasets consent stores consents activate.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Consent result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn healthcare_projects_locations_datasets_consent_stores_consents_activate(
        &self,
        args: &HealthcareProjectsLocationsDatasetsConsentStoresConsentsActivateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Consent, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = healthcare_projects_locations_datasets_consent_stores_consents_activate_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = healthcare_projects_locations_datasets_consent_stores_consents_activate_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Healthcare projects locations datasets consent stores consents create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Consent result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn healthcare_projects_locations_datasets_consent_stores_consents_create(
        &self,
        args: &HealthcareProjectsLocationsDatasetsConsentStoresConsentsCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Consent, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = healthcare_projects_locations_datasets_consent_stores_consents_create_builder(
            &self.http_client,
            &args.parent,
        )
        .map_err(ProviderError::Api)?;

        let task = healthcare_projects_locations_datasets_consent_stores_consents_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Healthcare projects locations datasets consent stores consents delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Empty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn healthcare_projects_locations_datasets_consent_stores_consents_delete(
        &self,
        args: &HealthcareProjectsLocationsDatasetsConsentStoresConsentsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Empty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = healthcare_projects_locations_datasets_consent_stores_consents_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = healthcare_projects_locations_datasets_consent_stores_consents_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Healthcare projects locations datasets consent stores consents delete revision.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Empty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn healthcare_projects_locations_datasets_consent_stores_consents_delete_revision(
        &self,
        args: &HealthcareProjectsLocationsDatasetsConsentStoresConsentsDeleteRevisionArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Empty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = healthcare_projects_locations_datasets_consent_stores_consents_delete_revision_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = healthcare_projects_locations_datasets_consent_stores_consents_delete_revision_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Healthcare projects locations datasets consent stores consents get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Consent result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn healthcare_projects_locations_datasets_consent_stores_consents_get(
        &self,
        args: &HealthcareProjectsLocationsDatasetsConsentStoresConsentsGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Consent, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = healthcare_projects_locations_datasets_consent_stores_consents_get_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = healthcare_projects_locations_datasets_consent_stores_consents_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Healthcare projects locations datasets consent stores consents list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ListConsentsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn healthcare_projects_locations_datasets_consent_stores_consents_list(
        &self,
        args: &HealthcareProjectsLocationsDatasetsConsentStoresConsentsListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ListConsentsResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = healthcare_projects_locations_datasets_consent_stores_consents_list_builder(
            &self.http_client,
            &args.parent,
            &args.filter,
            &args.pageSize,
            &args.pageToken,
        )
        .map_err(ProviderError::Api)?;

        let task = healthcare_projects_locations_datasets_consent_stores_consents_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Healthcare projects locations datasets consent stores consents list revisions.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ListConsentRevisionsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn healthcare_projects_locations_datasets_consent_stores_consents_list_revisions(
        &self,
        args: &HealthcareProjectsLocationsDatasetsConsentStoresConsentsListRevisionsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ListConsentRevisionsResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = healthcare_projects_locations_datasets_consent_stores_consents_list_revisions_builder(
            &self.http_client,
            &args.name,
            &args.filter,
            &args.pageSize,
            &args.pageToken,
        )
        .map_err(ProviderError::Api)?;

        let task = healthcare_projects_locations_datasets_consent_stores_consents_list_revisions_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Healthcare projects locations datasets consent stores consents patch.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Consent result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn healthcare_projects_locations_datasets_consent_stores_consents_patch(
        &self,
        args: &HealthcareProjectsLocationsDatasetsConsentStoresConsentsPatchArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Consent, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = healthcare_projects_locations_datasets_consent_stores_consents_patch_builder(
            &self.http_client,
            &args.name,
            &args.updateMask,
        )
        .map_err(ProviderError::Api)?;

        let task = healthcare_projects_locations_datasets_consent_stores_consents_patch_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Healthcare projects locations datasets consent stores consents reject.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Consent result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn healthcare_projects_locations_datasets_consent_stores_consents_reject(
        &self,
        args: &HealthcareProjectsLocationsDatasetsConsentStoresConsentsRejectArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Consent, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = healthcare_projects_locations_datasets_consent_stores_consents_reject_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = healthcare_projects_locations_datasets_consent_stores_consents_reject_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Healthcare projects locations datasets consent stores consents revoke.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Consent result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn healthcare_projects_locations_datasets_consent_stores_consents_revoke(
        &self,
        args: &HealthcareProjectsLocationsDatasetsConsentStoresConsentsRevokeArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Consent, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = healthcare_projects_locations_datasets_consent_stores_consents_revoke_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = healthcare_projects_locations_datasets_consent_stores_consents_revoke_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Healthcare projects locations datasets consent stores user data mappings archive.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ArchiveUserDataMappingResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn healthcare_projects_locations_datasets_consent_stores_user_data_mappings_archive(
        &self,
        args: &HealthcareProjectsLocationsDatasetsConsentStoresUserDataMappingsArchiveArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ArchiveUserDataMappingResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = healthcare_projects_locations_datasets_consent_stores_user_data_mappings_archive_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = healthcare_projects_locations_datasets_consent_stores_user_data_mappings_archive_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Healthcare projects locations datasets consent stores user data mappings create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the UserDataMapping result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn healthcare_projects_locations_datasets_consent_stores_user_data_mappings_create(
        &self,
        args: &HealthcareProjectsLocationsDatasetsConsentStoresUserDataMappingsCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<UserDataMapping, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = healthcare_projects_locations_datasets_consent_stores_user_data_mappings_create_builder(
            &self.http_client,
            &args.parent,
        )
        .map_err(ProviderError::Api)?;

        let task = healthcare_projects_locations_datasets_consent_stores_user_data_mappings_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Healthcare projects locations datasets consent stores user data mappings delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Empty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn healthcare_projects_locations_datasets_consent_stores_user_data_mappings_delete(
        &self,
        args: &HealthcareProjectsLocationsDatasetsConsentStoresUserDataMappingsDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Empty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = healthcare_projects_locations_datasets_consent_stores_user_data_mappings_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = healthcare_projects_locations_datasets_consent_stores_user_data_mappings_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Healthcare projects locations datasets consent stores user data mappings get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the UserDataMapping result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn healthcare_projects_locations_datasets_consent_stores_user_data_mappings_get(
        &self,
        args: &HealthcareProjectsLocationsDatasetsConsentStoresUserDataMappingsGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<UserDataMapping, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = healthcare_projects_locations_datasets_consent_stores_user_data_mappings_get_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = healthcare_projects_locations_datasets_consent_stores_user_data_mappings_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Healthcare projects locations datasets consent stores user data mappings list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ListUserDataMappingsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn healthcare_projects_locations_datasets_consent_stores_user_data_mappings_list(
        &self,
        args: &HealthcareProjectsLocationsDatasetsConsentStoresUserDataMappingsListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ListUserDataMappingsResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = healthcare_projects_locations_datasets_consent_stores_user_data_mappings_list_builder(
            &self.http_client,
            &args.parent,
            &args.filter,
            &args.pageSize,
            &args.pageToken,
        )
        .map_err(ProviderError::Api)?;

        let task = healthcare_projects_locations_datasets_consent_stores_user_data_mappings_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Healthcare projects locations datasets consent stores user data mappings patch.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the UserDataMapping result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn healthcare_projects_locations_datasets_consent_stores_user_data_mappings_patch(
        &self,
        args: &HealthcareProjectsLocationsDatasetsConsentStoresUserDataMappingsPatchArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<UserDataMapping, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = healthcare_projects_locations_datasets_consent_stores_user_data_mappings_patch_builder(
            &self.http_client,
            &args.name,
            &args.updateMask,
        )
        .map_err(ProviderError::Api)?;

        let task = healthcare_projects_locations_datasets_consent_stores_user_data_mappings_patch_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Healthcare projects locations datasets data mapper workspaces get iam policy.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Policy result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn healthcare_projects_locations_datasets_data_mapper_workspaces_get_iam_policy(
        &self,
        args: &HealthcareProjectsLocationsDatasetsDataMapperWorkspacesGetIamPolicyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Policy, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = healthcare_projects_locations_datasets_data_mapper_workspaces_get_iam_policy_builder(
            &self.http_client,
            &args.resource,
            &args.options.requestedPolicyVersion,
        )
        .map_err(ProviderError::Api)?;

        let task = healthcare_projects_locations_datasets_data_mapper_workspaces_get_iam_policy_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Healthcare projects locations datasets data mapper workspaces set iam policy.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Policy result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn healthcare_projects_locations_datasets_data_mapper_workspaces_set_iam_policy(
        &self,
        args: &HealthcareProjectsLocationsDatasetsDataMapperWorkspacesSetIamPolicyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Policy, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = healthcare_projects_locations_datasets_data_mapper_workspaces_set_iam_policy_builder(
            &self.http_client,
            &args.resource,
        )
        .map_err(ProviderError::Api)?;

        let task = healthcare_projects_locations_datasets_data_mapper_workspaces_set_iam_policy_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Healthcare projects locations datasets data mapper workspaces test iam permissions.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TestIamPermissionsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn healthcare_projects_locations_datasets_data_mapper_workspaces_test_iam_permissions(
        &self,
        args: &HealthcareProjectsLocationsDatasetsDataMapperWorkspacesTestIamPermissionsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TestIamPermissionsResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = healthcare_projects_locations_datasets_data_mapper_workspaces_test_iam_permissions_builder(
            &self.http_client,
            &args.resource,
        )
        .map_err(ProviderError::Api)?;

        let task = healthcare_projects_locations_datasets_data_mapper_workspaces_test_iam_permissions_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Healthcare projects locations datasets dicom stores create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the DicomStore result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn healthcare_projects_locations_datasets_dicom_stores_create(
        &self,
        args: &HealthcareProjectsLocationsDatasetsDicomStoresCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<DicomStore, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = healthcare_projects_locations_datasets_dicom_stores_create_builder(
            &self.http_client,
            &args.parent,
            &args.dicomStoreId,
        )
        .map_err(ProviderError::Api)?;

        let task = healthcare_projects_locations_datasets_dicom_stores_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Healthcare projects locations datasets dicom stores deidentify.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn healthcare_projects_locations_datasets_dicom_stores_deidentify(
        &self,
        args: &HealthcareProjectsLocationsDatasetsDicomStoresDeidentifyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = healthcare_projects_locations_datasets_dicom_stores_deidentify_builder(
            &self.http_client,
            &args.sourceStore,
        )
        .map_err(ProviderError::Api)?;

        let task = healthcare_projects_locations_datasets_dicom_stores_deidentify_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Healthcare projects locations datasets dicom stores delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Empty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn healthcare_projects_locations_datasets_dicom_stores_delete(
        &self,
        args: &HealthcareProjectsLocationsDatasetsDicomStoresDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Empty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = healthcare_projects_locations_datasets_dicom_stores_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = healthcare_projects_locations_datasets_dicom_stores_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Healthcare projects locations datasets dicom stores export.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn healthcare_projects_locations_datasets_dicom_stores_export(
        &self,
        args: &HealthcareProjectsLocationsDatasetsDicomStoresExportArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = healthcare_projects_locations_datasets_dicom_stores_export_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = healthcare_projects_locations_datasets_dicom_stores_export_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Healthcare projects locations datasets dicom stores get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the DicomStore result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn healthcare_projects_locations_datasets_dicom_stores_get(
        &self,
        args: &HealthcareProjectsLocationsDatasetsDicomStoresGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<DicomStore, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = healthcare_projects_locations_datasets_dicom_stores_get_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = healthcare_projects_locations_datasets_dicom_stores_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Healthcare projects locations datasets dicom stores get d i c o m store metrics.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the DicomStoreMetrics result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn healthcare_projects_locations_datasets_dicom_stores_get_d_i_c_o_m_store_metrics(
        &self,
        args: &HealthcareProjectsLocationsDatasetsDicomStoresGetDICOMStoreMetricsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<DicomStoreMetrics, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = healthcare_projects_locations_datasets_dicom_stores_get_d_i_c_o_m_store_metrics_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = healthcare_projects_locations_datasets_dicom_stores_get_d_i_c_o_m_store_metrics_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Healthcare projects locations datasets dicom stores get iam policy.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Policy result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn healthcare_projects_locations_datasets_dicom_stores_get_iam_policy(
        &self,
        args: &HealthcareProjectsLocationsDatasetsDicomStoresGetIamPolicyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Policy, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = healthcare_projects_locations_datasets_dicom_stores_get_iam_policy_builder(
            &self.http_client,
            &args.resource,
            &args.options.requestedPolicyVersion,
        )
        .map_err(ProviderError::Api)?;

        let task = healthcare_projects_locations_datasets_dicom_stores_get_iam_policy_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Healthcare projects locations datasets dicom stores import.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn healthcare_projects_locations_datasets_dicom_stores_import(
        &self,
        args: &HealthcareProjectsLocationsDatasetsDicomStoresImportArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = healthcare_projects_locations_datasets_dicom_stores_import_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = healthcare_projects_locations_datasets_dicom_stores_import_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Healthcare projects locations datasets dicom stores list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ListDicomStoresResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn healthcare_projects_locations_datasets_dicom_stores_list(
        &self,
        args: &HealthcareProjectsLocationsDatasetsDicomStoresListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ListDicomStoresResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = healthcare_projects_locations_datasets_dicom_stores_list_builder(
            &self.http_client,
            &args.parent,
            &args.filter,
            &args.pageSize,
            &args.pageToken,
        )
        .map_err(ProviderError::Api)?;

        let task = healthcare_projects_locations_datasets_dicom_stores_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Healthcare projects locations datasets dicom stores patch.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the DicomStore result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn healthcare_projects_locations_datasets_dicom_stores_patch(
        &self,
        args: &HealthcareProjectsLocationsDatasetsDicomStoresPatchArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<DicomStore, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = healthcare_projects_locations_datasets_dicom_stores_patch_builder(
            &self.http_client,
            &args.name,
            &args.updateMask,
        )
        .map_err(ProviderError::Api)?;

        let task = healthcare_projects_locations_datasets_dicom_stores_patch_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Healthcare projects locations datasets dicom stores search for instances.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the HttpBody result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn healthcare_projects_locations_datasets_dicom_stores_search_for_instances(
        &self,
        args: &HealthcareProjectsLocationsDatasetsDicomStoresSearchForInstancesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<HttpBody, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = healthcare_projects_locations_datasets_dicom_stores_search_for_instances_builder(
            &self.http_client,
            &args.parent,
            &args.dicomWebPath,
        )
        .map_err(ProviderError::Api)?;

        let task = healthcare_projects_locations_datasets_dicom_stores_search_for_instances_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Healthcare projects locations datasets dicom stores search for series.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the HttpBody result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn healthcare_projects_locations_datasets_dicom_stores_search_for_series(
        &self,
        args: &HealthcareProjectsLocationsDatasetsDicomStoresSearchForSeriesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<HttpBody, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = healthcare_projects_locations_datasets_dicom_stores_search_for_series_builder(
            &self.http_client,
            &args.parent,
            &args.dicomWebPath,
        )
        .map_err(ProviderError::Api)?;

        let task = healthcare_projects_locations_datasets_dicom_stores_search_for_series_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Healthcare projects locations datasets dicom stores search for studies.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the HttpBody result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn healthcare_projects_locations_datasets_dicom_stores_search_for_studies(
        &self,
        args: &HealthcareProjectsLocationsDatasetsDicomStoresSearchForStudiesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<HttpBody, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = healthcare_projects_locations_datasets_dicom_stores_search_for_studies_builder(
            &self.http_client,
            &args.parent,
            &args.dicomWebPath,
        )
        .map_err(ProviderError::Api)?;

        let task = healthcare_projects_locations_datasets_dicom_stores_search_for_studies_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Healthcare projects locations datasets dicom stores set blob storage settings.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn healthcare_projects_locations_datasets_dicom_stores_set_blob_storage_settings(
        &self,
        args: &HealthcareProjectsLocationsDatasetsDicomStoresSetBlobStorageSettingsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = healthcare_projects_locations_datasets_dicom_stores_set_blob_storage_settings_builder(
            &self.http_client,
            &args.resource,
        )
        .map_err(ProviderError::Api)?;

        let task = healthcare_projects_locations_datasets_dicom_stores_set_blob_storage_settings_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Healthcare projects locations datasets dicom stores set iam policy.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Policy result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn healthcare_projects_locations_datasets_dicom_stores_set_iam_policy(
        &self,
        args: &HealthcareProjectsLocationsDatasetsDicomStoresSetIamPolicyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Policy, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = healthcare_projects_locations_datasets_dicom_stores_set_iam_policy_builder(
            &self.http_client,
            &args.resource,
        )
        .map_err(ProviderError::Api)?;

        let task = healthcare_projects_locations_datasets_dicom_stores_set_iam_policy_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Healthcare projects locations datasets dicom stores store instances.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the HttpBody result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn healthcare_projects_locations_datasets_dicom_stores_store_instances(
        &self,
        args: &HealthcareProjectsLocationsDatasetsDicomStoresStoreInstancesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<HttpBody, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = healthcare_projects_locations_datasets_dicom_stores_store_instances_builder(
            &self.http_client,
            &args.parent,
            &args.dicomWebPath,
        )
        .map_err(ProviderError::Api)?;

        let task = healthcare_projects_locations_datasets_dicom_stores_store_instances_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Healthcare projects locations datasets dicom stores test iam permissions.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TestIamPermissionsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn healthcare_projects_locations_datasets_dicom_stores_test_iam_permissions(
        &self,
        args: &HealthcareProjectsLocationsDatasetsDicomStoresTestIamPermissionsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TestIamPermissionsResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = healthcare_projects_locations_datasets_dicom_stores_test_iam_permissions_builder(
            &self.http_client,
            &args.resource,
        )
        .map_err(ProviderError::Api)?;

        let task = healthcare_projects_locations_datasets_dicom_stores_test_iam_permissions_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Healthcare projects locations datasets dicom stores dicom web studies get study metrics.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the StudyMetrics result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn healthcare_projects_locations_datasets_dicom_stores_dicom_web_studies_get_study_metrics(
        &self,
        args: &HealthcareProjectsLocationsDatasetsDicomStoresDicomWebStudiesGetStudyMetricsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<StudyMetrics, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = healthcare_projects_locations_datasets_dicom_stores_dicom_web_studies_get_study_metrics_builder(
            &self.http_client,
            &args.study,
        )
        .map_err(ProviderError::Api)?;

        let task = healthcare_projects_locations_datasets_dicom_stores_dicom_web_studies_get_study_metrics_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Healthcare projects locations datasets dicom stores dicom web studies set blob storage settings.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn healthcare_projects_locations_datasets_dicom_stores_dicom_web_studies_set_blob_storage_settings(
        &self,
        args: &HealthcareProjectsLocationsDatasetsDicomStoresDicomWebStudiesSetBlobStorageSettingsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = healthcare_projects_locations_datasets_dicom_stores_dicom_web_studies_set_blob_storage_settings_builder(
            &self.http_client,
            &args.resource,
        )
        .map_err(ProviderError::Api)?;

        let task = healthcare_projects_locations_datasets_dicom_stores_dicom_web_studies_set_blob_storage_settings_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Healthcare projects locations datasets dicom stores dicom web studies series get series metrics.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the SeriesMetrics result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn healthcare_projects_locations_datasets_dicom_stores_dicom_web_studies_series_get_series_metrics(
        &self,
        args: &HealthcareProjectsLocationsDatasetsDicomStoresDicomWebStudiesSeriesGetSeriesMetricsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<SeriesMetrics, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = healthcare_projects_locations_datasets_dicom_stores_dicom_web_studies_series_get_series_metrics_builder(
            &self.http_client,
            &args.series,
        )
        .map_err(ProviderError::Api)?;

        let task = healthcare_projects_locations_datasets_dicom_stores_dicom_web_studies_series_get_series_metrics_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Healthcare projects locations datasets dicom stores dicom web studies series instances get storage info.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the StorageInfo result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn healthcare_projects_locations_datasets_dicom_stores_dicom_web_studies_series_instances_get_storage_info(
        &self,
        args: &HealthcareProjectsLocationsDatasetsDicomStoresDicomWebStudiesSeriesInstancesGetStorageInfoArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<StorageInfo, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = healthcare_projects_locations_datasets_dicom_stores_dicom_web_studies_series_instances_get_storage_info_builder(
            &self.http_client,
            &args.resource,
        )
        .map_err(ProviderError::Api)?;

        let task = healthcare_projects_locations_datasets_dicom_stores_dicom_web_studies_series_instances_get_storage_info_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Healthcare projects locations datasets dicom stores studies delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn healthcare_projects_locations_datasets_dicom_stores_studies_delete(
        &self,
        args: &HealthcareProjectsLocationsDatasetsDicomStoresStudiesDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = healthcare_projects_locations_datasets_dicom_stores_studies_delete_builder(
            &self.http_client,
            &args.parent,
            &args.dicomWebPath,
        )
        .map_err(ProviderError::Api)?;

        let task = healthcare_projects_locations_datasets_dicom_stores_studies_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Healthcare projects locations datasets dicom stores studies retrieve metadata.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the HttpBody result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn healthcare_projects_locations_datasets_dicom_stores_studies_retrieve_metadata(
        &self,
        args: &HealthcareProjectsLocationsDatasetsDicomStoresStudiesRetrieveMetadataArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<HttpBody, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = healthcare_projects_locations_datasets_dicom_stores_studies_retrieve_metadata_builder(
            &self.http_client,
            &args.parent,
            &args.dicomWebPath,
        )
        .map_err(ProviderError::Api)?;

        let task = healthcare_projects_locations_datasets_dicom_stores_studies_retrieve_metadata_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Healthcare projects locations datasets dicom stores studies retrieve study.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the HttpBody result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn healthcare_projects_locations_datasets_dicom_stores_studies_retrieve_study(
        &self,
        args: &HealthcareProjectsLocationsDatasetsDicomStoresStudiesRetrieveStudyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<HttpBody, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = healthcare_projects_locations_datasets_dicom_stores_studies_retrieve_study_builder(
            &self.http_client,
            &args.parent,
            &args.dicomWebPath,
        )
        .map_err(ProviderError::Api)?;

        let task = healthcare_projects_locations_datasets_dicom_stores_studies_retrieve_study_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Healthcare projects locations datasets dicom stores studies search for instances.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the HttpBody result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn healthcare_projects_locations_datasets_dicom_stores_studies_search_for_instances(
        &self,
        args: &HealthcareProjectsLocationsDatasetsDicomStoresStudiesSearchForInstancesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<HttpBody, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = healthcare_projects_locations_datasets_dicom_stores_studies_search_for_instances_builder(
            &self.http_client,
            &args.parent,
            &args.dicomWebPath,
        )
        .map_err(ProviderError::Api)?;

        let task = healthcare_projects_locations_datasets_dicom_stores_studies_search_for_instances_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Healthcare projects locations datasets dicom stores studies search for series.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the HttpBody result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn healthcare_projects_locations_datasets_dicom_stores_studies_search_for_series(
        &self,
        args: &HealthcareProjectsLocationsDatasetsDicomStoresStudiesSearchForSeriesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<HttpBody, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = healthcare_projects_locations_datasets_dicom_stores_studies_search_for_series_builder(
            &self.http_client,
            &args.parent,
            &args.dicomWebPath,
        )
        .map_err(ProviderError::Api)?;

        let task = healthcare_projects_locations_datasets_dicom_stores_studies_search_for_series_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Healthcare projects locations datasets dicom stores studies store instances.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the HttpBody result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn healthcare_projects_locations_datasets_dicom_stores_studies_store_instances(
        &self,
        args: &HealthcareProjectsLocationsDatasetsDicomStoresStudiesStoreInstancesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<HttpBody, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = healthcare_projects_locations_datasets_dicom_stores_studies_store_instances_builder(
            &self.http_client,
            &args.parent,
            &args.dicomWebPath,
        )
        .map_err(ProviderError::Api)?;

        let task = healthcare_projects_locations_datasets_dicom_stores_studies_store_instances_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Healthcare projects locations datasets dicom stores studies series delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn healthcare_projects_locations_datasets_dicom_stores_studies_series_delete(
        &self,
        args: &HealthcareProjectsLocationsDatasetsDicomStoresStudiesSeriesDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = healthcare_projects_locations_datasets_dicom_stores_studies_series_delete_builder(
            &self.http_client,
            &args.parent,
            &args.dicomWebPath,
        )
        .map_err(ProviderError::Api)?;

        let task = healthcare_projects_locations_datasets_dicom_stores_studies_series_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Healthcare projects locations datasets dicom stores studies series retrieve metadata.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the HttpBody result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn healthcare_projects_locations_datasets_dicom_stores_studies_series_retrieve_metadata(
        &self,
        args: &HealthcareProjectsLocationsDatasetsDicomStoresStudiesSeriesRetrieveMetadataArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<HttpBody, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = healthcare_projects_locations_datasets_dicom_stores_studies_series_retrieve_metadata_builder(
            &self.http_client,
            &args.parent,
            &args.dicomWebPath,
        )
        .map_err(ProviderError::Api)?;

        let task = healthcare_projects_locations_datasets_dicom_stores_studies_series_retrieve_metadata_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Healthcare projects locations datasets dicom stores studies series retrieve series.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the HttpBody result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn healthcare_projects_locations_datasets_dicom_stores_studies_series_retrieve_series(
        &self,
        args: &HealthcareProjectsLocationsDatasetsDicomStoresStudiesSeriesRetrieveSeriesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<HttpBody, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = healthcare_projects_locations_datasets_dicom_stores_studies_series_retrieve_series_builder(
            &self.http_client,
            &args.parent,
            &args.dicomWebPath,
        )
        .map_err(ProviderError::Api)?;

        let task = healthcare_projects_locations_datasets_dicom_stores_studies_series_retrieve_series_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Healthcare projects locations datasets dicom stores studies series search for instances.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the HttpBody result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn healthcare_projects_locations_datasets_dicom_stores_studies_series_search_for_instances(
        &self,
        args: &HealthcareProjectsLocationsDatasetsDicomStoresStudiesSeriesSearchForInstancesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<HttpBody, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = healthcare_projects_locations_datasets_dicom_stores_studies_series_search_for_instances_builder(
            &self.http_client,
            &args.parent,
            &args.dicomWebPath,
        )
        .map_err(ProviderError::Api)?;

        let task = healthcare_projects_locations_datasets_dicom_stores_studies_series_search_for_instances_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Healthcare projects locations datasets dicom stores studies series instances delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Empty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn healthcare_projects_locations_datasets_dicom_stores_studies_series_instances_delete(
        &self,
        args: &HealthcareProjectsLocationsDatasetsDicomStoresStudiesSeriesInstancesDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Empty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = healthcare_projects_locations_datasets_dicom_stores_studies_series_instances_delete_builder(
            &self.http_client,
            &args.parent,
            &args.dicomWebPath,
        )
        .map_err(ProviderError::Api)?;

        let task = healthcare_projects_locations_datasets_dicom_stores_studies_series_instances_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Healthcare projects locations datasets dicom stores studies series instances retrieve instance.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the HttpBody result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn healthcare_projects_locations_datasets_dicom_stores_studies_series_instances_retrieve_instance(
        &self,
        args: &HealthcareProjectsLocationsDatasetsDicomStoresStudiesSeriesInstancesRetrieveInstanceArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<HttpBody, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = healthcare_projects_locations_datasets_dicom_stores_studies_series_instances_retrieve_instance_builder(
            &self.http_client,
            &args.parent,
            &args.dicomWebPath,
        )
        .map_err(ProviderError::Api)?;

        let task = healthcare_projects_locations_datasets_dicom_stores_studies_series_instances_retrieve_instance_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Healthcare projects locations datasets dicom stores studies series instances retrieve metadata.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the HttpBody result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn healthcare_projects_locations_datasets_dicom_stores_studies_series_instances_retrieve_metadata(
        &self,
        args: &HealthcareProjectsLocationsDatasetsDicomStoresStudiesSeriesInstancesRetrieveMetadataArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<HttpBody, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = healthcare_projects_locations_datasets_dicom_stores_studies_series_instances_retrieve_metadata_builder(
            &self.http_client,
            &args.parent,
            &args.dicomWebPath,
        )
        .map_err(ProviderError::Api)?;

        let task = healthcare_projects_locations_datasets_dicom_stores_studies_series_instances_retrieve_metadata_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Healthcare projects locations datasets dicom stores studies series instances retrieve rendered.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the HttpBody result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn healthcare_projects_locations_datasets_dicom_stores_studies_series_instances_retrieve_rendered(
        &self,
        args: &HealthcareProjectsLocationsDatasetsDicomStoresStudiesSeriesInstancesRetrieveRenderedArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<HttpBody, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = healthcare_projects_locations_datasets_dicom_stores_studies_series_instances_retrieve_rendered_builder(
            &self.http_client,
            &args.parent,
            &args.dicomWebPath,
            &args.viewport,
        )
        .map_err(ProviderError::Api)?;

        let task = healthcare_projects_locations_datasets_dicom_stores_studies_series_instances_retrieve_rendered_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Healthcare projects locations datasets dicom stores studies series instances bulkdata retrieve bulkdata.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the HttpBody result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn healthcare_projects_locations_datasets_dicom_stores_studies_series_instances_bulkdata_retrieve_bulkdata(
        &self,
        args: &HealthcareProjectsLocationsDatasetsDicomStoresStudiesSeriesInstancesBulkdataRetrieveBulkdataArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<HttpBody, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = healthcare_projects_locations_datasets_dicom_stores_studies_series_instances_bulkdata_retrieve_bulkdata_builder(
            &self.http_client,
            &args.parent,
            &args.dicomWebPath,
        )
        .map_err(ProviderError::Api)?;

        let task = healthcare_projects_locations_datasets_dicom_stores_studies_series_instances_bulkdata_retrieve_bulkdata_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Healthcare projects locations datasets dicom stores studies series instances frames retrieve frames.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the HttpBody result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn healthcare_projects_locations_datasets_dicom_stores_studies_series_instances_frames_retrieve_frames(
        &self,
        args: &HealthcareProjectsLocationsDatasetsDicomStoresStudiesSeriesInstancesFramesRetrieveFramesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<HttpBody, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = healthcare_projects_locations_datasets_dicom_stores_studies_series_instances_frames_retrieve_frames_builder(
            &self.http_client,
            &args.parent,
            &args.dicomWebPath,
        )
        .map_err(ProviderError::Api)?;

        let task = healthcare_projects_locations_datasets_dicom_stores_studies_series_instances_frames_retrieve_frames_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Healthcare projects locations datasets dicom stores studies series instances frames retrieve rendered.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the HttpBody result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn healthcare_projects_locations_datasets_dicom_stores_studies_series_instances_frames_retrieve_rendered(
        &self,
        args: &HealthcareProjectsLocationsDatasetsDicomStoresStudiesSeriesInstancesFramesRetrieveRenderedArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<HttpBody, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = healthcare_projects_locations_datasets_dicom_stores_studies_series_instances_frames_retrieve_rendered_builder(
            &self.http_client,
            &args.parent,
            &args.dicomWebPath,
            &args.viewport,
        )
        .map_err(ProviderError::Api)?;

        let task = healthcare_projects_locations_datasets_dicom_stores_studies_series_instances_frames_retrieve_rendered_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Healthcare projects locations datasets fhir stores apply admin consents.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn healthcare_projects_locations_datasets_fhir_stores_apply_admin_consents(
        &self,
        args: &HealthcareProjectsLocationsDatasetsFhirStoresApplyAdminConsentsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = healthcare_projects_locations_datasets_fhir_stores_apply_admin_consents_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = healthcare_projects_locations_datasets_fhir_stores_apply_admin_consents_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Healthcare projects locations datasets fhir stores apply consents.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn healthcare_projects_locations_datasets_fhir_stores_apply_consents(
        &self,
        args: &HealthcareProjectsLocationsDatasetsFhirStoresApplyConsentsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = healthcare_projects_locations_datasets_fhir_stores_apply_consents_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = healthcare_projects_locations_datasets_fhir_stores_apply_consents_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Healthcare projects locations datasets fhir stores bulk export group.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the HttpBody result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn healthcare_projects_locations_datasets_fhir_stores_bulk_export_group(
        &self,
        args: &HealthcareProjectsLocationsDatasetsFhirStoresBulkExportGroupArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<HttpBody, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = healthcare_projects_locations_datasets_fhir_stores_bulk_export_group_builder(
            &self.http_client,
            &args.name,
            &args._since,
            &args._type,
            &args.organizeOutputBy,
            &args.outputFormat,
        )
        .map_err(ProviderError::Api)?;

        let task = healthcare_projects_locations_datasets_fhir_stores_bulk_export_group_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Healthcare projects locations datasets fhir stores bulk delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn healthcare_projects_locations_datasets_fhir_stores_bulk_delete(
        &self,
        args: &HealthcareProjectsLocationsDatasetsFhirStoresBulkDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = healthcare_projects_locations_datasets_fhir_stores_bulk_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = healthcare_projects_locations_datasets_fhir_stores_bulk_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Healthcare projects locations datasets fhir stores create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the FhirStore result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn healthcare_projects_locations_datasets_fhir_stores_create(
        &self,
        args: &HealthcareProjectsLocationsDatasetsFhirStoresCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<FhirStore, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = healthcare_projects_locations_datasets_fhir_stores_create_builder(
            &self.http_client,
            &args.parent,
            &args.fhirStoreId,
        )
        .map_err(ProviderError::Api)?;

        let task = healthcare_projects_locations_datasets_fhir_stores_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Healthcare projects locations datasets fhir stores deidentify.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn healthcare_projects_locations_datasets_fhir_stores_deidentify(
        &self,
        args: &HealthcareProjectsLocationsDatasetsFhirStoresDeidentifyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = healthcare_projects_locations_datasets_fhir_stores_deidentify_builder(
            &self.http_client,
            &args.sourceStore,
        )
        .map_err(ProviderError::Api)?;

        let task = healthcare_projects_locations_datasets_fhir_stores_deidentify_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Healthcare projects locations datasets fhir stores delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Empty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn healthcare_projects_locations_datasets_fhir_stores_delete(
        &self,
        args: &HealthcareProjectsLocationsDatasetsFhirStoresDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Empty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = healthcare_projects_locations_datasets_fhir_stores_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = healthcare_projects_locations_datasets_fhir_stores_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Healthcare projects locations datasets fhir stores explain data access.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ExplainDataAccessResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn healthcare_projects_locations_datasets_fhir_stores_explain_data_access(
        &self,
        args: &HealthcareProjectsLocationsDatasetsFhirStoresExplainDataAccessArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ExplainDataAccessResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = healthcare_projects_locations_datasets_fhir_stores_explain_data_access_builder(
            &self.http_client,
            &args.name,
            &args.resourceId,
        )
        .map_err(ProviderError::Api)?;

        let task = healthcare_projects_locations_datasets_fhir_stores_explain_data_access_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Healthcare projects locations datasets fhir stores export.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn healthcare_projects_locations_datasets_fhir_stores_export(
        &self,
        args: &HealthcareProjectsLocationsDatasetsFhirStoresExportArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = healthcare_projects_locations_datasets_fhir_stores_export_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = healthcare_projects_locations_datasets_fhir_stores_export_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Healthcare projects locations datasets fhir stores get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the FhirStore result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn healthcare_projects_locations_datasets_fhir_stores_get(
        &self,
        args: &HealthcareProjectsLocationsDatasetsFhirStoresGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<FhirStore, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = healthcare_projects_locations_datasets_fhir_stores_get_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = healthcare_projects_locations_datasets_fhir_stores_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Healthcare projects locations datasets fhir stores get f h i r store metrics.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the FhirStoreMetrics result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn healthcare_projects_locations_datasets_fhir_stores_get_f_h_i_r_store_metrics(
        &self,
        args: &HealthcareProjectsLocationsDatasetsFhirStoresGetFHIRStoreMetricsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<FhirStoreMetrics, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = healthcare_projects_locations_datasets_fhir_stores_get_f_h_i_r_store_metrics_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = healthcare_projects_locations_datasets_fhir_stores_get_f_h_i_r_store_metrics_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Healthcare projects locations datasets fhir stores get iam policy.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Policy result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn healthcare_projects_locations_datasets_fhir_stores_get_iam_policy(
        &self,
        args: &HealthcareProjectsLocationsDatasetsFhirStoresGetIamPolicyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Policy, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = healthcare_projects_locations_datasets_fhir_stores_get_iam_policy_builder(
            &self.http_client,
            &args.resource,
            &args.options.requestedPolicyVersion,
        )
        .map_err(ProviderError::Api)?;

        let task = healthcare_projects_locations_datasets_fhir_stores_get_iam_policy_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Healthcare projects locations datasets fhir stores import.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn healthcare_projects_locations_datasets_fhir_stores_import(
        &self,
        args: &HealthcareProjectsLocationsDatasetsFhirStoresImportArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = healthcare_projects_locations_datasets_fhir_stores_import_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = healthcare_projects_locations_datasets_fhir_stores_import_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Healthcare projects locations datasets fhir stores list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ListFhirStoresResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn healthcare_projects_locations_datasets_fhir_stores_list(
        &self,
        args: &HealthcareProjectsLocationsDatasetsFhirStoresListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ListFhirStoresResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = healthcare_projects_locations_datasets_fhir_stores_list_builder(
            &self.http_client,
            &args.parent,
            &args.filter,
            &args.pageSize,
            &args.pageToken,
        )
        .map_err(ProviderError::Api)?;

        let task = healthcare_projects_locations_datasets_fhir_stores_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Healthcare projects locations datasets fhir stores patch.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the FhirStore result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn healthcare_projects_locations_datasets_fhir_stores_patch(
        &self,
        args: &HealthcareProjectsLocationsDatasetsFhirStoresPatchArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<FhirStore, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = healthcare_projects_locations_datasets_fhir_stores_patch_builder(
            &self.http_client,
            &args.name,
            &args.updateMask,
        )
        .map_err(ProviderError::Api)?;

        let task = healthcare_projects_locations_datasets_fhir_stores_patch_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Healthcare projects locations datasets fhir stores rollback.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn healthcare_projects_locations_datasets_fhir_stores_rollback(
        &self,
        args: &HealthcareProjectsLocationsDatasetsFhirStoresRollbackArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = healthcare_projects_locations_datasets_fhir_stores_rollback_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = healthcare_projects_locations_datasets_fhir_stores_rollback_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Healthcare projects locations datasets fhir stores set iam policy.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Policy result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn healthcare_projects_locations_datasets_fhir_stores_set_iam_policy(
        &self,
        args: &HealthcareProjectsLocationsDatasetsFhirStoresSetIamPolicyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Policy, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = healthcare_projects_locations_datasets_fhir_stores_set_iam_policy_builder(
            &self.http_client,
            &args.resource,
        )
        .map_err(ProviderError::Api)?;

        let task = healthcare_projects_locations_datasets_fhir_stores_set_iam_policy_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Healthcare projects locations datasets fhir stores test iam permissions.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TestIamPermissionsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn healthcare_projects_locations_datasets_fhir_stores_test_iam_permissions(
        &self,
        args: &HealthcareProjectsLocationsDatasetsFhirStoresTestIamPermissionsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TestIamPermissionsResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = healthcare_projects_locations_datasets_fhir_stores_test_iam_permissions_builder(
            &self.http_client,
            &args.resource,
        )
        .map_err(ProviderError::Api)?;

        let task = healthcare_projects_locations_datasets_fhir_stores_test_iam_permissions_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Healthcare projects locations datasets fhir stores fhir binary create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the HttpBody result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn healthcare_projects_locations_datasets_fhir_stores_fhir_binary_create(
        &self,
        args: &HealthcareProjectsLocationsDatasetsFhirStoresFhirBinaryCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<HttpBody, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = healthcare_projects_locations_datasets_fhir_stores_fhir_binary_create_builder(
            &self.http_client,
            &args.parent,
        )
        .map_err(ProviderError::Api)?;

        let task = healthcare_projects_locations_datasets_fhir_stores_fhir_binary_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Healthcare projects locations datasets fhir stores fhir binary read.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the HttpBody result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn healthcare_projects_locations_datasets_fhir_stores_fhir_binary_read(
        &self,
        args: &HealthcareProjectsLocationsDatasetsFhirStoresFhirBinaryReadArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<HttpBody, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = healthcare_projects_locations_datasets_fhir_stores_fhir_binary_read_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = healthcare_projects_locations_datasets_fhir_stores_fhir_binary_read_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Healthcare projects locations datasets fhir stores fhir binary update.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the HttpBody result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn healthcare_projects_locations_datasets_fhir_stores_fhir_binary_update(
        &self,
        args: &HealthcareProjectsLocationsDatasetsFhirStoresFhirBinaryUpdateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<HttpBody, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = healthcare_projects_locations_datasets_fhir_stores_fhir_binary_update_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = healthcare_projects_locations_datasets_fhir_stores_fhir_binary_update_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Healthcare projects locations datasets fhir stores fhir binary vread.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the HttpBody result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn healthcare_projects_locations_datasets_fhir_stores_fhir_binary_vread(
        &self,
        args: &HealthcareProjectsLocationsDatasetsFhirStoresFhirBinaryVreadArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<HttpBody, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = healthcare_projects_locations_datasets_fhir_stores_fhir_binary_vread_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = healthcare_projects_locations_datasets_fhir_stores_fhir_binary_vread_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Healthcare projects locations datasets fhir stores fhir consent enforcement status.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the HttpBody result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn healthcare_projects_locations_datasets_fhir_stores_fhir_consent_enforcement_status(
        &self,
        args: &HealthcareProjectsLocationsDatasetsFhirStoresFhirConsentEnforcementStatusArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<HttpBody, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = healthcare_projects_locations_datasets_fhir_stores_fhir_consent_enforcement_status_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = healthcare_projects_locations_datasets_fhir_stores_fhir_consent_enforcement_status_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Healthcare projects locations datasets fhir stores fhir patient consent enforcement status.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the HttpBody result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn healthcare_projects_locations_datasets_fhir_stores_fhir_patient_consent_enforcement_status(
        &self,
        args: &HealthcareProjectsLocationsDatasetsFhirStoresFhirPatientConsentEnforcementStatusArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<HttpBody, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = healthcare_projects_locations_datasets_fhir_stores_fhir_patient_consent_enforcement_status_builder(
            &self.http_client,
            &args.name,
            &args._count,
            &args._page_token,
        )
        .map_err(ProviderError::Api)?;

        let task = healthcare_projects_locations_datasets_fhir_stores_fhir_patient_consent_enforcement_status_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Healthcare projects locations datasets fhir stores fhir patient everything.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the HttpBody result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn healthcare_projects_locations_datasets_fhir_stores_fhir_patient_everything(
        &self,
        args: &HealthcareProjectsLocationsDatasetsFhirStoresFhirPatientEverythingArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<HttpBody, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = healthcare_projects_locations_datasets_fhir_stores_fhir_patient_everything_builder(
            &self.http_client,
            &args.name,
            &args._count,
            &args._page_token,
            &args._since,
            &args._type,
            &args.end,
            &args.start,
        )
        .map_err(ProviderError::Api)?;

        let task = healthcare_projects_locations_datasets_fhir_stores_fhir_patient_everything_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Healthcare projects locations datasets fhir stores fhir resource purge.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Empty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn healthcare_projects_locations_datasets_fhir_stores_fhir_resource_purge(
        &self,
        args: &HealthcareProjectsLocationsDatasetsFhirStoresFhirResourcePurgeArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Empty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = healthcare_projects_locations_datasets_fhir_stores_fhir_resource_purge_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = healthcare_projects_locations_datasets_fhir_stores_fhir_resource_purge_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Healthcare projects locations datasets fhir stores fhir resource validate.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the HttpBody result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn healthcare_projects_locations_datasets_fhir_stores_fhir_resource_validate(
        &self,
        args: &HealthcareProjectsLocationsDatasetsFhirStoresFhirResourceValidateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<HttpBody, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = healthcare_projects_locations_datasets_fhir_stores_fhir_resource_validate_builder(
            &self.http_client,
            &args.parent,
            &args.type_rs,
            &args.profile,
        )
        .map_err(ProviderError::Api)?;

        let task = healthcare_projects_locations_datasets_fhir_stores_fhir_resource_validate_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Healthcare projects locations datasets fhir stores fhir bulk export.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the HttpBody result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn healthcare_projects_locations_datasets_fhir_stores_fhir_bulk_export(
        &self,
        args: &HealthcareProjectsLocationsDatasetsFhirStoresFhirBulkExportArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<HttpBody, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = healthcare_projects_locations_datasets_fhir_stores_fhir_bulk_export_builder(
            &self.http_client,
            &args.name,
            &args._since,
            &args._type,
            &args.outputFormat,
        )
        .map_err(ProviderError::Api)?;

        let task = healthcare_projects_locations_datasets_fhir_stores_fhir_bulk_export_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Healthcare projects locations datasets fhir stores fhir capabilities.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the HttpBody result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn healthcare_projects_locations_datasets_fhir_stores_fhir_capabilities(
        &self,
        args: &HealthcareProjectsLocationsDatasetsFhirStoresFhirCapabilitiesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<HttpBody, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = healthcare_projects_locations_datasets_fhir_stores_fhir_capabilities_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = healthcare_projects_locations_datasets_fhir_stores_fhir_capabilities_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Healthcare projects locations datasets fhir stores fhir conditional delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Empty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn healthcare_projects_locations_datasets_fhir_stores_fhir_conditional_delete(
        &self,
        args: &HealthcareProjectsLocationsDatasetsFhirStoresFhirConditionalDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Empty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = healthcare_projects_locations_datasets_fhir_stores_fhir_conditional_delete_builder(
            &self.http_client,
            &args.parent,
            &args.type_rs,
        )
        .map_err(ProviderError::Api)?;

        let task = healthcare_projects_locations_datasets_fhir_stores_fhir_conditional_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Healthcare projects locations datasets fhir stores fhir conditional patch.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the HttpBody result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn healthcare_projects_locations_datasets_fhir_stores_fhir_conditional_patch(
        &self,
        args: &HealthcareProjectsLocationsDatasetsFhirStoresFhirConditionalPatchArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<HttpBody, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = healthcare_projects_locations_datasets_fhir_stores_fhir_conditional_patch_builder(
            &self.http_client,
            &args.parent,
            &args.type_rs,
        )
        .map_err(ProviderError::Api)?;

        let task = healthcare_projects_locations_datasets_fhir_stores_fhir_conditional_patch_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Healthcare projects locations datasets fhir stores fhir conditional update.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the HttpBody result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn healthcare_projects_locations_datasets_fhir_stores_fhir_conditional_update(
        &self,
        args: &HealthcareProjectsLocationsDatasetsFhirStoresFhirConditionalUpdateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<HttpBody, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = healthcare_projects_locations_datasets_fhir_stores_fhir_conditional_update_builder(
            &self.http_client,
            &args.parent,
            &args.type_rs,
        )
        .map_err(ProviderError::Api)?;

        let task = healthcare_projects_locations_datasets_fhir_stores_fhir_conditional_update_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Healthcare projects locations datasets fhir stores fhir create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the HttpBody result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn healthcare_projects_locations_datasets_fhir_stores_fhir_create(
        &self,
        args: &HealthcareProjectsLocationsDatasetsFhirStoresFhirCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<HttpBody, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = healthcare_projects_locations_datasets_fhir_stores_fhir_create_builder(
            &self.http_client,
            &args.parent,
            &args.type_rs,
        )
        .map_err(ProviderError::Api)?;

        let task = healthcare_projects_locations_datasets_fhir_stores_fhir_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Healthcare projects locations datasets fhir stores fhir delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the HttpBody result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn healthcare_projects_locations_datasets_fhir_stores_fhir_delete(
        &self,
        args: &HealthcareProjectsLocationsDatasetsFhirStoresFhirDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<HttpBody, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = healthcare_projects_locations_datasets_fhir_stores_fhir_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = healthcare_projects_locations_datasets_fhir_stores_fhir_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Healthcare projects locations datasets fhir stores fhir execute bundle.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the HttpBody result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn healthcare_projects_locations_datasets_fhir_stores_fhir_execute_bundle(
        &self,
        args: &HealthcareProjectsLocationsDatasetsFhirStoresFhirExecuteBundleArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<HttpBody, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = healthcare_projects_locations_datasets_fhir_stores_fhir_execute_bundle_builder(
            &self.http_client,
            &args.parent,
        )
        .map_err(ProviderError::Api)?;

        let task = healthcare_projects_locations_datasets_fhir_stores_fhir_execute_bundle_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Healthcare projects locations datasets fhir stores fhir history.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the HttpBody result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn healthcare_projects_locations_datasets_fhir_stores_fhir_history(
        &self,
        args: &HealthcareProjectsLocationsDatasetsFhirStoresFhirHistoryArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<HttpBody, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = healthcare_projects_locations_datasets_fhir_stores_fhir_history_builder(
            &self.http_client,
            &args.name,
            &args._at,
            &args._count,
            &args._page_token,
            &args._since,
        )
        .map_err(ProviderError::Api)?;

        let task = healthcare_projects_locations_datasets_fhir_stores_fhir_history_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Healthcare projects locations datasets fhir stores fhir patch.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the HttpBody result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn healthcare_projects_locations_datasets_fhir_stores_fhir_patch(
        &self,
        args: &HealthcareProjectsLocationsDatasetsFhirStoresFhirPatchArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<HttpBody, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = healthcare_projects_locations_datasets_fhir_stores_fhir_patch_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = healthcare_projects_locations_datasets_fhir_stores_fhir_patch_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Healthcare projects locations datasets fhir stores fhir read.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the HttpBody result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn healthcare_projects_locations_datasets_fhir_stores_fhir_read(
        &self,
        args: &HealthcareProjectsLocationsDatasetsFhirStoresFhirReadArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<HttpBody, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = healthcare_projects_locations_datasets_fhir_stores_fhir_read_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = healthcare_projects_locations_datasets_fhir_stores_fhir_read_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Healthcare projects locations datasets fhir stores fhir search.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the HttpBody result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn healthcare_projects_locations_datasets_fhir_stores_fhir_search(
        &self,
        args: &HealthcareProjectsLocationsDatasetsFhirStoresFhirSearchArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<HttpBody, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = healthcare_projects_locations_datasets_fhir_stores_fhir_search_builder(
            &self.http_client,
            &args.parent,
            &args.resourceType,
        )
        .map_err(ProviderError::Api)?;

        let task = healthcare_projects_locations_datasets_fhir_stores_fhir_search_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Healthcare projects locations datasets fhir stores fhir search type.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the HttpBody result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn healthcare_projects_locations_datasets_fhir_stores_fhir_search_type(
        &self,
        args: &HealthcareProjectsLocationsDatasetsFhirStoresFhirSearchTypeArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<HttpBody, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = healthcare_projects_locations_datasets_fhir_stores_fhir_search_type_builder(
            &self.http_client,
            &args.parent,
            &args.resourceType,
        )
        .map_err(ProviderError::Api)?;

        let task = healthcare_projects_locations_datasets_fhir_stores_fhir_search_type_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Healthcare projects locations datasets fhir stores fhir update.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the HttpBody result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn healthcare_projects_locations_datasets_fhir_stores_fhir_update(
        &self,
        args: &HealthcareProjectsLocationsDatasetsFhirStoresFhirUpdateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<HttpBody, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = healthcare_projects_locations_datasets_fhir_stores_fhir_update_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = healthcare_projects_locations_datasets_fhir_stores_fhir_update_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Healthcare projects locations datasets fhir stores fhir vread.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the HttpBody result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn healthcare_projects_locations_datasets_fhir_stores_fhir_vread(
        &self,
        args: &HealthcareProjectsLocationsDatasetsFhirStoresFhirVreadArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<HttpBody, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = healthcare_projects_locations_datasets_fhir_stores_fhir_vread_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = healthcare_projects_locations_datasets_fhir_stores_fhir_vread_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Healthcare projects locations datasets fhir stores operations delete fhir operation.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the HttpBody result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn healthcare_projects_locations_datasets_fhir_stores_operations_delete_fhir_operation(
        &self,
        args: &HealthcareProjectsLocationsDatasetsFhirStoresOperationsDeleteFhirOperationArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<HttpBody, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = healthcare_projects_locations_datasets_fhir_stores_operations_delete_fhir_operation_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = healthcare_projects_locations_datasets_fhir_stores_operations_delete_fhir_operation_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Healthcare projects locations datasets fhir stores operations get fhir operation status.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the HttpBody result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn healthcare_projects_locations_datasets_fhir_stores_operations_get_fhir_operation_status(
        &self,
        args: &HealthcareProjectsLocationsDatasetsFhirStoresOperationsGetFhirOperationStatusArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<HttpBody, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = healthcare_projects_locations_datasets_fhir_stores_operations_get_fhir_operation_status_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = healthcare_projects_locations_datasets_fhir_stores_operations_get_fhir_operation_status_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Healthcare projects locations datasets hl7 v2 stores create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Hl7V2Store result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn healthcare_projects_locations_datasets_hl7_v2_stores_create(
        &self,
        args: &HealthcareProjectsLocationsDatasetsHl7V2StoresCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Hl7V2Store, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = healthcare_projects_locations_datasets_hl7_v2_stores_create_builder(
            &self.http_client,
            &args.parent,
            &args.hl7V2StoreId,
        )
        .map_err(ProviderError::Api)?;

        let task = healthcare_projects_locations_datasets_hl7_v2_stores_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Healthcare projects locations datasets hl7 v2 stores delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Empty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn healthcare_projects_locations_datasets_hl7_v2_stores_delete(
        &self,
        args: &HealthcareProjectsLocationsDatasetsHl7V2StoresDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Empty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = healthcare_projects_locations_datasets_hl7_v2_stores_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = healthcare_projects_locations_datasets_hl7_v2_stores_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Healthcare projects locations datasets hl7 v2 stores export.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn healthcare_projects_locations_datasets_hl7_v2_stores_export(
        &self,
        args: &HealthcareProjectsLocationsDatasetsHl7V2StoresExportArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = healthcare_projects_locations_datasets_hl7_v2_stores_export_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = healthcare_projects_locations_datasets_hl7_v2_stores_export_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Healthcare projects locations datasets hl7 v2 stores get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Hl7V2Store result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn healthcare_projects_locations_datasets_hl7_v2_stores_get(
        &self,
        args: &HealthcareProjectsLocationsDatasetsHl7V2StoresGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Hl7V2Store, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = healthcare_projects_locations_datasets_hl7_v2_stores_get_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = healthcare_projects_locations_datasets_hl7_v2_stores_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Healthcare projects locations datasets hl7 v2 stores get h l7 v2 store metrics.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Hl7V2StoreMetrics result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn healthcare_projects_locations_datasets_hl7_v2_stores_get_h_l7_v2_store_metrics(
        &self,
        args: &HealthcareProjectsLocationsDatasetsHl7V2StoresGetHL7V2StoreMetricsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Hl7V2StoreMetrics, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = healthcare_projects_locations_datasets_hl7_v2_stores_get_h_l7_v2_store_metrics_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = healthcare_projects_locations_datasets_hl7_v2_stores_get_h_l7_v2_store_metrics_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Healthcare projects locations datasets hl7 v2 stores get iam policy.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Policy result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn healthcare_projects_locations_datasets_hl7_v2_stores_get_iam_policy(
        &self,
        args: &HealthcareProjectsLocationsDatasetsHl7V2StoresGetIamPolicyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Policy, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = healthcare_projects_locations_datasets_hl7_v2_stores_get_iam_policy_builder(
            &self.http_client,
            &args.resource,
            &args.options.requestedPolicyVersion,
        )
        .map_err(ProviderError::Api)?;

        let task = healthcare_projects_locations_datasets_hl7_v2_stores_get_iam_policy_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Healthcare projects locations datasets hl7 v2 stores import.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn healthcare_projects_locations_datasets_hl7_v2_stores_import(
        &self,
        args: &HealthcareProjectsLocationsDatasetsHl7V2StoresImportArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = healthcare_projects_locations_datasets_hl7_v2_stores_import_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = healthcare_projects_locations_datasets_hl7_v2_stores_import_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Healthcare projects locations datasets hl7 v2 stores list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ListHl7V2StoresResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn healthcare_projects_locations_datasets_hl7_v2_stores_list(
        &self,
        args: &HealthcareProjectsLocationsDatasetsHl7V2StoresListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ListHl7V2StoresResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = healthcare_projects_locations_datasets_hl7_v2_stores_list_builder(
            &self.http_client,
            &args.parent,
            &args.filter,
            &args.pageSize,
            &args.pageToken,
        )
        .map_err(ProviderError::Api)?;

        let task = healthcare_projects_locations_datasets_hl7_v2_stores_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Healthcare projects locations datasets hl7 v2 stores patch.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Hl7V2Store result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn healthcare_projects_locations_datasets_hl7_v2_stores_patch(
        &self,
        args: &HealthcareProjectsLocationsDatasetsHl7V2StoresPatchArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Hl7V2Store, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = healthcare_projects_locations_datasets_hl7_v2_stores_patch_builder(
            &self.http_client,
            &args.name,
            &args.updateMask,
        )
        .map_err(ProviderError::Api)?;

        let task = healthcare_projects_locations_datasets_hl7_v2_stores_patch_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Healthcare projects locations datasets hl7 v2 stores rollback.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn healthcare_projects_locations_datasets_hl7_v2_stores_rollback(
        &self,
        args: &HealthcareProjectsLocationsDatasetsHl7V2StoresRollbackArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = healthcare_projects_locations_datasets_hl7_v2_stores_rollback_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = healthcare_projects_locations_datasets_hl7_v2_stores_rollback_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Healthcare projects locations datasets hl7 v2 stores set iam policy.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Policy result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn healthcare_projects_locations_datasets_hl7_v2_stores_set_iam_policy(
        &self,
        args: &HealthcareProjectsLocationsDatasetsHl7V2StoresSetIamPolicyArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Policy, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = healthcare_projects_locations_datasets_hl7_v2_stores_set_iam_policy_builder(
            &self.http_client,
            &args.resource,
        )
        .map_err(ProviderError::Api)?;

        let task = healthcare_projects_locations_datasets_hl7_v2_stores_set_iam_policy_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Healthcare projects locations datasets hl7 v2 stores test iam permissions.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the TestIamPermissionsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn healthcare_projects_locations_datasets_hl7_v2_stores_test_iam_permissions(
        &self,
        args: &HealthcareProjectsLocationsDatasetsHl7V2StoresTestIamPermissionsArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<TestIamPermissionsResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = healthcare_projects_locations_datasets_hl7_v2_stores_test_iam_permissions_builder(
            &self.http_client,
            &args.resource,
        )
        .map_err(ProviderError::Api)?;

        let task = healthcare_projects_locations_datasets_hl7_v2_stores_test_iam_permissions_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Healthcare projects locations datasets hl7 v2 stores messages create.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Message result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn healthcare_projects_locations_datasets_hl7_v2_stores_messages_create(
        &self,
        args: &HealthcareProjectsLocationsDatasetsHl7V2StoresMessagesCreateArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Message, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = healthcare_projects_locations_datasets_hl7_v2_stores_messages_create_builder(
            &self.http_client,
            &args.parent,
        )
        .map_err(ProviderError::Api)?;

        let task = healthcare_projects_locations_datasets_hl7_v2_stores_messages_create_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Healthcare projects locations datasets hl7 v2 stores messages delete.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Empty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn healthcare_projects_locations_datasets_hl7_v2_stores_messages_delete(
        &self,
        args: &HealthcareProjectsLocationsDatasetsHl7V2StoresMessagesDeleteArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Empty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = healthcare_projects_locations_datasets_hl7_v2_stores_messages_delete_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = healthcare_projects_locations_datasets_hl7_v2_stores_messages_delete_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Healthcare projects locations datasets hl7 v2 stores messages get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Message result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn healthcare_projects_locations_datasets_hl7_v2_stores_messages_get(
        &self,
        args: &HealthcareProjectsLocationsDatasetsHl7V2StoresMessagesGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Message, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = healthcare_projects_locations_datasets_hl7_v2_stores_messages_get_builder(
            &self.http_client,
            &args.name,
            &args.view,
        )
        .map_err(ProviderError::Api)?;

        let task = healthcare_projects_locations_datasets_hl7_v2_stores_messages_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Healthcare projects locations datasets hl7 v2 stores messages ingest.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the IngestMessageResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn healthcare_projects_locations_datasets_hl7_v2_stores_messages_ingest(
        &self,
        args: &HealthcareProjectsLocationsDatasetsHl7V2StoresMessagesIngestArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<IngestMessageResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = healthcare_projects_locations_datasets_hl7_v2_stores_messages_ingest_builder(
            &self.http_client,
            &args.parent,
        )
        .map_err(ProviderError::Api)?;

        let task = healthcare_projects_locations_datasets_hl7_v2_stores_messages_ingest_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Healthcare projects locations datasets hl7 v2 stores messages list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ListMessagesResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn healthcare_projects_locations_datasets_hl7_v2_stores_messages_list(
        &self,
        args: &HealthcareProjectsLocationsDatasetsHl7V2StoresMessagesListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ListMessagesResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = healthcare_projects_locations_datasets_hl7_v2_stores_messages_list_builder(
            &self.http_client,
            &args.parent,
            &args.filter,
            &args.orderBy,
            &args.pageSize,
            &args.pageToken,
            &args.view,
        )
        .map_err(ProviderError::Api)?;

        let task = healthcare_projects_locations_datasets_hl7_v2_stores_messages_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Healthcare projects locations datasets hl7 v2 stores messages patch.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Message result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn healthcare_projects_locations_datasets_hl7_v2_stores_messages_patch(
        &self,
        args: &HealthcareProjectsLocationsDatasetsHl7V2StoresMessagesPatchArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Message, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = healthcare_projects_locations_datasets_hl7_v2_stores_messages_patch_builder(
            &self.http_client,
            &args.name,
            &args.updateMask,
        )
        .map_err(ProviderError::Api)?;

        let task = healthcare_projects_locations_datasets_hl7_v2_stores_messages_patch_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Healthcare projects locations datasets operations cancel.
    ///
    /// Automatically stores the result in the state store on success.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Empty result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request or state storage fails.
    pub fn healthcare_projects_locations_datasets_operations_cancel(
        &self,
        args: &HealthcareProjectsLocationsDatasetsOperationsCancelArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Empty, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = healthcare_projects_locations_datasets_operations_cancel_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = healthcare_projects_locations_datasets_operations_cancel_task(builder)
            .map_err(ProviderError::Api)?;

        let state_store = self.client.state_store.clone();
        let stage = Some(self.client.stage.clone());

        let store_task = StoreStateIdentifierTask::new(task, state_store, args, stage);

        execute(store_task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Healthcare projects locations datasets operations get.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the Operation result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn healthcare_projects_locations_datasets_operations_get(
        &self,
        args: &HealthcareProjectsLocationsDatasetsOperationsGetArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<Operation, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = healthcare_projects_locations_datasets_operations_get_builder(
            &self.http_client,
            &args.name,
        )
        .map_err(ProviderError::Api)?;

        let task = healthcare_projects_locations_datasets_operations_get_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Healthcare projects locations datasets operations list.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the ListOperationsResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn healthcare_projects_locations_datasets_operations_list(
        &self,
        args: &HealthcareProjectsLocationsDatasetsOperationsListArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<ListOperationsResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = healthcare_projects_locations_datasets_operations_list_builder(
            &self.http_client,
            &args.name,
            &args.filter,
            &args.pageSize,
            &args.pageToken,
            &args.returnPartialSuccess,
        )
        .map_err(ProviderError::Api)?;

        let task = healthcare_projects_locations_datasets_operations_list_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

    /// Healthcare projects locations services nlp analyze entities.
    ///
    /// Read-only operation - no state tracking.
    ///
    /// # Arguments
    ///
    /// * `args` - Request arguments
    ///
    /// # Returns
    ///
    /// StreamIterator yielding the AnalyzeEntitiesResponse result.
    ///
    /// # Errors
    ///
    /// Returns ProviderError if the API request fails.
    pub fn healthcare_projects_locations_services_nlp_analyze_entities(
        &self,
        args: &HealthcareProjectsLocationsServicesNlpAnalyzeEntitiesArgs,
    ) -> Result<
        impl StreamIterator<
            D = Result<AnalyzeEntitiesResponse, ProviderError<ApiError>>,
            P = crate::providers::gcp::clients::types::ApiPending,
        > + Send
        + 'static,
        ProviderError<ApiError>,
    > {
        let builder = healthcare_projects_locations_services_nlp_analyze_entities_builder(
            &self.http_client,
            &args.nlpService,
        )
        .map_err(ProviderError::Api)?;

        let task = healthcare_projects_locations_services_nlp_analyze_entities_task(builder)
            .map_err(ProviderError::Api)?;

        execute(task, None).map_err(|e: String| ProviderError::ExecuteFailed(e.to_string()))
    }

}
