//! Auto-generated API module for gcp/searchconsole searchconsole.
//!
//! Generated by `cargo run --bin ewe_platform gen_api`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp_searchconsole_searchconsole `

#![cfg(feature = "gcp_searchconsole_searchconsole")]
#![allow(clippy::too_many_arguments, clippy::type_complexity)]
#![allow(
    clippy::missing_errors_doc,
    clippy::doc_markdown,
    clippy::useless_format
)]
#![allow(unused_imports)]

use foundation_core::valtron::{TaskIterator, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{ClientRequestBuilder, SimpleHttpClient};
use foundation_macros::JsonHash;
use serde::{Deserialize, Serialize};

use super::shared::ApiResponse;

// =============================================================================
// TYPE DECLARATIONS
// =============================================================================

/// `RichResultsInspectionResult` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct RichResultsInspectionResult {
    /// detectedItems property.
    pub detected_items: Option<Vec<DetectedItems>>,
    /// verdict property.
    pub verdict: Option<String>,
}

/// `ApiDimensionFilter` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ApiDimensionFilter {
    /// dimension property.
    pub dimension: Option<String>,
    /// expression property.
    pub expression: Option<String>,
    /// operator property.
    pub operator: Option<String>,
}

/// `InspectUrlIndexRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InspectUrlIndexRequest {
    /// inspectionUrl property.
    pub inspection_url: Option<String>,
    /// languageCode property.
    pub language_code: Option<String>,
    /// siteUrl property.
    pub site_url: Option<String>,
}

/// `TestStatus` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct TestStatus {
    /// details property.
    pub details: Option<String>,
    /// status property.
    pub status: Option<String>,
}

/// `DetectedItems` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct DetectedItems {
    /// items property.
    pub items: Option<Vec<Item>>,
    /// richResultType property.
    pub rich_result_type: Option<String>,
}

/// `SearchAnalyticsQueryRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SearchAnalyticsQueryRequest {
    /// aggregationType property.
    pub aggregation_type: Option<String>,
    /// dataState property.
    pub data_state: Option<String>,
    /// dimensionFilterGroups property.
    pub dimension_filter_groups: Option<Vec<ApiDimensionFilterGroup>>,
    /// dimensions property.
    pub dimensions: Option<Vec<String>>,
    /// endDate property.
    pub end_date: Option<String>,
    /// rowLimit property.
    pub row_limit: Option<i64>,
    /// searchType property.
    pub search_type: Option<String>,
    /// startDate property.
    pub start_date: Option<String>,
    /// startRow property.
    pub start_row: Option<i64>,
    /// type property.
    pub r#type: Option<String>,
}

/// `RunMobileFriendlyTestResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct RunMobileFriendlyTestResponse {
    /// mobileFriendliness property.
    pub mobile_friendliness: Option<String>,
    /// mobileFriendlyIssues property.
    pub mobile_friendly_issues: Option<Vec<MobileFriendlyIssue>>,
    /// resourceIssues property.
    pub resource_issues: Option<Vec<ResourceIssue>>,
    /// screenshot property.
    pub screenshot: Option<Image>,
    /// testStatus property.
    pub test_status: Option<TestStatus>,
}

/// `MobileUsabilityIssue` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct MobileUsabilityIssue {
    /// issueType property.
    pub issue_type: Option<String>,
    /// message property.
    pub message: Option<String>,
    /// severity property.
    pub severity: Option<String>,
}

/// `WmxSitemap` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct WmxSitemap {
    /// contents property.
    pub contents: Option<Vec<WmxSitemapContent>>,
    /// errors property.
    pub errors: Option<String>,
    /// isPending property.
    pub is_pending: Option<bool>,
    /// isSitemapsIndex property.
    pub is_sitemaps_index: Option<bool>,
    /// lastDownloaded property.
    pub last_downloaded: Option<String>,
    /// lastSubmitted property.
    pub last_submitted: Option<String>,
    /// path property.
    pub path: Option<String>,
    /// type property.
    pub r#type: Option<String>,
    /// warnings property.
    pub warnings: Option<String>,
}

/// `MobileUsabilityInspectionResult` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct MobileUsabilityInspectionResult {
    /// issues property.
    pub issues: Option<Vec<MobileUsabilityIssue>>,
    /// verdict property.
    pub verdict: Option<String>,
}

/// `RichResultsIssue` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct RichResultsIssue {
    /// issueMessage property.
    pub issue_message: Option<String>,
    /// severity property.
    pub severity: Option<String>,
}

/// `MobileFriendlyIssue` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct MobileFriendlyIssue {
    /// rule property.
    pub rule: Option<String>,
}

/// `SitemapsListResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SitemapsListResponse {
    /// sitemap property.
    pub sitemap: Option<Vec<WmxSitemap>>,
}

/// `WmxSitemapContent` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct WmxSitemapContent {
    /// indexed property.
    pub indexed: Option<String>,
    /// submitted property.
    pub submitted: Option<String>,
    /// type property.
    pub r#type: Option<String>,
}

/// `ApiDimensionFilterGroup` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ApiDimensionFilterGroup {
    /// filters property.
    pub filters: Option<Vec<ApiDimensionFilter>>,
    /// groupType property.
    pub group_type: Option<String>,
}

/// `IndexStatusInspectionResult` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct IndexStatusInspectionResult {
    /// coverageState property.
    pub coverage_state: Option<String>,
    /// crawledAs property.
    pub crawled_as: Option<String>,
    /// googleCanonical property.
    pub google_canonical: Option<String>,
    /// indexingState property.
    pub indexing_state: Option<String>,
    /// lastCrawlTime property.
    pub last_crawl_time: Option<String>,
    /// pageFetchState property.
    pub page_fetch_state: Option<String>,
    /// referringUrls property.
    pub referring_urls: Option<Vec<String>>,
    /// robotsTxtState property.
    pub robots_txt_state: Option<String>,
    /// sitemap property.
    pub sitemap: Option<Vec<String>>,
    /// userCanonical property.
    pub user_canonical: Option<String>,
    /// verdict property.
    pub verdict: Option<String>,
}

/// `Item` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Item {
    /// issues property.
    pub issues: Option<Vec<RichResultsIssue>>,
    /// name property.
    pub name: Option<String>,
}

/// `Image` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Image {
    /// data property.
    pub data: Option<String>,
    /// mimeType property.
    pub mime_type: Option<String>,
}

/// `ResourceIssue` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ResourceIssue {
    /// blockedResource property.
    pub blocked_resource: Option<BlockedResource>,
}

/// `UrlInspectionResult` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct UrlInspectionResult {
    /// ampResult property.
    pub amp_result: Option<AmpInspectionResult>,
    /// indexStatusResult property.
    pub index_status_result: Option<IndexStatusInspectionResult>,
    /// inspectionResultLink property.
    pub inspection_result_link: Option<String>,
    /// mobileUsabilityResult property.
    pub mobile_usability_result: Option<MobileUsabilityInspectionResult>,
    /// richResultsResult property.
    pub rich_results_result: Option<RichResultsInspectionResult>,
}

/// `SitesListResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SitesListResponse {
    /// siteEntry property.
    pub site_entry: Option<Vec<WmxSite>>,
}

/// `AmpInspectionResult` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AmpInspectionResult {
    /// ampIndexStatusVerdict property.
    pub amp_index_status_verdict: Option<String>,
    /// ampUrl property.
    pub amp_url: Option<String>,
    /// indexingState property.
    pub indexing_state: Option<String>,
    /// issues property.
    pub issues: Option<Vec<AmpIssue>>,
    /// lastCrawlTime property.
    pub last_crawl_time: Option<String>,
    /// pageFetchState property.
    pub page_fetch_state: Option<String>,
    /// robotsTxtState property.
    pub robots_txt_state: Option<String>,
    /// verdict property.
    pub verdict: Option<String>,
}

/// `AmpIssue` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct AmpIssue {
    /// issueMessage property.
    pub issue_message: Option<String>,
    /// severity property.
    pub severity: Option<String>,
}

/// `ApiDataRow` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ApiDataRow {
    /// clicks property.
    pub clicks: Option<f64>,
    /// ctr property.
    pub ctr: Option<f64>,
    /// impressions property.
    pub impressions: Option<f64>,
    /// keys property.
    pub keys: Option<Vec<String>>,
    /// position property.
    pub position: Option<f64>,
}

/// `BlockedResource` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct BlockedResource {
    /// url property.
    pub url: Option<String>,
}

/// `RunMobileFriendlyTestRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct RunMobileFriendlyTestRequest {
    /// requestScreenshot property.
    pub request_screenshot: Option<bool>,
    /// url property.
    pub url: Option<String>,
}

/// `InspectUrlIndexResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct InspectUrlIndexResponse {
    /// inspectionResult property.
    pub inspection_result: Option<UrlInspectionResult>,
}

/// `SearchAnalyticsQueryResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SearchAnalyticsQueryResponse {
    /// metadata property.
    pub metadata: Option<Metadata>,
    /// responseAggregationType property.
    pub response_aggregation_type: Option<String>,
    /// rows property.
    pub rows: Option<Vec<ApiDataRow>>,
}

/// `WmxSite` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct WmxSite {
    /// permissionLevel property.
    pub permission_level: Option<String>,
    /// siteUrl property.
    pub site_url: Option<String>,
}

/// `Metadata` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Metadata {
    /// firstIncompleteDate property.
    pub first_incomplete_date: Option<String>,
    /// firstIncompleteHour property.
    pub first_incomplete_hour: Option<String>,
}

// =============================================================================
// ARGS TYPES (per-endpoint)
// =============================================================================

/// Arguments for [`webmasters.searchanalytics.query_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct WebmastersSearchanalyticsQueryArgs {
    /// Path parameter: `siteUrl`.
    pub site_url: String,
    /// Request body.
    pub body: SearchAnalyticsQueryRequest,
}

/// Arguments for [`webmasters.sitemaps.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct WebmastersSitemapsDeleteArgs {
    /// Path parameter: `siteUrl`.
    pub site_url: String,
    /// Path parameter: `feedpath`.
    pub feedpath: String,
}

/// Arguments for [`webmasters.sitemaps.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct WebmastersSitemapsGetArgs {
    /// Path parameter: `siteUrl`.
    pub site_url: String,
    /// Path parameter: `feedpath`.
    pub feedpath: String,
}

/// Arguments for [`webmasters.sitemaps.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct WebmastersSitemapsListArgs {
    /// Path parameter: `siteUrl`.
    pub site_url: String,
    /// Query parameter: `sitemapIndex`.
    pub sitemap_index: Option<String>,
}

/// Arguments for [`webmasters.sitemaps.submit_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct WebmastersSitemapsSubmitArgs {
    /// Path parameter: `siteUrl`.
    pub site_url: String,
    /// Path parameter: `feedpath`.
    pub feedpath: String,
}

/// Arguments for [`webmasters.sites.add_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct WebmastersSitesAddArgs {
    /// Path parameter: `siteUrl`.
    pub site_url: String,
}

/// Arguments for [`webmasters.sites.delete_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct WebmastersSitesDeleteArgs {
    /// Path parameter: `siteUrl`.
    pub site_url: String,
}

/// Arguments for [`webmasters.sites.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct WebmastersSitesGetArgs {
    /// Path parameter: `siteUrl`.
    pub site_url: String,
}

/// Arguments for [`searchconsole.urlInspection.index.inspect_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct SearchconsoleUrlInspectionIndexInspectArgs {
    /// Request body.
    pub body: InspectUrlIndexRequest,
}

/// Arguments for [`searchconsole.urlTestingTools.mobileFriendlyTest.run_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct SearchconsoleUrlTestingToolsMobileFriendlyTestRunArgs {
    /// Request body.
    pub body: RunMobileFriendlyTestRequest,
}

// =============================================================================
// CLIENT FUNCTIONS (per-endpoint)
// =============================================================================

// -----------------------------------------------------------------------------
// POST webmasters/v3/sites/{siteUrl}/searchAnalytics/query
// -----------------------------------------------------------------------------

/// POST webmasters/v3/sites/{siteUrl}/searchAnalytics/query.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = webmasters_searchanalytics_query_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn webmasters_searchanalytics_query_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &WebmastersSearchanalyticsQueryArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SearchAnalyticsQueryResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://searchconsole.googleapis.com/webmasters/v3/sites/{}/searchAnalytics/query",
        args.site_url,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: SearchAnalyticsQueryResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE webmasters/v3/sites/{siteUrl}/sitemaps/{feedpath}
// -----------------------------------------------------------------------------

/// DELETE webmasters/v3/sites/{siteUrl}/sitemaps/{feedpath}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = webmasters_sitemaps_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn webmasters_sitemaps_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &WebmastersSitemapsDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://searchconsole.googleapis.com/webmasters/v3/sites/{}/sitemaps/{}",
        args.site_url, args.feedpath,
    );

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream: _,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET webmasters/v3/sites/{siteUrl}/sitemaps/{feedpath}
// -----------------------------------------------------------------------------

/// GET webmasters/v3/sites/{siteUrl}/sitemaps/{feedpath}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = webmasters_sitemaps_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn webmasters_sitemaps_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &WebmastersSitemapsGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<WmxSitemap>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://searchconsole.googleapis.com/webmasters/v3/sites/{}/sitemaps/{}",
        args.site_url, args.feedpath,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: WmxSitemap =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET webmasters/v3/sites/{siteUrl}/sitemaps
// -----------------------------------------------------------------------------

/// GET webmasters/v3/sites/{siteUrl}/sitemaps.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = webmasters_sitemaps_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn webmasters_sitemaps_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &WebmastersSitemapsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SitemapsListResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://searchconsole.googleapis.com/webmasters/v3/sites/{}/sitemaps",
        args.site_url,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.sitemap_index {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("sitemapIndex=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: SitemapsListResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PUT webmasters/v3/sites/{siteUrl}/sitemaps/{feedpath}
// -----------------------------------------------------------------------------

/// PUT webmasters/v3/sites/{siteUrl}/sitemaps/{feedpath}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = webmasters_sitemaps_submit_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn webmasters_sitemaps_submit_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &WebmastersSitemapsSubmitArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://searchconsole.googleapis.com/webmasters/v3/sites/{}/sitemaps/{}",
        args.site_url, args.feedpath,
    );

    let mut builder = client
        .put(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream: _,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PUT webmasters/v3/sites/{siteUrl}
// -----------------------------------------------------------------------------

/// PUT webmasters/v3/sites/{siteUrl}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = webmasters_sites_add_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn webmasters_sites_add_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &WebmastersSitesAddArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://searchconsole.googleapis.com/webmasters/v3/sites/{}",
        args.site_url,
    );

    let mut builder = client
        .put(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream: _,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE webmasters/v3/sites/{siteUrl}
// -----------------------------------------------------------------------------

/// DELETE webmasters/v3/sites/{siteUrl}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = webmasters_sites_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn webmasters_sites_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &WebmastersSitesDeleteArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://searchconsole.googleapis.com/webmasters/v3/sites/{}",
        args.site_url,
    );

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream: _,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET webmasters/v3/sites/{siteUrl}
// -----------------------------------------------------------------------------

/// GET webmasters/v3/sites/{siteUrl}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = webmasters_sites_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn webmasters_sites_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &WebmastersSitesGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<WmxSite>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://searchconsole.googleapis.com/webmasters/v3/sites/{}",
        args.site_url,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: WmxSite =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET webmasters/v3/sites
// -----------------------------------------------------------------------------

/// GET webmasters/v3/sites.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = webmasters_sites_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn webmasters_sites_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &WebmastersSitesListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SitesListResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://searchconsole.googleapis.com/webmasters/v3/sites",);

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: SitesListResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/urlInspection/index:inspect
// -----------------------------------------------------------------------------

/// POST v1/urlInspection/index:inspect.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = searchconsole_url_inspection_index_inspect_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn searchconsole_url_inspection_index_inspect_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &SearchconsoleUrlInspectionIndexInspectArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<InspectUrlIndexResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url =
        format!("https://searchconsole.googleapis.com/v1/urlInspection/index:inspect",);

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: InspectUrlIndexResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/urlTestingTools/mobileFriendlyTest:run
// -----------------------------------------------------------------------------

/// POST v1/urlTestingTools/mobileFriendlyTest:run.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = searchconsole_url_testing_tools_mobile_friendly_test_run_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn searchconsole_url_testing_tools_mobile_friendly_test_run_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &SearchconsoleUrlTestingToolsMobileFriendlyTestRunArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<RunMobileFriendlyTestResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url =
        format!("https://searchconsole.googleapis.com/v1/urlTestingTools/mobileFriendlyTest:run",);

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: RunMobileFriendlyTestResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}
