//! Auto-generated API module for gcp/vision images:annotate.
//!
//! Generated by `cargo run --bin ewe_platform gen_api`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp_vision_images_annotate `

#![cfg(feature = "gcp_vision_images_annotate")]
#![allow(clippy::too_many_arguments, clippy::type_complexity)]
#![allow(
    clippy::missing_errors_doc,
    clippy::doc_markdown,
    clippy::useless_format
)]
#![allow(unused_imports)]

use foundation_core::valtron::{TaskIterator, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{ClientRequestBuilder, SimpleHttpClient};
use foundation_macros::JsonHash;
use serde::{Deserialize, Serialize};

use super::shared::ApiResponse;

// =============================================================================
// TYPE DECLARATIONS
// =============================================================================

/// `CropHint` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct CropHint {
    /// boundingPoly property.
    pub bounding_poly: Option<BoundingPoly>,
    /// confidence property.
    pub confidence: Option<f64>,
    /// importanceFraction property.
    pub importance_fraction: Option<f64>,
}

/// `WebDetection` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct WebDetection {
    /// bestGuessLabels property.
    pub best_guess_labels: Option<Vec<WebLabel>>,
    /// fullMatchingImages property.
    pub full_matching_images: Option<Vec<WebImage>>,
    /// pagesWithMatchingImages property.
    pub pages_with_matching_images: Option<Vec<WebPage>>,
    /// partialMatchingImages property.
    pub partial_matching_images: Option<Vec<WebImage>>,
    /// visuallySimilarImages property.
    pub visually_similar_images: Option<Vec<WebImage>>,
    /// webEntities property.
    pub web_entities: Option<Vec<WebEntity>>,
}

/// `LocalizedObjectAnnotation` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct LocalizedObjectAnnotation {
    /// boundingPoly property.
    pub bounding_poly: Option<BoundingPoly>,
    /// languageCode property.
    pub language_code: Option<String>,
    /// mid property.
    pub mid: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// score property.
    pub score: Option<f64>,
}

/// `ImageAnnotationContext` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ImageAnnotationContext {
    /// pageNumber property.
    pub page_number: Option<i64>,
    /// uri property.
    pub uri: Option<String>,
}

/// `ResultType` response type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ResultType {
    /// Raw JSON value - full schema generated from `OpenAPI`
    #[serde(flatten)]
    pub data: std::collections::HashMap<String, serde_json::Value>,
}

/// `Product` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Product {
    /// description property.
    pub description: Option<String>,
    /// displayName property.
    pub display_name: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// productCategory property.
    pub product_category: Option<String>,
    /// productLabels property.
    pub product_labels: Option<Vec<KeyValue>>,
}

/// `WebPage` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct WebPage {
    /// fullMatchingImages property.
    pub full_matching_images: Option<Vec<WebImage>>,
    /// pageTitle property.
    pub page_title: Option<String>,
    /// partialMatchingImages property.
    pub partial_matching_images: Option<Vec<WebImage>>,
    /// score property.
    pub score: Option<f64>,
    /// url property.
    pub url: Option<String>,
}

/// `KeyValue` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct KeyValue {
    /// key property.
    pub key: Option<String>,
    /// value property.
    pub value: Option<String>,
}

/// `Vertex` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Vertex {
    /// x property.
    pub x: Option<i64>,
    /// y property.
    pub y: Option<i64>,
}

/// `Color` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Color {
    /// alpha property.
    pub alpha: Option<f64>,
    /// blue property.
    pub blue: Option<f64>,
    /// green property.
    pub green: Option<f64>,
    /// red property.
    pub red: Option<f64>,
}

/// `SafeSearchAnnotation` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct SafeSearchAnnotation {
    /// adult property.
    pub adult: Option<String>,
    /// medical property.
    pub medical: Option<String>,
    /// racy property.
    pub racy: Option<String>,
    /// spoof property.
    pub spoof: Option<String>,
    /// violence property.
    pub violence: Option<String>,
}

/// `Block` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Block {
    /// blockType property.
    pub block_type: Option<String>,
    /// boundingBox property.
    pub bounding_box: Option<BoundingPoly>,
    /// confidence property.
    pub confidence: Option<f64>,
    /// paragraphs property.
    pub paragraphs: Option<Vec<Paragraph>>,
    /// property property.
    pub property: Option<TextProperty>,
}

/// `Word` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Word {
    /// boundingBox property.
    pub bounding_box: Option<BoundingPoly>,
    /// confidence property.
    pub confidence: Option<f64>,
    /// property property.
    pub property: Option<TextProperty>,
    /// symbols property.
    pub symbols: Option<Vec<Symbol>>,
}

/// `Landmark` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Landmark {
    /// position property.
    pub position: Option<Position>,
    /// type property.
    pub r#type: Option<String>,
}

/// `Position` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Position {
    /// x property.
    pub x: Option<f64>,
    /// y property.
    pub y: Option<f64>,
    /// z property.
    pub z: Option<f64>,
}

/// `Paragraph` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Paragraph {
    /// boundingBox property.
    pub bounding_box: Option<BoundingPoly>,
    /// confidence property.
    pub confidence: Option<f64>,
    /// property property.
    pub property: Option<TextProperty>,
    /// words property.
    pub words: Option<Vec<Word>>,
}

/// `GroupedResult` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct GroupedResult {
    /// boundingPoly property.
    pub bounding_poly: Option<BoundingPoly>,
    /// objectAnnotations property.
    pub object_annotations: Option<Vec<ObjectAnnotation>>,
    /// results property.
    pub results: Option<Vec<ResultType>>,
}

/// `TextAnnotation` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct TextAnnotation {
    /// pages property.
    pub pages: Option<Vec<Page>>,
    /// text property.
    pub text: Option<String>,
}

/// `DominantColorsAnnotation` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct DominantColorsAnnotation {
    /// colors property.
    pub colors: Option<Vec<ColorInfo>>,
}

/// `NormalizedVertex` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct NormalizedVertex {
    /// x property.
    pub x: Option<f64>,
    /// y property.
    pub y: Option<f64>,
}

/// `WebLabel` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct WebLabel {
    /// label property.
    pub label: Option<String>,
    /// languageCode property.
    pub language_code: Option<String>,
}

/// `DetectedLanguage` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct DetectedLanguage {
    /// confidence property.
    pub confidence: Option<f64>,
    /// languageCode property.
    pub language_code: Option<String>,
}

/// `ObjectAnnotation` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ObjectAnnotation {
    /// languageCode property.
    pub language_code: Option<String>,
    /// mid property.
    pub mid: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// score property.
    pub score: Option<f64>,
}

/// `Property` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Property {
    /// name property.
    pub name: Option<String>,
    /// uint64Value property.
    pub uint64_value: Option<String>,
    /// value property.
    pub value: Option<String>,
}

/// `LocationInfo` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct LocationInfo {
    /// latLng property.
    pub lat_lng: Option<LatLng>,
}

/// `ImageProperties` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ImageProperties {
    /// dominantColors property.
    pub dominant_colors: Option<DominantColorsAnnotation>,
}

/// `BoundingPoly` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct BoundingPoly {
    /// normalizedVertices property.
    pub normalized_vertices: Option<Vec<NormalizedVertex>>,
    /// vertices property.
    pub vertices: Option<Vec<Vertex>>,
}

/// `ColorInfo` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ColorInfo {
    /// color property.
    pub color: Option<Color>,
    /// pixelFraction property.
    pub pixel_fraction: Option<f64>,
    /// score property.
    pub score: Option<f64>,
}

/// `AnnotateImageResponse` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct AnnotateImageResponse {
    /// context property.
    pub context: Option<ImageAnnotationContext>,
    /// cropHintsAnnotation property.
    pub crop_hints_annotation: Option<CropHintsAnnotation>,
    /// error property.
    pub error: Option<Status>,
    /// faceAnnotations property.
    pub face_annotations: Option<Vec<FaceAnnotation>>,
    /// fullTextAnnotation property.
    pub full_text_annotation: Option<TextAnnotation>,
    /// imagePropertiesAnnotation property.
    pub image_properties_annotation: Option<ImageProperties>,
    /// labelAnnotations property.
    pub label_annotations: Option<Vec<EntityAnnotation>>,
    /// landmarkAnnotations property.
    pub landmark_annotations: Option<Vec<EntityAnnotation>>,
    /// localizedObjectAnnotations property.
    pub localized_object_annotations: Option<Vec<LocalizedObjectAnnotation>>,
    /// logoAnnotations property.
    pub logo_annotations: Option<Vec<EntityAnnotation>>,
    /// productSearchResults property.
    pub product_search_results: Option<ProductSearchResults>,
    /// safeSearchAnnotation property.
    pub safe_search_annotation: Option<SafeSearchAnnotation>,
    /// textAnnotations property.
    pub text_annotations: Option<Vec<EntityAnnotation>>,
    /// webDetection property.
    pub web_detection: Option<WebDetection>,
}

/// `WebImage` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct WebImage {
    /// score property.
    pub score: Option<f64>,
    /// url property.
    pub url: Option<String>,
}

/// `TextProperty` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct TextProperty {
    /// detectedBreak property.
    pub detected_break: Option<DetectedBreak>,
    /// detectedLanguages property.
    pub detected_languages: Option<Vec<DetectedLanguage>>,
}

/// `ProductSearchResults` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ProductSearchResults {
    /// indexTime property.
    pub index_time: Option<String>,
    /// productGroupedResults property.
    pub product_grouped_results: Option<Vec<GroupedResult>>,
    /// results property.
    pub results: Option<Vec<ResultType>>,
}

/// `EntityAnnotation` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct EntityAnnotation {
    /// boundingPoly property.
    pub bounding_poly: Option<BoundingPoly>,
    /// confidence property.
    pub confidence: Option<f64>,
    /// description property.
    pub description: Option<String>,
    /// locale property.
    pub locale: Option<String>,
    /// locations property.
    pub locations: Option<Vec<LocationInfo>>,
    /// mid property.
    pub mid: Option<String>,
    /// properties property.
    pub properties: Option<Vec<Property>>,
    /// score property.
    pub score: Option<f64>,
    /// topicality property.
    pub topicality: Option<f64>,
}

/// `LatLng` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct LatLng {
    /// latitude property.
    pub latitude: Option<f64>,
    /// longitude property.
    pub longitude: Option<f64>,
}

/// `DetectedBreak` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct DetectedBreak {
    /// isPrefix property.
    pub is_prefix: Option<bool>,
    /// type property.
    pub r#type: Option<String>,
}

/// `Status` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Status {
    /// code property.
    pub code: Option<i64>,
    /// details property.
    pub details: Option<Vec<serde_json::Value>>,
    /// message property.
    pub message: Option<String>,
}

/// `BatchAnnotateImagesResponse` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct BatchAnnotateImagesResponse {
    /// responses property.
    pub responses: Option<Vec<AnnotateImageResponse>>,
}

/// `FaceAnnotation` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct FaceAnnotation {
    /// angerLikelihood property.
    pub anger_likelihood: Option<String>,
    /// blurredLikelihood property.
    pub blurred_likelihood: Option<String>,
    /// boundingPoly property.
    pub bounding_poly: Option<BoundingPoly>,
    /// detectionConfidence property.
    pub detection_confidence: Option<f64>,
    /// fdBoundingPoly property.
    pub fd_bounding_poly: Option<BoundingPoly>,
    /// headwearLikelihood property.
    pub headwear_likelihood: Option<String>,
    /// joyLikelihood property.
    pub joy_likelihood: Option<String>,
    /// landmarkingConfidence property.
    pub landmarking_confidence: Option<f64>,
    /// landmarks property.
    pub landmarks: Option<Vec<Landmark>>,
    /// panAngle property.
    pub pan_angle: Option<f64>,
    /// rollAngle property.
    pub roll_angle: Option<f64>,
    /// sorrowLikelihood property.
    pub sorrow_likelihood: Option<String>,
    /// surpriseLikelihood property.
    pub surprise_likelihood: Option<String>,
    /// tiltAngle property.
    pub tilt_angle: Option<f64>,
    /// underExposedLikelihood property.
    pub under_exposed_likelihood: Option<String>,
}

/// `WebEntity` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct WebEntity {
    /// description property.
    pub description: Option<String>,
    /// entityId property.
    pub entity_id: Option<String>,
    /// score property.
    pub score: Option<f64>,
}

/// `Page` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Page {
    /// blocks property.
    pub blocks: Option<Vec<Block>>,
    /// confidence property.
    pub confidence: Option<f64>,
    /// height property.
    pub height: Option<i64>,
    /// property property.
    pub property: Option<TextProperty>,
    /// width property.
    pub width: Option<i64>,
}

/// `CropHintsAnnotation` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct CropHintsAnnotation {
    /// cropHints property.
    pub crop_hints: Option<Vec<CropHint>>,
}

/// `Symbol` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Symbol {
    /// boundingBox property.
    pub bounding_box: Option<BoundingPoly>,
    /// confidence property.
    pub confidence: Option<f64>,
    /// property property.
    pub property: Option<TextProperty>,
    /// text property.
    pub text: Option<String>,
}

// =============================================================================
// ARGS TYPES (per-endpoint)
// =============================================================================

/// Arguments for [`vision.projects.images.annotate_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct VisionProjectsImagesAnnotateArgs {
    /// Path parameter: `parent`.
    pub parent: String,
}

/// Arguments for [`vision.projects.locations.images.annotate_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct VisionProjectsLocationsImagesAnnotateArgs {
    /// Path parameter: `parent`.
    pub parent: String,
}

// =============================================================================
// CLIENT FUNCTIONS (per-endpoint)
// =============================================================================

// -----------------------------------------------------------------------------
// POST v1/images:annotate
// -----------------------------------------------------------------------------

/// POST v1/images:annotate.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = vision_images_annotate_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn vision_images_annotate_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<BatchAnnotateImagesResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!("https://vision.googleapis.com/v1/images:annotate",);

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: BatchAnnotateImagesResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/projects/{projectsId}/images:annotate
// -----------------------------------------------------------------------------

/// POST v1/projects/{projectsId}/images:annotate.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = vision_projects_images_annotate_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn vision_projects_images_annotate_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<BatchAnnotateImagesResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url =
        format!("https://vision.googleapis.com/v1/projects/{{projectsId}}/images:annotate",);

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: BatchAnnotateImagesResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/projects/{projectsId}/locations/{locationsId}/images:annotate
// -----------------------------------------------------------------------------

/// POST v1/projects/{projectsId}/locations/{locationsId}/images:annotate.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = vision_projects_locations_images_annotate_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn vision_projects_locations_images_annotate_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<BatchAnnotateImagesResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://vision.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/images:annotate",
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: BatchAnnotateImagesResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}
