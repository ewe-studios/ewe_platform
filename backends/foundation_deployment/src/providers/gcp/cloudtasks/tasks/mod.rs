//! Auto-generated API module for gcp/cloudtasks tasks.
//!
//! Generated by `cargo run --bin ewe_platform gen_api`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp_cloudtasks_tasks `

#![cfg(feature = "gcp_cloudtasks_tasks")]
#![allow(clippy::too_many_arguments, clippy::type_complexity)]
#![allow(
    clippy::missing_errors_doc,
    clippy::doc_markdown,
    clippy::useless_format
)]

use foundation_core::valtron::{execute, StreamIterator, TaskIterator, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{ClientRequestBuilder, SimpleHttpClient};
use foundation_macros::JsonHash;
use serde::{Deserialize, Serialize};

// Import shared types used by this module
use super::shared::Empty;

use super::shared::{ApiError, ApiPending, ApiResponse};

// =============================================================================
// TYPE DECLARATIONS
// =============================================================================

/// `BufferTaskResponse` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct BufferTaskResponse {
    /// task property.
    pub task: Option<Task>,
}

/// `OAuthToken` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct OAuthToken {
    /// scope property.
    pub scope: Option<String>,
    /// serviceAccountEmail property.
    pub service_account_email: Option<String>,
}

/// `OidcToken` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct OidcToken {
    /// audience property.
    pub audience: Option<String>,
    /// serviceAccountEmail property.
    pub service_account_email: Option<String>,
}

/// `HttpRequest` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct HttpRequest {
    /// body property.
    pub body: Option<String>,
    /// headers property.
    pub headers: Option<serde_json::Value>,
    /// httpMethod property.
    pub http_method: Option<String>,
    /// oauthToken property.
    pub oauth_token: Option<OAuthToken>,
    /// oidcToken property.
    pub oidc_token: Option<OidcToken>,
    /// url property.
    pub url: Option<String>,
}

/// `AppEngineHttpRequest` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct AppEngineHttpRequest {
    /// appEngineRouting property.
    pub app_engine_routing: Option<AppEngineRouting>,
    /// body property.
    pub body: Option<String>,
    /// headers property.
    pub headers: Option<serde_json::Value>,
    /// httpMethod property.
    pub http_method: Option<String>,
    /// relativeUri property.
    pub relative_uri: Option<String>,
}

/// `Status` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Status {
    /// code property.
    pub code: Option<i64>,
    /// details property.
    pub details: Option<Vec<serde_json::Value>>,
    /// message property.
    pub message: Option<String>,
}

/// `AppEngineRouting` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct AppEngineRouting {
    /// host property.
    pub host: Option<String>,
    /// instance property.
    pub instance: Option<String>,
    /// service property.
    pub service: Option<String>,
    /// version property.
    pub version: Option<String>,
}

/// `Task` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Task {
    /// appEngineHttpRequest property.
    pub app_engine_http_request: Option<AppEngineHttpRequest>,
    /// createTime property.
    pub create_time: Option<String>,
    /// dispatchCount property.
    pub dispatch_count: Option<i64>,
    /// dispatchDeadline property.
    pub dispatch_deadline: Option<String>,
    /// firstAttempt property.
    pub first_attempt: Option<Attempt>,
    /// httpRequest property.
    pub http_request: Option<HttpRequest>,
    /// lastAttempt property.
    pub last_attempt: Option<Attempt>,
    /// name property.
    pub name: Option<String>,
    /// responseCount property.
    pub response_count: Option<i64>,
    /// scheduleTime property.
    pub schedule_time: Option<String>,
    /// view property.
    pub view: Option<String>,
}

/// `ListTasksResponse` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ListTasksResponse {
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// tasks property.
    pub tasks: Option<Vec<Task>>,
}

/// `Attempt` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Attempt {
    /// dispatchTime property.
    pub dispatch_time: Option<String>,
    /// responseStatus property.
    pub response_status: Option<Status>,
    /// responseTime property.
    pub response_time: Option<String>,
    /// scheduleTime property.
    pub schedule_time: Option<String>,
}

// =============================================================================
// ARGS TYPES (per-endpoint)
// =============================================================================

/// Arguments for [`cloudtasks.projects.locations.queues.tasks.buffer_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudtasksProjectsLocationsQueuesTasksBufferArgs {
    /// Path parameter: `queue`.
    pub queue: String,
    /// Path parameter: `taskId`.
    pub task_id: String,
}

/// Arguments for [`cloudtasks.projects.locations.queues.tasks.create_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudtasksProjectsLocationsQueuesTasksCreateArgs {
    /// Path parameter: `parent`.
    pub parent: String,
}

/// Arguments for [`cloudtasks.projects.locations.queues.tasks.delete_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudtasksProjectsLocationsQueuesTasksDeleteArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`cloudtasks.projects.locations.queues.tasks.get_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudtasksProjectsLocationsQueuesTasksGetArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `responseView`.
    pub response_view: Option<String>,
}

/// Arguments for [`cloudtasks.projects.locations.queues.tasks.list_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudtasksProjectsLocationsQueuesTasksListArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `responseView`.
    pub response_view: Option<String>,
}

/// Arguments for [`cloudtasks.projects.locations.queues.tasks.run_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct CloudtasksProjectsLocationsQueuesTasksRunArgs {
    /// Path parameter: `name`.
    pub name: String,
}

// =============================================================================
// CLIENT FUNCTIONS (per-endpoint)
// =============================================================================

// -----------------------------------------------------------------------------
// POST v2/projects/{projectsId}/locations/{locationsId}/queues/{queuesId}/tasks/{taskId}:buffer
// -----------------------------------------------------------------------------

/// POST v2/projects/{projectsId}/locations/{locationsId}/queues/{queuesId}/tasks/{taskId}:buffer.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = cloudtasks_projects_locations_queues_tasks_buffer_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn cloudtasks_projects_locations_queues_tasks_buffer_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &CloudtasksProjectsLocationsQueuesTasksBufferArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<BufferTaskResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://cloudtasks.googleapis.com/v2/projects/{{projectsId}}/locations/{{locationsId}}/queues/{{queuesId}}/tasks/{}:buffer",
        args.task_id,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: BufferTaskResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v2/projects/{projectsId}/locations/{locationsId}/queues/{queuesId}/tasks
// -----------------------------------------------------------------------------

/// POST v2/projects/{projectsId}/locations/{locationsId}/queues/{queuesId}/tasks.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = cloudtasks_projects_locations_queues_tasks_create_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn cloudtasks_projects_locations_queues_tasks_create_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Task>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://cloudtasks.googleapis.com/v2/projects/{{projectsId}}/locations/{{locationsId}}/queues/{{queuesId}}/tasks",
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Task =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE v2/projects/{projectsId}/locations/{locationsId}/queues/{queuesId}/tasks/{tasksId}
// -----------------------------------------------------------------------------

/// DELETE v2/projects/{projectsId}/locations/{locationsId}/queues/{queuesId}/tasks/{tasksId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = cloudtasks_projects_locations_queues_tasks_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn cloudtasks_projects_locations_queues_tasks_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://cloudtasks.googleapis.com/v2/projects/{{projectsId}}/locations/{{locationsId}}/queues/{{queuesId}}/tasks/{{tasksId}}",
    );

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Empty =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v2/projects/{projectsId}/locations/{locationsId}/queues/{queuesId}/tasks/{tasksId}
// -----------------------------------------------------------------------------

/// GET v2/projects/{projectsId}/locations/{locationsId}/queues/{queuesId}/tasks/{tasksId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = cloudtasks_projects_locations_queues_tasks_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn cloudtasks_projects_locations_queues_tasks_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Task>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://cloudtasks.googleapis.com/v2/projects/{{projectsId}}/locations/{{locationsId}}/queues/{{queuesId}}/tasks/{{tasksId}}",
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Task =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v2/projects/{projectsId}/locations/{locationsId}/queues/{queuesId}/tasks
// -----------------------------------------------------------------------------

/// GET v2/projects/{projectsId}/locations/{locationsId}/queues/{queuesId}/tasks.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = cloudtasks_projects_locations_queues_tasks_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn cloudtasks_projects_locations_queues_tasks_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListTasksResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://cloudtasks.googleapis.com/v2/projects/{{projectsId}}/locations/{{locationsId}}/queues/{{queuesId}}/tasks",
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListTasksResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v2/projects/{projectsId}/locations/{locationsId}/queues/{queuesId}/tasks/{tasksId}:run
// -----------------------------------------------------------------------------

/// POST v2/projects/{projectsId}/locations/{locationsId}/queues/{queuesId}/tasks/{tasksId}:run.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = cloudtasks_projects_locations_queues_tasks_run_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn cloudtasks_projects_locations_queues_tasks_run_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Task>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://cloudtasks.googleapis.com/v2/projects/{{projectsId}}/locations/{{locationsId}}/queues/{{queuesId}}/tasks/{{tasksId}}:run",
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Task =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}
