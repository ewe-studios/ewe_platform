//! Auto-generated API module for gcp/travelimpactmodel flights:computeScope3FlightEmissions.
//!
//! Generated by `cargo run --bin ewe_platform gen_api`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp_travelimpactmodel_flights_compute_scope3flight_emissions `

#![cfg(feature = "gcp_travelimpactmodel_flights_compute_scope3flight_emissions")]
#![allow(clippy::too_many_arguments, clippy::type_complexity)]
#![allow(
    clippy::missing_errors_doc,
    clippy::doc_markdown,
    clippy::useless_format
)]

use foundation_core::valtron::{execute, StreamIterator, TaskIterator, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{ClientRequestBuilder, SimpleHttpClient};
use foundation_macros::JsonHash;
use serde::{Deserialize, Serialize};

use super::shared::{ApiError, ApiPending, ApiResponse};

// =============================================================================
// TYPE DECLARATIONS
// =============================================================================

/// `ModelVersion` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ModelVersion {
    /// dated property.
    pub dated: Option<String>,
    /// major property.
    pub major: Option<i64>,
    /// minor property.
    pub minor: Option<i64>,
    /// patch property.
    pub patch: Option<i64>,
}

/// `Scope3FlightSegment` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Scope3FlightSegment {
    /// cabinClass property.
    pub cabin_class: Option<String>,
    /// carrierCode property.
    pub carrier_code: Option<String>,
    /// departureDate property.
    pub departure_date: Option<Date>,
    /// destination property.
    pub destination: Option<String>,
    /// distanceKm property.
    pub distance_km: Option<String>,
    /// flightNumber property.
    pub flight_number: Option<i64>,
    /// origin property.
    pub origin: Option<String>,
}

/// `Date` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Date {
    /// day property.
    pub day: Option<i64>,
    /// month property.
    pub month: Option<i64>,
    /// year property.
    pub year: Option<i64>,
}

/// `Scope3FlightEmissions` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Scope3FlightEmissions {
    /// flight property.
    pub flight: Option<Scope3FlightSegment>,
    /// source property.
    pub source: Option<String>,
    /// ttwEmissionsGramsPerPax property.
    pub ttw_emissions_grams_per_pax: Option<String>,
    /// wttEmissionsGramsPerPax property.
    pub wtt_emissions_grams_per_pax: Option<String>,
    /// wtwEmissionsGramsPerPax property.
    pub wtw_emissions_grams_per_pax: Option<String>,
}

/// `ComputeScope3FlightEmissionsResponse` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ComputeScope3FlightEmissionsResponse {
    /// flightEmissions property.
    pub flight_emissions: Option<Vec<Scope3FlightEmissions>>,
    /// modelVersion property.
    pub model_version: Option<ModelVersion>,
}

// =============================================================================
// ARGS TYPES (per-endpoint)
// =============================================================================

// =============================================================================
// CLIENT FUNCTIONS (per-endpoint)
// =============================================================================

// -----------------------------------------------------------------------------
// POST v1/flights:computeScope3FlightEmissions
// -----------------------------------------------------------------------------

/// POST v1/flights:computeScope3FlightEmissions.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = travelimpactmodel_flights_compute_scope3_flight_emissions_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn travelimpactmodel_flights_compute_scope3_flight_emissions_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<
                ApiResponse<ComputeScope3FlightEmissionsResponse>,
                super::shared::ApiError,
            >,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://travelimpactmodel.googleapis.com/v1/flights:computeScope3FlightEmissions",
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ComputeScope3FlightEmissionsResponse = serde_json::from_str(&body)
                    .map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}
