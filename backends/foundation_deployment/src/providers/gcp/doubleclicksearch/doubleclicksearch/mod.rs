//! Auto-generated API module for gcp/doubleclicksearch doubleclicksearch.
//!
//! Generated by `cargo run --bin ewe_platform gen_api`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp_doubleclicksearch_doubleclicksearch `

#![cfg(feature = "gcp_doubleclicksearch_doubleclicksearch")]
#![allow(clippy::too_many_arguments, clippy::type_complexity)]
#![allow(
    clippy::missing_errors_doc,
    clippy::doc_markdown,
    clippy::useless_format
)]
#![allow(unused_imports)]

use foundation_core::valtron::{TaskIterator, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{ClientRequestBuilder, SimpleHttpClient};
use foundation_macros::JsonHash;
use serde::{Deserialize, Serialize};

use super::shared::ApiResponse;

// =============================================================================
// TYPE DECLARATIONS
// =============================================================================

/// `UpdateAvailabilityRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct UpdateAvailabilityRequest {
    /// availabilities property.
    pub availabilities: Option<Vec<Availability>>,
}

/// `CustomMetric` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct CustomMetric {
    /// name property.
    pub name: Option<String>,
    /// value property.
    pub value: Option<f64>,
}

/// `Report` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Report {
    /// files property.
    pub files: Option<Vec<std::collections::HashMap<String, serde_json::Value>>>,
    /// id property.
    pub id: Option<String>,
    /// isReportReady property.
    pub is_report_ready: Option<bool>,
    /// kind property.
    pub kind: Option<String>,
    /// request property.
    pub request: Option<ReportRequest>,
    /// rowCount property.
    pub row_count: Option<i64>,
    /// rows property.
    pub rows: Option<Vec<ReportRow>>,
    /// statisticsCurrencyCode property.
    pub statistics_currency_code: Option<String>,
    /// statisticsTimeZone property.
    pub statistics_time_zone: Option<String>,
}

/// `ConversionList` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ConversionList {
    /// conversion property.
    pub conversion: Option<Vec<Conversion>>,
    /// kind property.
    pub kind: Option<String>,
}

/// `SavedColumnList` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SavedColumnList {
    /// items property.
    pub items: Option<Vec<SavedColumn>>,
    /// kind property.
    pub kind: Option<String>,
}

/// `UpdateAvailabilityResponse` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct UpdateAvailabilityResponse {
    /// availabilities property.
    pub availabilities: Option<Vec<Availability>>,
}

/// `IdMappingFile` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct IdMappingFile {}

/// `ReportRequest` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ReportRequest {
    /// columns property.
    pub columns: Option<Vec<ReportApiColumnSpec>>,
    /// downloadFormat property.
    pub download_format: Option<String>,
    /// filters property.
    pub filters: Option<Vec<std::collections::HashMap<String, serde_json::Value>>>,
    /// includeDeletedEntities property.
    pub include_deleted_entities: Option<bool>,
    /// includeRemovedEntities property.
    pub include_removed_entities: Option<bool>,
    /// maxRowsPerFile property.
    pub max_rows_per_file: Option<i64>,
    /// orderBy property.
    pub order_by: Option<Vec<std::collections::HashMap<String, serde_json::Value>>>,
    /// reportScope property.
    pub report_scope: Option<std::collections::HashMap<String, serde_json::Value>>,
    /// reportType property.
    pub report_type: Option<String>,
    /// rowCount property.
    pub row_count: Option<i64>,
    /// startRow property.
    pub start_row: Option<i64>,
    /// statisticsCurrency property.
    pub statistics_currency: Option<String>,
    /// timeRange property.
    pub time_range: Option<std::collections::HashMap<String, serde_json::Value>>,
    /// verifySingleTimeZone property.
    pub verify_single_time_zone: Option<bool>,
}

/// `CustomDimension` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct CustomDimension {
    /// name property.
    pub name: Option<String>,
    /// value property.
    pub value: Option<String>,
}

/// `Conversion` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Conversion {
    /// adGroupId property.
    pub ad_group_id: Option<String>,
    /// adId property.
    pub ad_id: Option<String>,
    /// adUserDataConsent property.
    pub ad_user_data_consent: Option<String>,
    /// advertiserId property.
    pub advertiser_id: Option<String>,
    /// agencyId property.
    pub agency_id: Option<String>,
    /// attributionModel property.
    pub attribution_model: Option<String>,
    /// campaignId property.
    pub campaign_id: Option<String>,
    /// channel property.
    pub channel: Option<String>,
    /// clickId property.
    pub click_id: Option<String>,
    /// conversionId property.
    pub conversion_id: Option<String>,
    /// conversionModifiedTimestamp property.
    pub conversion_modified_timestamp: Option<String>,
    /// conversionTimestamp property.
    pub conversion_timestamp: Option<String>,
    /// countMillis property.
    pub count_millis: Option<String>,
    /// criterionId property.
    pub criterion_id: Option<String>,
    /// currencyCode property.
    pub currency_code: Option<String>,
    /// customDimension property.
    pub custom_dimension: Option<Vec<CustomDimension>>,
    /// customMetric property.
    pub custom_metric: Option<Vec<CustomMetric>>,
    /// customerId property.
    pub customer_id: Option<String>,
    /// deviceType property.
    pub device_type: Option<String>,
    /// dsConversionId property.
    pub ds_conversion_id: Option<String>,
    /// engineAccountId property.
    pub engine_account_id: Option<String>,
    /// floodlightOrderId property.
    pub floodlight_order_id: Option<String>,
    /// inventoryAccountId property.
    pub inventory_account_id: Option<String>,
    /// productCountry property.
    pub product_country: Option<String>,
    /// productGroupId property.
    pub product_group_id: Option<String>,
    /// productId property.
    pub product_id: Option<String>,
    /// productLanguage property.
    pub product_language: Option<String>,
    /// quantityMillis property.
    pub quantity_millis: Option<String>,
    /// revenueMicros property.
    pub revenue_micros: Option<String>,
    /// segmentationId property.
    pub segmentation_id: Option<String>,
    /// segmentationName property.
    pub segmentation_name: Option<String>,
    /// segmentationType property.
    pub segmentation_type: Option<String>,
    /// state property.
    pub state: Option<String>,
    /// storeId property.
    pub store_id: Option<String>,
    /// type property.
    pub r#type: Option<String>,
}

/// `ReportRow` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ReportRow {
    #[serde(flatten)]
    pub data: std::collections::HashMap<String, serde_json::Value>,
}

/// `Availability` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct Availability {
    /// advertiserId property.
    pub advertiser_id: Option<String>,
    /// agencyId property.
    pub agency_id: Option<String>,
    /// availabilityTimestamp property.
    pub availability_timestamp: Option<String>,
    /// customerId property.
    pub customer_id: Option<String>,
    /// segmentationId property.
    pub segmentation_id: Option<String>,
    /// segmentationName property.
    pub segmentation_name: Option<String>,
    /// segmentationType property.
    pub segmentation_type: Option<String>,
}

/// `ReportApiColumnSpec` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct ReportApiColumnSpec {
    /// columnName property.
    pub column_name: Option<String>,
    /// customDimensionName property.
    pub custom_dimension_name: Option<String>,
    /// customMetricName property.
    pub custom_metric_name: Option<String>,
    /// endDate property.
    pub end_date: Option<String>,
    /// groupByColumn property.
    pub group_by_column: Option<bool>,
    /// headerText property.
    pub header_text: Option<String>,
    /// platformSource property.
    pub platform_source: Option<String>,
    /// productReportPerspective property.
    pub product_report_perspective: Option<String>,
    /// savedColumnName property.
    pub saved_column_name: Option<String>,
    /// startDate property.
    pub start_date: Option<String>,
}

/// `SavedColumn` type.
#[derive(Debug, Clone, Default, Serialize, Deserialize, JsonHash)]
pub struct SavedColumn {
    /// kind property.
    pub kind: Option<String>,
    /// savedColumnName property.
    pub saved_column_name: Option<String>,
    /// type property.
    pub r#type: Option<String>,
}

// =============================================================================
// ARGS TYPES (per-endpoint)
// =============================================================================

/// Arguments for [`doubleclicksearch.conversion.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DoubleclicksearchConversionGetArgs {
    /// Path parameter: `agencyId`.
    pub agency_id: String,
    /// Path parameter: `advertiserId`.
    pub advertiser_id: String,
    /// Path parameter: `engineAccountId`.
    pub engine_account_id: String,
    /// Query parameter: `adGroupId`.
    pub ad_group_id: Option<String>,
    /// Query parameter: `adId`.
    pub ad_id: Option<String>,
    /// Query parameter: `campaignId`.
    pub campaign_id: Option<String>,
    /// Query parameter: `criterionId`.
    pub criterion_id: Option<String>,
    /// Query parameter: `customerId`.
    pub customer_id: Option<String>,
    /// Query parameter: `endDate`.
    pub end_date: Option<String>,
    /// Query parameter: `rowCount`.
    pub row_count: Option<String>,
    /// Query parameter: `startDate`.
    pub start_date: Option<String>,
    /// Query parameter: `startRow`.
    pub start_row: Option<String>,
}

/// Arguments for [`doubleclicksearch.conversion.getByCustomerId_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DoubleclicksearchConversionGetByCustomerIdArgs {
    /// Path parameter: `customerId`.
    pub customer_id: String,
    /// Query parameter: `adGroupId`.
    pub ad_group_id: Option<String>,
    /// Query parameter: `adId`.
    pub ad_id: Option<String>,
    /// Query parameter: `advertiserId`.
    pub advertiser_id: Option<String>,
    /// Query parameter: `agencyId`.
    pub agency_id: Option<String>,
    /// Query parameter: `campaignId`.
    pub campaign_id: Option<String>,
    /// Query parameter: `criterionId`.
    pub criterion_id: Option<String>,
    /// Query parameter: `endDate`.
    pub end_date: Option<String>,
    /// Query parameter: `engineAccountId`.
    pub engine_account_id: Option<String>,
    /// Query parameter: `rowCount`.
    pub row_count: Option<String>,
    /// Query parameter: `startDate`.
    pub start_date: Option<String>,
    /// Query parameter: `startRow`.
    pub start_row: Option<String>,
}

/// Arguments for [`doubleclicksearch.conversion.insert_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DoubleclicksearchConversionInsertArgs {
    /// Request body.
    pub body: ConversionList,
}

/// Arguments for [`doubleclicksearch.conversion.update_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DoubleclicksearchConversionUpdateArgs {
    /// Request body.
    pub body: ConversionList,
}

/// Arguments for [`doubleclicksearch.conversion.updateAvailability_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DoubleclicksearchConversionUpdateAvailabilityArgs {
    /// Request body.
    pub body: UpdateAvailabilityRequest,
}

/// Arguments for [`doubleclicksearch.reports.generate_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DoubleclicksearchReportsGenerateArgs {
    /// Request body.
    pub body: ReportRequest,
}

/// Arguments for [`doubleclicksearch.reports.get_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DoubleclicksearchReportsGetArgs {
    /// Path parameter: `reportId`.
    pub report_id: String,
}

/// Arguments for [`doubleclicksearch.reports.getFile_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DoubleclicksearchReportsGetFileArgs {
    /// Path parameter: `reportId`.
    pub report_id: String,
    /// Path parameter: `reportFragment`.
    pub report_fragment: String,
}

/// Arguments for [`doubleclicksearch.reports.getIdMappingFile_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DoubleclicksearchReportsGetIdMappingFileArgs {
    /// Path parameter: `agencyId`.
    pub agency_id: String,
    /// Path parameter: `advertiserId`.
    pub advertiser_id: String,
}

/// Arguments for [`doubleclicksearch.reports.request_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DoubleclicksearchReportsRequestArgs {
    /// Request body.
    pub body: ReportRequest,
}

/// Arguments for [`doubleclicksearch.savedColumns.list_builder`].
#[derive(Debug, Clone, Default, Serialize, JsonHash)]
pub struct DoubleclicksearchSavedColumnsListArgs {
    /// Path parameter: `agencyId`.
    pub agency_id: String,
    /// Path parameter: `advertiserId`.
    pub advertiser_id: String,
}

// =============================================================================
// CLIENT FUNCTIONS (per-endpoint)
// =============================================================================

// -----------------------------------------------------------------------------
// GET doubleclicksearch/v2/agency/{agencyId}/advertiser/{advertiserId}/engine/{engineAccountId}/conversion
// -----------------------------------------------------------------------------

/// GET doubleclicksearch/v2/agency/{agencyId}/advertiser/{advertiserId}/engine/{engineAccountId}/conversion.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = doubleclicksearch_conversion_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn doubleclicksearch_conversion_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DoubleclicksearchConversionGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ConversionList>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://doubleclicksearch.googleapis.com/doubleclicksearch/v2/agency/{}/advertiser/{}/engine/{}/conversion",
        args.agency_id,
        args.advertiser_id,
        args.engine_account_id,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.ad_group_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("adGroupId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.ad_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("adId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.campaign_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("campaignId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.criterion_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("criterionId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.customer_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("customerId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.end_date {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("endDate=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.row_count {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("rowCount=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.start_date {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("startDate=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.start_row {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("startRow=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ConversionList =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET doubleclicksearch/v2/customer/{customerId}/conversion
// -----------------------------------------------------------------------------

/// GET doubleclicksearch/v2/customer/{customerId}/conversion.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = doubleclicksearch_conversion_get_by_customer_id_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn doubleclicksearch_conversion_get_by_customer_id_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DoubleclicksearchConversionGetByCustomerIdArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ConversionList>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://doubleclicksearch.googleapis.com/doubleclicksearch/v2/customer/{}/conversion",
        args.customer_id,
    );

    let endpoint_url = {
        let mut url = endpoint_url;
        let mut first = true;
        if let Some(ref v) = args.ad_group_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("adGroupId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.ad_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("adId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.advertiser_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("advertiserId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.agency_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("agencyId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.campaign_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("campaignId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.criterion_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("criterionId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.end_date {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("endDate=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.engine_account_id {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("engineAccountId=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.row_count {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("rowCount=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.start_date {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("startDate=");
            url.push_str(&urlencoding::encode(v));
        }
        if let Some(ref v) = args.start_row {
            if first {
                url.push('?');
                first = false;
            } else {
                url.push('&');
            }
            url.push_str("startRow=");
            url.push_str(&urlencoding::encode(v));
        }
        url
    };

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ConversionList =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST doubleclicksearch/v2/conversion
// -----------------------------------------------------------------------------

/// POST doubleclicksearch/v2/conversion.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = doubleclicksearch_conversion_insert_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn doubleclicksearch_conversion_insert_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DoubleclicksearchConversionInsertArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ConversionList>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url =
        format!("https://doubleclicksearch.googleapis.com/doubleclicksearch/v2/conversion",);

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ConversionList =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PUT doubleclicksearch/v2/conversion
// -----------------------------------------------------------------------------

/// PUT doubleclicksearch/v2/conversion.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = doubleclicksearch_conversion_update_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn doubleclicksearch_conversion_update_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DoubleclicksearchConversionUpdateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ConversionList>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url =
        format!("https://doubleclicksearch.googleapis.com/doubleclicksearch/v2/conversion",);

    let mut builder = client
        .put(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ConversionList =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST doubleclicksearch/v2/conversion/updateAvailability
// -----------------------------------------------------------------------------

/// POST doubleclicksearch/v2/conversion/updateAvailability.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = doubleclicksearch_conversion_update_availability_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn doubleclicksearch_conversion_update_availability_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DoubleclicksearchConversionUpdateAvailabilityArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<UpdateAvailabilityResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://doubleclicksearch.googleapis.com/doubleclicksearch/v2/conversion/updateAvailability",
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: UpdateAvailabilityResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST doubleclicksearch/v2/reports/generate
// -----------------------------------------------------------------------------

/// POST doubleclicksearch/v2/reports/generate.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = doubleclicksearch_reports_generate_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn doubleclicksearch_reports_generate_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DoubleclicksearchReportsGenerateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Report>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url =
        format!("https://doubleclicksearch.googleapis.com/doubleclicksearch/v2/reports/generate",);

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Report =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET doubleclicksearch/v2/reports/{reportId}
// -----------------------------------------------------------------------------

/// GET doubleclicksearch/v2/reports/{reportId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = doubleclicksearch_reports_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn doubleclicksearch_reports_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DoubleclicksearchReportsGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Report>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://doubleclicksearch.googleapis.com/doubleclicksearch/v2/reports/{}",
        args.report_id,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Report =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET doubleclicksearch/v2/reports/{reportId}/files/{reportFragment}
// -----------------------------------------------------------------------------

/// GET doubleclicksearch/v2/reports/{reportId}/files/{reportFragment}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = doubleclicksearch_reports_get_file_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn doubleclicksearch_reports_get_file_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DoubleclicksearchReportsGetFileArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<()>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://doubleclicksearch.googleapis.com/doubleclicksearch/v2/reports/{}/files/{}",
        args.report_id, args.report_fragment,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream: _,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: (),
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET doubleclicksearch/v2/agency/{agencyId}/advertiser/{advertiserId}/idmapping
// -----------------------------------------------------------------------------

/// GET doubleclicksearch/v2/agency/{agencyId}/advertiser/{advertiserId}/idmapping.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = doubleclicksearch_reports_get_id_mapping_file_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn doubleclicksearch_reports_get_id_mapping_file_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DoubleclicksearchReportsGetIdMappingFileArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<IdMappingFile>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://doubleclicksearch.googleapis.com/doubleclicksearch/v2/agency/{}/advertiser/{}/idmapping",
        args.agency_id,
        args.advertiser_id,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: IdMappingFile =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST doubleclicksearch/v2/reports
// -----------------------------------------------------------------------------

/// POST doubleclicksearch/v2/reports.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = doubleclicksearch_reports_request_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn doubleclicksearch_reports_request_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DoubleclicksearchReportsRequestArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Report>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url =
        format!("https://doubleclicksearch.googleapis.com/doubleclicksearch/v2/reports",);

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    builder = builder
        .body_json(&args.body)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Report =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET doubleclicksearch/v2/agency/{agencyId}/advertiser/{advertiserId}/savedcolumns
// -----------------------------------------------------------------------------

/// GET doubleclicksearch/v2/agency/{agencyId}/advertiser/{advertiserId}/savedcolumns.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = doubleclicksearch_saved_columns_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn doubleclicksearch_saved_columns_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &DoubleclicksearchSavedColumnsListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SavedColumnList>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://doubleclicksearch.googleapis.com/doubleclicksearch/v2/agency/{}/advertiser/{}/savedcolumns",
        args.agency_id,
        args.advertiser_id,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: SavedColumnList =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}
