//! Auto-generated API module for gcp/discoveryengine tasks.
//!
//! Generated by `cargo run --bin ewe_platform gen_api`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp_discoveryengine_tasks `

#![cfg(feature = "gcp_discoveryengine_tasks")]
#![allow(clippy::too_many_arguments, clippy::type_complexity)]
#![allow(
    clippy::missing_errors_doc,
    clippy::doc_markdown,
    clippy::useless_format
)]

use foundation_core::valtron::{execute, StreamIterator, TaskIterator, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{ClientRequestBuilder, SimpleHttpClient};
use foundation_macros::JsonHash;
use serde::{Deserialize, Serialize};

// Import shared types used by this module
use super::shared::A2aV1StreamResponse;

use super::shared::{ApiError, ApiPending, ApiResponse};

// =============================================================================
// TYPE DECLARATIONS
// =============================================================================

/// `A2AV1TaskStatus` response type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct A2AV1TaskStatus {
    /// Raw JSON value - full schema generated from `OpenAPI`
    #[serde(flatten)]
    pub data: std::collections::HashMap<String, serde_json::Value>,
}

/// `A2AV1DataPart` response type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct A2AV1DataPart {
    /// Raw JSON value - full schema generated from `OpenAPI`
    #[serde(flatten)]
    pub data: std::collections::HashMap<String, serde_json::Value>,
}

/// `A2AV1FilePart` response type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct A2AV1FilePart {
    /// Raw JSON value - full schema generated from `OpenAPI`
    #[serde(flatten)]
    pub data: std::collections::HashMap<String, serde_json::Value>,
}

/// `A2AV1TaskArtifactUpdateEvent` response type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct A2AV1TaskArtifactUpdateEvent {
    /// Raw JSON value - full schema generated from `OpenAPI`
    #[serde(flatten)]
    pub data: std::collections::HashMap<String, serde_json::Value>,
}

/// `A2AV1TaskStatusUpdateEvent` response type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct A2AV1TaskStatusUpdateEvent {
    /// Raw JSON value - full schema generated from `OpenAPI`
    #[serde(flatten)]
    pub data: std::collections::HashMap<String, serde_json::Value>,
}

/// `A2AV1Task` response type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct A2AV1Task {
    /// Raw JSON value - full schema generated from `OpenAPI`
    #[serde(flatten)]
    pub data: std::collections::HashMap<String, serde_json::Value>,
}

/// `A2AV1Artifact` response type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct A2AV1Artifact {
    /// Raw JSON value - full schema generated from `OpenAPI`
    #[serde(flatten)]
    pub data: std::collections::HashMap<String, serde_json::Value>,
}

/// `A2AV1Part` response type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct A2AV1Part {
    /// Raw JSON value - full schema generated from `OpenAPI`
    #[serde(flatten)]
    pub data: std::collections::HashMap<String, serde_json::Value>,
}

/// `A2aV1Task` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct A2aV1Task {
    /// artifacts property.
    pub artifacts: Option<Vec<A2AV1Artifact>>,
    /// contextId property.
    pub context_id: Option<String>,
    /// history property.
    pub history: Option<Vec<A2AV1Message>>,
    /// id property.
    pub id: Option<String>,
    /// metadata property.
    pub metadata: Option<serde_json::Value>,
    /// status property.
    pub status: Option<A2AV1TaskStatus>,
}

/// `A2AV1Message` response type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct A2AV1Message {
    /// Raw JSON value - full schema generated from `OpenAPI`
    #[serde(flatten)]
    pub data: std::collections::HashMap<String, serde_json::Value>,
}

// =============================================================================
// ARGS TYPES (per-endpoint)
// =============================================================================

/// Arguments for [`discoveryengine.projects.locations.collections.engines.assistants.agents.a2a.v1.tasks.cancel_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DiscoveryengineProjectsLocationsCollectionsEnginesAssistantsAgentsA2AV1TasksCancelArgs {
    /// Path parameter: `tenant`.
    pub tenant: String,
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`discoveryengine.projects.locations.collections.engines.assistants.agents.a2a.v1.tasks.get_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DiscoveryengineProjectsLocationsCollectionsEnginesAssistantsAgentsA2AV1TasksGetArgs {
    /// Path parameter: `tenant`.
    pub tenant: String,
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `historyLength`.
    pub history_length: Option<String>,
}

/// Arguments for [`discoveryengine.projects.locations.collections.engines.assistants.agents.a2a.v1.tasks.subscribe_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct DiscoveryengineProjectsLocationsCollectionsEnginesAssistantsAgentsA2AV1TasksSubscribeArgs
{
    /// Path parameter: `tenant`.
    pub tenant: String,
    /// Path parameter: `name`.
    pub name: String,
}

// =============================================================================
// CLIENT FUNCTIONS (per-endpoint)
// =============================================================================

// -----------------------------------------------------------------------------
// POST v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/engines/{enginesId}/assistants/{assistantsId}/agents/{agentsId}/a2a/v1/tasks/{tasksId}:cancel
// -----------------------------------------------------------------------------

/// POST v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/engines/{enginesId}/assistants/{assistantsId}/agents/{agentsId}/a2a/v1/tasks/{tasksId}:cancel.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = discoveryengine_projects_locations_collections_engines_assistants_agents_a2a_v1_tasks_cancel_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn discoveryengine_projects_locations_collections_engines_assistants_agents_a2a_v1_tasks_cancel_request<
    R,
    F,
>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<A2aV1Task>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/collections/{{collectionsId}}/engines/{{enginesId}}/assistants/{{assistantsId}}/agents/{{agentsId}}/a2a/v1/tasks/{{tasksId}}:cancel",
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: A2aV1Task =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/engines/{enginesId}/assistants/{assistantsId}/agents/{agentsId}/a2a/v1/tasks/{tasksId}
// -----------------------------------------------------------------------------

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/engines/{enginesId}/assistants/{assistantsId}/agents/{agentsId}/a2a/v1/tasks/{tasksId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = discoveryengine_projects_locations_collections_engines_assistants_agents_a2a_v1_tasks_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn discoveryengine_projects_locations_collections_engines_assistants_agents_a2a_v1_tasks_get_request<
    R,
    F,
>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<A2aV1Task>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/collections/{{collectionsId}}/engines/{{enginesId}}/assistants/{{assistantsId}}/agents/{{agentsId}}/a2a/v1/tasks/{{tasksId}}",
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: A2aV1Task =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/engines/{enginesId}/assistants/{assistantsId}/agents/{agentsId}/a2a/v1/tasks/{tasksId}:subscribe
// -----------------------------------------------------------------------------

/// GET v1/projects/{projectsId}/locations/{locationsId}/collections/{collectionsId}/engines/{enginesId}/assistants/{assistantsId}/agents/{agentsId}/a2a/v1/tasks/{tasksId}:subscribe.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = discoveryengine_projects_locations_collections_engines_assistants_agents_a2a_v1_tasks_subscribe_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn discoveryengine_projects_locations_collections_engines_assistants_agents_a2a_v1_tasks_subscribe_request<
    R,
    F,
>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<A2aV1StreamResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://discoveryengine.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/collections/{{collectionsId}}/engines/{{enginesId}}/assistants/{{assistantsId}}/agents/{{agentsId}}/a2a/v1/tasks/{{tasksId}}:subscribe",
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: A2aV1StreamResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}
