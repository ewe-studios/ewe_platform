//! Auto-generated API module for gcp/groupssettings default.
//!
//! Generated by `cargo run --bin ewe_platform gen_api`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp_groupssettings_default `

#![cfg(feature = "gcp_groupssettings_default")]
#![allow(clippy::too_many_arguments, clippy::type_complexity)]
#![allow(
    clippy::missing_errors_doc,
    clippy::doc_markdown,
    clippy::useless_format
)]
#![allow(unused_imports)]

use foundation_core::valtron::{TaskIterator, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{ClientRequestBuilder, SimpleHttpClient};
use foundation_macros::JsonHash;
use serde::{Deserialize, Serialize};

use super::shared::ApiResponse;

// =============================================================================
// TYPE DECLARATIONS
// =============================================================================

/// `Groups` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Groups {
    /// allowExternalMembers property.
    pub allow_external_members: Option<String>,
    /// allowGoogleCommunication property.
    pub allow_google_communication: Option<String>,
    /// allowWebPosting property.
    pub allow_web_posting: Option<String>,
    /// archiveOnly property.
    pub archive_only: Option<String>,
    /// customFooterText property.
    pub custom_footer_text: Option<String>,
    /// customReplyTo property.
    pub custom_reply_to: Option<String>,
    /// customRolesEnabledForSettingsToBeMerged property.
    pub custom_roles_enabled_for_settings_to_be_merged: Option<String>,
    /// defaultMessageDenyNotificationText property.
    pub default_message_deny_notification_text: Option<String>,
    /// default_sender property.
    pub default_sender: Option<String>,
    /// description property.
    pub description: Option<String>,
    /// email property.
    pub email: Option<String>,
    /// enableCollaborativeInbox property.
    pub enable_collaborative_inbox: Option<String>,
    /// favoriteRepliesOnTop property.
    pub favorite_replies_on_top: Option<String>,
    /// includeCustomFooter property.
    pub include_custom_footer: Option<String>,
    /// includeInGlobalAddressList property.
    pub include_in_global_address_list: Option<String>,
    /// isArchived property.
    pub is_archived: Option<String>,
    /// kind property.
    pub kind: Option<String>,
    /// maxMessageBytes property.
    pub max_message_bytes: Option<i64>,
    /// membersCanPostAsTheGroup property.
    pub members_can_post_as_the_group: Option<String>,
    /// messageDisplayFont property.
    pub message_display_font: Option<String>,
    /// messageModerationLevel property.
    pub message_moderation_level: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// primaryLanguage property.
    pub primary_language: Option<String>,
    /// replyTo property.
    pub reply_to: Option<String>,
    /// sendMessageDenyNotification property.
    pub send_message_deny_notification: Option<String>,
    /// showInGroupDirectory property.
    pub show_in_group_directory: Option<String>,
    /// spamModerationLevel property.
    pub spam_moderation_level: Option<String>,
    /// whoCanAdd property.
    pub who_can_add: Option<String>,
    /// whoCanAddReferences property.
    pub who_can_add_references: Option<String>,
    /// whoCanApproveMembers property.
    pub who_can_approve_members: Option<String>,
    /// whoCanApproveMessages property.
    pub who_can_approve_messages: Option<String>,
    /// whoCanAssignTopics property.
    pub who_can_assign_topics: Option<String>,
    /// whoCanAssistContent property.
    pub who_can_assist_content: Option<String>,
    /// whoCanBanUsers property.
    pub who_can_ban_users: Option<String>,
    /// whoCanContactOwner property.
    pub who_can_contact_owner: Option<String>,
    /// whoCanDeleteAnyPost property.
    pub who_can_delete_any_post: Option<String>,
    /// whoCanDeleteTopics property.
    pub who_can_delete_topics: Option<String>,
    /// whoCanDiscoverGroup property.
    pub who_can_discover_group: Option<String>,
    /// whoCanEnterFreeFormTags property.
    pub who_can_enter_free_form_tags: Option<String>,
    /// whoCanHideAbuse property.
    pub who_can_hide_abuse: Option<String>,
    /// whoCanInvite property.
    pub who_can_invite: Option<String>,
    /// whoCanJoin property.
    pub who_can_join: Option<String>,
    /// whoCanLeaveGroup property.
    pub who_can_leave_group: Option<String>,
    /// whoCanLockTopics property.
    pub who_can_lock_topics: Option<String>,
    /// whoCanMakeTopicsSticky property.
    pub who_can_make_topics_sticky: Option<String>,
    /// whoCanMarkDuplicate property.
    pub who_can_mark_duplicate: Option<String>,
    /// whoCanMarkFavoriteReplyOnAnyTopic property.
    pub who_can_mark_favorite_reply_on_any_topic: Option<String>,
    /// whoCanMarkFavoriteReplyOnOwnTopic property.
    pub who_can_mark_favorite_reply_on_own_topic: Option<String>,
    /// whoCanMarkNoResponseNeeded property.
    pub who_can_mark_no_response_needed: Option<String>,
    /// whoCanModerateContent property.
    pub who_can_moderate_content: Option<String>,
    /// whoCanModerateMembers property.
    pub who_can_moderate_members: Option<String>,
    /// whoCanModifyMembers property.
    pub who_can_modify_members: Option<String>,
    /// whoCanModifyTagsAndCategories property.
    pub who_can_modify_tags_and_categories: Option<String>,
    /// whoCanMoveTopicsIn property.
    pub who_can_move_topics_in: Option<String>,
    /// whoCanMoveTopicsOut property.
    pub who_can_move_topics_out: Option<String>,
    /// whoCanPostAnnouncements property.
    pub who_can_post_announcements: Option<String>,
    /// whoCanPostMessage property.
    pub who_can_post_message: Option<String>,
    /// whoCanTakeTopics property.
    pub who_can_take_topics: Option<String>,
    /// whoCanUnassignTopic property.
    pub who_can_unassign_topic: Option<String>,
    /// whoCanUnmarkFavoriteReplyOnAnyTopic property.
    pub who_can_unmark_favorite_reply_on_any_topic: Option<String>,
    /// whoCanViewGroup property.
    pub who_can_view_group: Option<String>,
    /// whoCanViewMembership property.
    pub who_can_view_membership: Option<String>,
}

// =============================================================================
// ARGS TYPES (per-endpoint)
// =============================================================================

/// Arguments for [`groupsSettings.groups.get_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GroupsSettingsGroupsGetArgs {
    /// Path parameter: `groupUniqueId`.
    pub group_unique_id: String,
}

/// Arguments for [`groupsSettings.groups.patch_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GroupsSettingsGroupsPatchArgs {
    /// Path parameter: `groupUniqueId`.
    pub group_unique_id: String,
}

/// Arguments for [`groupsSettings.groups.update_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct GroupsSettingsGroupsUpdateArgs {
    /// Path parameter: `groupUniqueId`.
    pub group_unique_id: String,
}

// =============================================================================
// CLIENT FUNCTIONS (per-endpoint)
// =============================================================================

// -----------------------------------------------------------------------------
// GET {groupUniqueId}
// -----------------------------------------------------------------------------

/// GET {groupUniqueId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = groups_settings_groups_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn groups_settings_groups_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &GroupsSettingsGroupsGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Groups>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://www.googleapis.com/groups/v1/groups/{}",
        args.group_unique_id,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Groups =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH {groupUniqueId}
// -----------------------------------------------------------------------------

/// PATCH {groupUniqueId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = groups_settings_groups_patch_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn groups_settings_groups_patch_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &GroupsSettingsGroupsPatchArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Groups>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://www.googleapis.com/groups/v1/groups/{}",
        args.group_unique_id,
    );

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Groups =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PUT {groupUniqueId}
// -----------------------------------------------------------------------------

/// PUT {groupUniqueId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = groups_settings_groups_update_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn groups_settings_groups_update_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &GroupsSettingsGroupsUpdateArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Groups>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://www.googleapis.com/groups/v1/groups/{}",
        args.group_unique_id,
    );

    let mut builder = client
        .put(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Groups =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}
