//! Auto-generated API module for gcp/healthcare fhirStores.
//!
//! Generated by `cargo run --bin ewe_platform gen_api`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp_healthcare_fhir_stores `

#![cfg(feature = "gcp_healthcare_fhir_stores")]
#![allow(clippy::too_many_arguments, clippy::type_complexity)]
#![allow(
    clippy::missing_errors_doc,
    clippy::doc_markdown,
    clippy::useless_format
)]

use foundation_core::valtron::{execute, StreamIterator, TaskIterator, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{ClientRequestBuilder, SimpleHttpClient};
use foundation_macros::JsonHash;
use serde::{Deserialize, Serialize};

// Import shared types used by this module
use super::shared::Empty;
use super::shared::Operation;
use super::shared::Policy;
use super::shared::TestIamPermissionsResponse;

use super::shared::{ApiError, ApiPending, ApiResponse};

// =============================================================================
// TYPE DECLARATIONS
// =============================================================================

/// `GoogleCloudHealthcareV1FhirBigQueryDestination` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct GoogleCloudHealthcareV1FhirBigQueryDestination {
    /// datasetUri property.
    pub dataset_uri: Option<String>,
    /// force property.
    pub force: Option<bool>,
    /// schemaConfig property.
    pub schema_config: Option<SchemaConfig>,
    /// writeDisposition property.
    pub write_disposition: Option<String>,
}

/// `ExplainDataAccessConsentInfo` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ExplainDataAccessConsentInfo {
    /// cascadeOrigins property.
    pub cascade_origins: Option<Vec<String>>,
    /// consentResource property.
    pub consent_resource: Option<String>,
    /// enforcementTime property.
    pub enforcement_time: Option<String>,
    /// matchingAccessorScopes property.
    pub matching_accessor_scopes: Option<Vec<ConsentAccessorScope>>,
    /// patientConsentOwner property.
    pub patient_consent_owner: Option<String>,
    /// type property.
    pub r#type: Option<String>,
    /// variants property.
    pub variants: Option<Vec<String>>,
}

/// `TextConfig` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct TextConfig {
    /// additionalTransformations property.
    pub additional_transformations: Option<Vec<InfoTypeTransformation>>,
    /// excludeInfoTypes property.
    pub exclude_info_types: Option<Vec<String>>,
    /// transformations property.
    pub transformations: Option<Vec<InfoTypeTransformation>>,
}

/// `SchemaConfig` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct SchemaConfig {
    /// lastUpdatedPartitionConfig property.
    pub last_updated_partition_config: Option<TimePartitioning>,
    /// recursiveStructureDepth property.
    pub recursive_structure_depth: Option<String>,
    /// schemaType property.
    pub schema_type: Option<String>,
}

/// `TimePartitioning` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct TimePartitioning {
    /// expirationMs property.
    pub expiration_ms: Option<String>,
    /// type property.
    pub r#type: Option<String>,
}

/// `DeidentifiedStoreDestination` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct DeidentifiedStoreDestination {
    /// config property.
    pub config: Option<DeidentifyConfig>,
    /// store property.
    pub store: Option<String>,
}

/// `FhirStoreMetric` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct FhirStoreMetric {
    /// count property.
    pub count: Option<String>,
    /// resourceType property.
    pub resource_type: Option<String>,
    /// structuredStorageSizeBytes property.
    pub structured_storage_size_bytes: Option<String>,
    /// versionedStorageSizeBytes property.
    pub versioned_storage_size_bytes: Option<String>,
}

/// `ConsentAccessorScope` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ConsentAccessorScope {
    /// actor property.
    pub actor: Option<String>,
    /// environment property.
    pub environment: Option<String>,
    /// purpose property.
    pub purpose: Option<String>,
}

/// `AccessDeterminationLogConfig` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct AccessDeterminationLogConfig {
    /// logLevel property.
    pub log_level: Option<String>,
}

/// `FhirConfig` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct FhirConfig {
    /// defaultKeepExtensions property.
    pub default_keep_extensions: Option<bool>,
    /// fieldMetadataList property.
    pub field_metadata_list: Option<Vec<FieldMetadata>>,
}

/// `DicomConfig` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct DicomConfig {
    /// filterProfile property.
    pub filter_profile: Option<String>,
    /// keepList property.
    pub keep_list: Option<TagFilterList>,
    /// removeList property.
    pub remove_list: Option<TagFilterList>,
    /// skipIdRedaction property.
    pub skip_id_redaction: Option<bool>,
}

/// `AuditLogConfig` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct AuditLogConfig {
    /// exemptedMembers property.
    pub exempted_members: Option<Vec<String>>,
    /// logType property.
    pub log_type: Option<String>,
}

/// `FhirNotificationConfig` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct FhirNotificationConfig {
    /// pubsubTopic property.
    pub pubsub_topic: Option<String>,
    /// sendFullResource property.
    pub send_full_resource: Option<bool>,
    /// sendPreviousResourceOnDelete property.
    pub send_previous_resource_on_delete: Option<bool>,
}

/// `Expr` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Expr {
    /// description property.
    pub description: Option<String>,
    /// expression property.
    pub expression: Option<String>,
    /// location property.
    pub location: Option<String>,
    /// title property.
    pub title: Option<String>,
}

/// `Binding` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Binding {
    /// condition property.
    pub condition: Option<Expr>,
    /// members property.
    pub members: Option<Vec<String>>,
    /// role property.
    pub role: Option<String>,
}

/// `RedactConfig` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct RedactConfig {}

/// `DateShiftConfig` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct DateShiftConfig {
    /// cryptoKey property.
    pub crypto_key: Option<String>,
    /// kmsWrapped property.
    pub kms_wrapped: Option<KmsWrappedCryptoKey>,
}

/// `InfoTypeTransformation` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct InfoTypeTransformation {
    /// characterMaskConfig property.
    pub character_mask_config: Option<CharacterMaskConfig>,
    /// cryptoHashConfig property.
    pub crypto_hash_config: Option<CryptoHashConfig>,
    /// dateShiftConfig property.
    pub date_shift_config: Option<DateShiftConfig>,
    /// infoTypes property.
    pub info_types: Option<Vec<String>>,
    /// redactConfig property.
    pub redact_config: Option<RedactConfig>,
    /// replaceWithInfoTypeConfig property.
    pub replace_with_info_type_config: Option<ReplaceWithInfoTypeConfig>,
}

/// `CharacterMaskConfig` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct CharacterMaskConfig {
    /// maskingCharacter property.
    pub masking_character: Option<String>,
}

/// `BulkExportGcsDestination` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct BulkExportGcsDestination {
    /// uriPrefix property.
    pub uri_prefix: Option<String>,
}

/// `FhirStoreMetrics` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct FhirStoreMetrics {
    /// metrics property.
    pub metrics: Option<Vec<FhirStoreMetric>>,
    /// name property.
    pub name: Option<String>,
}

/// `Status` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Status {
    /// code property.
    pub code: Option<i64>,
    /// details property.
    pub details: Option<Vec<serde_json::Value>>,
    /// message property.
    pub message: Option<String>,
}

/// `ImageConfig` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ImageConfig {
    /// textRedactionMode property.
    pub text_redaction_mode: Option<String>,
}

/// `CryptoHashConfig` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct CryptoHashConfig {
    /// cryptoKey property.
    pub crypto_key: Option<String>,
    /// kmsWrapped property.
    pub kms_wrapped: Option<KmsWrappedCryptoKey>,
}

/// `ListFhirStoresResponse` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ListFhirStoresResponse {
    /// fhirStores property.
    pub fhir_stores: Option<Vec<FhirStore>>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
}

/// `StreamConfig` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct StreamConfig {
    /// bigqueryDestination property.
    pub bigquery_destination: Option<GoogleCloudHealthcareV1FhirBigQueryDestination>,
    /// deidentifiedStoreDestination property.
    pub deidentified_store_destination: Option<DeidentifiedStoreDestination>,
    /// resourceTypes property.
    pub resource_types: Option<Vec<String>>,
}

/// `ExplainDataAccessConsentScope` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ExplainDataAccessConsentScope {
    /// accessorScope property.
    pub accessor_scope: Option<ConsentAccessorScope>,
    /// decision property.
    pub decision: Option<String>,
    /// enforcingConsents property.
    pub enforcing_consents: Option<Vec<ExplainDataAccessConsentInfo>>,
    /// exceptions property.
    pub exceptions: Option<Vec<ExplainDataAccessConsentScope>>,
}

/// `AuditConfig` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct AuditConfig {
    /// auditLogConfigs property.
    pub audit_log_configs: Option<Vec<AuditLogConfig>>,
    /// service property.
    pub service: Option<String>,
}

/// `ReplaceWithInfoTypeConfig` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ReplaceWithInfoTypeConfig {}

/// `ConsentConfig` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ConsentConfig {
    /// accessDeterminationLogConfig property.
    pub access_determination_log_config: Option<AccessDeterminationLogConfig>,
    /// accessEnforced property.
    pub access_enforced: Option<bool>,
    /// consentHeaderHandling property.
    pub consent_header_handling: Option<ConsentHeaderHandling>,
    /// enforcedAdminConsents property.
    pub enforced_admin_consents: Option<Vec<String>>,
    /// version property.
    pub version: Option<String>,
}

/// `FhirStore` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct FhirStore {
    /// bulkExportGcsDestination property.
    pub bulk_export_gcs_destination: Option<BulkExportGcsDestination>,
    /// complexDataTypeReferenceParsing property.
    pub complex_data_type_reference_parsing: Option<String>,
    /// consentConfig property.
    pub consent_config: Option<ConsentConfig>,
    /// defaultSearchHandlingStrict property.
    pub default_search_handling_strict: Option<bool>,
    /// disableReferentialIntegrity property.
    pub disable_referential_integrity: Option<bool>,
    /// disableResourceVersioning property.
    pub disable_resource_versioning: Option<bool>,
    /// enableUpdateCreate property.
    pub enable_update_create: Option<bool>,
    /// labels property.
    pub labels: Option<serde_json::Value>,
    /// name property.
    pub name: Option<String>,
    /// notificationConfig property.
    pub notification_config: Option<NotificationConfig>,
    /// notificationConfigs property.
    pub notification_configs: Option<Vec<FhirNotificationConfig>>,
    /// streamConfigs property.
    pub stream_configs: Option<Vec<StreamConfig>>,
    /// validationConfig property.
    pub validation_config: Option<ValidationConfig>,
    /// version property.
    pub version: Option<String>,
}

/// `NotificationConfig` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct NotificationConfig {
    /// pubsubTopic property.
    pub pubsub_topic: Option<String>,
    /// sendForBulkImport property.
    pub send_for_bulk_import: Option<bool>,
}

/// `ValidationConfig` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ValidationConfig {
    /// disableFhirpathValidation property.
    pub disable_fhirpath_validation: Option<bool>,
    /// disableProfileValidation property.
    pub disable_profile_validation: Option<bool>,
    /// disableReferenceTypeValidation property.
    pub disable_reference_type_validation: Option<bool>,
    /// disableRequiredFieldValidation property.
    pub disable_required_field_validation: Option<bool>,
    /// enableFhirpathProfileValidation property.
    pub enable_fhirpath_profile_validation: Option<bool>,
    /// enabledImplementationGuides property.
    pub enabled_implementation_guides: Option<Vec<String>>,
}

/// `DeidentifyConfig` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct DeidentifyConfig {
    /// dicom property.
    pub dicom: Option<DicomConfig>,
    /// fhir property.
    pub fhir: Option<FhirConfig>,
    /// image property.
    pub image: Option<ImageConfig>,
    /// text property.
    pub text: Option<TextConfig>,
    /// useRegionalDataProcessing property.
    pub use_regional_data_processing: Option<bool>,
}

/// `KmsWrappedCryptoKey` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct KmsWrappedCryptoKey {
    /// cryptoKey property.
    pub crypto_key: Option<String>,
    /// wrappedKey property.
    pub wrapped_key: Option<String>,
}

/// `FieldMetadata` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct FieldMetadata {
    /// action property.
    pub action: Option<String>,
    /// paths property.
    pub paths: Option<Vec<String>>,
}

/// `TagFilterList` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct TagFilterList {
    /// tags property.
    pub tags: Option<Vec<String>>,
}

/// `ConsentHeaderHandling` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ConsentHeaderHandling {
    /// profile property.
    pub profile: Option<String>,
}

/// `ExplainDataAccessResponse` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ExplainDataAccessResponse {
    /// consentScopes property.
    pub consent_scopes: Option<Vec<ExplainDataAccessConsentScope>>,
    /// warning property.
    pub warning: Option<String>,
}

// =============================================================================
// ARGS TYPES (per-endpoint)
// =============================================================================

/// Arguments for [`healthcare.projects.locations.datasets.fhirStores.applyAdminConsents_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct HealthcareProjectsLocationsDatasetsFhirStoresApplyAdminConsentsArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`healthcare.projects.locations.datasets.fhirStores.applyConsents_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct HealthcareProjectsLocationsDatasetsFhirStoresApplyConsentsArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`healthcare.projects.locations.datasets.fhirStores.bulkDelete_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct HealthcareProjectsLocationsDatasetsFhirStoresBulkDeleteArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`healthcare.projects.locations.datasets.fhirStores.create_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct HealthcareProjectsLocationsDatasetsFhirStoresCreateArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `fhirStoreId`.
    pub fhir_store_id: Option<String>,
}

/// Arguments for [`healthcare.projects.locations.datasets.fhirStores.deidentify_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct HealthcareProjectsLocationsDatasetsFhirStoresDeidentifyArgs {
    /// Path parameter: `sourceStore`.
    pub source_store: String,
}

/// Arguments for [`healthcare.projects.locations.datasets.fhirStores.delete_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct HealthcareProjectsLocationsDatasetsFhirStoresDeleteArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`healthcare.projects.locations.datasets.fhirStores.explainDataAccess_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct HealthcareProjectsLocationsDatasetsFhirStoresExplainDataAccessArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `resourceId`.
    pub resource_id: Option<String>,
}

/// Arguments for [`healthcare.projects.locations.datasets.fhirStores.export_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct HealthcareProjectsLocationsDatasetsFhirStoresExportArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`healthcare.projects.locations.datasets.fhirStores.get_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct HealthcareProjectsLocationsDatasetsFhirStoresGetArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`healthcare.projects.locations.datasets.fhirStores.getFHIRStoreMetrics_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct HealthcareProjectsLocationsDatasetsFhirStoresGetFHIRStoreMetricsArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`healthcare.projects.locations.datasets.fhirStores.getIamPolicy_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct HealthcareProjectsLocationsDatasetsFhirStoresGetIamPolicyArgs {
    /// Path parameter: `resource`.
    pub resource: String,
    /// Query parameter: `options.requestedPolicyVersion`.
    pub options_requested_policy_version: Option<String>,
}

/// Arguments for [`healthcare.projects.locations.datasets.fhirStores.import_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct HealthcareProjectsLocationsDatasetsFhirStoresImportArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`healthcare.projects.locations.datasets.fhirStores.list_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct HealthcareProjectsLocationsDatasetsFhirStoresListArgs {
    /// Path parameter: `parent`.
    pub parent: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `pageSize`.
    pub page_size: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
}

/// Arguments for [`healthcare.projects.locations.datasets.fhirStores.patch_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct HealthcareProjectsLocationsDatasetsFhirStoresPatchArgs {
    /// Path parameter: `name`.
    pub name: String,
    /// Query parameter: `updateMask`.
    pub update_mask: Option<String>,
}

/// Arguments for [`healthcare.projects.locations.datasets.fhirStores.rollback_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct HealthcareProjectsLocationsDatasetsFhirStoresRollbackArgs {
    /// Path parameter: `name`.
    pub name: String,
}

/// Arguments for [`healthcare.projects.locations.datasets.fhirStores.setIamPolicy_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct HealthcareProjectsLocationsDatasetsFhirStoresSetIamPolicyArgs {
    /// Path parameter: `resource`.
    pub resource: String,
}

/// Arguments for [`healthcare.projects.locations.datasets.fhirStores.testIamPermissions_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct HealthcareProjectsLocationsDatasetsFhirStoresTestIamPermissionsArgs {
    /// Path parameter: `resource`.
    pub resource: String,
}

// =============================================================================
// CLIENT FUNCTIONS (per-endpoint)
// =============================================================================

// -----------------------------------------------------------------------------
// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}:applyAdminConsents
// -----------------------------------------------------------------------------

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}:applyAdminConsents.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = healthcare_projects_locations_datasets_fhir_stores_apply_admin_consents_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn healthcare_projects_locations_datasets_fhir_stores_apply_admin_consents_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://healthcare.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/datasets/{{datasetsId}}/fhirStores/{{fhirStoresId}}:applyAdminConsents",
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}:applyConsents
// -----------------------------------------------------------------------------

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}:applyConsents.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = healthcare_projects_locations_datasets_fhir_stores_apply_consents_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn healthcare_projects_locations_datasets_fhir_stores_apply_consents_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://healthcare.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/datasets/{{datasetsId}}/fhirStores/{{fhirStoresId}}:applyConsents",
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}:bulkDelete
// -----------------------------------------------------------------------------

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}:bulkDelete.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = healthcare_projects_locations_datasets_fhir_stores_bulk_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn healthcare_projects_locations_datasets_fhir_stores_bulk_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://healthcare.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/datasets/{{datasetsId}}/fhirStores/{{fhirStoresId}}:bulkDelete",
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/fhirStores
// -----------------------------------------------------------------------------

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/fhirStores.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = healthcare_projects_locations_datasets_fhir_stores_create_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn healthcare_projects_locations_datasets_fhir_stores_create_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<FhirStore>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://healthcare.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/datasets/{{datasetsId}}/fhirStores",
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: FhirStore =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}:deidentify
// -----------------------------------------------------------------------------

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}:deidentify.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = healthcare_projects_locations_datasets_fhir_stores_deidentify_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn healthcare_projects_locations_datasets_fhir_stores_deidentify_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://healthcare.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/datasets/{{datasetsId}}/fhirStores/{{fhirStoresId}}:deidentify",
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// DELETE v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}
// -----------------------------------------------------------------------------

/// DELETE v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = healthcare_projects_locations_datasets_fhir_stores_delete_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn healthcare_projects_locations_datasets_fhir_stores_delete_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Empty>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://healthcare.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/datasets/{{datasetsId}}/fhirStores/{{fhirStoresId}}",
    );

    let mut builder = client
        .delete(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Empty =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}:explainDataAccess
// -----------------------------------------------------------------------------

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}:explainDataAccess.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = healthcare_projects_locations_datasets_fhir_stores_explain_data_access_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn healthcare_projects_locations_datasets_fhir_stores_explain_data_access_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ExplainDataAccessResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://healthcare.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/datasets/{{datasetsId}}/fhirStores/{{fhirStoresId}}:explainDataAccess",
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ExplainDataAccessResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}:export
// -----------------------------------------------------------------------------

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}:export.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = healthcare_projects_locations_datasets_fhir_stores_export_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn healthcare_projects_locations_datasets_fhir_stores_export_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://healthcare.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/datasets/{{datasetsId}}/fhirStores/{{fhirStoresId}}:export",
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}
// -----------------------------------------------------------------------------

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = healthcare_projects_locations_datasets_fhir_stores_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn healthcare_projects_locations_datasets_fhir_stores_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<FhirStore>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://healthcare.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/datasets/{{datasetsId}}/fhirStores/{{fhirStoresId}}",
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: FhirStore =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}:getFHIRStoreMetrics
// -----------------------------------------------------------------------------

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}:getFHIRStoreMetrics.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = healthcare_projects_locations_datasets_fhir_stores_get_fhir_store_metrics_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn healthcare_projects_locations_datasets_fhir_stores_get_fhir_store_metrics_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<FhirStoreMetrics>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://healthcare.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/datasets/{{datasetsId}}/fhirStores/{{fhirStoresId}}:getFHIRStoreMetrics",
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: FhirStoreMetrics =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}:getIamPolicy
// -----------------------------------------------------------------------------

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}:getIamPolicy.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = healthcare_projects_locations_datasets_fhir_stores_get_iam_policy_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn healthcare_projects_locations_datasets_fhir_stores_get_iam_policy_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://healthcare.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/datasets/{{datasetsId}}/fhirStores/{{fhirStoresId}}:getIamPolicy",
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Policy =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}:import
// -----------------------------------------------------------------------------

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}:import.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = healthcare_projects_locations_datasets_fhir_stores_import_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn healthcare_projects_locations_datasets_fhir_stores_import_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://healthcare.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/datasets/{{datasetsId}}/fhirStores/{{fhirStoresId}}:import",
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/fhirStores
// -----------------------------------------------------------------------------

/// GET v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/fhirStores.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = healthcare_projects_locations_datasets_fhir_stores_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn healthcare_projects_locations_datasets_fhir_stores_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ListFhirStoresResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://healthcare.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/datasets/{{datasetsId}}/fhirStores",
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ListFhirStoresResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// PATCH v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}
// -----------------------------------------------------------------------------

/// PATCH v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = healthcare_projects_locations_datasets_fhir_stores_patch_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn healthcare_projects_locations_datasets_fhir_stores_patch_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<FhirStore>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://healthcare.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/datasets/{{datasetsId}}/fhirStores/{{fhirStoresId}}",
    );

    let mut builder = client
        .patch(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: FhirStore =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}:rollback
// -----------------------------------------------------------------------------

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}:rollback.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = healthcare_projects_locations_datasets_fhir_stores_rollback_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn healthcare_projects_locations_datasets_fhir_stores_rollback_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://healthcare.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/datasets/{{datasetsId}}/fhirStores/{{fhirStoresId}}:rollback",
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}:setIamPolicy
// -----------------------------------------------------------------------------

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}:setIamPolicy.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = healthcare_projects_locations_datasets_fhir_stores_set_iam_policy_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn healthcare_projects_locations_datasets_fhir_stores_set_iam_policy_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Policy>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://healthcare.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/datasets/{{datasetsId}}/fhirStores/{{fhirStoresId}}:setIamPolicy",
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Policy =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}:testIamPermissions
// -----------------------------------------------------------------------------

/// POST v1/projects/{projectsId}/locations/{locationsId}/datasets/{datasetsId}/fhirStores/{fhirStoresId}:testIamPermissions.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = healthcare_projects_locations_datasets_fhir_stores_test_iam_permissions_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn healthcare_projects_locations_datasets_fhir_stores_test_iam_permissions_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<TestIamPermissionsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://healthcare.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/datasets/{{datasetsId}}/fhirStores/{{fhirStoresId}}:testIamPermissions",
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: TestIamPermissionsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}
