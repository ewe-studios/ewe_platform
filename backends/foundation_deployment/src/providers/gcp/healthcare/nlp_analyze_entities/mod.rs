//! Auto-generated API module for gcp/healthcare nlp:analyzeEntities.
//!
//! Generated by `cargo run --bin ewe_platform gen_api`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp_healthcare_nlp_analyze_entities `

#![cfg(feature = "gcp_healthcare_nlp_analyze_entities")]
#![allow(clippy::too_many_arguments, clippy::type_complexity)]
#![allow(
    clippy::missing_errors_doc,
    clippy::doc_markdown,
    clippy::useless_format
)]
#![allow(unused_imports)]

use foundation_core::valtron::{TaskIterator, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{ClientRequestBuilder, SimpleHttpClient};
use foundation_macros::JsonHash;
use serde::{Deserialize, Serialize};

use super::shared::ApiResponse;

// =============================================================================
// TYPE DECLARATIONS
// =============================================================================

/// `Entity` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Entity {
    /// entityId property.
    pub entity_id: Option<String>,
    /// preferredTerm property.
    pub preferred_term: Option<String>,
    /// vocabularyCodes property.
    pub vocabulary_codes: Option<Vec<String>>,
}

/// `AnalyzeEntitiesResponse` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct AnalyzeEntitiesResponse {
    /// entities property.
    pub entities: Option<Vec<Entity>>,
    /// entityMentions property.
    pub entity_mentions: Option<Vec<EntityMention>>,
    /// fhirBundle property.
    pub fhir_bundle: Option<String>,
    /// relationships property.
    pub relationships: Option<Vec<EntityMentionRelationship>>,
}

/// `EntityMention` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct EntityMention {
    /// certaintyAssessment property.
    pub certainty_assessment: Option<Feature>,
    /// confidence property.
    pub confidence: Option<f64>,
    /// linkedEntities property.
    pub linked_entities: Option<Vec<LinkedEntity>>,
    /// mentionId property.
    pub mention_id: Option<String>,
    /// subject property.
    pub subject: Option<Feature>,
    /// temporalAssessment property.
    pub temporal_assessment: Option<Feature>,
    /// text property.
    pub text: Option<TextSpan>,
    /// type property.
    pub r#type: Option<String>,
}

/// `EntityMentionRelationship` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct EntityMentionRelationship {
    /// confidence property.
    pub confidence: Option<f64>,
    /// objectId property.
    pub object_id: Option<String>,
    /// subjectId property.
    pub subject_id: Option<String>,
}

/// `Feature` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Feature {
    /// confidence property.
    pub confidence: Option<f64>,
    /// value property.
    pub value: Option<String>,
}

/// `LinkedEntity` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct LinkedEntity {
    /// entityId property.
    pub entity_id: Option<String>,
}

/// `TextSpan` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct TextSpan {
    /// beginOffset property.
    pub begin_offset: Option<i64>,
    /// content property.
    pub content: Option<String>,
}

// =============================================================================
// ARGS TYPES (per-endpoint)
// =============================================================================

/// Arguments for [`healthcare.projects.locations.services.nlp.analyzeEntities_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct HealthcareProjectsLocationsServicesNlpAnalyzeEntitiesArgs {
    /// Path parameter: `nlpService`.
    pub nlp_service: String,
}

// =============================================================================
// CLIENT FUNCTIONS (per-endpoint)
// =============================================================================

// -----------------------------------------------------------------------------
// POST v1/projects/{projectsId}/locations/{locationsId}/services/nlp:analyzeEntities
// -----------------------------------------------------------------------------

/// POST v1/projects/{projectsId}/locations/{locationsId}/services/nlp:analyzeEntities.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = healthcare_projects_locations_services_nlp_analyze_entities_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn healthcare_projects_locations_services_nlp_analyze_entities_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<AnalyzeEntitiesResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://healthcare.googleapis.com/v1/projects/{{projectsId}}/locations/{{locationsId}}/services/nlp:analyzeEntities",
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: AnalyzeEntitiesResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}
