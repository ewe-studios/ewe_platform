//! Auto-generated API module for gcp/agentregistry agents:search.
//!
//! Generated by `cargo run --bin ewe_platform gen_api`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp_agentregistry_agents_search `

#![cfg(feature = "gcp_agentregistry_agents_search")]
#![allow(clippy::too_many_arguments, clippy::type_complexity)]
#![allow(
    clippy::missing_errors_doc,
    clippy::doc_markdown,
    clippy::useless_format
)]
#![allow(unused_imports)]

use foundation_core::valtron::{TaskIterator, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{ClientRequestBuilder, SimpleHttpClient};
use foundation_macros::JsonHash;
use serde::{Deserialize, Serialize};

use super::shared::ApiResponse;

// =============================================================================
// TYPE DECLARATIONS
// =============================================================================

/// `Interface` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Interface {
    /// protocolBinding property.
    pub protocol_binding: Option<String>,
    /// url property.
    pub url: Option<String>,
}

/// `SearchAgentsResponse` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct SearchAgentsResponse {
    /// agents property.
    pub agents: Option<Vec<Agent>>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
}

/// `Card` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Card {
    /// content property.
    pub content: Option<serde_json::Value>,
    /// type property.
    pub r#type: Option<String>,
}

/// `Protocol` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Protocol {
    /// interfaces property.
    pub interfaces: Option<Vec<Interface>>,
    /// protocolVersion property.
    pub protocol_version: Option<String>,
    /// type property.
    pub r#type: Option<String>,
}

/// `Skill` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Skill {
    /// description property.
    pub description: Option<String>,
    /// examples property.
    pub examples: Option<Vec<String>>,
    /// id property.
    pub id: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// tags property.
    pub tags: Option<Vec<String>>,
}

/// `Agent` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Agent {
    /// agentId property.
    pub agent_id: Option<String>,
    /// attributes property.
    pub attributes: Option<serde_json::Value>,
    /// card property.
    pub card: Option<Card>,
    /// createTime property.
    pub create_time: Option<String>,
    /// description property.
    pub description: Option<String>,
    /// displayName property.
    pub display_name: Option<String>,
    /// location property.
    pub location: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// protocols property.
    pub protocols: Option<Vec<Protocol>>,
    /// skills property.
    pub skills: Option<Vec<Skill>>,
    /// uid property.
    pub uid: Option<String>,
    /// updateTime property.
    pub update_time: Option<String>,
    /// version property.
    pub version: Option<String>,
}

// =============================================================================
// ARGS TYPES (per-endpoint)
// =============================================================================

/// Arguments for [`agentregistry.projects.locations.agents.search_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct AgentregistryProjectsLocationsAgentsSearchArgs {
    /// Path parameter: `parent`.
    pub parent: String,
}

// =============================================================================
// CLIENT FUNCTIONS (per-endpoint)
// =============================================================================

// -----------------------------------------------------------------------------
// POST v1alpha/projects/{projectsId}/locations/{locationsId}/agents:search
// -----------------------------------------------------------------------------

/// POST v1alpha/projects/{projectsId}/locations/{locationsId}/agents:search.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = agentregistry_projects_locations_agents_search_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn agentregistry_projects_locations_agents_search_request<R, F>(
    client: &SimpleHttpClient<R>,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<SearchAgentsResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://agentregistry.googleapis.com/v1alpha/projects/{{projectsId}}/locations/{{locationsId}}/agents:search",
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: SearchAgentsResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}
