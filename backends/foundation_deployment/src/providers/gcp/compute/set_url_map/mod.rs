//! Auto-generated API module for gcp/compute setUrlMap.
//!
//! Generated by `cargo run --bin ewe_platform gen_api`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp_compute_set_url_map `

#![cfg(feature = "gcp_compute_set_url_map")]
#![allow(clippy::too_many_arguments, clippy::type_complexity)]
#![allow(
    clippy::missing_errors_doc,
    clippy::doc_markdown,
    clippy::useless_format
)]

use foundation_core::valtron::{execute, StreamIterator, TaskIterator, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{ClientRequestBuilder, SimpleHttpClient};
use foundation_macros::JsonHash;
use serde::{Deserialize, Serialize};

// Import shared types used by this module
use super::shared::Operation;

use super::shared::{ApiError, ApiPending, ApiResponse};

// =============================================================================
// TYPE DECLARATIONS
// =============================================================================

/// `SetCommonInstanceMetadataOperationMetadata` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct SetCommonInstanceMetadataOperationMetadata {
    /// clientOperationId property.
    pub client_operation_id: Option<String>,
    /// perLocationOperations property.
    pub per_location_operations: Option<serde_json::Value>,
}

/// `ErrorInfo` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ErrorInfo {
    /// domain property.
    pub domain: Option<String>,
    /// metadatas property.
    pub metadatas: Option<serde_json::Value>,
    /// reason property.
    pub reason: Option<String>,
}

/// `GetVersionOperationMetadata` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct GetVersionOperationMetadata {
    /// inlineSbomInfo property.
    pub inline_sbom_info: Option<GetVersionOperationMetadataSbomInfo>,
}

/// `LocalizedMessage` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct LocalizedMessage {
    /// locale property.
    pub locale: Option<String>,
    /// message property.
    pub message: Option<String>,
}

/// `QuotaExceededInfo` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct QuotaExceededInfo {
    /// dimensions property.
    pub dimensions: Option<serde_json::Value>,
    /// futureLimit property.
    pub future_limit: Option<f64>,
    /// limit property.
    pub limit: Option<f64>,
    /// limitName property.
    pub limit_name: Option<String>,
    /// metricName property.
    pub metric_name: Option<String>,
    /// rolloutStatus property.
    pub rollout_status: Option<String>,
}

/// `InstancesBulkInsertOperationMetadata` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct InstancesBulkInsertOperationMetadata {
    /// perLocationStatus property.
    pub per_location_status: Option<serde_json::Value>,
}

/// `Help` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct Help {
    /// links property.
    pub links: Option<Vec<HelpLink>>,
}

/// `GetVersionOperationMetadataSbomInfo` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct GetVersionOperationMetadataSbomInfo {
    /// currentComponentVersions property.
    pub current_component_versions: Option<serde_json::Value>,
    /// targetComponentVersions property.
    pub target_component_versions: Option<serde_json::Value>,
}

/// `HelpLink` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct HelpLink {
    /// description property.
    pub description: Option<String>,
    /// url property.
    pub url: Option<String>,
}

// =============================================================================
// ARGS TYPES (per-endpoint)
// =============================================================================

/// Arguments for [`compute.regionTargetHttpProxies.setUrlMap_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionTargetHttpProxiesSetUrlMapArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `targetHttpProxy`.
    pub target_http_proxy: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
}

/// Arguments for [`compute.regionTargetHttpsProxies.setUrlMap_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeRegionTargetHttpsProxiesSetUrlMapArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `region`.
    pub region: String,
    /// Path parameter: `targetHttpsProxy`.
    pub target_https_proxy: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
}

/// Arguments for [`compute.targetHttpProxies.setUrlMap_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeTargetHttpProxiesSetUrlMapArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `targetHttpProxy`.
    pub target_http_proxy: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
}

/// Arguments for [`compute.targetHttpsProxies.setUrlMap_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeTargetHttpsProxiesSetUrlMapArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `targetHttpsProxy`.
    pub target_https_proxy: String,
    /// Query parameter: `requestId`.
    pub request_id: Option<String>,
}

// =============================================================================
// CLIENT FUNCTIONS (per-endpoint)
// =============================================================================

// -----------------------------------------------------------------------------
// POST projects/{project}/regions/{region}/targetHttpProxies/{targetHttpProxy}/setUrlMap
// -----------------------------------------------------------------------------

/// POST projects/{project}/regions/{region}/targetHttpProxies/{targetHttpProxy}/setUrlMap.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_target_http_proxies_set_url_map_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_target_http_proxies_set_url_map_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionTargetHttpProxiesSetUrlMapArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/targetHttpProxies/{}/setUrlMap",
        args.project,
        args.region,
        args.target_http_proxy,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/regions/{region}/targetHttpsProxies/{targetHttpsProxy}/setUrlMap
// -----------------------------------------------------------------------------

/// POST projects/{project}/regions/{region}/targetHttpsProxies/{targetHttpsProxy}/setUrlMap.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_region_target_https_proxies_set_url_map_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_region_target_https_proxies_set_url_map_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeRegionTargetHttpsProxiesSetUrlMapArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/regions/{}/targetHttpsProxies/{}/setUrlMap",
        args.project,
        args.region,
        args.target_https_proxy,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/targetHttpProxies/{targetHttpProxy}/setUrlMap
// -----------------------------------------------------------------------------

/// POST projects/{project}/targetHttpProxies/{targetHttpProxy}/setUrlMap.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_target_http_proxies_set_url_map_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_target_http_proxies_set_url_map_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeTargetHttpProxiesSetUrlMapArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/targetHttpProxies/{}/setUrlMap",
        args.project, args.target_http_proxy,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// POST projects/{project}/targetHttpsProxies/{targetHttpsProxy}/setUrlMap
// -----------------------------------------------------------------------------

/// POST projects/{project}/targetHttpsProxies/{targetHttpsProxy}/setUrlMap.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_target_https_proxies_set_url_map_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_target_https_proxies_set_url_map_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeTargetHttpsProxiesSetUrlMapArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<Operation>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/targetHttpsProxies/{}/setUrlMap",
        args.project, args.target_https_proxy,
    );

    let mut builder = client
        .post(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: Operation =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}
