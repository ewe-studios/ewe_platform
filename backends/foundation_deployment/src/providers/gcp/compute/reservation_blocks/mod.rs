//! Auto-generated API module for gcp/compute reservationBlocks.
//!
//! Generated by `cargo run --bin ewe_platform gen_api`.
//! DO NOT EDIT MANUALLY.
//!
//! Feature flag: `gcp_compute_reservation_blocks `

#![cfg(feature = "gcp_compute_reservation_blocks")]
#![allow(clippy::too_many_arguments, clippy::type_complexity)]
#![allow(
    clippy::missing_errors_doc,
    clippy::doc_markdown,
    clippy::useless_format
)]
#![allow(unused_imports)]

use foundation_core::valtron::{TaskIterator, TaskIteratorExt};
use foundation_core::wire::simple_http::client::{ClientRequestBuilder, SimpleHttpClient};
use foundation_macros::JsonHash;
use serde::{Deserialize, Serialize};

use super::shared::ApiResponse;

// =============================================================================
// TYPE DECLARATIONS
// =============================================================================

/// `ReservationBlockPhysicalTopologyInstance` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ReservationBlockPhysicalTopologyInstance {
    /// instanceId property.
    pub instance_id: Option<String>,
    /// physicalHostTopology property.
    pub physical_host_topology:
        Option<ReservationBlockPhysicalTopologyInstancePhysicalHostTopology>,
    /// projectId property.
    pub project_id: Option<String>,
}

/// `ReservationBlock` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ReservationBlock {
    /// count property.
    pub count: Option<i64>,
    /// creationTimestamp property.
    pub creation_timestamp: Option<String>,
    /// healthInfo property.
    pub health_info: Option<ReservationBlockHealthInfo>,
    /// id property.
    pub id: Option<String>,
    /// inUseCount property.
    pub in_use_count: Option<i64>,
    /// inUseHostCount property.
    pub in_use_host_count: Option<i64>,
    /// kind property.
    pub kind: Option<String>,
    /// name property.
    pub name: Option<String>,
    /// physicalTopology property.
    pub physical_topology: Option<ReservationBlockPhysicalTopology>,
    /// reservationMaintenance property.
    pub reservation_maintenance: Option<GroupMaintenanceInfo>,
    /// reservationSubBlockCount property.
    pub reservation_sub_block_count: Option<i64>,
    /// reservationSubBlockInUseCount property.
    pub reservation_sub_block_in_use_count: Option<i64>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// selfLinkWithId property.
    pub self_link_with_id: Option<String>,
    /// status property.
    pub status: Option<String>,
    /// zone property.
    pub zone: Option<String>,
}

/// `UpcomingMaintenance` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct UpcomingMaintenance {
    /// canReschedule property.
    pub can_reschedule: Option<bool>,
    /// latestWindowStartTime property.
    pub latest_window_start_time: Option<String>,
    /// maintenanceOnShutdown property.
    pub maintenance_on_shutdown: Option<bool>,
    /// maintenanceReasons property.
    pub maintenance_reasons: Option<Vec<String>>,
    /// maintenanceStatus property.
    pub maintenance_status: Option<String>,
    /// type property.
    pub r#type: Option<String>,
    /// windowEndTime property.
    pub window_end_time: Option<String>,
    /// windowStartTime property.
    pub window_start_time: Option<String>,
}

/// `GroupMaintenanceInfo` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct GroupMaintenanceInfo {
    /// instanceMaintenanceOngoingCount property.
    pub instance_maintenance_ongoing_count: Option<i64>,
    /// instanceMaintenancePendingCount property.
    pub instance_maintenance_pending_count: Option<i64>,
    /// maintenanceOngoingCount property.
    pub maintenance_ongoing_count: Option<i64>,
    /// maintenancePendingCount property.
    pub maintenance_pending_count: Option<i64>,
    /// schedulingType property.
    pub scheduling_type: Option<String>,
    /// subblockInfraMaintenanceOngoingCount property.
    pub subblock_infra_maintenance_ongoing_count: Option<i64>,
    /// subblockInfraMaintenancePendingCount property.
    pub subblock_infra_maintenance_pending_count: Option<i64>,
    /// upcomingGroupMaintenance property.
    pub upcoming_group_maintenance: Option<UpcomingMaintenance>,
}

/// `ReservationBlocksListResponse` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ReservationBlocksListResponse {
    /// id property.
    pub id: Option<String>,
    /// items property.
    pub items: Option<Vec<ReservationBlock>>,
    /// kind property.
    pub kind: Option<String>,
    /// nextPageToken property.
    pub next_page_token: Option<String>,
    /// selfLink property.
    pub self_link: Option<String>,
    /// warning property.
    pub warning: Option<std::collections::HashMap<String, serde_json::Value>>,
}

/// `ReservationBlockHealthInfo` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ReservationBlockHealthInfo {
    /// degradedSubBlockCount property.
    pub degraded_sub_block_count: Option<i64>,
    /// healthStatus property.
    pub health_status: Option<String>,
    /// healthySubBlockCount property.
    pub healthy_sub_block_count: Option<i64>,
}

/// `ReservationBlockPhysicalTopology` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ReservationBlockPhysicalTopology {
    /// block property.
    pub block: Option<String>,
    /// cluster property.
    pub cluster: Option<String>,
    /// instances property.
    pub instances: Option<Vec<ReservationBlockPhysicalTopologyInstance>>,
}

/// `ReservationBlocksGetResponse` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ReservationBlocksGetResponse {
    /// resource property.
    pub resource: Option<ReservationBlock>,
}

/// `ReservationBlockPhysicalTopologyInstancePhysicalHostTopology` type.
#[derive(Debug, Clone, Serialize, Deserialize, JsonHash)]
pub struct ReservationBlockPhysicalTopologyInstancePhysicalHostTopology {
    /// host property.
    pub host: Option<String>,
    /// subBlock property.
    pub sub_block: Option<String>,
}

// =============================================================================
// ARGS TYPES (per-endpoint)
// =============================================================================

/// Arguments for [`compute.reservationBlocks.get_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeReservationBlocksGetArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Path parameter: `reservation`.
    pub reservation: String,
    /// Path parameter: `reservationBlock`.
    pub reservation_block: String,
    /// Query parameter: `view`.
    pub view: Option<String>,
}

/// Arguments for [`compute.reservationBlocks.list_builder`].
#[derive(Debug, Clone, Serialize, JsonHash)]
pub struct ComputeReservationBlocksListArgs {
    /// Path parameter: `project`.
    pub project: String,
    /// Path parameter: `zone`.
    pub zone: String,
    /// Path parameter: `reservation`.
    pub reservation: String,
    /// Query parameter: `filter`.
    pub filter: Option<String>,
    /// Query parameter: `maxResults`.
    pub max_results: Option<String>,
    /// Query parameter: `orderBy`.
    pub order_by: Option<String>,
    /// Query parameter: `pageToken`.
    pub page_token: Option<String>,
    /// Query parameter: `returnPartialSuccess`.
    pub return_partial_success: Option<String>,
}

// =============================================================================
// CLIENT FUNCTIONS (per-endpoint)
// =============================================================================

// -----------------------------------------------------------------------------
// GET projects/{project}/zones/{zone}/reservations/{reservation}/reservationBlocks/{reservationBlock}
// -----------------------------------------------------------------------------

/// GET projects/{project}/zones/{zone}/reservations/{reservation}/reservationBlocks/{reservationBlock}.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_reservation_blocks_get_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_reservation_blocks_get_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeReservationBlocksGetArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ReservationBlocksGetResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/reservations/{}/reservationBlocks/{}",
        args.project,
        args.zone,
        args.reservation,
        args.reservation_block,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ReservationBlocksGetResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}

// -----------------------------------------------------------------------------
// GET projects/{project}/zones/{zone}/reservations/{reservation}/reservationBlocks
// -----------------------------------------------------------------------------

/// GET projects/{project}/zones/{zone}/reservations/{reservation}/reservationBlocks.
///
/// Takes client and args, builds the request, optionally applies modifications,
/// and returns a `TaskIterator` for execution.
///
/// # Arguments
///
/// * `client` - HTTP client for making the request
/// * `args` - Request arguments (path params, query params, body)
/// * `builder_mod` - Optional closure to modify the request builder (e.g., add headers)
///
/// # Example
///
/// ```ignore
/// let task = compute_reservation_blocks_list_request(&client, &args, Some(|b| {
///     b.header("X-Custom-Header", "value")
/// }))?;
/// ```
#[inline]
pub fn compute_reservation_blocks_list_request<R, F>(
    client: &SimpleHttpClient<R>,
    args: &ComputeReservationBlocksListArgs,
    builder_mod: Option<F>,
) -> Result<
    impl TaskIterator<
            Ready = Result<ApiResponse<ReservationBlocksListResponse>, super::shared::ApiError>,
            Pending = super::shared::ApiPending,
            Spawner = super::shared::BoxedSendExecutionAction,
        > + Send
        + 'static,
    super::shared::ApiError,
>
where
    R: foundation_core::wire::simple_http::client::DnsResolver + Clone + Default + 'static,
    F: FnOnce(&mut ClientRequestBuilder<R>),
{
    let endpoint_url = format!(
        "https://compute.googleapis.com/compute/v1/projects/{}/zones/{}/reservations/{}/reservationBlocks",
        args.project,
        args.zone,
        args.reservation,
    );

    let mut builder = client
        .get(&endpoint_url)
        .map_err(|e| super::shared::ApiError::RequestBuildFailed(e.to_string()))?;

    if let Some(f) = builder_mod {
        f(&mut builder);
    }

    Ok(builder
        .build_send_request()
        .map_err(|e: foundation_core::wire::simple_http::HttpClientError| {
            super::shared::ApiError::RequestBuildFailed(e.to_string())
        })?
        .map_ready(|intro| match intro {
            super::shared::RequestIntro::Success {
                stream,
                intro,
                headers,
                ..
            } => {
                let status: usize = intro.0.into();
                if status < 200 || status >= 300 {
                    return Err(super::shared::ApiError::HttpStatus {
                        code: status as u16,
                        headers: headers.clone(),
                        body: None,
                    });
                }
                let body =
                    foundation_core::wire::simple_http::client::body_reader::collect_string(stream);
                let parsed: ReservationBlocksListResponse =
                    serde_json::from_str(&body).map_err(|e: serde_json::Error| {
                        super::shared::ApiError::ParseFailed(e.to_string())
                    })?;
                Ok(ApiResponse {
                    status: status as u16,
                    headers: headers.clone(),
                    body: parsed,
                })
            }
            super::shared::RequestIntro::Failed(e) => {
                Err(super::shared::ApiError::RequestSendFailed(e.to_string()))
            }
        })
        .map_pending(|_| super::shared::ApiPending::Sending))
}
